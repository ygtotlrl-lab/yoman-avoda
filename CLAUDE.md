# יומן עבודה — CLAUDE.md

עודכן לאחרונה: סבב 69 · 2026-08-28

<!-- SHARED:start id="rules-table" -->
## ⭐ טבלת התשתית — מקור האמת היחיד ליכולת

> ⛔ **יכולת שקיימת בטבלת התשתית קיימת בארבע האפליקציות** — ⛔ אלא אם היא
> מוגדרת ומאושרת כחריגה מנומקת בעמודת ההערות.

### ⛔ שרשרת היכולת החדשה
⛔ **יכולת תשתיתית שנכתבה או שזוהתה נכנסת בשרשרת מלאה:**
1. **שורה בטבלה**, בקטגוריה הנכונה.
2. **`probe` שמאמת ערך** — ⛔ ולא קיום שם.
3. **שער עם מוטציה ומוטציית-נגד**, ⛔ **מחווט ל-`APP.gates` שב-`check-js`**.
4. **חריגה מנומקת בעמודת ההערות**, אם אינה בארבעתן.

⚠️ **שלב חסר — היכולת אינה קיימת מבחינת המנגנון.** ⭐ הנימוק נמדד: מסך שינוי סיסמה סומן
✅ והיה **קוד מת**, מפני ש-probe הסתפק בקיום השם.

### ⛔ מספור רציף ומבני, וסריקת כפילויות
⛔ **בכל עדכון — מספור מחדש רציף**, ⚠️ ושורה חדשה נכנסת **בקטגוריה הנכונה** ולא בסוף:
הקטגוריה היא מה שמאפשר למצוא שורה בלי לקרוא את כולן. ⛔ **ובכל עדכון — סריקת כפילויות.**
⚠️ נמדד: שלוש כפילויות בשעה אחת.

### ⛔ תקן עמודת ההערות
⛔ ההערה מחזיקה **אחד משלושה בלבד**: נימוק לחריגה · מה נדרש לשורה שאינה מטופלת · או מה
יימדד. ⛔ **שורה שסימונה ✅ — הערה ריקה.** ⛔⛔ אין בטבלה היסטוריה ואין נימוקי כללים; התיאור
יושב בעמודת «מה זה». **הסימונים:** ✅ תקין · ❌ לא מטופל · ⭕ חסר-מנומק · 🔲 טרם נמדד.

| # | שם | מה זה | יומן | הנהלה | שכר | גיוס | הערה |
|---|---|---|---|---|---|---|---|
| | **א · תיעוד** | | | | | | |
| 1 | בלוקי SHARED — טקסט | תוכן זהה בית-לבית, נאכף ב-sha256 | ✅ | ✅ | ✅ | ✅ |  |
| 2 | בלוקי SHARED — מבנה | סדר פנימי אחיד בארבעתם | 🔲 | 🔲 | 🔲 | 🔲 | טרם נמדד |
| 3 | בלוקי הכללים — ארבעה | `rules-table` · `enforce` · `writing` · `session` | ✅ | ✅ | ✅ | ✅ |  |
| 4 | פרק כלל — טקסט | ניסוח הכלל זהה בארבעתם | ✅ | ✅ | ✅ | ✅ |  |
| 5 | פרק כלל — מבנה | כותרת · מה נאכף · נימוק · שער | 🔲 | 🔲 | 🔲 | 🔲 | טרם נמדד |
| 6 | תקציב כולל | סך שורות `CLAUDE.md` | ✅ | ✅ | ✅ | ✅ |  |
| 7 | `README` — תוכן | התקנה והפעלה בלבד | ✅ | ✅ | ✅ | ✅ |  |
| 8 | `CONTEXT` — תוכן | לקוח וצורך בלבד | ✅ | ✅ | ✅ | ✅ |  |
| 9 | `android/README` — תוכן | בנייה וחתימה בלבד | ✅ | ✅ | ✅ | ✅ |  |
| 10 | `CLAUDE.md` משותף — תוכן | כללים והטבלה בלבד | ✅ | ✅ | ✅ | ✅ |  |
| 11 | `CLAUDE.md` משותף — כמות | תקרת 300 שורות | ✅ | ✅ | ✅ | ✅ |  |
| 12 | `CLAUDE.md` פרטי — תוכן | מסכים ולוגיקה של האפליקציה בלבד | ✅ | ✅ | ✅ | ✅ |  |
| 13 | `CLAUDE.md` פרטי — כמות | תקרת 300 שורות | ✅ | ✅ | ✅ | ✅ |  |
| 14 | `README` — כמות | תקרת 50 שורות | ✅ | ✅ | ✅ | ✅ |  |
| 15 | `CONTEXT` — כמות | תקרת 100 שורות | ✅ | ✅ | ✅ | ✅ |  |
| 16 | `android/README` — כמות | תקרת 150 שורות | ✅ | ✅ | ✅ | ✅ |  |
| | **ב · אכיפה** | | | | | | |
| 17 | בודקים — קיום | שבעת ה-`check-*` המשותפים | ✅ | ✅ | ✅ | ✅ |  |
| 18 | בודקים — תוכן | תקן שמסדיר מה כל בודק בודק | 🔲 | 🔲 | 🔲 | 🔲 | טרם נמדד |
| 19 | מבחנים — בסיס משותף | הסט המוכרז ב-`test_filesets` | ✅ | ✅ | ✅ | ✅ |  |
| 20 | מבחנים — חריגות | כל מבחן פרטי נושא נימוק כתוב | ✅ | ✅ | ✅ | ✅ |  |
| 21 | מבחנים — תוכן | טענות · מוטציה · באנר | 🔲 | 🔲 | 🔲 | 🔲 | טרם נמדד |
| 22 | מוטציות — כיסוי | מוטציה ומוטציית-נגד לכל מבחן | ❌ | ❌ | ❌ | ❌ | מוטציית-נגד ב-30/43 · 28/47 · 27/39 · 26/39 |
| 23 | מוטציות — תוכן | תקן: מה המוטציה שוברת | 🔲 | 🔲 | 🔲 | 🔲 | טרם נמדד |
| 24 | כל שורה נושאת `probe` | או שורת נימוק מדוע אינה נאכפת | ✅ | ✅ | ✅ | ✅ |  |
| 25 | שורות פער | תג מדיד/לא-מדיד, וטריגר לכל שורה | ✅ | ✅ | ✅ | ✅ |  |
| 26 | חתימות sha256 | המודולים · `ShellActivity` · ליבת `sw.js` | ✅ | ✅ | ✅ | ✅ |  |
| | **ג · אחד עשר המודולים המשותפים — קוד זהה בית-לבית** | | | | | | |
| 27 | `pend` — ממתין לסנכרון | סימון ⏳, ומונה שמתרענן תקופתית | ✅ | ✅ | ✅ | ✅ |  |
| 28 | `bk` — גיבוי יומי ויומן | גיבוי יומי אוטומטי מנקודת העלייה | ✅ | ✅ | ✅ | ✅ |  |
| 29 | `hw` — חלון חם | צמצום העותק המקומי, ושחזור מהענן | ✅ | ✅ | ✅ | ✅ |  |
| 30 | `merge` — ליבת המיזוג | הכרעה לפי חותמת, ורשומה ⏳ מנצחת | ✅ | ✅ | ✅ | ✅ |  |
| 31 | `ids` — מזהי רשומות | מזהה שנוצר במכשיר לפני שראה שרת | ✅ | ✅ | ✅ | ✅ |  |
| 32 | `devid` — מזהה מכשיר | מזהה יציב לרישום ביומן | ✅ | ✅ | ✅ | ✅ |  |
| 33 | `rty` — ניסיון חוזר | נסיגה אקספוננציאלית והגנת ריצה כפולה | ✅ | ✅ | ✅ | ✅ |  |
| 34 | `pull` — מנגנון המשיכה | משיכה ← מיזוג ← דחיפה, פר-קטגוריה | ✅ | ✅ | ✅ | ✅ |  |
| 35 | `lock` — נעילת חוסר-פעילות | נעילה אחרי שהות ללא פעילות | ⭕ | ✅ | ✅ | ✅ | אין כניסה ביומן |
| 36 | `sess` — מודל הסשן | המשתמש המחובר בזיכרון בלבד | ⭕ | ✅ | ✅ | ✅ | אין כניסה ביומן |
| 37 | `status` — אזור מצב | «☁️ סנכרון» ו«מידע טכני ›» בסוף ההגדרות | ✅ | ✅ | ✅ | ✅ |  |
| | **ד · דפוסי מימוש** | | | | | | |
| 38 | העברת מזהה ל-DOM | `data-id`, ⛔ ולא הטבעה ב-`onclick` | ✅ | ✅ | ✅ | ✅ |  |
| 39 | מודאלים | מיכל אחד ו-`openModal(title, body, foot)` | ✅ | ❌ | ✅ | ✅ | בהנהלה 26 אתרי `overlay` דינמיים |
| 40 | טיפול באירועים | דלגציה ממאזין אחד | ❌ | ❌ | ❌ | ✅ | `onclick` מוטבע: 29 · 203 · 29 · 7 |
| 41 | אחסון מקומי | כתיבה אך ורק דרך `lsSet`/`lsSetArray` | ✅ | ✅ | ✅ | ✅ |  |
| 42 | הודעות שגיאה | דפוס אחד להודעה שנכשלת ברעש | 🔲 | 🔲 | 🔲 | 🔲 | שני דפוסים — טרם הוכרע |
| 43 | מצב טעינה | חיווי וכפתור מושבת בפעולה שדורשת רשת | ✅ | ✅ | ✅ | ✅ |  |
| 44 | פסק זמן אחיד לקריאות רשת | תקרת זמן על כל קריאה לענן | ✅ | ✅ | ✅ | ✅ |  |
| 45 | מאזיני מצב רשת | `online`/`offline` מחווטים | ✅ | ✅ | ✅ | ✅ |  |
| | **ה · תקן כתיבה** | | | | | | |
| 46 | שפת ההערות | עברית; מונחים ומזהים באנגלית | ✅ | ✅ | ✅ | ✅ |  |
| 47 | מפרידי `═` ברוחב 74 | כותרת בלוק ב-`index.html` | ✅ | ✅ | ✅ | ✅ |  |
| 48 | מפרידים ב-`tools` | באנר `──` ברוחב 78 | ✅ | ✅ | ✅ | ✅ |  |
| 49 | הערות עם מספר סבב | תבנית «⛔ אל תעשה X (סבב N) — הסיבה» | ✅ | ✅ | ✅ | ✅ |  |
| 50 | מחלקות CSS מתות | מחלקה שאינה מוחלת לעולם | ✅ | ✅ | ✅ | ✅ |  |
| 51 | תוכן ההערות — אחידות | לא רק שפה ומפרידים | 🔲 | 🔲 | 🔲 | 🔲 | טרם נמדד |
| 52 | הערה אינה מפנה לקובץ | אין שם קובץ, בלוק או פרק בהערה | ✅ | ✅ | ✅ | ✅ |  |
| | **ו · אנדרואיד** | | | | | | |
| 53 | מעטפת `ShellActivity` | ליבת המעטפת, זהה בית-לבית | ✅ | ✅ | ✅ | ✅ |  |
| 54 | מספר קבצים ב-`android/` | הסט הקנוני | ⭕ | ✅ | ✅ | ✅ | 21 ביומן — `file_paths` לגשר השיתוף |
| 55 | שכבת האייקונים | כמות · ממדים · תוכן · רקע | ✅ | ✅ | ✅ | ✅ |  |
| 56 | הכפלה מוקדמת באלפא | הקטנת נכס עם שקיפות חלקית | ✅ | ⭕ | ⭕ | ⭕ | `fgDriftMax` מוצהר: 24 · 43 · 54 מול הסף 8 |
| 57 | `manifest.json` | שלושה אייקונים, `maskable` נפרד | ✅ | ✅ | ✅ | ✅ |  |
| 58 | גשר שיתוף | `createChooser`, ⛔ בלי `NEW_TASK` | ✅ | ⭕ | ⭕ | ⭕ | רק יומן מייצאת קובץ החוצה |
| 59 | `workflows` | בנייה וניקוי ענפים ממוזגים | ✅ | ✅ | ✅ | ✅ |  |
| 60 | `sign-apk.sh` | חתימה מול `EXPECTED_SHA256` | ✅ | ✅ | ✅ | ✅ |  |
| 61 | `versionCode` | מקודם בכל שינוי תחת `android/` | ✅ | ✅ | ✅ | ✅ |  |
| 62 | מפתח חתימה קבוע | ⛔ לעולם לא keystore חדש | ✅ | ✅ | ✅ | ✅ |  |
| 63 | סוג המעטפת | `WebView` מקורי — ⛔ לעולם לא TWA | ✅ | ✅ | ✅ | ✅ |  |
| | **ז · נתונים** | | | | | | |
| 64 | עריכת נתונים אופליין | כתיבה מקומית-תחילה, בלי רשת | ✅ | ✅ | ✅ | ✅ |  |
| 65 | נתונים בטבלאות מובנות | הנתונים יושבים בטבלאות ולא כערכי `kv` | ✅ | ❌ | ✅ | ✅ | בהנהלה הנתונים עדיין ב-`kv` |
| 66 | מקור הקריאה — טבלאות בלבד | כיבוי דגל ה-`kv`, ואז מחיקת המפתחות | ❌ | ❌ | ✅ | ✅ | ביומן ובהנהלה ה-`kv` עדיין נקרא |
| 67 | מקור אמת יחיד לסכימה | קובץ מיגרציה אחד, ⛔ בלי עותק מוטבע | ✅ | ✅ | ✅ | ✅ |  |
| 68 | קובץ התקנה מלא | התקנה טרייה מקובץ אחד, אידמפוטנטית | ✅ | ✅ | ✅ | ✅ |  |
| 69 | מיגרציות | קובץ חדש, רץ קדימה, אידמפוטנטי | ✅ | ✅ | ✅ | ✅ |  |
| 70 | הרשאות במסד | `select`+`insert`+`update` בלבד | ✅ | ✅ | ✅ | ✅ |  |
| 71 | דפוס `upsert` | `onConflict` אחיד — `client_id` או `id` | 🔲 | 🔲 | 🔲 | 🔲 | טרם נמדד |
| 72 | גיבוי במסד | `pg_cron` יומי — 7 עותקים או 30 יום | ✅ | ✅ | ✅ | ✅ |  |
| 73 | `tombstones` | מחיקה היא סימון + חותמת, וגריעה לפי גיל | ✅ | ❌ | ❌ | ❌ | גריעה לפי גיל ביומן בלבד; ⛔ טרם הוכרע בשלוש |
| 74 | מחיקה רכה בלבד | ⛔ אין `DELETE` פיזי בשום מסלול | ✅ | ✅ | ✅ | ✅ |  |
| 75 | אימות פינוי מול הענן | פינוי רק מול ראיה שהרשומה בענן | ✅ | ✅ | ⭕ | ⭕ | schar אינה מפנה; ב-gius העֵד כבר נגזר מהענן |
| | **ח · שכבת קלט ואבטחה** | | | | | | |
| 76 | שדה מספרי | `type=text` + `inputmode` + ולידציה | ✅ | ✅ | ✅ | ✅ |  |
| 77 | שדות סיסמה | `type=password` + `autocomplete` + `maxlength` | ⭕ | ✅ | ✅ | ✅ | אין כניסה ביומן |
| 78 | נגישות | כל שדה מקושר ל-`label` או `aria-label` | ✅ | ✅ | ✅ | ✅ |  |
| 79 | שכבת כניסה | כניסה · החלפת משתמש · שינוי סיסמה | ⭕ | ✅ | ⭕ | ✅ | אין כניסה ביומן; בשכר רק המנהל משתמש |
| 80 | נתיב עדכון חלקי למראת המשתמשים | עדכון שורה אחת בלי משיכה מלאה | ⭕ | ✅ | ❌ | ✅ | אין כניסה ביומן |
| 81 | מנוע תאריך עברי | מנוע אחד לכל צרכני התאריך | ❌ | ❌ | ⭕ | ⭕ | שני מנועים; ⛔ טרם הוכרע איזה |
| 82 | רישום כשלי כתיבה | `catch` סביב כתיבה אינו ריק | ❌ | ✅ | ❌ | ❌ | `ysWriteFail` בהנהלה בלבד |
| 83 | `PBKDF2` — פרמטרים | 100,000 סיבובים, קידומת הקשר | ⭕ | ✅ | ✅ | ✅ | אין כניסה ביומן |
| 84 | מלח אקראי לכל משתמש | `pass_salt` פר-משתמש | ⭕ | ✅ | ✅ | ✅ | אין כניסה ביומן |
| 85 | כניסה אופליין | אימות מול הטביעה, מרובת-משתמשים | ⭕ | ✅ | ✅ | ✅ | אין כניסה ביומן |
| 86 | סיסמאות בענן | עמודה גלויה שנכתבת ואינה נקראת | ⭕ | ⭕ | ⭕ | ⭕ | דגל נתיב-חזרה; ההסרה היא פעולת מנהל |
| 87 | ⛔ אין סוד בקובץ שנדחף | לא סיסמה, לא טוקן, ולא ערך «לדוגמה» | ✅ | ✅ | ✅ | ✅ |  |
| | **ט · ניקוי שאריות** | | | | | | |
| 88 | דגלי מעבר | `LEGACY_WRITE` · `BOOTSTRAP` | ❌ | ❌ | ❌ | ❌ | דלוקים: 3 · 2 · 1 · 1 |
| 89 | מבחני מעבר שהסתיימו | ⏳ בכותרת, וטריגר להסרה | ✅ | ✅ | ✅ | ✅ |  |
| 90 | הערות מיושנות | הערה שמתארת מנגנון שאינו קיים | ✅ | ✅ | ✅ | ✅ |  |
| 91 | ארכיונים שרידיים | טבלה או מפתח שאיש אינו קורא | ❌ | ❌ | ✅ | ✅ | `tb_archive` ביומן; מפתחות `kv` בהנהלה |
| 92 | מיגרציות שלא הורצו | נכתבו ונשארו | ❌ | ❌ | ❌ | ❌ | ההרצה היא פעולת מנהל |
| 93 | קבצים בלי קוראים | קובץ שאין לו אף קורא בארבעתן | ❌ | ❌ | ❌ | ❌ | אין שער; נסרק ידנית בכל סבב שנוגע |
| 94 | מפתחות `kv` בלי קוראים | מפתח שנשאר במסד אחרי המעבר | ❌ | ❌ | ✅ | ✅ | הקוראים זמניים — סבב `kv` ד |
| 95 | שורות ברשימת-היתר | חריגה רשומה שאינה קיימת בפועל | ❌ | ❌ | ❌ | ❌ | רשימת-היתר הגיבויים אינה נראית מהריפו |
| 96 | ענפים שלא מוזגו | ענף מרוחק שאינו ב-`main` | ✅ | ✅ | ✅ | ✅ |  |
| | **י · אחר** | | | | | | |
| 97 | עדכון אוטומטי מ-GitHub | משיכת גרסה חדשה למכשיר מותקן | ✅ | ⭕ | ⭕ | ⭕ | קיים ביומן בלבד; ⛔ טרם הוכרע אם נדרש בשלוש |
| 98 | באנר עדכון `sw` | «גרסה חדשה זמינה», ובדיקה תקופתית | ✅ | ✅ | ✅ | ✅ |  |
| 99 | אסטרטגיית `localStorage` | `tier1`/`tier2`, אופק הפינוי ופינוי אוטומטי | ✅ | ✅ | ❌ | ✅ | `tier` ריקים בשכר — תנועות הן כספים |
| 100 | שמות ענפים | `claude/<נושא>-<סיומת>` | ✅ | ✅ | ✅ | ✅ |  |
| 101 | ליבת `sw.js` | קו הבסיס ההתנהגותי, ומטמון CDN מראש | ✅ | ✅ | ✅ | ✅ |  |
| 102 | `APP` בקובצי השערים | הבלוק היחיד שנבדל בין הריפו | ✅ | ✅ | ✅ | ✅ |  |
<!-- SHARED:end -->
<!-- SHARED:start id="rules-enforce" -->
## ⭐ כללי האכיפה

> ⛔ **לכל שורה בטבלה — שער שאוכף אותה, ⛔ או שורת נימוק מפורשת מדוע אינה
> ניתנת לאכיפה מכנית.** ⚠️ **אין אפשרות שלישית.**

⚠️ שורה שאיש אינו מודד ממשיכה לתאר עולם שהשתנה, ⛔ ואז נקראת כעדות.

### ⛔ `probe` מאמת **ערך** — ⛔ ולא קיום
⛔ `probe` שמסתפק ב«השם קיים בקובץ» מאשר קוד מת. ⚠️ הוא מאמת **ערך**: שהפונקציה נקראת
ממקום חי, שהשדה קיים ב-DOM, שהמספר הוא המספר.

### ⛔ תקן תוכן המבחן
1. **באנר** בראש הקובץ: **מה נאכף ולמה זה יכול להישבר** — ⛔ ולא המדידה
ההיסטורית שהובילה להכרעה.
2. **טענות** על התנהגות, ⛔ לא על נוכחות מחרוזת.
3. **מוטציה** שמפילה את המבחן, ו**מוטציית-נגד** שאינה מפילה אותו.

⛔ **מבחן שאינו ניתן למוטציה נושא נימוק כתוב בגופו**, ⛔ ואינו מדלג בשתיקה. ⚠️ מבחן בלי
מוטציה נראה כרשת ביטחון ופועל כאישור. ⛔ **המוטציות אינן נכתבות לעץ** — עותק זמני, והשער
האמיתי רץ עליו.

### ⛔ תקן תוכן המוטציה
⛔ המוטציה שוברת את **המנגנון** ⛔ ולא את הצורה: היפוך תנאי, החלפת `break` ב-`continue`,
הסרת בדיקה — ⛔ ולא שינוי רווח או שם. ⛔ **ומוטציית-נגד היא שינוי אמיתי שאסור לו להפיל** —
היא מה שמבחין בין שער ש**מודד** לשער ש**סופר גולמית**.

### ⛔ מה שאינו ניתן לאכיפה מכנית — נרשם ככזה
⛔ **פרק «פערים פתוחים»**: כל שורה נושאת תג בסופה — **`[מדיד: <תנאי>]`** (`code:` ·
`no-code:` · `file:` · `no-file:`) או **`[לא-מדיד: <נימוק>]`** עם נימוק אמיתי. ⛔ שורה
בלי תג אינה מצב חוקי. ⛔ **וכל שורה נושאת טריגר** — האירוע שמחייב לפתוח את הסבב שסוגר
אותה; ⛔ **תנאי מדיד שחדל להתקיים מפיל את השער**, והשער **מדווח ואינו מוחק**. ⛔ **וסמל
שנעלם אינו «הפער נסגר»** אלא «העוגן נרקב»: ⚠️ שינוי שם או ריפקטור אינם ראיה שהפער נסגר,
וההכרעה חוזרת לבן-אדם.

### ⛔ שער חדש נכנס ל-`APP.gates`
⛔ **שער שאינו ב-`APP.gates` שב-`check-js` הוא שער שקיים ואינו רץ**, ⛔ וכל שער חדש נבדק
על המצב הקיים לפני החיווט — נפל, ⛔ הסף שגוי.

### ⛔ שינוי מכוון ברכיב משותף
⛔ **שינוי בבלוק משותף = שינוי בארבע האפליקציות ובארבעת עותקי השער**, באותו סבב. ⚠️
החתימות מסתירות סטייה חלקית: שניים שלא עודכנו **עוברים**, מפני שהחתימה הישנה תואמת לתוכן
הישן. ⛔ ולכן סבב שנוגע בבלוק משותף נפתח מלכתחילה עם ארבעת הריפו; ⛔ סשן שאין לו ארבעתם
רושם את הצורך וסוגר — ⚠️ פער גלוי עדיף על פער שהשער מאשר.
<!-- SHARED:end -->
<!-- SHARED:start id="rules-writing" -->
## ⭐ כללי הכתיבה

### ⛔⛔ רכיב משותף = קוד זהה בית-לבית + מיקום זהה + חיווט חי
⛔ **סשן שכותב בקוד משותף מעתיק אות באות** — ⛔ בלי לשנות רווח, סדר או ניסוח, ⭐ **וההערות
שעליו זהות אף הן.** ⚠️ קוד זהה לבדו אינו רכיב משותף: נדרשים גם **מיקום זהה במסך**
ו**חיווט חי** — ⛔ רכיב שאינו נקרא מאף מקום הוא קוד מת שהשער מאשר. ⛔ **ואין נקודת הפעלה
שנייה**, גם «ליתר ביטחון»: ⛔ מעטפת דקה היא נקודת הפעלה שנייה — ⚠️ ברגע שיש עוטף, כל מקום
בקוד רשאי לקרוא לו.

### ⛔ מתי כותבים הערה
⛔ **רק במקום שבו קורא סביר יופתע, או ינסה «לתקן» משהו שאינו שבור.** ⛔ ההערה מסבירה
**למה** ⛔ ולא **מה**, ⛔ ואינה נכתבת על שורה מובנת מאליה, על כלל שכבר בטבלה, או עם שם
קובץ.

### ⛔ הערה אינה מפנה לקובץ
⛔ **הערה לעולם אינה נוקבת בשם קובץ, בלוק או פרק.** ⚠️ הנימוק: **השער מגן, ⛔ לא ההפניה**
— והפניה נשברת בכל שינוי שם, בשקט. ⭐ מסירים את ההפניה ומשאירים את הנימוק.

### ⛔ צורת ההערה
⛔ **שלושה סימנים בלבד** — **⛔** איסור · **⚠️** אזהרה · **⭐** החלטה מרכזית. ⛔ אין להמציא
רביעי: קורא שרואה חמישה סמלים מפסיק לעצור באף אחד. ⚠️ הכלל חל על פתיחת שורת הערה; ✅/❌/⏳
בתוך משפט הם טקסט. ⛔ **כל ⛔ נושא סבב וסיבה** — «⛔ אל תעשה X (סבב N) — הסיבה». ⚠️ הסבב
הוא הדרך למצוא את הרקע; הסיבה היא מה שמונע מהסשן הבא לבטל את האיסור בתום לב. ⛔ **כותרת
בלוק לכל אזור לוגי** ב-`index.html`: מסגרת של **74** תווי `═`; ⛔ ובקובצי `tools/` — באנר
`──` באורך **78**. ⛔ **טרמינולוגיה אחידה** — מראה · טביעה · אופק הפינוי · שער · פינוי ·
דחיפת-מצב · מטמון · אופליין · `tombstone`. ⚠️ שני כתיבים לאותו מושג הופכים כל חיפוש בין
הריפו ל**חלקי**. ⛔ **שפת ההערה עברית**; מונחים טכניים ושמות מזהים באנגלית. ⛔ **וכל החלטה
שנראית שרירותית מנומקת במקומה** — קבוע, טבלה שמוחרגת, סדר שתי שורות שאסור להפוך: ⚠️ מי
שעורך את הקוד אינו קורא תיעוד קודם.

### ⛔ הבדל מכוון מנומק בכתב, במקום שבו רואים אותו
⛔ סטייה מדפוס — קובץ שקיים באחת, ערך שנבדל, שער שאינו חל — **נושאת נימוק כתוב במקום שבו
נתקלים בה**: ⛔ נימוק שאיש אינו קורא כשהוא נוגע בקוד אינו מגן על דבר.

### ⛔ ערך שקיים בקוד או במסד אינו מוצהר בתיעוד
⛔ מספר גרסה, חתימה, ספירת שורות, שם טבלה או מצב מיגרציה — **נקראים מהמקום שבו הם
נקבעים**. ⭐ **הבחנה מעשית:** אם «האם זה עדיין נכון?» נענה ע"י `grep` בקובץ אחר — ⛔ הערך
אינו שייך לתיעוד. ⚠️ ומה שכן נשאר בכתב הוא ה**תבנית** וה**חובה**, ⛔ לא צילום המצב. ⛔
**והכלל חל גם על הערות בקוד** — ⛔ היסטוריית גרסאות אינה נכתבת בהערה.

### ⛔ קובץ נשפט לפי תפקידו
⛔ **`CLAUDE.md`** — כללים והטבלה, ואחריהם מסכים ולוגיקה של האפליקציה · ⛔ **`README.md`**
— מה שאדם צריך כדי להתקין ולהפעיל · ⛔ **`CONTEXT.md`** — הלקוח והצורך · ⛔
**`android/README.md`** — המעטפת, הבנייה והחתימה. ⚠️ **הנימוק אינו סדר אלא קהל:** את
`CLAUDE.md` קורא **כל סשן, בכל עבודה**, ולכן כל שורה בו היא מס קבוע; את `README` קוראים
פעם אחת. ⛔ **תקרות:** `CLAUDE.md` 300 משותף ו-300 פרטי · `README` 50 · `CONTEXT` 100 ·
`android/README` 150. ⛔ **וחלון של שני פרקי סבבים** ב-`CLAUDE.md`; ⚠️ סבב שלישי נכנס —
הראשון נמחק **באותו קומיט**. ⛔ ולקח שראוי להישמר עולה לכלל, לטבלה או להערה בקוד לפני
המחיקה: ⚠️ git זוכר, ⛔ אבל git אינו מקום שקוראים בו. ⛔ **פרוזה רק למה שאין לו שער**:
ההכרעה, הכשל שהוליד אותה, ומה אסור לעשות — ⛔ בדיוק מה ששער אינו יכול לומר; ⛔ וכל «הבעיה
שנמדדה» היא עד שלוש שורות.
<!-- SHARED:end -->
<!-- SHARED:start id="rules-session" -->
## ⭐ כללי הסשן

### ⛔ git
⛔ **כל עבודה על ענף ייעודי** — `claude/<נושא>-<סיומת>`, ⛔ לא על `main`. ⛔ **מיזוג
ל-`main` רק באישור מפורש**, אחרי בדיקה חיצונית של המנהל. ⛔ **לפני כל מיזוג — `fetch`,
ואז `merge --ff-only origin/main`.** ⚠️ סשן ענן מחזיק עותק שקפא ברגע פתיחתו, וכל מה
שנדחף מאז אינו קיים בו; ⛔ ו- `--ff-only` **נכשל** במקום ליצור מיזוג שקט שמחזיר את הקוד
אחורה. ⛔ **בעבודה רב-ריפו — כל פקודת git מציינת את הריפו: `git -C <path> …`**; ⚠️ שם
הענף **זהה בארבעתם**, ולכן פקודה שרצה בתיקייה הלא-נכונה תמצא שם ענף תקף ותמזג אותו — ⛔
מיזוג שקט, בלי שגיאה ובלי סימן. ⛔ **מחיקת ענפים מרוחקים חסומה בענן** — לדלג ולדווח שהענף
נותר; ⛔ הודעות קומיט בעברית חופשית, עם קידומת `סבב N:` לעבודה מרוכזת.

### ⛔ מדידה לפני שינוי
⛔ **הוראת מחיקה שנימוקה «שריד» או «לא בשימוש» אינה מבוצעת לפני מדידה שמוכיחה שאין לו
קורא.** נמצא קורא — ⛔ הסשן **עוצר, מדווח ואינו מוחק**, גם אם ההוראה הייתה מפורשת. ⛔
**שלושת התנאים:** חיפוש קוראים ב**ארבעת** הריפו ובכל סוגי הקבצים · בדיקה שהרכיב אינו
נתיב-חזרה · ורישום המדידה. ⚠️ «אין לו קוראים היום» אינו «אינו נחוץ»: מנגנון שנכשל-סגור,
הגירה חד-פעמית ונתיב חזרה מכובה הם רכיבים בלי קוראים **עד שהם נחוצים**. ⛔ וסריקה שמוגבלת
לסוג קובץ אחד היא ראיה לסוג שנסרק בלבד. ⛔ **ומדידה תקפה להיקף שנסרק ולרגע שבו נעשתה** —
מי שמרחיב את ההיקף מודד שוב ורושם את שני הממצאים.

### ⛔ סריקת האזור שנגעו בו
⛔ **כל סבב שנוגע באזור סורק אותו בשלושה ממדים לפני שהוא מסיים:**
1. **באג שקט** — מפתח סימון שנכתב בצורה אחת ונקרא באחרת · מזהה שמשמש
כמפתח מיזוג · גבול חודש · מסלול שרץ פעם בשנה.
2. **האם משהו כאן היה אמור להיות משותף** — ⛔ ואם כן, עולה מיד לרמת
הארגון ואינו נשאר מקומי.
3. **האם התיעוד באזור תואם למציאות.**
⚠️ **ממצא רוחבי עוצר את הסבב ומדווח**, ⛔ ואינו מטופל מקומית: תיקון מקומי סוגר את הפער
באחת ומשאיר שלוש פתוחות בלי שאיש יידע. ⛔ **והסריקה חלה על האזור שנגעו בו** — ⛔ לא על
הקובץ כולו.

### ⛔ משמעת קריאה וחיסכון טוקנים
⛔ **לעולם לא לקרוא את `index.html` במלואו** — `grep -n` לאיתור, ואז `sed -n
'<from>,<to>p'` לקטע. ⚠️ קריאה מלאה מכלה את ההקשר לפני שהעבודה מתחילה. ⛔ **לא לקרוא קובץ
שלם כשחיפוש מספיק** — גם קובץ קטן. השאלה היא תמיד «איזו שורה אני צריך», ⛔ ולא «מה יש
בקובץ». ⛔ **לא לשפוך תוכן קבצים לצ'אט** — פלט נכתב **לקובץ**, והדיווח הוא מונה ושורת
סיכום: ⚠️ פלט שנשפך נשאר בהקשר, ומחירו משולם בכל צעד. ⛔ **קומיט ודחיפה אחרי כל סעיף** —
⛔ ולא בסוף הסבב. ⚠️ **וסשן שנחנק אינו מותיר שום סימן** — אין שער שנופל ואין קומיט, ⛔
והוא מאבד את כל מה שלא נדחף.

### ⛔ הכרעה שאינה של הסשן
⛔ **מודל אבטחה, בחירת שפה ומדיניות נתונים הם החלטת המנהל.** מנגנון שנבחר במפורש — כולל
בחירה **לא** להקשיח — אינו חוב טכני שסשן מוסמך לסגור מיוזמתו: ⛔ הוא **מדווח ומציע**,
וההגירה מחייבת אישור. ⛔ **ואין להריץ מיגרציה, למחוק נתון או לגעת במסד בלי אישור מפורש**;
⛔ שינוי סכימה — אך ורק ב-`apply_migration`, ושאילתות קריאה חופשיות. ⛔ **וסוד שנדחף לגיט
נחשב חשוף לצמיתות** — ⚠️ מחיקה מאוחרת אינה מסירה אותו מההיסטוריה, ⛔ והטיפול היחיד הוא
**החלפתו במסד**.
<!-- SHARED:end -->

## סביבת עבודה
- **ריפו:** `ygtotlrl-lab/yoman-avoda`
- **Pages:** `https://ygtotlrl-lab.github.io/yoman-avoda/`
- **קובץ ראשי:** `index.html`
- **Supabase:** `kxbtskqobynewvnckaaz`
- **אופן העבודה (נכון ליולי 2026):** העבודה מתנהלת בסשני ענן (Claude Code cloud) — הריפו משוכפל טרי בתחילת כל סשן, העבודה נעשית על ענף ייעודי, והדחיפה בסוף הסשן. **אין עותקים מקומיים קבועים.**
- **עבודה מקומית במחשב (אם בכל זאת):** שכפול **אך ורק** לתיקייה יציבה כמו `C:\Users\F\Documents\repos\` — **לעולם לא ל-Temp** (מנקה Windows / Storage Sense מוחק משם קבצים באמצע עבודה; זה גרם ל"מחיקות רפאים" חוזרות). טוקן: מאוחסן ב-Windows Credential Manager (host `github.com`) — לעולם לא בקובץ; `git push`/`clone` מושכים אותו אוטומטית דרך GCM.

## גישת Supabase
כשזמין ה-Supabase MCP, נהג לפי הכללים הבאים — ללא יוצאים מן הכלל:
- **שינויי סכימה** (יצירה/שינוי/מחיקת טבלאות, עמודות, פוליסות, הרשאות) — **אך ורק דרך `apply_migration`** עם שם ברור ותיאורי (למשל `add_sync_log_table`). לא דרך `execute_sql`.
- **שאילתות אבחון וקריאה** (SELECT, בדיקת מבנה, ספירות, `list_tables` וכו') — **חופשיות**, ללא אישור.
- **עדכון או מחיקת נתונים בטבלאות `kv`** (כאן: `kv_rishon` / `kv_ramataviv`, וכל טבלת נתונים אחרת) — **מחייבים אישור מפורש מהמשתמש לפני הרצה**. אין להריץ `UPDATE`/`DELETE`/`upsert` על נתונים בלי אישור.

```bash
```

## לפני כל push — חובה: שער אחד, `node tools/check-js.mjs`
**כל שינוי בקוד (`index.html` או `sw.js`) חייב לעבור את השער הזה לפני דחיפה.
דחיפה ללא הבדיקה — אסורה.**

```bash
node tools/check-js.mjs
```

⭐ **מסבב 33 זו פקודה אחת ולא רשימה.** השער מחלץ את כל ה-JS המוטבע
מ-`index.html`, מריץ עליו `node --check` ועל `sw.js`, מאמת את כללי-האמת
שפרסר אינו תופס (תבנית `CACHE_NAME`, דילוג על supabase, נעילת גרסת ה-CDN),
ואז מריץ **את כל שערי האחידות ואת כל חבילות בדיקות הסבבים** — כך שאין דרך
לדחוף בלי שהם רצו.
⚠️ בדיקת איזון-סוגריים בלבד אינה מספיקה (עיוורת לשגיאות בתוך מחרוזות);
`node --check` = parse מלא אמיתי ב-V8. node זמין בסביבת הענן.

**מה שהשער מריץ, בסדר הזה:**
- **`check-structure.mjs`** — המבנה הקנוני (סבב 33, כלל ברזל 14).
- **`check-status-area.mjs`** — אחידות «אזור מצב» (סבב 17, כלל ברזל 7).
- **`check-docs.mjs`** — אחידות התיעוד (סבב 18, כלל ברזל 8).
- **`check-comments.mjs`** — תקן ההערות בקוד (סבב 28, כלל ברזל 11).
- **`check-capabilities.mjs`** — אחידות היכולות המשותפות (סבב 30 השלמה, כלל ברזל 12).
- **`check-gaps.mjs`** — אכיפת פרק «פערים פתוחים» (סבב 39, כלל ברזל 15).
- **`test_sources.mjs`** — שער מקורות הגיבוי (סבב 62): ⛔ כל מקור
  `kind:'table'` שאינו מאחורי דגל חייב להופיע ב-`APP.tables`. ⚠️ קורא
  **קבצים** — ⛔ אינו רואה את המסד החי, והרשימה היא הצהרה שאדם מתחזק.
- **`test_backup_policy.mjs`** — מדיניות הגיבויים (סבב 61, כלל
  ברזל 20): שלושת הערכים (7 · 30 · 14)
  שב-`hanhala-ruchanit/migrations/012_backup_retention_cap.sql` תואמים
  לטבלה שבפרק. ⚠️ קורא **קבצים** — ⛔ אינו רואה את המסד החי.
- **`test_bump.mjs`** — שער ה-`versionCode`: הבסיס הוא **קומיט
  הקידום האחרון** ולא `origin/main`. בונה ריפו git סינתטי בתיקייה זמנית,
  מריץ בו את `test_gradle.mjs` האמיתי, ⛔ ומוכיח במוטציית-לוגיקה
  שהבסיס הישן **עובר** בדיוק על התרחיש שהחדש מפיל.
- **`test_icons.mjs`** — שכבת האייקונים: `icons/` בדיוק ששת
  הנכסים הקנוניים, `design/` קובץ מאסטר אחד, ⛔ ואף קובץ מוגש אינו מפנה
  אליה — עם **חמש מוטציות** ומוטציית-נגד אחת (שינוי תוכן אינו מפיל).
- **`test_read.mjs`** — מתג המעבר: מקור הקריאה. נועל את הדפוס
  «טבלאות תחילה, ה-`kv` רק כנפילה-חזרה» — ⛔ כל קריאת `kv` של מפתח שעבר
  חייבת להיות מותנית בכישלון השורות — ומריץ את `tbRowsGet` **האמיתית**
  ברתמת `vm` על השליפה בעמודים ועל הנכשל-סגור. עם שתי מוטציות
  ומוטציית-נגד. ⚠️ פרטי כאן.
- **`test_stage_a.mjs`** — גיבוי יומי ויומן פעולות (מודול משותף).
- **`test_stage_b.mjs`** — שכבת השורות בענן: שקילות ומיזוג.
- **`test_archive.mjs`** — תצוגת הארכיון ומקור הרשומות האוטומטיות.
- **`test_unify.mjs`** — איחוד הארכיון: טבלה אחת ודגל `archived`.
- **`test_structure.mjs`** — הנעילה המבנית ושער ה-JS, עם מוטציות.
- **`test_hotwin.mjs`** — מודול החלון החם ויכולות סבב 35, עם מוטציות.
- **`test_cron.mjs`** — פינוי הגיבויים במסד (pg_cron), עם מוטציות.
- **`test_merge_pending.mjs`** — הגנת ה-⏳ במנוע המיזוג וחסימת
  משתמש מושבת בכניסה אופליין, עם מוטציות.
- **`test_matrix.mjs`** — אכיפת מטריצת היכולות: היפוך כל תא מכוסה
  חייב להפיל את `check-capabilities`.
- **`test_merge_core.mjs`** — ליבת המיזוג המשותפת: כלל ההכרעה
  מופיע פעם אחת, שלושת כללי ברזל 6, וידיות המדיניות — עם חמש מוטציות.
- **`test_ids_yoman.mjs`** — מזהי היומן: דו-קיום של מזהה מספרי
  ישן ו-uuid חדש, עם ארבע מוטציות. ⚠️ פרטי כאן.
- **`test_gaps.mjs`** — אכיפת פרק «פערים פתוחים»: פער שנסגר מפיל
  את השער, ושורה `לא-מדיד` עוברת — עם שמונה מוטציות.
- **`test_md.mjs`** — שלושת קובצי ה-md הנלווים (`README.md` ·
  `CONTEXT.md` · `android/README.md`): השלד (סבב 39) **ותוכן שש הפסקאות
  המשותפות** שנחתמות ב-sha256 (סבב 41א), עם שש מוטציות.
- **`test_sw.mjs`** — קו הבסיס ההתנהגותי של ה-service worker: מה
  חוזר ל-`respondWith` בכל תרחיש (ניווט, תת-משאב, 404, supabase, סריקת
  המטמון) ומה נכנס למטמון. ⚠️ **נמדד לפני האיחוד** ומשמש מאז כשער: ⛔ כל
  תא שלא סומן `defect` חייב להישאר זהה בדיוק, וחמשת תאי הליקוי הופכו
  בסבב 42ג. מסבב 42ג `defectCount` הוא **0**.
- **`test_swcore.mjs`** — ליבת ה-service worker: החתימה, מספר
  השורות, `SW_CFG` מעל הליבה, ועשר ידיות המדיניות שנמדדו בסבב 40 —
  עם **ארבע מוטציות**, שלוש מהן מריצות את רתמת קו-הבסיס על עותק מוטב
  בתיקייה זמנית ודורשות שהיא **תיפול**.
- **`test_workflows.mjs`** — היפוך ברירת המחדל בשני ה-workflows:
  חתימה אחת ל-`build-apk.yml` ואחת ל-`cleanup-merged-branches.yml` בארבעתן,
  ⛔ ושם הריפו הוא הדבר היחיד המוכרז פרטי — עם שש מוטציות.
- **`test_android.mjs`** — שער שכבת האנדרואיד: חוזה המניפסט (חמש
  שורות), `configChanges` כסופרסט הקנוני, שלושת רכיבי גשר השיתוף כקבוצה
  אחת, ו**חתימה על החלק המשותף** של `AndroidManifest.xml` ושל
  `android/app/build.gradle` — עם **שמונה מוטציות**, שלוש שחייבות להזיז
  את החתימה וחמש שאסור להן.
- **`test_lists.mjs`** — שקילות שתי רשימות המודולים המשותפים:
  `CAPS` שב-`check-capabilities.mjs` מול `SHARED_BLOCKS` שב-`check-comments.mjs`.
  ⛔ מודול שקיים באחת ואינו בשנייה מפיל את הדחיפה — עם **שלוש מוטציות**,
  וטענה שרשימה ריקה היא כישלון ולא «אין הפרש».
- **`test_retry.mjs`** — מודול הניסיון החוזר בסנכרון: הדריכה
  המותנית, שלושת התנאים, הנסיגה האקספוננציאלית והגנת הריצה הכפולה —
  ברתמת `vm` עם שעון מזויף, ועם **ארבע מוטציות**.
- **`test_build.mjs`** — שער הבנייה והחתימה: שם ה-workflow, שם
  ה-artifact, שם קובץ הפלט, ⛔ היעדר `apksigner` ב-YAML, ו-`EXPECTED_SHA256`
  שאינו ריק ואינו מציין-מקום — עם שמונה מוטציות.

אם `node --check` מדווח שגיאה — אסור לדחוף עד שהיא מתוקנת.
אם `check-structure.mjs` נכשל — ר' הפרק «כלל ברזל 14» למטה. ⛔ קובץ שורש
חדש או תיקייה חדשה מחייבים שורה **מנומקת** ברשימת-ההיתר, בארבעת עותקי
הבדיקה — «קיים רק כאן, בשקט» אינו מצב חוקי.
אם `check-status-area.mjs` נכשל — ר' הפרק «כלל ברזל 7» למטה. שינוי בבלוק
משותף מחייב עדכון בארבע האפליקציות ובארבעת עותקי קובץ הבדיקה, באותו סבב.
אם `check-docs.mjs` נכשל — ר' הפרק «כלל ברזל 8» למטה. אותו כלל חל על פרק
תיעוד מסומן `SHARED`: משנים בארבעתם ובארבעת עותקי הבדיקה, באותו סבב.
אם `check-comments.mjs` נכשל — ר' הפרק «כלל ברזל 11» למטה. ⛔ התקן חל על
הקוד הפרטי בלבד; הבלוקים המשותפים והמודולים הקפואים מוחרגים ממנו במפורש.
אם `check-capabilities.mjs` נכשל — ר' הפרק «כלל ברזל 12» למטה. ⛔ נקודת
ההפעלה של יכולות העלייה היא **פונקציית העלייה של האפליקציה** — זו שקוראת
גם ל-`lsBoot()` וגם ל-`pendBoot()` — ואין להזיז אותה למסלול אחר.

## עדכוני Service Worker
- **כל שינוי בקוד מחייב קידום `CACHE_NAME` ב-`sw.js` לגרסה הבאה** — בלי זה המשתמשים לא יקבלו את העדכון.
- **וגם קידום `<meta name="app-version">` בראש `index.html`** — זה מזהה הגרסה שמנגנון האוטו-אפדייט משווה מולו (החליף את `simpleHash`, שרץ על 5000 התווים הראשונים ולכן תמיד החזיר אותו ערך והבאנר לא הופיע לעולם). בלי קידום — הבאנר לא יופיע.
- מנגנון באנר "גרסה חדשה זמינה" קיים באפליקציה — המשתמשים מקבלים את העדכון בלחיצה על הבאנר.
- בנוסף קיים מנגנון אוטו-אפדייט מבוסס `raw.githubusercontent` (`GITHUB_FILE`/`RAW_URL` ב-`index.html`) עבור מכשירים עם ה-APK — ראה כלל קריטי 5. הבדיקה רצה **פעם בשעה**, עם `Range: bytes=0-4095` + `cache:'no-store'` + timeout, ו-`sw.js` לא שומר אותה במטמון (קודם: משיכת ~950KB כל 5 דקות, כל אחת נשמרה במטמון תחת `?t=` ייחודי).
- ⛔ **אין לקדם גרסה בלי שינוי בקוד הרץ (סבב 26 השלמה) — קידום סרק שולח
  באנר «גרסה חדשה זמינה» לכל המשתמשים לשום שינוי.** ⚠️ שינוי הערות נרחב
  ב-`index.html` הוא כן שינוי שמקודם (תקדים סבב 28); הערה בודדת — לא.

## Push
```bash
git add . && git commit -m "תיאור השינוי"
git push -u origin <שם-הענף>   # דחיפה לענף העבודה — לא ל-main
```

## סיום משימה
בסיום כל משימה משמעותית — **עדכן קובץ זה בתמצית** לפני סיום הסשן: מה שונה, מה הוחלט. כך הסשן הבא מתחיל עם תמונת מצב עדכנית.

## כללים קריטיים
1. **בדיקת תחביר עם `node --check` לפני כל push** (הסעיף למעלה) — חובה מוחלטת. כל שינוי ב-`index.html` חייב לעבור חילוץ-JS + `node --check` (וגם `sw.js`). דחיפה בלי זה — אסורה.
2. **קידום `CACHE_NAME` ב-`sw.js` וגם `<meta name="app-version">` ב-`index.html` בכל שינוי קוד** (הסעיף למעלה) — בלי זה העדכון לא מגיע למשתמשים.
3. **smali בלבד** לתיקון URLs ב-APK בנוי — ⛔ לא binary patch: עריכה בינארית
   שוברת את החתימה ואינה ניתנת לאימות.
4. **cache APK — שם חדש בכל בנייה**, עם `TS=$(date +%s)` — ⛔ שם חוזר נתפס
   במטמון והמשתמש מתקין שוב את הבנייה הקודמת בלי לדעת.
5. **מקור אמת יחיד = `index.html`** — זה הקובץ ש-Pages מגיש, שאליו מצביע `start_url` במניפסט, ושאותו **מעטפת ה-APK טוענת מהרשת** (מגרסה 2 — ר' הפרק "APK"). כל עדכון קוד נכנס לכאן בלבד. **אסור ליצור קבצי HTML כפולים** (בעבר היה `יומן עבודה.html` — שונה ל-`index.html`). ⛔ **ואסור להחזיר עותק מוטבע ל-`android/app/src/main/assets/`** — עותק `file://` הוא מקור אמת שני *ובנוסף* origin אחסון נפרד, כלומר localStorage אחר. **`GITHUB_FILE` במנגנון האוטו-אפדייט חייב להתאים לשם הקובץ האמיתי בריפו** (`index.html`) — אם הוא מצביע על שם אחר, `RAW_URL` מקבל 404, האפליקציה לא מזהה גרסה חדשה, ועדכונים לא מגיעים למכשירים מותקנים.
6. **חתימת APK: רק עם `signing/yoman.keystore` (alias `yoman`, pass `yoman123`)** — המפתח הקבוע. לעולם לא ליצור keystore חדש, אחרת APK עתידי לא יתקין מעל הקיים.
7. **המעטפת היא WebView מקורי (`android/`) — לעולם לא TWA ולא PWABuilder.** TWA מריץ את האתר בתוך כרום, וסינון התוכן במכשירים של המשתמשים חוסם את כרום. ר' הפרק "APK — מעטפת WebView מקורית".

## APK, חתימה ובנייה — ⛔ הפרטים ב-`android/README.md`
⛔ **תוכן תפעולי אינו יושב כאן** (כלל ברזל 23) — המעטפת, המפתח הקבוע, צנרת
הבנייה והגשר מתועדים ב-`android/README.md`; ⛔ מה שנשאר כאן הוא ההכרעות שאין
לחזור ולדון בהן:
- ⛔ **לעולם לא TWA ולא PWABuilder (נרשם בסבב 13)** — TWA מריץ את האתר בתוך
  כרום, וסינון התוכן במכשירי המשתמשים חוסם את כרום; `WebView` רץ בתוך תהליך
  האפליקציה ואינו נחסם. ⚠️ נמדד: gius כ-TWA לא נפתחה כלל אצל המשתמשים.
- ⛔ **לעולם לא ליצור keystore חדש (נרשם בסבב 13)** — אנדרואיד מזהה אפליקציה
  לפי חתימה, והחלפה מחייבת הסרה והתקנה מחדש אצל כל משתמש.
- ⛔ **אין נכסים מוטבעים ואין להחזירם (סבב 13)** — עותק `file://` הוא origin
  אחסון נפרד (localStorage אחר = אובדן נתונים שקט) וגם מקור אמת שני.
- ⛔ **הגשר המקורי מוגבל לדומיין, והמסלול המאובטח נוסה ראשון (סבב 13)** —
  היפוך הסדר הוא ויתור על אכיפה שהפלטפורמה נותנת בחינם.
- ⛔ **כל `http`/`https` נשאר בתוך המעטפת (נרשם בסבב 13)** — הפניה לדפדפן
  חיצוני מגיעה לכרום, שהסינון חוסם.

## מצב נוכחי
- ייצוא PDF ושיתוף הדוח כתמונה (השיתוף מייצר JPEG דרך `_buildReportDiv`/`_renderReport`;
  אין כפתור "הורד JPEG" עצמאי — `exportJPEG` הוסרה כקוד מת בסבב 7)
- ארכיב ועריכה inline; Hebrew calendar עם leap year
- Supabase sync (polling 3 שניות) — מיזוג ברמת רשומה, ר' «מנוע המיזוג» למטה
- הייצוג בענן: טבלאות מובנות (`tb_entries` מאוחדת עם דגל `archived`);
  הכתיבה הכפולה למפתחות ה-`kv` ול-`tb_archive` **כובתה בסבב 35** —
  ר' «הייצוג בענן» למטה
- PWA מותקן — ⛔ **הגרסה עצמה אינה מוצהרת כאן** (כלל ברזל 21): `CACHE_NAME`
  ב-`sw.js` ו-`<meta name="app-version">` בראש `index.html` הם מקור האמת

## מוסכמת הודעות קומיט (סבב 8 — משותפת לשלושת הפרויקטים)
**עברית חופשית**, לא conventional-commits. זה הדפוס שהתגבש בפועל בשלושת הריפו,
ומעכשיו הוא ההחלטה המוצהרת:
- שורה ראשונה: תיאור תמציתי בעברית של מה השתנה (עד ~72 תווים).
- עבודה מרוכזת מקבלת קידומת `סבב N:` (למשל `סבב 8: אחידות בין שלושת הפרויקטים`).
- בלי קידומות `feat:`/`fix:` באנגלית.

## מנוע המיזוג — מצב נוכחי (סבב 3ב, גריעת tombstones בסבב 7)
**הכלל היחיד שחשוב:** היעדר רשומה אצל מכשיר אינו מחיקה. מחיקה =
`deleted:true` + `updatedAt` (tombstone).
- **המנוע:** `mergeRecords(local, remote, getKey, mergePair)` — הענן הוא
  בסיס הסדר ומנצח בשוויון (דטרמיניסטי). נגזרות: `mergeEntries` (לפי `id`),
  `mergeArchive` (סנאפשוט לפי `gdate`, ובתוכו מיזוג רשומות לפי `id`),
  `mergeCats` (לפי `letter`), `mergeSubs` (פר-מפתח לפי `SUBS_META` — מפה
  `{מפתח: updatedAt}` ב-`tb_subs_meta`). עזרים: `recTs`/`recTouch`/
  `recDelete`/`isLive`/`liveOnly`.
- `autoSyncData` ו-`syncFromCloud` **ממזגים** ולא מחליפים blob — החלפה
  שלמה הייתה מקור אובדן הנתונים המקורי.
- ⛔ **אין stamping של `Date.now()` בטעינה (סבב 3ב)** — רשומה בלי
  `updatedAt` נחשבת ts=0 («ותיקה») אך לעולם לא נופלת רק כי הצד השני לא
  מכיר אותה; stamping היה גורם למכשיר ישן לנצח נתונים חדשים.
- **«סיום יום» (`autoArchiveDay`):** העותקים שנכנסים לארכיון וה-tombstones
  של החיים מקבלים **אותה חותמת**; בשוויון הסנאפשוט מנצח, ולכן ארכוב אינו
  מחיקה מהארכיון. משימות ותתי-משימות הן מחרוזות בלי מזהה — נוסעות עם
  רשומת האב וכל שינוי מקדם את ה-`updatedAt` שלו.
- **גריעת tombstones:** `prunePastTombstones(arr, now)` + `TOMBSTONE_TTL_MS`
  (90 יום). רק `deleted:true` עם `updatedAt` **מספרי** נגרע — tombstone
  בלי חותמת נשאר (recTs מחזיר לו 0, וגריעה לפי 0 הייתה מוחקת דווקא את
  הישנים ביותר). ⛔ הדגל `_tombPrunePending` אינו קישוט (סבב 7): הגריעה
  חייבת לרוץ על **התוצאה הממוזגת** במיזוג הראשון אחרי העלייה — גריעה
  מקומית בלבד הייתה מוחזרת מהענן תוך שניות. לא בפולינג ולא בתוך המנוע.
- **מחיר מודע:** מכשיר שנותק מעל 90 יום יחזיר רשומה מחוקה **פעם אחת**;
  מחיקה חוזרת יוצרת tombstone אמיתי.

## הייצוג בענן — טבלאות מובנות והארכיון המאוחד (סבבים 30–32ב)
הנתונים בענן יושבים בטבלאות מובנות — שורה לכל רשומה — ולא כערכי `kv`:
- **`tb_entries` היא הטבלה המאוחדת** (סבב 32): גם היומן החי וגם הארכיון,
  עם דגל `archived boolean` ועמודת `yeshiva`. «העברה לארכיון» היא דגל על
  השורה, לא העברה בין טבלאות. `tbTableOf`/`tbArchivedFlag` מנתבות,
  ו-`tbRowsGet` מסננת `.eq('archived', …)` עם `order('rec_key')` **וגם**
  `tbSortRows` בקוד (סבב 31 — בלי `ORDER BY` פוסטגרס אינו מבטיח סדר,
  ומיון לקסיקוגרפי של `rec_key` אינו סדר תאריכים).
- ⭐ **עמודת `yeshiva` ולא טבלה לכל מוסד** — מוסד שלישי דורש אפס DDL.
  ⛔ ההפרדה **במכשיר** (סיומת `LS`) לא נגעה.
- ⭐ **`client_id` נגזר ממפתח המיזוג** (`entryKey`/`archiveKey`) ואינו uuid
  חדש — לרשומה כבר יש זהות שנוצרה במכשיר, ומזהה שני היה מאפשר לה להתקיים
  פעמיים. ⛔ **אין טריגר `updated_at` בצד השרת** — החותמת של המכשיר היא
  האמת, ולכן אין כאן את אובדן העריכה שכלל ברזל 6 מתאר.
- ⭐ **הסנאפשוט הוא יחידת הארכיון** — הוא מה שמנוע המיזוג מכיר
  (`archiveKey`), ובתוכו כבר רץ מיזוג פר-רשומה. ⛔ פירוקו לרשומות הוא
  מנוע מיזוג אחר — סבב ייעודי בהחלטת המנהל, לא תופעת לוואי.
- ⭐ **בונה סנאפשוט אחד — `arcPutSnapshot` — לשני מסלולי היצירה** (סבב 31):
  `autoArchiveDay` (ידני) ו-`checkDayChange` (מעבר יום) קוראים לו, ולכן
  אין מסלול שממלא שדה שהשני משמיט; מעבר יום על יום שכבר יש לו סנאפשוט
  מתמזג אליו. שדות היום נגזרים **מהרשומות עצמן** קודם, מהשעון רק
  כנפילה-חזרה. `hebFromText`/`snapHDate` משחזרות תאריך עברי **לתצוגה
  בלבד** — ⛔ `snapHDate` טהורה ואינה כותבת לסנאפשוט (סבב 31).
- ⭐ **הכתיבה הכפולה כובתה בסבב 35** — `TB_KV_LEGACY_WRITE = false`
  ו-`TB_ARC_LEGACY_WRITE = false`: היומן והארכיון נכתבים לטבלאות בלבד,
  ועֵד הפינוי ואישור ה-⏳ נשענים מעכשיו על `tbRowsPush` לבדה. נתיב
  החזרה נשאר החלפת דגל — מפתחות ה-`kv` והטבלה הישנה לא נמחקו.
  ⛔ המחיקה עצמה ב-30.8, פעולת מנהל, לא בסבב (סבב 35) — כיבוי ומחיקה
  יחד לא היו משאירים חלון לגלות טעות. ⚠️ `tb_cats`/`tb_subs`/
  `tb_subs_meta` ממשיכים להיכתב ל-`kv` — זה ביתם היחיד
  בענן והם אינם חלק מהכיבוי; גם חותמת `tb_last_changed` נשארת (אות
  הפולינג).
- ⚠️ **הגיבוי היומי עבר לטבלאות באותו סבב שכיבה את הכתיבה הכפולה**
  (סבב 35, כפי שסבב 30 קבע): `tb_entries` נגבית כטבלה — מקור
  `kind:'table'` עם `eq` פר-מוסד, `order('rec_key')` ומפתח הגיבוי
  `tb_entries_rows` — ולצדה מפתחות ה-`kv` שנשארו ביתם היחיד
  (`tb_cats`/`tb_subs`/`tb_subs_meta`).

### פינוי הגיבויים — הקובץ אינו מקומי
⚠️ **ופינוי הגיבויים כאן אינו קובץ מקומי** — הוא
`hanhala-ruchanit/migrations/004`, של הפרויקט המשותף.

⭐ **וארבעה שמות גיבוי שיצאו משימוש נמצאים ברשימת-ההיתר שלו במפורש**
(השלמת סבב 35ג, בהחלטת המנהל): `rishon_tb_entries` · `rishon_tb_archive` ·
`ramataviv_tb_entries` · `ramataviv_tb_archive` — שמות גיבוי-השגרה שרצו עד
סבב 35, לפני המעבר לגיבוי-הטבלאות (`*_tb_entries_rows`). ⚠️ **נמדד מול
המסד: 23 שורות שלהם יושבות ב-`kv_backup`**, ומפני שהרשימה הראשונה כללה רק
את השמות החדשים הן לא היו מתפנות לעולם. ⛔ **ההוספה היא ארבעה שמות
מפורשים ולא קידומת** (השלמת סבב 35ג) — קידומת `rishon_` הייתה תופסת גם
גיבויי `PRE_*` פר-מוסדיים, שאין למחקם אוטומטית לעולם.
⚠️ הפסקה הזו **נאכפת בשער** — `tools/test_cron.mjs` דורש
שארבעת השמות יהיו מתועדים כאן, ⛔ ולכן אין למחוק אותה בגיזום.

## מיגרציית מפתחות-שריד ב-localStorage — `migrateLegacyLsKeys()` (סבב 13)
מפתחות בלי סיומת מוסד (`tb_entries`/`tb_archive`) קדמו לרב-הדיירות ותפסו
~1.5MB מהמכסה המשותפת. ההגירה עדיין בקוד, אידמפוטנטית (אין שריד ⇒ יציאה
מיידית), ואלה כלליה: מיזוג אמיתי דרך המנוע (היעד «מרוחק» ומנצח בשוויון —
tombstone גובר על שריד חסר-חותמת) · אימות פריט-פריט, כשל ⇒ ההגירה מתבטלת
והשריד נשאר · כותבים לפני שמוחקים, רק כתיבה שהחזירה `true` מתירה מחיקה ·
⛔ **ראשון לציון בלבד (סבב 13)** — המפתחות קדמו לרב-דיירות ומיזוג לרמת
אביב היה דליפה בין מוסדות · ערך פגום אינו נמחק ונרשם ביומן. רצה
ב-`selectYeshiva` בין `lsRebuildPolicy()` ל-`lsBoot()`, לפני
`loadLocalData`.

---

### היישום כאן (yoman-avoda)
- **19 אתרי כתיבה גולמיים — כולם היו חשופים, כולם עברו לעוזר.** `saveCats`,
  `saveSubs`, `saveEntries`, `saveArchive`, חמש הכתיבות ב-`syncFromCloud`,
  שלוש ב-`autoArchiveDay`, שתיים ב-`arcSaveEntry`, דגל דחיית
  העדכון ומזהה המכשיר. עד היום אף אחת מהן לא נבדקה: באחסון מלא **הארכוב
  האוטומטי של סוף יום היה מוחק את הרשומות מ-`ENTRIES` בלי שהסנאפשוט נשמר**.
- **המערכים הגדולים דרך `lsSetArray`** (`tb_entries`+LS, `tb_archive`+LS).
- **⚠️ `_lastKnownTimestamp` אינו עֵד סנכרון ואסור להשתמש בו לפינוי.** הוא
  מתעדכן גם ב**משיכה** (משמעותו "ראיתי את מצב הענן") וגם נכתב ב-`saveEntries`/
  `saveArchive` **בלי לבדוק את תוצאת ה-`sbSet`**. פינוי שהיה נשען עליו היה
  יכול למחוק מהדיסק רשומה שמעולם לא הגיעה לענן. במקומו נוסף **`_tbPushedAt[key]`**,
  שנכתב **רק** אחרי `sbSet` שהחזירה `ok` — ומוזרם למנוע דרך `spec.syncedThrough`
  פר-מפתח. **אין להחליף אותו חזרה.**
- **`LS_CFG.tier2` נבנית ב-`lsRebuildPolicy()`, שנקראת מ-`selectYeshiva`** —
  המפתחות נושאים סיומת מוסד (`_rishon` / `_ramataviv`). הפינוי חל על **המוסד
  הפעיל בלבד**: לעֵד הסנכרון של מוסד אחד אין שום תוקף לגבי הנתונים של השני.
  המוסד השני יפונה כשייבחר. רצפות: 60 יום לארכיון, 90 לרשומות.
- **`_tbRecTs`** קוראת `updatedAt`, ובהיעדרו גוזרת מ-`gdate` — שנשמר בצורה
  `"3/09/2025"` (d/mm/yyyy), לא ISO — עם עוגן בצהריים מקומיים, כמו כל חישוב
  תאריך בפרויקטים האלה.
- **`lsBoot()`** נקראת ב-`selectYeshiva`, אחרי `lsRebuildPolicy()` ולפני
  `loadLocalData()`.
- **הגדרות** — האבחון מוצג כיום דרך «מידע טכני ›» בתחתית הפאנל (קופל
  בסבב 15; במקור כרטיס נפרד מעל עריכת הקטגוריות).

### ⚠️ yoman היא הצרכנית הגדולה בדומיין
`tb_archive` של ראשון לציון לבדו הוא **1.55MB** ב-localStorage, ושני המוסדות
יחד **~1.99MB** — קרוב ל-40% מהמכסה המשותפת של ארבע האפליקציות. זו הסיבה
שדווקא כאן שלב ב הוא אמיתי ולא תיאורטי.

### היישום כאן — אימות הפינוי מול הענן (מסבב 14)
- שני מפתחות ה-`tier2` ב-`lsRebuildPolicy()` קיבלו `idOf` ו-`verify`:
  `tb_archive`+LS לפי **`archiveKey`** (`g:<gdate>`, ובהיעדרו `i:<id>`)
  ו-`tb_entries`+LS לפי **`entryKey`** — בדיוק המפתחות של מנוע המיזוג, כך
  שאין דרך שהאימות והמיזוג יחלקו על זהות רשומה.
- **`_tbVerify(kvKey)`** מושכת דרך `sbGetResult`. ⚠️ **`ok` לבדו אינו
  מספיק:** `sbGetResult` מחזירה `{ok:true, data:null}` גם למפתח שאינו קיים
  בענן (PGRST116 מ-`.single()`), ולכן נדרש **מערך** — אחרת מפתח חסר בענן
  היה נקרא כ"ענן ריק" ו... לא היה מפנה דבר בכל מקרה (הרשומה הראשונה לא
  מאומתת), אבל התיעוד ביומן היה מטעה.
- **מפתח הענן הוא בלי סיומת המוסד** (`tb_archive`, לא `tb_archive_rishon`) —
  ההפרדה בין המוסדות בענן היא ב-`KV_TABLE` (`kv_rishon`/`kv_ramataviv`),
  והמפתח בתוך הטבלה זהה. הפינוי ממשיך לחול על **המוסד הפעיל בלבד**.
- `_tbPushedAt` **נשאר כפי שהוא** — הוא עדיין המסלול המהיר והמועדף כשיש
  דחיפה. ⛔ ואין להחליף אותו ב-`_lastKnownTimestamp` (ר' אזהרת סבב 11).

**כאן:** `mergeRecords`/`mergeEntries`/`mergeArchive` ו-`saveEntries`/
`saveArchive` כדחיפת-מצב מלא, עם `_tbPushedAt` כעֵד הדחיפה.

---

### היישום כאן (yoman-avoda)
- אזור המצב יושב בסוף `panel-settings`; `renderSettings()` קורא
  `statusAreaMount()`.
- **`_tbLastSyncAt` + `_tbMarkSynced()`** — חותמת תצוגה על כל שיחה מוצלחת
  עם הענן (`autoSyncData` ו-`syncFromCloud`). ⛔ **אינה `_tbPushedAt`**
  ואינה נוגעת בפינוי (סבבים 11 ו-14).
- **טוסט חזרת הסנכרון** (`MSG_SYNC_BACK`, דגל `_tbNetWarned`) — הכישלון
  נשאר שקט בכוונה (פולינג כל 3 שניות; טוסט על כל מחזור היה רעש), אבל
  החזרה נאמרת: זה הרגע שבו מה שהמתין מתחיל לעלות.

### היישום כאן — קיפול מסכי האחסון (מסבב 15)
כרטיס «💾 אחסון מקומי» הוסר מ-`panel-settings`; `<div id="tech-info-box">`
נוסף בתחתית הפאנל, אחרי כרטיס עריכת הקטגוריות; `renderSettings()` קורא
`techInfoMount()` במקום `lsRenderStatus()` (הקריאות שבתוך המודול הקפוא
נשארו — הן no-op כשהרכיב סגור או לא נטען).

---

```html
```

---

**הרישום הנדרש כאן (yoman-avoda):** ⛔ ליומן אין שער סיסמה, אין טבלת
משתמשים ואין תפקידים — הכניסה היא בחירת מוסד, ואין סוד להשוות מולו; מנגנוני
הסיסמאות של האחיות אינם רלוונטיים, ואין פער ואין טריגר. ⚠️ אבל «אין
משתמשים» ≠ «אין הרשאות עודפות»: צמצום הרשאות המפתח הציבורי (סבב 29,
`migrations/001`) חל גם כאן — `kv_rishon`/`kv_ramataviv` מחזיקות
`INSERT, SELECT, UPDATE` בלבד לשני התפקידים, ומוסד שלישי עתידי מחייב
`revoke` משלו.

---

**כאן:** ליומן אין משתמשים, ולכן רק סעיף 2 חל בפועל — ⛔ אין להוסיף שער
סיסמה מעל ההגדרות (שער כזה כאן היה שכבת הרשאה שלמה יש מאין; הערת ההגנה
יושבת בקוד מעל `renderSettings()`). ⚠️ אם אי-פעם יתווספו משתמשים — הכלל
הזה הוא הדבר שיימצא בקובץ, במקום סדרת הסבבים שהאחיות עברו.

---

---

**כאן:** ⚠️ הערה נגררת נשארת נגררת — `if (YESHIVA !== 'rishon') return report;`
נושאת את «⛔ ראשון לציון בלבד (סבב 13) — דליפה בין מוסדות» באותה שורה;
ההצמדה היא מה שמונע «לפשט» את התנאי.

---

---

---

---

**כאן:** `APP.rules` של `tools/check-js.mjs` נמדדו מהקבצים של האפליקציה
הזו: תבנית `CACHE_NAME` (`yoman-avoda-v<N>` — תבנית ולא מספר קבוע, כלל
שמקבע מספר חוסם את הקידום שהוא עצמו מחייב), דילוג על supabase, ניקוי מטמון
לפי קידומת, `mode:'cors'`, Content-Type בדף האופליין, ונעילת supabase-js
ל-2.111.0. חריגה מנומקת אחת ב-`check-structure.mjs`: `tools/fixtures/`
(הפיקסטורה של סבב 31). ⚠️ `.nojekyll` **אינו חריגה עוד** — הוא עלה לסט
הקנוני בסבב 39 והשער דורש אותו בארבעתן.

---

---
---

---

## רתמות הבדיקה ההיסטוריות — מצב מקובל, ⛔ לא פער

סבבים מוקדמים הריצו את הרתמות שלהם **בסשן בלבד** ולא שמרו אותן כקובץ;
מסבב 30 כל סבב שומר קובץ בדיקה בריפו (כלל ברזל 8 סעיף 6). ⛔ **הפער הזה
אינו נסגר בדיעבד, וזו החלטה ולא הזנחה** (סבב 39):

- ⚠️ **רתמה שתיכתב היום מול הקוד הנוכחי מקבעת את ההתנהגות הנוכחית** — כולל
  באג שטרם התגלה. היא תיראה כמו רשת ביטחון, ותפעל בפועל כאישור: כל תיקון
  עתידי של אותו באג ייראה כרגרסיה ויפיל את השער.
- ⚠️ **הלקחים עצמם לא אבדו** — הם זוקקו בסבב 34 לכללי הברזל ולפרקי
  המצב-הנוכחי, ושם הם נאכפים על **כל** קוד חדש ולא רק על האזור שנבדק.
- ⛔ **ולכן אין לפתוח סבב «שחזור בדיקות» יזום** — החלטת המנהל. מה שכן חל:
  סבב שנוגע באזור כזה **משחזר את רתמת האזור באותו סבב**, מול השינוי שהוא
  עושה ולא מול הקוד ההיסטורי.

**כאן:** ⭐ מנוע המיזוג כוסה בסבבים 37–38 — `test_merge_pending.mjs`
(הכרעת ההתנגשות והגנת ה-⏳) ו-`test_merge_core.mjs` (הליבה המשותפת
וידיות המדיניות). ⚠️ **ועדיין לא מכוסים:** `mergeSubs`, גריעת ה-tombstones
ו-`migrateLegacyLsKeys()` — שלושתם ייכסו בסבב שייגע בהם.

## פערים פתוחים (yoman-avoda)
- **⛔ הקוד עדיין מכיר את הטבלה הישנה `tb_archive`** — `TB_ROW_TABLES`
  ממפה אותה, והמשפך עדיין יודע לקרוא ממנה. ⚠️ הכתיבה אליה **כובתה
  בסבב 35** (`TB_ARC_LEGACY_WRITE = false`), ⛔ אך המיפוי נשאר כנתיב
  חזרה. **הטריגר:** אחרי שהמנהל מחק את הטבלה מהמסד — הסרת המיפוי,
  בסבב נפרד מהמחיקה (סבב 30).
  **נפתח:** סבב 35 (התאריך אינו רשום בקובץ); ⚠️ נמדד מחדש 2026-08-28 והמיפוי עדיין בקוד.
  [מדיד: code:TB_ROW_TABLES = { tb_entries: 'tb_entries', tb_archive: 'tb_archive' }]
- **מחיקת `tb_archive` הישנה ומפתחות ה-`kv` של היומן מהמסד** — פעולת
  מנהל, אחרי חלון של עבודה תקינה על הטבלאות בלבד. ⛔ הכיבוי והמחיקה
  בשני סבבים נפרדים (סבב 30) — אחרת אין חלון לגלות טעות.
  ⚠️ **וזה החצי שאינו מדיד, ⛔ בעוד שהחצי הראשון כן** — קיום מפתח
  ב-Supabase אינו נראה מהריפו, אבל הקוד שקורא אותו כן.
  **נפתח:** סבב 35 (התאריך אינו רשום בקובץ); ⚠️ נמדד מחדש 2026-08-28.
  [לא-מדיד: קיום מפתחות ה-`kv` הוא מצב מסד, ואינו נראה מהריפו]

## סבב 67 (2026-08-28) — היישור הגדול: שכבת הקלט, שם המבחן, ו-`data-id`

⚠️ **סבב אחידות שנוגע גם בקוד הרץ.** ⛔ לא הורצה שום מיגרציה ולא נמחק אף
נתון מהמסד.

### מה נעשה
- **⭐⭐ שכבת הקלט נסרקה לראשונה** — 139 שדות, ⛔ ואף אחת מ-46 שורות
  המטריצה לא תיארה אותה. ⭐ מהמדידה נולד **כלל ברזל 26**: דפוס אחד
  לשדה מספרי · ארבעת מאפייני שדה הסיסמה · קישור כל שדה ל-label · מגן
  שליחה כפולה · ו⛔ נקודת כניסה **חיה** ליכולת שמסומנת ✅.
- **⭐⭐ ה-probe שאוכף את הסעיף האחרון תפס שלוש הפרות מיד** — מסך שינוי
  הסיסמה בהנהלה היה **קוד מת** (הפונקציה שם, בלי אף קורא ובלי שדות
  ב-DOM), ושני probes נוספים הסתפקו בקיום פונקציה.
- **⭐ שם המבחן נגזר מהנושא** — 164 קבצים שונו מ-`test_round<N>_<נושא>`
  ל-`test_<נושא>`, ומספר הסבב עבר לשורת הכותרת שבגוף הקובץ.
- **⭐ `data-id` ודלגציה — כלל ברזל 27.** ⚠️ זו האי-אחידות שהולידה את כל
  באגי סבב 64. ⛔ 18 אתרים הומרו, כל אחד אחרי מדידת מסלול הקליטה שלו.
- **⭐ 183 בלוקי הערה באנגלית תורגמו** — סבב 65 מדד והשאיר, מפני
  שבחירת שפה הוגדרה כהחלטת מנהל; ⭐ הסבב הזה הכריע אותה. ⛔ חריגה
  מנומקת אחת: ה-`javadoc` שב-`android/`.
- **⭐ שער חדש לסט הקבצים** — `check-structure` אוכף מבנה **בתוך** ריפו
  אחד, ⚠️ ולכן קובץ שנעלם משלושה ונשאר באחד עבר בו במלואו.

### ⭐ טבלת הממצאים — ⛔ מה נעשה, או «לא נעשה, ומדוע»
| # | הממצא | מה נעשה |
|---|---|---|
| 1 | `changeMyPassword` היה קוד מת | מסך חי + שדה אימות; ה-probe תוקן |
| 2 | שני דפוסי שדה-מספר | הוכרע `type=text`+`inputmode`; 7 שדות הומרו |
| 3 | שלושת שדות הכמות ביומן | `inputmode="numeric"` |
| 4 | `gregDateInput` בלי pattern | `pattern`; היעדר `inputmode` מנומק |
| 5 | `autocomplete` חלקי | הושלם בכל שדות הסיסמה |
| 6 | 4 שדות סיסמה `type=text` בגיוס | הומרו, ולשניים נוסף מתג 👁 |
| 7 | אפס `aria-label` ואפס `for=` | כל 139 השדות מקושרים |
| 8 | `saveUser` בלי מגן שליחה כפולה | `ysBusy`, בשתי הפונקציות |
| 9 | `um-curr-pass` בלי `inputmode` | נימוק כתוב במקום |
| 10 | שכבת הכניסה נבדלת | גיוס קיבלה החלפת משתמש; שכר מנומקת |
| 11 | `manifest` של יומן — 2 אייקונים | יושר; שער + סעיף בכלל 25 |
| 12 | 53 מבחנים בשמות סבבים | 164 קבצים שונו + שער |
| 13 | 19 מבחנים בלי מוטציה | מוטציה ומוטציית-נגד לכל אחד + שער |
| 14 | רצפה מיושנת ב-`test_ids_yoman` | ספירה מדויקת במקום `>= 6` |
| 15 | שתי שורות פער «לא-מדיד» ביומן | אחת פוצלה ונעשתה מדידה |
| 16 | `onclick` מוטבע | 18 אתרים ל-`data-id`; 29 נרשמו כפער |
| 17 | מאזין לכל אלמנט | דלגציה, יחד עם 16 |
| 18 | ארבעה דפוסי מודאל | ⛔ הוכרע ולא הומר — ר' «פערים פתוחים» |
| 19 | קריאה ישירה מ-localStorage | 32 אתרים ל-`lsGet`; אפס נותרו |
| 20 | משמעת שגיאות | ⚠️ המדידה לא שוחזרה; שתי קריטיות תוקנו |
| 21 | באנרי `─` ברוחב לא אחיד | ⚠️ כבר נאכף מסבב 65 — אומת 78 |
| 22 | תחולת `check-comments` | ⚠️ כבר חלה מסבב 65 — אומת |
| 23 | 2 הערות מיושנות בגיוס | ⚠️ כבר נמחקו בסבב 65 — אומת |
| 24 | 32 מחלקות CSS מתות | ⚠️ 30 נמחקו בסבב 65; 2 נותרו ונמחקו |
| 25 | 278 הערות באנגלית | 183 בלוקים תורגמו |
| 26 | מספר ← שם קבוע | `TOAST_DEFAULT_MS` בשלושה ריפו |
| 27 | קובץ שקיים באחת | שער `test_filesets` + סעיף בכלל 14 |

⛔ **וארבעה ממצאים לא נעשו כפי שנמסרו, ⛔ וזו מדידה ולא השמטה:**
⚠️ **18** — ההמרה היא הכול-או-כלום ודורשת סבב משלה; ההכרעה נרשמה,
וההמרה נרשמה כפער מדיד בשלושת הריפו.
⚠️ **20** — המספרים שנמסרו לא שוחזרו: `catch` ריק ל-1000 שורות הוא
7.9 · 10.1 · 10.5 · 8.3, ⛔ כלומר הארבע דומות.
⚠️ **21–24** — רובם כבר בוצעו בסבב 65; מה שנותר נמדד ונסגר.

### מה נעשה כאן (yoman-avoda)
- ⭐⭐ **כאן ישבו שבעת אתרי ההעברה שהומרו ראשונים**, וביניהם השניים
  המסוכנים: `editEntry` איתרה את שורת הרשומה ב-`querySelector` על
  **מחרוזת ה-`onclick`** — ⛔ בורר כזה מפסיק למצוא דבר ברגע שהמאפיין
  יורד, והעריכה הייתה מתה בשקט. שניהם הומרו באותו קומיט.
- ⭐ `manifest.json` יושר לדפוס האחיות (⛔ `any` ×2 ו-`maskable` נפרד),
  ⛔ ושורת הפער בת 19 השורות שתיארה זאת נמחקה — הטריגר שלה («סבב
  שנוגע בקוד הרץ ומקדם גרסה») התממש כאן.
- ⭐ 116 בלוקי הערה תורגמו — הגבוה בארבעה.
- ⚠️ הפער היחיד שנותר היה «לא-מדיד», ולכן מנגנון העוגן לא רץ עליו
  כלל; הוא פוצל, והחצי שבקוד הוא מעכשיו מדיד.
- `CACHE_NAME` קודם ל-`yoman-avoda-v46`, `app-version` ל-`40-2026-08-27-1`,
  ו-`versionCode` ל-12.
- **גיזום:** פרק סבב 65 נמחק (חלון שני הסבבים).

### אימותים
- `node tools/check-js.mjs` עבר מלא בארבעת הריפו.
- `test_inputlayer` — 11 טענות; `test_filesets` — 5 טענות;
  `test_ids_yoman` — 30 טענות (הרצפה `>= 6` הוחלפה בספירה מדויקת).
- ⛔ לא הורצה שום מיגרציה, ⛔ ולא נמחק אף נתון מהמסד.

## סבב 68 (2026-08-28) — סגירת פערי סבב 67

⚠️ **סבב סגירת פערים שנוגע גם בקוד הרץ.** ⛔ לא הורצה שום מיגרציה ולא נמחק
אף נתון מהמסד.

### מה נעשה
- **⭐ מוטציית-נגד לחמשת המבחנים שנותרו בלעדיה** — `test_devid` ·
  `test_gaps` · `test_matrix` · `test_passwords` · `test_swcore`.
  ⚠️ **המדידה תוקנה:** מעשרת המבחנים שנמסרו, **חמישה כבר החזיקו** אחת
  (`filesets` · `read` · `signscript` · `sources` · `structure`).
- **⭐ `stopPropagation` הוכרע כ**חריגה מנומקת קבועה**, ⛔ ולא כפער** —
  ⚠️ אין אירוע שיהפוך את ההמרה לנכונה: היפוך סדר הבועות הוא תכונה של
  הפלטפורמה. ⛔ הערה צמודה ליד כל אתר, וסעיף בכלל ברזל 27.
- **⭐ שער חדש: `check-capabilities` שורה 45 מפילה אתר שהומר בכל זאת** —
  שורת מקור שנושאת גם `data-act=` וגם `stopPropagation`.
- **⭐ הכפלה מוקדמת באלפא — סעיף בכלל ברזל 25 ושתי טענות בשער.**
  ⛔ הדיו מוצהר ב-`APP.fgInk` ⚠️ ואינו נגזר מהתמונה, שהיה מאשר כל סטייה
  בדיעבד; עם מוטציה ומוטציית-נגד.
- **⭐ היסטוריית הגרסאות ירדה מהערות `build.gradle`** (כלל ברזל 21) —
  ⚠️ בלוק ההערות ביומן מנה עד סבב 7 בזמן שהערך היה 13. ⛔ ההיסטוריה
  יושבת בטבלה שב-`android/README.md`, ושער ב-`test_android` אוכף זאת.
- **⭐ מבניות המבחנים — `APP.testsOnly` מוצהר ב-`test_filesets`.**
  ⛔ הפטור הגורף על `tools/test_` הוסר: כל מבחן שאינו בסט המשותף נושא
  שורה עם הסיבה, ⚠️ ו-⏳ מסמן מבחן מעבר שנושא טריגר להסרה בכותרתו.
- **⭐⭐ שכבת המודאל — שלוש מארבע בדפוס שנבחר.** ⛔ יומן ושכר הומרו
  למיכל אחד ול-`openModal(title, body, foot)`; ⚠️ **והמדידות שנמסרו
  תוקנו:** ביומן היה **דיאלוג אחד** ולא שמונה, ובשכר **שניים** ולא
  חמישה. ⛔ הנהלה **לא הומרה** — 26 אתרי `overlay` דינמיים, ר' למטה.
- **⭐ שורה 49 נוספה למטריצה — «שכבת המודאל»**, ✅ ❌ ✅ ✅, ⛔ עם probe
  שמאמת **חתימה** ולא קיום שם: אותו שם נשא כאן שתי משמעויות.

### ⭐ טבלת עשרת הסעיפים — ⛔ בוצע, או «לא בוצע ומדוע»
| # | הסעיף | מה נעשה |
|---|---|---|
| 1 | מוטציות לעשרת המבחנים | 5 נוספו; 5 כבר החזיקו — נמדד |
| 2 | onclick עם מזהה ביומן | 9 הומרו; 2 עם `stopPropagation` נותרו כחריגה |
| 3 | שורות הפער — טריגר ותאריך | ⭐ לכולן טריגר; חמש קיבלו תאריך פתיחה |
| 4 | הכפלה מוקדמת באלפא | סעיף בכלל 25 + שתי טענות + מוטציות |
| 5 | המודאלים | 3 מ-4 בדפוס; הנהלה נרשמה כפער עם מדידה |
| 6 | `stopPropagation` | חריגה קבועה + הערות בקוד + שער |
| 7 | שורת פער «catch ריקים» | ⛔ **לא הייתה שורה כזו** — נמדד; לא נוסף כלל ולא שער |
| 8 | `catch` ריק סביב כתיבה | 15 תוקנו בהנהלה; ⛔ אפס בשלוש האחרות |
| 9 | מספרי סבב ב-`build.gradle` | הוסרו בארבעתן + שער |
| 10 | מבניות המבחנים | `testsOnly` + טריגרי הסרה + כלל ברזל 14 |

⛔ **וארבע מדידות סתרו את מה שנמסר, ⛔ וזו מדידה ולא השמטה:**
⚠️ **1** — חמישה מהעשרה כבר החזיקו מוטציית-נגד.
⚠️ **4** — «רק ליומן יש שקיפות חלקית» אינו נכון: ⛔ בשלוש האחרות יש
4778 · 3872 · 2248 פיקסלים בעלי אלפא חלקית, ⚠️ ובכולן סטייה מהדיו
(20 · 39 · 50). ⛔ נרשמו כפער עם היתר מוצהר, ולא «יושרו» בשקט.
⚠️ **5** — ספירות הדיאלוגים שנמסרו היו גבוהות פי 4–8 מהנמדד ביומן
ובשכר, ⛔ ולכן שתיהן הומרו במלואן ולא נדחו.
⚠️ **6** — «6 אתרים בהנהלה» הוא **4**; ⛔ ו-26 האתרים הנותרים **אינם**
נושאים `stopPropagation` והם פער אמיתי, ולכן שורת הפער תוקנה ולא נמחקה.

### ⛔ סעיף 7 — מה נמדד, ולמה לא נעשה דבר
⛔ **אין ולא הייתה שורת פער «`catch` ריקים» באף אחד מארבעת הריפו**
(נמדד 2026-08-28) — ⚠️ הממצא ישב ב**טבלת הממצאים של פרק סבב 67** בלבד,
ופרק סבב אינו פרק «פערים פתוחים». ⛔ ולכן לא נמחקה שורה, ⛔ ולא נוסף
כלל ולא שער — בדיוק כפי שנמסר.

### סריקת האזור שנגעו בו (כלל ברזל 17)
1. **באג שקט.** ⭐⭐ נמצא: ⛔ **כל ערך שחוזר מ-`data-*` הוא מחרוזת**, ו-9
   האתרים שהומרו ביומן מעבירים **אינדקסים במערך** (`ci`/`ti`/`si`).
   ⛔ ההמרה ל-`Number` נעשתה **באותו קומיט**; ⚠️ בלעדיה `CATS[ci].tasks[ti]`
   עדיין עובד בזכות ההמרה המשתמעת של JS — ⛔ ונשבר בשקט ברגע שמישהו
   יוסיף שם השוואת `===`.
2. **מה היה אמור להיות משותף.** ⭐ שתיים: **הכרעת המודאל** — שלוש
   מארבע מחזיקות מעכשיו את אותה חתימה, והרביעית מוצהרת ❌; ⭐ ו**הכרזת
   המבחנים הפרטיים**, שהייתה עד היום פטור גורף.
3. **התיעוד באזור.** ⚠️ שלוש שורות פער תיארו מציאות שהשתנתה — ספירת
   ה-`onclick` בהנהלה, ספירות המודאלים ביומן ובשכר. ⛔ תוקנו לפי מדידה,
   ⛔ ולא נמחקו על סמך הנחה (כלל ברזל 16).

### מה נעשה כאן (yoman-avoda)
- ⭐⭐ **כאן ישבו תשעת אתרי ה-`onclick` שהומרו** — `removeTask` ·
  `addTask` · `removeSub` · `addSub` · ארבעה כפתורי ניווט בארכיון ובפירורי
  הלחם. ⚠️ **ושם המשימה עבר ל-`data-task`** במקום `safeT`, ⛔ שהיה בורח
  מגרשים ידנית — בדיוק הציטוט שיכול להישבר.
- ⛔ **ושני אתרים לא הומרו בכוונה** — `editSubInline` ו-`editTaskInline`
  נושאים `event.stopPropagation()` באותו מאפיין; ⚠️ הערה צמודה מסבירה
  למה, ו-`safeT`/`safeS` נשארים בשבילם בלבד.
- ⭐⭐ **שכבת המודאל הומרה במלואה** — ⚠️ **ונמדד שהיה כאן דיאלוג אחד
  (`showConfirm`) ולא שמונה**: שאר ה-`createElement` הם באנרים וטוסטים,
  לא דיאלוגים. ⛔ `showConfirm` נשארה כשם הקורא ⚠️ והיא מעטפת דקה מעל
  `ask`, ולא מסלול שני.
- ⭐ **שורת הפער של המודאל נמחקה** — השער `check-gaps` הוא שהודיע
  שהתנאי `no-code:function openModal` חדל להתקיים.
- ⚠️ **הרקע כאן עבר את טענת ההכפלה המוקדמת בסף המשותף 8** (סטייה 3) —
  ⭐ התיקון שהמנהל העלה ב-27.8 מאומת מעכשיו בשער.
- `CACHE_NAME` קודם ל-`yoman-avoda-v48`, `app-version` ל-`42-2026-08-28-2`,
  ו-`versionCode` ל-14.
- **גיזום:** פרק סבב 66 נמחק (חלון שני הסבבים).

### אימותים
- `node tools/check-js.mjs` עבר מלא בארבעת הריפו.
- `test_iconlayer` — 25 טענות (שתיים חדשות + מוטציה ומוטציית-נגד);
  `test_filesets` — 7; `test_devid` · `test_gaps` · `test_matrix` ·
  `test_passwords` · `test_swcore` — כולם עם מוטציית-נגד מעכשיו.
- ⛔ לא הורצה שום מיגרציה, ⛔ ולא נמחק אף נתון מהמסד.
