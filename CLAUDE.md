# יומן עבודה — CLAUDE.md

עודכן לאחרונה: סבב 32 · 2026-08-16

## סביבת עבודה
- **ריפו:** `ygtotlrl-lab/yoman-avoda`
- **Pages:** `https://ygtotlrl-lab.github.io/yoman-avoda/`
- **קובץ ראשי:** `index.html`
- **Supabase:** `kxbtskqobynewvnckaaz`
- **אופן העבודה (נכון ליולי 2026):** העבודה מתנהלת בסשני ענן (Claude Code cloud) — הריפו משוכפל טרי בתחילת כל סשן, העבודה נעשית על ענף ייעודי, והדחיפה בסוף הסשן. **אין עותקים מקומיים קבועים.**
- **עבודה מקומית במחשב (אם בכל זאת):** שכפול **אך ורק** לתיקייה יציבה כמו `C:\Users\F\Documents\repos\` — **לעולם לא ל-Temp** (מנקה Windows / Storage Sense מוחק משם קבצים באמצע עבודה; זה גרם ל"מחיקות רפאים" חוזרות). טוקן: מאוחסן ב-Windows Credential Manager (host `github.com`) — לעולם לא בקובץ; `git push`/`clone` מושכים אותו אוטומטית דרך GCM.

## גישת Supabase
כשזמין ה-Supabase MCP, נהג לפי הכללים הבאים — ללא יוצאים מן הכלל:
- **שינויי סכימה** (יצירה/שינוי/מחיקת טבלאות, עמודות, פוליסות, הרשאות) — **אך ורק דרך `apply_migration`** עם שם ברור ותיאורי (למשל `add_sync_log_table`). לא דרך `execute_sql`.
- **שאילתות אבחון וקריאה** (SELECT, בדיקת מבנה, ספירות, `list_tables` וכו') — **חופשיות**, ללא אישור.
- **עדכון או מחיקת נתונים בטבלאות `kv`** (כאן: `kv_rishon` / `kv_ramataviv`, וכל טבלת נתונים אחרת) — **מחייבים אישור מפורש מהמשתמש לפני הרצה**. אין להריץ `UPDATE`/`DELETE`/`upsert` על נתונים בלי אישור.

<!-- SHARED:start id="branch-rules" -->
## כללי ענפים
- כל עבודה נעשית על **ענף ייעודי** — לא ישירות על `main`.
- **מיזוג ל-`main` רק לאחר אישור מפורש מהמשתמש**, אחרי שבדק את השינוי בבדיקה חיצונית.
- **מחיקת ענפים מרוחקים חסומה בסביבת הענן** — אין לנסות למחוק; לדלג ולדווח למשתמש שהענף נותר.
- **ענפים ממוזגים נמחקים אוטומטית אחרי מיזוג** (`.github/workflows/cleanup-merged-branches.yml`, רץ ב-push ל-`main`); ההגנה — prefix `claude/` + בדיקת `merge-base --is-ancestor`.

### ⚠️ שני כללי תפעול (סבב 21) — שניהם נלמדו מכשל אמיתי בסבב 20

**1. לפני כל מיזוג — מרעננים את `main` המקומי, ורק אז ממזגים.**
```bash
git -C <path> fetch origin
git -C <path> checkout main
git -C <path> merge --ff-only origin/main   # חייב להצליח
git -C <path> merge <ענף-העבודה>
```
⛔ **אין למזג מעל `main` שאינו זהה ל-`origin/main`.** סשן ענן מחזיק עותק
שנוצר ברגע פתיחתו והוא **קופא שם**: כל מה שנדחף ל-`origin/main` מאז — מסבב
מקביל, מסשן אחר, או מהמשתמש — פשוט אינו קיים בעותק הזה. בסבב 20 נמצא
`main` מקומי שפיגר **בשני סבבים שלמים**. `--ff-only` הוא מה שהופך את זה
לגלוי: הוא **נכשל** במקום ליצור קומיט מיזוג שקט שמחזיר את הקוד אחורה.

**2. בעבודה רב-ריפו — כל פקודת git מציינת את הריפו במפורש: `git -C <path> …`.**
בסבב 20 פקודת מיזוג רצה בתיקייה של ריפו **אחר**, בגלל משתנה לולאה שדלף
מבלוק קודם. שם היא הייתה no-op **במקרה** — הענף לא היה קיים באותו ריפו.
⚠️ **וזה בדיוק מה שלא ניתן לסמוך עליו כאן:** שם ענף העבודה **זהה בארבעת
הריפו** (אותה תבנית `claude/<נושא>-<סיומת>`), ולכן פקודה שרצה בתיקייה
הלא-נכונה תמצא שם ענף תקף ותמזג אותו — **מיזוג שקט בריפו הלא נכון**, בלי
שגיאה ובלי סימן. ⛔ **אין להסתמך על `cd` ואין להסתמך על תיקיית העבודה
הנוכחית** — הנתיב נכתב בפקודה עצמה.
<!-- SHARED:end -->

## לפני כל push — חובה: בדיקת תחביר עם node
**כל שינוי בקוד (`index.html` או `sw.js`) חייב לעבור את הבדיקה הזו לפני דחיפה. דחיפה ללא הבדיקה — אסורה.**
בדיקת איזון-סוגריים בלבד אינה מספיקה (עיוורת לשגיאות בתוך מחרוזות). `node --check` = parse מלא אמיתי ב-V8. (node זמין בסביבת הענן.)
```bash
# 1) חלץ את כל ה-JS המוטבע מ-index.html לקובץ
python3 -c "
import io, re
s = io.open('index.html', encoding='utf-8').read()
js = chr(10).join(re.findall(r'<script(?![^>]*src)[^>]*>(.*?)</script>', s, re.DOTALL))
io.open('_app.js', 'w', encoding='utf-8').write(js)
"
# 2) בדיקת parse — חובה ששתי הפקודות יעברו בלי שגיאה
node --check _app.js
node --check sw.js
rm -f _app.js
# 3) אחידות "אזור מצב" (סבב 17, כלל ברזל 7) — קוד זהה + מיקום זהה + חיווט חי
node tools/check-status-area.mjs
# 4) אחידות התיעוד (סבב 18, כלל ברזל 8) — סימוני SHARED, חתימות, שורת העדכון
node tools/check-docs.mjs
# 5) תקן ההערות בקוד (סבב 28, כלל ברזל 11) — כותרות בלוק, ⛔ עם סבב וסיבה,
#    שלושה סימנים בלבד וטרמינולוגיה אחידה
node tools/check-comments.mjs
# 6) אחידות היכולות המשותפות (סבב 30 השלמה, כלל ברזל 12) — ליבה זהה,
#    נקודת הפעלה זהה, ורישום תואם במטריצת היכולות
node tools/check-capabilities.mjs
# 7) בדיקות הסבבים — כלל ברזל 8 סעיף 6 (בדיקות נשמרות כקובץ ורצות שוב)
node tools/test_round30_stage_a.mjs   # גיבוי יומי ויומן פעולות (מודול משותף)
node tools/test_round30_stage_b.mjs   # שכבת השורות בענן — שקילות ומיזוג
node tools/test_round31_archive.mjs   # תצוגת הארכיון ומקור הרשומות האוטומטיות
node tools/test_round32_unify.mjs     # איחוד הארכיון — טבלה אחת ודגל archived
```
אם `node --check` מדווח שגיאה — אסור לדחוף עד שהיא מתוקנת.
אם `check-status-area.mjs` נכשל — ר' הפרק «כלל ברזל 7» למטה. שינוי בבלוק
משותף מחייב עדכון בארבע האפליקציות ובארבעת עותקי קובץ הבדיקה, באותו סבב.
אם `check-docs.mjs` נכשל — ר' הפרק «כלל ברזל 8» למטה. אותו כלל חל על פרק
תיעוד מסומן `SHARED`: משנים בארבעתם ובארבעת עותקי הבדיקה, באותו סבב.
אם `check-comments.mjs` נכשל — ר' הפרק «כלל ברזל 11» למטה. ⛔ התקן חל על
הקוד הפרטי בלבד; הבלוקים המשותפים והמודולים הקפואים מוחרגים ממנו במפורש.
אם `check-capabilities.mjs` נכשל — ר' הפרק «כלל ברזל 12» למטה. ⛔ נקודת
ההפעלה של יכולות העלייה היא **פונקציית העלייה של האפליקציה** — זו שקוראת
גם ל-`lsBoot()` וגם ל-`pendBoot()` — ואין להזיז אותה למסלול אחר.

## עדכוני Service Worker
- **כל שינוי בקוד מחייב קידום `CACHE_NAME` ב-`sw.js` לגרסה הבאה** — בלי זה המשתמשים לא יקבלו את העדכון.
- **וגם קידום `<meta name="app-version">` בראש `index.html`** — זה מזהה הגרסה שמנגנון האוטו-אפדייט משווה מולו (החליף את `simpleHash`, שרץ על 5000 התווים הראשונים ולכן תמיד החזיר אותו ערך והבאנר לא הופיע לעולם). בלי קידום — הבאנר לא יופיע.
- מנגנון באנר "גרסה חדשה זמינה" קיים באפליקציה — המשתמשים מקבלים את העדכון בלחיצה על הבאנר.
- בנוסף קיים מנגנון אוטו-אפדייט מבוסס `raw.githubusercontent` (`GITHUB_FILE`/`RAW_URL` ב-`index.html`) עבור מכשירים עם ה-APK — ראה כלל קריטי 5. הבדיקה רצה **פעם בשעה**, עם `Range: bytes=0-4095` + `cache:'no-store'` + timeout, ו-`sw.js` לא שומר אותה במטמון (קודם: משיכת ~950KB כל 5 דקות, כל אחת נשמרה במטמון תחת `?t=` ייחודי).

## Push
```bash
git add . && git commit -m "תיאור השינוי"
git push -u origin <שם-הענף>   # דחיפה לענף העבודה — לא ל-main
```

## סיום משימה
בסיום כל משימה משמעותית — **עדכן קובץ זה בתמצית** לפני סיום הסשן: מה שונה, מה הוחלט. כך הסשן הבא מתחיל עם תמונת מצב עדכנית.

## כללים קריטיים
1. **בדיקת תחביר עם `node --check` לפני כל push** (הסעיף למעלה) — חובה מוחלטת. כל שינוי ב-`index.html` חייב לעבור חילוץ-JS + `node --check` (וגם `sw.js`). דחיפה בלי זה — אסורה.
2. **קידום `CACHE_NAME` ב-`sw.js` וגם `<meta name="app-version">` ב-`index.html` בכל שינוי קוד** (הסעיף למעלה) — בלי זה העדכון לא מגיע למשתמשים.
3. **smali בלבד** לתיקון URLs ב-APK
4. **cache APK** — תמיד `TS=$(date +%s)` בשם
5. **מקור אמת יחיד = `index.html`** — זה הקובץ ש-Pages מגיש, שאליו מצביע `start_url` במניפסט, ושאותו **מעטפת ה-APK טוענת מהרשת** (מגרסה 2 — ר' הפרק "APK"). כל עדכון קוד נכנס לכאן בלבד. **אסור ליצור קבצי HTML כפולים** (בעבר היה `יומן עבודה.html` — שונה ל-`index.html`). ⛔ **ואסור להחזיר עותק מוטבע ל-`android/app/src/main/assets/`** — עותק `file://` הוא מקור אמת שני *ובנוסף* origin אחסון נפרד, כלומר localStorage אחר. **`GITHUB_FILE` במנגנון האוטו-אפדייט חייב להתאים לשם הקובץ האמיתי בריפו** (`index.html`) — אם הוא מצביע על שם אחר, `RAW_URL` מקבל 404, האפליקציה לא מזהה גרסה חדשה, ועדכונים לא מגיעים למכשירים מותקנים.
6. **חתימת APK: רק עם `signing/yoman.keystore` (alias `yoman`, pass `yoman123`)** — המפתח הקבוע. לעולם לא ליצור keystore חדש, אחרת APK עתידי לא יתקין מעל הקיים.
7. **המעטפת היא WebView מקורי (`android/`) — לעולם לא TWA ולא PWABuilder.** TWA מריץ את האתר בתוך כרום, וסינון התוכן במכשירים של המשתמשים חוסם את כרום. ר' הפרק "APK — מעטפת WebView מקורית".

## חתימת APK — מפתח קבוע (לעולם לא משתנה!)
- **Keystore בריפו:** `signing/yoman.keystore` (PKCS12, RSA 2048, תקף עד 2053)
- **alias:** `yoman`
- **storepass / keypass:** `KEYSTORE_PASS_IN_MEMORY` = `yoman123` (זהה לשניהם)
- **SHA256:** `29:F5:0B:29:60:79:0B:77:28:25:7C:88:79:12:31:28:7A:B8:F1:D9:3E:90:B6:3B:50:F4:1E:41:B9:FA:F8:B5`
- **קריטי:** כל APK חדש נחתם **אך ורק** עם המפתח הזה כדי שיתקין מעל הקיים בלי הסרה. לעולם לא ליצור keystore חדש.
- חתימה (כשיש Android SDK / apksigner):
  ```bash
  apksigner sign --ks signing/yoman.keystore --ks-key-alias yoman \
    --ks-pass pass:yoman123 --key-pass pass:yoman123 app.apk
  ```
- חלופת jarsigner (אם אין apksigner):
  ```bash
  jarsigner -keystore signing/yoman.keystore -storepass yoman123 -keypass yoman123 app.apk yoman
  ```
- **שים לב:** המפתח הקודם (`/tmp/yoman.keystore`) אבד; המפתח הקבוע החדש מחליף אותו. ההתקנה הראשונה של APK חתום במפתח החדש דורשת **הסרה חד-פעמית** של האפליקציה הישנה (אי-התאמת חתימה); מאז — קבוע לתמיד.

## APK — מעטפת WebView מקורית (⛔ לא TWA)
המעטפת היא פרויקט אנדרואיד מקורי ב-`android/` — `Activity` יחידה עם `WebView`
(`com.yoman.avoda.MainActivity`). תיעוד מלא ב-`android/README.md`.

### ⛔ אין להחליף ל-TWA ואין לבנות מחדש ב-PWABuilder
TWA (Trusted Web Activity) אינו רכיב עצמאי — הוא מריץ את האתר **בתוך כרום**
ורק מסתיר את שורת הכתובת. **סינון התוכן שמותקן במכשירים של המשתמשים חוסם את
כרום**, ואיתו נופל כל TWA. `WebView` הוא מנוע רינדור שרץ בתוך תהליך האפליקציה
ואינו עובר דרך כרום, ולכן הסינון לא נוגע בו.

זה לא תיאורטי: gius נבנתה ב-PWABuilder כ-TWA ופשוט **לא נפתחה** אצל המשתמשים,
בעוד yoman ו-hanhala (שתיהן WebView) עובדות. גם gius הומרה מאז ל-WebView.
**PWABuilder יודע לייצר רק TWA** — הבנייה היא מ-`android/`, דרך
`.github/workflows/build-apk.yml` או Gradle מקומי.

> תיעוד קודם כאן המליץ על "TWA דרך PWABuilder" בשביל `navigator.share({files})`.
> **ההמלצה הזו שגויה ובוטלה** — היא הייתה מחליפה מעטפת עובדת במעטפת חסומה.
> השיתוף פתור ממילא בגשר מקורי, ולא דרך `navigator.share`.

### פרטי המעטפת (⭐ versionCode 2 — טוענת מהרשת)
- אייקון: `res/mipmap-*/ic_launcher.png` (לוח משימות + גרף, מ-`icons/icon-512.png`)
- **מה המעטפת טוענת:** `https://ygtotlrl-lab.github.io/yoman-avoda/` — **מהרשת**,
  בדפוס של gius. **שחרור קוד web כבר לא מצריך APK חדש**: `location.reload()`
  על דף https מביא באמת את הקוד החדש, ומנגנון האוטו-אפדייט + ה-service worker
  עובדים בדיוק כמו בדפדפן. APK חדש נדרש רק כששינוי נוגע במעטפת עצמה.
- ⛔ **אין נכסים מוטבעים, ואין להחזיר אותם.** ר' הפרק «מעבר לטעינה מהרשת».
- **ניווט:** כל `http`/`https` **נשאר בתוך ה-WebView** — הפניה לדפדפן חיצוני
  הייתה מגיעה לכרום, שהסינון במכשירי המשתמשים חוסם. שאר הסכימות (`tel:`,
  `whatsapp:`, …) נמסרות למערכת, וזה מה שמפעיל את מסלול הנפילה-חזרה של השיתוף.
- **שיתוף תמונות — גשר מוגבל לדומיין.** ר' הפרק «הגשר המקורי» למטה.
- בורר קבצים: `WebChromeClient.onShowFileChooser` מחובר ל-`<input type=file>`.
- תיקוני URL ב-APK **קיים ובנוי** (בלי מקור): smali בלבד, לא binary patch.

## בנייה — דורש כלים שאינם זמינים בכל סביבה
לבנייה מקומית צריך Android SDK (`aapt2`, `d8`, `apksigner`/`zipalign`).
בסביבת הענן הם אינם מותקנים, ו-`dl.google.com` חסום — לכן הדרך המעשית היא
**`.github/workflows/build-apk.yml`**: Actions → Build Signed APK → Run workflow.
ה-workflow בונה וחותם עם `signing/yoman.keystore`. ⛔ **שלב "copy web assets"
הוסר ואין להחזיר אותו** — המעטפת טוענת מהרשת.
**לא להשתמש ב-PWABuilder** — הוא מייצר TWA (ר' האזהרה למעלה).

## מצב נוכחי
- ייצוא PDF ושיתוף וואטסאפ (השיתוף מייצר JPEG דרך `_buildReportDiv`/`_renderReport`).
  **אין יותר כפתור "הורד JPEG" עצמאי** — `exportJPEG` הייתה קוד מת והוסרה בסבב 7.
- ארכיב ועריכה inline
- Hebrew calendar עם leap year
- Supabase sync (polling 3 שניות)
- PWA מותקן (sw.js, `CACHE_NAME` נוכחי: `yoman-avoda-v26`; `app-version` נוכחי: `20-2026-08-16-1`)
- **סנכרון: מיזוג ברמת רשומה** (`updatedAt` + tombstones) — ראה סבב 3ב למטה


## מוסכמת הודעות קומיט (סבב 8 — משותפת לשלושת הפרויקטים)
**עברית חופשית**, לא conventional-commits. זה הדפוס שהתגבש בפועל בשלושת הריפו,
ומעכשיו הוא ההחלטה המוצהרת:
- שורה ראשונה: תיאור תמציתי בעברית של מה השתנה (עד ~72 תווים).
- עבודה מרוכזת מקבלת קידומת `סבב N:` (למשל `סבב 8: אחידות בין שלושת הפרויקטים`).
- בלי קידומות `feat:`/`fix:` באנגלית.

### סבב 9 (אוגוסט 2026, `sw.js` v13, `app-version` 7-2026-08-03-1) — מטמון חוצה-פרויקטים
**שלוש האפליקציות חיות על אותו origin (ygtotlrl-lab.github.io) וחולקות CacheStorage אחד.**
ה-activate של כל SW מחק כל מטמון שאינו ה-`CACHE_NAME` שלו — כלומר כל אפליקציה השמידה
את המטמונים של אחיותיה (אומת חי: ביקור כאן רוקן את מטמון hanhala מ-11 רשומות ל-0,
כולל סקריפטי ה-CDN). התיקון — בשלושת הפרויקטים באותו סבב:
1. **סינון מחיקה לפי קידומת:** ב-activate נמחקים רק מטמונים שמתחילים ב-`'yoman-avoda-'`
   (וכן `'schar-limud-'`/`'hanhala-ruchanit-'` אצל האחיות). **אין להסיר את הסינון** —
   כל SW חדש על ה-origin הזה חייב לסנן לפי קידומת משלו.
2. **`ensureCdnCached()` — ריפוי עצמי של מטמון ה-CDN.** עד היום `CDN_ASSETS` נשמרו רק
   ב-install; אם חסרו (נמחקו ע"י אחות לפני התיקון, או כשל רשת רגעי בהתקנה) — שום מנגנון
   לא השלים אותם: בזמן-ריצה הדף טוען אותם כ-no-cors ⇒ opaque ⇒ `networkFirst` מסרב
   לשמור. (ההערה הישנה ב-install שטענה שהם "נשמרים שוב ב-fetch הראשון" הייתה שגויה —
   תוקנה.) הפונקציה רצה ב-activate וגם פעם אחת בכל עליית SW, משלימה ב-`mode:'cors'`
   רק את מה שחסר, וכשל בה שקט. כך מכשירים שנפגעו מתרפאים לבד בלי לחכות לעדכון גרסה.

### סבב 8 (אוגוסט 2026, `sw.js` v12, `app-version` 6-2026-08-02-1) — אחידות בין שלושת הפרויקטים
יישור מוסכמות בלבד — בלי שינוי לוגיקה. **לא נגענו במנוע המיזוג.**
1. **`sw.js`:** `urlsToCache` → **`CORE`** (השם המשותף; שינוי שם בלבד).
2. **`showToast(msg)` → `toast(msg, dur)`** — השם והחתימה המשותפים לשלושת הפרויקטים.
   קודם פרמטר המשך נבלע — שני קוראים העבירו 3000/2000 והם מכובדים עכשיו. נוסף גם
   `clearTimeout` פנימי כך שטוסט חדש לא נסגר מוקדם ע"י הטיימר של הקודם.
3. **`esc()` גלובלי + הוחל** — היה מוגדר מקומית (בתוך עריכת inline) ולא נקרא כלל.
   עכשיו: פונקציה גלובלית (זהה ל-schar/hanhala) שהוחלה על כל ערך משתמש שנכנס
   ל-`innerHTML` — שורות היומן (משימה/תת-משימה/הרחבה/קטגוריה), טבלאות ה-PDF
   והשיתוף, מסך ההגדרות (שמות קטגוריות/משימות/תתי-משימות), פירוט הארכיון ועריכתו.
4. **עוזרי רשת משותפים:** `isNetErr` / `withTimeout` (8ש׳) / `guardOnline` / `errMsg` —
   לא היו כאן כלל. `withTimeout` הוחל בנקודת החנק `sbSet`/`sbGetResult` (כל קריאה
   שהמשתמש ממתין לה), והזריקה נתפסת ב-catch הקיים והופכת ל-`{ok:false}`.
5. **`getDeviceId`** נוסף (מוקשח מול localStorage חסום; מפתח `tb_device_id`,
   בלי סיומת LS — מזהה מכשיר, לא מוסד).
6. **הודעות:** `MSG_OFFLINE`/`MSG_SAVED`/`MSG_BAD_LOGIN` זהים מילה במילה לשלושת
   הפרויקטים; סמלים ✅/⚠️/❌ תמיד בתחילת ההודעה (`✓` הוסר).
7. **manifest.json:** נוסף `scope: "./"` בלבד — `start_url` נשאר `./index.html` (כלל 5).

### סבב 7 (יולי 2026) — ניקיון וחוב טכני, ענף `claude/cleanup-tech-debt-three-projects-9feqdh`
**לא נגענו במנוע המיזוג של סבב 3ב.**
1. **גרסאות CDN נעוצות** — `@supabase/supabase-js@2` ו-`@hebcal/core@4` היו טווחים צפים:
   שחרור בצד הספק שובר את האפליקציה בלי שינוי קוד, ובלתי ניתן לשחזור מהריפו.
   נעוצות עכשיו ב-**`@2.111.0/dist/umd/supabase.js`** ו-**`@4.5.1/dist/hdate-bundle.min.js`**
   (`html2canvas@1.4.1` כבר היה נעוץ), **גם ב-`index.html` וגם ב-`CDN_ASSETS` שב-`sw.js`** —
   הן חייבות להישאר זהות, אחרת ה-SW שומר במטמון בנייה שהדף לא מבקש.
   ⚠️ נעצנו למה ש-`@2`/`@4` הגישו **באותו רגע**, כדי שלא תהיה שום שינוי התנהגות היום.
   ~~זו גרסה שונה מ-hanhala/schar (שם `2.39.3`)~~ — **יושר בסבב 8**: שלושת הפרויקטים
   על `2.111.0` עכשיו.
   שים לב: `2.111.0` כבר לא כולל `dist/umd/supabase.min.js` בחבילה, ולכן הנתיב הוא
   ה-`.js` הרגיל. שטח ה-API בשימוש כאן קטן: `createClient`/`from`/`select`/`eq`/`single`/`upsert`.
2. **גריעת tombstones ישנים מ-90 יום** (הפריט שנותר פתוח מסבב 3ב) — `tb_entries` צבר
   tombstone לכל רשומה שנמחקה או אורכבה ולעולם לא התנקה (~1.3MB בשנה, נדחף בכל שמירה).
   נוספה **`prunePastTombstones(arr, now)`** + `TOMBSTONE_TTL_MS`. שלושה כללים:
   - **רק `deleted:true`.** רשומה חיה לא נגרעת לעולם, בלי קשר לגיל.
   - **רק `updatedAt` מספרי.** tombstone בלי חותמת **נשאר** — אי אפשר לדעת מתי נמחק,
     ו-`recTs` מחזיר 0 עבורו, כך שגריעה לפי 0 הייתה מוחקת דווקא את הישנים ביותר בלי ראיה.
   - הפונקציה טהורה (לא משנה את הקלט) ומחזירה `[]` על קלט לא-מערך.
   - **מתי היא רצה:** `loadLocalData` גורעת מהמערך המקומי ומרימה דגל חד-פעמי
     **`_tombPrunePending`**, שנצרך במיזוג הראשון שאחריו ב-`syncFromCloud`.
     ⚠️ **הדגל אינו קישוט.** גריעה מקומית בלבד היא חסרת משמעות: `mergeRecords` מחזיר כל
     רשומה שקיימת בענן ואין לה מקבילה מקומית, ולכן ה-tombstones היו חוזרים תוך שניות
     והעותק הענני לעולם לא היה מצטמצם. הגריעה מופעלת על **התוצאה הממוזגת**, פעם אחת
     לכל עליית אפליקציה — **לא בפולינג של 3 השניות** (`autoSyncData` לא נוגעת בזה)
     ו**לא בתוך מנוע המיזוג**.
   - **מחיר מודע** (זהה לזה שכבר תועד במנוע): מכשיר שהיה מנותק יותר מ-90 יום ולא קיבל
     את ה-tombstone עדיין מחזיק את הרשומה כחיה, וכשיסתנכרן היא תחזור **פעם אחת**.
3. **קוד מת הוסר — 209 שורות.** כל אחת אומתה כלא-נקראת (כולל מ-`onclick` ב-HTML ומחוץ
   ל-`index.html`) לפני ההסרה: `exportJPEG` (**שלוש הגדרות** — הבסיסית ועוד שתי "טלאי"
   שדרסו זו את זו, אף אחת לא נקראה), `archiveCurrent` (גם פנתה ל-`#archiveName` שלא קיים),
   `exportTxt`, `endDay`, `restoreArchive`, `deleteArchive`, `backupYoman`.
   - **הארכוב החי הוא `autoArchiveDay`** — הוא נקרא מ-`addEntry`/`delEntry`/עריכה, וכל
     הלוגיקה של "חותמת אחת לעותק בארכיון ול-tombstone" נמצאת בו. `endDay` הייתה מסלול מקביל מת.
   - `instName`, `renderArchive`, `_buildReportDiv`/`_renderReport` **נשארו** — הן עדיין
     בשימוש (PDF ושיתוף וואטסאפ). הערות שהזכירו את הפונקציות שהוסרו עודכנו.

בדיקות סבב 7: **18 טענות ב-Chromium**, בשני המוסדות — עלייה נקייה בלי שגיאות דף,
כל 7 הפונקציות המתות אכן נעלמו, כל 11 הפונקציות החיות שרדו, `prunePastTombstones` על
שבעה מקרי קצה (tombstone ישן/טרי/בלי חותמת/חותמת לא-מספרית, רשומה חיה ישנה, זבל),
טוהר הפונקציה, וסף 90 היום המדויק (בדיוק 90 יום נשאר, +1ms נגרע). וכן `node --check`
על ה-JS המחולץ ועל `sw.js`. (ה-CDN חסום בסביבת הבדיקה — הוגש כ-stub מקומי, hebcal אמיתי מ-npm.)

### סבב 4 (יולי 2026) — נכונות תאריכים ורב-דיירות, ענף `claude/yoman-avoda-dates-multitenancy-2olt3x`
שמונה תיקונים מדוח ביקורת. **לא נגענו במנוע המיזוג של סבב 3ב.**
1. **אדר א׳ / אדר ב׳** — `MONTH_MAP` מיפה גם 12 וגם 13 לאינדקס 5 ("אדר"), כך שבשנה מעוברת ~59 יום היו קורסים לכפתור חודש אחד (תשפ״ז מתחילה בספטמבר 2026 והיא מעוברת). עכשיו: `isHebLeapYear(y) = ((y*7+1)%19) < 7` (מחזור 19 שנים, לא תלוי בטעינת hebcal); חודש 12 → "אדר א" בשנה מעוברת / "אדר" בפשוטה, חודש 13 → "אדר ב". נבדק מעבר על כל ימות תשפ״ז (13 חודשים: 30+29 יום באדר א/ב) ותשפ״ו (12 חודשים, אדר אחד).
2. **מספר וואטסאפ פר-מוסד** — `tb_wa_phone` נכתב בלי סיומת `LS` ודרס בין הישיבות. עכשיו `waPhoneKey() = 'tb_wa_phone'+LS`, עם `getWAPhone()/setWAPhone()`. **הגירה:** המפתח הישן (בלי סיומת) מועתק פעם אחת למוסד הפעיל ולא נמחק. הצד הענני כבר היה מופרד (`KV_TABLE`).
3. **שם המוסד בפלטים** — נוספה `instName()` כמקור אמת יחיד; `exportTxt` ו-`exportPDF` הפסיקו לקודד קשיח "ראשון לציון". `window.open` ב-`exportPDF` נבדק מול null (חוסם חלונות קופצים) ומציג הודעה במקום ליפול בשקט.
4. **סיכום הדוח** — `_buildReportDiv` בנתה טבלה מרשומות היום אבל ספרה את כל `ENTRIES`. עכשיו הסיכום = `sortedPDF.length` בדיוק (גם ב-`exportPDF`).
5. **"חשון" מול "מרחשון"** — נוצרו שני כפתורי חשוון באותה שנה (בנתונים הקיימים 15 ימי "מרחשון"). נוספו `normHDate`/`normHMonth`; "מרחשון" הוסר מ-HMO. **הנרמול הוא בקריאה/תצוגה בלבד — הנתונים בענן לא משתנים.**
6. **רשומות בלי `gdate` — שחזור במקום השמדה** — בארכיון היו 9 סנאפשוטים בלי `gdate` (203 רשומות), מהם 4 בלתי נגישים לחלוטין (`hdate` בצורת "30 בנוב׳ 2025"). נוסף שלב 0 ב-`arcCleanupCorrupted`: `parseGregLike` משחזרת תאריך מ-`date`/`hdate`/`name`, ה-hdate נגזר מחדש דרך hebcal, ורשומות בלי תאריך יורשות את תאריך הסנאפשוט. מה שלא ניתן לשחזור נשאר נגיש: `extractYM` מחזירה דלי `HUNKNOWN` ("לא ידוע") במקום להשמיט, וניווט הימים עובר לפי `d.key` (סנאפשוט בלי gdate ממופתח לפי id) במקום `d.gdate` הריק. תוצאה נמדדת: 2433 רשומות, נגישות 2230 → **2433**.
7. **מסך ריק** — `loadLocalData` עטופה: `lsRead()` עם `try` + בדיקת `Array.isArray`/object, סינון רשומות זבל, `try/catch` סביב הכל וסביב `startApp()`. ערך פגום כבר לא מונע מהאפליקציה לעלות.
8. **`arcSaveEntry`** — כבר טיפלה בשני המקורות (תוקן בסבב 3ב), אך הציגה "נשמר!" גם כשלא נמצאה רשומה. עכשיו סופרת פגיעות, מדווחת "הרשומה לא נמצאה — לא נשמר" כשאין, מגינה על טופס סגור, ומרעננת גם את היומן.

בדיקות: 82 טענות אוטומטיות ב-Chromium (hebcal אמיתי מ-npm, Supabase/html2canvas מדומים) — מעבר על כל ימות תשפ״ז/תשפ״ו, נרמול חודשים, `parseGregLike`, עליית שני המוסדות בלי שגיאות דף, הפרדת מספר הוואטסאפ כולל הגירה, ספירת ארכיון לפני/אחרי הניקוי (0 רשומות אבודות, אידמפוטנטי), התאמת סיכום הדוח לשורות, עריכת רשומה שקיימת רק ב-ENTRIES, וחמישה ערכי localStorage פגומים. וכן `node --check` על ה-JS המחולץ ועל `sw.js`.

### סבב 4ב (יולי 2026) — באנר עדכון יחיד + שחזור גופנים, אותו ענף
9. **באנר עדכון כפול** — היו שני באנרים שהופיעו יחד: העליון ברוחב מלא (`id="updateBanner"`, מבדיקת הגרסה השעתית מול raw GitHub) והתחתון הקטן (`id="sw-update-banner"`, מ-`updatefound` של ה-service worker). עכשיו **ממשק אחד**: `window.showAppUpdateBanner(version)` מוגדר בסקריפט שבראש `index.html` (מחוץ לבדיקת `'serviceWorker' in navigator`, כדי שגם דפדפן בלי SW יקבל אותו), ושני מנגנוני הזיהוי מפעילים אותו. **בדיקת ה-raw השעתית נשמרה** — היא חיונית ל-APK. הבאנר העליון הוסר לגמרי (הפונקציה נמחקה, לא הושארה כקוד מת); `showUpdateBanner` היא כעת מעטפת דקה.
   - **עותק אחד בלבד:** בדיקת `getElementById('sw-update-banner')` לפני יצירה. אם הבאנר כבר מוצג ורק אז נודעה הגרסה — היא נרשמת על האלמנט (`_updVersion`), כך שה-✕ עדיין שומר `tb_dismissed_version`.
   - **"עדכן עכשיו" בשני המקרים:** יש `reg.waiting` → `postMessage({type:'SKIP_WAITING'})` + reload אחרי 1.5ש׳; אין (הזיהוי הגיע מבדיקת הגרסה) → `reg.update()` ואז reload, עם גיבוי קשיח של 3ש׳ כדי לא להיתקע על "מעדכן...". ה-SW הוא network-first, ולכן טעינה מחדש מביאה את הקובץ החדש גם בלי SW חדש.
10. **קישור גופני Google** — ה-href היה מרוקן (66 רווחים במקום ה-URL) גם ב-`index.html` וגם ב-HTML של `exportPDF`, כך שהאפליקציה והדוחות נפלו לגופני מערכת. שוחזר: `https://fonts.googleapis.com/css2?family=Frank+Ruhl+Libre:wght@400;700;900&family=Heebo:wght@300;400;600;700;800;900&display=swap` (המסך; ב-PDF משקלי Heebo 400;600;700;900), עם `preconnect` ל-googleapis/gstatic.

בדיקות סבב 4ב: 29 טענות נוספות ב-Chromium — באנר יחיד משני הטריגרים, `✕` + `tb_dismissed_version` (כולל גרסה שנודעה אחרי שהבאנר כבר הוצג), "עדכן עכשיו" טוען מחדש בשני המסלולים, עדכון service worker אמיתי (דף מבוקר → הבאנר עולה מעצמו, ואין באנר כשאין שינוי), ה-URL של הגופנים נדרש בפועל ע"י הדפדפן בשתי הנקודות, ושני המוסדות עולים נקי בלי באנר. סה״כ הענף: **111 טענות עוברות**.

### סבב 3ב (יולי 2026) — מנוע מיזוג ברמת רשומה, ענף `claude/yoman-avoda-round-3a-fixes-f5cw9p`
**הכלל היחיד שחשוב:** היעדר רשומה אצל מכשיר אינו מחיקה. מחיקה = `deleted:true` + `updatedAt` (tombstone).

- **מנוע:** `mergeRecords(local, remote, getKey, mergePair)` — הענן (`remote`) הוא בסיס הסדר ומנצח בשוויון (דטרמיניסטי). נגזרות: `mergeEntries` (לפי `id`), `mergeArchive` (סנאפשוט לפי `gdate`, ובתוכו מיזוג רשומות לפי `id`), `mergeCats` (לפי `letter`), `mergeSubs` (פר-מפתח לפי `SUBS_META`). עזרים: `recTs`/`recTouch`/`recDelete`/`isLive`/`liveOnly`.
- **`autoSyncData` ו-`syncFromCloud` ממזגים** במקום להחליף blob שלם — זה היה מקור אובדן הנתונים.
- **נתוני legacy:** רשומה בלי `updatedAt` נחשבת ts=0 ("ותיקה"), אך **לעולם לא נופלת** רק מפני שהצד השני לא מכיר אותה. **אין stamping של `Date.now()` בטעינה** — זה היה גורם למכשיר ישן לנצח נתונים חדשים.
- **מחיר מודע:** רשומה שנמחקה פיזית ע"י הקוד הישן במכשיר אחר עלולה לחזור **פעם אחת** בסנכרון הראשון. עדיף על אובדן נתונים; מחיקה חוזרת יוצרת tombstone אמיתי.
- **"סיום יום"/יום חדש:** העותקים שנכנסים לארכיון וה-tombstones של העותקים החיים מקבלים **אותה חותמת**; בשוויון הסנאפשוט מנצח, ולכן ארכוב אינו מחיקה מהארכיון.
- **גרנולריות:** משימות ותתי-משימות הן מחרוזות בלי מזהה — הן נוסעות עם רשומת האב (קטגוריה / מפתח SUBS), וכל שינוי בהן מקדם את ה-`updatedAt` של האב.
- **`SUBS_META`** — מפה `{מפתח: updatedAt}`, נשמרת ב-localStorage (`tb_subs_meta`+LS) ובענן (`tb_subs_meta`).
- **מוסדות:** נבדק שאין דליפה בין `kv_rishon` ל-`kv_ramataviv`.

בדיקות: 28 טענות אוטומטיות ב-Chromium מול ענן מדומה לשני מכשירים (הבאג המקורי, legacy בלי `updatedAt`, tombstones בשני הכיוונים, שני מכשירים באותו יום בארכיון, סיום-יום, כפילות סנאפשוטים), בדיקת שדרוג מנתוני legacy אמיתיים משני הצדדים (0 רשומות אבודות, 92 ימי ארכיון נשמרו), בדיקת UI מלאה בשני המוסדות, ו-`node --check`.

~~**פתוח לסבב הבא:** ניקוי tombstones ישנים~~ — **בוצע בסבב 7** (ראה למעלה).

### סבב 3א (יולי 2026) — ענף `claude/yoman-avoda-round-3a-fixes-f5cw9p`
חמישה תיקונים שאינם נוגעים בליבת הסנכרון (המיזוג — סבב הבא, **לא נגענו** ב-`autoSyncData`/`syncFromCloud` merge):
1. **באנר עדכון** — `simpleHash` הוסר; ההשוואה היא מול `<meta name="app-version">`. ✕ שומר `tb_dismissed_version` כדי לא להציק שוב על אותה גרסה.
2. **תעבורה** — `checkForUpdates`: פעם בשעה, `Range` ל-4KB הראשונים, `cache:'no-store'`, timeout 10ש׳ (AbortController), ו-`!r.ok` נזרק כדי ש-404 לא ייחשב כאפליקציה.
3. **`sw.js` v7** — שמירת סקריפטי CDN במטמון (`mode:'cors'`, best-effort), פסילת opaque/לא-200, `GET` בלבד, נפילה-חזרה של כל ניווט ל-`index.html`, דף אופליין בעברית (503 + Content-Type), מחיקת מטמון ישן רק אחרי אימות ש-`index.html` נכנס לחדש, אף פעם לא `respondWith(undefined)`, ועקיפה מלאה של `supabase.co` ושל בדיקת הגרסה (`raw.githubusercontent`).
4. **הודעות אמת** — `sbSet` מחזירה `{ok,error}`, נוספה `sbGetResult` (`sbGet` נשארה value-only). `syncFromCloud` מדווחת אילו משיכות נכשלו; `addEntry` מציגה "נוסף — שומר בענן…" ורק `scheduleSyncPush` מכריזה על הצלחה/כישלון אמיתי. `scheduleSyncPush` גם **לא כותבת `tb_last_changed` אם דחיפת הנתונים נכשלה** (אחרת מכשירים אחרים מושכים ענן ישן על נתונים חדשים).
5. **דוחות** — נוספו `_renderReport`/`_reportError`; כל חמש שרשראות html2canvas עם `catch` שמסתיר את הפאנל הלבן ומציג הודעה. כש-html2canvas לא נטען (אופליין) אין יותר ReferenceError ולא נשאר מסך לבן.

בדיקות שבוצעו: `node --check` (JS מחולץ + sw.js), טעינה נקייה בשני מצבי המוסד (ראשון לציון / רמת אביב) ב-Chromium, בדיקת באנר (אותה גרסה / 404 / גרסה חדשה / dismiss), בדיקת הודעות סנכרון בהצלחה ובכישלון, ובדיקת SW מול שרת מת (ניווט עמוק → app shell, נכס חסר → דף אופליין 503).

---

<!-- SHARED:start id="iron-rules-storage" -->
## ⭐ כללי ברזל של הארגון — עמידות אחסון ואופליין (סבב 11, אוגוסט 2026)
**ארבעת הכללים האלה זהים מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius). הם חלים על כל קוד חדש בארגון.

1. **כל כתיבה ל-localStorage עוברת דרך העוזר המוקשח.** `lsSet(key, value)` או
   `lsSetArray(key, arr, tsOf)` — **ואין קריאה ישירה ל-`localStorage.setItem`**
   בשום מקום מחוץ למודול המשותף. `setItem` זורק באחסון חסום או מלא, וכל נתיב
   לא-מוגן מת באמצע: הזיכרון כבר עודכן, המסך מתרענן, ושום דבר לא נשמר.
   - החריג היחיד המותר הוא **`lsSetRaw`** — העוזר של המודול עצמו, שמחזיר
     `false` בלי להרים באנר. הוא שמור לערכים **חסרי-נזק שנכתבים בכל עליית
     דף** (מזהה מכשיר, יומן הפינוי). **אין להשתמש בו לשום נתון של המשתמש.**
2. **אף כישלון שקט.** `lsSet` מחזירה `true`/`false`, מרימה באנר אדום קבוע
   (`#ls-alert`) שנשאר עד שהמשתמש סוגר אותו, ורושמת ליומן.
   - נקודת האכיפה היא שורה אחת בראש `toast()` של כל אפליקציה:
     `if (typeof lsGuardToast === 'function' && !lsGuardToast(msg)) return;`
     — כל הודעה שמתחילה ב-✅ נבלעת אם כתיבה מקומית נכשלה בשתי וחצי השניות
     האחרונות. **אין להסיר את השורה הזו.** בלעדיה חוזר בדיוק המצב שהסבב הזה
     בא למנוע: כתיבה שנכשלה ומשתמש שרואה "נשמר בהצלחה".
   - במסלול קריטי (רישום נוכחות, שמירת תשלום, הוספת רשומה, רישום תנועה) יש
     בנוסף עצירה מפורשת — `lsFails()` / `lsOkSince(t)`, או ערך מוחזר שנבדק.
3. **עבודה אופליין מלאה היא דרישת בסיס בכל אפליקציה בארגון — מהיום הראשון,
   לא תוספת מאוחרת.** ארבעת הרכיבים, כולם:
   עותק מקומי של **כל** הנתונים · קריאה ממנו בעליית האפליקציה, **לפני** כל
   נגיעה ברשת · סנכרון ברקע · מנוע מיזוג **ברמת רשומה** לפי חותמת עדכון,
   עם tombstones. הכלל היחיד שחשוב במנוע: **היעדר רשומה אצל צד אחד אינו
   מחיקה** — מחיקה היא `deleted=true` + חותמת.
4. **ניטור מכסה ופינוי יזום נבנים יחד עם האופליין, לא אחריו.** עותק מקומי
   בלי ניטור הוא פצצת מכסה מושהית: הוא גדל עד שהכתיבה נכשלת. `lsBoot()`
   נקראת בכל עליית אפליקציה, מודדת את **כל** המפתחות בדומיין (לא רק של
   האפליקציה הנוכחית — המכסה משותפת!), מתריעה מעל 3MB ובאנר מעל 4MB,
   ומפנה מיוזמתה מעל 60%.
5. **כל אפליקציה חדשה בארגון נבנית עם התשתית הזו מלכתחילה** — מעתיקים את
   המודול המשותף כמו שהוא, ממלאים `LS_CFG`, מחברים `lsBoot()` בעלייה,
   את רכיב «מידע טכני ›» (`techInfoMount()`) בהגדרות, ואת שורת השער ב-`toast()`.

### המודול המשותף
`index.html` של כל אחת מארבע האפליקציות מכיל את אותו בלוק בדיוק, בין
`/* ═══ עמידות אחסון מקומי — מודול משותף (סבב 11)` לבין
`/* ═══════════════ סוף המודול המשותף`. **הוא זהה בית-לבית בארבעתן**
(474 שורות, sha1 `8345d842b294`). כל שינוי בו חייב להיעשות בארבע באותו סבב.
מעליו יושבת `LS_CFG` — **הדבר היחיד שנבדל בין האפליקציות** — ומתאם
`lsToast(msg, dur)` בן שורה.

**ה-API:** `lsSet` · `lsSetArray` · `lsGet` · `lsRemove` · `lsSetRaw` ·
`lsUsage` · `lsBoot` · `lsSweep`/`lsSweepGuarded` · `lsLog`/`lsLogRead` ·
`lsAlert` · `lsGuardToast` · `lsFails`/`lsOkSince` · `lsStatusHTML`/`lsRenderStatus` ·
`lsHorizon`/`lsClearHorizons`.

### סדר הפינוי — קפדני, ואין לשנות אותו
- **א. מטמונים וערכים שניתן לשחזר מהענן בכל רגע** (`LS_CFG.tier1`), הגדול קודם.
- **ב. ארכיון ורשומות ישנות שכבר מסונכרנות** (`LS_CFG.tier2`), **הישן ביותר
  קודם**, ורק מתחת לרצפת גיל פר-מפתח (`minAgeMs`).
- **ג. ⛔ נתון שטרם הסתנכרן לענן לא מפונה בשום מצב, גם אם המקום נגמר לגמרי.**
  במצב כזה עדיף להיכשל ברעש מאשר למחוק. שתי הגנות נפרדות אוכפות את זה:
  `LS_CFG.pending()` — יש משהו בתור? שלב ב מדולג **לגמרי**; ו-`syncedThrough()` —
  רק רשומות שגילן קודם לדחיפה/משיכה המוצלחת האחרונה נחשבות "בענן".
  ספק ⇐ לא מפנים.

**הפינוי שקט כלפי המשתמש אבל לא בלתי נראה** — כל פינוי, וכל **דילוג** על
פינוי, נרשם ביומן (`LS_CFG.logKey`, 12 רשומות אחרונות) ומוצג בהגדרות.

### אופק הפינוי — למה שלב ב לא חסר משמעות
פינוי מקומי בלבד היה חסר תועלת: מנוע המיזוג מחזיר כל רשומה מרוחקת שאין לה
מקבילה מקומית, ולכן הרשומות שפונו היו חוזרות לדיסק תוך שניות. לכן לכל מפתח
שמפונה נשמר **אופק** (`LS_CFG.hzPrefix + key`) — חותמת הרשומה החדשה ביותר
שפונתה — ו-`lsSetArray` מסננת מתחתיו **בכל כתיבה**.
**הזיכרון והענן לא נוגעים; רק העותק שעל הדיסק מצטמצם.** כשיש רשת המשתמש
רואה הכול כרגיל; אופליין הוא רואה את התקופה האחרונה בלבד, וההגדרות
(«מידע טכני ›») אומרות זאת במפורש. המנגנון שמנקה את האופק (`lsRestoreAll`)
קיים במודול אך אינו נגיש מה-UI מאז סבב 15; ניקוי אופק ייעשה בסבב ייעודי
אם יידרש.

### מנגנון חירום
אם למרות הכול כתיבה נכשלת ב-`QuotaExceededError`, `lsSet` מריצה פינוי נוסף
**לפי אותו סדר בדיוק** ומנסה **פעם אחת** נוספת. הצליח — זו הצלחה לכל דבר
(המונה מוחזר וטוסט ההצלחה מותר). נכשל — באנר, טוסט ויומן. אין רקורסיה:
`_lsSweeping` חוסם פינוי בתוך פינוי.
<!-- SHARED:end -->

### מה נעשה כאן (yoman-avoda, `sw.js` v14, `app-version` 8-2026-08-07-1)
- **19 אתרי כתיבה גולמיים — כולם היו חשופים, כולם עברו לעוזר.** `saveCats`,
  `saveSubs`, `saveEntries`, `saveArchive`, חמש הכתיבות ב-`syncFromCloud`,
  שלוש ב-`autoArchiveDay`, שתיים ב-`arcSaveEntry`, מספר הוואטסאפ, דגל דחיית
  העדכון ומזהה המכשיר. עד היום אף אחת מהן לא נבדקה: באחסון מלא **הארכוב
  האוטומטי של סוף יום היה מוחק את הרשומות מ-`ENTRIES` בלי שהסנאפשוט נשמר**.
- **המערכים הגדולים דרך `lsSetArray`** (`tb_entries`+LS, `tb_archive`+LS).
- **⚠️ `_lastKnownTimestamp` אינו עֵד סנכרון ואסור להשתמש בו לפינוי.** הוא
  מתעדכן גם ב**משיכה** (משמעותו "ראיתי את מצב הענן") וגם נכתב ב-`saveEntries`/
  `saveArchive` **בלי לבדוק את תוצאת ה-`sbSet`**. פינוי שהיה נשען עליו היה
  יכול למחוק מהדיסק רשומה שמעולם לא הגיעה לענן. במקומו נוסף **`_tbPushedAt[key]`**,
  שנכתב **רק** אחרי `sbSet` שהחזירה `ok` — ומוזרם למנוע דרך `spec.syncedThrough`
  פר-מפתח. **אין להחליף אותו חזרה.**
- **`LS_CFG.tier2` נבנית ב-`lsRebuildPolicy()`, שנקראת מ-`selectYeshiva`** —
  המפתחות נושאים סיומת מוסד (`_rishon` / `_ramataviv`). הפינוי חל על **המוסד
  הפעיל בלבד**: לעֵד הסנכרון של מוסד אחד אין שום תוקף לגבי הנתונים של השני.
  המוסד השני יפונה כשייבחר. רצפות: 60 יום לארכיון, 90 לרשומות.
- **`_tbRecTs`** קוראת `updatedAt`, ובהיעדרו גוזרת מ-`gdate` — שנשמר בצורה
  `"3/09/2025"` (d/mm/yyyy), לא ISO — עם עוגן בצהריים מקומיים, כמו כל חישוב
  תאריך בפרויקטים האלה.
- **`lsBoot()`** נקראת ב-`selectYeshiva`, אחרי `lsRebuildPolicy()` ולפני
  `loadLocalData()`.
- **הגדרות** — האבחון מוצג כיום דרך «מידע טכני ›» בתחתית הפאנל (קופל
  בסבב 15; במקור כרטיס נפרד מעל עריכת הקטגוריות).

### ⚠️ yoman היא הצרכנית הגדולה בדומיין
`tb_archive` של ראשון לציון לבדו הוא **1.55MB** ב-localStorage, ושני המוסדות
יחד **~1.99MB** — קרוב ל-40% מהמכסה המשותפת של ארבע האפליקציות. זו הסיבה
שדווקא כאן שלב ב הוא אמיתי ולא תיאורטי.

---

<!-- SHARED:start id="pending-module" -->
## ⏳ סימון "ממתין לסנכרון" — מודול משותף (סבב 12, שלב 1)
**הפרק הזה, עד הסעיף «מה נעשה כאן», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md.**

### הבעיה
אחרי שמירה מקומית המסך נראה **בדיוק** כמו אחרי שמירה בענן. המשתמש רושם תשלום
ברכבת התחתית, רואה אותו ברשימה, וסוגר את האפליקציה בהנחה שהוא שמור — בזמן שהוא
יושב רק על המכשיר הזה. הסבב הזה הופך את ההבדל לגלוי.

### שלוש רמות תצוגה, כולן ממפה אחת
- **תגית על הרשומה עצמה** — `pendTag(key)`, נכנסת לשורה ברשימה (⏳ ממתין, ובאיחור ⚠️).
- **מונה** — `#pend-bar`, סרגל תחתון קבוע, **מוצג רק כשיש** משהו ממתין.
- **התרעה** — משהו ממתין מעל 24 שעות ⇐ `#pend-alert`, באנר אדום עליון עם כפתור סגירה
  (נסגר ל-6 שעות; **הסימון עצמו לא נמחק בסגירה**).

### ⚠️ ההגדרה של "ממתין" היא ראייתית, לא אופטימית
רשומה מסומנת ברגע שנכתבה במכשיר, ויורדת מהסימון **רק כשיש ראיה שהענן קיבל אותה**.
שלושת מסלולי האישור, לפי אופי האפליקציה:

| מסלול | מי משתמש בו | הראיה |
|---|---|---|
| `pendClear` | schar-limud | תשובת הצלחה מהשרת, או משיכה שמחזירה את ה-`client_id` |
| `pendConfirmPush(prefix, t0)` | hanhala, yoman | דחיפת-מצב מלא שהצליחה |
| `pendSyncOutbox()` | gius | הפעולה ירדה מהתור היוצא |

- **כשל רשת אינו ראיה לכלום ⇒ הסימון נשאר.** תשובה שאבדה ברשת היא בדיוק המקרה שבו
  הרשומה אולי כן נכנסה ואולי לא. ספק ⇐ מסומן.
- **כשל סמכותי כן מוריד את הסימון** (`pendFailed`) — ולידציה/הרשאה/סכימה אומרות
  שהשורה **לא** נכתבה, וסימון "ממתין" עליה מבטיח שליחה חוזרת שלא תקרה לעולם.
- **⚠️ `t0` נלקח לפני שצילום המצב נקרא, לא אחרי שהדחיפה הסתיימה.** רשומה שנכתבה
  באמצע הדחיפה אינה בתוכה, והורדת הסימון ממנה הייתה מאבדת אותה בשקט.
- **חותמת קיימת אינה נדרסת** בסימון חוזר — אחרת עריכה חוזרת של אותה רשומה הייתה
  מאפסת את שעון ה-24 שעות לנצח.

### המודול
`index.html` של כל אחת מארבע האפליקציות מכיל את אותו בלוק בדיוק, בין
`/* ═══ ממתין לסנכרון — מודול משותף (סבב 12)` לבין
`/* ═══════════════ סוף מודול "ממתין לסנכרון"`. **זהה בית-לבית בארבעתן**
(267 שורות, sha1 `2b3d6b779b1f`). כל שינוי בו חייב להיעשות בארבע באותו סבב.
מעליו יושבת `PEND_CFG` — **הדבר היחיד שנבדל בין האפליקציות** — ומתחתיו בלוק
"חיבור <אפליקציה> למודול" עם הקידומות והעוזרים המקומיים.

**ה-API:** `pendMark`/`pendMarkMany` · `pendClear`/`pendClearMany` · `pendFailed` ·
`pendConfirmPush` · `pendHas`/`pendSince`/`pendCount`/`pendOldest`/`pendLateCount` ·
`pendTag` · `pendRender`/`pendCounterText` · `pendStatusHTML` · `pendBoot`/`pendReload` ·
`pendReset`.

**כתיבה:** דרך `lsSet` בלבד (כלל ברזל 1), למפתח יחיד פר-אפליקציה
(`sl_pending` / `ys_pending` / `tb_pending<מוסד>` / `g_pending`).
**תקרה:** 800 סימונים; בגלישה נגרעים הישנים ביותר **ונרשמים ליומן** — סימון שנעלם
בלי אישור הוא בדיוק מה שהמודול בא למנוע, ולכן הוא לא נעלם בשקט.

### `PEND_CFG`
- **schar-limud:** `sl_pending`.
- **hanhala-ruchanit:** `ys_pending`; `extra()` מוסיף למונה את מספר המפתחות
  ב-`ys_offline_queue` — התור הישן הוא **פר-מפתח kv** ולא פר-רשומה, ובלי זה
  המשתמש היה רואה "0 ממתינות" בזמן שארבעה מפתחות תקועים.
- **yoman-avoda:** המפתח הוא **פונקציה** — `tb_pending` + סיומת המוסד. שני המוסדות
  חולקים localStorage, וסימון של ראשון לציון אינו תקף לרמת אביב. `selectYeshiva`
  קוראת ל-`pendReload()`.
- **gius:** `g_pending`; `extra()` מציג פעולות שנכשלו כשל תוכן חוזר (`dead`).

### מה סומן בכל אפליקציה
| | רשומות מסומנות | קידומות |
|---|---|---|
| schar-limud | תשלום, תלמיד, ומחיקה של כל אחד מהם | `txn:` `student:` `txn-del:` `student-del:` |
| hanhala | סדר נוכחות, סדר שינה, טיפולים, תלמידים | `at-sess:` `sl-sess:` `at-treat:` `sl-treat:` `student:` |
| yoman | רשומת יומן, סנאפשוט ארכיון | `entry:` `arc:` |
| gius | כל שבע הטבלאות, לפי `<table>:<id>` | `g_donors:` `g_txns:` … |

**מסך "⏳ ממתין לסנכרון" נוסף להגדרות בארבעתן** (`pendStatusHTML`) — מה ממתין,
מאיזה גיל, ומצב הרשת.

### ⚠️ מגבלה מתועדת ב-schar-limud (שלב 1)
schar עדיין **אינה עובדת אופליין** (הפער מכלל ברזל 3). לכן הסימון מכסה שם היום
מקרה אחד ומדויק: **תשובה שאבדה ברשת** — הכתיבה נשלחה, השורה אולי נכנסה למסד,
והמכשיר לא יודע. שלב 2 הוא שהופך את זה לכיסוי מלא.
<!-- SHARED:end -->

### מה נעשה כאן (yoman-avoda, `sw.js` v15, `app-version` 9-2026-08-07-1)
`pendReload()`+`pendBoot()` ב-`selectYeshiva`, אחרי `lsRebuildPolicy()`.
`addEntry`/`saveEntry`/`delEntry`/`arcSaveEntry` מסמנות; `saveEntries`/`saveArchive`
לוקחות `t0` לפני הצילום ומאשרות על `sbSet` שהחזירה `ok` — **אותה עדות שמזינה את
`_tbPushedAt`, ולא `_lastKnownTimestamp`** (שמתעדכן גם במשיכה ולכן אינו ראיה
לדחיפה — ר' אזהרת סבב 11). תגית ⏳ בשורת היומן.


---

<!-- SHARED:start id="iron-rule-6-sync" -->
## ⭐ כלל ברזל 6 — שיטת הסנכרון בארגון (סבב 12 שלב 4, אוגוסט 2026)
**הפרק הזה, עד שורת «כאן:» שבסופו, זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius), והוא ממשיך את חמשת כללי
הברזל שלמעלה.

> **שיטת הסנכרון בארגון היא דחיפת מצב מלא + מיזוג ברמת רשומה. אין תורים.
> כל רשומה שטרם אושרה בענן מסומנת למשתמש.**

עד הסבב הזה חיו כאן שלוש שיטות שונות: hanhala ו-yoman דחפו מצב מלא, gius
החזיקה תור יוצא פר-פעולה, ו-schar-limud לא עבדה אופליין כלל. מעכשיו — אחת.

### למה דווקא דחיפת מצב
- **אין פריט שיכול "להיתקע" לבדו.** בתור FIFO, פעולה שנכשלה כשל תוכן חוסמת
  את כל מה שאחריה, או מסומנת `dead` ונשארת תלויה לנצח. בדחיפת-מצב כל רשומה
  עומדת בפני עצמה, וכל סנכרון מתקן את מה שחסר — כולל מה שנכשל בפעם הקודמת.
- **אין מצב ביניים.** אין תור שיכול להתפגם, להתמלא או לצאת מסנכרון מול
  הנתונים עצמם. מצב האמת הוא הרשומות, ותו לא.
- **התאוששות אוטומטית.** מכשיר שאיבד את הסימונים עדיין מסתנכרן נכון:
  ההשוואה היא בין העותק המקומי לענן, לא בין יומן פעולות למציאות.

### חמשת הכללים
1. **עותק מקומי מלא של כל הנתונים, כולל tombstones.** רשומה מחוקה **חייבת**
   להישמר עם `deleted=true` + חותמת. היעדר רשומה אצל צד אחד **אינו** מחיקה.
2. **קריאה מהעותק בעליית האפליקציה, לפני כל נגיעה ברשת.**
3. **כתיבה מקומית-תחילה.** הכתיבה נשמרת במכשיר ומוצגת מיד; הרשת היא עניין
   של הרקע. כשל כתיבה מקומית עוצר ברעש ולא מציג "נשמר".
4. **סנכרון = משיכה ← מיזוג ← דחיפת מה שמקומי-וחדש-יותר.** בסדר הזה: אי
   אפשר לדעת מה לדחוף בלי לראות קודם מה בענן, ודחיפה עיוורת מחזירה לחיים
   רשומות שנמחקו במכשיר אחר. **דוחפים רק לקטגוריה שנמשכה בהצלחה.**
5. **מיזוג ברמת רשומה לפי חותמת עדכון**, עם שובר-שוויון דטרמיניסטי (הענן
   מנצח בשוויון), כך ששני מכשירים שממזגים את אותם צדדים מגיעים לאותה תוצאה.

### ⚠️ הכלל שמונע אובדן עריכה — רשומה מסומנת ⏳ מנצחת במיזוג
בשלוש מהאפליקציות החותמת נקבעת **בצד השרת** בטריגר `BEFORE UPDATE`. המשמעות:
ברגע שמכשיר א' דוחף, השורה בענן מקבלת חותמת **מאוחרת יותר** מעריכה חדשה יותר
שיושבת אצל מכשיר ב' וטרם עלתה — ומכשיר ב' היה מאמץ את גרסת א', כלומר **מוחק
את העריכה שלו בשקט**. זה לא תרחיש תיאורטי; הוא נתפס בבדיקה.

לכן, בשני מקומות ולא באחד:
- **במיזוג** — רשומה מקומית מסומנת ⏳ מנצחת ללא תלות בחותמת.
- **בבחירת מה לדחוף** — רשומה מסומנת נחשבת תמיד "לדחיפה". בלי זה היא הייתה
  נשארת מסומנת לנצח ולעולם לא נדחפת.

**המחיר המודע:** מכשיר שהיה מנותק זמן רב ידרוס בעלייתו עריכה מרוחקת חדשה
יותר. זו העדפה מכוונת — הרשומה מסומנת ⏳ כל אותו זמן ומתריעה אחרי 24 שעות,
בעוד שעריכה שנמחקת בשקט אינה משאירה עקבות. אותה עדיפות כמו "היעדר רשומה
אינו מחיקה". **אין להסיר את הבדיקה הזו משני המקומות.**

### ⛔ אין תורים
`g_outbox` הוסר (gius, שלב 3) ולא ייבנה מחדש. `ys_offline_queue` בהנהלה נשאר
בינתיים כמנגנון פר-מפתח kv **בלבד** ואינו תור פעולות; אין להרחיב אותו.
**אין ליצור תור פעולות חדש בשום אפליקציה בארגון.**

### ⛔ מה לא נדחף לעולם
רשומה שהעותק המקומי שלה **חלקי** אינה ניתנת לדחיפת-מצב: הדחיפה הייתה
כותבת את מה שחסר כערך ריק. המקרה השכיח הוא **סיסמה**: היא מוסרת
מהמראה כדי שלא תשב בטקסט גלוי על הדיסק, ולכן אין מה לדחוף.
שני מקרים קיימים היום, ושניהם מתועדים במקומם — `g_users` ב-gius
(`strip: ['password']`) ו-`sl_settings.admin_pass` ב-schar-limud
(`slStripSecrets`). **שינוי סיסמה דורש רשת, בשתי האפליקציות.**

⚠️ **החריגה היא ברמת השורה ולא ברמת הטבלה.** כל שאר השורות של
`sl_settings` נדחפות כרגיל (מ-`migrations/009`). וכשהסוד מוסר מהמראה —
**אסור ששער הכניסה ייפול חזרה לברירת המחדל.** בדיקה כמו
`p !== (SETTINGS['admin_pass'] || 'admin')` הופכת בדיוק ברגע ההסרה
לשער שנפתח לכל מי שמקליד «`admin`». ההשוואה חייבת להיות מול טביעה
נגזרת (PBKDF2) או מול הענן — **ולהיכשל סגור כשאין אף אחת מהן.**

### מזהים
כל רשומה חדשה מקבלת **מזהה שנוצר במכשיר** לפני שראתה שרת — uuid v4.
בטבלאות עם `SERIAL` (schar-limud) זהו `client_id` לצד ה-`id` של המסד, והכתיבה
היא `upsert` עליו. זה מה שהופך ניסיון חוזר אחרי תשובה שאבדה ברשת
לאידמפוטנטי, במקום ליצור שורה שנייה — בכסף, תשלום כפול.
<!-- SHARED:end -->

**כאן:** `mergeRecords`/`mergeEntries`/`mergeArchive` ו-`saveEntries`/
`saveArchive` כדחיפת-מצב מלא, עם `_tbPushedAt` כעֵד הדחיפה.

---

## ⭐ סבב 13 (אוגוסט 2026, `sw.js` v17, `app-version` 11-2026-08-09-1)

### 1. מעבר לטעינה מהרשת — `versionCode` 2
המעטפת טענה `file:///android_asset/index.html`, כלומר עותק שנצרב ב-APK. לכן
**כל שחרור קוד חייב בניית APK והפצה ידנית** — מנגנון האוטו-אפדייט זיהה גרסה
חדשה, אבל `location.reload()` ב-`file://` טען שוב את אותו עותק. מעכשיו היא
טוענת `https://ygtotlrl-lab.github.io/yoman-avoda/`, בדפוס gius: הכתובת
מתעדכנת לבד, וה-service worker נותן אופליין מלא בדיוק כמו בדפדפן.

**מה השתנה במעטפת:** `MainActivity` נכתבה מחדש בדפוס gius —
`ShellWebViewClient` עם `shouldOverrideUrlLoading`, דף שגיאה בעברית להפעלה
ראשונה בלי רשת (`showOfflinePage`, מוצג **רק** כל עוד שום דף לא נטען),
`saveState`/`restoreState`, `versionCode` 1 → **2**, ו-`usesCleartextTraffic`
`true` → **`false`** (האתר https בלבד; ה-`true` נדרש רק כדי שדף `file://`
יוכל לפנות ל-Supabase). גם `setAllowFileAccess`/`setAllowContentAccess`
כובו — אין יותר דף `file://` שיזדקק להם.

⛔ **`http`/`https` נשאר בתוך המעטפת ואינו נמסר לדפדפן חיצוני.** הפניה החוצה
הייתה מגיעה לכרום, שהסינון במכשירי המשתמשים חוסם — בדיוק התקלה שהפילה את
ה-TWA. רק סכימות שאין להן מנוע רינדור כאן (`tel:`, `whatsapp:`, …) נמסרות
למערכת, וזה מה שמפעיל את מסלול הנפילה-חזרה של השיתוף.

### 2. ⛔ הגשר המקורי מוגבל לדומיין שלנו — **הנקודה הרגישה**
`addJavascriptInterface` מזריק את האובייקט ל**כל frame של כל דף**, בלי שום
מושג של origin. במעטפת הישנה זה היה חסר-נזק: הדף היחיד היה זה שנצרב ב-APK.
**טעינה מהרשת משנה את זה מהיסוד** — מרגע שהמעטפת יכולה להימצא על דף שאיננו
מגישים, הגשר נוסע איתו. gius פתרה את זה בכך שאין לה גשר בכלל; כאן הגשר
**נדרש** לשיתוף דוחות כתמונה, ולכן הוא נשאר — מוגבל:

- **מסלול מועדף — `WebViewCompat.addWebMessageListener`** עם
  `allowedOriginRules = {"https://ygtotlrl-lab.github.io"}`. **WebView עצמו**
  אוכף את רשימת המקורות, **פר-frame**, לפני שההודעה נמסרת; גם iframe
  חוצה-origin אינו מגיע אליו. דורש WebView 88+ ו-`androidx.webkit`.
  שם האובייקט: `AndroidShareBridge`, והמטען הוא JSON
  (`{data, mime, pkg}`) דרך `postMessage`.
- **נפילה-חזרה — `addJavascriptInterface` הישן** (`AndroidShare`), למכשירים
  עם WebView ישן יותר. **נעול פעמיים:** מחובר רק כשהמעטפת על הדומיין שלנו
  ומנותק ברגע שהיא מנווטת משם (`syncLegacyBridge` מ-`onPageStarted`/
  `onPageFinished`), **וכל קריאה מאמתת מחדש** את מקור הדף על ה-UI thread
  לפני שהיא נוגעת במשהו.
- `_androidShareImage` ב-`index.html` מנסה את המאובטח **קודם**.
  **אין להפוך את הסדר** — זה ויתור על אכיפה שהפלטפורמה נותנת בחינם.

### 3. הנכסים המוטבעים — נשקלו והוסרו, לא נשכחו
השאלה נשקלה במפורש: להשאיר את `assets/` **כגיבוי** לעלייה ראשונה בלי רשת?
**המלצה והחלטה: לא. השארתם מזיקה, לא רק מיותרת.**
- ⛔ **`file://` הוא origin אחסון אחר.** localStorage של `file://` ושל
  `https://ygtotlrl-lab.github.io` הן שתי מחיצות נפרדות לחלוטין. רשומת יומן
  שנכתבת לעותק המוטבע לא נראית לאפליקציה האמיתית **לעולם**, וגם לא
  מסונכרנת — אובדן נתונים שקט באפליקציה שכל תפקידה רישום. גיבוי שמייצר
  אובדן נתונים אינו גיבוי.
- **מקור אמת שני** שמתיישן בכל שחרור — נגד כלל קריטי 5.
- **מה שהוא אמור לפתור כבר פתור:** אחרי עלייה מוצלחת אחת ה-SW מגיש הכול
  אופליין. נשאר רק «התקנה + הפעלה ראשונה בלי רשת בכלל» — ולהורדת APK ממילא
  צריך רשת. שם מוצג דף שגיאה בעברית עם «נסה שוב».

`copy-assets.sh` נשאר בריפו **כשלד עם הסבר בלבד**, ושלב ההעתקה הוסר
מ-`build-apk.yml`.

### ⚠️ מעבר מגרסה 1 — העותק המקומי מתאפס פעם אחת
מי שמתקין את גרסה 2 מעל גרסה 1 עובר מ-origin `file://` ל-`https://…`, כלומר
ל-localStorage **ריק**: בחירת המוסד תתבקש שוב, והנתונים ייטענו מחדש מהענן
(`kv_rishon`/`kv_ramataviv`) — שהוא ממילא מקור האמת.
⛔ **מה שכן יכול ללכת לאיבוד: רשומה שנרשמה במכשיר ולא הספיקה לעלות.**
**לפני הפצת ה-APK — ודא בכל מכשיר שההגדרות ← «⏳ ממתין לסנכרון» מציג 0.**

### 4. ניקוי מפתחות שריד ב-localStorage — `migrateLegacyLsKeys()`
**נמדד בדפדפן אמיתי:** הדומיין תפס **4.31MB מתוך ~5MB (86%)**, ויומן לבדה
**3.52MB**. הסיבה — ארבעה מפתחות כפולים:

| שריד (בלי סיומת) | | יעד |
|---|---|---|
| `tb_archive` = 157 פריטים | | `tb_archive_rishon` = 167 |
| `tb_entries` = 310 פריטים | | `tb_entries_rishon` = 451 |

`LS` מתחיל כמחרוזת ריקה והופך ל-`'_rishon'` רק אחרי בחירת הישיבה, ולכן קוד
שקדם לרב-דיירות כתב למפתחות חסרי הסיומת. מאז **אף אחד לא קורא ולא כותב
אליהם** — הם פשוט תופסים ~1.5MB מהמכסה **המשותפת לארבע האפליקציות**.

⛔ **מחיקה עיוורת אסורה** — 157 מול 167 אינם בהכרח תת-קבוצה. שרשרת שערים,
והמחיקה היא הצעד האחרון:
1. **מיזוג אמיתי, לא השוואת אורכים** — מנוע המיזוג הקיים
   (`mergeEntries`/`mergeArchive`), כשהיעד הוא ה"מרוחק" ולכן מנצח בשוויון.
   **וזה מה שמונע תחיית מתים:** `tombstone` ביעד גובר על רשומת שריד חסרת
   חותמת (ts=0).
2. **אימות פריט-פריט** — כל פריט שריד חייב להיות מיוצג בתוצאה. אחד שאינו
   (או אובייקט בלי מזהה, שאי אפשר לאמת) ⇒ **ההגירה מתבטלת כולה** לאותו
   מפתח והשריד נשאר.
3. **כותבים לפני שמוחקים**, ורק כתיבה שהחזירה `true` מתירה מחיקה (כלל ברזל 2).
4. **אידמפוטנטי** — אין שריד ⇒ יציאה מיידית. הרצה שנייה היא no-op.
5. **⛔ ראשון לציון בלבד** — המפתחות קדמו לרב-דיירות ולכן תוכנם שייך לראשון.
   מיזוג לרמת אביב היה **דליפה בין מוסדות**, בדיוק מה שסיומת ה-`LS` מונעת.
6. **ערך פגום** (לא JSON / לא מערך) — לא נמחק, נרשם ביומן.

**מתי היא רצה:** ב-`selectYeshiva`, **בין** `lsRebuildPolicy()` ל-`lsBoot()`.
לא במקרה: היא מפנה ~1.5MB, ולכן `lsBoot` שאחריה רואה את הניצולת האמיתית ולא
מפנה ארכיון שאין צורך לפנות; והיא חייבת להיות **לפני `loadLocalData`** כדי
שהטעינה תראה את המערך הממוזג.

**דיווח:** כל הגירה וכל **דילוג** נרשמים ביומן הפינוי (`LS_CFG.logKey`)
ומוצגים בהגדרות ← מידע טכני ›, עם כמה פריטים מוזגו וכמה בתים פונו.
הפונקציה גם מחזירה `{ran, cleared, merged, freed, skipped}`.

### בדיקה
**50 טענות** על הקוד האמיתי המחולץ מ-`index.html` (`_test_legacy.js`, בסשן):
שריד שהוא תת-קבוצה · פריט בלעדי שמוזג פנימה · אידמפוטנטיות (שלוש ריצות) ·
⛔ רמת אביב שלא רצה ולא דלפה · ⛔ tombstone שגובר · ⛔ אימות שנכשל ⇒ לא נמחק ·
⛔ ערך פגום ⇒ לא נמחק · ⛔ כתיבה חסומה ⇒ לא נמחק · מיזוג ארכיון לפי `gdate`
כולל רשומות בתוך אותו יום · **התרחיש המדוד של המשתמש (310/451 + 157/167)**
עם אימות שהניצולת ירדה בפועל ושהמספר המדווח תואם · וסדר הניסיון של שני
הגשרים. `node --check` על ה-JS המחולץ ועל `sw.js` עבר.


---

<!-- SHARED:start id="round-14-verified-sweep" -->
## ⭐ סבב 14 (אוגוסט 2026) — עדות סנכרון חלופית לפינוי היזום
**הפרק הזה, עד הסעיף «בדיקה», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius).

### הבעיה שנמדדה
שלב ב של הפינוי (רשומות ישנות מסונכרנות) מותנה ב**עֵד סנכרון מקומי** —
חותמת שנכתבת רק אחרי דחיפה מוצלחת מהמכשיר הזה (`_tbPushedAt` ביומן,
`_ysPushedAt` בהנהלה). **בדפדפן שקורא ולא כותב** — כרום שבמחשב המנהל, שרק
מסתכלים בו — אין דחיפה, ולכן אין עֵד, ולכן שלב ב מדולג לגמרי: הפינוי מחזיר
0 וההתרעה הצהובה («מעל 3MB») נשארת קבועה, בזמן שכל הנתונים כבר בענן.
**נמדד בפועל: 3.06MB / 61%, ופינוי שהחזיר 0.**

### התיקון — מחליפים את מקור הראיה, לא מרפים מהדרישה
כשאין עֵד מקומי (או שהוא 0), שלב ב עובר ל**אימות ישיר מול הענן**: משיכת
העותק הענני של המפתח המועמד לפינוי, ומחיקה של רשומה **רק** אם היא נמצאת שם
לפי מזהה **וגם** חותמת הענן אינה ישנה מהחותמת המקומית (הענן מחזיק את הגרסה
הזו או חדשה ממנה).

**ה-API החדש במודול המשותף:**
`lsGlobalWitness` · `lsSpecWitness` · `lsVerifySpecs`/`lsHasVerifiers` ·
`lsCloudIndex` · `lsPruneKeyVerified` · `lsSweepNeed` · `lsSweepVerified` ·
`lsScheduleDeferred`/`lsBootDeferred` · `lsBootAlert`.

**שני שדות חדשים ב-spec של `LS_CFG.tier2`:**
- `idOf(rec)` — מזהה הרשומה, אותו מפתח שבו משתמש מנוע המיזוג.
- `verify()` — מחזירה `Promise` של `{ok:true, rows:[...]}` מהענן.
  `spec.ts` הקיימת היא שגוזרת את החותמות משני הצדדים.

### ⛔ ארבעה כללים שאין לשנות
1. **נכשל סגור.** אין רשת, `verify` זרקה, החזירה `ok:false`, או שהמפתח כלל
   אינו קיים בענן ⇒ **לא מפנים דבר**. היעדר ראיה אינו ראיה. (ההגנה משוכפלת
   בשלוש שכבות: בדיקת התשובה, `lsCloudIndex` שמחזירה `null` על קלט שאינו
   מערך, ו-`lsPruneKeyVerified` שיוצאת על `idx` ריק.)
2. **רצפות הגיל (`minAgeMs`) נשארות כפי שהיו** — 60/90 יום ביומן, 180 בהנהלה.
   הן שכבת בטיחות **נפרדת** מהראיה ולא תחליף לה: רשומה מאומתת בענן שעדיין
   בתוך הרצפה אינה מפונה.
3. **`LS_CFG.pending()` נבדק גם כאן**, בדיוק כמו בשלב ב הסינכרוני: יש משהו
   שטרם עלה ⇒ לא נוגעים ברשת ולא מפנים דבר.
4. **⛔⛔ פינוי רצף בלבד — עוצרים ברשומה הראשונה שלא אומתה, לא מדלגים עליה.**
   זו הנקודה המסוכנת בסבב, והיא **לא** אופטימיזציה: אחרי הפינוי נשמר
   `hz` = חותמת הרשומה החדשה ביותר שנמחקה, ו-`lsSetArray` מסננת **בכל
   כתיבה** כל רשומה שחותמתה ≤ `hz`. דילוג על רשומה לא-מאומתת והמשך מעליה
   היה מציב אופק **מעליה**, וכתיבת המערך הבאה הייתה מוחקת אותה בשקט —
   דווקא את הרשומה היחידה שאין לה עותק בענן. (מאומת: מוטציה שהחליפה
   `break` ב-`continue` הפילה שלוש טענות, כולל האימות ש-`lsSetArray`
   מוחקת בפועל את היתומה.)

### ⚠️ אסינכרוני — ולכן גם ההתרעה נדחית
`lsBoot` סינכרונית ורצה **לפני** שהמסך עולה, ואילו האימות דורש רשת. לכן:
- `lsBoot` מבצעת כרגיל את שלב א ואת שלב ב הסינכרוני, ואם עדיין מעל הסף
  **ויש מפתחות שניתן לאמת** — היא מתזמנת `lsBootDeferred()` (3 שניות)
  **ודוחה איתו את ההתרעה**. הצגתה בעלייה הייתה אזהרה צהובה על מצב שנפתר
  שתי שניות אחריה.
- `lsBootDeferred` מריצה את האימות, מודדת מחדש, ורק אז קוראת ל-`lsBootAlert`.
  אין מה לפנות או אין רשת ⇒ ההתרעה כן מוצגת, וזה הנכון.
- `lsBoot` מחזירה עכשיו `{usage, swept, deferred}`, ומקבלת `opts`
  אופציונלי (`noDefer`, `deferMs`) — לבדיקות ולנתיבים שאסור לדחות בהם.
- `lsSweepNow()` (הכפתור בהגדרות) מחזירה **Promise**: היא מריצה את שני
  המסלולים ומדווחת אחרי שניהם.
- מסך «מידע טכני ›» (הגדרות) מציג אילו מפתחות תלויים באימות מול הענן.

### תיאום גרסאות
`sw.js`: hanhala **v29** · yoman **v18** (`app-version` **12-2026-08-10-1**) ·
schar **v19** · gius **gius-v6**.

### המודול המשותף — גודל חדש
הבלוק שבין `/* ═══ עמידות אחסון מקומי — מודול משותף (סבב 11)` לבין
`/* ═══════════════ סוף המודול המשותף` הוא כעת **691 שורות**, sha1
`22c1fb96b0ea` (היה 474). **זהה בית-לבית בארבע האפליקציות** — כל שינוי בו
חייב להיעשות בארבעתן באותו סבב.
<!-- SHARED:end -->

### בדיקה
⚠️ **פרק פרטי — מתאר את מה שבאמת רץ באפליקציה הזו** (כלל ברזל 8, סעיף 5).
עד סבב 18 ישב כאן נוסח אחד משוכפל לארבעת הריפו; הוא תיאר תצורה שקיימת
רק בהנהלה וביומן, ולכן היה שקרי בשתי האחרות. **אין ליישר אותו מריפו אחר.**

**84 טענות** על הקוד האמיתי המחולץ מ-`index.html` (`test_verify.js` +
`harness.js`, בסשן — מריצים את הקטע שמ-`var LS_CFG` ועד סוף המודול בתוך
`vm` עם localStorage וענן מדומים). **התצורה שנבדקה היא זו של yoman-avoda:**
`tier2` על `tb_archive`+LS ו-`tb_entries`+LS, `idOf` לפי `archiveKey`/`entryKey`
(אותם מפתחות שבמנוע המיזוג), `verify: _tbVerify` מעל `sbGetResult`,
ו-`syncedThrough` פר-מפתח מ-`_tbPushedAt`:
פינוי מאומת שמוריד את הדומיין · ⛔ רשומה שאינה בענן ששורדת **וגם שורדת
`lsSetArray` שאחריו** · ⛔ ענן עם חותמת ישנה יותר · ⛔ ארבעה מצבי כשל רשת
(זריקה / מפתח חסר / תשובה שאינה מערך / `ok:false`) · ⛔ רצפת הגיל ·
⛔ `pending()` שמדלג בלי לגעת ברשת · עֵד מקומי שמחזיר למסלול הישן ·
עֵד דחיפה שאינו מעיד על מפתח אחר · **מבחן הקבלה** (61% → מתחת ל-60%
בלי התרעה) · ואופליין שבו ההתרעה כן מוצגת.
אומת גם ב-**מוטציות**: `break`→`continue` מפיל 3 טענות, ביטול רצפת הגיל
מפיל 2.
**שערי הדחיפה כאן:** `node --check` על ה-JS המחולץ מ-`index.html` ועל `sw.js`.

### מה נעשה כאן (yoman-avoda)
- שני מפתחות ה-`tier2` ב-`lsRebuildPolicy()` קיבלו `idOf` ו-`verify`:
  `tb_archive`+LS לפי **`archiveKey`** (`g:<gdate>`, ובהיעדרו `i:<id>`)
  ו-`tb_entries`+LS לפי **`entryKey`** — בדיוק המפתחות של מנוע המיזוג, כך
  שאין דרך שהאימות והמיזוג יחלקו על זהות רשומה.
- **`_tbVerify(kvKey)`** מושכת דרך `sbGetResult`. ⚠️ **`ok` לבדו אינו
  מספיק:** `sbGetResult` מחזירה `{ok:true, data:null}` גם למפתח שאינו קיים
  בענן (PGRST116 מ-`.single()`), ולכן נדרש **מערך** — אחרת מפתח חסר בענן
  היה נקרא כ"ענן ריק" ו... לא היה מפנה דבר בכל מקרה (הרשומה הראשונה לא
  מאומתת), אבל התיעוד ביומן היה מטעה.
- **מפתח הענן הוא בלי סיומת המוסד** (`tb_archive`, לא `tb_archive_rishon`) —
  ההפרדה בין המוסדות בענן היא ב-`KV_TABLE` (`kv_rishon`/`kv_ramataviv`),
  והמפתח בתוך הטבלה זהה. הפינוי ממשיך לחול על **המוסד הפעיל בלבד**.
- `_tbPushedAt` **נשאר כפי שהוא** — הוא עדיין המסלול המהיר והמועדף כשיש
  דחיפה. ⛔ ואין להחליף אותו ב-`_lastKnownTimestamp` (ר' אזהרת סבב 11).

---

<!-- SHARED:start id="round-15-tech-info" -->
## ⭐ סבב 15 (אוגוסט 2026) — קיפול מסכי האחסון ל«מידע טכני»
**הפרק הזה, עד הסעיף «מה נעשה כאן», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius).

מסך «💾 אחסון מקומי» בהגדרות קופל מלוח-מחוונים למידע טכני צנוע. העיקרון:
המנהל צריך לדעת שהכל מתנהל מושלם מאחורי הקלעים, בלי להתבלבל ממספרים,
גרפים וכפתורים. **שינוי תצוגה בלבד — המודול המשותף של סבב 14 לא נגוע**
(אומת: הבלוק שבין סמני המודול זהה בית-לבית ל-HEAD בארבעתן, sha1 `18c08fcc4b67`).

- **אין יותר כפתורי פעולה באזור האחסון.** «🧹 פנה מקום עכשיו», «↻ רענן»
  ו-«☁️ טען הכל מחדש מהענן» אינם מוצגים — מוסתרים ב-CSS
  (`.tech-info .ls-status button{display:none}`); ה-HTML שלהם עדיין נוצר
  ע"י `lsStatusHTML` שבמודול הקפוא, ולכן המודול לא נערך. **המנגנון
  האוטומטי הוא הדרך היחידה לפינוי.** `lsSweepNow`/`lsRestoreAll` קיימות
  אך אינן נגישות מה-UI.
- **האבחון נעשה דרך רכיב «מידע טכני ›»** — שורה מוקטנת בתחתית מסך
  ההגדרות, סגורה כברירת מחדל (`<details class="tech-info">`). בפתיחה
  `techInfoToggle` קורא ל-`lsRenderStatus`, והתוכן — ניצולת, פילוחים,
  אזהרות האופק/האימות ויומן הפינוי — מרונדר לתוך `#ls-status-box` שחי
  עכשיו בתוך הרכיב. **התוכן לא השתנה, רק המיקום.**
- **שקט הוא ברירת המחדל** — במקום המסך הישן לא מוצג דבר.
- **חריג יחיד ומכוון:** מעל `LS_CRIT_BYTES` (4MB) — מצב שהמנגנון האוטומטי
  לא הצליח למנוע — `techInfoHTML` מציג שורת מצב עדינה (`.ls-crit-line`)
  גם מחוץ ל«מידע טכני». זו רשת הביטחון היחידה שמוצגת ביוזמת המערכת.
  (באנרי המודול עצמו — `#ls-alert`, התרעות lsBootAlert — לא נגעו.)
- **אחידות:** בלוק ה-JS (`techInfoHTML`/`techInfoToggle`/`techInfoMount`,
  יושב מיד אחרי «סוף המודול המשותף») ובלוק ה-CSS זהים בית-לבית בארבעתן
  (sha1 `72fc844f885e` / `f74f1f7510e7`). כל שינוי בהם — בארבע באותו סבב.
- מסך «⏳ ממתין לסנכרון» **לא נגוע** — הוא אינו ממסכי האחסון.
<!-- SHARED:end -->

### מה נעשה כאן (yoman-avoda, `sw.js` v19, `app-version` 13-2026-08-11-1)
כרטיס «💾 אחסון מקומי» הוסר מ-`panel-settings`; `<div id="tech-info-box">`
נוסף בתחתית הפאנל, אחרי כרטיס עריכת הקטגוריות; `renderSettings()` קורא
`techInfoMount()` במקום `lsRenderStatus()` (הקריאות שבתוך המודול הקפוא
נשארו — הן no-op כשהרכיב סגור או לא נטען).

---

<!-- SHARED:start id="round-16-pend-delay" -->
## ⭐ סבב 16 (אוגוסט 2026) — יישור ניסוחים + השהיית ציור ה-⏳
**הפרק הזה, עד הסעיף «בדיקה», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius).

### חלק א — יישור אזכורים תפעוליים אחרי קיפול סבב 15 (תיעוד בלבד)
אפס שינוי התנהגות. כלל ההבחנה שהופעל:
- שורות הכפתורים בתוך `lsStatusHTML` שבמודול האחסון הקפוא — **לא נגעו**
  (חלק מהמודול, מוסתרות ב-CSS, מתועד בסבב 15).
- תיאורים היסטוריים בפרקי הסבבים — **לא נגעו**. היסטוריה נשארת היסטוריה.
- אזכורים **תפעוליים בהווה** של מסך «💾 אחסון מקומי» הישן יושרו
  ל-«הגדרות ← מידע טכני ›»: הערת מיגרציית השרידים ב-`index.html` של יומן,
  ובארבעת ה-CLAUDE.md — פרק אופק הפינוי (כפתור «☁️ טען הכל מחדש מהענן»
  אינו נגיש מה-UI מאז סבב 15 — נוסח מחדש: המנגנון `lsRestoreAll` קיים
  במודול, וניקוי אופק ייעשה בסבב ייעודי אם יידרש), כלל ברזל 5 (חיווט
  אפליקציה חדשה: `techInfoMount()` במקום `lsRenderStatus()`), שורת «אילו
  מפתחות תלויים באימות» בפרק סבב 14, ותיאורי המיקום בסעיפי «מה נעשה כאן».

### חלק ב — השהיית ציור ⏳ (שינוי UI בלבד, בארבעתן)
עד היום כתיבה מקומית ציירה ⏳ מיידית, והדחיפה המוצלחת מחקה אותו — באונליין
תקין זה היה הבהוב של שבריר שנייה על כל שמירה. מעכשיו הציור מאחר
**`PEND_DRAW_DELAY_MS` = 2000 מ״ש**: הגיע אישור הענן לפני — ה-⏳ לא מופיע
כלל; לא הגיע (אופליין, רשת איטית/חולה) — הוא מופיע וממשיך להתנהג בדיוק
כמו היום.

⚠️ **זו השהיית *ציור* בלבד — ארבעה גבולות קשיחים:**
1. **הסימון הלוגי נכתב ל-localStorage מיידית וללא שינוי** (`pendMark`
   ומקביליו) — הוא חלק ממנגנון הבטיחות: הפינוי, בחירת מה לדחוף והמיגרציות
   נשענים עליו. ההחזקה (`_pendDrawHold`) חיה **בזיכרון בלבד** ואינה נשמרת.
2. **מכשיר שנסגר בתוך שתי השניות** — בפתיחה הבאה ה-⏳ מוצג מיד: ההשהיה
   חלה רק על סימון שנוצר הרגע בכתיבה חדשה, לא על סימון שנטען מהדיסק.
3. **מסך «⏳ ממתין לסנכרון» בהגדרות והמונים הלוגיים** (`pendCount`/`pendHas`/
   `pendSince`/`pendStatusHTML`) מציגים תמיד את האמת הלוגית המלאה, בלי
   השהיה. גם סרגל `#pend-bar`, כשהוא מוצג, מציג את המונה הלוגי המלא —
   ההשהיה קובעת רק **אם** מציירים, לא **מה** המספר.
4. **מימוש אחיד בארבעתן** — אותן פונקציות עזר במודול המשותף
   (`pendHoldDraw` · `pendDrawSince` · `pendDrawCount` · `pendDrawFlush` ·
   `pendScheduleFlush`), אותו קבוע. ההתאמה הפר-אפליקציתית היא שדה
   **`PEND_CFG.redraw`** בלבד: בתום ההחזקה, אם הסימון עדיין קיים,
   `pendDrawFlush` מרנדר מחדש את הרשימות שמציירות `pendTag` — כדי שהתגית
   תיכנס לשורות שכבר רונדרו בלעדיה. אושר בינתיים ⇒ אין redraw ואין רינדור
   מיותר.

### המודול המשותף — גודל חדש
הבלוק שבין `/* ═══ ממתין לסנכרון — מודול משותף (סבב 12)` לבין
`/* ═══════════════ סוף מודול "ממתין לסנכרון"` הוא כעת **328 שורות**, sha1
`a6fbdd2c73ff` (היה 267 / `2b3d6b779b1f`). **זהה בית-לבית בארבע
האפליקציות** — כל שינוי בו חייב להיעשות בארבעתן באותו סבב. מודול האחסון
הקפוא של סבב 14 **לא נגוע** (אומת: הבלוק זהה בית-לבית ל-HEAD בארבעתן).
<!-- SHARED:end -->

### בדיקה
**28 טענות** על הקוד האמיתי המחולץ מ-`index.html` (`test_pend_delay.js`,
בסשן, עם שעון ו-`setTimeout` מדומים): כתיבה+אישור מהיר ⇒ הסימון הלוגי
מיידי ב-localStorage, אפס תגית, אפס סרגל, אפס redraw · כתיבה בלי אישור ⇒
תגית וסרגל אחרי 2ש׳ בדיוק (ב-1.9ש׳ — עדיין לא) עם redraw יחיד · סימון
שנטען מהדיסק ⇒ ציור מיידי · עירוב ותיק+טרי ⇒ הסרגל מציג את המונה הלוגי
המלא · `pendMarkMany`/`pendClearMany` מנקים החזקות · `pendReload` מאפס
החזקות (החלפת מוסד). וכן `node --check` על ה-JS המחולץ ועל `sw.js`
בארבעתן, `sync-setup-sql.py --check` ב-schar ו-`scripts/check-js.mjs`
ב-gius.

### מה נעשה כאן (yoman-avoda, `sw.js` v20, `app-version` 14-2026-08-11-2)
`PEND_CFG.redraw` = `renderLog()` — האתר היחיד שמצייר `pendTag` כאן (שורת
היומן). הערת מיגרציית השרידים ב-`index.html` יושרה ל-«בהגדרות ← מידע
טכני ›».

---

<!-- SHARED:start id="iron-rule-7-status-area" -->
## ⭐ כלל ברזל 7 — רכיב משותף = קוד זהה + מיקום זהה + חיווט חי (סבב 17, אוגוסט 2026)
**הפרק הזה, עד הסעיף «בדיקה», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius), והוא ממשיך את ששת כללי
הברזל שלמעלה.

> **רכיב שמוכרז «משותף» חייב שלושה דברים, לא אחד: אותו קוד, באותו מקום
> במסך, ומחובר לקוד חי. תיעוד לבדו אינו אוכף אף אחד מהם — ולכן יש בדיקה
> אוטומטית שרצה עם שערי התחביר לפני כל דחיפה.**

### הבעיה שנמדדה
סבב 15 קיפל את מסכי האחסון לרכיב «מידע טכני ›» בארבע האפליקציות, והתיעוד
הכריז שהבלוק זהה בית-לבית. הבלוק אכן היה זהה — **וכל השאר לא**:

| | מסך «⏳ ממתין לסנכרון» | «מידע טכני ›» |
|---|---|---|
| schar-limud | **ראש** מסך ההגדרות | סוף המסך ✅ |
| yoman-avoda | **אמצע** מסך ההגדרות | סוף המסך ✅ |
| hanhala | **אמצע** מודול המערכת | **אמצע**, לפני ההרשאות |
| gius | כרטיס «☁️ מצב סנכרון» נפרד | נבנה בכל render() — **`techInfoMount` הייתה קוד מת** |

כלומר: קוד זהה, שלוש חוויות שונות, ובאחת מהן הרכיב לא היה מחובר בכלל.
בגיוס הייתה לזה גם תוצאה גלויה — מסך ההגדרות נבנה מחדש בכל `render()`,
העוגן חזר ריק, ו«מידע טכני» **נסגר בכל שמירה**.

### ההגדרה המחייבת — «אזור מצב»
בתחתית מסך (או מודול) ההגדרות של כל אפליקציה בארגון יושבים **תמיד, ובסדר
הזה, שני האלמנטים האחרונים** — חשופים, לא עטופים בכרטיס של האפליקציה:

1. **`<div id="sync-status-box">` — בלוק «☁️ סנכרון».** מציג את מסך ה-⏳
   הקיים (`pendStatusHTML`, **בלי שינוי בתוכנו**) ומתחתיו שורת מצב תמציתית:
   מצב חיבור, מועד הסנכרון המוצלח האחרון, וכפתור «🔄 סנכרן עכשיו».
2. **`<div id="tech-info-box">` — רכיב «מידע טכני ›».** האלמנט האחרון במסך.

שניהם מורכבים בקריאה אחת — **`statusAreaMount()`** — וזו נקודת החיווט
היחידה. מסך הגדרות שאינו קורא לה הוא מסך בלי אזור מצב, והבדיקה נכשלת.

### שלושת הבלוקים המשותפים
זהים בית-לבית בארבע האפליקציות, וכל שינוי בהם נעשה בארבעתן באותו סבב:

| בלוק | סמנים | חתימה (sha256, 16 תווים) |
|---|---|---|
| CSS אזור המצב | `/* ═══ אזור מצב — CSS משותף (סבב 15/17)` … `/* ═══ סוף CSS אזור המצב` | `aa10b52e9ce1157e` |
| «מידע טכני» | `/* ═══ "מידע טכני" — קיפול מסכי האחסון` … `/* ═══ סוף רכיב "מידע טכני"` | `83096d1117e813f0` |
| «☁️ סנכרון» | `/* ═══ אזור מצב — בלוק "☁️ סנכרון"` … `/* ═══ סוף בלוק "☁️ סנכרון"` | `65ff4f75bc4f6756` |

**ה-API:** `syncFmtTime` · `syncLineHTML` · `syncStatusHTML` ·
`syncStatusMount` · `syncNowFromStatus` · `statusAreaMount` ·
`techInfoHTML`/`techInfoToggle`/`techInfoMount`.

מעל הבלוק יושבת **`SYNC_CFG`** — הדבר היחיד שנבדל בין האפליקציות — עם שני
שדות בלבד: `lastSync()` (חותמת הסנכרון המוצלח האחרון) ו-`sync()` (מה
שהכפתור מפעיל, מחזיר Promise).

### ⚠️ שלוש הבהרות שאין לפספס
1. **הכפתור «סנכרן עכשיו» אינו מנגנון סנכרון.** הסנכרון האוטומטי ממשיך
   לרוץ ברקע בדיוק כמו קודם; זו רק דרך לבקש אותו עכשיו ולראות תשובה. לכן
   גם **אין בבלוק טוסט כשל משלו** — מסלול הסנכרון של כל אפליקציה כבר מדווח
   על כשל פעם אחת, וכפילות הייתה רעש.
2. **`lastSync()` אינו עֵד דחיפה ואינו משמש לפינוי.** הוא חותמת תצוגה,
   שנכתבת על כל שיחה מוצלחת עם הענן — כולל בדפדפן שרק קורא. לפינוי יש
   `_ysPushedAt`/`_tbPushedAt`/`syncedThrough` (סבבים 11 ו-14), **ואין
   לערבב ביניהם.** ⛔ אין להזין `LS_CFG.syncedThrough` מ-`lastSync()`.
3. **הבלוק אינו נשען על מחלקות הכרטיס של אף אפליקציה** — הן נבדלות ביניהן
   — אלא מרנדר חלונית משלו (`.sync-box`), וה-CSS יורש צבע וגופן. זה מה
   שמאפשר לו להישאר זהה בית-לבית.

### מצב הפתיחה של «מידע טכני» חי בזיכרון
`_techInfoOpen` (משתנה מודול, **לא** localStorage) שומר אם הרכיב פתוח.
בלעדיו, אפליקציה שבונה את מסך ההגדרות מחדש בכל רינדור מקבלת עוגן ריק,
ה-`details` הקודם כבר לא קיים, והרכיב נסגר בכל שמירה ובכל ציור מושהה של
⏳ (סבב 16). המצב בזיכרון בכוונה: זו העדפת תצוגה של הרגע, ולא נתון שראוי
לצרוך ממנו מכסת אחסון משותפת.

### הודעת חזרת הסנכרון — נוסח אחד
**`MSG_SYNC_BACK = '☁️ הסנכרון חזר לפעול'`** בארבע האפליקציות, לצד
`MSG_OFFLINE`/`MSG_SAVED`/`MSG_BAD_LOGIN`. הוא החליף את «☁️ החיבור לנתונים
חזר» ב-schar-limud וב-gius, ונוסף ל-yoman שלא הייתה לה הודעת התאוששות
בכלל. ⛔ **אין להשתמש במחרוזת קשיחה** — הבדיקה נכשלת עליה.

### הבדיקה האוכפת
`tools/check-status-area.mjs` (ב-gius: `scripts/`) — **זהה בית-לבית
בארבעת הריפו פרט לבלוק `APP` שבראשו**, ורצה עם שערי התחביר לפני כל דחיפה.
היא נכשלת על שלושת סוגי הסטייה:
- **א. מיקום** — `sync-status-box` ו-`tech-info-box` אינם שני ה**ילדים
  הישירים** האחרונים של מיכל ההגדרות, בסדר הזה. ההשוואה היא על ילדים
  ישירים דווקא: עוגן שיושב בתוך כרטיס היה עובר בדיקה רופפת («שני המזהים
  מופיעים בסוף») גם כשמשהו אחר מוצג אחריו. ב-gius, שבונה את המסך כמחרוזת,
  הבדיקה היא על זנב `viewSettings()`.
- **ב. קוד** — אחד משלושת הבלוקים אינו תואם לחתימה שבטבלה למעלה.
- **ג. חיווט** — `statusAreaMount()` אינה נקראת בשום מקום מחוץ לבלוק
  המשותף, כלומר הרכיב קיים אך אינו מחובר. זו בדיוק התקלה של gius.
  בנוסף נכשלת על שריד `id="pend-status-box"` מחוץ לאזור המצב.

**שינוי מכוון בבלוק משותף** = עדכון הבלוק בארבע האפליקציות **ובארבעת
עותקי קובץ הבדיקה** (החתימות כתובות בתוכו), באותו סבב.

### תיאום גרסאות
`sw.js`: hanhala **v33** · yoman **v21** (`app-version` **15-2026-08-12-1**) ·
schar **v23** · gius **gius-v9**.
<!-- SHARED:end -->

### בדיקה
**60 טענות** על הקוד האמיתי המחולץ מ-`index.html` של ארבע האפליקציות
(`test_status_area.mjs`, בסשן — שני הבלוקים המשותפים רצים ב-`vm` מעל DOM
מזויף שמדמה את החוזה שהקוד נשען עליו, כולל `ontoggle` שנורה גם על פתיחה
תכנותית): שורת המצב בשלושת מצביה (טרם הסתנכרן / מועד אמיתי / מנותק) ·
`pendStatusHTML` **לפני** שורת המצב · שני העוגנים החסרים כ-no-op ·
«סנכרן עכשיו» שנועל בזמן ריצה ולא מפעיל סנכרון כפול · `statusAreaMount`
שמרכיב סנכרון→טכני בסדר הזה · ⭐ **מצב פתוח ששורד רינדור מלא של העוגן**
(התרחיש של gius) וסגירה שנשמרת גם היא · `MSG_SYNC_BACK` · וזהות בית-לבית
של שלושת הבלוקים ושל קובץ הבדיקה.

אומת גם ב-**מוטציות**: היפוך סדר העוגנים, אלמנט שנוסף אחריהם, עוגן שנעטף
בכרטיס, שינוי בבלוק המשותף והסרת החיווט החי — כל אחת מפילה את
`check-status-area.mjs`; והחזרת מצב הפתיחה ל-DOM בלבד מפילה את טענת
ה-⭐. `node --check` על ה-JS המחולץ ועל `sw.js` עבר בארבעתן, וכן
`sync-setup-sql.py --check` ב-schar ו-`scripts/check-js.mjs` ב-gius.
המודולים הקפואים לא נגעו — בלוק האחסון (סבב 14) ובלוק ה-pending (סבב 16)
זהים בית-לבית ל-HEAD בארבע האפליקציות.

### מה נעשה כאן (yoman-avoda, `sw.js` v21, `app-version` 15-2026-08-12-1)
- כרטיס «⏳ ממתין לסנכרון» ישב **באמצע** `panel-settings` (בין הוואטסאפ
  לעריכת הקטגוריות) והועבר לאזור המצב בסוף הפאנל.
- **כפתור «🔄 סנכרן» ותווית «☁️ ענן» הוסרו מהכותרת.** הם היו הפעולה והמידע
  היחידים שחיו מחוץ להגדרות, ושניהם קיימים עכשיו באזור המצב — כפתור
  «סנכרן עכשיו» לצד מועד הסנכרון האחרון ומצב החיבור. ⚠️ `syncFromCloud`
  עצמה לא נגעה; רק אתר הקריאה אליה זז.
- **`pendRenderStatus` הוסרה** (וכך גם העוגן `pend-status-box`);
  `renderSettings()` קורא `statusAreaMount()`.
- **`_tbLastSyncAt` + `_tbMarkSynced()`** — חותמת תצוגה על כל שיחה מוצלחת עם
  הענן (`autoSyncData` ו-`syncFromCloud`). ⛔ **אינה `_tbPushedAt`** ואינה
  נוגעת בפינוי (סבבים 11 ו-14).
- **טוסט חזרת הסנכרון נוסף כאן לראשונה.** הכישלון נשאר שקט בכוונה — הפולינג
  הוא כל 3 שניות, וטוסט על כל מחזור היה רעש — אבל **החזרה** נאמרת: זה הרגע
  שבו מה שהמתין מתחיל לעלות. הדגל הוא `_tbNetWarned`.
- הבדיקה: `node tools/check-status-area.mjs` — **שער חובה נוסף לפני push**.

---

<!-- SHARED:start id="iron-rule-8-docs" -->
## ⭐ כלל ברזל 8 — פרוטוקול תיעוד קבוע (סבב 18, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את שבעת כללי הברזל שלמעלה.

> **התיעוד עצמו הוא רכיב משותף, ולכן חלים עליו אותם תנאים שכלל ברזל 7
> קבע לקוד משותף: אותו נוסח, במקום קבוע, ומסומן במפורש. פרק שאינו מסומן
> «משותף» הוא פרטי לאפליקציה — ואין לגעת בו על סמך נוסח מריפו אחר.**

### הבעיה שנמדדה
ביקורת חיצונית סרקה את ארבעת קבצי ה-CLAUDE.md ומצאה עשרות פרקים שאמורים
להיות זהים. רובם אכן היו — אבל שום מנגנון לא אכף את זה, ולכן ארבעה נסחפו
בלי שאיש שם לב. באחד מהם הסחיפה הייתה **הפוכה**: פרק «בדיקה» של סבב 14
הועתק כלשונו לארבעת הריפו, כולל לשתי אפליקציות שבהן הבדיקות שהוא מתאר
מעולם לא רצו ולא יכלו לרוץ (אין בהן `verify` ואין `tier2`). תיעוד
שמועתק «כי כך כתוב אצל האחות» גרוע מתיעוד חסר: הוא נקרא כעדות.

### ההגדרה המחייבת — סימון משותף/פרטי
כל פרק שאמור להיות זהה בארבעת הריפו עטוף בסימוני HTML-comment:

```html
<!-- SHARED:start id="שם-מזהה-קצר" -->
…
<!-- SHARED:end -->
```

**הסימון הוא מקור האמת:**
- מה שבתוך הסימון — **חייב** להיות זהה בית-לבית בארבעת הריפו.
- מה שמחוצה לו — **פרטי לאפליקציה**, ואיש אינו מיישר אותו.

הסימון עוטף **רצף** של פרקים, לא בהכרח פרק בודד: פרק סבב שכולו משותף
פרט לזנב פר-אפליקציה (למשל שורת «**כאן:**» שבסוף «מזהים», או «מה נעשה
כאן») נחתך בדיוק שם — הרצף המשותף בפנים, הזנב בחוץ.

### שבעת הכללים
1. **כל סבב מוסיף פרק סבב במקום קבוע ובמבנה קבוע** — בסוף הקובץ, אחרי
   פרק הסבב הקודם, עם שלושת התת-פרקים ובסדר הזה: **מה נעשה** (משותף) ·
   **מה נעשה כאן** (פר-אפליקציה) · **אימותים** (פר-אפליקציה).
2. **כל סבב מקדם את שורת «עודכן לאחרונה»** שבראש הקובץ, בפורמט הקבוע
   `עודכן לאחרונה: סבב N · YYYY-MM-DD`. גם סבב שנגע רק בקובץ אחד מקדם
   אותה בקובץ שבו נגע.
3. **סבב שנוגע בקוד משותף חייב לעדכן את הפרק המשותף בארבעת הריפו באותו
   סבב** — לעולם לא באחד בלבד. פרק מסומן SHARED שמשתנה, משתנה בארבעתם,
   באותו שינוי בדיוק, ובאותו קומיט-סבב.
4. **⛔ אסור לסשן לשנות תוכן של פרק שאינו מסומן SHARED על סמך נוסח מריפו
   אחר.** זה בדיוק מה שייצר את באג «84 הטענות»: טקסט נכון באפליקציה אחת
   שהועתק לשלוש שבהן הוא שקרי. אם נראה שפרק פרטי «חסר» משהו שיש לאחות —
   זו אינדיקציה לבדוק את **המציאות באפליקציה הזו**, לא להעתיק.
5. **«בדיקה» ו«חתימת APK» הם פרקים פרטיים בהגדרה.** «בדיקה» מתאר מה
   באמת רץ באפליקציה הזו ולכן הוא נבדל בהכרח; «חתימת APK» מחזיק
   keystore, alias וטביעת SHA ייחודיים — **איחוד שלו יהיה שגוי ומסוכן**,
   ולכן הבדיקה נכשלת אם הוא סומן SHARED.
6. **סבב שכותב בדיקות שומר אותן כקובץ בריפו.** רתמה שרצה בסשן בלבד
   נעלמת ברגע שהסשן נסגר — הסשן הבא אינו יכול להריץ אותה, לא לפני שינוי
   ולא אחריו, ולכן היא אינה מגינה על דבר.
   **⛔ ובפרט: אין לתעד בדיקות כאלה כאילו הן קיימות.** «N טענות עברו»
   בפרק סבב, בלי קובץ שאפשר להריץ, נקרא כעדות שאינווריאנטה שמורה — בזמן
   שאין שום דבר ששומר עליה. זה בדיוק הכשל של סעיף 4, בכיוון אחר: תיעוד
   שמתאר מציאות שאיננה.
   ⚠️ הכלל חל על **בדיקות** — טענות שניתן להריץ מחדש. סריקה חד-פעמית או
   מדידה שנעשתה כדי להכריע החלטה אינן בדיקות; הן נרשמות כמדידה, ואין
   להציג אותן כשער.
7. **⛔ אין לזרוע פרטי כניסה — שם משתמש, סיסמה, מפתח, טוקן — בשום קובץ
   שנדחף לגיט.** לא בסכימה, לא במיגרציה, לא בקוד, לא ב-README ולא בקובץ
   בדיקה. ארבעת הריפו **ציבוריים**, ומפתחות ה-anon של Supabase יושבים
   ב-`index.html` — כלומר סיסמה שנדחפה היא סיסמה שכל אחד יכול להשתמש בה
   מול המסד החי.
   ⚠️ **ומחיקה מאוחרת אינה מסירה אותה.** הערך נשאר קריא בהיסטוריית ה-git
   לנצח, ומי שכבר מצא אותו מחזיק בו. הטיפול היחיד בסוד שנדחף הוא
   **להחליף אותו במסד**, ורק אחר כך לנקות את הקובץ.
   המשתמש הראשון נוצר **ידנית**, וקובץ ההתקנה מסביר איך — עם מצייני מקום
   ולא עם ערכים אמיתיים.

### הבדיקה האוכפת
`tools/check-docs.mjs` (ב-gius: `scripts/`, ו-`check-js.mjs` קורא לה) —
**זהה בית-לבית בארבעת הריפו פרט לבלוק `APP` שבראשה**, ורצה עם שערי
התחביר לפני כל דחיפה. היא נכשלת על חמישה סוגי סטייה:
- **א. סימון** — `SHARED:start`/`SHARED:end` אינם מאוזנים, מקוננים, בלי
  `id`, עם `id` כפול, או שרשימת המזהים בקובץ אינה תואמת לרשימה הקנונית.
- **ב. תוכן** — תוכנו של בלוק SHARED אינו תואם לחתימת ה-sha256 הקנונית
  הרשומה בקובץ הבדיקה.
- **ג. שורת העדכון** — «עודכן לאחרונה» חסרה, אינה בראש הקובץ, או בפורמט
  שגוי.
- **ד. פרק פרטי שסומן** — «חתימת APK» או «בדיקה» נמצאים בתוך בלוק
  SHARED, בניגוד לסעיף 5.
- **ה. שורת עדכון שלא קודמה** (סבב 20) — `CLAUDE.md` שונה מול
  `origin/main` אבל שורת «עודכן לאחרונה» זהה לזו שב-`origin/main`.
  סעיפים א–ד בודקים את **צורת** השורה; זה בודק שהיא באמת **קודמה**.
  ⚠️ **הסעיף הזה מדלג ואינו מפיל** כשאין git, כשאין `origin/main`, או
  כש-`CLAUDE.md` אינו קיים שם — ר' פרק סבב 20.

סימונים שבתוך גדר קוד (```) אינם נספרים — אחרת הדוגמה שבפרק הזה עצמו
הייתה נקראת כבלוק אמיתי.

**שינוי מכוון בפרק משותף** = עדכון הפרק בארבעת הריפו **ובארבעת עותקי
קובץ הבדיקה** (החתימות כתובות בתוכו), באותו סבב. אותו נוהל בדיוק שכלל
ברזל 7 קבע לבלוקי הקוד המשותפים.
<!-- SHARED:end -->

---

<!-- SHARED:start id="round-18-what" -->
## ⭐ סבב 18 (אוגוסט 2026) — תיעוד אחיד ואכיפה

### מה נעשה
**סבב תיעוד וכלי בדיקה בלבד.** `index.html`, `sw.js` וגרסאות המטמון
**לא נגעו באף אחת מארבע האפליקציות**; התוספת היחידה מחוץ ל-CLAUDE.md היא
קובץ הבדיקה החדש והקריאה אליו בשער הדחיפה.

1. **יישור הפרקים שנסחפו.** מדידה של ארבעת הקבצים מצאה **46 כותרות
   משותפות לארבעתם, מהן 40 זהות בית-לבית ו-6 שנסחפו** (שלוש מהן הן
   שלושת מופעי «בדיקה»). הטיפול:
   - **«בדיקה» (סבב 14)** — 🔴 החמור מכולם. הנוסח «84 טענות» היה זהה
     בארבעת הריפו, אבל התצורה שהוא מתאר — `tier2` + `idOf` + `verify` +
     מבחן הקבלה — קיימת רק ב-hanhala וב-yoman. ב-schar-limud וב-gius
     סבב 14 היה **עדכון המודול בלבד**, ו-`tier1`/`tier2` נשארו ריקים
     במפורש ומנומק. הפרק נכתב מחדש בכל אפליקציה כך שיתאר את מה שבאמת
     רץ בה, **והוכרז פרטי** (כלל ברזל 8, סעיף 5).
   - **«מזהים»** — הכלל עצמו כבר היה זהה בית-לבית; מה שנבדל הוא שורת
     «**כאן:**» שבסופו, שהיא פר-אפליקציה מעצם טבעה. הכלל סומן SHARED
     והשורה נשארה מחוצה לו, ומשפט הפתיח של כלל ברזל 6 עודכן בהתאם.
   - **«כללי ענפים»** — ב-gius היה נוסח מקוצר (שתי נקודות במקום ארבע),
     כי הוא נכתב מחדש כשנוסף מנקה הענפים ואיבד את שתי הנקודות על אישור
     המיזוג ועל מחיקת ענפים מרוחקים. אוחד לנוסח הקנוני בן ארבע הנקודות.
   - **«חתימת APK — מפתח קבוע»** — ⛔ **לא אוחד ולא ייאוחד.** לכל
     אפליקציה keystore, alias וטביעת SHA משלה, ואיחוד כאן היה שגוי
     ומסוכן. הפרק פרטי בהגדרה, והבדיקה נכשלת אם יסומן SHARED.
2. **סימון משותף/פרטי** — עשרה בלוקי `SHARED` בכל קובץ, שעוטפים יחד
   43 פרקים: `branch-rules` · `iron-rules-storage` · `pending-module` ·
   `iron-rule-6-sync` · `round-14-verified-sweep` · `round-15-tech-info` ·
   `round-16-pend-delay` · `iron-rule-7-status-area` · `iron-rule-8-docs` ·
   `round-18-what`.
3. **שורת «עודכן לאחרונה»** נוספה בראש כל אחד מארבעת הקבצים.
4. **כלל ברזל 8** — פרוטוקול התיעוד הקבוע (הפרק שמעל).
5. **`check-docs.mjs`** — הבדיקה האוכפת, בדפוס `check-status-area.mjs`
   של סבב 17, ונוספה לשער הדחיפה בארבעת הריפו.

**משפטי הפתיח של פרקי הסבבים תוקנו לפי הגבול האמיתי** של כל בלוק: סבבים
14/16/17 אומרים עכשיו «עד הסעיף «בדיקה»» (ולא «עד הסעיף «מה נעשה כאן»»,
שהיה כולל בתוכו את «בדיקה» ולכן היה מקור הבאג); פרק «⏳ סימון ממתין
לסנכרון» אומר «עד הסעיף «מה נעשה כאן»»; וכלל ברזל 6 אומר «עד שורת
«כאן:» שבסופו».
<!-- SHARED:end -->

### מה נעשה כאן (yoman-avoda)
- «כללי ענפים» כבר היה בנוסח הקנוני — לא נגע, רק סומן.
- **«בדיקה» של סבב 14 נכתב מחדש** ומתאר עכשיו את התצורה שבאמת נבדקה כאן:
  `tier2` על `tb_archive`+LS ו-`tb_entries`+LS, `idOf` לפי `archiveKey`/
  `entryKey` (אותם מפתחות שבמנוע המיזוג), `verify: _tbVerify` מעל
  `sbGetResult`, ו-`syncedThrough` פר-מפתח מ-`_tbPushedAt`.
- עשרה בלוקי SHARED סומנו. **«חתימת APK — מפתח קבוע»** (`signing/yoman.keystore`,
  alias `yoman`, SHA256 `29:F5:0B:29:…`) נשאר **מחוץ** לסימון, כמתחייב.
- `tools/check-docs.mjs` נוסף, וקריאה אליו נוספה לפרק «לפני כל push» —
  שער חובה שלישי לצד `node --check` ו-`node tools/check-status-area.mjs`.
- ⛔ `index.html` ו-`sw.js` **לא נגעו**. `CACHE_NAME` נשאר **v21** ו-
  `<meta name="app-version">` נשאר **15-2026-08-12-1** (סבב 17). אין שינוי
  קוד, ולכן אין מה לקדם — קידום כאן היה מייצר באנר «גרסה חדשה זמינה»
  לשום שינוי.

### אימותים
- **`check-docs.mjs` עובר בארבעת הריפו:** שורת העדכון, עשרה בלוקי SHARED
  מאוזנים, כל עשר החתימות תואמות, ופרקי «חתימת APK» ו«בדיקה» מחוץ לבלוקים.
- **הזהות נמדדה בפועל, לא הוכרזה** — עשרת הבלוקים חושבו בארבעת הקבצים
  והושוו זה לזה: זהים. וקובץ הבדיקה עצמו זהה בארבעת הריפו פרט לשורת
  ה-`app` שבבלוק `APP` (אומת ב-`diff`).
- **הבודק אינו חותמת גומי — תשע מוטציות, כל אחת מפילה אותו:**
  1. שינוי מילה בתוך בלוק SHARED («ענף ייעודי» ← «ענף נפרד») ⇒ חתימה שונה.
  2. הסרת `SHARED:end` אחד ⇒ קינון + בלוק חסר + חתימה שגויה.
  3. הסרת שורת «עודכן לאחרונה» ⇒ חסרה.
  4. סימון SHARED על «חתימת APK» ⇒ מזהה לא-קנוני **וגם** פרק פרטי בתוך בלוק.
  5. `id` כפול ⇒ כפילות + בלוק חסר.
  6. `SHARED:start` בלי `id` ⇒ סימון פסול + `end` בלי `start`.
  7. פורמט שגוי בשורת העדכון (`12.8.2026`) ⇒ פורמט.
  8. שורת העדכון שהוזזה לאמצע הקובץ ⇒ אינה בראש הקובץ.
  9. סימון SHARED על «בדיקה» — **הבאג שהסבב הזה בא למנוע** ⇒ נכשל.
  ובקרה חיובית: הדוגמה שבתוך גדר הקוד בפרק כלל ברזל 8 **אינה** נספרת
  כבלוק, והבדיקה עוברת על המסמך כמות שהוא.
- ⛔ **`git diff` על `index.html` ו-`sw.js` — ריק בארבעת הריפו.**
- שאר השערים הורצו ועברו ללא שינוי: `node --check` על ה-JS המחולץ ועל `sw.js`,
  ו-`node tools/check-status-area.mjs`.

---

<!-- SHARED:start id="round-20-what" -->
## ⭐ סבב 20 (אוגוסט 2026) — «עודכן לאחרונה» שקודמה באמת

### מה נעשה
**סבב כלי בדיקה ותיעוד בלבד.** `index.html`, `sw.js` וגרסאות המטמון
**לא נגעו באף אחת מארבע האפליקציות**; השינוי היחיד מחוץ ל-CLAUDE.md הוא
סעיף אחד שנוסף ל-`check-docs.mjs`.

סבב 18 דיווח בכנות על פער בבודק שהוא עצמו הוסיף, ואומת חיצונית שהפער
אכן קיים: הבודק ידע לוודא ש**קיימת** שורת «עודכן לאחרונה», שהיא בראש
הקובץ ושהפורמט תקין — כלומר את **צורת** השורה בלבד. סשן שערך פרק פרטי
והשאיר «סבב 18» עבר את השער בלי הערה, וזו בדיוק ההפרה של סעיף 2 בחמשת
הכללים שלמעלה.

**התיקון — סעיף ה בבודק:** אם `CLAUDE.md` שונה מול `origin/main` אבל
שורת «עודכן לאחרונה» זהה לזו שב-`origin/main` — הבדיקה נכשלת בהודעה
**«CLAUDE.md שונה ושורת העדכון לא קודמה»**. ההשוואה היא בין **הקובץ
שעל הדיסק** לבין `git show origin/main:./CLAUDE.md`, ולכן היא תופסת גם
שינוי שכבר קומט בענף וגם שינוי שטרם קומט — ואינה תלויה ב-`git status`.

שני מקרים שאינם כישלון, במכוון: **קובץ זהה ל-`origin/main`** (אין
שינוי, אין מה לקדם) ו**שורת עדכון שלא קיימת ב-`origin/main`** (היא
נוספת בשינוי הזה, כלומר קודמה מאין).

### ⚠️ מדלג ולא מפיל — היעדר בסיס להשוואה אינו סטייה
לסעיף ה, ולו בלבד, יש מסלול דילוג: **אין `git` בסביבה** · **אין
`origin/main`** · **`CLAUDE.md` אינו קיים ב-`origin/main`**. בכל אחד
מהם מודפסת אזהרה מפורשת (⚠️) עם הסיבה, **והשער אינו נופל** — clone
רדוד, ריפו בלי remote או ארכיון שחולץ מ-tarball הם מצבים לגיטימיים
בסביבת בנייה, ושער שנשבר בהם היה גורם לעקיפה של הבדיקה כולה. ארבעת
הסעיפים האחרים אינם מדלגים לעולם.

⛔ **אין להפוך את הדילוג לכישלון ואין להרחיב אותו** לשום מקרה אחר:
הראשון שובר סביבות בנייה, והשני מחזיר בדיוק את הפער שהסבב הזה סגר.
<!-- SHARED:end -->

### מה נעשה כאן (yoman-avoda)
- `tools/check-docs.mjs` — נוסף סעיף ה, ולוגיקת גדרות-הקוד חולצה
  ל-`fenceMask()`/`stampOf()` כדי ששני צדי ההשוואה (הדיסק ו-`origin/main`)
  יזהו את שורת העדכון באותה דרך בדיוק. שאר הסעיפים לא נגעו.
- שני בלוקי SHARED עודכנו: `iron-rule-8-docs` (חמישה סוגי סטייה במקום
  ארבעה) ו-`round-20-what` שנוסף — ושתי החתימות עודכנו בארבעת עותקי
  קובץ הבדיקה, באותו סבב.
- שורת «עודכן לאחרונה» קודמה ל-**סבב 20 · 2026-08-12**.
- ⛔ `index.html` ו-`sw.js` **לא נגעו**. `CACHE_NAME` נשאר **v21** ו-
  `<meta name="app-version">` נשאר **15-2026-08-12-1** (סבב 17) — אין שינוי
  קוד, וקידום כאן היה מייצר באנר «גרסה חדשה זמינה» לשום שינוי.

### אימותים
- **שלושת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html` ועל
  `sw.js`, `node tools/check-status-area.mjs`, ו-`node tools/check-docs.mjs`.
- **⭐ מוטציית הפער — הבדיקה שהסבב הזה נועד לה:** שינוי תוכן ב-`CLAUDE.md`
  בלי לקדם את שורת העדכון ⇒ השער **נפל** בהודעה «CLAUDE.md שונה ושורת
  העדכון לא קודמה». לפני הסבב אותה מוטציה בדיוק **עברה**.
- **מסלולי הדילוג נבדקו ואינם מפילים:** ריפו בלי `origin/main` ⇒ אזהרה
  ויציאה 0; `git` שאינו זמין ב-`PATH` ⇒ אזהרה ויציאה 0.
- ⛔ **`git diff` על `index.html` ו-`sw.js` — ריק.**


---

<!-- SHARED:start id="iron-rule-9-security-spread" -->
## ⭐ כלל ברזל 9 — שדרוג אבטחתי מתפשט (סבב 21, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את שמונת כללי הברזל שלמעלה.

> **כשסבב מחליף מנגנון אבטחה באפליקציה אחת — טיפול בסודות, אימות, הצפנה,
> הרשאות — הוא חייב לרשום במפורש, בפרק הסבב, האם המנגנון החדש רלוונטי גם
> לשאר האפליקציות. רלוונטי ולא יושם ⇐ נרשם פער מתועד עם טריגר.
> שקט אינו תשובה.**

### הבעיה שנמדדה
סבב 13 העביר את schar-limud מהשוואת סיסמה גלויה ל**טביעת PBKDF2-SHA256**.
ההחלטה שם נכונה ומנומקת: מיגרציה 009 הורידה את `sl_settings` למראה המקומי,
ו-`admin_pass` בטקסט גלוי **על הדיסק** לא היה מתקבל על הדעת.

מה שלא נעשה: **לא נרשם באותו סבב אם המנגנון רלוונטי גם לשלוש האחיות.**
לא «כן והוחל», לא «לא, וזו הסיבה» — פשוט כלום. התוצאה היא ש-hanhala
ו-gius המשיכו שמונה סבבים עם מודל סיסמאות אחר לגמרי, **בלי שאיש החליט
על כך** — ההבדל התגלה רק בביקורת של סבב 21. תיעוד שותק אינו מתעד החלטה;
הוא מסתיר את היעדרה.

### שלוש התשובות המותרות — ורק שלוש
סבב שנגע במנגנון אבטחה חייב לרשום **אחת** מהן, במפורש:
1. **הוחל בכולן** — עם ציון מה השתנה בכל אחת.
2. **אינו רלוונטי לאחרות** — עם **הנימוק**, לא כהצהרה. «אין שם סוד על
   הדיסק» הוא נימוק; «לא נדרש» אינו.
3. **רלוונטי, ולא יושם** — ואז **פער מתועד עם טריגר**: מה חסר, מה הסיכון,
   ו**מה האירוע שמחייב לפתוח את הסבב** (ר' הדוגמה החיה: «הוספת משתמשים
   ל-gius» בפרק הפער שם).

⛔ **מה שאסור הוא הרביעית — לא לכתוב כלום.**

### ⚠️ הכלל דורש **רישום**, לא יישור — ואינו גובר על כלל ברזל 8 סעיף 4
הרישום נעשה **בפרק הפרטי של כל אפליקציה, לפי המציאות שלה**. כלל ברזל 9
**אינו** רישיון להעתיק נוסח מריפו אחר, ו**אינו** מחייב שהמנגנונים יהיו
זהים: אפליקציות שונות חיות במודלי איום שונים, ואחידות מנגנון אינה מטרה
בפני עצמה. שני הכללים אומרים יחד דבר אחד: **מה שנבדל — נבדל בכוונה
ובכתב.**

### ⛔ הלקח נרשם בארבעתן — גם כשהמנגנון רלוונטי רק לאחת (סבב 26)
**לקח אבטחתי שנלמד באפליקציה אחת נרשם כהערת ⛔ בארבע האפליקציות**, ליד
המקום בקוד שבו הוא עלול להישבר — ולא רק באפליקציה שבה הוא נלמד.

⚠️ **וזו אינה כפילות של שלוש התשובות שלמעלה.** הרישום שהן מחייבות הוא
**החלטה**: «הוחל» / «אינו רלוונטי, וזה הנימוק» / «פער עם טריגר», והוא
יושב בפרק הפרטי של כל אפליקציה. ההערה בקוד עונה על שאלה אחרת לגמרי —
**מה אסור לעשות כאן בעתיד.** אפליקציה שהמנגנון «אינו רלוונטי» לה עדיין
חשופה ל**כשל** שהוליד אותו: סשן עתידי יוסיף בתום לב בדיוק את הדבר
שהוסר במקום אחר, מפני שאצלו הוא מעולם לא היה — ולכן גם מעולם לא נכתב
אצלו שאסור.

> **הערה שיושבת במקום אחד אינה מגינה על הארגון.** היא מגינה על הקובץ
> שהיא יושבת בו, ועל שום קובץ אחר.

**הדוגמה החיה** היא הסבב שהוליד את הסעיף: שער הסיסמה הנפרד של מסך
ההגדרות הוסר מ-schar-limud (סבב 26), ובשלוש האחיות שער כזה לא היה
מעולם — כלומר לפי שלוש התשובות, «אינו רלוונטי». ובכל זאת ההערה נוספה
**בארבעתן**, בראש אזור ההגדרות: לכל אחת מהן יש מסך הגדרות, וכל אחת מהן
היא מקום שבו אפשר להמציא את השער הזה מחדש.

### ⛔ החלטת מודל אבטחה היא של המנהל, לא של הסשן
מנגנון שנבחר במפורש ע"י המנהל — כולל בחירה **לא** להקשיח — אינו «חוב
טכני» שסשן עתידי מוסמך לסגור מיוזמתו. סבב שמוצא פער מתועד **מדווח עליו
ומציע**; ההגירה עצמה מחייבת החלטה מפורשת. ר' הדוגמה החיה בפרקי הסיסמאות
של hanhala ושל gius: שם ההחלטה על טקסט גלוי היא **בחירה תפעולית מודעת**,
והמרתה ל-PBKDF2 בלי אישור הייתה **שוברת יכולת ניהול שהמנהל בחר בה בכוונה**.
<!-- SHARED:end -->

---

<!-- SHARED:start id="round-21-what" -->
## ⭐ סבב 21 (אוגוסט 2026) — לקחים תפעוליים, כלל ברזל 9, ושני תיקוני אבטחה

### מה נעשה
סבב מעורב: **תיעוד בארבעת הריפו, וקוד ב-hanhala-ruchanit בלבד.**
`index.html` ו-`sw.js` של yoman-avoda, schar-limud ו-gius **לא נגעו**,
ולכן גם `CACHE_NAME` שלהן לא קודם — קידום בלי שינוי קוד מייצר באנר
«גרסה חדשה זמינה» לשום שינוי.

1. **שני כללי תפעול נוספו ל«כללי ענפים»** (בלוק `branch-rules`): רענון
   `main` ב-`--ff-only` לפני כל מיזוג, ו-`git -C <path>` בכל פקודה בעבודה
   רב-ריפו. שניהם נלמדו מכשל שנמדד בסבב 20, ושניהם מתוארים שם במלואם.
2. **כלל ברזל 9 — שדרוג אבטחתי מתפשט** (הפרק שמעל).
3. **תיעוד ההחלטה על מודל הסיסמאות ב-hanhala וב-gius** — פרק **פרטי**
   בכל אחת מהן, שמעגן את הטקסט הגלוי בשש ספרות כ**בחירה תפעולית מודעת**
   לפי מודל האיום, ולא כפער שממתין לסגירה. זהו היישום הראשון של כלל
   ברזל 9: schar-limud היא החריגה, והנימוק שלה כתוב — סבב 13 הוריד שם
   את `sl_settings` למכשיר.
4. **hanhala — הסרת מסלול האתחול `admin`/`123456`** (פרטי, ר' שם).
5. **hanhala — צמצום `ys_users_cache` למשתמש המחובר בלבד** (פרטי, ר' שם).

### תיאום גרסאות
`sw.js`: hanhala **v34** · yoman **v21** (`app-version` **15-2026-08-12-1**) ·
schar **v24** · gius **gius-v10**. **רק hanhala קודמה** — היא היחידה שקודה
השתנה בסבב הזה.
<!-- SHARED:end -->
### מה נעשה כאן (yoman-avoda)
- **תיעוד בלבד.** בלוק `branch-rules` עודכן, ושני בלוקים חדשים נוספו:
  `iron-rule-9-security-spread` ו-`round-21-what`. שלוש החתימות עודכנו
  בארבעת עותקי `check-docs.mjs`, באותו סבב.
- שורת «עודכן לאחרונה» קודמה ל-**סבב 21 · 2026-08-12**.
- **כלל ברזל 9, סעיף 2 — הרישום הנדרש לאפליקציה הזו:** מנגנון ה-PBKDF2
  של schar-limud (סבב 13) **אינו רלוונטי כאן, ויש לכך נימוק ולא הצהרה** —
  ⛔ **ליומן אין שער סיסמה ואין טבלת משתמשים כלל.** הכניסה היא בחירת מוסד
  (ראשון לציון / רמת אביב), ואין בקוד שום סוד להשוות מולו: אין `ys_users`,
  אין `admin_pass`, ואין ערך סיסמה שיורד ל-localStorage או לענן. אין כאן
  מה להקשיח, ולכן גם אין פער ואין טריגר.
- ⛔ `index.html` ו-`sw.js` **לא נגעו**. `CACHE_NAME` נשאר **v21** ו-
  `<meta name="app-version">` נשאר **15-2026-08-12-1** (סבב 17).

### אימותים
- **שלושת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html` ועל
  `sw.js`, `node tools/check-status-area.mjs`, ו-`node tools/check-docs.mjs`.
- **שלושת הבלוקים החדשים/המעודכנים נמדדו כזהים בית-לבית בארבעת הקבצים** —
  חושבו בארבעתם והושוו זה לזה, לא הוכרזו.
- ⛔ **`git diff` על `index.html` ו-`sw.js` — ריק.**

---

<!-- SHARED:start id="round-24b-what" -->
## ⭐ סבב 24 — השלמה (אוגוסט 2026): מקור אמת אחד, בלי סודות זרועים

### מה נעשה
השלמה לסבב 24, לפני המיזוג. **התיעוד המשותף השתנה בארבעת הריפו; קוד
נגע ב-schar-limud וב-gius בלבד.**

1. **כלל ברזל 8 גדל לשבעה כללים** (הפרק שמעל) — שני החדשים נלמדו ממה
   שנמדד בסבב 24 עצמו:
   - **סעיף 6 — בדיקות נשמרות כקובץ.** סבב 24 כתב 109 טענות, הריץ אותן,
     ותיעד אותן — ולא שמר אותן. הפרק «אימותים» הכריז על אינווריאנטה
     («`password` אינו נוגע בדיסק») שאין שום שער שמגן עליה, בדיוק הדפוס
     שסעיף 4 בא למנוע.
   - **סעיף 7 — ⛔ אין לזרוע פרטי כניסה.** שתי מיגרציות התקנה זרעו
     חשבונות: `admin`/`admin` ב-schar (סיסמת ברירת מחדל גנרית, הוסרה
     בסבב 24) ו**סיסמה אמיתית של המנהל** ב-gius — בריפו ציבורי, מול
     מסד שה-RLS שלו `using (true)`.
2. **schar-limud — העותק המוטבע של הסכימה הוסר** (פרטי, ר' שם).
3. **gius — זריעת המשתמש הוסרה מ-`0001_init.sql`** (פרטי, ר' שם).
4. **schar-limud — 109 הטענות של סבב 24 נשמרו כקובץ בריפו** (פרטי, ר' שם).

### תיאום גרסאות
`sw.js`: hanhala **v35** · yoman **v21** (`app-version` **15-2026-08-12-1**) ·
schar **v26** · gius **gius-v11**. **רק schar קודמה** בהשלמה הזו — היא
היחידה שקוד האפליקציה שלה השתנה. ב-gius השינוי הוא בקובץ מיגרציה,
בפיקסטורת בדיקה ובתיעוד; `index.html` ו-`sw.js` שם לא נגעו.
<!-- SHARED:end -->

### מה נעשה כאן (yoman-avoda)
- **תיעוד בלבד.** בלוק `iron-rule-8-docs` עודכן (שני כללים חדשים), ובלוק
  `round-24b-what` נוסף — ושתי החתימות עודכנו בארבעת עותקי
  `tools/check-docs.mjs`, באותו סבב.
- שורת «עודכן לאחרונה» קודמה מ-«סבב 21» ל-**סבב 24 · 2026-08-12**.
- **כלל ברזל 8 סעיף 7 — אינו רלוונטי כאן, עם נימוק ולא כהצהרה:** ⛔ ליומן
  אין טבלת משתמשים, אין מסך כניסה ואין שער סיסמה. הכניסה היא בחירת מוסד
  (ראשון לציון / רמת אביב), ואין בריפו סכימה שזורעת חשבון או קובץ שמחזיק
  סיסמה. אין מה להסיר ואין פער.
- ⚠️ **כלל ברזל 8 סעיף 6 — כאן יש פער מתועד.** כל רתמות הבדיקה של יומן
  (סבבים 3ב–17: 28 טענות מנוע המיזוג, 111 טענות סבבים 4/4ב ב-Chromium,
  18 טענות סבב 7, 50 טענות `_test_legacy.js` של סבב 13, ו-84 טענות
  `test_verify.js` של סבב 14) **רצו בסשן ולא נשמרו כקובץ** — יומן הוא
  הריפו היחיד מהארבעה שאין בו אף קובץ בדיקה משלו מלבד שני הבודקים
  המשותפים. הפרקים מתארים אותן ככאלה, וזה נשאר כפי שהוא: ⛔ הכלל החדש
  חל **קדימה**, ואין לשכתב פרקי היסטוריה.
  **הטריגר:** הסבב הבא שנוגע בקוד של יומן שומר את הרתמה שהוא כותב, ואם
  הוא נוגע במנוע המיזוג או במיגרציית השרידים — משחזר גם את הרתמה של אותו
  אזור. ⛔ אין לפתוח סבב שחזור-בדיקות מיוזמת הסשן; זו החלטה של המנהל.
- ⛔ `index.html` ו-`sw.js` **לא נגעו**. `CACHE_NAME` נשאר **v21**
  ו-`<meta name="app-version">` נשאר **15-2026-08-12-1** (סבב 17).

### אימותים
- **שלושת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html` ועל
  `sw.js`, `node tools/check-status-area.mjs`, ו-`node tools/check-docs.mjs`.
- **שני הבלוקים המעודכנים נמדדו כזהים בית-לבית בארבעת הקבצים** — חושבו
  בארבעתם והושוו זה לזה, לא הוכרזו.
- ⛔ **`git diff` על `index.html` ו-`sw.js` — ריק.**

---

<!-- SHARED:start id="iron-rule-10-users" -->
## ⭐ כלל ברזל 10 — מודל המשתמשים וההרשאות בארגון (סבב 26, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את תשעת כללי הברזל שלמעלה.

> **אפליקציה בארגון שיש בה משתמשים נבנית לפי שמונת הסעיפים שלמטה, מהיום
> הראשון.** הדפוס הזה לא תוכנן מראש — הוא התגבש בסבבים 13–26, סעיף אחרי
> סעיף, כל אחד מהם אחרי כשל שנמדד בפועל. הניסוח כאן נועד לדבר אחד:
> שהאפליקציה **הבאה** לא תעבור את אותה סדרה מחדש.

### ⚠️ למה זה כלל ולא פרק תיעוד
מי שבונה כאן אפליקציה חדשה **מעתיק ריפו קיים** — הוא לוקח את המודול
המשותף, את `LS_CFG`, את אזור המצב ואת מבנה `index.html`, ומתחיל לעבוד.
הוא **אינו** קורא שלושים פרקי סבבים היסטוריים כדי לגלות למה `role` הוא
בלי `DEFAULT` או למה הסיסמה אינה יורדת לדיסק. **דפוס שחי רק בפרקי
היסטוריה אינו עובר בהעתקה** — הוא נשאר מאחור, והאפליקציה החדשה נולדת עם
בדיוק הכשלים שכבר שילמנו עליהם. כלל ברזל כן עובר, מפני שהוא נקרא לפני
שכותבים ולא אחרי שנשבר.

### שמונת הסעיפים
לצד כל סעיף רשום **הסבב שבו נלמד** — הרקע המלא נמצא שם, ואין צורך לחפש.

1. **טבלת משתמשים: `role TEXT NOT NULL`, ⛔ בלי `DEFAULT`.**
   *(נלמד בסבב 26 והושלם בהשלמה שלו.)* יצירת משתמש בלי תפקיד **נכשלת
   במסד**, וזו ההתנהגות הרצויה: **תפקיד הוא החלטה, ולא ערך שנופל
   מאליו.** ברירת מחדל ש**מעניקה** הרשאה היא הכשל החמור מבין השניים,
   אבל גם ברירת מחדל שנכשלת סגור סוטה מהדפוס.
   ⚠️ **הערכים עצמם אינם משותפים ואין ליישר אותם** — כל אפליקציה ומודל
   ההרשאות שלה, בטבלה נפרדת ולעיתים בפרויקט Supabase נפרד.

2. **⛔ אין שער סיסמה נפרד מעל מסך ההגדרות.** *(נלמד בסבב 26.)* ההרשאה
   נגזרת **מהמשתמש המחובר בלבד**. סיסמה שנייה לאותו אדם אינה שכבת
   אבטחה נוספת — היא מנגנון שני עם מסך משלו, מסלול שינוי משלו ותחזוקה
   משלו, ובהתקנה טרייה היא נוטה ליפול חזרה לערך ברירת מחדל ולהיפתח לכל
   מי שמנחש אותו. ⚠️ הכלל חל גם על אפליקציה שאין בה משתמשים כלל: שם שער
   כזה אינו «יישור לאחיות» אלא **שכבת הרשאה שלמה יש מאין**.

3. **⛔ אין לזרוע משתמש, סיסמה או תפקיד בשום קובץ שנדחף לגיט.**
   *(נלמד בסבב 21 בהנהלה, ובסבב 24 ובהשלמתו ב-schar-limud וב-gius.)*
   לא בסכימה, לא במיגרציה, לא בקוד, לא ב-README ולא בקובץ בדיקה. הריפו
   **ציבורי**, ומפתחות ה-anon יושבים ב-`index.html` — כלומר סוד שנדחף
   הוא סוד שכל אחד יכול להשתמש בו מול המסד החי.
   ⚠️ **ומחיקה מאוחרת אינה מסירה אותו:** הערך נשאר קריא בהיסטוריית ה-git
   לנצח. הטיפול היחיד בסוד שנדחף הוא **להחליף אותו במסד**, ורק אחר כך
   לנקות את הקובץ. המשתמש הראשון נוצר **ידנית**, וקובץ ההתקנה מסביר
   איך — עם מצייני מקום ולא עם ערכים.

4. **הסיסמה גלויה בענן, ⛔ אך לעולם אינה יורדת למכשיר.**
   *(נלמד בסבב 13 ב-schar-limud, והורחב בסבבים 22–24.)* הטקסט הגלוי
   בענן הוא **החלטה תפעולית מתועדת** — המנהל צריך לראות ולנהל סיסמאות —
   ולא פער שממתין לסגירה; ⛔ שינוי שלה הוא החלטת המנהל, לא של הסשן
   (כלל ברזל 9). מה שיורד למכשיר הוא **`pass_salt` + `pass_fp` בלבד**:
   PBKDF2-SHA256, **100,000 סיבובים**, **מלח אקראי פר-משתמש**, ועם
   **קידומת הקשר פר-אפליקציה** בתוך המלח — ה-origin משותף לארבע
   האפליקציות, ובלי הפרדת הקשר טבלה מחושבת מראש עבור אחת מהן תקפה
   לכולן.
   ⛔ **כישלון גזירה מאפס את שני השדות ולא מדלג על הכתיבה** — טביעה
   ישנה ששורדת שינוי סיסמה ממשיכה לפתוח את הכניסה האופליין עם הסיסמה
   **הקודמת**, לנצח.

5. **כל נתיבי הכתיבה למראת המשתמשים עוברים דרך פונקציית סינון אחת.**
   *(נלמד בסבב 23 ב-gius, וחזר בסבב 26 שם.)* **רשימת היתר** ולא רשימת
   איסור — רשימת היתר נשארת נכונה גם כשתיווסף לטבלה עמודה רגישה חדשה.
   ⛔ **ואין נתיב שני שעוקף אותה.** זו הנקודה שנשברה בפועל יותר מפעם
   אחת: מסנן שיושב במשיכה בלבד, וכתיבה מקומית שכותבת את הסוד לדיסק
   מסביבו. כל נתיב חדש — משיכה, כתיבה מקומית, עדכון חלקי, מיגרציה,
   ייבוא — חייב לעבור דרך אותה פונקציה, ורצוי שגם שער הדיסק יסנן שוב
   כשכבה שנייה.

6. **כניסה אופליין מרובת-משתמשים היא דרישה, לא תוספת.**
   *(נלמד בסבב 22 בהנהלה, בסבב 23 ב-gius ובסבב 24 ב-schar-limud.)*
   **כל משתמש נכנס בכל מכשיר, גם בלי רשת** — מול הטביעה שבסעיף 4, ולא
   מול הענן. מטמון שמחזיק רק את המשתמש האחרון שנכנס **שובר את הדרישה**:
   מי שקיבל מכשיר שמישהו אחר עבד בו נעול בחוץ.
   ⚠️ **וההודעות נבדלות זו מזו.** «אין לך במכשיר במה לאמת» אינו «סיסמה
   שגויה»: ⛔ הודעה מאוחדת היא כשל שקט שמוביל את המשתמש להקליד שוב ושוב
   סיסמה נכונה לחלוטין, במקום לומר לו את הדבר היחיד שיפתור — חיבור אחד.

7. **קובץ התקנה שמוכרז «אידמפוטנטי» חייב שורת התכנסות לכל שינוי מבני —
   ולא רק `create table if not exists`.** *(נלמד בסבב 27.)*
   `create table if not exists` **מדלג על טבלה קיימת, ועל כל מה
   שבתוכה**: עמודה שנוספה אחרי ההתקנה הראשונה, אינדקס, טריגר, פוליסה,
   אילוץ, או `drop default`. כלומר התקנה קיימת שמריצה את הקובץ מחדש
   מקבלת «רץ בהצלחה» — **ונשארת בלי השינוי**, בשקט.
   לכן לכל שינוי מבני שנעשה מאז ההתקנה הראשונה יושבת בקובץ **גם** שורת
   התכנסות: `add column if not exists` · `create index if not exists` ·
   `create or replace` לטריגר · `drop policy if exists` + `create policy` ·
   `alter column … drop default` · `revoke`+`grant` מפורשים.
   ⛔ **ואין לגעת בנתונים — מבנה בלבד.** ה-`UPDATE` היחיד שמותר הוא
   מילוי עמודה שזה עתה נוספה, ורק ב-`WHERE <col> IS NULL`.
   ⚠️ **הסעיף יושב דווקא כאן** מפני שקובץ ההתקנה הוא המקום שבו נולדים
   טבלת המשתמשים, ה-`role` והטביעה — כלומר בדיוק שלושת הסעיפים
   הראשונים שלמעלה. התקנה שהריצה את הקובץ מחדש ולא קיבלה את
   `pass_salt`/`pass_fp` או את `role` היא התקנה **בלי כניסה אופליין
   ובלי הרשאות**, בלי שאיש יידע.

8. **⛔ האיסור על זריעת פרטי כניסה חל על כל סוגי הקבצים — לא רק על
   מיגרציות.** *(נלמד בסבב 27.)* SQL · HTML · Markdown · README ·
   קובץ בדיקה · פיקסטורה · כל קובץ שנדחף לגיט. ⛔ **וגם ערך «לדוגמה»
   אסור** — מציין מקום בלבד, לעולם לא סיסמה אמיתית ולא סיסמה גנרית
   שמישהו עלול להקליד.
   ⚠️ **הרקע, וזה מה שהופך את הסעיף לנחוץ:** סעיף 3 כבר אסר את זה
   מסבב 21 — ובכל זאת סוד שרד בהנהלה עד סבב 27, ב-`setup-db.html`,
   מפני שהחיפוש שאישר את «אין בריפו פרטי כניסה» נעשה **בקבצי SQL
   ובקוד בלבד**. כלי עזר ב-HTML לא נסרק, והצהרת סבב 24 נכתבה על סמך
   סריקה חלקית. ⛔ **סריקה שמוגבלת לסוג קובץ אחד אינה ראיה** —
   היא ראיה לסוג הקובץ שנסרק, ותו לא.

9. **`anon` ו-`authenticated` מקבלים `SELECT`, `INSERT`, `UPDATE` — ותו
   לא.** *(נלמד בסבב 29.)* ⛔ **אין להעניק להם `DELETE` או `TRUNCATE`.**
   מחיקה בארגון היא **תמיד סימון** — `deleted=true` + חותמת, או tombstone
   בתוך הערך — ו**לעולם לא הסרת שורה** (כלל ברזל 6 סעיף 1). כלומר ההרשאה
   מיותרת בהגדרה, ובתמורה היא פותחת למי שמחזיק את המפתח הגלוי
   שב-`index.html` דרך למחוק שורות ואף לרוקן טבלה שלמה.
   ⚠️ **והכלל חל על שני התפקידים גם כשאין משתמשי Auth:** `authenticated`
   קיים בכל פרויקט Supabase ומחזיק את אותן הרשאות בירושה, ולכן פתיחת
   signup או Auth בעתיד הייתה פוערת את ההגנה **בשקט**. ⛔ `service_role`
   אינו בכלל הזה — הוא תפקיד השרת ואינו נגיש מהדפדפן.
   ⚠️ **וזה אינו מתקיים מאליו:** פרויקט Supabase סטנדרטי מגיע עם
   `alter default privileges … grant all on tables to anon, authenticated`,
   ולכן **כל טבלה נולדת עם `DELETE` ו-`TRUNCATE`**. ⛔ **`GRANT` הוא
   אדיטיבי ואינו מסיר דבר** — נדרש `REVOKE` מפורש, וכל מיגרציה שמוסיפה
   טבלה חייבת אחד משלה.
   ⛔ **וטבלאות יומן הן החריג, בכיוון ההפוך:** `sync_log` ו-`kv_backup`
   מקבלות `INSERT` ו-`SELECT` בלבד — **בלי `UPDATE` ובלי `DELETE`, לשני
   התפקידים כאחד** — כדי שלא ניתן יהיה לזייף רישום קיים ביומן ראיות ולא
   להעלים אותו. ⚠️ אין «ליישר» אותן לשאר הטבלאות בשם האחידות: הסט הצר
   שם **הוא** ההגנה, ו-`UPDATE` נחוץ בשאר הטבלאות רק מפני שהכתיבה שם
   היא upsert.
   ⛔ **ובדיקת הרשאות סורקת את שני התפקידים, תמיד** *(נלמד בהשלמת סבב
   29)* — **הרשאה שהוחלה על תפקיד אחד בלבד היא הגנה חלקית.** זה נמדד
   כאן בפועל: שתי טבלאות היומן החזיקו `INSERT, SELECT` ל-`anon` ואת
   הסט **המלא** ל-`authenticated`, ולכן ההגנה שתועדה כקיימת התקיימה מול
   מפתח אחד בלבד. ⚠️ סריקה שכיסתה תפקיד אחד היא ראיה לתפקיד שנסרק,
   ותו לא — בדיוק כמו סעיף 8 לגבי סוגי קבצים.
<!-- SHARED:end -->

---

<!-- SHARED:start id="round-26b-what" -->
## ⭐ סבב 26 — השלמה (אוגוסט 2026): ברירת מחדל שאינה מעניקה הרשאה, והערה שנרשמת בארבעתן

### מה נעשה
השלמה לסבב 26, לפני המיזוג. **התיעוד המשותף וההערה שבקוד השתנו בארבעת
הריפו; מעבר לכך נגע קוד ב-schar-limud וב-gius בלבד** — קבצי הסכימה
שלהן וקובץ הבדיקה של schar.

1. **`role` ב-`sl_users` — `NOT NULL` בלי `DEFAULT` כלל** (פרטי
   ל-schar-limud, ר' שם). הניסוח הראשון של סבב 26 היה `DEFAULT 'admin'`,
   כלומר ברירת מחדל ש**מעניקה** הרשאה — וזו הייתה החריגה היחידה בארגון:
   ב-`ys_users` אין `DEFAULT` כלל, וב-`g_users` יש `DEFAULT` אבל של
   התפקיד ה**נמוך** (`'manager'`), כלומר גם הוא נכשל סגור. גם
   `DEFAULT 'user'` היה עדיין סוטה מהדפוס: **תפקיד הוא החלטה, ולא ערך
   שנופל מאליו.**

   ⚠️ **המשמעות המעשית לשלוש האפליקציות שיש בהן משתמשים: יצירת משתמש
   מחייבת ציון `role` מפורש.** INSERT בלי העמודה הזו נכשל במסד, וזו
   ההתנהגות הרצויה. הערכים אינם משותפים:
   `'admin'` / `'user'` ב-schar-limud · `'admin'` / `'senior'` /
   `'junior'` ב-hanhala-ruchanit · `'owner'` / `'manager'` ב-gius.
   ⛔ **ואין ליישר ביניהם** — שלוש טבלאות נפרדות בשני פרויקטי Supabase
   נפרדים, כל אחת עם מודל ההרשאות שלה. ל-yoman-avoda אין משתמשים כלל.

2. **הערת ההגנה של אזור ההגדרות — נוספה בארבע האפליקציות.** הלקח של
   סבב 26 (שער סיסמה נפרד מעל מסך ההגדרות = שכבת הרשאה כפולה, ובהתקנה
   טרייה גם שער שנפתח למי שהקליד «admin») נרשם עד כה בריפו אחד בלבד —
   זה שבו הכשל נמדד. מעכשיו ההערה יושבת בראש אזור ההגדרות של כל אחת
   מארבע האפליקציות, **זהה בית-לבית** פרט לשורת «כאן:» שבסופה.
   היא קובעת שלושה דברים: ⛔ אין להוסיף שער סיסמה נפרד מעל מסך ההגדרות;
   ההרשאה נגזרת מהמשתמש המחובר בלבד; ⛔ ואין לזרוע ערך ברירת מחדל
   לסיסמה או לתפקיד בשום מקום.

3. **כלל ברזל 9 — סעיף חדש** (בלוק `iron-rule-9-security-spread`):
   לקח אבטחתי שנלמד באפליקציה אחת נרשם כהערת ⛔ בארבעתן, גם כשהמנגנון
   עצמו רלוונטי רק לאחת. הסעיף מבחין במפורש בין ה**החלטה** ששלוש
   התשובות מחייבות («הוחל» / «אינו רלוונטי, וזה הנימוק» / «פער עם
   טריגר») לבין ה**איסור** שיושב בקוד: הראשון עונה «האם ליישם כאן»,
   השני עונה «מה אסור לעשות כאן בעתיד».

4. **⭐ `g_users.role` — ה-DEFAULT הוסר, ושלוש הטבלאות מיושרות.** בהחלטת
   המנהל בוצע `ALTER TABLE public.g_users ALTER COLUMN role DROP DEFAULT`,
   אחרי שאומת שאין שורה עם `role` ריק ושהבעלים הקיים נשאר `owner`.
   מעכשיו `g_users` · `ys_users` · `sl_users` — שלושתן `TEXT NOT NULL`
   **בלי `DEFAULT`**, ובשלושתן **יצירת משתמש בלי `role` נכשלת במסד**.
   ⚠️ ⛔ הערכים עצמם עדיין אינם משותפים ואין ליישר אותם.
   ⚠️ ב-gius ה-DEFAULT הקודם (`'manager'`) **נכשל סגור** — הוא היה
   התפקיד הנמוך — ולכן זהו תיקון אחידות ולא סגירת פרצה.

5. **כלל ברזל 10 — מודל המשתמשים וההרשאות בארגון** (בלוק
   `iron-rule-10-users`): ששת הסעיפים שהתגבשו בסבבים 13–26, מנוסחים
   כדפוס מחייב לאפליקציה הבאה במקום כלקח שפזור בפרקי היסטוריה. הנימוק
   כתוב שם: מי שבונה אפליקציה חדשה **מעתיק ריפו קיים** ואינו קורא
   שלושים פרקי סבבים, ולכן דפוס שחי רק בהיסטוריה אינו עובר בהעתקה.

### ⚠️ `CACHE_NAME` לא קודם באף אחת מהארבע — וזו החלטה, לא השמטה
מה שנכנס ל-`index.html` כאן הוא **הערה בלבד**: אפס שינוי התנהגות, אפס
שינוי בקוד שרץ. קידום `CACHE_NAME` היה מציג לכל המשתמשים באנר «גרסה
חדשה זמינה» ומכריח טעינה מחדש — בשביל טקסט שאיש מהם לא יראה.
⚠️ **המחיר המודע:** עד הקידום הבא, המכשירים שכבר ביקרו ימשיכו להגיש
מהמטמון `index.html` בלי ההערה. זה מקובל בדיוק מפני שההערה מכוונת
לסשן הבא שקורא את הריפו, ולא למשתמש שמריץ את האפליקציה.
⛔ **הכלל עצמו לא נחלש:** כל שינוי שנוגע בקוד שרץ — ולו שורה אחת —
מחייב קידום, ר' «עדכוני Service Worker» וכללי הברזל למעלה.
<!-- SHARED:end -->

### מה נעשה כאן (yoman-avoda)
> **אין כאן פרק «סבב 25» ואין פרק «סבב 26» בסיסי — במכוון.** שני הסבבים
> האלה לא נגעו ביומן כלל (סבב 26 נגע ב-schar-limud וב-gius בלבד), והמספור
> קופץ מ-24 ל-«סבב 26 — השלמה». זו אינה השמטה.

- **הערת ההגנה של אזור ההגדרות נוספה** ב-`index.html`, מיד מעל
  `renderSettings()`. הליבה זהה בית-לבית לשלוש האחיות; שורת «כאן:» היא
  היחידה שנבדלת.
- **כלל ברזל 9, סעיף «הלקח נרשם בארבעתן» — הרישום הנדרש כאן:** ⛔ **המנגנון
  אינו רלוונטי ליומן, ויש לכך נימוק ולא הצהרה — וזו בדיוק הסיבה שההערה
  כן נוספה.** ליומן **אין טבלת משתמשים, אין מסך כניסה ואין תפקידים כלל**:
  הכניסה היא בחירת מוסד (ראשון לציון / רמת אביב), אין `role` להשוות מולו
  ואין סוד לשמור. כלומר אין כאן שער סיסמה להסיר, ואין ברירת מחדל של
  תפקיד לתקן. ⚠️ **ודווקא מכאן נובע הסיכון שההערה מכסה:** שער סיסמה שיתווסף
  כאן בעתיד לא יהיה «יישור לאחיות» אלא **שכבת הרשאה שלמה יש מאין**, על
  אפליקציה שכל מודל הכניסה שלה הוא בחירת מוסד. ⛔ אין פער פתוח ואין טריגר.
- **שורת «עודכן לאחרונה» קודמה** מ-«סבב 24» ל-**סבב 26 · 2026-08-13**.
- **כלל ברזל 10 — ⚠️ אינו חל כאן, ודווקא לכן הוא נכתב גם בקובץ הזה.**
  ליומן אין טבלת משתמשים, אין מסך כניסה ואין תפקידים, ולכן חמישה מששת
  הסעיפים אינם ישימים כאן היום. **סעיף 2 כן חל** — ⛔ אין שער סיסמה
  נפרד מעל ההגדרות, והוא רלוונטי כאן בדיוק כפי שהערת ההגנה שבקוד
  מסבירה. ⚠️ ואם אי-פעם יתווספו ליומן משתמשים, הכלל הוא **הדבר שיימצא
  בקובץ הזה** — במקום סדרת הסבבים 13–26 שהאחיות עברו כדי להגיע אליו.
- **שלוש חתימות SHARED עודכנו** בארבעת עותקי `tools/check-docs.mjs`:
  `iron-rule-9-security-spread` (סעיף חדש) · `iron-rule-10-users` (בלוק
  חדש — כלל ברזל 10) · `round-26b-what` (בלוק חדש, שהורחב בהשלמה).
- ⛔ **`sw.js` לא נגע.** `CACHE_NAME` נשאר **v21** ו-`<meta name="app-version">`
  נשאר **15-2026-08-12-1** (סבב 17) — ר' הנימוק בפרק המשותף שלמעלה.
  ⚠️ ביומן זה חשוב פי שניים: יש כאן **שני** מזהי גרסה, ומנגנון האוטו-אפדייט
  משווה מול ה-`app-version`. קידום שלו היה שולח באנר «גרסה חדשה זמינה»
  לכל מכשיר עם ה-APK, בשביל הערה.

### אימותים
- **שלושת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html` ועל
  `sw.js`, `node tools/check-status-area.mjs`, ו-`node tools/check-docs.mjs`.
- **ליבת הערת ההגנה נמדדה כזהה בית-לבית בארבע האפליקציות** — חושבה
  בארבעתן והושוותה זו לזו, לא הוכרזה: sha256[:16] = `0c399aa3a60d0cb2`
  (הקטע שמתחילת הבלוק ועד שורת «כאן:»).
- **שלושת בלוקי ה-SHARED נמדדו כזהים בית-לבית בארבעת הקבצים** — חושבו
  בארבעתם והושוו זה לזה, לא הוכרזו: `8ab6dcc38c449f51` (כלל ברזל 9) ·
  `91911ec339c9e6fb` (כלל ברזל 10) · `46da920b39793d46` (round-26b).
- ⛔ **`git diff` על `sw.js` — ריק.** ב-`index.html` הדיפרנס הוא **הערה
  בלבד** — אומת שאין בו שום שורת קוד.

---

<!-- SHARED:start id="capability-matrix" -->
## ⭐ מטריצת היכולות של ארבע האפליקציות (סבב 27, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius).

> **מה שנבדל בין האפליקציות — נבדל בכתב, בטבלה אחת.** סבבים 17–26 חזרו
> ומצאו שוב ושוב הבדלי יכולת שאיש לא החליט עליהם ואיש לא רשם אותם: אחת
> עובדת אופליין והשנייה לא, לאחת כניסה מרובת-משתמשים ולשנייה מטמון של
> משתמש אחד, לאחת קובץ התקנה ולשנייה שדרוגים בלי מקור אמת. בכל אחד
> מהמקרים ה**גילוי** עלה סבב שלם. התשובה לשאלה «מה ההבדל בין
> האפליקציות» צריכה להיות **כתובה**, לא משהו שמחפשים בקוד.

⚠️ **הטבלה נמדדה מהקוד ולא הוצהרה** — כל תא אומת מול `index.html`,
`sw.js`, `migrations/`, `signing/` ו-`android/` של האפליקציה שלו.
⛔ **ומאותה סיבה אין לעדכן אותה «לפי ההיגיון»:** סבב שמשנה יכולת מודד
מחדש את התא שהוא שינה ומעדכן אותו **בארבעת הקבצים באותו סבב** (כלל
ברזל 8 סעיף 3).

### שלושת הערכים — ומה ההבדל ביניהם
- **✅** — היכולת קיימת ופעילה.
- **❌** — היכולת **חסרה** באפליקציה שהיא כן רלוונטית לה. כל ❌ נושא
  **נימוק**, ואם הוא פער — גם **טריגר**: האירוע שמחייב לפתוח סבב.
- **לא רלוונטי** — אין לאפליקציה הזו את המושג שהיכולת מדברת עליו, ולכן
  אין מה לממש. ⛔ זה **אינו** «❌ מנומס»: ❌ אומר «חסר»; «לא רלוונטי»
  אומר «אין כאן שאלה כזו».

| # | יכולת | yoman-avoda | hanhala-ruchanit | schar-limud | gius |
|---|---|---|---|---|---|
| 1 | כניסה אופליין | לא רלוונטי | ✅ | ✅ | ✅ |
| 2 | עריכת נתונים אופליין | ✅ | ✅ | ✅ | ✅ |
| 3 | מודל משתמשים | אין | רב-משתמשים | רב-משתמשים | רב-משתמשים |
| 4 | נתיב עדכון חלקי למראת המשתמשים | לא רלוונטי | ✅ | ❌ | ✅ |
| 5 | סוד במכשיר | אין | טביעה | טביעה | טביעה |
| 6 | פינוי אוטומטי | ✅ | ✅ | ❌ | ✅ |
| 7 | אימות פינוי מול הענן | ✅ | ✅ | לא רלוונטי | ❌ |
| 8 | שיתוף קבצים | ✅ | ❌ | ❌ | ❌ |
| 9 | מעטפת APK (WebView) | ✅ | ✅ | ✅ | ✅ |
| 10 | מפתח חתימה קבוע בריפו | ✅ | ✅ | ✅ | ✅ |
| 11 | מקור אמת יחיד לסכימה | ✅ | ✅ | ✅ | ✅ |
| 12 | קובץ התקנה מלא | ✅ | ✅ | ✅ | ✅ |
| 13 | גיבוי יומי אוטומטי | ✅ | ✅ | ✅ | ✅ |
| 14 | יומן פעולות | ✅ | ✅ | ✅ | ✅ |
| 15 | נתונים בטבלאות מובנות | ✅ | ❌ | ✅ | ✅ |

### הנימוקים — שורה אחר שורה
**1 · כניסה אופליין.** ל-yoman **אין טבלת משתמשים, אין מסך כניסה ואין
תפקידים** — הכניסה היא בחירת מוסד, ולכן אין מה לאמת. בשלוש האחרות
האימות הוא מול `pass_salt`+`pass_fp` שבמכשיר (סעיף 4 של כלל ברזל 10):
hanhala בסבב 22, gius בסבב 23, schar-limud בסבב 24.

**2 · עריכת נתונים אופליין.** בארבעתן, לפי כלל ברזל 6: כתיבה
מקומית-תחילה, מיזוג ברמת רשומה, tombstones, וסימון ⏳ עד שיש ראיה
מהענן. schar-limud הייתה האחרונה שנסגרה (סבב 12 שלב 2), ו-gius לפניה
(סבב 11).

**3 · מודל משתמשים.** ל-yoman אין. בשלוש האחרות `role TEXT NOT NULL`
בלי `DEFAULT`, ו⛔ **הערכים אינם משותפים ואין ליישר אותם**:
`'admin'`/`'senior'`/`'junior'` בהנהלה · `'admin'`/`'user'` ב-schar ·
`'owner'`/`'manager'` ב-gius.

**4 · נתיב עדכון חלקי למראת המשתמשים.** כתיבת שורת משתמש **בודדת**
למראה, בלי משיכה מלאה — כך שהחלפת משתמש במכשיר אינה תלויה בסנכרון הבא.
hanhala (`ysUsersCacheSave`) ו-gius (`usersCacheSave`, סבב 26) — ✅.
❌ **ב-schar-limud, וזה אינו פער:** שם `doLogin` מריצה **משיכה מלאה
כפויה** (`slPullUsers(true)`) מיד אחרי כניסה מוצלחת, וזה ממלא בדיוק את
אותו תפקיד. ⛔ ואם ייבנה שם נתיב חלקי בעתיד — הוא חייב לעבור דרך
`slUserPub`, כמו שלוש נקודות האכיפה הקיימות (כלל ברזל 10 סעיף 5).
ל-yoman אין מראת משתמשים כלל.

**5 · סוד במכשיר.** בשלוש שיש בהן משתמשים יורדת **טביעה בלבד** —
PBKDF2-SHA256, 100,000 סיבובים, מלח אקראי פר-משתמש עם קידומת הקשר.
⛔ **הסיסמה עצמה אינה יורדת לדיסק באף אחת מהן.** ב-yoman אין סוד
כלשהו במכשיר, מפני שאין משתמשים.

**6 · פינוי אוטומטי.** yoman (`tb_archive`/`tb_entries`, רצפות 60/90
יום) · hanhala (ארבעת מפתחות ה-`kv`, 180 יום) · gius (`g_mirror_txns`/
`g_mirror_pledges` 400 יום, `g_mirror_tasks` 180).
❌ **ב-schar-limud, וזו החלטה מתועדת ולא פער:** `tier1` ו-`tier2` ריקים
בכוונה — `sl_mirror_transactions` הם **כספים**, ו-`sl_mirror_students`
הם אבות המפתח הזר שלהם. ⛔ אין להוסיף שם פינוי; בקשה «לחסוך מקום»
היא בקשה לפתוח את סבב «מראה-חלון» המתועד שם, לא לאלתר טלאי.

**7 · אימות פינוי מול הענן** (סבב 14 — `idOf`+`verify` על `tier2`).
yoman ו-hanhala — ✅. ל-schar אין מה לאמת, מפני שאין שם פינוי כלל
(שורה 6) — לכן «לא רלוונטי» ולא ❌.
❌ **ב-gius, וזה אינו פער:** העֵד שלה `g_synced_at` נכתב אחרי **משיכה
מלאה מוצלחת של כל שבע הטבלאות**, כלומר הוא כבר עדות מהענן ונכתב גם
בדפדפן שרק קורא — ולכן הבאג שסבב 14 בא לפתור מעולם לא היה קיים שם.

**8 · שיתוף קבצים.** ל-yoman יש גשר מקורי לשיתוף דוח כתמונה, **מוגבל
ל-origin שלנו** ב-`addWebMessageListener` עם נפילה-חזרה נעולה-כפליים
(סבב 13). בשלוש האחרות **אין `navigator.share` בקוד כלל** (אומת
ב-grep), ולכן אין גשר — וזו ההחלטה הנכונה: ⛔ לא נותנים גשר מקורי לדף
שנטען מהרשת בלי צורך. אם יידרש שם שיתוף — **מעתיקים את הדפוס של
yoman**, לעולם לא `addJavascriptInterface` חשוף.

**9 · מעטפת APK.** בארבעתן WebView מקורי שטוען מהרשת. ⛔ **לעולם לא
TWA ולא PWABuilder** — TWA מריץ את האתר בתוך כרום, וסינון התוכן
במכשירי המשתמשים חוסם את כרום. זה נמדד בפועל: ה-TWA של gius פשוט לא
נפתח.

**10 · מפתח חתימה קבוע בריפו.** לכל אחת keystore משלה, וטביעת ה-SHA
שלה כתובה בפרק «חתימת APK» ה**פרטי** שלה. ⛔ הפרק הזה פרטי בהגדרה
(כלל ברזל 8 סעיף 5) — איחוד שלו יהיה שגוי ומסוכן.

**11 · מקור אמת יחיד לסכימה** — קובץ אחד בריפו שהוא ההגדרה המלאה של
המסד. schar-limud: `migrations/000_initial_schema.sql`. gius:
`migrations/0001_init.sql` (+0002/0003 קדימה). yoman-avoda:
`migrations/000_initial_schema.sql`, שנוצר בסבב 28.
✅ **ב-hanhala — הפער נסגר בסבב 32.** קובץ ההתקנה נוצר שם בסבב 28, אבל
`index.html` המשיך להחזיק **עותק חלקי** של הסכימה בשני מקומות
(`showSetupScreen` להצגה, `setupWithServiceKey` להרצה) — `ys_users`
בלבד, בלי `kv`, `sync_log` ו-`kv_backup`. שניהם **מושכים** מעכשיו את
`migrations/000_initial_schema.sql`, בדיוק כפי ש-`setup-db.html` כבר
עשה. ⛔ **ואין עותק-גיבוי מוטבע:** הקובץ יושב על אותו origin שממנו נטען
הדף, ואם הוא אינו נגיש האפליקציה עצמה לא נטענה — כלומר העותק לא היה מגן
על תרחיש אמיתי, ומחירו (מקור אמת שני שמתיישן) ודאי. בכשל משיכה מוצגת
הודעה עם קישור ישיר לקובץ, ו⛔ **לא מורצת סכימה כלשהי**.
⚠️ **וזו הייתה בדיוק תבנית סבב 24 ב-schar-limud** (`SETUP_SQL_FALLBACK`),
ריפו אחר וכמעט שנתיים של סבבים אחרי — ר' הסעיף החדש בכלל ברזל 12.

**12 · קובץ התקנה מלא** — קובץ שהרצתו על מסד ריק נותנת התקנה עובדת.
✅ **בארבעתן, מסבב 28.** שתי האפליקציות שחסרו קיבלו שם קובץ שמופה
**מהמסד החי** ולא מקריאת הקוד:
- **yoman-avoda:** `migrations/000_initial_schema.sql` — `kv_rishon`
  ו-`kv_ramataviv` עם RLS, פוליסות והרשאות, ופרק שמסביר שההפרדה בין
  המוסדות היא **טבלה בענן וסיומת מפתח במכשיר**.
  ⚠️ `supabase-multitenant.sql` **אינו** קובץ התקנה ומעולם לא היה כזה:
  הוא מסתיים ב-`INSERT … SELECT … FROM public.kv`, כלומר הגירה חד-פעמית
  מטבלה שכבר אינה קיימת. הוא סומן בכותרתו כהיסטורי. ⛔ אין להריץ אותו.
- **hanhala-ruchanit:** `migrations/000_initial_schema.sql` — `kv`,
  `ys_users` (כולל `pass_salt`/`pass_fp` מסבב 22 וה-`CHECK` על `role`),
  `sync_log` ו-`kv_backup`. `setup-db.html` **מושך אותו** ואינו מחזיק
  עותק.
- **⚠️ ומה שעדיין לא נעשה, במפורש:** שני הקבצים **לא הורצו מול המסד**
  ולא נגעו בנתונים. הם נמסרים לאימות חיצוני **טבלה-טבלה** לפני המיזוג,
  וכל מקום שבו לא הושגה ודאות רשום בגוף הקובץ. ⛔ «נכתב» אינו «אומת».

**13 · גיבוי יומי אוטומטי** (סבב 30, שלב א) — ✅ **בארבעתן**, מאותו מודול
משותף. עד הסבב הזה גיבו רק hanhala ו-schar, כל אחת במימוש משלה; הליבה
חולצה, והכיסוי הורחב. ⚠️ **וזה נמדד ולא הונח:** הגיבוי של hanhala
**הפסיק לרוץ ב-2026-07-20** ואיש לא ידע — בדיוק הסיבה ש«גובה לאחרונה»
נוסף ל«מידע טכני» בארבעתן. ⛔ אין להשוות «יש גיבוי» ל«הגיבוי רץ».

**14 · יומן פעולות** (סבב 30, שלב א) — ✅ **בארבעתן**, ל-`sync_log`.
עד הסבב הזה היה רק בהנהלה. ⚠️ **מה שנרשם נבדל לפי האפליקציה** ואינו
מיושר: הנהלה רושמת כניסות וסנכרון, gius כניסות וגיבוי, schar ו-yoman
גיבוי בלבד. ⛔ **המשותף הוא המנגנון — הטבלה, התור האופליין והתקרה —
ולא רשימת האירועים**, שהיא פר-אפליקציה מעצם טבעה.

**15 · נתונים בטבלאות מובנות** — שורה לכל רשומה, ולא ערך יחיד.
schar-limud (חמש טבלאות `sl_`) ו-gius (שבע טבלאות `g_`) — ✅ מאז ההקמה.
✅ **ב-yoman-avoda — הפער שנפתח בסבב 30 נסגר בסבב 32ב:** מיגרציות
`002`+`003` הורצו ב-2026-08-16 דרך `apply_migration` ונרשמו
ב-`schema_migrations` (`yoman_002_structured_tables` ·
`yoman_003_migrate_kv_to_rows`). ⚠️ שתיהן היו בפועל **no-op** — הכתיבה
הכפולה שרצה מאז סבב 30 כבר מילאה את הטבלאות — וזו בדיוק העדות שההרצה
נתנה. בדיקת השקילות רצה **מוסד-מול-מוסד**: ראשון לציון 455 רשומות
ו-156 סנאפשוטים, רמת אביב 265 ו-15, סה"כ **720/171 — התאמה מלאה
לערכי ה-`kv`**.
❌ **ב-hanhala-ruchanit, וזה פער מתועד ולא החלטה:** **כל** נתוני
האפליקציה יושבים כ-15 מפתחות ב-`kv`, כשהגדול בהם (`ys_attend_sessions`)
הוא ~286KB — כלומר כל סימון נוכחות דורס את מלוא הערך.
**⚠️ הטריגר:** אחרי ש-yoman תרוץ על הטבלאות שבועיים ותוכיח את הדפוס,
הוא מועתק לכאן בסבב ייעודי. ⛔ ולא לפני — מעבר שני לפני שהראשון אומת
מכפיל סיכון במקום ללמוד ממנו.
<!-- SHARED:end -->

---

<!-- SHARED:start id="round-27-what" -->
## ⭐ סבב 27 (אוגוסט 2026) — תיעוד ואיכות

### מה נעשה
**סבב תיעוד ואיכות. ⛔ אפס שינוי התנהגות בארבע האפליקציות.** מה שנכנס
ל-`index.html` הוא **הערות בלבד**, ומה שנכנס לקבצי הסכימה הוא **שורות
התכנסות מבניות** שאינן נוגעות בנתונים.

1. **תיעוד פנימי ב-gius** (פרטי, ר' שם) — כותרות בלוק לכל אזור לוגי,
   נימוקים להחלטות שנראות שרירותיות, והערות ⛔ במקומות שבהם «תיקון
   טבעי» היה מחזיר באג שכבר תוקן.
2. **מטריצת היכולות** — הפרק שמעל, בלוק `capability-matrix`. שתים-עשרה
   יכולות × ארבע אפליקציות, כל תא ✅ / ❌ / «לא רלוונטי», וכל ❌ עם
   נימוק — ואם הוא פער, גם עם טריגר.
3. **אידמפוטנטיות אמיתית של קבצי ההתקנה** — ר' סעיף 4, ובפרקים הפרטיים
   של schar-limud ושל gius.
4. **כלל ברזל 10 גדל לשמונה סעיפים** — **סעיף 7** קובע שקובץ התקנה
   שמוכרז אידמפוטנטי חייב **שורת התכנסות לכל שינוי מבני**, ולא רק
   `create table if not exists`; **סעיף 8** מרחיב את איסור זריעת פרטי
   הכניסה **לכל סוגי הקבצים** — SQL, HTML, Markdown וקבצי בדיקה — אחרי
   שסוד שרד בהנהלה שישה סבבים מפני שהסריקה כיסתה קבצי SQL בלבד.

### ⚠️ שלוש הנחות שנמדדו — ושתיים מהן לא אושרו
הבקשה לסבב נשענה על מדידה חיצונית. ⛔ **המדידה הוחזרה כאן מהקוד ולא
הועתקה**, ולכן נרשם גם מה שלא התאים — כלל ברזל 8 סעיף 6 בכיוון שלו:
מספר שמדווח בלי שנמדד מחדש נקרא כעדות.

1. **«יחס ההערות ב-gius ~5% מול 11–13% באחיות».** ⛔ **לא שוחזר.**
   נמדד כאן על **האזורים הפרטיים בלבד** (חמשת הבלוקים המשותפים
   והמודולים הקפואים הוחרגו — הם זהים בארבעתן ולכן מטים כל השוואה),
   על ה-JS המוטבע: **gius 11.6% · hanhala 11.0% · yoman 14.2% ·
   schar 20.9%**. כלומר gius אכן בקצה הנמוך — אבל **hanhala נמוכה
   ממנה**, והפער אינו פי-שניים ואינו פי-שלושה. ⚠️ **העבודה נעשתה
   במלואה בכל זאת**, מפני שהיא נכונה לגופה ולא בגלל המספר: התוספת היא
   נימוקים והערות ⛔ במקומות שנמדדו כמסוכנים, לא מילוי מכסה.
2. **«ב-schar 5 טבלאות מול 2 שורות התכנסות».** ⛔ **לא שוחזר.** נסרקו
   כל העמודות שנוספו ב-001–011 מול שורות ההתכנסות שב-`000_initial_schema.sql`:
   **לכל אחת מהן כבר יש `add column if not exists`**, וכך גם לכל אינדקס,
   טריגר, פוליסה ואילוץ. מה שנעשה שם הוא **עיגון הכלל בכותרת הקובץ**
   ותיקון תיאור מיושן שנשאר בו, ולא הוספת שורות חסרות.
3. **«ב-gius 7 טבלאות מול שורת התכנסות אחת».** ✅ **שוחזר, ותוקן.**
   שלושה פערים אמיתיים נמצאו ונסגרו שם — ר' הפרק הפרטי של gius.

### תיאום גרסאות
`sw.js`: hanhala **v35** · yoman **v21** (`app-version` **15-2026-08-12-1**) ·
schar **v26** · gius **gius-v13**. **רק gius קודמה** — היא היחידה
ש-`index.html` שלה השתנה בסבב הזה.
⛔ **ובשלוש האחרות הקידום היה שגוי:** קידום בלי שינוי קוד שולח לכל
המשתמשים באנר «גרסה חדשה זמינה» לשום שינוי.
<!-- SHARED:end -->
### מה נעשה כאן (yoman-avoda)
- **תיעוד בלבד.** ⛔ `index.html` ו-`sw.js` **לא נגעו**. `CACHE_NAME`
  נשאר **v21** ו-`<meta name="app-version">` נשאר **15-2026-08-12-1**
  (סבב 17) — אין שינוי קוד, וקידום כאן היה שולח באנר «גרסה חדשה זמינה»
  לכל מכשיר עם ה-APK בשביל שינוי בקובץ Markdown.
- **⚠️ שורות 11–12 במטריצה — הפער הפתוח היחיד של האפליקציה הזו, וזה
  מה שנמדד כאן ולא הועתק:** `supabase-multitenant.sql` **כן** יוצר את
  `kv_rishon` ואת `kv_ramataviv` על GRANT, RLS ופוליסות — כלומר הוא קרוב
  להיות קובץ התקנה — **אבל הוא נגמר ב-`INSERT INTO kv_rishon … SELECT …
  FROM public.kv`**. זהו קובץ **הגירה חד-פעמי** מטבלת ה-`kv` הישנה
  שקדמה לרב-דיירות, והיא אינה קיימת עוד; הרצה חוזרת שלו על מסד נקי
  תיכשל בשלב הזה. ⛔ ולכן הוא **אינו** מוכרז כקובץ התקנה, ואין לתאר
  אותו ככזה.
- ⛔ **לא נוצר כאן קובץ התקנה בסבב הזה, וזו הנחיה מפורשת ולא השמטה.**
  קובץ שנכתב מתוך קריאת הקוד בלבד — שתי טבלאות `kv`, המפתחות שבתוכן,
  ה-GRANT והפוליסות — הוא בדיוק סוג התיעוד שכלל ברזל 8 אוסר: הוא נקרא
  כעדות בזמן שאיש לא אימת אותו מול המסד החי. **הטריגר** רשום במטריצה:
  סבב ייעודי של מיפוי סכימה מלא ואימות מול המסד.
- **כלל ברזל 10 סעיף 7 (החדש) — הרישום הנדרש כאן:** ⛔ **אין כאן קובץ
  התקנה שמוכרז אידמפוטנטי, ולכן אין מה להתכנס.** וכשייכתב — הוא נכתב
  לפי הסעיף מהיום הראשון, ולא מתוקן אחריו.
- **שלוש חתימות SHARED עודכנו** בארבעת עותקי `tools/check-docs.mjs`:
  `iron-rule-10-users` (סעיף 7 החדש) · `capability-matrix` (בלוק חדש) ·
  `round-27-what` (בלוק חדש).

### אימותים
- **שלושת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html`
  ועל `sw.js`, `node tools/check-status-area.mjs`, ו-`node tools/check-docs.mjs`.
- **שלושת בלוקי ה-SHARED נמדדו כזהים בית-לבית בארבעת הקבצים** — חושבו
  בארבעתם והושוו זה לזה, לא הוכרזו.
- ⛔ **`git diff` על `index.html` ו-`sw.js` — ריק.**

---

<!-- SHARED:start id="iron-rule-11-comments" -->
## ⭐ כלל ברזל 11 — תקן ההערות בקוד (סבב 28, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את עשרת כללי הברזל שלמעלה.

> **הערה על אותו סוג דבר נראית אותו דבר בארבע האפליקציות.** כלל ברזל 7
> דרש עד היום זהות **תוכן** — הבלוקים המשותפים זהים בית-לבית. מעכשיו
> נדרשת גם זהות **צורה** באזורים הפרטיים: כותרת בלוק אחת, שלושה סימנים,
> תבנית איסור אחת ומילון מונחים אחד — כדי ששתי אפליקציות זו לצד זו
> ייקראו כשני עותקים של אותו ספר, ולא כשני פרויקטים של שני אנשים.

### הבעיה שנמדדה
סבב 27 הוסיף תיעוד פנימי ל-gius, ומדד אגב כך שהאחיות נבדלות זו מזו
**לא באחוזים אלא בכיסוי ובצורה**: אזורים לוגיים שלמים בלי שום כותרת,
הערות ⛔ שאומרות «אל תעשה» בלי לומר **מתי זה נלמד ולמה**, סימנים
שהומצאו לצורך שורה אחת, ואותו מושג בשלושה כתיבים.
⛔ **והנזק אינו אסתטי: הערת איסור בלי סבב ובלי סיבה היא הזמנה להסיר
אותה.** הסשן הבא קורא «אל תעשה X», אינו מוצא הצדקה, ומסיק שזו קפריזה —
ומחזיר בדיוק את הבאג שהאיסור בא למנוע.

### ששת הסעיפים
לצד כל סעיף רשום **הסבב שבו נלמד** — הרקע המלא נמצא שם.

1. **כותרת בלוק לכל אזור לוגי, בפורמט אחד ויחיד.**
   *(נלמד בסבב 27 ב-gius, והורחב לארבעתן בסבב 28.)* המסגרת היא **בדיוק
   74 תווי `═`**, שורת השם היא שלושה רווחים ואחריהם שם האזור (עד 78
   תווים), ואחריה מסגרת נוספת — גוף אופציונלי — ומסגרת סוגרת:

   ```
   /* ══…(74)══
      <שם האזור>
      ══…(74)══
      [גוף אופציונלי — נימוקים, ⛔, ⚠️]
      ══…(74)══ */
   ```

   ⚠️ **הרוחב הקבוע אינו קישוט:** הוא מה שהופך «אותו דבר» למדיד. מסגרת
   ברוחב אחר נראית לעין כמעט זהה, ולכן היא נסחפת בשקט — בדיוק הסחיפה
   שכלל ברזל 7 נבנה נגדה.

2. **שלושה סימנים בלבד — ⛔ איסור · ⚠️ אזהרה · ⭐ החלטה מרכזית.**
   *(נלמד בסבב 28.)* ⛔ **ואין להמציא סימנים נוספים.** סימן רביעי אינו
   מוסיף מידע — הוא מוריד את המשקל של השלושה: קורא שרואה חמישה סמלים
   שונים מפסיק להתייחס לאף אחד מהם כאל עצירה.
   ⚠️ הכלל חל על **פתיחת שורת הערה**. ✅/❌/⏳ בתוך משפט, או בתוך מחרוזת
   שמוצגת למשתמש, אינם סימון — הם טקסט.

3. **כל ⛔ נושא את הסבב שבו נלמד ואת הסיבה** — בתבנית
   **«⛔ אל תעשה X (סבב N) — הסיבה»**. *(נלמד בסבב 28.)* שני החלקים
   נדרשים **יחד**: הסבב הוא הדרך למצוא את הרקע המלא בלי לחפש, והסיבה
   היא מה שמונע מהסשן הבא לבטל את האיסור בתום לב.
   ⚠️ איסור שקדם למספור הסבבים נרשם כ«(נרשם בסבב N)» — האמת היא שהוא
   **תועד** אז, לא שנולד אז, ואין לכתוב מספר סבב שלא היה.

4. **טרמינולוגיה אחידה — אותו מונח עברי לאותו מושג בארבעתן.**
   *(נלמד בסבב 28.)* **מראה** (לא «מירור») · **טביעה** (לא «טביעת
   אצבע») · **אופק הפינוי** · **שער** · **פינוי** · **דחיפת-מצב**
   (עם מקף) · **מטמון** (לא «קאש») · **אופליין** · **tombstone**
   באנגלית. ⚠️ שני כתיבים לאותו מושג הופכים כל חיפוש טקסטואלי בין
   הריפו ל**חלקי** — וחיפוש חלקי הוא בדיוק איך שנשמט נתיב כתיבה שני
   (כלל ברזל 10 סעיף 5).

5. **כל החלטה ארכיטקטונית שנראית שרירותית מנומקת במקומה.**
   *(נלמד בסבב 27 ב-gius.)* קבוע שנראה שרירותי (רצפת גיל, תקרה,
   השהיה), טבלה שמוחרגת מרשימה, סדר שתי שורות שאסור להפוך — הנימוק
   יושב **ליד הקוד**, ולא רק ב-CLAUDE.md. ⚠️ מי שעורך את הקוד אינו
   קורא שלושים פרקי סבבים לפני שהוא נוגע בשורה.

6. **⛔ הבלוקים המשותפים והמודולים הקפואים אינם באזור התחולה.**
   *(נלמד בסבב 28.)* חמשת הבלוקים של סבבים 11–17 זהים בית-לבית
   ונשמרים ע"י `check-status-area.mjs`; עריכת הערה בתוכם מחייבת שינוי
   בארבעת הריפו **ובחתימות**. כלומר תקן שהיה חל עליהם היה **מייצר
   בעצמו** את הסחיפה שהוא בא למנוע. התקן חל על **הקוד הפרטי בלבד**.

### הבדיקה האוכפת
`tools/check-comments.mjs` (ב-gius: `scripts/`, ו-`check-js.mjs` קורא
לה) — **זהה בית-לבית בארבעת הריפו פרט לבלוק `APP` שבראשה**, ורצה עם
שערי התחביר לפני כל דחיפה. היא נכשלת על ארבעה סוגי סטייה:
- **א. כותרת בלוק** — כותרת שצורתה אינה תקנית (רוחב המסגרת, שורת השם),
  או **רצף של יותר מ-400 שורות קוד פרטי לא-ריקות בלי כותרת**.
  ⚠️ הכיסוי נמדד כמרווח בין כותרות ולא כ«כותרת לכל פונקציה»: אזור לוגי
  הוא רעיון, לא הצהרה. והמרווח נספר ב**שורות קוד פרטי לא-ריקות** ולא
  במספרי שורות — בין שני אזורים יושבים HTML ו-CSS, וספירה גולמית הייתה
  מדווחת «1700 שורות בלי כותרת» על אזור שאין בו קוד כלל.
- **ב. ⛔ בלי סבב או בלי סיבה** — בלוק הערה שיש בו ⛔ חייב לשאת
  `סבב <N>` **וגם** מפריד « — » שאחריו הנימוק.
- **ג. סימן שאינו מהרשימה** — שורת הערה שנפתחת בסמל שאינו ⛔/⚠️/⭐.
- **ד. טרמינולוגיה** — מונח מהרשימה בכתיב שאינו התקני.

⚠️ **הבדיקה קוראת הערות בלבד, דרך טוקניזציה של JS** (מחרוזות, תבניות
ו-regex). ⛔ **אין להחליף אותה בביטוי רגולרי על `//` ועל פתיחת בלוק:**
כל URL מכיל `//`, ולכן מחרוזת הייתה נקראת כהערה — `'✅ נשמר'` היה נספר
כסימן אסור, וביטוי רגולרי שמכיל כוכבית היה פותח בלוק שלא נסגר.

### ⛔ מה התקן אינו
**אינו יעד אחוזים.** ⛔ אין להשוות יחסי הערות בין האפליקציות, ואין
להמציא הערות כדי להגיע למספר. הדרישה היא **כיסוי**: כל אזור לוגי, כל
החלטה ארכיטקטונית וכל באג שתוקן — מכוסים, באותה צורה, בארבעתן.
⚠️ ואחוז גבוה אינו ראיה לכיסוי: קובץ יכול להיות מלא בהערות שחוזרות על
שם הפונקציה, ולא לענות על אף אחת מארבע השאלות שהתקן שואל.
<!-- SHARED:end -->
---

<!-- SHARED:start id="round-28-what" -->
## ⭐ סבב 28 (אוגוסט 2026) — תקן ההערות, וקבצי התקנה לשתיים שחסרו

### מה נעשה
**שני דברים, ושניהם נמדדו ולא הוצהרו:** תקן ההערות (כלל ברזל 11) הוחל
על ארבע האפליקציות עם בדיקה שאוכפת אותו, ושתי האפליקציות שהיו בלי קובץ
התקנה קיבלו אחד — **ממיפוי הסכימה החיה, ולא מקריאת הקוד**.

1. **כלל ברזל 11 — תקן ההערות בקוד** (הפרק שמעל).
2. **סריקת כיסוי ויישור לתקן בארבעת קבצי ה-`index.html`** — כותרת בלוק
   לכל אזור לוגי, ⛔ שנושא סבב וסיבה, שלושה סימנים בלבד וטרמינולוגיה
   אחידה. ⛔ **הערות בלבד, וזה נמדד ולא הוצהר:** הסרת כל שורות ההערה
   משתי הגרסאות — `HEAD` והעץ הנוכחי — נותנת קוד רץ **זהה בית-לבית**
   בכל אחת מהארבע, פרט לשורת **מזהה הגרסה** במקום שבו היא יושבת
   ב-`index.html`. ⚠️ היא נרשמת במפורש בפרק הפרטי של אותה אפליקציה:
   קידומה הוא חובה (ר' «עדכוני Service Worker»), והשמטתה הייתה משאירה
   מזהה שמדווח מספר שאינו הגרסה שרצה — הבאג שתוקן בסבב 21.
3. **`tools/check-comments.mjs`** — הבדיקה האוכפת, בדפוס
   `check-status-area.mjs` של סבב 17 ו-`check-docs.mjs` של סבב 18,
   ונוספה לשער הדחיפה בארבעת הריפו.
4. **קובץ התקנה מלא ל-yoman-avoda ול-hanhala-ruchanit** — הפער ששורה 12
   במטריצת היכולות תיארה, והטריגר שכתוב בה («סבב ייעודי: מיפוי סכימה
   מלא מול המסד החי»). ר' הסעיף הבא.

### ⚠️ קבצי ההתקנה — מה נעשה, ומה **לא** נעשה
- **מופו מהמסד החי** — טבלאות, עמודות וטיפוסים, `identity`, אינדקסים,
  אילוצים, טריגרים, פוליסות RLS והרשאות `anon`/`authenticated`/
  `service_role` — ב-`SELECT` בלבד.
- **אידמפוטנטיים לפי כלל ברזל 10 סעיף 7:** `create table if not exists`
  לבדו אינו מספיק, ולכן לכל שינוי מבני שנעשה מאז ההתקנה הראשונה יש
  **גם** שורת התכנסות (`add column if not exists` · `drop policy if
  exists` + `create policy` · `alter column … drop default` ·
  `revoke`+`grant` מפורשים).
- ⛔ **אפס פרטי כניסה, גם לא «לדוגמה»** — מצייני מקום בלבד (כלל ברזל 10
  סעיף 8). המשתמש הראשון נוצר ידנית, והקובץ מסביר איך.
- ⛔ **לא הורצה שום פקודה מול המסד ולא נגעו נתונים.** הקבצים נכתבו
  לקריאה ול**אימות חיצוני לפני המיזוג**, טבלה-טבלה מול הסכימה החיה.
- ⚠️ **וכל מקום שבו לא הושגה ודאות נרשם בקובץ עצמו במפורש**, ולא הושלם
  בניחוש. ⛔ קובץ התקנה שמשלים פערים מהיגיון הוא בדיוק סוג התיעוד שכלל
  ברזל 8 אוסר: הוא **נקרא כעדות** בזמן שאיש לא אימת אותו.

### ⚠️ מה שהתקן לא כיסה, במכוון
תקן ההערות **אינו חל על חמשת הבלוקים המשותפים ועל שני המודולים
הקפואים** (סעיף 6 של כלל ברזל 11), ולכן `git diff` עליהם ריק בארבעתן —
בדיוק כפי שנדרש מהם מאז סבב 14.

### תיאום גרסאות
`sw.js`: hanhala **v36** · yoman **v22** (`app-version` **16-2026-08-13-1**) ·
schar **v28** · gius **gius-v14**. **ארבעתן קודמו** — `index.html` של כל
אחת מהן השתנה.
⚠️ **ותוקן כאן מספר שנסחף:** שורת התיאום של סבב 27 אמרה «schar **v26**»
בזמן שהקובץ כבר היה **v27** מסבב 26. המספר נמדד עכשיו מ-`sw.js` ולא
הועתק מהשורה הקודמת — ⛔ מספר גרסה שמועתק ולא נמדד הוא בדיוק סוג
העדות שכלל ברזל 8 סעיף 6 אוסר.
⚠️ **וזו הבחנה מכוונת מול השלמת סבב 26**, שבה `CACHE_NAME` **לא** קודם
על הערה: שם הייתה **שורה אחת** בכל ריפו, וכאן אלה עשרות אזורים בכל
קובץ — הפרש שגורם ל-`index.html` שבמטמון להיות שונה מהותית ממה שבריפו,
וזה בדיוק מה שמקשה על אבחון בהמשך.
<!-- SHARED:end -->
### מה נעשה כאן (yoman-avoda, `sw.js` v22, `app-version` 16-2026-08-13-1)

**א. ⭐ `migrations/000_initial_schema.sql` — קובץ ההתקנה, חדש.**
שורות 11–12 במטריצה תיארו כאן פער אמיתי, וזה מה שנמדד: הריפו החזיק
**קובץ הגירה** ולא קובץ התקנה. הקובץ החדש מוגדר בכותרתו כמקור האמת
לסכימה, ומכיל את שתי הטבלאות כפי שנמדדו במסד החי — **`kv_rishon`**
ו-**`kv_ramataviv`**, שתיהן `(key text primary key, value text)` — עם
RLS, פוליסה והרשאות.
- **אידמפוטנטי לפי כלל ברזל 10 סעיף 7:** `create table if not exists`
  לבדו מדלג על טבלה קיימת **ועל כל מה שבתוכה**, ולכן לכל אובייקט יש
  שורת התכנסות משלו — `drop policy if exists` לפני `create policy`,
  ו-`grant` מפורש שרץ שוב בכל הרצה.
- ⚠️ **ההרשאות משקפות את מה שנמדד, ולא המלצה:** ל-`anon` יש בפועל גם
  `delete` וגם `truncate` — ירושה מברירות המחדל של פרויקט Supabase
  סטנדרטי, שאיש לא הסיר. הקובץ אומר זאת במפורש במקום «לתקן» בשקט.
- ⭐ **פרק שלם הוקדש להפרדת המוסדות, כי היא נבדלת בין שתי השכבות:**
  בענן ההפרדה היא **טבלה לכל מוסד** (`KV_TABLE`), ובמכשיר היא **סיומת
  למפתח** (`LS` — `tb_entries_rishon` מול `tb_entries_ramataviv`).
  ⛔ אין לערבב ביניהן, וזו בדיוק ההפרדה שסבב 13 שיחזר ידנית כשמצא
  מפתחות-שריד בלי סיומת. הקובץ כולל **רשימת ארבעת הצעדים** להוספת מוסד
  שלישי, כדי שמי שיעשה זאת לא יגלה את החצי השני מהקוד.
- ⛔ **אין כאן טבלת משתמשים ואין פרטי כניסה** — הכניסה היא בחירת מוסד,
  ולכן אין מה לזרוע ואין מה להשמיט (כלל ברזל 10 סעיף 8).
- ⚠️ **הצעה שנרשמה ולא בוצעה:** `revoke delete, truncate … from anon`
  יושבת בקובץ **כהערה מנוטרלת**. ⛔ הסרת הרשאה היא שינוי התנהגות במסד,
  והיא החלטת המנהל ולא של הסשן (כלל ברזל 9).

**ב. `supabase-multitenant.sql` סומן בכותרתו כהיסטורי.**
⛔ **הוא אינו קובץ התקנה ומעולם לא היה כזה:** הוא מסתיים ב-
`INSERT … SELECT … FROM public.kv`, כלומר הגירה חד-פעמית מטבלת ה-`kv`
שקדמה לרב-דיירות — טבלה **שכבר אינה קיימת**. הרצה שלו על מסד נקי תיכשל
בדיוק בשלב הזה. הוא נשאר בריפו כתיעוד הצעד שבוצע בזמנו, לא כהוראת
הפעלה.

**ג. תקן ההערות — 14 אזורים לוגיים, 7 הערות ⛔.** כותרת בלוק לכל אזור,
כל ⛔ עם מספר סבב ועם סיבה, ומונחים בכתיב אחד.
⚠️ **הערה נגררת נשארה נגררת:** `if (YESHIVA !== 'rishon') return report;`
נושאת את האיסור «⛔ ראשון לציון בלבד (סבב 13) — דליפה בין מוסדות»
**באותה שורה**, ולא בבלוק מעליה. ⛔ אין להעביר אותה למעלה: היא מתייחסת
לתנאי הזה ולא לפונקציה, וההצמדה היא מה שמונע מסשן עתידי «לפשט» את
השורה.

**ד. `tools/check-comments.mjs`** נוסף, וקריאה אליו נוספה לפרק «לפני כל
push» — **שער חובה רביעי**.

**ה. ⚠️ שני מזהי גרסה, ושניהם קודמו.** `CACHE_NAME` → **v22** ו-
`<meta name="app-version">` → **16-2026-08-13-1**. ⛔ אין לקדם אחד בלי
השני: ה-service worker מגיש את הקליפה לפי הראשון, ומנגנון האוטו-אפדייט
שב-APK משווה מול השני — קידום חלקי משאיר חצי מהמכשירים בלי העדכון,
בלי סימן.

### אימותים
- **ארבעת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html`
  ועל `sw.js`, `node tools/check-status-area.mjs`,
  `node tools/check-docs.mjs`, ו-`node tools/check-comments.mjs`.
- **⭐ «הערות בלבד» נמדד ולא הוצהר:** הסרת כל ההערות (כולל נגררות,
  בטוקניזציה מלאה של JS) מ-`HEAD:index.html` ומהעץ הנוכחי נותנת
  **3,139 שורות קוד רץ, זהות בית-לבית**.
  ⚠️ **ומה שאינו נספר במדידה נרשם כאן במפורש:** `<meta name="app-version">`
  שב-`<head>` קודם ל-`16-2026-08-13-1`. הוא HTML ולא JS ולכן אינו נכנס
  לחילוץ ה-`<script>` — ⛔ אבל הוא **כן** קוד רץ, וקידומו חובה (סעיף ה).
  מדידה שאינה אומרת מה היא אינה מודדת היא הצהרה.
- **הבודק אינו חותמת גומי** — חמש המוטציות מתוארות בפרק הפרטי של
  hanhala-ruchanit, שם הן הורצו; כאן הורצה בקרה חיובית בלבד (0 כשלים).
- **חמשת הבלוקים המשותפים ושני המודולים הקפואים לא נגעו** — `git diff`
  עליהם ריק.
- ⛔ **המסד לא נגע.** המיפוי נעשה ב-`SELECT` בלבד; לא הורצה מיגרציה ולא
  שונו נתונים. ⚠️ **קובץ ההתקנה טרם אומת חיצונית** — «נכתב» אינו
  «אומת», והאימות טבלה-טבלה הוא תנאי למיזוג.

---

<!-- SHARED:start id="round-29-what" -->
## ⭐ סבב 29 (אוגוסט 2026) — יישור הרשאות המפתח הציבורי

### מה נעשה
**סבב הרשאות מסד. ⛔ אפס שינוי בקוד הרץ בארבע האפליקציות** — לא
ב-`index.html`, לא ב-`sw.js`, ולכן **אף `CACHE_NAME` לא קודם**. מה שנוסף
הוא ארבע מיגרציות (schar-limud · hanhala-ruchanit ×2 · yoman-avoda),
שורות התכנסות בקבצי ההתקנה, וסעיף 9 בכלל ברזל 10.

### הבעיה שנמדדה
מיפוי `information_schema.role_table_grants` מול **שני** פרויקטי
ה-Supabase מצא ש-**gius לבדה מוגנת**: שבע טבלאות ה-`g_` שלה מעניקות
ל-`anon` ול-`authenticated` `SELECT, INSERT, UPDATE` בלבד (מיגרציה
`0002_revoke_delete`, סבב 11). **תשע טבלאות בפרויקט המשותף** —
`sl_lists` · `sl_settings` · `sl_students` · `sl_transactions` ·
`sl_users` · `ys_users` · `kv` · `kv_rishon` · `kv_ramataviv` — מעניקות
לשני התפקידים גם `DELETE`, `TRUNCATE`, `REFERENCES` ו-`TRIGGER`.
מפתח ה-anon יושב **גלוי** ב-`index.html` בארבעת הריפו הציבוריים, וה-RLS
הוא `using (true)` — כלומר מי שמחזיק בו יכול תיאורטית למחוק שורות ואף
לרוקן טבלה שלמה.

⚠️ **מאיפה זה הגיע — לא מקבצי הסכימה שלנו.** פרויקט Supabase סטנדרטי
מגיע עם `alter default privileges in schema public grant all on tables to
anon, authenticated, service_role`, ולכן **כל טבלה נולדה עם ההרשאות
האלה**, וה-`grant` שבקובץ ההתקנה רק תיאר את המצב שכבר היה.
⛔ **`GRANT` הוא אדיטיבי בלבד ואינו מסיר דבר** — רק `REVOKE` מסיר.

### ⛔ למה ההסרה בטוחה — נמדד, לא הונח
**אין באף אחת מארבע האפליקציות מסלול מחיקה מול המסד:** סריקה של ארבעת
הריפו מצאה **אפס** קריאות `.delete()` ל-PostgREST (ההתאמות היחידות הן
`caches.delete()` של ה-service worker ו-`Set.delete` בשתי רתמות בדיקה).
מחיקה בארגון היא **תמיד סימון** — `deleted=true` + חותמת ב-`sl_*`,
tombstone בתוך הערך ב-`kv`/`kv_rishon`/`kv_ramataviv` — ותמיד מתבצעת
ב-`UPDATE` (כלל ברזל 6 סעיף 1). ההרשאה מיותרת לחלוטין, וההסרה אינה
שוברת דבר.

### ⛔ שתי טבלאות היומן — החריג, והוא בכיוון ההפוך
`sync_log` ו-`kv_backup` מקבלות **`INSERT` ו-`SELECT` בלבד — בלי
`UPDATE` ובלי `DELETE`, לשני התפקידים**. הן יומני ראיות: מי שאינו יכול
לעדכן אינו יכול לזייף רישום קיים, ומי שאינו יכול למחוק אינו יכול
להעלים אותו. ⛔ **אין «ליישר» אותן לשאר הטבלאות בשם האחידות** — הסט
הצר שם **הוא** ההגנה, ואין להוסיף להן הרשאות.
⚠️ הצד של `anon` היה כבר נכון; הצד של `authenticated` **לא** — ר'
הסעיף הבא.

### ⭐ `authenticated` צומצם גם הוא — בהחלטת המנהל
הבקשה המקורית הייתה על המפתח הציבורי בלבד. ההרחבה נעשתה **בהחלטה
מפורשת**: לתפקיד יש **היום** בדיוק אותן הרשאות מלאות, מאותה ירושה, ולכן
פתיחת signup או Supabase Auth בעתיד הייתה פוערת את ההגנה **בשקט**. זהו
גם יישור למצב קיים ולא החלטה חדשה — `0002` של gius כבר צמצמה את שני
התפקידים.

⚠️ **והצמצום נעשה כשאין משתמשי Auth כלל** — `select count(*) from
auth.users` מחזיר **0 בשני הפרויקטים** — ולכן הוא **אינו נבדק מול מסלול
חי**. אם ייפתח Auth בעתיד, יש לוודא ש-`SELECT, INSERT, UPDATE` מספיקים
למסלול שייבנה. ⛔ `service_role` לא נגע: הוא תפקיד השרת ואינו נגיש
מהדפדפן.

### ⭐ טבלאות היומן — הפער נמדד, ונסגר בהשלמה
המדידה חשפה דבר שלא היה ידוע לפניה: `sync_log` ו-`kv_backup` מעניקות
ל-`anon` `INSERT, SELECT` בלבד — אבל ל-**`authenticated`** את **הסט
המלא**, כולל `UPDATE`, `DELETE` ו-`TRUNCATE`. כלומר ההגנה מפני זיוף
רישום ביומן ראיות התקיימה **מול תפקיד אחד בלבד**.

הניסוח הראשון של הסבב רשם את זה כפער עם טריגר («כשייפתח Auth»), מפני
שההנחיה «אל תיגע ב-`sync_log` וב-`kv_backup`» נקראה כחלה על שתיהן
במלואן. **ההנחיה חלה על `anon` ועל האיסור להוסיף להן `UPDATE` «בשם
האחידות» — לא על השארת `authenticated` פתוח.** ⛔ **הפער נסגר בהשלמה
ואין לו טריגר:** `hanhala/migrations/002_revoke_log_tables.sql` מצמצמת
את שתיהן ל-`INSERT` + `SELECT` בלבד, **לשני התפקידים**, וקובץ ההתקנה
עודכן כך שהתקנה טרייה נולדת נכונה.

⛔ **הלקח, והוא מה שנשאר מהפער הזה** — **הרשאה שהוחלה על תפקיד אחד
בלבד היא הגנה חלקית.** בדיקת הרשאות סורקת את **כל** התפקידים הנגישים
(`anon` **וגם** `authenticated`), ולא מסתפקת בזה שהתפקיד הבולט נראה
תקין. ⚠️ זה אותו כשל בדיוק שסעיף 8 בכלל ברזל 10 מתאר על **סוגי
קבצים** — סריקה חלקית שנקראת כראיה מלאה — והפעם בציר התפקידים.
הוא נכנס לסעיף 9.

### מה נכתב
1. **ארבע מיגרציות**, בדפוס `gius/migrations/0002_revoke_delete.sql`:
   לולאה על שמות הטבלאות עם `revoke` ואז `grant` מפורש, **בסדר הזה**.
   ⛔ אידמפוטנטיות ו**אינן נוגעות בנתונים** — הרשאות בלבד, בלי `insert`,
   `update` או `delete` על אף שורה.
   ⚠️ שלוש מהן על טבלאות הנתונים (`SELECT, INSERT, UPDATE`), והרביעית —
   `hanhala/002_revoke_log_tables.sql` — על **טבלאות היומן**, עם סט צר
   יותר: `INSERT` + `SELECT` בלבד. ⛔ אין לאחד ביניהן.
2. **`alter default privileges in schema public revoke delete, truncate on
   tables from anon, authenticated`** בכל אחת מהן, כדי שטבלה עתידית לא
   תיוולד שוב עם ההרשאות האלה. ⚠️ **אינו תחליף ל-`revoke` המפורש** — הוא
   משפיע רק על ברירות מחדל שבבעלות התפקיד שמריץ אותו, ורק על טבלאות
   שייווצרו **מכאן והלאה**. ⛔ ולכן הכלל נשאר: **כל מיגרציה שמוסיפה טבלה
   חייבת `revoke` משלה.**
3. **קבצי ההתקנה עודכנו** — כדי שהתקנה טרייה תיוולד נכונה, **וגם**
   כשורת התכנסות להתקנה קיימת (כלל ברזל 10 סעיף 7).
4. **כלל ברזל 10 גדל לתשעה סעיפים** — סעיף 9, הפרק שמעל.

### ✅ המיגרציות הורצו ואומתו — חיצונית
הסשן עצמו **לא נגע במסד**: כל המיפוי נעשה ב-`SELECT` בלבד, וארבע
המיגרציות נמסרו לאישור והרצה חיצונית. ⭐ **הן אושרו, הורצו ואומתו**,
בשני צדי ההרצה:
- **לפני:** אומת שאף אחת מהן אינה נוגעת בנתונים — `REVOKE`/`GRANT`
  בלבד, בלי `insert`/`update`/`delete` על אף שורה.
- **אחרי:** **תשע טבלאות הנתונים** מחזיקות `INSERT, SELECT, UPDATE`,
  ו**שתי טבלאות היומן** `INSERT, SELECT` — ⛔ **אפס `DELETE` ואפס
  `TRUNCATE`**. gius אומתה כמוגנת מראש: שבע טבלאות, אותו סט.

⚠️ **והאימות נעשה על שני התפקידים, לא על `anon` לבדו** — זה בדיוק מה
שסעיף 9 מחייב, ובדיוק מה שהיעדרו הסתיר את הפער בטבלאות היומן עד
ההשלמה. ⛔ `service_role` לא נגע.
⚠️ **מה שעדיין לא נבדק:** `auth.users` ריקה בשני הפרויקטים, ולכן
הצמצום של `authenticated` **אינו נבדק מול מסלול חי**. אם ייפתח Auth —
יש לוודא שהסט שנשאר מספיק למסלול שייבנה.

### תיאום גרסאות
`sw.js`: hanhala **v36** · yoman **v22** (`app-version` **16-2026-08-13-1**) ·
schar **v28** · gius **gius-v14**. **אף אחת לא קודמה** — אין שינוי בקוד
הרץ, וקידום היה שולח לכל המשתמשים באנר «גרסה חדשה זמינה» לשום שינוי.
⚠️ המספרים נמדדו מ-`sw.js` של כל ריפו ולא הועתקו משורת התיאום של סבב 28.
<!-- SHARED:end -->
### מה נעשה כאן (yoman-avoda)

**א. ⭐ `migrations/001_revoke_delete_anon.sql` — חדש, והמיגרציה
הראשונה כאן מאז קובץ ההתקנה** (`000_initial_schema.sql`, סבב 28). שתי
טבלאות: `kv_rishon` ו-`kv_ramataviv`.
- ⚠️ **כל מפתחות היומן יושבים כשורות בשתי הטבלאות האלה** —
  `tb_entries`, `tb_archive`, `tb_cats`, `tb_subs` וכל השאר — ולכן
  `truncate` על אחת מהן היה מרוקן **מוסד שלם** בפקודה אחת.
- ⚠️ **מוסד שלישי בעתיד:** טבלת `kv_<מוסד>` חדשה **תיוולד שוב** עם
  ההרשאות המלאות, ולכן היא חייבת `revoke` משלה. הדרישה כתובה גם בקובץ
  ההתקנה, ליד רשימת ארבעת הצעדים.
- ⛔ `sync_log` ו-`kv_backup` (של האחיות, באותו פרויקט) **אינן כאן**. הן
  שייכות להנהלה רוחנית, ומטופלות שם ב-`002_revoke_log_tables.sql`
  (השלמת סבב 29) — `INSERT`+`SELECT` בלבד, לשני התפקידים.

**ב. `000_initial_schema.sql`** — שני בלוקי ההרשאות נכתבו מחדש בדפוס
`revoke` → `grant`, ו-`service_role` שומר את הסט המלא. הפרק שבסוף
הקובץ שאמר «⚠️ הצעה שלא בוצעה» עודכן ל«✅ בוצעה בסבב 29».

**ג. ⛔ `index.html` ו-`sw.js` לא נגעו.** `CACHE_NAME` נשאר **v22**
ו-`<meta name="app-version">` נשאר **16-2026-08-13-1** (סבב 28).
⚠️ כאן זה חשוב פי שניים: יש שני מזהי גרסה, ומנגנון האוטו-אפדייט משווה
מול ה-`app-version` — קידום היה שולח באנר «גרסה חדשה זמינה» לכל מכשיר
עם ה-APK, בשביל שינוי בהרשאות מסד שאינו נוגע לקוד כלל.

**ד. כלל ברזל 9 — הרישום הנדרש כאן:** ⚠️ **הצמצום רלוונטי גם לאפליקציה
שאין בה משתמשים.** ליומן אין טבלת משתמשים, אין מסך כניסה ואין תפקידים —
ובכל זאת המפתח הציבורי שלה מחזיק היום `DELETE` ו-`TRUNCATE` על שתי
טבלאות הנתונים. ⛔ אין קשר בין «אין משתמשים» לבין «אין הרשאות עודפות»,
וזו בדיוק ההנחה שהייתה משאירה את יומן בחוץ.

### אימותים
- **ארבעת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html`
  ועל `sw.js`, `node tools/check-status-area.mjs`,
  `node tools/check-docs.mjs`, ו-`node tools/check-comments.mjs`.
- **⭐ ההרשאות נמדדו מול המסד החי ולא הוצהרו** — `kv_rishon` ו-
  `kv_ramataviv` מחזיקות היום `DELETE, INSERT, REFERENCES, SELECT,
  TRIGGER, TRUNCATE, UPDATE` ל-`anon` ול-`authenticated`.
- **⭐ «אין מסלול מחיקה» נמדד ולא הונח** — סריקת `.delete(` בכל הריפו:
  ההתאמה היחידה היא `caches.delete()` שב-`sw.js`. כל כתיבה עוברת
  ב-`sbSet` שהוא `upsert`, ומחיקת רשומה היא tombstone בתוך הערך.
- ⛔ **`git diff` על `index.html` ו-`sw.js` — ריק.**
- ⛔ **הסשן לא נגע במסד** — המיפוי ב-`SELECT` בלבד.
- ⭐ **001 הורצה ואומתה חיצונית.** אחרי ההרצה `kv_rishon` ו-
  `kv_ramataviv` מחזיקות `INSERT, SELECT, UPDATE` בשני התפקידים —
  ⛔ אפס `DELETE` ואפס `TRUNCATE`.

---

<!-- SHARED:start id="round-30-what" -->
## ⭐ סבב 30 (אוגוסט 2026) — גיבוי ויומן בארבעתן, ויומן-עבודה לטבלאות

### ⛔⛔ סדר מחייב — שני שלבים, ואין לערבב ביניהם
**שלב א נכנס לתוקף ומוכח בפועל לפני ששלב ב רץ.** שלב ב מזיז 1.2MB של
נתוני יומן לטבלאות, ובלי גיבוי פעיל **שאומת** אין נקודת חזרה.
⛔ **הסשן שכתב את שני השלבים לא הריץ אף מיגרציה ולא נגע במסד** — כל
המדידות נעשו ב-`SELECT` בלבד. ההרצה חיצונית, **בשני גלים**:
1. שלב א ← אימות ש-`kv_backup` קיבלה שורה טרייה ← ורק אז
2. שלב ב (`002` ואז `003`), עם בדיקת השקילות שבגוף `003`.

### שלב א — גיבוי יומי ויומן פעולות בארבע האפליקציות

**הבעיה שנמדדה.** גיבו רק hanhala-ruchanit ו-schar-limud, כל אחת
במימוש משלה, ויומן פעולות היה רק בהנהלה. ⚠️ **ומעבר לכיסוי החלקי —
הגיבוי של הנהלה הפסיק לרוץ ב-2026-07-20** (החותמת האחרונה של כל ארבעת
המפתחות שלה) בעוד ש-schar גיבתה באותו יום עצמו. שלושה שבועות בלי גיבוי,
בלי שאיש ידע: **מנגנון שקט שנעצר נראה בדיוק כמו מנגנון שקט שעובד.**

**מה נעשה.**
1. **⛔ הליבה חולצה ולא הומצאה מחדש.** שני המימושים הקיימים נקראו, זוהתה
   הליבה המשותפת — נעילת ריצה, דגל-יומי שנכתב **אחרי** ההצלחה, בדיקת
   `error` מפורשת — והיא הפכה ל**בלוק משותף זהה בית-לבית בארבעתן**,
   בין `/* ═══ גיבוי יומי ויומן פעולות` לבין `סוף מודול הגיבוי היומי`.
   מעליו יושבת `BK_CFG`, שהיא הדבר היחיד שנבדל.
   ה-API: `bkBoot` · `bkMaybeDaily` · `bkLastAt`/`bkStale` ·
   `bkStatusHTML`/`bkStatusMount` · `logAction`/`logFlush` · `bkToday`/`bkSig`.
2. **⭐ גיבוי דיפרנציאלי** — ערך שלא השתנה מאז הגיבוי האחרון אינו נכתב
   שוב, לפי חתימה מקומית (אורך + FNV-1a) שאינה נוגעת ברשת. ⚠️ זה אינו
   קיצור דרך אלא **תנאי לכך שהגיבוי יהיה בר-קיימא**: הערך הגדול בארגון
   הוא 1.2MB, וכתיבה יומית עיוורת שלו לבדו הייתה מוסיפה ~36MB בחודש.
   ⛔ ובספק — כותבים: חתימה חסרה או מכשיר חדש מביאים לכתיבה מלאה.
3. **הכיסוי הורחב בזכות זה:** הנהלה עברה מארבעה מפתחות ל-14, ו-yoman
   ו-gius הצטרפו. ⛔ **וטבלאות שיש בהן סוד מוחרגות או נשלפות בעמודות
   מפורשות** — `kv_backup` קריאה ל-`anon`, וגיבוי גורף של טבלת משתמשים
   היה מעתיק סיסמאות למקום שני.
4. **`kv_backup` ו-`sync_log` נוצרו בפרויקט של gius**
   (`migrations/0004_backup_log.sql`) — ⚠️ היא חיה בפרויקט **נפרד**
   ולכן אין לה גישה לאלה שבפרויקט המשותף. הסכימה זהה בית-לבית, וזה מה
   שמאפשר למודול להיות זהה. ⛔ `INSERT`+`SELECT` לשני התפקידים
   (כלל ברזל 10 סעיף 9).
5. **«גובה לאחרונה» ב«מידע טכני», בארבעתן.** ⛔ הרכיב **נתלה אחרי**
   הבלוק הקפוא ואינו עורך אותו — «מידע טכני» חתום, ועריכתו הייתה
   מחייבת שינוי בארבעת הריפו ובחתימות עבור שורת תצוגה אחת.
6. **שימור `kv_backup` — נמדד, והוצע ולא בוצע.** 136 שורות · 5,940 kB,
   מהן ~2.6MB **גיבויים ידניים חד-פעמיים** (`PRE_SYNC_UNIFY_`,
   `PRE_ROUND3B_`, `ORPHAN_`) ש⛔ **אין למחוק לעולם** — הם אינם מצטברים
   ואין להם עותק אחר. ההצעה (שמירת 30 אחרונים לכל מפתח **אוטומטי**)
   יושבת מוערת ב-`hanhala/migrations/003_backup_retention.sql`.
   ⛔ **לא נמחקה אף שורה**, ומחיקת נתוני גיבוי היא החלטת המנהל.

### שלב ב — yoman-avoda עוברת לטבלאות מובנות

**הבעיה שנמדדה.** yoman היא היחידה בארגון שנתוניה אינם בטבלאות אלא
כ-13 מפתחות ב-`kv_rishon`/`kv_ramataviv`, כשכל מפתח הוא **ערך יחיד**:
`tb_archive` של ראשון לציון לבדו **1,205,501 בתים · 164 סנאפשוטים**.
כלומר עריכת רשומה בודדת דורסת את מלוא הערך, והמיזוג בענן הוא ברמת קובץ.

**⛔ הדפוס לא תוכנן מאפס** — schar-limud עברה בדיוק את המעבר הזה
בסבבים 12–13, והמימוש שם נקרא והועתק: זהות שנוצרת במכשיר, חותמת עדכון,
tombstones, מיזוג ברמת רשומה, ומודול ה-⏳ הקיים.

**מה נכתב (⛔ ולא הורץ).**
- **`migrations/002`** — `tb_entries` ו-`tb_archive`, שורה לכל רשומה,
  עם עמודת `yeshiva`, `client_id`, `updated_at`, tombstones ו**אינדקסים
  מלאים** (⛔ לא חלקיים — הלקח מ-`007` של schar).
- **`migrations/003`** — פירוק הערכים לשורות, **אדיטיבי בלבד**:
  ⛔ המפתחות הישנים ב-`kv` נשארים במקומם ואינם נמחקים. בגוף הקובץ
  **בדיקת שקילות דו-כיוונית** שהיא תנאי למעבר, ופרק **נתיב חזרה**.
- **בקוד:** שכבת שורות עם נפילה-חזרה לבלוק, ⭐ כך ש**הקוד בטוח לפריסה
  לפני שהמיגרציה רצה** — וזה מה שמאפשר את סדר שני הגלים.
  ⭐ **כתיבה כפולה** — לטבלאות וגם לבלוק הישן, כדי שנתיב החזרה יהיה
  החלפת דגל אחד ולא שחזור נתונים.
  ⚠️ **המחיר המודע:** כתיבת ה-1.2MB לא נעלמה בסבב הזה; היא נעלמת בסבב
  שיפסיק את הכתיבה הכפולה. מה שכן הושג כבר: משיכה ומיזוג פר-רשומה.
- ⛔ **מחיקת המפתחות הישנים אינה בסבב הזה.** **הטריגר:** שבועיים של
  עבודה תקינה על הטבלאות. ⚠️ וגם אז — קודם מפסיקים את הכתיבה הכפולה,
  ורק בסבב שאחריו מוחקים; ⛔ לא שניהם יחד, אחרת אין חלון לגלות טעות.

### ⛔ אחידות — נבדקה בכל סעיף, ולא הונחה
כל רכיב שנוסף כאן נבדק מול שלוש האחרות **לפני** שהסעיף נסגר, ורכיב
שאינו נכנס לאפליקציה מסוימת קיבל נימוק כתוב במטריצה — ואם הוא פער, גם
טריגר. שלוש שורות נוספו למטריצה: **גיבוי יומי אוטומטי · יומן פעולות ·
נתונים בטבלאות מובנות**.
⚠️ הרקע: לאורך סבבים 17–29 התגלו שוב ושוב רכיבים שהוחלו על אפליקציה
אחת בלבד — פינוי, כניסה אופליין, הרשאות, גיבוי, יומן — **כל אחד התגלה
במקרה ולא בבדיקה**.

### תיאום גרסאות
`sw.js`: hanhala **v37** · yoman **v23** (`app-version` **17-2026-08-14-1**) ·
schar **v29** · gius **gius-v15**. **ארבעתן קודמו** — `index.html` של כל
אחת מהן השתנה (מודול הגיבוי נכנס לכולן).
⚠️ המספרים נמדדו מ-`sw.js` של כל ריפו ולא הועתקו משורת התיאום של סבב 29.
<!-- SHARED:end -->

### מה נעשה כאן (yoman-avoda, `sw.js` v23, `app-version` 17-2026-08-14-1)

**שלב א.** `BK_CFG` כאן **פר-מוסד בשלוש שכבות שאין לערבב**: הטבלה בענן
היא `KV_TABLE`, הדגל המקומי נושא את סיומת ה-`LS`, ומפתח הגיבוי נושא
קידומת `<מוסד>_` — אחרת שני המוסדות היו כותבים ל-`kv_backup` תחת אותו
מפתח. שישה מפתחות מגובים (`tb_entries`, `tb_archive`, `tb_cats`,
`tb_subs`, `tb_subs_meta`, `tb_wa_phone`).
⛔ `sources` מחזירה ריק לפני `selectYeshiva` — `KV_TABLE` הוא `null`
עד אז. הקריאה יושבת ב-`_tbMarkSynced()`, הנקודה היחידה שנקראת רק אחרי
שיחה **שהצליחה** עם הענן.
⚠️ **ואין כאן סוד להחריג** — אין טבלת משתמשים ואין סיסמאות, בשונה
משלוש האחיות.

**שלב ב — הפרטים שנבדלים כאן.**
- **⭐ עמודת `yeshiva` ולא טבלה לכל מוסד**, בניגוד לדפוס ה-`kv`.
  הנימוק המלא בראש `migrations/002`; בקצרה: מוסד שלישי דורש כך **אפס
  DDL**, בעוד שבדפוס הקיים הוא דורש טבלה + RLS + פוליסה + `revoke`
  משלו — בדיוק הצעד שקל לשכוח וההגנה נופלת בשקט. ⛔ וההפרדה **במכשיר**
  (סיומת `LS`) לא נגעה.
- **⭐ `client_id` נגזר ממפתח המיזוג ואינו uuid חדש** — ⛔ הפוך
  מ-schar-limud, ובכוונה: שם `id` הוא `SERIAL` ולרשומה לא הייתה זהות עד
  שראתה שרת; כאן לרשומה **כבר יש** זהות שנוצרה במכשיר (`id`/`gdate`),
  ומזהה שני ועצמאי היה מאפשר לאותה רשומה להתקיים פעמיים.
- **⭐ שורה לסנאפשוט בארכיון, לא שורה לרשומה שבתוכו** — הסנאפשוט הוא
  היחידה שמנוע המיזוג מכיר, ובתוכו כבר רץ מיזוג פר-רשומה. 164 סנאפשוטים
  ב-~7KB כל אחד: עריכת יום כותבת ~7KB במקום 1.2MB.
- **⛔ אין טריגר `updated_at` בצד השרת** — החותמת של המכשיר היא האמת,
  ולכן אין כאן את אובדן העריכה שכלל ברזל 6 מתאר ושלוש האחיות מתגוננות
  מפניו.
- **הצד המקומי לא נגע** — localStorage ממשיך להחזיק את אותם מערכים
  באותם מפתחות, ולכן מודול האחסון, מודול ה-⏳ ומדיניות הפינוי אינם
  מושפעים. ⚠️ מה שהשתנה הוא **הייצוג בענן** בלבד.
- **`_tbVerify` עברה לאמת מול הטבלה** (עם נפילה-חזרה ל-`kv`), כלומר
  אימות הפינוי של סבב 14 נשען מעכשיו על אותה אמת פר-רשומה.
- ⛔ **עֵד הפינוי ואישור ה-⏳ דורשים ש**שתי** הכתיבות יצליחו** — ראיה
  חלקית הייתה מאפשרת לפינוי למחוק מהדיסק רשומה שאין לה עותק שלם בענן.

**⚠️ הגיבוי ממשיך לגבות את מפתחות ה-`kv`, וזה נכון כל עוד הכתיבה
הכפולה פעילה** — הם עותק מלא. ⛔ **הסבב שיפסיק את הכתיבה הכפולה חייב
להעביר את הגיבוי לטבלאות באותו סבב**, אחרת הגיבוי יתעד מצב שהפסיק
להתעדכן.

**⭐ כלל ברזל 8 סעיף 6 — הפער שנרשם בסבב 24 נסגר כאן.** יומן היה הריפו
היחיד בלי אף קובץ בדיקה משלו, והטריגר שנרשם שם היה «הסבב הבא שנוגע
בקוד». הסבב הזה נוגע, ולכן נשמרו **שני** קבצים:
`tools/test_round30_stage_a.mjs` ו-`tools/test_round30_stage_b.mjs`.

### אימותים
- **ארבעת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html`
  ועל `sw.js`, `check-status-area.mjs`, `check-docs.mjs`,
  `check-comments.mjs`.
- **`node tools/test_round30_stage_a.mjs` — 59 עברו, 0 נכשלו.**
- **`node tools/test_round30_stage_b.mjs` — 61 עברו, 0 נכשלו.**
- **⭐ שבע מוטציות על שלב ב, כל אחת מפילה טענות** (נמדד, עם בקרה חיובית
  לפני ואחרי כל אחת): `client_id` בלי קידומת מוסד ⇒ 3 · משיכה שנכשלה
  שמדווחת הצלחה ⇒ 1 · התעלמות מסימון ⏳ ⇒ 1 · משיכה בלי סינון מוסד ⇒ 4 ·
  `upsert` על מפתח אחר ⇒ 1 · «טרם נמשך» שאינו מסמן הכל לדחיפה ⇒ 4 ·
  היפוך סדר `archiveKey` ⇒ 1.
- **⚠️ מוטציה שלא הפילה בהתחלה, ומה נעשה איתה:** הסרת בדיקת ה-`error`
  מהמשיכה **עברה** — הלקוח המדומה החזיר `data:null`, ולכן הקוד נפל-חזרה
  בשוגג דרך ה-`catch`. ⛔ זו הייתה חולשה **בבדיקה** ולא בקוד: נוספה
  טענה עם תשובה שיש בה `error` **וגם** מערך תקין (הצורה שמפילה בדיקה
  רופפת), ומאז המוטציה מפילה.
- **⭐ נוסחת הזהות נבדקת מול מימוש עצמאי של ה-SQL** שב-`migrations/003`,
  ולא מול עצמה — שני צדדים שמחשבים זהות אחרת יוצרים שתי שורות לאותה
  רשומה, וזו התקלה היחידה כאן שאין ממנה חזרה שקטה.
- **⭐ ההרשאות והנפחים נמדדו מול המסד החי ולא הוצהרו** — 13 המפתחות,
  1,205,501 בתים ב-`kv_rishon.tb_archive`, 164 סנאפשוטים ו-452 רשומות.
- ⛔ **הסשן לא נגע במסד** — `SELECT` בלבד, ואף מיגרציה לא הורצה.
- **חמשת הבלוקים המשותפים ושני המודולים הקפואים לא נגעו** — `git diff`
  עליהם ריק.

---

<!-- SHARED:start id="iron-rule-12-capabilities" -->
## ⭐ כלל ברזל 12 — יכולת משותפת נמדדת בשלושה ממדים (סבב 30 — השלמה, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את אחד-עשר כללי הברזל שלמעלה.

> **יכולת שמוכרזת «משותפת» מוגדרת בשלושה ממדים, לא באחד: ליבה זהה
> בית-לבית · **נקודת הפעלה זהה** · רישום תואם במטריצת היכולות.
> ⛔ שני מימושים שונים לאותה יכולת הם פער — גם כששניהם עובדים היום.
> הפער מתגלה רק כשאחד מהם נשבר, וזה בדיוק מאוחר מדי.**

### הבעיה שנמדדה
סבב 30 חילץ את הגיבוי היומי לבלוק משותף אחד, זהה בית-לבית בארבעתן —
כלומר קיים את כלל ברזל 7 במלואו. ובכל זאת התברר שהגיבוי של
hanhala-ruchanit **הפסיק לרוץ ב-2026-07-20** ואיש לא ידע:

| | היכן הופעל הגיבוי | התוצאה |
|---|---|---|
| schar-limud | בכניסה למערכת (`enterApp`) | ✅ רץ בכל יום שבו מישהו נכנס |
| hanhala-ruchanit | **מתוך מסלול הדחיפה** (`ysPushToCloud`) | 🔴 25 יום בלי גיבוי |

ההבדל אינו באג בקוד — **הקוד עצמו היה תקין בשתיהן.** ההבדל הוא היכן
הוא מופעל: בהנהלה הגיבוי רץ רק כשמישהו דחף אחד מארבעת המפתחות שהוא
גיבה. בתקופה סטטית — חופשה, שבוע בלי עריכות — אין דחיפה, אין גיבוי,
ואין שום סימן. ⛔ **מנגנון בטיחות שתלוי בפעילות רגילה אינו מנגנון
בטיחות:** הוא נעצר בדיוק בתקופה שבה תקלה עוברת בלי שאיש שם לב.

⚠️ **וזה נמדד ולא הונח:** `sync_log` מראה **444 פעולות ב-30 יום ו-47
בשבוע** באותה תקופה — האפליקציה הייתה בשימוש מלא. תשע מפתחות נוספים לא
גובו **38 יום**. כלומר לא «אף אחד לא נכנס», אלא «אף אחד לא דחף את
המפתחות הנכונים».

### שלושת הממדים
1. **ליבה זהה בית-לבית** — מה שכלל ברזל 7 כבר דורש, ומה
   ש-`check-status-area.mjs` כבר אוכף.
2. **⭐ נקודת הפעלה זהה.** לכל יכולת משותפת יש **תפקיד** קבוע שממנו היא
   מופעלת, זהה בארבעתן, גם כששם הפונקציה נבדל:
   - **יכולות עלייה** (`lsBoot` · `pendBoot` · `bkBoot`) — **פונקציית
     העלייה של האפליקציה**, זו שכבר קוראת לשתי האחרות.
   - **יכולות מסך** (`statusAreaMount` · `bkStatusMount`) — פונקציית
     מסך ההגדרות.
   ⛔ **ואין נקודת הפעלה שנייה.** קריאה נוספת לאותה יכולת ממקום אחר היא
   בדיוק הסטייה הזו בהתהוותה, גם אם היא «רק ליתר ביטחון».
3. **רישום תואם במטריצה** — יכולת שמסומנת ✅ קיימת בקוד, ומה שקיים בקוד
   מסומן ✅. ⛔ מטריצה שאינה תואמת לקוד גרועה מהיעדר מטריצה: היא נקראת
   כעדות.

### ⛔ מעטפת דקה אינה «נוחות» — היא נקודת הפעלה שנייה
כשיכולת נקראת דרך פונקציה מקומית שרק עוטפת אותה (`ysMaybeDailyBackup`,
`slMaybeDailyBackup`), **כל מקום בקוד יכול לקרוא לה** — והכלל שקובע
נקודת הפעלה אחת הופך להמלצה. לכן המעטפות הוסרו, והמודול חושף **קריאה
אחת** שקוד האפליקציה רשאי לעשות. הפונקציה הפנימית נשארת חשופה לבדיקות
ולמודול עצמו, ו-`check-capabilities.mjs` נכשל על כל קריאה אליה מקוד
האפליקציה.

### ⚠️ התרעה על מנגנון שקט שנעצר — `BK_STALE_DAYS`
המדד השני שהיה חסר: **גם אחרי שהגיבוי מופעל נכון, אין דרך לדעת שהוא
באמת רץ.** לכן «מידע טכני ›» מציג «גובה לאחרונה», ומעל
**`BK_STALE_DAYS` = 3 ימים** נוספת שורת התרעה.
⚠️ **הסף הוא שלושה ימים ולא יום אחד** — הדגל היומי אומר שכשל בודד הוא
רעש לגיטימי (מכשיר אופליין, סוף שבוע), ואילו שלושה ימים הם **דפוס**.
עם הסף הזה, ההשבתה בת 25 הימים הייתה מתגלה ביום הרביעי.
⛔ **ולא התראה קופצת** — סבב 15 קבע ששקט הוא ברירת המחדל באזור המצב,
וההתרעה יושבת במקום שבו מסתכלים כשבודקים.

### הבדיקה האוכפת
`tools/check-capabilities.mjs` (ב-gius: `scripts/`, ו-`check-js.mjs`
קורא לה) — **זהה בית-לבית בארבעת הריפו פרט לבלוק `APP` שבראשה**, ורצה
עם שערי התחביר לפני כל דחיפה. היא נכשלת על שלושה סוגי סטייה:
- **א. ליבה** — הבלוק המשותף של היכולת אינו תואם לחתימה הקנונית.
- **ב. נקודת הפעלה** — פונקציית החיווט אינה נקראת מתוך הפונקציה
  הקנונית, נקראת **גם** ממקום אחר, או שפונקציה אסורה (`bkMaybeDaily`)
  נקראת מקוד האפליקציה.
- **ג. מטריצה** — יכולת שמסומנת ✅ אינה קיימת בקוד, או להפך.

⚠️ **הסריקה נעשית על קוד בלבד**, אחרי טוקניזציה שמנקה הערות ומחרוזות.
⛔ אין להחליף אותה בחיפוש טקסטואלי: שם הפונקציה מופיע גם ברשימת ה-API
שבהערת המודול, וכל אזכור בתיעוד היה נספר כקריאה.

### ⛔ מנגנון פיצוי נבדק מחדש כשהמבנה שהוליד אותו תוקן (סבב 32)
**מנגנון שנולד כדי לפצות על מבנה או על מגבלה — נבדק מחדש כשהמבנה מתוקן,
ומוסר אם אינו נחוץ עוד.** ⛔ מנגנון פיצוי ששרד את הסיבה לו הוא **חוב טכני
שנראה כמו ארכיטקטורה**: הוא עבר ביקורת פעם אחת, הוא מתועד, והוא נראה
כהחלטה — ולכן איש אינו שואל עליו שוב.

⚠️ **וזה ממד רביעי ולא חזרה על השלושה שלמעלה.** הם שואלים «האם היכולת
מיושמת אותו דבר בארבעתן»; זה שואל **«האם היא עדיין נחוצה בכלל»**. יכולת
יכולה להיות זהה בית-לבית, מופעלת מאותה נקודה, ורשומה נכון במטריצה — ועדיין
לפצות על בעיה שנפתרה לפני שני סבבים.

**שלוש הדוגמאות שהולידו את הסעיף**, וכולן נמדדו:
- **סבב 24** — `SETUP_SQL_FALLBACK` ב-schar-limud: עותק מוטבע של הסכימה
  שנועד לכסות על כך שאין קובץ התקנה. משנוצר הקובץ, העותק נשאר — מקור אמת
  שני שמתיישן בשקט.
- **סבב 30** — הגיבוי היומי הופעל ממסלול הדחיפה, פיצוי על כך שלא הייתה
  נקודת עלייה משותפת. משנוצרה `bkBoot`, החיווט הישן היה נשאר ומשבית את
  הגיבוי 25 יום.
- **סבב 32** — שתי הדוגמאות החיות: **הארכוב ביומן** (פיצוי על כך שכל
  היומן נמשך כערך `kv` יחיד — בוטל כשהטבלאות המובנות אפשרו שליפה
  מסוננת), ו**הסכימה המוטבעת בהנהלה** (אותו דפוס של סבב 24, ריפו אחר).

⚠️ **הבדיקה היא שאלה, לא סריקה אוטומטית** — ולכן היא נשאלת בכל סבב שמשנה
מבנה: *מה נבנה כדי לפצות על המבנה הזה, והאם הוא עדיין נחוץ?* ⛔ והתשובה
נרשמת גם כשהיא «כן, ומהסיבה הזו» — שקט כאן נקרא כ«לא נשאל».
⚠️ **וההסרה אינה מיידית:** המנגנון הישן נשאר כנתיב חזרה, עם **טריגר כתוב**
לכיבויו ולמחיקתו — בדיוק כפי שסבב 30 קבע לכתיבה הכפולה.

**שינוי מכוון ביכולת משותפת** = עדכון בארבע האפליקציות **ובארבעת עותקי
קובץ הבדיקה** (החתימות ונקודות ההפעלה כתובות בתוכו), באותו סבב.
<!-- SHARED:end -->

---

<!-- SHARED:start id="round-30b-what" -->
## ⭐ סבב 30 — השלמה (אוגוסט 2026): נקודת הפעלה אחת, והתרעה על גיבוי שנעצר

### מה נעשה
השלמה לסבב 30, לפני המיזוג. **הבלוק המשותף של הגיבוי, שער הדחיפה
והתיעוד השתנו בארבעת הריפו.** ⛔ אף מיגרציה לא הורצה, והמסד לא נגע.

### ⚠️ האירוע, כפי שנמדד — והוא הסיבה לכל השאר
סבב 30 דיווח ש«הגיבוי של הנהלה הפסיק לרוץ ב-2026-07-20», ותלה את זה
בכיסוי החלקי. **בדיקה חיצונית מצאה את הסיבה האמיתית, והיא אחרת:**
- **ארבעת המפתחות המגובים** (`ys_students` · `ys_attend` ·
  `ys_approvals` · `ys_attend_sessions`) — חותמת אחרונה **2026-07-20**,
  כלומר **25 יום בלי גיבוי**.
- **תשעה מפתחות נוספים** לא גובו **38 יום**.
- ובאותה תקופה בדיוק `sync_log` מראה **444 פעולות ב-30 יום** ו-**47
  בשבוע** — האפליקציה הייתה בשימוש מלא.

⛔ **כלומר זו לא הייתה תקלה ולא היעדר שימוש, אלא הבדל מימוש:**
`slMaybeDailyBackup` של schar נקראה **בכניסה למערכת**, ואילו
`ysMaybeDailyBackup` של הנהלה נקראה **מתוך מסלול הדחיפה** — רק כשמישהו
דחף אחד מארבעת המפתחות שהיא גיבתה. שתי האפליקציות «עבדו»; הפער נחשף רק
כשאחת מהן נעצרה. **מנגנון בטיחות שתלוי בפעילות רגילה אינו מנגנון
בטיחות.**

### מה תוקן
1. **⛔ נקודת הפעלה אחת בארבעתן — `bkBoot()`.** המודול המשותף חושף
   מעכשיו **קריאה אחת** שקוד האפליקציה עושה, והיא נקראת מ**פונקציית
   העלייה** של כל אפליקציה — זו שכבר קוראת ל-`lsBoot()` ול-`pendBoot()`.
   ⛔ **המעטפות המקומיות הוסרו** (`ysMaybeDailyBackup` בהנהלה,
   `slMaybeDailyBackup` ב-schar): מעטפת דקה היא נקודת הפעלה שנייה בפועל,
   ולכן היא מה שאיפשר את הסטייה מלכתחילה.
   ⚠️ **`logFlush` הוא החריג המכוון** — הוא ריקון תור חוזר, ולכן מותר
   לקרוא לו שוב על חזרת רשת או על כניסה מוצלחת. נקודת ה**עלייה** שלו
   נשארת `bkBoot` בלבד.
2. **⚠️ התרעת גיבוי מיושן — `BK_STALE_DAYS` = 3.** «מידע טכני ›» מציג
   «גובה לאחרונה», ומעל שלושה ימים נוספת שורת התרעה. ⛔ **ולא התראה
   קופצת** (החלטת השקט של סבב 15). זה מה שהיה הופך את 25 הימים לגלויים
   ביום הרביעי.
3. **כלל ברזל 12** — הפרק שמעל.
4. **`check-capabilities.mjs`** — הבדיקה האוכפת, בדפוס
   `check-status-area.mjs` של סבב 17, ונוספה לשער הדחיפה בארבעת הריפו.
   ⚠️ היא סורקת גם את חמש היכולות המשותפות האחרות (אחסון · ⏳ · אזור
   המצב · יומן פעולות · כניסה אופליין), ולא את הגיבוי בלבד — הבדיקה
   שסבב 30 היה צריך כדי לתפוס את הפער **לפני** שהוא נמדד מבחוץ.

### ⛔ אותה בדיקה הורצה על כל יכולת שהסבב נגע בה — ולא רק על הגיבוי
כלל ברזל 12 נולד מפער אחד, אבל הוחל על כולן באותה נשימה:
**אחסון** (`lsBoot`) · **⏳** (`pendBoot`) · **אזור המצב**
(`statusAreaMount`) · **גיבוי** (`bkBoot` + `bkStatusMount`) —
כולן נמדדו, וכולן יושבות היום בפונקציה הקנונית **בארבע האפליקציות**.
**יומן הפעולות** ו**הכניסה האופליין** נבדקים מול המטריצה בלבד: רשימת
האירועים והמסלול נבדלים פר-אפליקציה מעצם טבעם (ר' שורות 14 ו-1
במטריצה), ולכן אין להם נקודת הפעלה משותפת לאכוף.

### תיאום גרסאות
`sw.js`: hanhala **v38** · yoman **v24** (`app-version` **18-2026-08-14-2**) ·
schar **v30** · gius **gius-v16**. **ארבעתן קודמו** — `index.html` של כל
אחת מהן השתנה (נקודת ההפעלה והתרעת הגיבוי המיושן).
⚠️ המספרים נמדדו מ-`sw.js` של כל ריפו ולא הועתקו משורת התיאום של סבב 30.
<!-- SHARED:end -->
### מה נעשה כאן (yoman-avoda, `sw.js` v24, `app-version` 18-2026-08-14-2)
- **`bkBoot()` נקראת מ-`selectYeshiva()`**, מיד אחרי `pendReload(); pendBoot();`.
  ⚠️ **וזו הנקודה הנכונה כאן ולא `DOMContentLoaded`:** `BK_CFG.sources()`
  נשענת על `KV_TABLE`, שהוא `null` עד שנבחר מוסד — כלומר הפעלה מוקדמת
  יותר הייתה מגבה ריק בשקט. `selectYeshiva` היא גם הפונקציה שקוראת
  ל-`lsBoot()` ול-`pendBoot()`, ולכן היא **פונקציית העלייה** לפי כלל
  ברזל 12.
- **החיווט הישן הוסר מ-`_tbMarkSynced()`** — הגיבוי היה תלוי שם בשיחה
  מוצלחת עם הענן, כלומר במסלול הסנכרון. ⛔ זו בדיוק הצורה שהשביתה את
  הגיבוי בהנהלה, גם אם כאן הפולינג של 3 שניות היה מסתיר אותה.
- `bkStatusMount()` נשארת ב-`renderSettings()`, אחרי `statusAreaMount()`.
- `tools/check-capabilities.mjs` נוסף, וקריאה אליו נוספה לפרק «לפני כל
  push» — **שער חובה חמישי**.

### אימותים
- **חמשת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html` ועל
  `sw.js`, `check-status-area.mjs`, `check-docs.mjs`, `check-comments.mjs`
  ו-**`check-capabilities.mjs`**.
- **`test_round30_stage_a.mjs` ו-`test_round30_stage_b.mjs`** הורצו ועברו.
- **⭐ חמש מוטציות על הבודק החדש, כל אחת מפילה אותו** (נמדד כאן, עם בקרה
  חיובית של 0 כשלים לפני ואחרי כל אחת):
  1. **נקודת ההפעלה זזה** — `bkBoot()` הועברה מ-`selectYeshiva` ל-
     `renderSettings` ⇒ «נקראת גם מחוץ ל-selectYeshiva()».
  2. **שינוי הליבה בריפו אחד** — רווח בודד בבלוק המשותף ⇒ חתימה שונה.
  3. **מטריצה שאינה תואמת לקוד** — שורה 13 סומנה ❌ ⇒ «קיימת בקוד אך
     המטריצה אומרת ❌».
  4. **יכולת שהוסרה** — הקריאה ל-`bkBoot()` נמחקה ⇒ «אינה נקראת בשום
     מקום — היכולת קיימת אך אינה מחווטת».
  5. **קריאה ישירה ל-`bkMaybeDaily()`** מקוד האפליקציה ⇒ נכשלת.
- **⭐ ארבעת הבלוקים המשותפים נמדדו כזהים בית-לבית בארבעת הריפו** —
  אחסון `b65b91300715b8f3` · ⏳ `9eefbb4da3ebc37c` · «☁️ סנכרון»
  `65ff4f75bc4f6756` · גיבוי `1fc7a97b5740a1df`. חושבו בארבעתם והושוו
  זה לזה, לא הוכרזו.
- ⛔ **הסשן לא נגע במסד** — אין מיגרציה חדשה בהשלמה הזו, ושתי המיגרציות
  של שלב ב (`002`/`003`) עדיין ממתינות להרצה חיצונית, בסדר שנקבע.

---

## ⭐ סבב 31 (אוגוסט 2026) — תצוגת הארכיון ומקור הרשומות האוטומטיות

### ⚠️ פרק פרטי במלואו — ואין בו בלוק SHARED, במכוון
הסבב הזה נגע ב-**yoman-avoda בלבד**, בהוראה מפורשת, ועל ענף נפרד שאינו
ממוזג. ⛔ ולכן לא נוצר כאן בלוק `SHARED` חדש: בלוק כזה חייב להיות זהה
בית-לבית בארבעת הריפו **ומזהה שלו חייב להופיע ברשימה הקנונית שבארבעת
עותקי `check-docs.mjs`** (כלל ברזל 8). יצירתו בריפו אחד בלבד הייתה
שוברת את זהות קובץ הבדיקה בין הריפו — כלומר מפרה את הכלל בשם קיומו.
⚠️ אם הדפוס שנקבע כאן יאומץ באחיות, זה ייעשה בסבב ייעודי שנוגע בארבעתן.

### הרקע — מה דווח, ומה נמדד
אחרי המעבר לטבלאות (סבב 30, שלב ב) דווחו שני ליקויים בארכיון, בשתי
הישיבות: «לא מופיע הכל» ו«נהיה שם שנה של לא ידוע». ⭐ **הדבר הראשון
שנבדק הוא אם אבדו נתונים — ולא אבדו:** ראשון לציון 155 סנאפשוטים בערך
ה-`kv` ו-155 שורות בטבלה, רמת אביב 15 ו-15, אפס נשמטו בסינון המוסד.
⛔ הבעיה כולה בתצוגה ובמקור, ו**המפתחות הישנים ב-`kv` לא נגעו** — הם
נתיב החזרה.

⚠️ **ושתי הנחות שבבסיס הדיווח תוקנו במדידה, ולא הועתקו:**
1. «הקיבוץ הראשי כיום לפי שנה לועזית מ-`gdate`» — ⛔ **לא שוחזר.**
   `getYearsWithData`/`getMonthsWithData`/`getDaysInMonth` מקבצות
   **תמיד** לפי `extractYM(hdate)`, כלומר לפי השנה והחודש ה**עבריים**.
   אין קיבוץ לועזי כלל, ולכן גם אין סכנת «רשומה שתופיע פעמיים» בין שני
   צירי קיבוץ — הציר אחד.
2. «חסרי ה-`hdate` הם רמת אביב» — ⛔ **הורחב.** גם בראשון לציון יש
   **שישה** כאלה. הדלי «לא ידוע» הופיע בשתי הישיבות, וזה תואם לדיווח
   המנהל שהתקלה בשתיהן.

**המדידה, בשורה אחת לכל מקרה** (`SELECT` בלבד):

| מקרה | ראשון | רמת אביב | הצורה |
|---|---|---|---|
| בלי `gdate`, עם `hdate` תקין | 5 | 0 | `i:3000080`…`i:3000084` |
| בלי `hdate`, התאריך העברי ב-`name` | 6 | 7 | «יום כ״ט אב ה׳תשפ״ו (אוטומטי)» |
| בלי שניהם | 0 | 0 | — |

### מה תוקן

**1 · סדר טעינה יציב.** `tbRowsGet` שלפה בלי `ORDER BY`, ו-Postgres אינו
מבטיח סדר בלעדיו. נוסף `order('rec_key')` בשאילתה **וגם** `tbSortRows`
בקוד — ארכיון מהיום החדש לישן (כמו `ARCHIVE.unshift`), רשומות לפי `id`
יורד (כמו המיון שכבר רץ אחרי המיזוג). ⛔ שניהם נדרשים: `rec_key` הוא
מחרוזת, ומיון לקסיקוגרפי שלה אינו סדר תאריכים.
⚠️ **ולמה זה נראה למשתמש ולא רק «לא מסודר»:** `getAllArchiveDays`
לוקחת את מטא-הדאטה של היום מהסנאפשוט ה**ראשון** שנתקלים בו. ברמת אביב
יש שני סנאפשוטים ל«11 אוגוסט 2026», האחד בשם «כ״ח אב» והשני «כ״ט אב» —
כלומר אותו יום קיבל **תאריך עברי אחר בכל טעינה**.
⚠️ **מה שלא הושג, ונאמר במפורש:** הסדר המקורי של מערך ה-`kv` הוא סדר
**הכנסה** היסטורי ואינו נגזר מהשורות עצמן (נמדד: 9 הפרות מונוטוניות
בכל כיוון — אינו ממוין לא לפי חותמת ולא לפי תאריך). ⛔ ולכן לא נטען
שהוא שוחזר. מה שנשמר ונבדק הוא הסדר הסמנטי ו**התוצאה המוצגת**, שהיא
זהה בין סדר ה-`kv` המקורי לסדר החדש.

**2 · «לא ידוע» — שחזור התאריך העברי לתצוגה.** נוספו `hebFromText`
(חילוץ תאריך עברי מטקסט חופשי) ו-`snapHDate` (`hdate` תקין ⇐ `name`
⇐ ריק), ו-`getAllArchiveDays` משתמשת בהן. אחרי התיקון **אין שנת «לא
ידוע» כלל** בשתי הישיבות.
⛔ **`snapHDate` טהורה ואינה נוגעת בסנאפשוט (סבב 31)** — זה תיקון
תצוגה, ולפי ההנחיה אין לגעת ברשומות קיימות. כתיבה כאן הייתה מקדמת את
`updatedAt` של 13 סנאפשוטים בבת אחת ודורסת את נתיב החזרה.
⚠️ החילוץ הוא **חיתוך אחרי אסימון השנה ואז השלת אסימונים משמאל**, ולא
רשימת תחיליות: `extractYM` קוראת את החודש כ-`hebrewParts.slice(1)`,
ולכן «יום», «יום רביעי», «מוצאי שבת», «(אוטומטי)» ו-«| 11 אפריל 2026»
כולם מזיזים אותה לאסימון הלא נכון. השלה גנרית מכסה את כל הצורות בלי
רשימה שתתיישן.

**3 · ⭐ המקור — רשומה אוטומטית זהה בשדותיה לידנית.** ההשוואה שדה-שדה
בין שני מסלולי היצירה:

| שדה | ידני (`autoArchiveDay`) | אוטומטי (`checkDayChange`) — **לפני** |
|---|---|---|
| `gdate` | ✅ | ❌ **חסר** |
| `hdate` | ✅ | ❌ **חסר** |
| `day` | ✅ | ❌ **חסר** |
| `name` | `hdate` | «יום <hdate> (אוטומטי)» |
| `date` | `gdate` | `toLocaleDateString('he-IL')` |
| `id`/`entries`/`count`/`updatedAt` | ✅ | ✅ |

שלושת השדות החסרים הם שורש שני הליקויים: בלי `hdate` הסנאפשוט נופל
לדלי «לא ידוע», ובלי `gdate` הוא ממופתח `i:<id>` במקום `g:<gdate>` —
ולכן אותו יום מופיע כ**שתי שורות מתחרות בענן**. זה נמדד בפועל: השורה
`i:1786699830512` ברמת אביב היא היחידה בכל המסד שבה
`rec_key ≠ archiveKey(data)`.

התיקון: **בונה סנאפשוט אחד — `arcPutSnapshot` — לשני המסלולים.**
`autoArchiveDay` ו-`checkDayChange` קוראות לו, ולכן אין דרך שאחד ימלא
שדה שהשני משמיט. `auto: true` נשאר כ**סימון מקור** ואינו מחליף שדה.
- ⭐ שדות היום נגזרים **מהרשומות עצמן** קודם כול — הן נכתבו עם התאריך
  שהמשתמש באמת עבד בו, וזה בדיוק מה שהמסלול הידני מקבל. השעון הוא
  נפילה-חזרה, לא מקור ראשון.
- ⛔ **ואין שדה שנשאר ריק בשקט:** `hebrewDate` מחזירה `""` כשספריית
  hebcal טרם נטענה, ולכן `hdate` יכול תיאורטית להישאר ריק. הנפילה-חזרה
  מפורשת ומתועדת בקוד, והשדה **קיים** תמיד — ובמקרה כזה `snapHDate`
  עדיין גוזרת מ-`name`.
- ⭐ **תופעת לוואי מכוונת:** מעכשיו מעבר יום על יום שכבר יש לו סנאפשוט
  **מתמזג** אליו במקום ליצור סנאפשוט שני. זה גם מונע קריסת `upsert`:
  שני איברים באותו מערך עם אותו `client_id` היו מפילים את הדחיפה כולה
  בשגיאת «cannot affect row a second time».

**4 · ⛔ באג נוסף שהבדיקה חשפה — רשומה שנספרה פעמיים.**
`getAllArchiveDays` כתבה ל-`days[k].seen` בלולאת הסנאפשוטים אבל **לא
קראה ממנה שם** (רק בלולאת `ENTRIES`). כששני סנאפשוטים ממופתחים לאותו
יום, הרשומה המשותפת נדחפה פעמיים. נמדד: «11 אוגוסט 2026» הציג **6
רשומות במקום 5**. ⚠️ זה לא נמצא בקריאת קוד אלא **נפל בבדיקה** — הטענה
נכתבה מול המספר האמיתי שבמסד, ולכן היא סתרה את הקוד.

### סריקה רחבה — ממצא לכל מקום, גם מה שלא תוקן
- ✅ **`tb_entries` באותה שאילתה** — אותו חוסר סדר בדיוק. תוקן באותה
  פונקציה (`tbSortRows` מטפלת בשני המפתחות).
- ✅ **`renderArcDays` · `renderArcBreadcrumb` · `getDaysInMonth`** —
  כולן נשענות על `d.hdate`, וכולן מקבלות מעכשיו את הערך המשוחזר דרך
  `getAllArchiveDays`. יש בהן נפילה-חזרה משלהן (`d.hdate||d.gdate||d.key`),
  ולכן הן לא שברו — הן רק הציגו «לא ידוע».
- ✅ **`arcAddEntry`** — בונה רשומה חדשה עם `hdate: d.hdate`. עד היום
  היה יורש `""` מיום «לא ידוע» ו**מייצר רשומה חלקית חדשה**; מעכשיו הוא
  יורש את הערך המשוחזר.
- ⚠️ **`renderArcDetail` (כותרת היום)** — מדפיסה `d.hdate` **בלי**
  נפילה-חזרה, ומציגה כותרת ריקה כשאין. אחרי התיקון אין כזה במסד;
  ⛔ הממצא נרשם ולא תוקן, כדי לא להרחיב סבב תצוגה לשינוי פריסה.
- ⚠️ **`arcCleanupCorrupted` מרפאה `hdate` רק לסנאפשוטים בלי `gdate`** —
  שלב 0 שלה מותנה ב-`if (!snap.gdate)`. ⛔ **וזו הסיבה ש-13 הסנאפשוטים
  האוטומטיים מעולם לא רופאו:** יש להם `gdate` תקין, ולכן הם לא נכנסו
  לענף. הממצא נרשם ולא תוקן — ריפוי שלהם הוא **כתיבה לנתונים קיימים**,
  והתצוגה כבר מטפלת בהם.
- ⚠️ **`_tbGdateTs` מפענחת `d/mm/yyyy` בלבד** ומחזירה 0 לצורה
  «19 יולי 2026» — שהיא בדיוק מה ש-`gregDateStr` מייצרת היום. כלומר
  רשומה בלי `updatedAt` עם `gdate` מודרני מקבלת חותמת 0 ב**מדיניות
  הפינוי**. ⛔ הממצא נרשם ולא תוקן: הוא נוגע לפינוי ולא לתצוגה, וכל
  שינוי שם משנה אילו רשומות נמחקות מהדיסק. **הטריגר:** הסבב הבא
  שנוגע ב-`LS_CFG`/מדיניות הפינוי מטפל בו, דרך `parseGregLike` שכבר
  יודעת את שתי הצורות.
- ✅ **דוחות ה-PDF והשיתוף** — נבדקו, ואינם נוגעים ב-`hdate` של הארכיון:
  הם עובדים על רשומות היום החי ועל שדה הקלט שבמסך.

### מה נעשה כאן (yoman-avoda, `sw.js` v25, `app-version` 19-2026-08-14-3)
- `index.html`: `tbSortRows`/`gdateOrderTs` · `hebFromText`/`snapHDate` ·
  `arcPutSnapshot` (בונה משותף) · תיקון ה-`seen` ב-`getAllArchiveDays` ·
  `order()` בשאילתת `tbRowsGet`.
- **⚠️ שני מזהי הגרסה קודמו, ושניהם חובה:** `CACHE_NAME` → **v25**
  ו-`<meta name="app-version">` → **19-2026-08-14-3**. ⛔ אין לקדם אחד
  בלי השני — ה-service worker מגיש את הקליפה לפי הראשון, ומנגנון
  האוטו-אפדייט שב-APK משווה מול השני.
- ⛔ **המסד והסכימה לא נגעו** — `SELECT` בלבד, אף מיגרציה לא הורצה, ואין
  מיגרציה חדשה. המפתחות הישנים ב-`kv` נשארים כנתיב חזרה.
- **⭐ כלל ברזל 8 סעיף 6 — נשמרו קובץ בדיקה ופיקסטורה:**
  `tools/test_round31_archive.mjs` ו-`tools/fixtures/round31_archive.txt`.
  הפיקסטורה אומרת בכותרתה מה בה אמיתי (המטא של כל 170 הסנאפשוטים, הסדר,
  והספירות) ומה מסונתז (מזהי הרשומות, למעט הזוג הכפול ברמת אביב שנשמר
  אמיתי) — פיקסטורה שאינה אומרת זאת נקראת כעדות.
- **`tools/test_round30_stage_b.mjs` עודכן** — המוק לא הכיר `order()`
  ולכן זרק, ו-`tbSortRows` לא הייתה ברשימת הפונקציות שנחתכות. ⚠️ טענה
  7ה שינתה משמעות במכוון: המשיכה מחזירה מעכשיו **ממוין**, ולכן הרשומה
  הראשונה היא `id` 2 ולא 1. נוספו 7ו ו-7ז.

### אימותים
- **חמשת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html` ועל
  `sw.js`, `check-status-area.mjs`, `check-docs.mjs`, `check-comments.mjs`,
  `check-capabilities.mjs`.
- **`test_round31_archive.mjs` — 132 עברו, 0 נכשלו.**
- **`test_round30_stage_a.mjs` — 59 עברו · `test_round30_stage_b.mjs` — 63 עברו**
  (היו 61; שלוש טענות נוספו/עודכנו סביב הסדר).
- **⭐ שבע מוטציות, כל אחת מפילה את החבילה** — עם בקרה חיובית (0 כשלים)
  לפני ואחרי כל אחת: הסרת `order()` מהשאילתה · משיכה שמחזירה בלי מיון ·
  `snapHDate` בלי נפילה-חזרה ל-`name` · ביטול בדיקת ה-`seen` · מעבר יום
  שחוזר ליצור סנאפשוט בלי `gdate` · `hebFromText` בלי החיתוך אחרי אסימון
  השנה · `arcPutSnapshot` שמוסיפה תמיד במקום למזג.
- **⭐ הבדיקה מריצה את הקוד האמיתי** — `checkDayChange` ו-`autoArchiveDay`
  נחתכות מ-`index.html` ורצות ב-`vm`, ולא נכתבו מחדש בקובץ הבדיקה. שוויון
  השדות נבדק בין **שני הפלטים בפועל**, לא מול רשימה שהודפסה ידנית.
- ⛔ **אין שעון ואין `setTimeout` בקובץ הבדיקה** (הלקח מסבב 24) — כל טענה
  נמדדת על אירוע שהסתיים.
- **חמשת הבלוקים המשותפים ושני המודולים הקפואים לא נגעו** — `git diff`
  עליהם ריק.

---

## ⭐ סבב 32 (אוגוסט 2026) — הסרת שני מנגנוני פיצוי שהסיבה להם נעלמה

### ⚠️ פרק פרטי, ואין בו בלוק SHARED חדש — במכוון
הסבב נגע ב-**yoman-avoda וב-hanhala-ruchanit בלבד**, בהוראה מפורשת.
⛔ ולכן לא נוצר כאן בלוק `SHARED` חדש: בלוק כזה חייב להיות זהה בית-לבית
בארבעת הריפו **ומזהה שלו חייב להופיע ברשימה הקנונית שבארבעת עותקי
`check-docs.mjs`** (כלל ברזל 8 סעיף 3), ויצירתו בשניים מתוך ארבעה הייתה
שוברת את זהות קובץ הבדיקה — כלומר מפרה את הכלל בשם קיומו. אותו שיקול
בדיוק שנרשם בסבבים 22 ו-31.

### הרקע המשותף — אותו לקח, שני מנגנונים
שני הסעיפים הם אותו דבר: **מנגנון שנולד כדי לפצות על בעיה, ונשאר אחרי
שהבעיה תוקנה.** זה מה שהוסר מ-schar-limud בסבב 24 (`SETUP_SQL_FALLBACK`),
וזה מה שהפך כאן לסעיף חדש בכלל ברזל 12.

### 1 · yoman-avoda — איחוד הארכיון
**מה פיצה על מה.** מנגנון הארכוב נולד כשכל רשומות היומן ישבו בערך `kv`
**יחיד**: טעינה משכה את הכול, ולכן ההעברה הצידה הייתה הדרך היחידה לשמור
על מהירות. סבב 30 העביר את הנתונים לטבלאות מובנות עם אינדקסים — ⭐ **ובכך
ביטל את הסיבה**: אפשר לשלוף רק את מה שמוצג. מה שנשאר היו **שתי טבלאות
לאותו סוג נתונים**, בלי מה שהוליד את הפיצול.
- **`migrations/004`** — `archived boolean not null default false` ועמודת
  `gdate` ל-`tb_entries`, ושני אינדקסים **מלאים** על `(yeshiva, archived, …)`.
  ⛔ לא חלקיים — הלקח מ-`007` של schar, ו⛔ `archived` **אינו** נכנס
  לאינדקס הייחודי `(yeshiva, rec_key)`.
- **`migrations/005`** — העברת שורות `tb_archive` ל-`tb_entries` עם
  `archived=true`, **אדיטיבית בלבד**: ⛔ `tb_archive` נשארת במקומה ואינה
  נמחקת. בגוף הקובץ **שער התנגשות מפתחות** שזורק, **בדיקת שקילות
  דו-כיוונית**, פרק **נתיב חזרה** ופער עם טריגר.
- **בקוד:** `tbTableOf`/`tbArchivedFlag` מנתבות את שני סוגי הרשומות
  לטבלה אחת, `tbRowsGet` מסננת `.eq('archived', …)` — ⭐ **וזו השליפה
  המסוננת שהחליפה את הארכוב**. «העברה לארכיון» היא מעכשיו **דגל על
  השורה** ולא העברה בין מקומות.
- ⭐ **הקוד בטוח לפריסה לפני שהמיגרציה רצה**, כמו בסבב 30: בלי העמודה
  השאילתה מחזירה `ok:false` וכל אתר קריאה נופל-חזרה לבלוק ה-`kv`.
- ⭐ **כתיבה כפולה ל-`tb_archive`** (`TB_ARC_LEGACY_WRITE`) כדי שנתיב
  החזרה יהיה **החלפת דגל אחד** ולא שחזור נתונים — אותו דפוס שסבב 30 קבע
  לבלוק ה-`kv`. ⚠️ והיא **אינה** קובעת את `ok`: אחרת טבלה ישנה שנמחקה
  הייתה מפילה את עֵד הפינוי ואת אישור ה-⏳ על כתיבה שהצליחה במלואה.

**⚠️ מה **לא** נעשה, ונרשם ולא הוסתר — הסנאפשוט נשאר יחידת הארכיון.**
הסבב איחד **טבלאות**; הוא לא פירק סנאפשוט לרשומות. הנימוק של `migrations/002`
עומד בעינו: הסנאפשוט הוא היחידה שמנוע המיזוג מכיר (`archiveKey`), יש לו
מטא-דאטה ו-tombstone משלו, ובתוכו כבר רץ מיזוג פר-רשומה — פירוקו הוא
**מנוע מיזוג אחר**. ⛔ ולכן הוא אינו תופעת לוואי של הסבב הזה.
**⚠️ הטריגר:** סבב ייעודי, ורק אחרי ש-004+005 רצו ובדיקת השקילות חזרה
נקייה.

**⚠️ ומה שנשאר תלוי: אף מיגרציה לא הורצה.** `002` ו-`003` של סבב 30
**עדיין ממתינות**, ו-`004`+`005` נכנסות אחריהן — סדר ההרצה המלא הוא
`002 → 003 → 004 → 005`, והוא כתוב בראש `004`. ⛔ ולכן **שורה 15 במטריצה
נשארת ❌** — «נכתב» אינו «אומת».
> ⚠️ **עודכן בסבב 32ב:** `002`, `003` ו-`004` הורצו ואומתו ב-2026-08-16,
> ושורה 15 התהפכה ל-✅. `005` לבדה עדיין ממתינה — ר' פרק סבב 32ב.

### 2 · hanhala-ruchanit — מקור אמת יחיד לסכימה
`index.html` החזיק **שתי** הצהרות `create table` ל-`ys_users` — אחת
להצגה ואחת להרצה — לצד `migrations/000_initial_schema.sql` שנוצר בסבב 28.
שתיהן גם כבר היו מיושנות: הן הגדירו טבלה אחת מארבע. שתיהן **מושכות**
מעכשיו את הקובץ, ובכשל מוצג קישור ישיר ל-GitHub. ⛔ **בלי עותק-גיבוי
מוטבע** ו⛔ **בלי להריץ סכימה שאיננו יודעים מאיפה באה.** שורה 11 במטריצה
התהפכה ל-✅ בארבעתן. הפרטים בפרק הפרטי של הריפו ההוא.

### 3 · כלל ברזל 12 — הסעיף החדש
**«מנגנון פיצוי נבדק מחדש כשהמבנה שהוליד אותו תוקן»** — ר' הבלוק
`iron-rule-12-capabilities` שלמעלה. הוא ממד **רביעי** ולא חזרה על
השלושה: הם שואלים «האם היכולת מיושמת אותו דבר», והוא שואל «האם היא
עדיין נחוצה».

### תיאום גרסאות
`sw.js`: hanhala **v39** · yoman **v26** (`app-version` **20-2026-08-16-1**) ·
schar **v30** · gius **gius-v16**. **שתי הראשונות קודמו** — `index.html`
של שתיהן השתנה. ⛔ schar ו-gius לא נגעו ולכן לא קודמו.
⚠️ המספרים של schar ושל gius **הועתקו משורת התיאום של סבב 30 ולא נמדדו**,
מפני ששני הריפו אינם בהיקף הסשן — ⛔ וזה נרשם במפורש, כי מספר שמועתק
ולא נמדד הוא בדיוק סוג העדות שכלל ברזל 8 סעיף 6 אוסר.

### מה נעשה כאן (yoman-avoda, `sw.js` v26, `app-version` 20-2026-08-16-1)
- **`index.html`:** `TB_ARC_UNIFIED` · `TB_ARC_LEGACY_WRITE` ·
  `tbTableOf`/`tbArchivedFlag` · `tbRowOf` עם ארגומנט יעד שלישי ·
  `tbRowsGet` עם השליפה המסוננת · `tbRowsPush` עם הכתיבה הכפולה.
- **⛔ הצד המקומי לא נגע:** localStorage מחזיק את אותם שני מפתחות ואת
  אותם שני מערכים, ולכן מודול האחסון הקפוא, מודול ה-⏳ ומדיניות הפינוי
  אינם מושפעים. ⚠️ מה שהשתנה הוא **הייצוג בענן** בלבד.
- **⚠️ כל תיקוני סבב 31 נשמרו ונבדקו:** `order('rec_key')` בשאילתה
  ו-`tbSortRows` בקוד (טענות 4ט/4י), חילוץ התאריך העברי מ-`name`
  (`hebFromText`/`snapHDate` — לא נגעו), והבונה האחד לשני מסלולי היצירה
  (`arcPutSnapshot` — לא נגע).
- **`_tbVerify` לא נגעה** — היא פונה דרך `tbRowsGet`, ולכן אימות הפינוי
  של סבב 14 עבר לטבלה המאוחדת מעצמו, כולל הסינון.
- **⚠️ שני מזהי הגרסה קודמו, ושניהם חובה:** `CACHE_NAME` → **v26**
  ו-`<meta name="app-version">` → **20-2026-08-16-1**. ⛔ אין לקדם אחד
  בלי השני — ה-service worker מגיש את הקליפה לפי הראשון, ומנגנון
  האוטו-אפדייט שב-APK משווה מול השני.
- **⭐ כלל ברזל 8 סעיף 6 — נשמר קובץ בדיקה:** `tools/test_round32_unify.mjs`,
  ונוסף לפרק «לפני כל push».
- **`tools/test_round30_stage_b.mjs` עודכן** — המוק לא הכיר את הסינון
  `.eq('archived', …)`, ו-`tbTableOf`/`tbArchivedFlag` לא היו ברשימת
  הפונקציות שנחתכות. ⚠️ שום טענה קיימת לא שינתה משמעות.

### אימותים
- **חמשת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html` ועל
  `sw.js`, `check-status-area.mjs`, `check-docs.mjs`, `check-comments.mjs`,
  `check-capabilities.mjs`.
- **`test_round32_unify.mjs` — 72 עברו, 0 נכשלו.**
- **`test_round31_archive.mjs` — 132 עברו · `test_round30_stage_a.mjs` — 59 עברו ·
  `test_round30_stage_b.mjs` — 63 עברו** (בלי שינוי במספר).
- **⭐ תשע מוטציות, כל אחת מפילה את החבילה** — עם בקרה חיובית (0 כשלים)
  לפני ואחרי כל אחת: ביטול האיחוד ב-`tbTableOf` ⇒ 10 טענות · הסרת
  `.eq('archived', …)` ⇒ 9 · `archived` שנכתב גם לטבלה הישנה ⇒ 2 ·
  ביטול הכתיבה הכפולה ⇒ 2 · כשל בטבלה הישנה שמפיל את הדחיפה ⇒ 2 ·
  `do update` במקום `do nothing` ב-005 ⇒ 1 · אינדקס חלקי ב-004 ⇒ 1 ·
  `archived` nullable ⇒ 1 · `drop table public.tb_archive` ב-005 ⇒ 1.
- **⭐ ההנחה שהאיחוד נשען עליה נבדקה ולא הונחה** — מרחבי המפתחות של
  `entryKey` (מספרי) ושל `archiveKey` (`g:`/`i:`) **זרים**, וזה נבדק גם
  בקוד (טענה 3א) וגם כשער שזורק ב-`005`.
- ⛔ **הסשן לא נגע במסד** — אף מיגרציה לא הורצה, ואין `SELECT` שהורץ
  בסבב הזה כלל. ⚠️ **«נכתב» אינו «אומת»:** ארבע המיגרציות ממתינות להרצה
  חיצונית, בסדר `002 → 003 → 004 → 005`.
- **חמשת הבלוקים המשותפים ושני המודולים הקפואים לא נגעו** — `git diff`
  עליהם ריק.

---

<!-- SHARED:start id="iron-rule-13-shared-scope" -->
## ⭐ כלל ברזל 13 — סבב שנוגע ב-SHARED נפתח עם גישה לארבעת הריפו (סבב 32ב, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את שנים-עשר כללי הברזל שלמעלה.

> ⛔ **סבב שנוגע בבלוק SHARED נפתח מלכתחילה עם גישה לארבעת הריפו.**
> אי אפשר לצרף ריפו לסשן קיים, ולכן סשן שאין לו גישה לארבעתם **אינו
> נוגע בבלוקי SHARED** — הוא רושם את הצורך וסוגר.

### הבעיה שנמדדה
בסבב 32 השתנו שני בלוקי SHARED — `capability-matrix`
ו-`iron-rule-12-capabilities` — **בשניים מתוך ארבעת הריפו**: לסשן לא
הייתה גישה ל-schar-limud ול-gius, וניסיון לצרף אותם באמצע העבודה נחסם.
התוצאה היא בדיוק המצב שכלל ברזל 8 סעיף 3 אוסר — פרק «משותף» שאינו זהה
בארבעתם — ⚠️ ובצורתו המסוכנת ביותר: `check-docs.mjs` של שני הריפו שלא
עודכנו **המשיך לעבור** אצלם, מפני שהחתימות הישנות שלהם תאמו לתוכן הישן
שלהם. **החתימות הישנות הסתירו את הסטייה** — השער שנבנה לתפוס סחיפה בין
הריפו אישר אותה, כי הוא מודד כל ריפו מול העותק המקומי של הרשימה
הקנונית, ולא בין הריפו זה לזה.

### ⛔ ההשלכה המבצעית — לפני פתיחת הסבב, לא באמצעו
- סבב שמתכנן לגעת בבלוק SHARED — או שעלול לגעת בו: שינוי יכולת, כלל
  ברזל חדש, עדכון תא במטריצה — **נפתח מלכתחילה עם ארבעת הריפו בהיקף.**
- סשן שמגלה באמצע העבודה שנדרש שינוי SHARED ואין לו גישה לארבעתם —
  ⛔ **אינו משנה את הבלוק בחלק מהריפו.** הוא רושם את הצורך בפרק הסבב
  (מה צריך להשתנות, ולמה) וסוגר; השינוי עצמו נעשה בסבב שנפתח נכון.
  ⚠️ עדיף פער **גלוי** — בלוק שטרם עודכן באף ריפו — על פער **שקט** שהשער
  מאשר בשניים מארבעה.
- סבב 32 בחר בדיעבד בדרך השנייה — עדכן בשניים ורשם טריגר מפורט עם שתי
  החתימות — וזה מה שאיפשר לסבב 32ב לסגור את הפער בהעתקה בית-לבית.
  ⛔ אבל זו נסיבה מקילה, לא דפוס: הטריגר ישב בפרק פרטי של שני ריפו,
  ושום שער לא אכף אותו.
<!-- SHARED:end -->

---

<!-- SHARED:start id="round-32b-what" -->
## ⭐ סבב 32ב (אוגוסט 2026) — סגירת אחידות ה-SHARED, וכלל ברזל 13

### מה נעשה
**סבב תיעוד ואכיפה בארבעת הריפו. ⛔ אפס שינוי בקוד הרץ** — לא
ב-`index.html`, לא ב-`sw.js`, ולכן אף `CACHE_NAME` לא קודם.

1. **הפער של סבב 32 נסגר.** סבב 32 עדכן שני בלוקי SHARED —
   `capability-matrix` ו-`iron-rule-12-capabilities` — בשניים מארבעת
   הריפו בלבד, מפני שלסשן לא הייתה גישה ל-schar-limud ול-gius. הבלוקים
   הועתקו עכשיו **בית-לבית** לארבעתם, והחתימות עודכנו בארבעת עותקי
   `check-docs.mjs`. ⚠️ ההנחיה שנרשמה בפרקי סבב 32 — «העתק ועדכן
   לחתימות `d6d539ff78571d5f` · `8223e5ba345c4ab3`» — בטלה: תוכן
   `capability-matrix` השתנה **שוב** בסבב הזה (שורה 15), ולכן חתימתו
   כבר אחרת; `iron-rule-12-capabilities` לא השתנה שוב, וחתימתו נותרה
   זו של סבב 32. ⛔ החתימות התקפות הן תמיד אלה שברשימה הקנונית של
   ארבעת עותקי הבדיקה — לא מספר שהועתק מפרק סבב.
2. **שורה 15 במטריצה התהפכה ל-✅ ב-yoman-avoda.** מיגרציות `002`+`003`
   הורצו ב-2026-08-16 ואומתו בבדיקת שקילות מוסד-מול-מוסד — המספרים
   במטריצה, והפרטים המלאים בפרק הפרטי של yoman. ⚠️ התא של
   hanhala-ruchanit נשאר ❌ — הנתונים שלה עדיין ב-`kv`, והטריגר שכתוב
   במטריצה בעינו.
3. **כלל ברזל 13** — הפרק שמעל: סבב שנוגע ב-SHARED נפתח מלכתחילה עם
   גישה לארבעת הריפו; סשן בלי גישה לארבעתם רושם את הצורך וסוגר.

### תיאום גרסאות
`sw.js`: hanhala **v39** · yoman **v26** (`app-version` **20-2026-08-16-1**) ·
schar **v30** · gius **gius-v16**. **אף אחת לא קודמה בסבב הזה** — אין
שינוי בקוד הרץ, וקידום היה שולח לכל המשתמשים באנר «גרסה חדשה זמינה»
לשום שינוי. ⚠️ המספרים נמדדו מ-`sw.js` של כל ריפו — לראשונה מאז סבב 30
גם של schar ושל gius, שחזרו להיקף — ולא הועתקו משורת תיאום קודמת.
<!-- SHARED:end -->
### מה נעשה כאן (yoman-avoda)

**א. ⭐ מיגרציות `002` · `003` · `004` הורצו ואומתו — 2026-08-16.**
ההרצה חיצונית, דרך `apply_migration`, ושלושתן רשומות
ב-`schema_migrations`: `yoman_002_structured_tables` ·
`yoman_003_migrate_kv_to_rows` · `yoman_004_entries_archived_flag`
(נמדד מול המסד, לא הוצהר).
- ⚠️ **`002` ו-`003` היו בפועל no-op** — הכתיבה הכפולה שרצה מאז סבב 30
  כבר מילאה את הטבלאות — וזו בדיוק העדות שההרצה נתנה: הסכימה והנתונים
  שבמסד תואמים למה שהקבצים מגדירים.
- **בדיקת השקילות רצה מוסד-מול-מוסד:** ראשון לציון 455 רשומות ו-156
  סנאפשוטים, רמת אביב 265 ו-15, סה"כ **720/171 — התאמה מלאה לערכי
  ה-`kv`** (`kv_rishon`/`kv_ramataviv`).
- **הערת הפתיחה של `migrations/004` עודכנה** — היא קבעה ש«`002` ו-`003`
  טרם הורצו», וזה כבר לא נכון. סדר ההרצה המחייב להתקנה חדשה נשאר כתוב
  שם כפי שהוא.
- ⚠️ וכך גם עודכנו: שורה 15 במטריצה (❌ → ✅, בבלוק המשותף), והפסקה
  «מה שנשאר תלוי» בפרק סבב 32 — בהערת עדכון, בלי לשכתב את ההיסטוריה.

**ב. ⚠️ `005` נשארת פער עם טריגר — ממתינה לאחרי הפריסה.**
`005_merge_archive_into_entries.sql` (איחוד הארכיון, סבב 32) **לא
הורצה, ובכוונה**: היא מעבירה שורות בין טבלאות, והקוד שכותב לטבלה
המאוחדת (v26) **טרם נפרס** למשתמשים. **הטריגר:** פריסת קוד סבב 32
ואימות שהכתיבה הכפולה ל-`tb_archive` עובדת בייצור — ואז `005`, עם
בדיקת השקילות שבגוף הקובץ. ⛔ אין להריץ אותה לפני כן.

**ג. ⭐ מפתחות ה-`tb_*` הישנים נמחקו מטבלת `kv` הלא-מוסדית — 2026-08-16.**
טבלת `public.kv` — זו שקדמה לרב-הדיירות, ואינה מקור אמת של יומן מאז
המעבר ל-`kv_rishon`/`kv_ramataviv` — עדיין החזיקה שישה מפתחות `tb_*`
שריד. הם נמחקו בהחלטת המנהל ובביצוע חיצוני, **אחרי גיבוי מלא
ל-`kv_backup`** (שש שורות, קידומת `pre-delete-2026-08-16:old-kv:` —
נמדד: שש שורות הגיבוי קיימות, ואפס מפתחות `tb_*` נותרו ב-`kv`).
- ⛔ **מפתחות ה-`kv` המוסדיים — `kv_rishon`/`kv_ramataviv` — לא נגעו
  (סבב 32ב)** — הם נתיב החזרה של סבב 30, ומחיקתם היא סבב נפרד עם
  הטריגר שכתוב שם (שבועיים של עבודה תקינה על הטבלאות, ואחרי הפסקת
  הכתיבה הכפולה).
- ⚠️ טבלת `kv` עצמה היא מקור הנתונים של hanhala-ruchanit — המחיקה נגעה
  **אך ורק** במפתחות ה-`tb_*` של יומן, לא בשום מפתח `ys_*`.

**ד. ניקוי פסקת «הפער התפעולי» מפרק סבב 32** — הפער נסגר, והתיעוד שלו
חי עכשיו בכלל ברזל 13 ובפרק הזה. שורת «עודכן לאחרונה» נשארה
«סבב 32 · 2026-08-16» — היא קודמה בסבב 32 עצמו, וההשלמה באותו יום אינה
מקדמת אותה שוב (התקדים: השלמת סבב 26 ב-gius).

**ה. ⛔ `index.html` ו-`sw.js` לא נגעו.** `CACHE_NAME` נשאר **v26**
ו-`<meta name="app-version">` נשאר **20-2026-08-16-1** (סבב 32) — אין
שינוי בקוד הרץ, וקידום היה שולח באנר «גרסה חדשה זמינה» לכל מכשיר בשביל
שינוי בתיעוד ובהרשאות בלבד.

### אימותים
- **חמשת השערים עברו:** `node --check` על ה-JS המחולץ מ-`index.html`
  ועל `sw.js`, `check-status-area.mjs`, `check-docs.mjs` (עם החתימות
  החדשות), `check-comments.mjs`, `check-capabilities.mjs`.
- **בדיקות הסבבים עברו:** `test_round30_stage_a.mjs` ·
  `test_round30_stage_b.mjs` · `test_round31_archive.mjs` ·
  `test_round32_unify.mjs`.
- **⭐ העובדות שבפרק נמדדו מול המסד החי ולא הוצהרו** — `SELECT` בלבד:
  שלוש השורות ב-`schema_migrations`, הספירות 455/156 ו-265/15, אפס
  מפתחות `tb_*` ב-`kv`, ושש שורות הגיבוי ב-`kv_backup`.
- **⭐ ארבעת הבלוקים המשותפים של הסבב נמדדו כזהים בית-לבית בארבעת
  הריפו** — חושבו בארבעתם והושוו זה לזה, לא הוכרזו.
- ⛔ **הסשן לא הריץ אף מיגרציה ולא נגע בנתונים** — ההרצות והמחיקה
  שמתועדות כאן נעשו חיצונית ב-2026-08-16; הסשן רק מדד ותיעד.
- ⛔ **`git diff` על `index.html` ו-`sw.js` — ריק בארבעת הריפו.**
