# יומן עבודה — CLAUDE.md

<!-- SHARED:start id="table" -->
## טבלת התשתית — מקור האמת היחיד ליכולת

> הטבלה היא ההוראה. אין כללים נפרדים ואין נספח, ואין בה שורה שסותרת שורה אחרת.
> ומה שאינו בטבלה — אינו תקן, וכל יכולת נושאת שורה.
> האותיות רצופות — `א` עד `ט`, בלי חור ובלי תת-מספור: ואין מרשם שמסביר אות שירדה —
> קטגוריה שיורדת מסיטה את הבאות אחריה, בכל מקום שנוקב בהן.

| # | שם | מה זה | יומן | הנהלה | שכר | גיוס | קופה | הערה |
| | א · קבצים ותיעוד | | | | | | | |
| | ↳ שם ⟵ קובץ | | | | | | | |
| 1 | שם האפליקציה — מקור אחד | המקור הוא התצורה — `name` ו-`shortName`, והמקומות נוצרים ממנה או קוראים ממנה: `name` · `short_name` · `<title>` · ו-`android:label`, וכותרת כל קובץ תיעוד נגזרת ממנה — מי שעורך מקום אחד אינו רואה את השאר · ושם הריפו — תעתיק לטיני של השם העברי, במקף · ותיאור הריפו — «שם האפליקציה — מה היא עושה», שלוש עד חמש מילים · ומקור אמת אחד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 2 | המשותף — שלוש דרגות | פרטי — רשימה סגורה: קוד המוצר (`index.html` · `app.css` · `sw.js` של האפליקציה) · `app.config.js` · התיעוד הפרטי (החלק הפרטי של `CLAUDE.md` · `README` · `CONTEXT` · `android/README`) · המאסטרים שב-`design/` · הלוגואים שב-`logos/` · קובץ הסכימה · ונוצר — כל מה ש-`gen-app` ו-`gen-icons` יוצרים מהתצורה, ואינו נערך ביד · ומשותף — כל השאר: זהה בית-לבית, באותו מקום, בכל ריפו שנושא אותו · וערכי האפליקציה — רק ב-`app.config.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 3 | מפת התיקיות — לכל סוג תיקייה אחת | קוד ו-CSS משותפים ⟵ `core/` · כלים ותבניותיהם ⟵ `tools/` · תהליכי GitHub ⟵ `.github/workflows/` · אנדרואיד ⟵ `android/` · חתימה ⟵ `signing/` · קובץ הסכימה ⟵ `migrations/` · אייקונים שנוצרו ⟵ `icons/` · מאסטרים ⟵ `design/` · לוגואים ⟵ `logos/` · קוד המוצר, התצורה, התיעוד ומה שהפלטפורמה דורשת בשורש ⟵ השורש · וקובץ חדש — בתיקיית הסוג שלו · וסוג חדש — נכנס למפה לפני שהקובץ נכנס לריפו | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 4 | `app.config.js` — שלד | אחד-עשר מפתחות, באותו סדר בכל אפליקציה: `id` · `name` · `shortName` · `description` · `prefix` · `colors` · `offline` · `supabase` · `android` · `signSha256` · `icon` — מפתח שאינו רלוונטי מוצהר ריק ואינו נשמט · ומפתח חדש נכנס לכולן באותו מקום: סדר אחד הוא מה שמאפשר להשוות תצורה לתצורה בעין | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 5 | `CLAUDE.md` — תוכן, כמות ובלוקים | הטבלה במשותף · מסכים ולוגיקה בפרטי · ושורתו הראשונה — `# <name> — CLAUDE.md`, ומיד אחריה בלוק משותף אחד — `table`, הטבלה בלבד, נמדד ב-`sha256` · תקרה 700 שורות — 400 משותף · 300 פרטי, ואורך נמדד בתווים גולמיים · בלי היסטוריה ובלי פרקי סבב — git הוא ההיסטוריה · ואין בו מה שהטבלה כבר אומרת · וערך שקיים בקוד או במסד אינו מוצהר בו · והוא מתאר את האפליקציה שלו, וכל ערך בו נמדד מול מקורו · ותאריך בו — `YYYY-MM-DD` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 6 | `README` — תוכן, כמות ובלוקים | התקנה · הפעלה · פיתוח · ושורתו הראשונה — `# <name>` · תקרה 70 שורות — 20 משותף · 50 פרטי, ואורך נמדד בתווים גולמיים · שני בלוקים משותפים בסדר קנוני, נמדדים ב-`sha256` · בלי היסטוריה ובלי פרקי סבב — git הוא ההיסטוריה · ואין בו מה שהטבלה כבר אומרת · וערך שקיים בקוד או במסד אינו מוצהר בו · והוא מתאר את האפליקציה שלו, וכל ערך בו נמדד מול מקורו · ותאריך בו — `YYYY-MM-DD` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 7 | `CONTEXT` — תוכן, כמות ובלוקים | לקוח · צורך · הסכימה וההרשאות · ושורתו הראשונה — `# <name> — קונטקסט פיתוח` · תקרה 70 שורות — 20 משותף · 50 פרטי, ואורך נמדד בתווים גולמיים · בלוק משותף אחד במקומו הקנוני, נמדד ב-`sha256` · בלי היסטוריה ובלי פרקי סבב — git הוא ההיסטוריה · ואין בו מה שהטבלה כבר אומרת · וערך שקיים בקוד או במסד אינו מוצהר בו · והוא מתאר את האפליקציה שלו, וכל ערך בו נמדד מול מקורו · ותאריך בו — `YYYY-MM-DD` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 8 | `android/README` — תוכן, כמות ובלוקים | בנייה וחתימה בלבד · ושורתו הראשונה — `# <name> — Native WebView APK` · תקרה 300 שורות — 150 משותף · 150 פרטי, ואורך נמדד בתווים גולמיים · חמישה בלוקים משותפים בסדר קנוני, נמדדים ב-`sha256` · בלי היסטוריה ובלי פרקי סבב — git הוא ההיסטוריה · ואין בו מה שהטבלה כבר אומרת · וערך שקיים בקוד או במסד אינו מוצהר בו · והוא מתאר את האפליקציה שלו, וכל ערך בו נמדד מול מקורו · ותאריך בו — `YYYY-MM-DD` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| | ב · הטבלה עצמה | | | | | | | |
| | ↳ מבנה ⟵ תקן וסימון ⟵ הערות | | | | | | | |
| 9 | מבנה הטבלה — קטגוריה, מקום ומספור | שורה אחת לכל נושא — שורה שנושאת שני נושאים מתפצלת · ואין שתי שורות שאומרות אותו דבר · ושם השורה אומר את תפקידה, ונפתח בטקסט ולא בסימן · ושורה נכנסת לקטגוריה של הנושא שהיא מודדת, ליד השורה הקרובה לה — וסדר הפנים מוצהר בשורת הכותרת: `⟵` סדר ו-`·` קבוצה · ומספור רציף 1..N, ואות קטגוריה יחידה ורצופה · וכל שורה מתפרקת לכל עמודותיה, וכל קטגוריה נושאת כותרת · ונגזר ואינו מוקלד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 10 | עמודת התקן — תקן ותוכן | העמודה מחזיקה את הערך, המספר או האיסור שהופך את השורה להוראה שאפשר לאמת · ואין בה היסטוריה, ולא מספר שמשתנה בכל שינוי · וחריגה פר-אפליקציה — בעמודת ההערות · והסימון: ✅ מיושם · ⭕ הבדל מוצהר, ובהערה — למה · ושורה — עד כ-400 תווים: התקן, ונימוק אחד בפסוקית; סיפור, דוגמה והיסטוריה אינם כאן · ופסוקית שחוזרת בכמה שורות — בנוסח אחד, אות באות, ובמקום קבוע בסוף השורה: בכל שורה שצריכה אותה | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 11 | עמודת ההערות — תקן ותוכן | שורה שכל תאיה תקינים — הערה ריקה · והערה מחזיקה נימוק להבדל או עובדה על הקוד, ומסבירה למה ולא מה · והיא נפתחת, מודגשת, באחת משתיים: «הבדל מכוון» — ⭕ החלטת מנהל · «נמדד» — ✅ עובדה על הקוד שאינה בתקן · ומספר בהערה הוא עובדה קבועה, או ספירה שמשתנה עם המוצר — המשותף לכולן קודם · והערה מעל מבנה נתונים אומרת מה נכנס, מה אסור ולמה, ולא את תוכנו · והכלל חל גם על הערות הקוד · ושם שנוקבים בו — קיים בקוד · ואין בה הפניה לזמן | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| | ג · המודולים המשותפים | | | | | | | |
| | ↳ הכלל ⟵ סנכרון ⟵ אחסון ⟵ עזר ⟵ ממשק ⟵ כניסה | | | | | | | |
| 12 | הליבה המשותפת היא מודול | קוד משותף חי ב-`core/`, ולא ב-`index.html` · מודול לכל תחום — `ls` · `bk` · `sb` · `merge` · `push` · `hw` · `heb` · והתצורה נמסרת בקריאה · והמודול נטען ב-`import` ומוטמן ב-`sw.js` · ו-`index.html` בסדר קבוע: `import` · תצורה בבלוק אחד · קוד ייחודי · וה-CSS המשותף ב-`core/ui.css` וב-`core/chart.css`, ונטען לפני `app.css` · ובראש כל מודול באנר — שם הקובץ ותחומו · ומקור אמת אחד · ונגזר ואינו מוקלד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 13 | כל שם שנקרא — מיובא | כל שם שמיוצא ממודול הליבה ונקרא ב-`index.html` נמצא ברשימת הייבוא — ושם שיובא ואינו נקרא יורד ממנה: קריאה לשם שלא יובא היא `ReferenceError` שנבלע במטפל, והמסלול נעצר בלי שאיש רואה · ושם בתוך מחרוזת או בהערה אינו קריאה · ושני הצדדים נמדדים — אין שם שנקרא ואינו ברשימה, ואין שם ברשימה שאינו נקרא · ומודול הליבה מייצא רק מה שמיובא, או מה שמתועד לשימוש ידני עם נימוקו | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: אפס פער בשני הכיוונים בכולן — ומספר השמות שנקראים נגזר מהמוצר, ואינו מוצהר כאן |
| 14 | אפס תלות חיצונית, וספרייה בגרסה מוצהרת | הריפו רץ מקלון טרי בלי התקנה — אפס `package.json`, אפס `node_modules`: תלות חיצונית נשברת בלי שאיש שינה קוד · וכלי שדורש יכולת חיצונית נכתב בפנים · ו-action חיצוני נעול לגרסה, והחבילות שהוא מושך מוצהרות בשמן · וספרייה מ-CDN בגרסה מפורשת — לא `latest` ולא מייג'ור צף — בתג שבראש המסמך, ונקראת בסמל הגלובלי שלה · ופיגור של גרסה ראשית נושא נימוק במקומו | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: `supabase-js` בכולן — ומעליה ספריית הייצוא של כל אחת: יומן `html2canvas` · הנהלה `pdfmake` בשתי רשומות ו-`xlsx` · ובשכר ובגיוס `supabase-js` לבדה — ואין ספרייה שנייה |
| 15 | חתימות sha256 | זהותו של כל קובץ בדרגה הראשונה נמדדת בחתימה של 16 תווים, והגוף עצמו מושווה בין הריפו: חתימה שתואמת באותו ריפו אינה עדות לזהות בין הריפו · וחתימת בלוק משותף — מסמן הפתיחה ועד מסמן הסגירה, כולל שניהם, בבתים כפי שהם · ושינוי במודול נכנס לכולם באותו שינוי · והמודולים בסדר קנוני — כל מודול אחרי מה שהוא תלוי בו · וכל מודול נוקב בשורת נושא בטבלה · ו-`tools/` כולה בדרגה הראשונה — ואין בה ערך של אפליקציה | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 16 | כל פונקציה — בכולן או מוצהרת | שם פונקציה שמוגדר בחלק מהאפליקציות — מחווט לכולן, או נושא במקומו נימוק תפקידי: מה היא עושה, ולמה לתפקיד אין מקבילה בשאר — «אינה בכולן» היא המדידה ולא הנימוק · ושם שקיים באחת בלבד הוא מוצר ואינו נמדד כאן · וצורות ההגדרה: `function X` · `window.X = function` · `const X = function` · חץ ברמת המודול · וחץ על `window` — ומאפיין באובייקט אינו שם ברמת המודול | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: כל שם שאינו בכולן — מנומק במקומו |
| 17 | `pend` — ממתין לסנכרון | סימון ⏳, ומונה שמתרענן כל 60 שניות · והסימון יורד רק כשהכתיבה הוכרעה — כשל רשת משאיר אותו, וכשל סמכותי מוריד אותו והסיבה נרשמת: סימון שיורד על כשל רשת מתיר לפנות מהדיסק רשומה שמעולם לא עלתה, וסימון שנשאר על כשל סמכותי מקבע את המונה ואת הניסיון החוזר לנצח — ושורה פגומה אחת חוסמת את התור כולו · ומקומו — `core/sync.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 18 | `bk` — גיבוי יומי: מתי ואיך | גיבוי יומי אוטומטי מנקודת העלייה · הגיבוי קורא בעימוד ואינו נחתך בתקרת השרת — ומספר עגול של שורות הוא סימן לחיתוך · ומבנהו: עוגן מלא שבועי + דיפרנציאלי יומי — העוגן נשמר בארבעה עותקים, והדיפרנציאלי מגבה רק מה שחדש מחותמת העוגן · ומקומו — `core/backup.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 19 | `merge` — ליבת המיזוג | הכרעה לפי חותמת, ורשומה ⏳ מנצחת בשוויון — ואינה גוברת על חותמת חדשה יותר: עריכה מקומית ישנה שלא נדחפה אינה מוחקת עריכה מאוחרת שכבר סונכרנה · ורשומה שאינה בענן כלל — במסלול «מפתח שאינו בענן» · ומה שנבדל בין האפליקציות הוא פרמטר של המנוע ולא קוד כפול — איחוד כפילויות · כפילות מרוחקת · שורה בלי מפתח · ובחירת המקומית, ואין ליישר פרמטר «לשם אחידות» · ומקומו — `core/sync.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 20 | `ids` — מזהי רשומות | מזהה שנוצר במכשיר לפני שראה שרת · והמזהה הוא `text` — ואין להמיר אותו למספר: `parseInt` מחזיר `NaN`, וההשוואה נכשלת בשקט · ומזהה DOM אינו מזהה רשומה · ומקומו — `core/sync.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 21 | `rty` — ניסיון חוזר | נסיגה אקספוננציאלית מ-15 שניות עד 60, והגנת ריצה כפולה · ותשובה שמעידה על סכימה מיושנת עוצרת אותו — `42P01` · `42703` · 404 על טבלה: הן אינן שגיאת רשת, וכל החלטה לנסות עוברת בבדיקה אחת — והפולינג נעצר איתו: רענון הדף הוא היציאה היחידה · ומקומו — `core/sync.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 22 | `pull` — מנגנון המשיכה | משיכה ← מיזוג ← דחיפה, פר-קטגוריה · וחזרה לרשת מכוסה בפולינג, בתוך `PL_CFG.every` · ושלושת המאזינים המשותפים — `pendRender` · `rtyKick` · `plTick`, ומאזין `online`/`offline` נוסף נושא במקומו את מה שהפולינג אינו מכסה · וכל כתיבה מוצלחת לענן שאינה עוברת בשכבת הדחיפה מקדמת את אות הפולינג, פרט לכתיבה שמקורה במיזוג · ומשיכה אינה מוחקת קלט פתוח — הציור נדחה עד שהקלט נסגר · ומקומו — `core/sync.js`, והדחייה ב-`core/ui.js` | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: שלושת המשותפים בכולן — ומעליהם אחד בהנהלה, ואפס בשלוש האחרות |
| 23 | `push` — שכבת הדחיפה | שלוש שכבות נפרדות, פונקציה לכל אחת: שורה · טבלה · טבלאות · ומתזמן מושהה אחד — שתי שמירות רצופות הן מחזור אחד · והמנה 500, ומנה שנכשלה נדחפת שוב שורה-שורה: שורה פגומה אינה חוסמת את התקינות · וכל טבלה שנכתבת מקומית נמצאת ב-`PUSH_TABLES`, או שמסלול הדחיפה שלה מוצהר עם נימוקו · וטבלת ההגדרות ב-`PUSH_TABLES` בכל אפליקציה — מקומית-תחילה, ⏳ ו-`rty` · ואין תור פרטי · ומקומו — `core/sync.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 24 | פעולה מגיבה מיד | מסלול שמופעל מלחיצה מגיב לפני ההמתנה לרשת — כפתור מושבת, או ציור שקדם ל-`await` הראשון · וההצלחה מסומנת אחרי השמירה המקומית — כשל דחיפה הוא «ממתין לסנכרון» ולא שגיאה · וצינור השמירה אחד — `runSave` ו-`afterSave`, והוא מחזיר את ההבטחה · והשומר במקום אחד — בנקודת הניתוב או באתר הכתיבה · ונקודת הרענון `saveRefresh` פר-אפליקציה, ושמירה שאינה עוברת בצינור נושאת את נימוקה · ומקומו — `core/sync.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 25 | משיכה מסוננת בשרת | מסך מושך רק את החלון שהוא מציג — הסינון בשרת, ב-`.gte`/`.lte` על עמודת התאריך או ב-`.eq` על דגל החלון, ולא בדפדפן: הרשת כבר שילמה · והסנכרון, הגיבוי היומי והחלון החם מושכים מלא — מנוע ההכרעה חייב את כל הרשומות · ומשיכת חלון אינה ראיה עננית לפינוי — היא אינה אומרת דבר על מה שמחוצה לה · ומקומו — `core/sync.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 26 | `hw` — חלון חם | צמצום העותק המקומי, ושחזור מהענן — והשחזור הוא ניקוי האופק: אין כפתור «שחזר הכול», שאין מה לשחזר — הסימן שהפינוי משאיר הוא מה שחוסם את המשיכה, והסרתו מחזירה את הישן בסנכרון הבא · ומקומו — `core/storage.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 27 | אסטרטגיית `localStorage` | כתיבה דרך `lsSet` · `wholeKeys`/`oldRecords` ואופק פינוי, ופינוי אוטומטי ב-60% מהמכסה שנמדדה · כל פריט ברשימה נושא `key` ו-`syncedThrough`, והעֵד נבדק פר-פריט · האופק מתנקה מתחת לסף האזהרה · כל טבלת מראה — בפינוי · קבועה בגודלה עם נימוק · או נדרשת במלואה לחישוב שנקוב בשמה · חלון הפינוי — שנתית 400 יום, שוטפת 90 · והפינוי אינו משנה ערך מחושב · ומקומו — `core/storage.js` · ומקור אמת אחד · ונגזר ואינו מוקלד · ונמדד מול מקורו | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 28 | שכבת המראה | העותק המקומי של הענן חי במבנה אחד — `MIRROR`, ממופתח בשם הטבלה, בקידומת `<קידומת>_mirror_` · ומפתח האחסון נגזר, ולא מוקלד בהצהרת הפינוי · וכל טבלה ב-`PUSH_TABLES` יש לה מפתח במראה, ומפתח מראה שאינו נדחף מוכרז ב-`MIRROR_CFG.noPush` עם מסלולו: דחיפה עם רשומת אב · או הסרת סוד · ומפתחות המראה הם הטבלאות שבמסד — והפירוק בקריאה ובכתיבה כאחת · ובדיקת החלון החם יושבת בכתיבה האחת · ומקומו — `core/mirror.js` · ונגזר ואינו מוקלד | ⭕ | ✅ | ✅ | ✅ | ✅ | **הבדל מכוון**: אין ביומן שכבת מראה — הקריאה שם היא `KV_TABLE` פר-מפתח, ואין מבנה שממופתח בשם טבלה |
| 29 | מנוע תאריך עברי | מנוע אחד לכל צרכני התאריך — `hebDate` מעל `Intl`, ובנפילה-חזרה טבלה אריתמטית, בלי תלות ב-CDN · ושנה מעוברת נושאת «אדר א» ו«אדר ב» נפרדים · ושם החודש בגרש עברי `׳` בלבד — שתי צורות הן שני דליים בארכיון · ותווית אחת לכל ערך — שנה · חודש · יום, והצורה מוצהרת פעם אחת · וצורה שאין לה צרכן יורדת · והמנוע, הטבלה וצרכן התצוגה במודול אחד · ומקומו — `core/hebrew.js` · ומקור אמת אחד | ✅ | ✅ | ⭕ | ⭕ | ✅ | **הבדל מכוון**: אפס צרכני תאריך עברי בשכר ובגיוס — ובקופה יש צרכן, והמנוע מחווט בה — ואין מה לאכוף |
| 30 | היום — בשעון מקומי, ובמקום אחד | תאריך היום נגזר מהשעון המקומי ולא מ-UTC — תנועה אחרי חצות אינה נרשמת ביום הקודם · ובפונקציה אחת במודול · ותאריך שהופך ל-`Date` מעוגן בצהריים מקומיים, בפונקציה אחת באותו מודול · וחשבון על תאריכים הוא חשבון לוח, ואינו נוגע בשעון · ומקומו — `core/util.js` · ומקור אמת אחד · ונגזר ואינו מוקלד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 31 | `devid` — מזהה מכשיר | מזהה יציב לרישום ביומן הפעולות · ומקומו — `core/util.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 32 | יומן הפעולות — `sh_sync_log` | נרשמים — דחיפה · משיכה · גיבוי · כניסה, הצלחה וכישלון — מכשיר · משתמש · פעולה · מספר רשומות · והפולינג אינו נרשם · ואין בו סיסמה או סוד · והתור נשלח בעלייה, בחזרת הרשת ואחרי כניסה מקוונת · ונשמר 30 יום — גריעה יומית ב-`pg_cron` · ויומן הריצות של `pg_cron` — באותו חלון · ומקומו — `core/backup.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 33 | מחרוזת למשתמש היא קבוע | מה שהאפליקציה אומרת למשתמש הוא קבוע, ולא ליטרל באתר התצוגה — ואפס ליטרל עברי ב-`toast` · `ask` · `openModal` · והודעה שיותר מאפליקציה אחת אומרת יושבת במודול משותף — המשפחה תשתית ולא מפקד · וקבוע פר-אפליקציה שאין לו קורא יורד · והטקסט נכתב פעם אחת, ואין ליטרל לצד הקבוע · ומחרוזת שיש בה תגית היא בניית רכיב ולא הודעה · ומקומו — `core/util.js` | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: כל הודעה שיותר מאפליקציה אחת אומרת — במודול המשותף, בכולן |
| 34 | רשת — פסק זמן ומאזינים | תקרת זמן על כל קריאה — המודול אות באות · ו-`online`/`offline` מחווטים · ומקומו — `core/util.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 35 | מודאלים | מיכל אחד ו-`openModal(title, body, foot)`, ומסלול סגירה יחיד · ואישור «כן/לא» עובר ב-`ask` — גופו `textContent`, ודיאלוג שדורש HTML עובר ב-`openModal` · וגוף כל מיכל נבנה במודול — שמות מחלקות המבנה, מיקום ה-`role` וקישור הכותרת · והמרשם `DIALOG_ROLE` נוקב בתפקיד של כל מיכל · ומיכל בלי כפתור שמירה אינו נושא `ksave` · ומקומו — `core/ui.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 36 | מיכל הדיאלוג — מקומו ושכבתו | המיכל הוא ילד ישיר של `body` — ולא בתוך מסך שנמחק במעבר · ובמקור הוא לפני `</body>`: הדפדפן מתקן את מה שאחרי הסוגר, והמקור אינו מתוקן · ודרגת השכבה שלו בסולם ה-`--z-*` גבוהה מדרגת כל אזור שיש בו פעולה שפותחת דיאלוג — מיכל שנפתח מתחת למסך קיים ואינו נראה · ו«נעילה» היא התקרה · והפעולה שפותחת דיאלוג נגזרת ממפת הפעולות · ומקומו — `core/ui.js` | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: מיכל הדיאלוג בדרגת «מודאל» בכולן — ומעליה הטוסט והבאנר בלבד, ואפס אזור שפותח דיאלוג יושב בדרגה שאינה נמוכה ממנה |
| 37 | `Enter` שומר בכל שדה עריכה | שדה שנשמר בכפתור נשמר גם ב-`Enter` ומבוטל ב-`Escape`, במטפל גלובלי אחד · וההיקף הוא הטופס — `class="ksave"`, והכפתור שבו נושא `data-ksave` לשמירה ו-`data-kesc` לביטול, והפעולה קיימת במפת הפעולות · וכל שדה עריכה בתוך היקף `ksave`, או מוחרג במקומו: שדה בלי כפתור שמירה, או `Enter` שמטופל במקור · ושדה רב-שורתי אינו נכנס · וכל `<button>` נושא `data-act`, גם במודול משותף ובכפתור שנבנה ב-JS · ומקומו — `core/ui.js` | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: כל שדה עריכה בתוך היקף `ksave` — ומעליהם המוחרגים עם נימוקם: שדה בלי כפתור שמירה, או `Enter` שמטופל במקור |
| 38 | כפתור שכותב מושבת בזמן הכתיבה | מטפל שמחזיר הבטחה — הניתוב מנטרל את הכפתור עד שהסתיימה, בנקודת הניתוב האחת ולא באתר הקריאה · והמנגנון במודול משותף — `busy(btn, on, label)` ו-`actRun(el, fn)` · וכל פעולה שממתינה לכתיבה — מטפלה מחזיר את ההבטחה, גם כשהכתיבה מקומית-תחילה: מטפל שאינו מחזיר אינו נכנס לשומר · ותווית ההמתנה מ-`data-busy`, וברירת המחדל «⏳ שומר…» · ומקומו — `core/ui.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 39 | `toast` — חתימה, גוף ומחלקות | `toast(msg, dur, kind)` · `div` לכל הודעה ב-`#toasts` · ו-`--toast-bottom` הוא ההבדל היחיד — המרחק ממה שקבוע בתחתית המסך, בכל רוחב שבו הפריסה משתנה · וסיווג ההודעה נגזר מהמסלול — `'bad'` להודעה שסימנה שגיאה, ורק לה · והודעה שמערבת שני סיווגים מפוצלת לשתי קריאות · ושלושה סוגים — `.toast.good` · מידע · `.toast.bad` · ו-`aria-live="polite"` על `#toasts` · ומקומו — `core/ui.js` | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: `--toast-bottom` נגזר מגובה הסרגל התחתון בכל אפליקציה, והערך נקרא מ-`app.css` ואינו כתוב כאן · וסימן השגיאה שבטקסט הוא המסלול: `❌` · `🚫` · `📴` · «שגיאה» |
| 40 | גרף נבנה מ-CSS ולא מספרייה | הצגה חזותית של סדרה נבנית מרוחב יחסי — `div` ברוחב `N%` וצבע מאסימון · ורכיב אחד בכל אפליקציה שיש בה סדרה: ספריית גרפים היא מאות קילובייטים בכל עלייה · ואפליקציה בלי סדרה ובלי שיעור מוצהרת · וסדרה של פחות משתי נקודות מוצגת כטבלה · ופס יחיד ופס בסדרה הם רכיב אחד — `bar(pct)`, ו-`barChart` קורא לו: שני מימושים באותה אפליקציה הם שני מקומות לתקן · ומקומו — `core/chart.js` | ⭕ | ⭕ | ✅ | ✅ | ✅ | **הבדל מכוון**: אין יעד חודשי ביומן ובהנהלה — ואין סדרה שתוצג |
| 41 | עלייה שנכשלה — הודעה ורענון | שומר עלייה ב-`core/boot.js` — סקריפט רגיל שנטען ב-`<head>`, שמודול שנכשל בייבוא אינו מריץ דבר · והדף מודיע שעלה — `bootOk()`, קריאה אחת בסוף האתחול · ואם לא — שגיאה לפני ההודעה, או פרק זמן ארוך מעלייה איטית — הודעה אחת וכפתור רענון, זהים בכל האפליקציות, בצבעי התצורה בבהיר ובכהה | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 42 | כתיבת משתמש עוברת בפונקציה אחת | כל כתיבה לטבלת המשתמשים עוברת בפונקציה אחת, והטבלה אינה ב-`PUSH_TABLES` — המראה מסירה את הסיסמה, ודחיפה גורפת הייתה כותבת אותה ריקה · והכתיבה דורשת רשת: בלי רשת היא נכשלת ברעש, בהודעה ייעודית שהיא קבוע · ומסך שינוי הסיסמה קיים בכל אפליקציה שיש בה כניסה — `my-pass` ו-`my-pass-save` · ושכבת הטביעה במודול · וכל דיאלוג פונה ל-`uiNoDialog` כשהמיכל אינו ב-DOM · ומקומו — `core/auth.js` | ⭕ | ✅ | ✅ | ✅ | ⭕ | **הבדל מכוון**: אין ביומן ובקופה טבלת משתמשים ואין בהן כניסה — ולכן אין מה לאכוף |
| 43 | `lock` — נעילת חוסר-פעילות | נעילה אחרי 5 דקות ללא פעילות · ומקומו — `core/auth.js` | ⭕ | ✅ | ✅ | ✅ | ⭕ | **הבדל מכוון**: אין כניסה ביומן ובקופה |
| 44 | `sess` — המשתמש המחובר | המשתמש המחובר בזיכרון בלבד · ומקומו — `core/auth.js` | ⭕ | ✅ | ✅ | ✅ | ⭕ | **הבדל מכוון**: אין כניסה ביומן ובקופה |
| | ד · דפוסים בקוד האפליקציה | | | | | | | |
| | ↳ נתונים ⟵ מסך ⟵ כשלים ⟵ אחסון ⟵ עיצוב | | | | | | | |
| 45 | העברת מזהה ל-DOM | המזהה חי ב-DOM והמטפל קורא אותו משם — `data-id` ודלגציה; הטבעה ב-`onclick` היא הפרה · והמזהה מושווה דרך `idEq`, לעולם לא ב-`===` — מזהה שחזר מהמאפיין הוא מחרוזת, ומזהה DOM או קבוע פנימי אינם מזהה רשומה | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 46 | מיון אחד לכל התצוגות | פונקציית מיון אחת משרתת את המסך ואת כל מסלולי הייצוא — ואין מיון מקומי בשום מסך: כל העותקים של אותו משווה נתנו סדר שנבדל בין המסך לייצוא, והמשתמש ראה דף שאינו מה שהוא ייצא · ונקודת המיון אחת — שמה נבדל ביניהן והמנגנון אחד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 47 | אובייקט יקר נבנה פעם אחת | אובייקט `Intl` — `DateTimeFormat` · `NumberFormat` · `Collator` — נוצר ברמת המודול, ולא בגוף פונקציה שנקראת יותר מפעם אחת · ובנייה עצלה מותרת רק כשהיא נשמרת במטמון ברמת המודול ונבדקת לפניו — פעם אחת היא פעם אחת | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 48 | מפקד נגזר ואינו מוקלד | כל ספירה שיכולה להשתנות נגזרת ממרשם — השורות מהטבלה · הטבלאות מ-`PUSH_TABLES` · הדרגות מהסולם, בקוד ובניסוח כאחד: מפקד מוקלד מתיישן ביום שהמרשם משתנה · ורשימת הישויות נבנית מהמרשמים עצמם, ולא מרשימת ניסוחים ידועים · ומספר שהוא שם או תיאור של מבנה אינו מפקד · וספירה בטבלה חיה תחת «נמדד» בלבד · ונגזר ואינו מוקלד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 49 | מרשם חישוב נושא את המקרה הריק | מרשם שפריטיו נושאים שדה מספרי, או שדה מערך שאינו רשימת שמות — כולל את הקלט הריק: אפס רשומות · רשימה ריקה: טענה שנבדקה רק על קלט מלא עוברת בדיוק במקום שבו היא הכי עלולה להישבר, והמקרה הריק הוא מצב ההתקנה הטרייה · ואין חריגה — אין מרשם חישוב בלי מקרה ריק | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 50 | מסך מציג מיד ומרענן ברקע | מסך שמושך מהענן מצייר תחילה את מה שכבר יש — מהזיכרון או מהדיסק — ואין `await` לפני ה-`innerHTML` הראשון במסלול רינדור · ומשיכות מרובות רצות במקביל ולא בזו אחר זו · וכשל משיכה מסומן גלוי — ולא בשקט · ופעולה שדורשת רשת נושאת חיווי וכפתור מושבת · והמסך מציג מצב שהאפליקציה מחזיקה, ולא תהליך שקורה מחוצה לה | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 51 | `index.html` — שלד | `<head>`: `meta` · `title` · `base` הדינמי, כשיש · `manifest.json` · `app.config.js` · `core/boot.js` · אייקונים · גופנים · CDN · `core/ui.css` · `core/chart.css`, כשיש · `app.css` · והגוף קליפה: כותרת · ניווט, כשיש · `#view` אחד ובו הטעינה · `#modal` · `#toasts` · `#updater`, ו-`#ls-alert` מהקוד המשותף · והמסגרת נחשפת עם המסך הראשון שמשתמש בה · ותוכן הכותרת — מוצר · והמסכים מצוירים בקוד — שיטה אחת, שאפליקציה חדשה מעתיקה | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 52 | ייצוא — שני דפוסים ודרך אחת | מסלול ייצוא הוא אחד משניים — צילום מסך (`canvas`) כשהתוכן נכנס לדף אחד, או מנוע טבלה (`pdfMake`) כשהוא נמשך על פני עמודים · ובשום מקרה HTML חדש — הוא מאבד צבעים ועיצוב, ומייצר מקור שני לאותו דוח · ומסלול שלישי אינו נכתב · והאפליקציה מייצאת בדרך אחת — `share-image` (תמונה משותפת) או `download-file` (קובץ להורדה), ונפילה-חזרה אינה דרך שנייה: אותו תוצר בדיוק, והיא נושאת את נימוקה במקומה | ✅ | ✅ | ⭕ | ⭕ | ⭕ | **הבדל מכוון**: בשכר, בגיוס ובקופה אפס מסלולי ייצוא — ביומן `canvas` ו-`share-image` (יום אחד, נשלח כתמונה), ובהנהלה `pdfMake` ו-`download-file` (רשימה שנמשכת על עמודים) · וביומן נפילה-חזרה אחת — `_shareDownloadFallback` שומרת אותה תמונה כקובץ כשאין `navigator.share` |
| 53 | טיפול באירועים | דלגציה ממאזין אחד ב-`document` — המסכים נבנים מחדש בכל רינדור, ומאזין פר-אלמנט נשבר איתם · וכל שדה מקושר ל-`label` או `aria-label` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 54 | מאזין אחד, בהאצלה מ-`document` | כל מאזין יושב על `document`, ואחד לכל אירוע — מאזין על אלמנט נשבר כשהאלמנט נבנה מחדש, ושניים לאותו אירוע מריצים פעולה פעמיים · ומאזין נוסף, או על אלמנט, נושא במקומו את מה שהאחד אינו יכול · ואפס מטפל מוטבע בתגית, בכל סוג אירוע — הוא מכריח גשר על `window` · וגשר שאין לו צרכן יורד | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: מאזין אחד ל-`click` ואחד ל-`keydown` בכולן — ומעליהם לולאת מדידת חוסר-הפעילות, שנרשמת על ארבעה אירועים ואינה מסלול פעולה |
| 55 | גרירה עוברת ב-Pointer Events | כל גרירה ב-`pointerdown` · `pointermove` · `pointerup` — ולא ב-`dragstart`/`drop`: אירועי HTML5 אינם אמינים במגע · והידית היא נקודת האחיזה, ו-`touch-action:none` יושב עליה בלבד, עם נימוק במקומו · והסדר נגזר מה-DOM הסופי · והגרירה נבדקת בדפדפן, במגע · ואפליקציה שאין בה גרירה מצהירה את נימוקה | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: אפס אירוע גרירת HTML5 ואפס מאפיין `draggable` בכולן — ומעליהם הידיות, וכל אחת נושאת את נימוקה |
| 56 | רכיב משותף מתנהג אותו דבר | מחלקה שקיימת ביותר מאפליקציה אחת נושאת את אותם מצבים — `:hover` · `:focus` · `:active` · `:disabled`: רכיב שמגיב באחת ואינו מגיב באחרת נקרא כתקלה, והמשתמש עובר ביניהן · וערכי המצב הם מוצר — צבע · מיקום · גודל · ריווח: ואינם נמדדים | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: 111 מחלקות חיות ביותר מריפו אחד — ואפס מהן נבדלות במצביהן, והשם נחתך מהגיליון באסימון מלא: `tab-btn` אינו `btn` |
| 57 | המשוב בלחיצה נקבע ולא נירש | `-webkit-tap-highlight-color` מבוטל ברמת ה-`body` · ו-`user-select:none` על כל משטח לחיץ — `button` וכל נושא `data-act`, ולא על טקסט שמעתיקים · והמשוב במגע הוא `:active` בלבד, בערכי הערכה · ו-`:hover` חל רק במכשיר שיש לו ריחוף — `@media (hover: hover)` · ומשטח לחיץ מסמן מיקוד ב-`:focus-visible`, ו-`:focus` שמור לשדה קלט · וערכי המצב — מוצר · ומקור אמת אחד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 58 | הודעות שגיאה | כל הודעת שגיאה עוברת ב-`toast` — לא `alert`, לא `console` בלבד, ולא הודעה מוטבעת במסך · ואזור שגיאה מוטבע מותר רק כשהוא מוכרז בשמו ובנימוקו — הודעת אימות נשארת על המסך בזמן שהמשתמש מקליד מחדש, וטוסט שנעלם משאיר טופס בלי הסבר; ואין מזהה שמוכרז ואינו קיים | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 59 | כשל מגיע למשטח אחד, ובנוסח שנגזר מההתנהגות | כשל מערכת מגיע ב-`toast` — רשת · שרת · טבלה חסרה, גם לפני העלייה · והמשטח המוטבע שמור לשגיאת המשתמש, מה שהוא יכול לתקן בהקלדה · ואין משטח שלישי · והנוסח נגזר מההתנהגות — הבטחת סנכרון מאוחר נאמרת רק על נתון שכבר בתור · וכל הודעת כשל היא קבוע בתחילית `MSG_`, ושם אחד לכל מושג כשל · והודעה שיותר מאפליקציה אחת אומרת יושבת במודול המשותף | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: כל אתר חי של `MSG_OFFLINE` הוא קריאה שנכשלה — ואין בו הבטחת סנכרון, שלא נכתב דבר שיסונכרן |
| 60 | פונקציה משותפת אינה יוצאת בשקט | פונקציה בבלוק משותף שנוטשת פעולה מפני שאלמנט ב-DOM שהיא זקוקה לו חסר — מדווחת: `console.error` לפחות, ובנתיב שהמשתמש רואה גם `toast` · ומסלול שהיעדר האלמנט הוא בו תשובה תקפה מוכרז בשמו ובנימוקו, ואין הכרזה שאין לה אתר בפועל | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 61 | רישום כשלי כתיבה | `catch` ריק שגוף ה-`try` שלו כותב — מקומית או לענן — הוא הפרה, והרישום אינו משנה את הזרימה: הכתיבה נכשלת כפי שנכשלה, אך מפסיקה להיות שקטה · ו-`catch` ריק שאין בו כתיבה אינו נמדד כאן — יש מסלולים שבהם היעדר ערך הוא תשובה תקפה · וגוף ה-`try` נמצא בהתאמת סוגריים — חלון תווים קבוע מפספס `try` ארוך ממנו, והכשל השקט שבתוכו עובר | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 62 | לוג מסלול — תג ורמה | לוג שאינו הודעת משתמש נפתח בתג מודול בסוגריים מרובעים — `[sync]` · `[sw]` · `[ls]` · והרמה נגזרת מהאירוע — `log` למסלול · `warn` לכשל שאינו עוצר · `error` לכשל · ורישום ה-`sw` במודול · ושם התג — אותיות קטנות, מילים במקף, בלי נקודתיים ובלי שם פונקציה · ותחום שיש לו תג במודול המשותף — אותו תג בכל אפליקציה · ואין `console.log` בלי תג · ואין תג ניפוי — `[dbg]`, תג ואחריו מספר סידורי, או «called» בגוף ההודעה | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 63 | חותמת רעננות נכשלת סגור | חותמת שהשוואתה נכשלה מחזירה `0` — «אין ראיה שהענן חדש יותר», ולא «הענן חדש» ולא «הענן ריק»: אימוץ ערך מרוחק על סמך כשל דורס עריכה מקומית · וחל על כל שכבת חותמת פר-מפתח · והערך החסר נקרא `0` ולא `{}` · ואפליקציה שרעננותה מ-`updated_at` ברמת השורה מוצהרת | ✅ | ✅ | ⭕ | ⭕ | ⭕ | **הבדל מכוון**: אין בשכר, בגיוס ובקופה שכבת חותמת פר-מפתח — הרעננות שם נגזרת מ-`updated_at` ברמת השורה, ואין מפה שנמשכת בבת אחת ושכשלונה צריך הכרעה |
| 64 | פריסה במסכי טלפון וטאבלט | תג `viewport` אחד · וסולם נקודות שבירה אחד — 360 · 480 · 640 · 900 · 1200, בכיוון `min-width` בלבד: עירוב `min` ו-`max` מאפשר לשני כללים לחול יחד · וכל חמש הנקודות מוגדרות, ונקודה שאין בה שינוי מוצהרת בשמה · ועמודת התוכן — אסימון אחד, `--col`, וכל פס קבוע ואלמנט צף נגזרים ממנו · והרוחב — מוצר · וכל מסך נבדק בכל הרוחבים — בלי גלילה אופקית ובלי חיתוך | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 65 | ערכת נושא — בהיר וכהה | כל צבע במשתנה, ובהיר וכהה באותם שמות · ומשתנה צבע נושא תפקיד אחד — דיו או מילוי: במצב כהה השניים דורשים גוונים הפוכים | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 66 | שמות שהקוד המשותף קורא — בכל אפליקציה | כל שם משתנה שהקוד המשותף קורא — ב-`core/` ובגיליון של רכיב משותף — קיים בכל אפליקציה, באותו תפקיד: רכיב משותף שקורא שם שאינו קיים — נראה אחרת בכל אפליקציה · והערך — מוצר · ושמות הזהות ארבעה: `--brand` · `--brand-fill` · `--brand-ink` · `--brand-soft` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 67 | אין סגנון מוטבע | סגנון נכתב במחלקה ולא ב-`style="…"` — צבע ליטרל מוטבע אינו מקבל שינוי ערכה · ורוחב דינמי הוא החריג היחיד, ומוצהר · וערך שנגזר בזמן ריצה נכתב למשתנה CSS ב-`setProperty`, והמשתנה מוגדר בכלל שמשתמש בו · ופלט שנצרב לתמונה נושא את סגנונו מוטבע — רק בתוך בונה כזה, שנושא את נימוקו, וכל ליטרל צבע בו מוצלב בשש ספרות לערכו הבהיר של משתנה · וה-CSS של האפליקציה יושב ב-`app.css` | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: 23 · 0 · 0 · 0 · 0 אתרי `style=` — והנותרים כולם באזור מוצהר אחד: בונה הדוח שביומן, שנצרב לתמונה ונשלח החוצה |
| 68 | כל רכיב — מחלקה בשם תפקידו | כל אלמנט מעוצב נושא מחלקה ששמה אומר את תפקידו, גם במחרוזת JS שבונה HTML · ואין מחלקות עזר של תכונה אחת — שרשרת תכונות ב-HTML היא כלל CSS שנכתב בכל מופע, ושינויו דורש לערוך את כולם · ותפקיד שחוזר — מחלקה אחת · ומצב שמתחלף בזמן ריצה — מחלקת מצב בשם | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 69 | `app.css` — שלד | פרקים בסדר אחד, כל אחד בכותרת פרק: אסימוני העיצוב · הערכה הכהה · בסיס · קליפה · רכיבים · מסכים — לפי סדר הלשוניות · נקודות שבירה · וכלל `hover` וכלל כהה של רכיב יושבים ליד רכיבו · והמעבר בין פרקים אינו משנה מראה: כלל שהכרעתו תלויה במיקום עובר לסגוליות — ומי שמזיז כלל בודק שהמראה זהה | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| | ה · תקן כתיבה | | | | | | | |
| | ↳ מסמך ⟵ הערות ⟵ מפרידים ⟵ שמות | | | | | | | |
| 70 | המסמך מתפרסר נקי | `index.html` עובר פרסר תואם-תקן בלי שגיאה ובלי תיקון אוטומטי — אין תג עודף · אין תג שלא נסגר · אין תו BOM בגוף · ואין תוכן אחרי `</body>` · `'</body></html>'` בתוך מחרוזת JS אינו תג | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 71 | מתי נכתבת הערה בקוד | הערה נכתבת רק במקום שבו קורא סביר יופתע, או ינסה «לתקן» משהו שאינו שבור — ואינה נכתבת על שורה מובנת מאליה, על כלל שכבר בטבלה, או עם שם קובץ · וכל נושא סיבה: היא מה שמונע מהקורא הבא לבטל את האיסור בתום לב · והערה מעל מבנה נתונים אינה מתארת את תוכנו — הקוד כבר אומר אותו: היא אומרת מה נכנס, מה אסור, ולמה המבנה קיים | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 72 | שפת ההערות וצורתה | עברית; מונחים ומזהים באנגלית · ושלושה סימני פתיחה בלבד — איסור · אזהרה · החלטה מרכזית, ואין רביעי · וכל נושא סיבה — «אל תעשה X — הסיבה» | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 73 | הערה מכילה נימוק, ולא מצביע | הערה מסבירה למה הקוד כפי שהוא, ואינה מצביעה — לא למקום (קובץ · בלוק · פרק · שורה בטבלה) ולא לזמן (מה שהוסר · תקלה שתוקנה · מנגנון שאינו קיים): מצביע מתיישן · וכל טקסט שהמשתמש קורא מתאר את מה שקורה — תווית שדה מתארת את מה שהשדה מקבל, ותווית כפתור את פעולתו · וכפתור שפעולתו מחיקה נושא לשון מחיקה | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 74 | הערה אינה מפנה לאפליקציה אחרת | הערה מתארת את הריפו שהיא חיה בו — ולא את מצבו של ריפו אחר: הקורא אינו יכול לאמת אותה ממקום עמידתו, והיא הופכת לשקר בשקט כשהריפו האחר משתנה · והנימוק לסטייה נכתב בריפו שסוטה, ולא באחיותיו | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: אפס אזכורים בהערות בכולן — ושם קצר שהוא גם שם עצם נמדד בצורתו המלאה |
| 75 | הבדל מכוון מנומק במקומו | סטייה מדפוס — קובץ שקיים באחת · ערך שנבדל · יכולת שאינה חלה — נושאת נימוק כתוב במקום שבו נתקלים בה: סטייה בלי נימוק נקראת כטעות ו«מתוקנת» בתום לב · ופונקציה ששמה חוזר ביותר מריפו אחד בתוכן שונה נושאת במקומה את היכולת שמצדיקה אותה · וההשוואה על הגוף בלי ריווח והערות | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 76 | מפרידי `═` ברוחב 74 | הצורה אות באות, הכותרת שונה · ובאנר בראש כל מודול משותף נושא «/* ═══ core/<שם>.js — <תחום>» — שם הקובץ ושם התחום, באותו רוחב · וחריגה מוכרזת בשמה ונמדדת משני צדדיה | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 77 | אוצר מילים אחד לפעולה אחת | פעולה שחוזרת ביותר מריפו אחד נושאת אותו שם, גם כשהמימוש נבדל · ומונח שאול נכתב באיותו הנכון · ומפתח בטבלת הגדרות אינו נושא תחילית — הטבלה נושאת אותה בשמה · ומפתח האחסון המקומי ומפתח הגיבוי ב-`sh_backup` כן נושאים אותה, ב-`snake_case` · ומשטח משותף נושא בכל אפליקציה אותו סימון, אותו טקסט ואותם שמות פעולות ומחלקות, ואין בו רכיב חסר · וגוף כל מיכל דיאלוג — מ-`openModal` ומ-`ask` — זהה בכולן · ונמדד מול מקורו | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: שלוש משפחות מפתחות שאין לערבב — מפתח בטבלת הגדרות בלי תחילית, ומפתח אחסון מקומי ומפתח גיבוי איתה |
| 78 | שם טבלה נגזר מתפקידה | שם טבלה נגזר מתפקידה — `<תחילית>_settings` · `_users` · `_entries`, ולא מהיום שבו נוצרה · והתפקיד הוא חתימת העמודות בסכימה המוצהרת, והשם הקנוני הוא הסיומת הנפוצה בתפקיד · וסיומת חורגת נושאת במקומה את התפקיד שמצדיק אותה · ושם הטבלה חי בקבוע אחד, ולא פזור באתרים · ושם כל אילוץ, אינדקס, רצף ומדיניות נפתח בשם הטבלה שלו | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: כל סיומת חורגת בכולן נושאת את התפקיד שמצדיק אותה |
| 79 | שם נגזר מדפוס מוצהר | לכל תחום שמות דפוס אחד — פונקציות · קבועים · מחלקות · ותחילית פונקציה מסמנת שכבה או אפליקציה, ואין בה תחילית של אחות: שם כזה נקרא כשכבה משותפת · ותחילית קבוע כתחילית פונקציה, באותיות גדולות · ושם במודול משותף נושא את תחום המודול — `heb*` · `merge*` · `uniq*`, ולא תחילית של אפליקציה · ושם מודול רב-מילי ב-`-`, ומחלקה ב-kebab-case · וכל חריגה נושאת נימוק תפקידי במקומה | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 80 | תחילית הטבלאות והאחסון מוצהרת, ועקבית עם שם הריפו | התחילית מוצהרת בתצורה — `prefix`, והיא מקור האמת · וכל אות בה היא אות פותחת של מילה בשם הריפו, בסדר · ותחילית אחת לטבלה ולמפתח האחסון · ו-`migrations/` מוחרג | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: עקביות אינה ייחודיות, והייחודיות בשורת «כל שאילתה מול הסכימה המוצהרת» |
| 81 | קובץ הסכימה — שם ורשומה | `migrations/000_schema.sql` — קובץ אחד בכל ריפו · ורשומה אחת לכל ריפו בטבלת המעקב של הפרויקט — `<תחילית>000_schema`, וגופה הקובץ: כך נענית השאלה «האם הקובץ הזה רץ?» · והפניה לקובץ נוקבת בשמו המלא · ונמדד מול מקורו | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| | ו · נתונים ומסד | | | | | | | |
| | ↳ מקור ⟵ סכימה ⟵ הרשאות ⟵ כתיבה ⟵ גיבוי ⟵ מחיקה ⟵ מיזוג ⟵ אימות | | | | | | | |
| 82 | עריכת נתונים אופליין | כתיבה מקומית-תחילה, בלי רשת | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 83 | נתונים בטבלאות מובנות | הנתונים יושבים בטבלאות ולא כערכי `kv` — ואין כתיבת ערך שלם למפתח שיש לו טבלה: שתי כתיבות לאותה רשומה הן שני מקורות אמת, ומיזוג ביניהם הוא הכרעה בלי מפתח | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 84 | מקור הקריאה — טבלאות בלבד | הקריאה מטבלאות, בלי נפילה-חזרה ל-`kv` · וכשל מחזיר «אין ראיה» ולא «הענן ריק», ואינו נכנס לענף «הענן ריק» — מיזוג מול מערך ריק מוחק את מה שלא עלה · והענף יושב בגוף שכבת העימוד, נקודת הקריאה האחת · וכל מפתח שהקוד קורא קיים במסד, ומפתח חי בלי קורא נושא את נימוקו · ונמדד מול מקורו | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: אפס מפתחות שהקוד קורא ואינם במסד, ואפס מפתחות חיים בלי קורא — בכולן |
| 85 | שם נגזר אינו נשמר ברשומה | ערך שנגזר מטבלת עזר אינו נשמר ברשומה — הרשומה נושאת את המפתח, והשם נגזר בתצוגה · ושם ששמור ברשומה מתיישן בכל שינוי בטבלה: ובאפליקציה רב-מוסדית הוא משמר את השם של המוסד שהיה פתוח · ואם הוא נשמר בכל זאת — לארכיון, או כנפילה-חזרה למפתח שאינו קיים — הוא מוצהר | ✅ | ⭕ | ⭕ | ⭕ | ⭕ | **הבדל מכוון**: אין קטגוריות בארבע האחרות — נמדד אפס אתרי כתיבה של שם נגזר |
| 86 | התקופה נגזרת ואינה מאוחסנת | החודש והשנה מחושבים מתאריך הרשומה — ואינם נשלפים משורה שמישהו מילא: טבלת תקופות היא תחליף למנוע שחסר, והיא נשברת בהתקנה טרייה ובכל תקופה שלא הוזנה · וטבלה שנושאת ערך לתקופה אינה טבלת תקופה — ושמה יאמר את הערך, ולא את התקופה · והלוח — עברי או לועזי — נושא נימוק תפקידי במקומו | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 87 | מקור אמת יחיד לסכימה | `migrations/000_schema.sql`, בלי עותק מוטבע — ושני צדדיו נמדדים: שקובץ הסכימה נושא `create table` בפועל, ושאין `create table` ב-`index.html` · ומקור אמת אחד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 88 | קובץ התקנה מלא | התקנה טרייה מקובצי הפרויקט, הבעלים ראשון, על מסד ריק מניבה את חתימת המסד החי · והרצה חוזרת על החי אינה משנה דבר · וכל `create table` נושא `if not exists` · וההערות נחתכות לפני הספירה — המילים `create table` מופיעות בהערות, וספירה גולמית מדווחת פער על קובץ תקין | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 89 | שינוי בסכימה | שינוי רץ במסד, ונקלט בקובץ הסכימה ובגוף רשומתו באותו סבב — הקובץ הוא המסד החי ולא יומן של שינויים: git הוא ההיסטוריה | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 90 | `migrations/` — תוכן | כל `create table` נושא `if not exists` · ו-`grant` ל-`anon`/`authenticated` הוא `select, insert, update` בלבד — אין `delete` ואין `truncate` · כל טבלה שנושאת מחיקה רכה נושאת מזהה שנוצר במכשיר · ובאנר אחד בראש: שם הקובץ והאפליקציה · ואין בו נתונים · ואין שני אינדקסים על אותן עמודות באותה טבלה · ונמדד מול מקורו | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 91 | `migrations/` — כמות | קובץ אחד בכל ריפו — הסכימה המלאה | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 92 | בעלות הסכימה המשותפת | טבלה או פונקציה שמשרתת יותר מאפליקציה אחת באותו פרויקט מוגדרת בקובץ הסכימה של האפליקציה הראשונה בפרויקט — ובו בלבד · ובבלוק זהה בית-לבית בין שני הפרויקטים · ואין לה עותק בריפו אחר — עותק שני מתיישן · ומקור אמת אחד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 93 | דפוס עמודות אחיד | טבלה שהלקוח מסנכרן נושאת `client_id` · `updated_at` — `bigint`, חותמת מכשיר במילישניות · `deleted` · `deleted_at` — `timestamptz`, נכתב בכל מחיקה בפונקציה אחת ב-`core/sync.js` · `deleted_by` · ואין טריגר `touch` — חותמת שרת אומרת מתי הגיע ולא מתי נערך · ואין ברירת מחדל, ו-`not null` דוחה כתיבה בלי חותמת · וטבלה שאינה מסונכרנת — תוספת-בלבד או משתמשים — נושאת נימוק תפקידי | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 94 | טבלה מקבילה — סכימה זהה | טבלה שקיימת ביותר מאפליקציה אחת נושאת אותן עמודות, אותם טיפוסים, ואותו סדר — גם עמודה שאינה בשימוש באפליקציה הזו: טבלה שחסרה בה עמודה אינה ניתנת להשוואה, וסדר שנבדל הופך את `select *` לשלושה דברים שונים · והמשפחות שתיים — `users` ו-`settings`, וכל אחת נושאת רשימת עמודות קנונית אחת בסדרה · והמדידה מול המסד החי — ולא מול קובץ הסכימה: קובץ מודד את מה שהוצהר ולא את מה שרץ · ונמדד מול מקורו | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: משפחת המשתמשים ומשפחת ההגדרות זהות בסכימה בכולן — וביומן אין טבלת משתמשים |
| 95 | חתימת סכימה | סכימת המסד נגזרת לחתימה ומושווית מול המוצהר בקובץ הסכימה — סטייה היא סחיפה · וטבלה מוצהרת קיימת במסד, או מוכרזת בשמה ובנימוקה · והשאילתות עצמן נמדדות בשורת «כל שאילתה מול הסכימה המוצהרת» — וכאן אין סריקה: שתי שורות על אותו קלט הן שתי הכרעות על אותה ראיה · ומקומו — `core/sync.js` · ונמדד מול מקורו | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 96 | כל שאילתה מול הסכימה המוצהרת | כל `from` וכל תנאי — שם טבלה ושם עמודה, בשני הפרויקטים — קיימים בסכימה המוצהרת בקובץ הסכימה, ושם שהוא קבוע נפתר לערכו · וההערות מוחרגות · והסכימה המוצהרת היא מראה של החיה — שינוי שרץ במסד נקלט בקובץ, ושני הכיוונים והסדר נמדדים · וטבלה ריקה נמדדת בעמודות המוצהרות · וכל טבלה נושאת את תחילית בעליה, ורק הבעלים יוצר ומשנה אותה · ונמדד מול מקורו | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: 32 טבלאות מוצהרות בשני הפרויקטים, וכולן חיות במסד |
| 97 | הגדרת פונקציה נקראת מהמסד | גוף פונקציית `plpgsql` נקרא ב-`pg_get_functiondef` — ולא מקובץ הסכימה: הקובץ הוא מה שהוצהר, והמסד הוא מה שרץ, ו-`RENAME` אינו נוגע בגוף הפונקציה · וכל שם טבלה שבגוף קיים בסכימה — שם שהוסב ונשאר בגוף שובר את הפונקציה בזמן ריצה, והפינוי הלילי מפסיק לרוץ בשקט ואיש אינו רואה · ונמדד מול מקורו | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 98 | הרשאות במסד | `select`+`insert`+`update` בלבד · וההרשאות נמדדות מול המסד — `anon` ו-`authenticated` נושאים `INSERT, SELECT, UPDATE` ותו לא: `DELETE` ברמת המסד עוקף את המחיקה הרכה · והמדידה בהתנהגות — בקשה מסוננת שאינה תואמת אף שורה, ו«permission denied» היא הראיה: ל-`information_schema` אין חשיפה ב-REST, והזכות בפועל היא מה שנמדד · וטבלת תוספת-בלבד — ואין לה `UPDATE`: גיבוי ולוג נכתבים פעם אחת ואינם נערכים · ומדיניות RLS אחת לטבלה — `<טבלה>_all`, ‏`ALL`, ל-`anon` ול-`authenticated` · ובטבלת תוספת-בלבד — `<טבלה>_insert` ו-`<טבלה>_select`, לאותם שניים · ונמדד מול מקורו | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: אפס `DELETE` ואפס `TRUNCATE` בכל הטבלאות שבשני הפרויקטים — ומעליהן `sh_backup` ו-`sh_sync_log` נושאות `INSERT, SELECT` בלבד |
| 99 | דפוס `upsert` | כל `upsert` נושא `onConflict`, והמפתח מוכרז ומנומק | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 100 | חותמת בכל רשומה | אין רשומה שחותמתה `0` או `null` — אפס אינו «לא ידוע» אלא הישן ביותר, וכל עריכה מנצחת אותו · והעברת נתונים משמרת את החותמת המקורית, ואינה חותמת ב-`now()` · ואין בקוד זרע מוטבע שמזריק רשומות בלי חותמת — סנאפשוט שמוזרק מחדש בכל טעינה ובכל מיזוג מחזיר את האפסים אחרי כל תיקון במסד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 101 | החלפת הקשר מאפסת את כל המצב | כל משתנה שנטען ממוסד או ממשתמש מאופס לפני הטעינה החדשה — הנתונים, מפת החותמות המרוחקות, ומצב הניווט, ולא משתני הסנכרון בלבד · ואין דחיפה בין האיפוס לטעינה — מחזור «קרא ← מזג ← דחוף» שנפתח בהקשר אחד אינו כותב בהקשר אחר · ודחיפה מושהית מבוטלת באיפוס — אחרת הצלחת דחיפה של מוסד אחד נזקפת לחשבון השני | ✅ | ⭕ | ⭕ | ⭕ | ⭕ | **הבדל מכוון**: שלוש מהן מחליפות משתמש ולא מוסד, ובקופה אין הקשר שמתחלף — ואין בהן מסלול שמאפס מצב פר-מוסד. ובמסד — סנאפשוטי ארכיון של מוסד אחד זהים בית-לבית לאלה של השני: והנתון עצמו הוא מדיניות נתונים |
| 102 | הקשר נלכד בכניסה לפונקציה | כל פונקציה אסינכרונית לוכדת את ההקשר בכניסה, ואינה קוראת גלובלי פר-הקשר אחרי `await` · וכל רישום שאחרי המתנה נבדק מול ההקשר שנלכד — עֵד הפינוי · אישור ה-⏳ · חותמת הפולינג · וכל מחזור נבדק מול השומר, כולל סגור שמועבר לשכבת העימוד · והדחיפה המושהית לוכדת בקביעת המועד ובודקת בהתעוררות · ושומר ההקשר קיים בכולן — המוסד או המשתמש · ואפס אתרים עם `stale` עירום | ✅ | ✅ | ✅ | ✅ | ⭕ | **הבדל מכוון**: אין בקופה הקשר שמתחלף — ואין מחזור שנזקף להקשר אחר; ובארבע האחרות כל מחזור נבדק מול השומר, ואיתם הדחיפה המושהית, שהיא מחזור בשכבה המשותפת |
| 103 | `sh_backup` — השמירה והפינוי במסד | `pg_cron` יומי — 7 עותקים או 30 יום · הגיבוי היומי כותב ל-`sh_backup`, ו-`bk_retention_sweep` מפנה אותה ב-03:00 — ומפתח שאינו ברשימת-ההיתר אינו מתפנה לעולם · ושם הטבלה נקרא מהקבוע שבקוד — שם שנכתב במקום שני מגבה למקום שאיש אינו מפנה · ורשימת-ההיתר היא בדיוק מפתחות הגיבוי שהקוד כותב, ואין ב-`sh_backup` שורה במפתח אחר · והיא נקראת מהמסד · ונמדד מול מקורו | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 104 | ערך במפתח-ערך הוא JSON | עמודת `value` נושאת JSON תקין — גם למחרוזת: `"טקסט"` ולא `טקסט` · ואילוץ `check` במסד אוכף אותו — `value is null or value::jsonb is not null` · והקורא עושה `JSON.parse` בנקודת מעבר אחת, בכולן · וההודעה מבחינה בין «אין ערך» ל«ערך פגום», בכולן ובאותו נוסח · ומקומו — `core/util.js` | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: ארבע טבלאות בפרויקט המשותף ואחת בגיוס נושאות את האילוץ — ו-35 הערכים שבהן נבדקו אחד-אחד, ואפס אינם JSON |
| 105 | `tombstones` | מחיקה היא סימון + חותמת, וגריעה לפי גיל מסף שמוצהר ב-`TOMBSTONE_TTL_MS` — 90 יום · והמנגנון במודול משותף ומחווט בכולן — נקודת הפעלה אחת בעלייה, והגריעה על תוצאת המיזוג ולא על העותק המקומי: גריעה מקומית בלבד מוחזרת מהענן תוך שניות · ומחיר הגריעה מוצהר במקומו: מכשיר שנותק מעבר לסף מחזיר רשומה מחוקה פעם אחת, והסף עצמו הוא מדיניות נתונים · ומקומו — `core/sync.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 106 | מחיקה רכה בלבד | אין `DELETE` פיזי בשום מסלול | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 107 | אב-ובן — ירושה וסדר | בן יורש את מחיקת האב ואת חותמתה — מחיקת אב ובניו היא אירוע אחד · והאב נדחף לפני הבן, ב-`PUSH_TABLES` או בגוף הכותב · ואין מפתח זר שמוחק — אפס `on delete cascade`, ומצב המפתח הזר פר-זוג: `restrict` או אין כלל · ומפת האב-ובן חיה במקור, וכל זוג בה נושא טבלה חיה · ובן שיש לו שני הורים נושא סדר הכרעה מוצהר במפה | ⭕ | ✅ | ✅ | ✅ | ✅ | **הבדל מכוון**: היומן והארכיון הם `ya_entries` יחידה, והמעבר ביניהם דגל על השורה — ואין ביומן טבלה שתלויה בשורה של אחרת; נמדד: 2 · 1 · 3 · 2 זוגות בארבע האחרות, ומעליהם בן אחד לשני הורים — התנועה שבגיוס |
| 108 | מפתח הסימון הוא מפתח המיזוג | השורה מזוהה באותו מפתח בכל שכבותיה — מיזוג · סימון ⏳ · ומחיקה מדורגת: שני מפתחות לאותה שורה הם שתי זהויות, והסתירה מתגלה רק כשנכתב מסלול הכתיבה הראשון — ואז היא באג בשדה ולא בקוד · וטבלה בלי מסלול כתיבה אינה פטורה: היא זו שתישכח · ופער נושא את נימוקו ואינו נשמט · ומטפל הסימון מקבל מפתח אחד — קריאה בשני ארגומנטים מסמנת את שם הטבלה ולא את השורה | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 109 | מיזוג מכל — מפה ומערך | ערך שהוא מפה של מפתחות ממוזג פר-מפתח ולא באיחוד — ומחיקת מפתח היא חותמת ולא היעדר · ושדה שהוא מערך בתוך רשומה ממוזגת ממוזג פר-פריט, או שהרשומה נושאת חותמת נפרדת לשדה — מיזוג ברמת רשומה מחליף את המערך כולו · ופריט במערך נושא חותמת משלו — בגוף הפריט, או במפת חותמות לצידו | ✅ | ✅ | ⭕ | ⭕ | ⭕ | **הבדל מכוון** בשכר, בגיוס ובקופה: אין בהן רשומה ממוזגת שנושאת מערך פריטים — ו-`g_settings` בגיוס היא טבלת `{key, value}` שממוזגת פר-מפתח, ואין בה פריטים בעלי מזהה |
| 110 | ברירת מחדל אינה ממוזגת | ערך קבוע בקוד אינו נכנס למיזוג עם הענן — הוא נקודת פתיחה למצב ריק, ומצב ריק מוצג כריק · וכל ערך מפתח-ערך וכל טבלת מראה נושאים עידן בענן, שמכשיר מיושן זורק מולו את העותק המקומי · וסימן הזריקה `<מפתח>_reset` בפורמט ISO · ושינוי בצורת שורה מקדם את `DATA_ERA` ומוחק את הקורא לצורה הישנה באותו שינוי · והזריקה מותנית ברשת, בדחיפה שהחזירה `ok` ואפס `still`, ובתור ריק · ומקומו — `core/sync.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 111 | רשימה אינה נושאת פריט כפול | פריט אינו מופיע פעמיים באותה רשימה — לא בכתיבה ולא אחרי מיזוג: שני פריטים באותו ערך הם פריט אחד · והכיווץ עובר במודול המשותף בלבד — `uniqHas` בכתיבה ו-`uniqList` במיזוג, ושתיהן מכריעות לפי אותה זהות: ואין אתר שכותב בלי לעבור בהן · וחריגה נושאת את נימוקה במקומה — רשימה שהזהות בה אינה הערך · ונקודת מיזוג שאינה מכווצת נושאת נימוק כתוב · ומקומו — `core/util.js` | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: כל אתר כתיבה לרשימה עובר ב-`uniqHas` בכולן |
| 112 | כל מפתח שהקוד מבקש קיים במסד | כל מפתח שקריאת הגדרה מבקשת — קיים בטבלת ההגדרות · וקריאת מפתח יחיד אינה ב-`single` — שורה שאינה קיימת מסומנת שם כשגיאה, ומסך שנתוניו תקינים מכריז «הרענון מהענן נכשל» · ונמדד מול מקורו | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 113 | אימות פינוי מול הענן | פינוי רק מול ראיה שהרשומה בענן, והעֵד הוא ראיה מהענן עצמו — דחיפה שחזרה `ok`, או חותמת משיכה מלאה מוצלחת שנכתבת גם במכשיר שרק קורא: עֵד שמתעדכן גם במשיכה מוחק מהדיסק רשומה שמעולם לא עלתה, ולכן העֵד הוא פר-מפתח ואינו חותמת הסנכרון הכללית | ✅ | ✅ | ✅ | ⭕ | ⭕ | **הבדל מכוון**: אין בגיוס ובקופה טבלה שמתפנה — כל טבלה שגדלה נדרשת במלואה לחישוב, ואין פינוי לאמת מול הענן |
| | ז · קלט ואבטחה | | | | | | | |
| | ↳ קלט ⟵ כניסה ⟵ סודות | | | | | | | |
| 114 | שדה מספרי | `type=text` + `inputmode` + ולידציה · וקריאת הערך עוברת ב-`readNum` — המרה ישירה מחזירה `NaN` על שדה ריק, ו-`NaN` בהשוואה נכשל בשקט: `!val` תופס גם אפס תקין, ו-`val > 30` על `NaN` הוא `false` · והברירה מוצהרת בקריאה ואינה נגזרת מהטיפוס · וערך שמקורו במסד אינו שדה קלט · ומקומו — `core/util.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 115 | השוואה לאפס דורשת שהיעד קיים | הפרש שנגזר ב-`Math.max(0, יעד − בפועל)` ומשמש כהכרעת «הושלם» — דורש שהיעד גדול מאפס — אפס מתוך אפס אינו השלמה: המצב הריק נקרא כהצלחה, והמסך מברך על מה שלא נעשה · והשומר על היעד ולא על ההפרש — ההפרש אפס בשני המצבים, והיעד הוא מה שמבדיל ביניהם · והנמדד הוא ההכרעה — ולא ההפרש עצמו: הפרש שאינו מוכרע כמצב אינו נמדד כאן | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: אתרי ההפרש נמדדים בכולן — ואפס מהם מוכרע כהשלמה בלי שומר על היעד |
| 116 | כל ערך נכנס ל-HTML דרך `esc` | ערך שאינו קבוע בקוד — מהמסד, מהמשתמש או מהתצורה — נכנס למחרוזת HTML דרך `esc` · ומקטע HTML שכבר נבנה נושא שם שאומר זאת | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 117 | שדות סיסמה | `type=password` + `autocomplete` + `maxlength` | ⭕ | ✅ | ✅ | ✅ | ⭕ | **הבדל מכוון**: אין כניסה ביומן ובקופה |
| 118 | שכבת כניסה | כניסה · החלפת משתמש · שינוי סיסמה · ומבנה הטופס אחיד — `<form>` בכולן או באף אחת: שני מימושים לאותה יכולת הם שתי התנהגויות במקש · והשכבה — ב-`core/auth.js`: מלח · טביעה · שדות סיסמה · מראת משתמשים · ואימות בחזרת הרשת — וההקשר נגזר מהתצורה | ⭕ | ✅ | ⭕ | ✅ | ⭕ | **הבדל מכוון**: אין כניסה ביומן ובקופה, ובשכר משתמש יחיד |
| 119 | מודל ההרשאות | ההרשאה נגזרת מ-`role` שבטבלת המשתמשים — ולא מסיסמת שער · ואוצר המילים אחד: `admin` · `manager` · `junior` — ודרגה שאיש אינו נושא אין לה אתר בקוד · ובדיקה אחת: `isAdmin()` במודול משותף · ומי שאינו מורשה נחסם ומקבל הודעה שנבדלת מ«העמודה אינה קיימת» · ואף מסלול אינו משווה מול שם התפקיד כסיסמה — וחריגה מוכרזת בשמה ובנימוקה · ומקומו — `core/auth.js` | ⭕ | ✅ | ✅ | ✅ | ⭕ | **הבדל מכוון**: אין כניסה ביומן ובקופה — ואין `role` שיוכרע |
| 120 | מראת המשתמשים — ועדכונה החלקי | עותק מקומי של טבלת המשתמשים לכניסה אופליין, במפתח אחד: `<קידומת>_mirror_users` · והמראה נושאת `pass_salt` ו-`pass_fp` בלבד, ולעולם לא את הסיסמה — גם בעדכון חלקי · וכניסה אופליין עובדת לכל משתמש · והכניסה המקוונת והשלמת הטביעה מעדכנות את שורתן בלי משיכה מלאה · והמעבר לדיסק בנקודה אחת · ומקומו — `core/auth.js` | ⭕ | ✅ | ✅ | ✅ | ⭕ | **הבדל מכוון**: אין כניסה ביומן ובקופה — ואין מראה שתחזיק |
| 121 | `PBKDF2` — פרמטרים | 100,000 סיבובים · SHA-256 · טביעה של 256 ביט | ⭕ | ✅ | ✅ | ✅ | ⭕ | **הבדל מכוון**: אין כניסה ביומן ובקופה |
| 122 | מלח אקראי לכל משתמש | `pass_salt` פר-משתמש | ⭕ | ✅ | ✅ | ✅ | ⭕ | **הבדל מכוון**: אין כניסה ביומן ובקופה |
| 123 | כניסה אופליין | אימות מול הטביעה, מרובת-משתמשים · וטביעה חסרה מושלמת אוטומטית, או שאין מסלול שיוצר משתמש בלעדיה — ההשלמה היא בכניסה מקוונת מוצלחת ומול הסיסמה שהוקלדה, ולא מול עמודה גלויה; ובהיעדר `crypto.subtle` מדלגים בשקט ואין מכריזים על אי-התאמה · וכניסה אופליין מאומתת מחדש כשהרשת חוזרת — ומשתמש שהושבת יוצא | ⭕ | ✅ | ✅ | ✅ | ⭕ | **הבדל מכוון**: אין כניסה ביומן ובקופה |
| 124 | סיסמאות בענן | אין בקוד מסלול שכותב סיסמה בטקסט גלוי, ואין מסלול שקורא אותה — ואין עמודת סיסמה במסד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 125 | סודות אינם עוזבים את המכשיר | ערך שסומן כסוד נשלף מכל מטען לפני שהוא נכתב, ובנקודת מעבר אחת — משיכה · כתיבה מקומית · והכתיבה לדיסק עוברות כולן דרכה: סינון שיושב במשיכה בלבד הוא בטוח במקרה ולא במבנה, ונתיב אחד שנוסף עוקף אותו בשקט · ורשימת היתר ולא רשימת איסור | ⭕ | ✅ | ✅ | ✅ | ⭕ | **הבדל מכוון**: אין כניסה ביומן ובקופה ואין סוד שיעזוב |
| 126 | אין מפתח שירות ואין סכימה מוטבעת | אפס `service_role` בקובץ — גם בהערה: מפתח שירות הוא מפתח על שעוקף RLS, והוא ציבורי ברגע שהדף נטען · ואפס `exec_sql` · ואפס לקוח Supabase שנבנה מערך שהמשתמש הקליד — `.value` · `prompt` · או האחסון המקומי · ואין הצהרת סכימה בקוד הרץ — `alter table` · `create policy` · `drop`/`truncate` · `grant`: עותק מוטבע מתיישן בכל שינוי בסכימה, ומי שמתקין ממנו מקבל מסד שאינו זה שבייצור | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 127 | אין סוד בקובץ שנדחף | כל מחרוזת שנראית כאישור — ערך ליד מילת מפתח, או רצף ארוך שמערב אותיות גדולות, קטנות וספרות — היא סוד, אלא אם נימוקה כתוב במקומה, והנימוק אומר מה הערך הוא · ואפס קובץ מפתח במעקב בכל העץ · וכל קובץ ב-`signing/` מוצהר בשמו · ו-`.gitignore` חוסם את המפתח | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: אפס קובצי מפתח במעקב בכולן — וכל אתר שנותר הוא שם של סוד, מפתח `anon` ציבורי, או טביעה |
| | ח · אנדרואיד ומעטפת | | | | | | | |
| | ↳ מעטפת ⟵ נכסים ⟵ מניפסט ⟵ בנייה | | | | | | | |
| 128 | מעטפת `ShellActivity` | ליבת המעטפת — אותו קוד בכולן, ושורת ה-`package` היא ההבדל · והמעטפת נמדדת בערכה — המניפסט מכריז את הפעילות המשגרת, היא יורשת את `ShellActivity`, המעטפת בונה `WebView` בפועל, ואין בעץ סימן TWA: ארבעת התנאים יחד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 129 | סוג המעטפת | `WebView` מקורי — לעולם לא TWA | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: TWA עדיף טכנית — מנוע Chrome ויכולותיו; אך סינון כשר פליי בטלפון חוסם את תהליכי Chrome, ולכן WebView |
| 130 | מעטפת — ערכת נושא אחידה | `Theme.Material.Light.NoActionBar` בכולם — ובלי מסך מלא: שורת האנדרואיד נשארת גלויה, ומעטפת במסך מלא מסתירה את שעון המכשיר ואת חיווי הרשת — שני הדברים שהמשתמש בודק כשסנכרון אינו מתקדם | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 131 | אין נכסים מוטבעים | ה-APK טוען את הקוד מהרשת — ואינו כולל עותק מקומי | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 132 | מספר קבצים ב-`android/` | 20 קבצים | ⭕ | ✅ | ✅ | ✅ | ✅ | **הבדל מכוון**: 21 ביומן — `file_paths` לגשר השיתוף, ואין ליישר בכוח |
| 133 | נכס בינארי יושב בקובץ, לפי תפקידו | כל נכס בינארי יושב בקובץ, בתיקייה שנגזרת מתפקידו — מאסטר ⟵ `design/` · לוגו של גוף שהיא מציגה ⟵ `logos/` · ואייקון — נוצר ב-`icons/`, ואינו נערך ביד — ולא מוטבע ב-`base64`: הסף 500 תווים, וסימון `svg` קצר הוא רכיב · ושינוי תמונה מקדם את `CACHE_NAME` · וכל הנכסים מאותו סוג נטענים באותה דרך, מנתיב מוצהר · ו-`sw.js` מטמין מראש כל תיקיית נכסים · ותיקיית נכסים שקיימת באחת בלבד נושאת את תפקיד נכסיה | ✅ | ✅ | ✅ | ✅ | ✅ | **נמדד**: אפס נכס מוטבע מעל הסף בכולן — וכל תיקיית נכסים חיה מוטמנת מראש, ואפס קבוע נכס שערכו נקרא מה-DOM |
| 134 | המחולל — הפלטפורמה והאייקונים מהתצורה | כלי אחד — `tools/gen-app.mjs` — יוצר מהתצורה את `manifest.json`, את קובצי האנדרואיד — ושני קובצי ה-Java מהתבניות שב-`tools/java/`, וגשר השיתוף רק כשהוא מוצהר — ואת האייקונים, ו-`tools/gen-icons.mjs` נקרא ממנו · ובראש כל קובץ שנוצר — «נוצר מהתצורה» · וריצה שנייה — אפס שינוי, ו-`--check` מפיל כשהעץ נבדל · וכלי שצריך ערך קורא אותו ב-`--get` — ואינו מחזיק עותק: עותק שני מתיישן | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 135 | אייקונים — מקור אמת יחיד | המחולל מייצר את כל נכסי האייקון מאפס, והרצתו אינה משנה אף אחד מ-16 הנכסים · ונכס אינו נערך ידנית · והמאסטר ב-`design/` הוא המקור, והמחולל קורא אותו ואינו מצייר אותו שוב · ומאסטר נושא צורות בלבד — אפס `<text>` · והצורה מוצהרת ב-`icon.art` שבתצורה — `svg` או רסטרי, ותואמת את סיומת המאסטר · וכל שדה ב-`icon` שנבדל נושא נימוק במקומו, ושדה שאינו רלוונטי מוצהר ריק · והגזירה המלאה עד 3 שניות · ומקור אמת אחד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 136 | המחולל חותך לתיבת התוכן | המחולל חותך את המסכה לתיבת התוכן מיד אחרי הגזירה, ורק אז ממרכז — ולכן מיקום הצורה בקנבס המאסטר אינו משפיע על אף נכס: אין ליישר את המאסטר · והמדידה בסף `ALPHA_MIN` · ובמאסטר האטום הסף הוא מרחק-מרקע מ-`bgKey`: `Math.max` על שלושת הערוצים, חלקי `keyTol` · והזזת המאסטר בעותק זמני אינה מזיזה את תיבת התוכן של אף אחד מ-16 הנכסים · ונגזר ואינו מוקלד | ⭕ | ✅ | ✅ | ⭕ | ⭕ | **הבדל מכוון**: אין מאסטר רסטרי ביומן, בגיוס ובקופה — המאסטר שם גיאומטרי, ואין קנבס שאפשר להזיז בו דבר; וסף ההזזה שנבדק הוא 7/5 פיקסלים — ומרקם הנייר של האריחים נדגם מקואורדינטות מוחלטות, ולכן הם נבדלים בבייט אף שתיבת התוכן אינה זזה |
| 137 | שכבת האייקונים | 10 קובצי mipmap · מסגרת 48–192 ו-108–432 · תוכן 48–192 · רקע תואם · תיבת התוכן במרכז המסגרת, בלי סטייה · וצבע הדיו זהה ב-`tile()` וב-`foreground()`, ומוצהר בתצורה · ולנכס תקרת משקל — וחריגה מוכרזת בשמה ובנימוקה: נכס כבד נמשך בכל טעינה ואיש אינו מודד אותו | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 138 | שוליים שווים בכל נכס | L=R ו-T=B בכל אחד מ-16 הנכסים ובכל האפליקציות — שוליי תיבת התוכן שנמדדה שווים בדיוק בשני הצירים, והפרש של פיקסל אחד הוא הפרה: המסגרת זוגית ושתי צלעות התוכן מכוילות לזוגי, ולכן שני הצירים מתחלקים שווה. ואין למדוד כאן מרכז מסה — הוא תכונה של הציור ולא של המיקום · נמדד בסף `ALPHA_MIN` — סף אחר מזה שהמחולל מייצר בו מדווח חריגה על נכס תקין, או מפספס חריגה אמיתית | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 139 | מסכת המאסטר נקייה מרעש | פיקסל מתחת ל-`ALPHA_MIN` מתאפס — ורעש הנייר אינו צובע את רקע האריח | ⭕ | ✅ | ✅ | ⭕ | ⭕ | **הבדל מכוון**: אין מאסטר רסטרי ביומן, בגיוס ובקופה — המאסטר שם גיאומטרי ולא ציור, ואין רעש נייר שיתאפס |
| 140 | דגימת-יתר במחולל | קבועה וזהה בכולם — כיסוי הפיקסל נדגם ברשת ריבועית, והקבוע יושב בקוד ולא כאן: ערך אחר באחת מהן מזיז את הקצה, ואותו סמל בדיוק יוצא בצלע אחרת | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 141 | הכפלה מוקדמת באלפא | הכפלה מוקדמת בהקטנת נכס עם שקיפות חלקית | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 142 | `manifest.json` | שלושה אייקונים ברשימה שמית: 192 · 512 · maskable-512, ו-`maskable` נפרד · וה-manifest נושא רק אייקוני התקנה — כל אייקון נוסף הוא עוד דרך שבה כרום בוחר אייקון ראשי שאינו נטען · ו-favicon ו-apple-touch יושבים ב-`<link>` · ומטמון ה-service worker אינו שומר תמונה שאינה בממדיה — תחליף של מסנן ננעל שם לנצח · ומקור אמת אחד · ונגזר ואינו מוקלד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 143 | `manifest.json` — שדות זהים | `display` · `orientation` · `lang` · `dir` · ו-`start_url` נושאים ערך זהה בכולן · ו-`id` ו-`scope` ייחודיים לכל אפליקציה — ערך משותף גורם לכרום לראות אותן כאפליקציה אחת: ערך יחסי נפתר לכתובת מוחלטת, ובכולן זה אותו origin | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 144 | האפליקציה ניתנת להתקנה מכרום | חמשת התנאים מתקיימים בכולן — `manifest.json` מקושר ב-`<link rel="manifest">` שבתוך ה-`head` · `sw` נרשם בקוד, ברישום אחד · אייקוני 192 ו-512 ברשימה השמית · `display: standalone` · ואין שדה שמוריד את הדף מ-HTTPS — והשורות שמעל מודדות שדות ושם, ולא את עצם היכולת להתקין: אפליקציה שאינה ניתנת להתקנה עוברת את שתיהן | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 145 | גשר שיתוף | `createChooser`, בלי `NEW_TASK` · וכל מוסד מקבל נקודת שיתוף משלו | ✅ | ⭕ | ⭕ | ⭕ | ⭕ | **הבדל מכוון**: רק יומן מייצאת קובץ החוצה |
| 146 | `workflows` | שלושה: `build-apk` · `cleanup-merged-branches` · `guard` · ו-`cleanup-merged-branches` נושא שני קווי הגנה — תחילית `claude/` קשיחה, ו-`git merge-base --is-ancestor` מול `main` — ו-`main` לעולם אינו מועמד למחיקה · ו-`build-apk` נבנה מ-`main` בלבד, משינוי במעטפת ובחתימה · ומסלול החתימה אחד — הסקריפט המשותף | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 147 | כל `uses:` בזרימה נושא גרסה נתמכת | כל `uses:` בזרימה נעול לגרסה ראשית שהספק תומך בה — `actions/checkout` · `actions/setup-java` · `actions/upload-artifact` · `android-actions/setup-android`, והרצפה היא הגרסה הראשית הנתמכת: גרסה שהספק הוציא משימוש מדפיסה אזהרה בכל ריצה, וביום שהיא יורדת הבנייה נשברת בלי שאיש שינה קוד · ואין `uses:` בלי גרסה | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 148 | `sign-apk.sh` — חתימה | חותם מול טביעת המפתח שבתצורה · ושם קובץ פלט אחד לכולן — `app-signed.apk` · ונתיב המפתח והסיסמה מהסביבה, ונכשלים ברעש כשהם חסרים · וה-alias נגזר מהמפתח · וערך שמגיע מסוד מנורמל לפני שהוא נצרך — רווח ותווי סוף שורה מוסרים — ומה שאינו באלפבית עדיין נדחה · ומקור אמת אחד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 149 | `versionCode` | מקודם בכל שינוי בקובץ שנכנס ל-APK — ולא בתיעוד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 150 | מפתח חתימה קבוע | לעולם לא keystore חדש, וזהותו נמדדת מול טביעת המפתח שבתצורה — והקובץ עצמו אינו בריפו: הוא ב-GitHub Secrets ונמשך בזמן בנייה · וההצהרה היא שש-עשרה ספרות הקס ולא מציין-מקום, ועותק מקומי, כשהוא קיים, מושווה אליה — ואין שניים בתיקייה | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| | ט · פרסום ועדכון | | | | | | | |
| | ↳ שומר ⟵ בדיקה ⟵ באנר ⟵ ליבה ⟵ מטמון | | | | | | | |
| 151 | שומר הדחיפה — תחביר ומטמון | כל דחיפה עוברת בשומר אחד — `tools/guard.mjs`: תחביר הסקריפט הראשי, `sw.js` ו-`core/` · וכל קובץ ברשימת המטמון קיים · ו-`CACHE_NAME` מוגדר ב-`sw.js` · ורק מה ששובר את האפליקציה למשתמש ברגע הדחיפה · ומקור אמת אחד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 152 | עדכון אוטומטי — בדיקה מחזורית | מכשיר מותקן בודק מעצמו ובמחזוריות אם יצאה גרסה — ב-`reg.update()` בלבד, עם `updateViaCache: 'none'` · ו-`skipWaiting` דלוק בכולן, ו-`controllerchange` — מטפל אחד — מרענן בפועל, פרט להשתלטות ראשונה · ונפילה-חזרה בטיימר מוצהרת בשמה, 10 שניות ומעלה, ומבוטלת בהשתלטות · ובאנר שנדחה אינו חוזר לאותה גרסה — הסימן נושא את שם המטמון החי · והמטמון מסונן לפי קידומת המטמון · ומקומו — `core/ui.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 153 | באנר עדכון `sw` | «גרסה חדשה זמינה», ובדיקה תקופתית · ותשובת מסד שמעידה על סכימה ישנה מציגה את הבאנר מיד — טבלה שאינה קיימת (`42P01`) · עמודה שאינה קיימת (`42703`) · ו-404 על טבלה · ולחיצה בו — מפעילה את העובד הממתין, ומרעננת כשהעובד החדש כבר שולט · ויושב במקום הטוסט — `--toast-bottom` — וטוסט שמופיע בזמן שהוא פתוח עולה מעליו · והרישום — מ-`core/ui.js` בלבד, בטעינה · ומקומו — `core/ui.js` | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 154 | מטמון ה-service worker — שלו בלבד | כל חיפוש הוא במטמון של האפליקציה — ה-origin משותף, ו-`caches.match()` מחפש בכולם · ומטמון שכל מפתחותיו תחת ה-scope ושמו אינו הנוכחי נמחק בהפעלה — לפי תוכנו ולא לפי שמו · ושם קובץ אייקון נושא את תוכנו · והליבה ב-`core/sw.js`, ומטמינה מראש את ה-CDN ואת `app.css` · והדף והקוד מאותו מטמון · וקובצי `CORE` אינם מתרעננים ברקע — גרסה חדשה נכנסת רק בהתקנה של worker חדש, ומשתלטת בריענון אחד · ומקור אמת אחד · ונגזר ואינו מוקלד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 155 | `sw.js` של האפליקציה — שלד | חמישה רכיבים, בסדרם: `importScripts('./app.config.js')` · `CACHE_NAME` · `CORE` · `CDN_ASSETS` · `importScripts('./core/sw.js')` — ואין בו לוגיקה: ההתנהגות בליבה, וערכי האפליקציה בתצורה · ודף האופליין שהליבה מגישה — בבהיר ובכהה, בצבעי `offline` שבתצורה: דף בהיר במכשיר כהה מסנוור | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 156 | קידום המטמון | שינוי בקובץ שמוטמן מראש מקדם את `CACHE_NAME` באותו שינוי — בלעדיו המכשיר המותקן מגיש את הישן · והרשימה נגזרת מ-`CORE` שב-`sw.js` — כולל `core/`, המניפסט והאייקונים · וגרסת האפליקציה נקבעת ב-`CACHE_NAME` בלבד: המספר שבבאנר נגזר משם המטמון החי, ואין לו ליטרל שני ב-`index.html` · ונגזר ואינו מוקלד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
| 157 | התקנה טרייה עובדת | האפליקציה נפתחת ושמישה מול מסד ריק — בלי הזנה ידנית: מה שדורש זריעה ידנית אינו ניתן להתקנה, ואיש לא יגלה עד ההתקנה הבאה · וערך חסר הוא אפס, ולא כשל · והמדידה בשימוש בפועל — המסך הראשון נושא תוכן, ולא שלד | ✅ | ✅ | ✅ | ✅ | ✅ |  |
<!-- SHARED:end -->

## מסכים ולוגיקה — יומן עבודה

- **מסכים:** יומן יומי · ארכיון עם עריכה inline · ייצוא PDF ושיתוף הדוח
  כתמונה (JPEG דרך `_buildReportDiv`/`_renderReport`) · הגדרות.
- **שני מוסדות** — ראשון לציון ורמת אביב. ההפרדה **במכשיר** היא סיומת
  למפתח (`_rishon` / `_ramataviv`), ובענן היא **עמודת `yeshiva`** ולא טבלה
  לכל מוסד: ⛔ מוסד שלישי דורש אפס DDL.
- **החלפת מוסד בזמן ריצה** — לחיצה על הלוגו פותחת בורר ב-`openModal`,
  אישור ב-`ask`, ומעבר בלי טעינה מחדש. ⛔ **`yaResetTenantState` רצה לפני
  `selectYeshiva`** — ⚠️ הרשומות נטענות מחדש לפי הסיומת, ⛔ אבל עֵד הדחיפה
  ואות הפולינג הם זיכרון בלי סיומת: ⭐ אות שנשאר גורם למשיכה הראשונה של
  המוסד החדש לחשוב שאין מה למשוך. ⚠️ ו-`selectYeshiva` מקבלת `again`
  ⛔ שמדלגת על `pendBoot` בלבד — ⭐ שאר ה-`boot` שומרים על עצמם.
- **סנכרון:** פולינג כל 3 שניות, ומיזוג ברמת רשומה.

### מנוע המיזוג
`mergeRecords(local, remote, getKey, mergePair)` — הענן הוא בסיס הסדר
ומנצח בשוויון. נגזרות: `mergeEntries` (לפי `id`), `mergeArchive` (סנאפשוט
לפי `gdate`, ובתוכו מיזוג פר-רשומה), `mergeCats` (לפי `letter`),
`mergeSubs` (פר-מפתח לפי `SUBS_META`).
- ⛔ **אין stamping של `Date.now()` בטעינה** — רשומה בלי
  `updatedAt` נחשבת ts=0 אך לעולם אינה נופלת רק מפני שהצד השני אינו
  מכיר אותה; stamping היה גורם למכשיר ישן לנצח נתונים חדשים.
- **«סיום יום»:** העותקים שנכנסים לארכיון וה-tombstones של החיים מקבלים
  **אותה חותמת**, ובשוויון הסנאפשוט מנצח — ⛔ ולכן ארכוב אינו מחיקה
  מהארכיון.
- **גריעת tombstones:** `prunePastTombstones` + `TOMBSTONE_TTL_MS`
  (90 יום). ⛔ רק `deleted:true` עם `updatedAt` **מספרי** נגרע —
  tombstone בלי חותמת מקבל ts=0, וגריעה לפיו הייתה מוחקת את הישנים ביותר.
  ⛔ והגריעה רצה על **התוצאה הממוזגת** במיזוג הראשון שאחרי העלייה —
  גריעה מקומית בלבד הייתה מוחזרת מהענן תוך שניות.
- **מחיר מודע:** מכשיר שנותק מעל 90 יום יחזיר רשומה מחוקה **פעם אחת**.

### הייצוג בענן
- **`ya_entries` היא הטבלה המאוחדת** — היומן החי והארכיון יחד, עם דגל
  `archived`. «העברה לארכיון» היא דגל על השורה ולא העברה בין טבלאות.
  ⛔ `yaRowsGet` מסננת `.eq('archived', …)` עם `order('rec_key')` **וגם**
  `yaSortRows` בקוד — בלי `ORDER BY` פוסטגרס אינו מבטיח סדר,
  ומיון לקסיקוגרפי של `rec_key` אינו סדר תאריכים.
- ⭐ **`client_id` נגזר ממפתח המיזוג** (`entryKey`/`archiveKey`) ואינו
  uuid חדש — לרשומה כבר יש זהות שנוצרה במכשיר, ומזהה שני היה מאפשר לה
  להתקיים פעמיים.
- ⭐ **הסנאפשוט הוא יחידת הארכיון**, ובתוכו כבר רץ מיזוג פר-רשומה.
  ⛔ פירוקו לרשומות הוא מנוע מיזוג אחר — סבב ייעודי בהחלטת
  המנהל, ⛔ לא תופעת לוואי.
- ⭐ **בונה סנאפשוט אחד — `arcPutSnapshot` — לשני מסלולי היצירה**:
  `autoArchiveDay` ו-`checkDayChange`; ⛔ אחרת מסלול אחד ממלא שדה שהשני
  משמיט. `snapHDate` טהורה ו⛔ אינה כותבת לסנאפשוט.
- **ההגדרות — טבלה לכל ישיבה**: `ya_settings_rishon` / `ya_settings_ramataviv`,
  ⛔ והבעלות כאן בלבד — ⭐ הבית הענני של `ya_cats` · `ya_subs` · `ya_subs_meta`
  ואות הפולינג `ya_last_changed`.

### אחסון מקומי — מה שנבדל כאן
- ⛔ **`_lastKnownTimestamp` אינו עֵד סנכרון ואין להשתמש בו לפינוי
  ** — הוא מתעדכן גם ב**משיכה**, ופינוי שנשען עליו היה מוחק
  מהדיסק רשומה שמעולם לא הגיעה לענן. העֵד הוא **`_yaPushedAt[key]`**,
  שנכתב רק אחרי `sbSet` שהחזירה `ok`.
- **`LS_CFG.oldRecords` נבנית ב-`lsRebuildPolicy()`** ונקראת מ-`selectYeshiva`:
  הפינוי חל על **המוסד הפעיל בלבד** — ⛔ לעֵד של מוסד אחד אין תוקף לגבי
  השני. ⛔ והחלון נגזר מסוג האפליקציה — שנתית, ⚠️ ואין מספר ימים ברשומה.
- `_yaRecTs` קוראת `updatedAt`, ובהיעדרו גוזרת מ-`gdate` — שנשמר בצורה
  של `gregDateStr`, `"25 נובמבר 2025"`, ולא ISO — ⛔ עם עוגן בצהריים מקומיים.
  ⛔ **וזו הצורה היחידה** — ⚠️ הקלט בשדה התאריך מתורגם אליה לפני שהוא נשמר.
- ⚠️ **yoman היא הצרכנית הגדולה בדומיין**, ולכן שלב ב חל כאן
  בפועל.

### קידום גרסה כאן — ⛔ מזהה אחד
⛔ **`CACHE_NAME` ב-`sw.js` הוא מזהה הגרסה היחיד** — ⛔ ואין מנגנון
זיהוי שני, לא `<meta name="app-version">` ולא משיכה מ-`raw.githubusercontent`:
⚠️ שני מנגנונים הם שני מקורות אמת לאותה שאלה.
⛔ **ו-`RAW_BASE` נשאר** — ⚠️ מציג טבלת התשתית הוא הצרכן היחיד שלו.
