# יומן עבודה — CLAUDE.md

עודכן לאחרונה: סבב 40 · 2026-08-19

## סביבת עבודה
- **ריפו:** `ygtotlrl-lab/yoman-avoda`
- **Pages:** `https://ygtotlrl-lab.github.io/yoman-avoda/`
- **קובץ ראשי:** `index.html`
- **Supabase:** `kxbtskqobynewvnckaaz`
- **אופן העבודה (נכון ליולי 2026):** העבודה מתנהלת בסשני ענן (Claude Code cloud) — הריפו משוכפל טרי בתחילת כל סשן, העבודה נעשית על ענף ייעודי, והדחיפה בסוף הסשן. **אין עותקים מקומיים קבועים.**
- **עבודה מקומית במחשב (אם בכל זאת):** שכפול **אך ורק** לתיקייה יציבה כמו `C:\Users\F\Documents\repos\` — **לעולם לא ל-Temp** (מנקה Windows / Storage Sense מוחק משם קבצים באמצע עבודה; זה גרם ל"מחיקות רפאים" חוזרות). טוקן: מאוחסן ב-Windows Credential Manager (host `github.com`) — לעולם לא בקובץ; `git push`/`clone` מושכים אותו אוטומטית דרך GCM.

## גישת Supabase
כשזמין ה-Supabase MCP, נהג לפי הכללים הבאים — ללא יוצאים מן הכלל:
- **שינויי סכימה** (יצירה/שינוי/מחיקת טבלאות, עמודות, פוליסות, הרשאות) — **אך ורק דרך `apply_migration`** עם שם ברור ותיאורי (למשל `add_sync_log_table`). לא דרך `execute_sql`.
- **שאילתות אבחון וקריאה** (SELECT, בדיקת מבנה, ספירות, `list_tables` וכו') — **חופשיות**, ללא אישור.
- **עדכון או מחיקת נתונים בטבלאות `kv`** (כאן: `kv_rishon` / `kv_ramataviv`, וכל טבלת נתונים אחרת) — **מחייבים אישור מפורש מהמשתמש לפני הרצה**. אין להריץ `UPDATE`/`DELETE`/`upsert` על נתונים בלי אישור.


<!-- SHARED:start id="branch-rules" -->
## כללי ענפים
- כל עבודה נעשית על **ענף ייעודי** — לא ישירות על `main`.
- **מיזוג ל-`main` רק לאחר אישור מפורש מהמשתמש**, אחרי שבדק את השינוי בבדיקה חיצונית.
- **מחיקת ענפים מרוחקים חסומה בסביבת הענן** — אין לנסות למחוק; לדלג ולדווח למשתמש שהענף נותר.
- **ענפים ממוזגים נמחקים אוטומטית אחרי מיזוג** (`.github/workflows/cleanup-merged-branches.yml`, רץ ב-push ל-`main`); ההגנה — prefix `claude/` + בדיקת `merge-base --is-ancestor`.

### ⚠️ שני כללי תפעול (סבב 21) — שניהם נלמדו מכשל אמיתי בסבב 20

**1. לפני כל מיזוג — מרעננים את `main` המקומי, ורק אז ממזגים.**
```bash
git -C <path> fetch origin
git -C <path> checkout main
git -C <path> merge --ff-only origin/main   # חייב להצליח
git -C <path> merge <ענף-העבודה>
```
⛔ **אין למזג מעל `main` שאינו זהה ל-`origin/main`.** סשן ענן מחזיק עותק
שנוצר ברגע פתיחתו והוא **קופא שם**: כל מה שנדחף ל-`origin/main` מאז — מסבב
מקביל, מסשן אחר, או מהמשתמש — פשוט אינו קיים בעותק הזה. בסבב 20 נמצא
`main` מקומי שפיגר **בשני סבבים שלמים**. `--ff-only` הוא מה שהופך את זה
לגלוי: הוא **נכשל** במקום ליצור קומיט מיזוג שקט שמחזיר את הקוד אחורה.

**2. בעבודה רב-ריפו — כל פקודת git מציינת את הריפו במפורש: `git -C <path> …`.**
בסבב 20 פקודת מיזוג רצה בתיקייה של ריפו **אחר**, בגלל משתנה לולאה שדלף
מבלוק קודם. שם היא הייתה no-op **במקרה** — הענף לא היה קיים באותו ריפו.
⚠️ **וזה בדיוק מה שלא ניתן לסמוך עליו כאן:** שם ענף העבודה **זהה בארבעת
הריפו** (אותה תבנית `claude/<נושא>-<סיומת>`), ולכן פקודה שרצה בתיקייה
הלא-נכונה תמצא שם ענף תקף ותמזג אותו — **מיזוג שקט בריפו הלא נכון**, בלי
שגיאה ובלי סימן. ⛔ **אין להסתמך על `cd` ואין להסתמך על תיקיית העבודה
הנוכחית** — הנתיב נכתב בפקודה עצמה.
<!-- SHARED:end -->

## לפני כל push — חובה: שער אחד, `node tools/check-js.mjs`
**כל שינוי בקוד (`index.html` או `sw.js`) חייב לעבור את השער הזה לפני דחיפה.
דחיפה ללא הבדיקה — אסורה.**

```bash
node tools/check-js.mjs
```

⭐ **מסבב 33 זו פקודה אחת ולא רשימה.** השער מחלץ את כל ה-JS המוטבע
מ-`index.html`, מריץ עליו `node --check` ועל `sw.js`, מאמת את כללי-האמת
שפרסר אינו תופס (תבנית `CACHE_NAME`, דילוג על supabase, נעילת גרסת ה-CDN),
ואז מריץ **את כל שערי האחידות ואת כל חבילות בדיקות הסבבים** — כך שאין דרך
לדחוף בלי שהם רצו.
⚠️ בדיקת איזון-סוגריים בלבד אינה מספיקה (עיוורת לשגיאות בתוך מחרוזות);
`node --check` = parse מלא אמיתי ב-V8. node זמין בסביבת הענן.

**מה שהשער מריץ, בסדר הזה:**
- **`check-structure.mjs`** — המבנה הקנוני (סבב 33, כלל ברזל 14).
- **`check-status-area.mjs`** — אחידות «אזור מצב» (סבב 17, כלל ברזל 7).
- **`check-docs.mjs`** — אחידות התיעוד (סבב 18, כלל ברזל 8).
- **`check-comments.mjs`** — תקן ההערות בקוד (סבב 28, כלל ברזל 11).
- **`check-capabilities.mjs`** — אחידות היכולות המשותפות (סבב 30 השלמה, כלל ברזל 12).
- **`check-gaps.mjs`** — אכיפת פרק «פערים פתוחים» (סבב 39, כלל ברזל 15).
- **`test_round30_stage_a.mjs`** — גיבוי יומי ויומן פעולות (מודול משותף).
- **`test_round30_stage_b.mjs`** — שכבת השורות בענן: שקילות ומיזוג.
- **`test_round31_archive.mjs`** — תצוגת הארכיון ומקור הרשומות האוטומטיות.
- **`test_round32_unify.mjs`** — איחוד הארכיון: טבלה אחת ודגל `archived`.
- **`test_round33_structure.mjs`** — הנעילה המבנית ושער ה-JS, עם מוטציות.
- **`test_round35_hotwin.mjs`** — מודול החלון החם ויכולות סבב 35, עם מוטציות.
- **`test_round35c_cron.mjs`** — פינוי הגיבויים במסד (pg_cron), עם מוטציות.
- **`test_round37_merge_pending.mjs`** — הגנת ה-⏳ במנוע המיזוג וחסימת
  משתמש מושבת בכניסה אופליין, עם מוטציות.
- **`test_round37_matrix.mjs`** — אכיפת מטריצת היכולות: היפוך כל תא מכוסה
  חייב להפיל את `check-capabilities`.
- **`test_round38_merge_core.mjs`** — ליבת המיזוג המשותפת: כלל ההכרעה
  מופיע פעם אחת, שלושת כללי ברזל 6, וידיות המדיניות — עם חמש מוטציות.
- **`test_round38_ids_yoman.mjs`** — מזהי היומן: דו-קיום של מזהה מספרי
  ישן ו-uuid חדש, עם ארבע מוטציות. ⚠️ פרטי כאן.
- **`test_round39_gaps.mjs`** — אכיפת פרק «פערים פתוחים»: פער שנסגר מפיל
  את השער, ושורה `לא-מדיד` עוברת — עם שמונה מוטציות.
- **`test_round39_md.mjs`** — שלד שלושת קובצי ה-md הנלווים (`README.md` ·
  `CONTEXT.md` · `android/README.md`), עם שלוש מוטציות.

אם `node --check` מדווח שגיאה — אסור לדחוף עד שהיא מתוקנת.
אם `check-structure.mjs` נכשל — ר' הפרק «כלל ברזל 14» למטה. ⛔ קובץ שורש
חדש או תיקייה חדשה מחייבים שורה **מנומקת** ברשימת-ההיתר, בארבעת עותקי
הבדיקה — «קיים רק כאן, בשקט» אינו מצב חוקי.
אם `check-status-area.mjs` נכשל — ר' הפרק «כלל ברזל 7» למטה. שינוי בבלוק
משותף מחייב עדכון בארבע האפליקציות ובארבעת עותקי קובץ הבדיקה, באותו סבב.
אם `check-docs.mjs` נכשל — ר' הפרק «כלל ברזל 8» למטה. אותו כלל חל על פרק
תיעוד מסומן `SHARED`: משנים בארבעתם ובארבעת עותקי הבדיקה, באותו סבב.
אם `check-comments.mjs` נכשל — ר' הפרק «כלל ברזל 11» למטה. ⛔ התקן חל על
הקוד הפרטי בלבד; הבלוקים המשותפים והמודולים הקפואים מוחרגים ממנו במפורש.
אם `check-capabilities.mjs` נכשל — ר' הפרק «כלל ברזל 12» למטה. ⛔ נקודת
ההפעלה של יכולות העלייה היא **פונקציית העלייה של האפליקציה** — זו שקוראת
גם ל-`lsBoot()` וגם ל-`pendBoot()` — ואין להזיז אותה למסלול אחר.

## עדכוני Service Worker
- **כל שינוי בקוד מחייב קידום `CACHE_NAME` ב-`sw.js` לגרסה הבאה** — בלי זה המשתמשים לא יקבלו את העדכון.
- **וגם קידום `<meta name="app-version">` בראש `index.html`** — זה מזהה הגרסה שמנגנון האוטו-אפדייט משווה מולו (החליף את `simpleHash`, שרץ על 5000 התווים הראשונים ולכן תמיד החזיר אותו ערך והבאנר לא הופיע לעולם). בלי קידום — הבאנר לא יופיע.
- מנגנון באנר "גרסה חדשה זמינה" קיים באפליקציה — המשתמשים מקבלים את העדכון בלחיצה על הבאנר.
- בנוסף קיים מנגנון אוטו-אפדייט מבוסס `raw.githubusercontent` (`GITHUB_FILE`/`RAW_URL` ב-`index.html`) עבור מכשירים עם ה-APK — ראה כלל קריטי 5. הבדיקה רצה **פעם בשעה**, עם `Range: bytes=0-4095` + `cache:'no-store'` + timeout, ו-`sw.js` לא שומר אותה במטמון (קודם: משיכת ~950KB כל 5 דקות, כל אחת נשמרה במטמון תחת `?t=` ייחודי).
- ⛔ **אין לקדם גרסה בלי שינוי בקוד הרץ (סבב 26 השלמה) — קידום סרק שולח
  באנר «גרסה חדשה זמינה» לכל המשתמשים לשום שינוי.** ⚠️ שינוי הערות נרחב
  ב-`index.html` הוא כן שינוי שמקודם (תקדים סבב 28); הערה בודדת — לא.

## Push
```bash
git add . && git commit -m "תיאור השינוי"
git push -u origin <שם-הענף>   # דחיפה לענף העבודה — לא ל-main
```

## סיום משימה
בסיום כל משימה משמעותית — **עדכן קובץ זה בתמצית** לפני סיום הסשן: מה שונה, מה הוחלט. כך הסשן הבא מתחיל עם תמונת מצב עדכנית.

## כללים קריטיים
1. **בדיקת תחביר עם `node --check` לפני כל push** (הסעיף למעלה) — חובה מוחלטת. כל שינוי ב-`index.html` חייב לעבור חילוץ-JS + `node --check` (וגם `sw.js`). דחיפה בלי זה — אסורה.
2. **קידום `CACHE_NAME` ב-`sw.js` וגם `<meta name="app-version">` ב-`index.html` בכל שינוי קוד** (הסעיף למעלה) — בלי זה העדכון לא מגיע למשתמשים.
3. **smali בלבד** לתיקון URLs ב-APK
4. **cache APK** — תמיד `TS=$(date +%s)` בשם
5. **מקור אמת יחיד = `index.html`** — זה הקובץ ש-Pages מגיש, שאליו מצביע `start_url` במניפסט, ושאותו **מעטפת ה-APK טוענת מהרשת** (מגרסה 2 — ר' הפרק "APK"). כל עדכון קוד נכנס לכאן בלבד. **אסור ליצור קבצי HTML כפולים** (בעבר היה `יומן עבודה.html` — שונה ל-`index.html`). ⛔ **ואסור להחזיר עותק מוטבע ל-`android/app/src/main/assets/`** — עותק `file://` הוא מקור אמת שני *ובנוסף* origin אחסון נפרד, כלומר localStorage אחר. **`GITHUB_FILE` במנגנון האוטו-אפדייט חייב להתאים לשם הקובץ האמיתי בריפו** (`index.html`) — אם הוא מצביע על שם אחר, `RAW_URL` מקבל 404, האפליקציה לא מזהה גרסה חדשה, ועדכונים לא מגיעים למכשירים מותקנים.
6. **חתימת APK: רק עם `signing/yoman.keystore` (alias `yoman`, pass `yoman123`)** — המפתח הקבוע. לעולם לא ליצור keystore חדש, אחרת APK עתידי לא יתקין מעל הקיים.
7. **המעטפת היא WebView מקורי (`android/`) — לעולם לא TWA ולא PWABuilder.** TWA מריץ את האתר בתוך כרום, וסינון התוכן במכשירים של המשתמשים חוסם את כרום. ר' הפרק "APK — מעטפת WebView מקורית".

## חתימת APK — מפתח קבוע (לעולם לא משתנה!)
- **Keystore בריפו:** `signing/yoman.keystore` (PKCS12, RSA 2048, תקף עד 2053)
- **alias:** `yoman`
- **storepass / keypass:** `KEYSTORE_PASS_IN_MEMORY` = `yoman123` (זהה לשניהם)
- **SHA256:** `29:F5:0B:29:60:79:0B:77:28:25:7C:88:79:12:31:28:7A:B8:F1:D9:3E:90:B6:3B:50:F4:1E:41:B9:FA:F8:B5`
- **קריטי:** כל APK חדש נחתם **אך ורק** עם המפתח הזה כדי שיתקין מעל הקיים בלי הסרה. לעולם לא ליצור keystore חדש.
- חתימה (כשיש Android SDK / apksigner):
  ```bash
  apksigner sign --ks signing/yoman.keystore --ks-key-alias yoman \
    --ks-pass pass:yoman123 --key-pass pass:yoman123 app.apk
  ```
- חלופת jarsigner (אם אין apksigner):
  ```bash
  jarsigner -keystore signing/yoman.keystore -storepass yoman123 -keypass yoman123 app.apk yoman
  ```
- **שים לב:** המפתח הקודם (`/tmp/yoman.keystore`) אבד; המפתח הקבוע החדש מחליף אותו. ההתקנה הראשונה של APK חתום במפתח החדש דורשת **הסרה חד-פעמית** של האפליקציה הישנה (אי-התאמת חתימה); מאז — קבוע לתמיד.

## APK — מעטפת WebView מקורית (⛔ לא TWA)
המעטפת היא פרויקט אנדרואיד מקורי ב-`android/` — `Activity` יחידה עם `WebView`
(`com.yoman.avoda.MainActivity`). תיעוד מלא ב-`android/README.md`.

### ⛔ אין להחליף ל-TWA ואין לבנות מחדש ב-PWABuilder
TWA (Trusted Web Activity) אינו רכיב עצמאי — הוא מריץ את האתר **בתוך כרום**
ורק מסתיר את שורת הכתובת. **סינון התוכן שמותקן במכשירים של המשתמשים חוסם את
כרום**, ואיתו נופל כל TWA. `WebView` הוא מנוע רינדור שרץ בתוך תהליך האפליקציה
ואינו עובר דרך כרום, ולכן הסינון לא נוגע בו.

זה לא תיאורטי: gius נבנתה ב-PWABuilder כ-TWA ופשוט **לא נפתחה** אצל המשתמשים,
בעוד yoman ו-hanhala (שתיהן WebView) עובדות. גם gius הומרה מאז ל-WebView.
**PWABuilder יודע לייצר רק TWA** — הבנייה היא מ-`android/`, דרך
`.github/workflows/build-apk.yml` או Gradle מקומי.

> תיעוד קודם כאן המליץ על "TWA דרך PWABuilder" בשביל `navigator.share({files})`.
> **ההמלצה הזו שגויה ובוטלה** — היא הייתה מחליפה מעטפת עובדת במעטפת חסומה.
> השיתוף פתור ממילא בגשר מקורי, ולא דרך `navigator.share`.

### פרטי המעטפת (⭐ versionCode 2 — טוענת מהרשת)
- אייקון: `res/mipmap-*/ic_launcher.png` (לוח משימות + גרף, מ-`icons/icon-512.png`)
- **מה המעטפת טוענת:** `https://ygtotlrl-lab.github.io/yoman-avoda/` — **מהרשת**,
  בדפוס של gius. **שחרור קוד web כבר לא מצריך APK חדש**: `location.reload()`
  על דף https מביא באמת את הקוד החדש, ומנגנון האוטו-אפדייט + ה-service worker
  עובדים בדיוק כמו בדפדפן. APK חדש נדרש רק כששינוי נוגע במעטפת עצמה.
- ⛔ **אין נכסים מוטבעים, ואין להחזיר אותם (סבב 13)** — עותק `file://` הוא
  origin אחסון נפרד (localStorage אחר = אובדן נתונים שקט) וגם מקור אמת שני.
- **ניווט:** כל `http`/`https` **נשאר בתוך ה-WebView** — הפניה לדפדפן חיצוני
  הייתה מגיעה לכרום, שהסינון במכשירי המשתמשים חוסם. שאר הסכימות (`tel:`,
  `whatsapp:`, …) נמסרות למערכת, וזה מה שמפעיל את מסלול הנפילה-חזרה של השיתוף.
- **שיתוף תמונות — גשר מוגבל לדומיין.** ר' הפרק «הגשר המקורי» למטה.
- בורר קבצים: `WebChromeClient.onShowFileChooser` מחובר ל-`<input type=file>`.
- תיקוני URL ב-APK **קיים ובנוי** (בלי מקור): smali בלבד, לא binary patch.


### הגשר המקורי לשיתוף — מוגבל לדומיין שלנו (⛔ הנקודה הרגישה, סבב 13)
`addJavascriptInterface` מזריק את האובייקט ל**כל frame של כל דף**, בלי שום
מושג של origin — ומרגע שהמעטפת טוענת מהרשת, הגשר נוסע עם כל דף שהיא
עשויה להימצא בו. הגשר נדרש כאן לשיתוף דוחות כתמונה, ולכן הוא נשאר — מוגבל:
- **מסלול מועדף — `WebViewCompat.addWebMessageListener`** עם
  `allowedOriginRules = {"https://ygtotlrl-lab.github.io"}`. WebView עצמו
  אוכף את רשימת המקורות, פר-frame. שם האובייקט: `AndroidShareBridge`,
  והמטען JSON (`{data, mime, pkg}`) דרך `postMessage`. דורש WebView 88+.
- **נפילה-חזרה — `addJavascriptInterface` הישן** (`AndroidShare`), למכשירים
  ישנים. **נעול פעמיים:** מחובר רק כשהמעטפת על הדומיין שלנו ומנותק כשהיא
  מנווטת משם (`syncLegacyBridge`), וכל קריאה מאמתת מחדש את מקור הדף על
  ה-UI thread.
- ⛔ `_androidShareImage` ב-`index.html` מנסה את המאובטח **קודם** (סבב 13) —
  היפוך הסדר הוא ויתור על אכיפה שהפלטפורמה נותנת בחינם.

## בנייה — דורש כלים שאינם זמינים בכל סביבה
לבנייה מקומית צריך Android SDK (`aapt2`, `d8`, `apksigner`/`zipalign`).
בסביבת הענן הם אינם מותקנים, ו-`dl.google.com` חסום — לכן הדרך המעשית היא
**`.github/workflows/build-apk.yml`**: Actions → Build Signed APK → Run workflow.
ה-workflow בונה וחותם עם `signing/yoman.keystore`. ⛔ **שלב "copy web assets"
הוסר ואין להחזיר אותו** — המעטפת טוענת מהרשת.
**לא להשתמש ב-PWABuilder** — הוא מייצר TWA (ר' האזהרה למעלה).


## מצב נוכחי
- ייצוא PDF ושיתוף וואטסאפ (השיתוף מייצר JPEG דרך `_buildReportDiv`/`_renderReport`;
  אין כפתור "הורד JPEG" עצמאי — `exportJPEG` הוסרה כקוד מת בסבב 7)
- ארכיב ועריכה inline; Hebrew calendar עם leap year
- Supabase sync (polling 3 שניות) — מיזוג ברמת רשומה, ר' «מנוע המיזוג» למטה
- הייצוג בענן: טבלאות מובנות (`tb_entries` מאוחדת עם דגל `archived`);
  הכתיבה הכפולה למפתחות ה-`kv` ול-`tb_archive` **כובתה בסבב 35** —
  ר' «הייצוג בענן» למטה
- PWA מותקן (`sw.js`, `CACHE_NAME` נוכחי: `yoman-avoda-v28`; `app-version`
  נוכחי: `22-2026-08-18-1`)

## מוסכמת הודעות קומיט (סבב 8 — משותפת לשלושת הפרויקטים)
**עברית חופשית**, לא conventional-commits. זה הדפוס שהתגבש בפועל בשלושת הריפו,
ומעכשיו הוא ההחלטה המוצהרת:
- שורה ראשונה: תיאור תמציתי בעברית של מה השתנה (עד ~72 תווים).
- עבודה מרוכזת מקבלת קידומת `סבב N:` (למשל `סבב 8: אחידות בין שלושת הפרויקטים`).
- בלי קידומות `feat:`/`fix:` באנגלית.


## מנוע המיזוג — מצב נוכחי (סבב 3ב, גריעת tombstones בסבב 7)
**הכלל היחיד שחשוב:** היעדר רשומה אצל מכשיר אינו מחיקה. מחיקה =
`deleted:true` + `updatedAt` (tombstone).
- **המנוע:** `mergeRecords(local, remote, getKey, mergePair)` — הענן הוא
  בסיס הסדר ומנצח בשוויון (דטרמיניסטי). נגזרות: `mergeEntries` (לפי `id`),
  `mergeArchive` (סנאפשוט לפי `gdate`, ובתוכו מיזוג רשומות לפי `id`),
  `mergeCats` (לפי `letter`), `mergeSubs` (פר-מפתח לפי `SUBS_META` — מפה
  `{מפתח: updatedAt}` ב-`tb_subs_meta`). עזרים: `recTs`/`recTouch`/
  `recDelete`/`isLive`/`liveOnly`.
- `autoSyncData` ו-`syncFromCloud` **ממזגים** ולא מחליפים blob — החלפה
  שלמה הייתה מקור אובדן הנתונים המקורי.
- ⛔ **אין stamping של `Date.now()` בטעינה (סבב 3ב)** — רשומה בלי
  `updatedAt` נחשבת ts=0 («ותיקה») אך לעולם לא נופלת רק כי הצד השני לא
  מכיר אותה; stamping היה גורם למכשיר ישן לנצח נתונים חדשים.
- **«סיום יום» (`autoArchiveDay`):** העותקים שנכנסים לארכיון וה-tombstones
  של החיים מקבלים **אותה חותמת**; בשוויון הסנאפשוט מנצח, ולכן ארכוב אינו
  מחיקה מהארכיון. משימות ותתי-משימות הן מחרוזות בלי מזהה — נוסעות עם
  רשומת האב וכל שינוי מקדם את ה-`updatedAt` שלו.
- **גריעת tombstones:** `prunePastTombstones(arr, now)` + `TOMBSTONE_TTL_MS`
  (90 יום). רק `deleted:true` עם `updatedAt` **מספרי** נגרע — tombstone
  בלי חותמת נשאר (recTs מחזיר לו 0, וגריעה לפי 0 הייתה מוחקת דווקא את
  הישנים ביותר). ⛔ הדגל `_tombPrunePending` אינו קישוט (סבב 7): הגריעה
  חייבת לרוץ על **התוצאה הממוזגת** במיזוג הראשון אחרי העלייה — גריעה
  מקומית בלבד הייתה מוחזרת מהענן תוך שניות. לא בפולינג ולא בתוך המנוע.
- **מחיר מודע:** מכשיר שנותק מעל 90 יום יחזיר רשומה מחוקה **פעם אחת**;
  מחיקה חוזרת יוצרת tombstone אמיתי.

## הייצוג בענן — טבלאות מובנות והארכיון המאוחד (סבבים 30–32ב)
הנתונים בענן יושבים בטבלאות מובנות — שורה לכל רשומה — ולא כערכי `kv`:
- **`tb_entries` היא הטבלה המאוחדת** (סבב 32): גם היומן החי וגם הארכיון,
  עם דגל `archived boolean` ועמודת `yeshiva`. «העברה לארכיון» היא דגל על
  השורה, לא העברה בין טבלאות. `tbTableOf`/`tbArchivedFlag` מנתבות,
  ו-`tbRowsGet` מסננת `.eq('archived', …)` עם `order('rec_key')` **וגם**
  `tbSortRows` בקוד (סבב 31 — בלי `ORDER BY` פוסטגרס אינו מבטיח סדר,
  ומיון לקסיקוגרפי של `rec_key` אינו סדר תאריכים).
- ⭐ **עמודת `yeshiva` ולא טבלה לכל מוסד** — מוסד שלישי דורש אפס DDL.
  ⛔ ההפרדה **במכשיר** (סיומת `LS`) לא נגעה.
- ⭐ **`client_id` נגזר ממפתח המיזוג** (`entryKey`/`archiveKey`) ואינו uuid
  חדש — לרשומה כבר יש זהות שנוצרה במכשיר, ומזהה שני היה מאפשר לה להתקיים
  פעמיים. ⛔ **אין טריגר `updated_at` בצד השרת** — החותמת של המכשיר היא
  האמת, ולכן אין כאן את אובדן העריכה שכלל ברזל 6 מתאר.
- ⭐ **הסנאפשוט הוא יחידת הארכיון** — הוא מה שמנוע המיזוג מכיר
  (`archiveKey`), ובתוכו כבר רץ מיזוג פר-רשומה. ⛔ פירוקו לרשומות הוא
  מנוע מיזוג אחר — סבב ייעודי בהחלטת המנהל, לא תופעת לוואי.
- ⭐ **בונה סנאפשוט אחד — `arcPutSnapshot` — לשני מסלולי היצירה** (סבב 31):
  `autoArchiveDay` (ידני) ו-`checkDayChange` (מעבר יום) קוראים לו, ולכן
  אין מסלול שממלא שדה שהשני משמיט; מעבר יום על יום שכבר יש לו סנאפשוט
  מתמזג אליו. שדות היום נגזרים **מהרשומות עצמן** קודם, מהשעון רק
  כנפילה-חזרה. `hebFromText`/`snapHDate` משחזרות תאריך עברי **לתצוגה
  בלבד** — ⛔ `snapHDate` טהורה ואינה כותבת לסנאפשוט (סבב 31).
- ⭐ **הכתיבה הכפולה כובתה בסבב 35** — `TB_KV_LEGACY_WRITE = false`
  ו-`TB_ARC_LEGACY_WRITE = false`: היומן והארכיון נכתבים לטבלאות בלבד,
  ועֵד הפינוי ואישור ה-⏳ נשענים מעכשיו על `tbRowsPush` לבדה. נתיב
  החזרה נשאר החלפת דגל — מפתחות ה-`kv` והטבלה הישנה לא נמחקו.
  ⛔ המחיקה עצמה ב-30.8, פעולת מנהל, לא בסבב (סבב 35) — כיבוי ומחיקה
  יחד לא היו משאירים חלון לגלות טעות. ⚠️ `tb_cats`/`tb_subs`/
  `tb_subs_meta`/`tb_wa_phone` ממשיכים להיכתב ל-`kv` — זה ביתם היחיד
  בענן והם אינם חלק מהכיבוי; גם חותמת `tb_last_changed` נשארת (אות
  הפולינג).
- **מצב המיגרציות:** `002`–`004` הורצו ואומתו ב-2026-08-16, ו-**`005`
  (איחוד הארכיון) רצה ואומתה ב-2026-08-17** דרך `apply_migration`, עם
  בדיקת שקילות דו-כיוונית מלאה: 171 שורות מדוגלות `archived`, 728 חיות,
  899 סה"כ — אפס אי-התאמות. ⛔ מפתחות ה-`kv` המוסדיים לא נגעו — הם נתיב
  החזרה (ר' «פערים פתוחים»).
- ⚠️ **הגיבוי היומי עבר לטבלאות באותו סבב שכיבה את הכתיבה הכפולה**
  (סבב 35, כפי שסבב 30 קבע): `tb_entries` נגבית כטבלה — מקור
  `kind:'table'` עם `eq` פר-מוסד, `order('rec_key')` ומפתח הגיבוי
  `tb_entries_rows` — ולצדה מפתחות ה-`kv` שנשארו ביתם היחיד
  (`tb_cats`/`tb_subs`/`tb_subs_meta`/`tb_wa_phone`).

### מצב המיגרציות במסד הייצור (עודכן בסבב 39)
⚠️ **הטבלה הזו נוצרה בסבב 39** — עד אז מצב ההרצה היה שורת פרוזה שכיסתה
את `002`–`005` בלבד, ו-`000`/`001` לא הופיעו בה כלל.
⛔ «נכתב» אינו «רץ», וההרצה היא החלטת המנהל.

| | מה היא עושה | מצב |
|---|---|---|
| 000 | סכימת הבסיס — `kv_rishon` + `kv_ramataviv` | מקור אמת; מורץ על מסד ריק |
| 001 | `REVOKE DELETE/TRUNCATE` מ-`anon`/`authenticated` | ✅ הורצה ואומתה (סבב 29) |
| 002 | טבלאות מובנות — `tb_entries`/`tb_archive` | ✅ הורצה ואומתה (2026-08-16) |
| 003 | העברת ערכי ה-`kv` לשורות | ✅ הורצה ואומתה (2026-08-16) |
| 004 | דגל `archived` ל-`tb_entries` | ✅ הורצה ואומתה (2026-08-16) |
| 005 | איחוד הארכיון לתוך `tb_entries` | ✅ הורצה ואומתה (2026-08-17) |

⚠️ **`002`–`003` היו בפועל no-op** — הכתיבה הכפולה שרצה מסבב 30 כבר מילאה
את הטבלאות, וזו בדיוק העדות שההרצה נתנה. `005` אומתה בשקילות דו-כיוונית
מלאה: 171 שורות מדוגלות `archived`, 728 חיות, 899 סה"כ — אפס אי-התאמות.
⚠️ **ופינוי הגיבויים כאן אינו קובץ מקומי** — הוא
`hanhala-ruchanit/migrations/004`, של הפרויקט המשותף.

## מיגרציית מפתחות-שריד ב-localStorage — `migrateLegacyLsKeys()` (סבב 13)
מפתחות בלי סיומת מוסד (`tb_entries`/`tb_archive`) קדמו לרב-הדיירות ותפסו
~1.5MB מהמכסה המשותפת. ההגירה עדיין בקוד, אידמפוטנטית (אין שריד ⇒ יציאה
מיידית), ואלה כלליה: מיזוג אמיתי דרך המנוע (היעד «מרוחק» ומנצח בשוויון —
tombstone גובר על שריד חסר-חותמת) · אימות פריט-פריט, כשל ⇒ ההגירה מתבטלת
והשריד נשאר · כותבים לפני שמוחקים, רק כתיבה שהחזירה `true` מתירה מחיקה ·
⛔ **ראשון לציון בלבד (סבב 13)** — המפתחות קדמו לרב-דיירות ומיזוג לרמת
אביב היה דליפה בין מוסדות · ערך פגום אינו נמחק ונרשם ביומן. רצה
ב-`selectYeshiva` בין `lsRebuildPolicy()` ל-`lsBoot()`, לפני
`loadLocalData`.

---

<!-- SHARED:start id="iron-rules-storage" -->
## ⭐ כללי ברזל של הארגון — עמידות אחסון ואופליין (סבב 11, אוגוסט 2026)
**ארבעת הכללים האלה זהים מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius). הם חלים על כל קוד חדש בארגון.

1. **כל כתיבה ל-localStorage עוברת דרך העוזר המוקשח.** `lsSet(key, value)` או
   `lsSetArray(key, arr, tsOf)` — **ואין קריאה ישירה ל-`localStorage.setItem`**
   בשום מקום מחוץ למודול המשותף. `setItem` זורק באחסון חסום או מלא, וכל נתיב
   לא-מוגן מת באמצע: הזיכרון כבר עודכן, המסך מתרענן, ושום דבר לא נשמר.
   - החריג היחיד המותר הוא **`lsSetRaw`** — העוזר של המודול עצמו, שמחזיר
     `false` בלי להרים באנר. הוא שמור לערכים **חסרי-נזק שנכתבים בכל עליית
     דף** (מזהה מכשיר, יומן הפינוי). **אין להשתמש בו לשום נתון של המשתמש.**
2. **אף כישלון שקט.** `lsSet` מחזירה `true`/`false`, מרימה באנר אדום קבוע
   (`#ls-alert`) שנשאר עד שהמשתמש סוגר אותו, ורושמת ליומן.
   - נקודת האכיפה היא שורה אחת בראש `toast()` של כל אפליקציה:
     `if (typeof lsGuardToast === 'function' && !lsGuardToast(msg)) return;`
     — כל הודעה שמתחילה ב-✅ נבלעת אם כתיבה מקומית נכשלה בשתי וחצי השניות
     האחרונות. **אין להסיר את השורה הזו.** בלעדיה חוזר בדיוק המצב שהסבב הזה
     בא למנוע: כתיבה שנכשלה ומשתמש שרואה "נשמר בהצלחה".
   - במסלול קריטי (רישום נוכחות, שמירת תשלום, הוספת רשומה, רישום תנועה) יש
     בנוסף עצירה מפורשת — `lsFails()` / `lsOkSince(t)`, או ערך מוחזר שנבדק.
3. **עבודה אופליין מלאה היא דרישת בסיס בכל אפליקציה בארגון — מהיום הראשון,
   לא תוספת מאוחרת.** ארבעת הרכיבים, כולם:
   עותק מקומי של **כל** הנתונים · קריאה ממנו בעליית האפליקציה, **לפני** כל
   נגיעה ברשת · סנכרון ברקע · מנוע מיזוג **ברמת רשומה** לפי חותמת עדכון,
   עם tombstones. הכלל היחיד שחשוב במנוע: **היעדר רשומה אצל צד אחד אינו
   מחיקה** — מחיקה היא `deleted=true` + חותמת.
4. **ניטור מכסה ופינוי יזום נבנים יחד עם האופליין, לא אחריו.** עותק מקומי
   בלי ניטור הוא פצצת מכסה מושהית: הוא גדל עד שהכתיבה נכשלת. `lsBoot()`
   נקראת בכל עליית אפליקציה, מודדת את **כל** המפתחות בדומיין (לא רק של
   האפליקציה הנוכחית — המכסה משותפת!), מתריעה מעל 3MB ובאנר מעל 4MB,
   ומפנה מיוזמתה מעל 60%.
5. **כל אפליקציה חדשה בארגון נבנית עם התשתית הזו מלכתחילה** — מעתיקים את
   המודול המשותף כמו שהוא, ממלאים `LS_CFG`, מחברים `lsBoot()` בעלייה,
   את רכיב «מידע טכני ›» (`techInfoMount()`) בהגדרות, ואת שורת השער ב-`toast()`.

### מפתח ניווט — כללי הברזל 6–16
⚠️ **מפה, לא תוכן.** חמשת הכללים שבפרק הזה (1–5) הם עמידות אחסון ואופליין;
תשעת הכללים שאחריהם מפוזרים לאורך הקובץ, וכל אחד מהם יושב בפרק משלו.
המפתח כאן קיים כדי שסשן שנכנס לקובץ הזה בפעם הראשונה יידע **מה קיים**
ולאן ללכת, בלי לגלול שלושים פרקי סבבים.
⛔ **אין למספר מחדש** — המספרים הם **כתובות**, והפניות בקוד, בהערות ובקבצי
הבדיקה מצביעות עליהן. כלל חדש מקבל תמיד את המספר הבא בתור.

**מדרגת החומרה:** ⛔ = מוחלט, אין שיקול דעת · ⚠️ = מחייב, והיישום נתון
לשיקול דעת מנומק.

| נושא | כלל | חומרה |
|---|---|---|
| **בטיחות-מסד ונתונים** | **6** — שיטת הסנכרון בארגון: דחיפת-מצב + מיזוג ברמת רשומה, בלי תורים | ⛔ |
| | **9** — שדרוג אבטחתי מתפשט: נרשם בכל ארבעתן, ושקט אינו תשובה | ⛔ |
| | **10** — מודל המשתמשים וההרשאות (תשעה סעיפים, כולל ההרשאות במסד) | ⛔ |
| **אחידות בין הארבע** | **7** — רכיב משותף = קוד זהה + מיקום זהה + חיווט חי | ⛔ |
| | **12** — יכולת משותפת נמדדת בשלושה ממדים (ליבה · נקודת הפעלה · מטריצה) | ⛔ |
| | **14** — קיים באחת = קיים בארבעתן, או חריגה מנומקת במקום שהשער קורא | ⛔ |
| | **15** — פרק «פערים פתוחים» נאכף בשער: כל שורה נושאת תג מדיד/לא-מדיד | ⛔ |
| **תהליך העבודה** | **16** — הנחת השריד: אין מוחקים «שריד» לפני מדידה שאין לו קורא | ⛔ |
| **תהליך העבודה** | **13** — סבב שנוגע ב-SHARED נפתח מלכתחילה עם ארבעת הריפו | ⛔ |
| **תיעוד** | **8** — פרוטוקול התיעוד: סימון SHARED, מבנה פרק הסבב, שורת «עודכן לאחרונה» | ⛔ |
| | **11** — תקן ההערות בקוד: כותרת בלוק, שלושה סימנים, טרמינולוגיה | ⚠️ |

⚠️ **11 הוא היחיד ב-⚠️, ובכוונה:** הצורה נאכפת (`check-comments.mjs`),
אבל **הכיסוי** — מה נחשב «אזור לוגי» ומה ראוי להערה — הוא שיקול דעת.
⛔ הוא אינו יעד אחוזים, ואין להמציא הערות כדי להגיע למספר.

### המודול המשותף
`index.html` של כל אחת מארבע האפליקציות מכיל את אותו בלוק בדיוק, בין
`/* ═══ עמידות אחסון מקומי — מודול משותף (סבב 11)` לבין
`/* ═══════════════ סוף המודול המשותף`. **הוא זהה בית-לבית בארבעתן**
(474 שורות, sha1 `8345d842b294`). כל שינוי בו חייב להיעשות בארבע באותו סבב.
מעליו יושבת `LS_CFG` — **הדבר היחיד שנבדל בין האפליקציות** — ומתאם
`lsToast(msg, dur)` בן שורה.

**ה-API:** `lsSet` · `lsSetArray` · `lsGet` · `lsRemove` · `lsSetRaw` ·
`lsUsage` · `lsBoot` · `lsSweep`/`lsSweepGuarded` · `lsLog`/`lsLogRead` ·
`lsAlert` · `lsGuardToast` · `lsFails`/`lsOkSince` · `lsStatusHTML`/`lsRenderStatus` ·
`lsHorizon`/`lsClearHorizons`.

### סדר הפינוי — קפדני, ואין לשנות אותו
- **א. מטמונים וערכים שניתן לשחזר מהענן בכל רגע** (`LS_CFG.tier1`), הגדול קודם.
- **ב. ארכיון ורשומות ישנות שכבר מסונכרנות** (`LS_CFG.tier2`), **הישן ביותר
  קודם**, ורק מתחת לרצפת גיל פר-מפתח (`minAgeMs`).
- **ג. ⛔ נתון שטרם הסתנכרן לענן לא מפונה בשום מצב, גם אם המקום נגמר לגמרי.**
  במצב כזה עדיף להיכשל ברעש מאשר למחוק. שתי הגנות נפרדות אוכפות את זה:
  `LS_CFG.pending()` — יש משהו בתור? שלב ב מדולג **לגמרי**; ו-`syncedThrough()` —
  רק רשומות שגילן קודם לדחיפה/משיכה המוצלחת האחרונה נחשבות "בענן".
  ספק ⇐ לא מפנים.

**הפינוי שקט כלפי המשתמש אבל לא בלתי נראה** — כל פינוי, וכל **דילוג** על
פינוי, נרשם ביומן (`LS_CFG.logKey`, 12 רשומות אחרונות) ומוצג בהגדרות.

### אופק הפינוי — למה שלב ב לא חסר משמעות
פינוי מקומי בלבד היה חסר תועלת: מנוע המיזוג מחזיר כל רשומה מרוחקת שאין לה
מקבילה מקומית, ולכן הרשומות שפונו היו חוזרות לדיסק תוך שניות. לכן לכל מפתח
שמפונה נשמר **אופק** (`LS_CFG.hzPrefix + key`) — חותמת הרשומה החדשה ביותר
שפונתה — ו-`lsSetArray` מסננת מתחתיו **בכל כתיבה**.
**הזיכרון והענן לא נוגעים; רק העותק שעל הדיסק מצטמצם.** כשיש רשת המשתמש
רואה הכול כרגיל; אופליין הוא רואה את התקופה האחרונה בלבד, וההגדרות
(«מידע טכני ›») אומרות זאת במפורש. המנגנון שמנקה את האופק (`lsRestoreAll`)
קיים במודול אך אינו נגיש מה-UI מאז סבב 15; ניקוי אופק ייעשה בסבב ייעודי
אם יידרש.

### מנגנון חירום
אם למרות הכול כתיבה נכשלת ב-`QuotaExceededError`, `lsSet` מריצה פינוי נוסף
**לפי אותו סדר בדיוק** ומנסה **פעם אחת** נוספת. הצליח — זו הצלחה לכל דבר
(המונה מוחזר וטוסט ההצלחה מותר). נכשל — באנר, טוסט ויומן. אין רקורסיה:
`_lsSweeping` חוסם פינוי בתוך פינוי.
<!-- SHARED:end -->

### היישום כאן (yoman-avoda)
- **19 אתרי כתיבה גולמיים — כולם היו חשופים, כולם עברו לעוזר.** `saveCats`,
  `saveSubs`, `saveEntries`, `saveArchive`, חמש הכתיבות ב-`syncFromCloud`,
  שלוש ב-`autoArchiveDay`, שתיים ב-`arcSaveEntry`, מספר הוואטסאפ, דגל דחיית
  העדכון ומזהה המכשיר. עד היום אף אחת מהן לא נבדקה: באחסון מלא **הארכוב
  האוטומטי של סוף יום היה מוחק את הרשומות מ-`ENTRIES` בלי שהסנאפשוט נשמר**.
- **המערכים הגדולים דרך `lsSetArray`** (`tb_entries`+LS, `tb_archive`+LS).
- **⚠️ `_lastKnownTimestamp` אינו עֵד סנכרון ואסור להשתמש בו לפינוי.** הוא
  מתעדכן גם ב**משיכה** (משמעותו "ראיתי את מצב הענן") וגם נכתב ב-`saveEntries`/
  `saveArchive` **בלי לבדוק את תוצאת ה-`sbSet`**. פינוי שהיה נשען עליו היה
  יכול למחוק מהדיסק רשומה שמעולם לא הגיעה לענן. במקומו נוסף **`_tbPushedAt[key]`**,
  שנכתב **רק** אחרי `sbSet` שהחזירה `ok` — ומוזרם למנוע דרך `spec.syncedThrough`
  פר-מפתח. **אין להחליף אותו חזרה.**
- **`LS_CFG.tier2` נבנית ב-`lsRebuildPolicy()`, שנקראת מ-`selectYeshiva`** —
  המפתחות נושאים סיומת מוסד (`_rishon` / `_ramataviv`). הפינוי חל על **המוסד
  הפעיל בלבד**: לעֵד הסנכרון של מוסד אחד אין שום תוקף לגבי הנתונים של השני.
  המוסד השני יפונה כשייבחר. רצפות: 60 יום לארכיון, 90 לרשומות.
- **`_tbRecTs`** קוראת `updatedAt`, ובהיעדרו גוזרת מ-`gdate` — שנשמר בצורה
  `"3/09/2025"` (d/mm/yyyy), לא ISO — עם עוגן בצהריים מקומיים, כמו כל חישוב
  תאריך בפרויקטים האלה.
- **`lsBoot()`** נקראת ב-`selectYeshiva`, אחרי `lsRebuildPolicy()` ולפני
  `loadLocalData()`.
- **הגדרות** — האבחון מוצג כיום דרך «מידע טכני ›» בתחתית הפאנל (קופל
  בסבב 15; במקור כרטיס נפרד מעל עריכת הקטגוריות).

### ⚠️ yoman היא הצרכנית הגדולה בדומיין
`tb_archive` של ראשון לציון לבדו הוא **1.55MB** ב-localStorage, ושני המוסדות
יחד **~1.99MB** — קרוב ל-40% מהמכסה המשותפת של ארבע האפליקציות. זו הסיבה
שדווקא כאן שלב ב הוא אמיתי ולא תיאורטי.

---

<!-- SHARED:start id="pending-module" -->
## ⏳ סימון "ממתין לסנכרון" — מודול משותף (סבב 12, שלב 1)
**הפרק הזה, עד הסעיף «מה נעשה כאן», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md.**

### הבעיה
אחרי שמירה מקומית המסך נראה **בדיוק** כמו אחרי שמירה בענן. המשתמש רושם תשלום
ברכבת התחתית, רואה אותו ברשימה, וסוגר את האפליקציה בהנחה שהוא שמור — בזמן שהוא
יושב רק על המכשיר הזה. הסבב הזה הופך את ההבדל לגלוי.

### שלוש רמות תצוגה, כולן ממפה אחת
- **תגית על הרשומה עצמה** — `pendTag(key)`, נכנסת לשורה ברשימה (⏳ ממתין, ובאיחור ⚠️).
- **מונה** — `#pend-bar`, סרגל תחתון קבוע, **מוצג רק כשיש** משהו ממתין.
- **התרעה** — משהו ממתין מעל 24 שעות ⇐ `#pend-alert`, באנר אדום עליון עם כפתור סגירה
  (נסגר ל-6 שעות; **הסימון עצמו לא נמחק בסגירה**).

### ⚠️ ההגדרה של "ממתין" היא ראייתית, לא אופטימית
רשומה מסומנת ברגע שנכתבה במכשיר, ויורדת מהסימון **רק כשיש ראיה שהענן קיבל אותה**.
שלושת מסלולי האישור, לפי אופי האפליקציה:

| מסלול | מי משתמש בו | הראיה |
|---|---|---|
| `pendClear` | schar-limud | תשובת הצלחה מהשרת, או משיכה שמחזירה את ה-`client_id` |
| `pendConfirmPush(prefix, t0)` | hanhala, yoman | דחיפת-מצב מלא שהצליחה |
| `pendSyncOutbox()` | gius | הפעולה ירדה מהתור היוצא |

- **כשל רשת אינו ראיה לכלום ⇒ הסימון נשאר.** תשובה שאבדה ברשת היא בדיוק המקרה שבו
  הרשומה אולי כן נכנסה ואולי לא. ספק ⇐ מסומן.
- **כשל סמכותי כן מוריד את הסימון** (`pendFailed`) — ולידציה/הרשאה/סכימה אומרות
  שהשורה **לא** נכתבה, וסימון "ממתין" עליה מבטיח שליחה חוזרת שלא תקרה לעולם.
- **⚠️ `t0` נלקח לפני שצילום המצב נקרא, לא אחרי שהדחיפה הסתיימה.** רשומה שנכתבה
  באמצע הדחיפה אינה בתוכה, והורדת הסימון ממנה הייתה מאבדת אותה בשקט.
- **חותמת קיימת אינה נדרסת** בסימון חוזר — אחרת עריכה חוזרת של אותה רשומה הייתה
  מאפסת את שעון ה-24 שעות לנצח.

### המודול
`index.html` של כל אחת מארבע האפליקציות מכיל את אותו בלוק בדיוק, בין
`/* ═══ ממתין לסנכרון — מודול משותף (סבב 12)` לבין
`/* ═══════════════ סוף מודול "ממתין לסנכרון"`. **זהה בית-לבית בארבעתן**
(267 שורות, sha1 `2b3d6b779b1f`). כל שינוי בו חייב להיעשות בארבע באותו סבב.
מעליו יושבת `PEND_CFG` — **הדבר היחיד שנבדל בין האפליקציות** — ומתחתיו בלוק
"חיבור <אפליקציה> למודול" עם הקידומות והעוזרים המקומיים.

**ה-API:** `pendMark`/`pendMarkMany` · `pendClear`/`pendClearMany` · `pendFailed` ·
`pendConfirmPush` · `pendHas`/`pendSince`/`pendCount`/`pendOldest`/`pendLateCount` ·
`pendTag` · `pendRender`/`pendCounterText` · `pendStatusHTML` · `pendBoot`/`pendReload` ·
`pendReset`.

**כתיבה:** דרך `lsSet` בלבד (כלל ברזל 1), למפתח יחיד פר-אפליקציה
(`sl_pending` / `ys_pending` / `tb_pending<מוסד>` / `g_pending`).
**תקרה:** 800 סימונים; בגלישה נגרעים הישנים ביותר **ונרשמים ליומן** — סימון שנעלם
בלי אישור הוא בדיוק מה שהמודול בא למנוע, ולכן הוא לא נעלם בשקט.

### `PEND_CFG`
- **schar-limud:** `sl_pending`.
- **hanhala-ruchanit:** `ys_pending`; `extra()` מוסיף למונה את מספר המפתחות
  ב-`ys_offline_queue` — התור הישן הוא **פר-מפתח kv** ולא פר-רשומה, ובלי זה
  המשתמש היה רואה "0 ממתינות" בזמן שארבעה מפתחות תקועים.
- **yoman-avoda:** המפתח הוא **פונקציה** — `tb_pending` + סיומת המוסד. שני המוסדות
  חולקים localStorage, וסימון של ראשון לציון אינו תקף לרמת אביב. `selectYeshiva`
  קוראת ל-`pendReload()`.
- **gius:** `g_pending`; `extra()` מציג פעולות שנכשלו כשל תוכן חוזר (`dead`).

### מה סומן בכל אפליקציה
| | רשומות מסומנות | קידומות |
|---|---|---|
| schar-limud | תשלום, תלמיד, ומחיקה של כל אחד מהם | `txn:` `student:` `txn-del:` `student-del:` |
| hanhala | סדר נוכחות, סדר שינה, טיפולים, תלמידים | `at-sess:` `sl-sess:` `at-treat:` `sl-treat:` `student:` |
| yoman | רשומת יומן, סנאפשוט ארכיון | `entry:` `arc:` |
| gius | כל שבע הטבלאות, לפי `<table>:<id>` | `g_donors:` `g_txns:` … |

**מסך "⏳ ממתין לסנכרון" נוסף להגדרות בארבעתן** (`pendStatusHTML`) — מה ממתין,
מאיזה גיל, ומצב הרשת.

<!-- SHARED:end -->

### היישום כאן (yoman-avoda)
`pendReload()`+`pendBoot()` ב-`selectYeshiva`, אחרי `lsRebuildPolicy()`.
`addEntry`/`saveEntry`/`delEntry`/`arcSaveEntry` מסמנות; `saveEntries`/`saveArchive`
לוקחות `t0` לפני הצילום ומאשרות על `sbSet` שהחזירה `ok` — **אותה עדות שמזינה את
`_tbPushedAt`, ולא `_lastKnownTimestamp`** (שמתעדכן גם במשיכה ולכן אינו ראיה
לדחיפה — ר' אזהרת סבב 11). תגית ⏳ בשורת היומן.

---

<!-- SHARED:start id="iron-rule-6-sync" -->
## ⭐ כלל ברזל 6 — שיטת הסנכרון בארגון (סבב 12 שלב 4, אוגוסט 2026)
**הפרק הזה, עד שורת «כאן:» שבסופו, זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius), והוא ממשיך את חמשת כללי
הברזל שלמעלה.

> **שיטת הסנכרון בארגון היא דחיפת מצב מלא + מיזוג ברמת רשומה. אין תורים.
> כל רשומה שטרם אושרה בענן מסומנת למשתמש.**

עד הסבב הזה חיו כאן שלוש שיטות שונות: hanhala ו-yoman דחפו מצב מלא, gius
החזיקה תור יוצא פר-פעולה, ו-schar-limud לא עבדה אופליין כלל. מעכשיו — אחת.

### למה דווקא דחיפת מצב
- **אין פריט שיכול "להיתקע" לבדו.** בתור FIFO, פעולה שנכשלה כשל תוכן חוסמת
  את כל מה שאחריה, או מסומנת `dead` ונשארת תלויה לנצח. בדחיפת-מצב כל רשומה
  עומדת בפני עצמה, וכל סנכרון מתקן את מה שחסר — כולל מה שנכשל בפעם הקודמת.
- **אין מצב ביניים.** אין תור שיכול להתפגם, להתמלא או לצאת מסנכרון מול
  הנתונים עצמם. מצב האמת הוא הרשומות, ותו לא.
- **התאוששות אוטומטית.** מכשיר שאיבד את הסימונים עדיין מסתנכרן נכון:
  ההשוואה היא בין העותק המקומי לענן, לא בין יומן פעולות למציאות.

### חמשת הכללים
1. **עותק מקומי מלא של כל הנתונים, כולל tombstones.** רשומה מחוקה **חייבת**
   להישמר עם `deleted=true` + חותמת. היעדר רשומה אצל צד אחד **אינו** מחיקה.
2. **קריאה מהעותק בעליית האפליקציה, לפני כל נגיעה ברשת.**
3. **כתיבה מקומית-תחילה.** הכתיבה נשמרת במכשיר ומוצגת מיד; הרשת היא עניין
   של הרקע. כשל כתיבה מקומית עוצר ברעש ולא מציג "נשמר".
4. **סנכרון = משיכה ← מיזוג ← דחיפת מה שמקומי-וחדש-יותר.** בסדר הזה: אי
   אפשר לדעת מה לדחוף בלי לראות קודם מה בענן, ודחיפה עיוורת מחזירה לחיים
   רשומות שנמחקו במכשיר אחר. **דוחפים רק לקטגוריה שנמשכה בהצלחה.**
5. **מיזוג ברמת רשומה לפי חותמת עדכון**, עם שובר-שוויון דטרמיניסטי (הענן
   מנצח בשוויון), כך ששני מכשירים שממזגים את אותם צדדים מגיעים לאותה תוצאה.

### ⚠️ הכלל שמונע אובדן עריכה — רשומה מסומנת ⏳ מנצחת במיזוג
בשלוש מהאפליקציות החותמת נקבעת **בצד השרת** בטריגר `BEFORE UPDATE`. המשמעות:
ברגע שמכשיר א' דוחף, השורה בענן מקבלת חותמת **מאוחרת יותר** מעריכה חדשה יותר
שיושבת אצל מכשיר ב' וטרם עלתה — ומכשיר ב' היה מאמץ את גרסת א', כלומר **מוחק
את העריכה שלו בשקט**. זה לא תרחיש תיאורטי; הוא נתפס בבדיקה.

לכן, בשני מקומות ולא באחד:
- **במיזוג** — רשומה מקומית מסומנת ⏳ מנצחת ללא תלות בחותמת.
- **בבחירת מה לדחוף** — רשומה מסומנת נחשבת תמיד "לדחיפה". בלי זה היא הייתה
  נשארת מסומנת לנצח ולעולם לא נדחפת.

**המחיר המודע:** מכשיר שהיה מנותק זמן רב ידרוס בעלייתו עריכה מרוחקת חדשה
יותר. זו העדפה מכוונת — הרשומה מסומנת ⏳ כל אותו זמן ומתריעה אחרי 24 שעות,
בעוד שעריכה שנמחקת בשקט אינה משאירה עקבות. אותה עדיפות כמו "היעדר רשומה
אינו מחיקה". **אין להסיר את הבדיקה הזו משני המקומות.**

### ⛔ אין תורים
`g_outbox` הוסר (gius, שלב 3) ולא ייבנה מחדש. `ys_offline_queue` בהנהלה נשאר
בינתיים כמנגנון פר-מפתח kv **בלבד** ואינו תור פעולות; אין להרחיב אותו.
**אין ליצור תור פעולות חדש בשום אפליקציה בארגון.**

### ⛔ מה לא נדחף לעולם
רשומה שהעותק המקומי שלה **חלקי** אינה ניתנת לדחיפת-מצב: הדחיפה הייתה
כותבת את מה שחסר כערך ריק. המקרה השכיח הוא **סיסמה**: היא מוסרת
מהמראה כדי שלא תשב בטקסט גלוי על הדיסק, ולכן אין מה לדחוף.
מקרה אחד קיים היום, מתועד במקומו — `g_users` ב-gius
(`strip: ['password']`). **שינוי סיסמה דורש רשת.** המקרה השני,
`sl_settings.admin_pass` ב-schar-limud, נסגר בסבב 35: השורה נמחקה
מהמסד ומהגיבויים ע"י המנהל, הרשימה `SL_NEVER_MIRROR_SETTINGS` רוקנה —
ומנגנון הסינון נשאר דרוך לסוד עתידי.

⚠️ **חריגה כזו היא ברמת השורה ולא ברמת הטבלה.** כל שאר השורות של
`sl_settings` נדחפות כרגיל (מ-`migrations/009`). וכשהסוד מוסר מהמראה —
**אסור ששער הכניסה ייפול חזרה לברירת המחדל.** בדיקה כמו
`p !== (SETTINGS['admin_pass'] || 'admin')` הופכת בדיוק ברגע ההסרה
לשער שנפתח לכל מי שמקליד «`admin`». ההשוואה חייבת להיות מול טביעה
נגזרת (PBKDF2) או מול הענן — **ולהיכשל סגור כשאין אף אחת מהן.**

### מזהים
כל רשומה חדשה מקבלת **מזהה שנוצר במכשיר** לפני שראתה שרת — uuid v4.
בטבלאות עם `SERIAL` (schar-limud) זהו `client_id` לצד ה-`id` של המסד, והכתיבה
היא `upsert` עליו. זה מה שהופך ניסיון חוזר אחרי תשובה שאבדה ברשת
לאידמפוטנטי, במקום ליצור שורה שנייה — בכסף, תשלום כפול.
<!-- SHARED:end -->

**כאן:** `mergeRecords`/`mergeEntries`/`mergeArchive` ו-`saveEntries`/
`saveArchive` כדחיפת-מצב מלא, עם `_tbPushedAt` כעֵד הדחיפה.

---

<!-- SHARED:start id="round-14-verified-sweep" -->
## ⭐ סבב 14 (אוגוסט 2026) — עדות סנכרון חלופית לפינוי היזום
**הפרק הזה, עד הסעיף «בדיקה», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius).

### הבעיה שנמדדה
שלב ב של הפינוי (רשומות ישנות מסונכרנות) מותנה ב**עֵד סנכרון מקומי** —
חותמת שנכתבת רק אחרי דחיפה מוצלחת מהמכשיר הזה (`_tbPushedAt` ביומן,
`_ysPushedAt` בהנהלה). **בדפדפן שקורא ולא כותב** — כרום שבמחשב המנהל, שרק
מסתכלים בו — אין דחיפה, ולכן אין עֵד, ולכן שלב ב מדולג לגמרי: הפינוי מחזיר
0 וההתרעה הצהובה («מעל 3MB») נשארת קבועה, בזמן שכל הנתונים כבר בענן.
**נמדד בפועל: 3.06MB / 61%, ופינוי שהחזיר 0.**

### התיקון — מחליפים את מקור הראיה, לא מרפים מהדרישה
כשאין עֵד מקומי (או שהוא 0), שלב ב עובר ל**אימות ישיר מול הענן**: משיכת
העותק הענני של המפתח המועמד לפינוי, ומחיקה של רשומה **רק** אם היא נמצאת שם
לפי מזהה **וגם** חותמת הענן אינה ישנה מהחותמת המקומית (הענן מחזיק את הגרסה
הזו או חדשה ממנה).

**ה-API החדש במודול המשותף:**
`lsGlobalWitness` · `lsSpecWitness` · `lsVerifySpecs`/`lsHasVerifiers` ·
`lsCloudIndex` · `lsPruneKeyVerified` · `lsSweepNeed` · `lsSweepVerified` ·
`lsScheduleDeferred`/`lsBootDeferred` · `lsBootAlert`.

**שני שדות חדשים ב-spec של `LS_CFG.tier2`:**
- `idOf(rec)` — מזהה הרשומה, אותו מפתח שבו משתמש מנוע המיזוג.
- `verify()` — מחזירה `Promise` של `{ok:true, rows:[...]}` מהענן.
  `spec.ts` הקיימת היא שגוזרת את החותמות משני הצדדים.

### ⛔ ארבעה כללים שאין לשנות
1. **נכשל סגור.** אין רשת, `verify` זרקה, החזירה `ok:false`, או שהמפתח כלל
   אינו קיים בענן ⇒ **לא מפנים דבר**. היעדר ראיה אינו ראיה. (ההגנה משוכפלת
   בשלוש שכבות: בדיקת התשובה, `lsCloudIndex` שמחזירה `null` על קלט שאינו
   מערך, ו-`lsPruneKeyVerified` שיוצאת על `idx` ריק.)
2. **רצפות הגיל (`minAgeMs`) נשארות כפי שהיו** — 60/90 יום ביומן, 180 בהנהלה.
   הן שכבת בטיחות **נפרדת** מהראיה ולא תחליף לה: רשומה מאומתת בענן שעדיין
   בתוך הרצפה אינה מפונה.
3. **`LS_CFG.pending()` נבדק גם כאן**, בדיוק כמו בשלב ב הסינכרוני: יש משהו
   שטרם עלה ⇒ לא נוגעים ברשת ולא מפנים דבר.
4. **⛔⛔ פינוי רצף בלבד — עוצרים ברשומה הראשונה שלא אומתה, לא מדלגים עליה.**
   זו הנקודה המסוכנת בסבב, והיא **לא** אופטימיזציה: אחרי הפינוי נשמר
   `hz` = חותמת הרשומה החדשה ביותר שנמחקה, ו-`lsSetArray` מסננת **בכל
   כתיבה** כל רשומה שחותמתה ≤ `hz`. דילוג על רשומה לא-מאומתת והמשך מעליה
   היה מציב אופק **מעליה**, וכתיבת המערך הבאה הייתה מוחקת אותה בשקט —
   דווקא את הרשומה היחידה שאין לה עותק בענן. (מאומת: מוטציה שהחליפה
   `break` ב-`continue` הפילה שלוש טענות, כולל האימות ש-`lsSetArray`
   מוחקת בפועל את היתומה.)

### ⚠️ אסינכרוני — ולכן גם ההתרעה נדחית
`lsBoot` סינכרונית ורצה **לפני** שהמסך עולה, ואילו האימות דורש רשת. לכן:
- `lsBoot` מבצעת כרגיל את שלב א ואת שלב ב הסינכרוני, ואם עדיין מעל הסף
  **ויש מפתחות שניתן לאמת** — היא מתזמנת `lsBootDeferred()` (3 שניות)
  **ודוחה איתו את ההתרעה**. הצגתה בעלייה הייתה אזהרה צהובה על מצב שנפתר
  שתי שניות אחריה.
- `lsBootDeferred` מריצה את האימות, מודדת מחדש, ורק אז קוראת ל-`lsBootAlert`.
  אין מה לפנות או אין רשת ⇒ ההתרעה כן מוצגת, וזה הנכון.
- `lsBoot` מחזירה עכשיו `{usage, swept, deferred}`, ומקבלת `opts`
  אופציונלי (`noDefer`, `deferMs`) — לבדיקות ולנתיבים שאסור לדחות בהם.
- `lsSweepNow()` (הכפתור בהגדרות) מחזירה **Promise**: היא מריצה את שני
  המסלולים ומדווחת אחרי שניהם.
- מסך «מידע טכני ›» (הגדרות) מציג אילו מפתחות תלויים באימות מול הענן.

### תיאום גרסאות
`sw.js`: hanhala **v29** · yoman **v18** (`app-version` **12-2026-08-10-1**) ·
schar **v19** · gius **gius-v6**.

### המודול המשותף — גודל חדש
הבלוק שבין `/* ═══ עמידות אחסון מקומי — מודול משותף (סבב 11)` לבין
`/* ═══════════════ סוף המודול המשותף` הוא כעת **691 שורות**, sha1
`22c1fb96b0ea` (היה 474). **זהה בית-לבית בארבע האפליקציות** — כל שינוי בו
חייב להיעשות בארבעתן באותו סבב.
<!-- SHARED:end -->

### היישום כאן (yoman-avoda)
- שני מפתחות ה-`tier2` ב-`lsRebuildPolicy()` קיבלו `idOf` ו-`verify`:
  `tb_archive`+LS לפי **`archiveKey`** (`g:<gdate>`, ובהיעדרו `i:<id>`)
  ו-`tb_entries`+LS לפי **`entryKey`** — בדיוק המפתחות של מנוע המיזוג, כך
  שאין דרך שהאימות והמיזוג יחלקו על זהות רשומה.
- **`_tbVerify(kvKey)`** מושכת דרך `sbGetResult`. ⚠️ **`ok` לבדו אינו
  מספיק:** `sbGetResult` מחזירה `{ok:true, data:null}` גם למפתח שאינו קיים
  בענן (PGRST116 מ-`.single()`), ולכן נדרש **מערך** — אחרת מפתח חסר בענן
  היה נקרא כ"ענן ריק" ו... לא היה מפנה דבר בכל מקרה (הרשומה הראשונה לא
  מאומתת), אבל התיעוד ביומן היה מטעה.
- **מפתח הענן הוא בלי סיומת המוסד** (`tb_archive`, לא `tb_archive_rishon`) —
  ההפרדה בין המוסדות בענן היא ב-`KV_TABLE` (`kv_rishon`/`kv_ramataviv`),
  והמפתח בתוך הטבלה זהה. הפינוי ממשיך לחול על **המוסד הפעיל בלבד**.
- `_tbPushedAt` **נשאר כפי שהוא** — הוא עדיין המסלול המהיר והמועדף כשיש
  דחיפה. ⛔ ואין להחליף אותו ב-`_lastKnownTimestamp` (ר' אזהרת סבב 11).

---

<!-- SHARED:start id="round-15-tech-info" -->
## ⭐ סבב 15 (אוגוסט 2026) — קיפול מסכי האחסון ל«מידע טכני»
**הפרק הזה, עד הסעיף «מה נעשה כאן», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius).

מסך «💾 אחסון מקומי» בהגדרות קופל מלוח-מחוונים למידע טכני צנוע. העיקרון:
המנהל צריך לדעת שהכל מתנהל מושלם מאחורי הקלעים, בלי להתבלבל ממספרים,
גרפים וכפתורים. **שינוי תצוגה בלבד — המודול המשותף של סבב 14 לא נגוע**
(אומת: הבלוק שבין סמני המודול זהה בית-לבית ל-HEAD בארבעתן, sha1 `18c08fcc4b67`).

- **אין יותר כפתורי פעולה באזור האחסון.** «🧹 פנה מקום עכשיו», «↻ רענן»
  ו-«☁️ טען הכל מחדש מהענן» אינם מוצגים — מוסתרים ב-CSS
  (`.tech-info .ls-status button{display:none}`); ה-HTML שלהם עדיין נוצר
  ע"י `lsStatusHTML` שבמודול הקפוא, ולכן המודול לא נערך. **המנגנון
  האוטומטי הוא הדרך היחידה לפינוי.** `lsSweepNow`/`lsRestoreAll` קיימות
  אך אינן נגישות מה-UI.
- **האבחון נעשה דרך רכיב «מידע טכני ›»** — שורה מוקטנת בתחתית מסך
  ההגדרות, סגורה כברירת מחדל (`<details class="tech-info">`). בפתיחה
  `techInfoToggle` קורא ל-`lsRenderStatus`, והתוכן — ניצולת, פילוחים,
  אזהרות האופק/האימות ויומן הפינוי — מרונדר לתוך `#ls-status-box` שחי
  עכשיו בתוך הרכיב. **התוכן לא השתנה, רק המיקום.**
- **שקט הוא ברירת המחדל** — במקום המסך הישן לא מוצג דבר.
- **חריג יחיד ומכוון:** מעל `LS_CRIT_BYTES` (4MB) — מצב שהמנגנון האוטומטי
  לא הצליח למנוע — `techInfoHTML` מציג שורת מצב עדינה (`.ls-crit-line`)
  גם מחוץ ל«מידע טכני». זו רשת הביטחון היחידה שמוצגת ביוזמת המערכת.
  (באנרי המודול עצמו — `#ls-alert`, התרעות lsBootAlert — לא נגעו.)
- **אחידות:** בלוק ה-JS (`techInfoHTML`/`techInfoToggle`/`techInfoMount`,
  יושב מיד אחרי «סוף המודול המשותף») ובלוק ה-CSS זהים בית-לבית בארבעתן
  (sha1 `72fc844f885e` / `f74f1f7510e7`). כל שינוי בהם — בארבע באותו סבב.
- מסך «⏳ ממתין לסנכרון» **לא נגוע** — הוא אינו ממסכי האחסון.
<!-- SHARED:end -->

### היישום כאן (yoman-avoda)
כרטיס «💾 אחסון מקומי» הוסר מ-`panel-settings`; `<div id="tech-info-box">`
נוסף בתחתית הפאנל, אחרי כרטיס עריכת הקטגוריות; `renderSettings()` קורא
`techInfoMount()` במקום `lsRenderStatus()` (הקריאות שבתוך המודול הקפוא
נשארו — הן no-op כשהרכיב סגור או לא נטען).


---

<!-- SHARED:start id="round-16-pend-delay" -->
## ⭐ סבב 16 (אוגוסט 2026) — יישור ניסוחים + השהיית ציור ה-⏳
**הפרק הזה, עד הסעיף «בדיקה», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius).

### חלק א — יישור אזכורים תפעוליים אחרי קיפול סבב 15 (תיעוד בלבד)
אפס שינוי התנהגות. כלל ההבחנה שהופעל:
- שורות הכפתורים בתוך `lsStatusHTML` שבמודול האחסון הקפוא — **לא נגעו**
  (חלק מהמודול, מוסתרות ב-CSS, מתועד בסבב 15).
- תיאורים היסטוריים בפרקי הסבבים — **לא נגעו**. היסטוריה נשארת היסטוריה.
- אזכורים **תפעוליים בהווה** של מסך «💾 אחסון מקומי» הישן יושרו
  ל-«הגדרות ← מידע טכני ›»: הערת מיגרציית השרידים ב-`index.html` של יומן,
  ובארבעת ה-CLAUDE.md — פרק אופק הפינוי (כפתור «☁️ טען הכל מחדש מהענן»
  אינו נגיש מה-UI מאז סבב 15 — נוסח מחדש: המנגנון `lsRestoreAll` קיים
  במודול, וניקוי אופק ייעשה בסבב ייעודי אם יידרש), כלל ברזל 5 (חיווט
  אפליקציה חדשה: `techInfoMount()` במקום `lsRenderStatus()`), שורת «אילו
  מפתחות תלויים באימות» בפרק סבב 14, ותיאורי המיקום בסעיפי «מה נעשה כאן».

### חלק ב — השהיית ציור ⏳ (שינוי UI בלבד, בארבעתן)
עד היום כתיבה מקומית ציירה ⏳ מיידית, והדחיפה המוצלחת מחקה אותו — באונליין
תקין זה היה הבהוב של שבריר שנייה על כל שמירה. מעכשיו הציור מאחר
**`PEND_DRAW_DELAY_MS` = 2000 מ״ש**: הגיע אישור הענן לפני — ה-⏳ לא מופיע
כלל; לא הגיע (אופליין, רשת איטית/חולה) — הוא מופיע וממשיך להתנהג בדיוק
כמו היום.

⚠️ **זו השהיית *ציור* בלבד — ארבעה גבולות קשיחים:**
1. **הסימון הלוגי נכתב ל-localStorage מיידית וללא שינוי** (`pendMark`
   ומקביליו) — הוא חלק ממנגנון הבטיחות: הפינוי, בחירת מה לדחוף והמיגרציות
   נשענים עליו. ההחזקה (`_pendDrawHold`) חיה **בזיכרון בלבד** ואינה נשמרת.
2. **מכשיר שנסגר בתוך שתי השניות** — בפתיחה הבאה ה-⏳ מוצג מיד: ההשהיה
   חלה רק על סימון שנוצר הרגע בכתיבה חדשה, לא על סימון שנטען מהדיסק.
3. **מסך «⏳ ממתין לסנכרון» בהגדרות והמונים הלוגיים** (`pendCount`/`pendHas`/
   `pendSince`/`pendStatusHTML`) מציגים תמיד את האמת הלוגית המלאה, בלי
   השהיה. גם סרגל `#pend-bar`, כשהוא מוצג, מציג את המונה הלוגי המלא —
   ההשהיה קובעת רק **אם** מציירים, לא **מה** המספר.
4. **מימוש אחיד בארבעתן** — אותן פונקציות עזר במודול המשותף
   (`pendHoldDraw` · `pendDrawSince` · `pendDrawCount` · `pendDrawFlush` ·
   `pendScheduleFlush`), אותו קבוע. ההתאמה הפר-אפליקציתית היא שדה
   **`PEND_CFG.redraw`** בלבד: בתום ההחזקה, אם הסימון עדיין קיים,
   `pendDrawFlush` מרנדר מחדש את הרשימות שמציירות `pendTag` — כדי שהתגית
   תיכנס לשורות שכבר רונדרו בלעדיה. אושר בינתיים ⇒ אין redraw ואין רינדור
   מיותר.

### המודול המשותף — גודל חדש
הבלוק שבין `/* ═══ ממתין לסנכרון — מודול משותף (סבב 12)` לבין
`/* ═══════════════ סוף מודול "ממתין לסנכרון"` הוא כעת **328 שורות**, sha1
`a6fbdd2c73ff` (היה 267 / `2b3d6b779b1f`). **זהה בית-לבית בארבע
האפליקציות** — כל שינוי בו חייב להיעשות בארבעתן באותו סבב. מודול האחסון
הקפוא של סבב 14 **לא נגוע** (אומת: הבלוק זהה בית-לבית ל-HEAD בארבעתן).
<!-- SHARED:end -->

### היישום כאן (yoman-avoda)
`PEND_CFG.redraw` = `renderLog()` — האתר היחיד שמצייר `pendTag` כאן (שורת
היומן).

---

<!-- SHARED:start id="iron-rule-7-status-area" -->
## ⭐ כלל ברזל 7 — רכיב משותף = קוד זהה + מיקום זהה + חיווט חי (סבב 17, אוגוסט 2026)
**הפרק הזה, עד הסעיף «בדיקה», זהה מילה במילה בארבעת קבצי ה-CLAUDE.md**
(hanhala-ruchanit, schar-limud, yoman-avoda, gius), והוא ממשיך את ששת כללי
הברזל שלמעלה.

> **רכיב שמוכרז «משותף» חייב שלושה דברים, לא אחד: אותו קוד, באותו מקום
> במסך, ומחובר לקוד חי. תיעוד לבדו אינו אוכף אף אחד מהם — ולכן יש בדיקה
> אוטומטית שרצה עם שערי התחביר לפני כל דחיפה.**

### הבעיה שנמדדה
סבב 15 קיפל את מסכי האחסון לרכיב «מידע טכני ›» בארבע האפליקציות, והתיעוד
הכריז שהבלוק זהה בית-לבית. הבלוק אכן היה זהה — **וכל השאר לא**:

| | מסך «⏳ ממתין לסנכרון» | «מידע טכני ›» |
|---|---|---|
| schar-limud | **ראש** מסך ההגדרות | סוף המסך ✅ |
| yoman-avoda | **אמצע** מסך ההגדרות | סוף המסך ✅ |
| hanhala | **אמצע** מודול המערכת | **אמצע**, לפני ההרשאות |
| gius | כרטיס «☁️ מצב סנכרון» נפרד | נבנה בכל render() — **`techInfoMount` הייתה קוד מת** |

כלומר: קוד זהה, שלוש חוויות שונות, ובאחת מהן הרכיב לא היה מחובר בכלל.
בגיוס הייתה לזה גם תוצאה גלויה — מסך ההגדרות נבנה מחדש בכל `render()`,
העוגן חזר ריק, ו«מידע טכני» **נסגר בכל שמירה**.

### ההגדרה המחייבת — «אזור מצב»
בתחתית מסך (או מודול) ההגדרות של כל אפליקציה בארגון יושבים **תמיד, ובסדר
הזה, שני האלמנטים האחרונים** — חשופים, לא עטופים בכרטיס של האפליקציה:

1. **`<div id="sync-status-box">` — בלוק «☁️ סנכרון».** מציג את מסך ה-⏳
   הקיים (`pendStatusHTML`, **בלי שינוי בתוכנו**) ומתחתיו שורת מצב תמציתית:
   מצב חיבור, מועד הסנכרון המוצלח האחרון, וכפתור «🔄 סנכרן עכשיו».
2. **`<div id="tech-info-box">` — רכיב «מידע טכני ›».** האלמנט האחרון במסך.

שניהם מורכבים בקריאה אחת — **`statusAreaMount()`** — וזו נקודת החיווט
היחידה. מסך הגדרות שאינו קורא לה הוא מסך בלי אזור מצב, והבדיקה נכשלת.

### שלושת הבלוקים המשותפים
זהים בית-לבית בארבע האפליקציות, וכל שינוי בהם נעשה בארבעתן באותו סבב:

| בלוק | סמנים | חתימה (sha256, 16 תווים) |
|---|---|---|
| CSS אזור המצב | `/* ═══ אזור מצב — CSS משותף (סבב 15/17)` … `/* ═══ סוף CSS אזור המצב` | `aa10b52e9ce1157e` |
| «מידע טכני» | `/* ═══ "מידע טכני" — קיפול מסכי האחסון` … `/* ═══ סוף רכיב "מידע טכני"` | `83096d1117e813f0` |
| «☁️ סנכרון» | `/* ═══ אזור מצב — בלוק "☁️ סנכרון"` … `/* ═══ סוף בלוק "☁️ סנכרון"` | `65ff4f75bc4f6756` |

**ה-API:** `syncFmtTime` · `syncLineHTML` · `syncStatusHTML` ·
`syncStatusMount` · `syncNowFromStatus` · `statusAreaMount` ·
`techInfoHTML`/`techInfoToggle`/`techInfoMount`.

מעל הבלוק יושבת **`SYNC_CFG`** — הדבר היחיד שנבדל בין האפליקציות — עם שני
שדות בלבד: `lastSync()` (חותמת הסנכרון המוצלח האחרון) ו-`sync()` (מה
שהכפתור מפעיל, מחזיר Promise).

### ⚠️ שלוש הבהרות שאין לפספס
1. **הכפתור «סנכרן עכשיו» אינו מנגנון סנכרון.** הסנכרון האוטומטי ממשיך
   לרוץ ברקע בדיוק כמו קודם; זו רק דרך לבקש אותו עכשיו ולראות תשובה. לכן
   גם **אין בבלוק טוסט כשל משלו** — מסלול הסנכרון של כל אפליקציה כבר מדווח
   על כשל פעם אחת, וכפילות הייתה רעש.
2. **`lastSync()` אינו עֵד דחיפה ואינו משמש לפינוי.** הוא חותמת תצוגה,
   שנכתבת על כל שיחה מוצלחת עם הענן — כולל בדפדפן שרק קורא. לפינוי יש
   `_ysPushedAt`/`_tbPushedAt`/`syncedThrough` (סבבים 11 ו-14), **ואין
   לערבב ביניהם.** ⛔ אין להזין `LS_CFG.syncedThrough` מ-`lastSync()`.
3. **הבלוק אינו נשען על מחלקות הכרטיס של אף אפליקציה** — הן נבדלות ביניהן
   — אלא מרנדר חלונית משלו (`.sync-box`), וה-CSS יורש צבע וגופן. זה מה
   שמאפשר לו להישאר זהה בית-לבית.

### מצב הפתיחה של «מידע טכני» חי בזיכרון
`_techInfoOpen` (משתנה מודול, **לא** localStorage) שומר אם הרכיב פתוח.
בלעדיו, אפליקציה שבונה את מסך ההגדרות מחדש בכל רינדור מקבלת עוגן ריק,
ה-`details` הקודם כבר לא קיים, והרכיב נסגר בכל שמירה ובכל ציור מושהה של
⏳ (סבב 16). המצב בזיכרון בכוונה: זו העדפת תצוגה של הרגע, ולא נתון שראוי
לצרוך ממנו מכסת אחסון משותפת.

### הודעת חזרת הסנכרון — נוסח אחד
**`MSG_SYNC_BACK = '☁️ הסנכרון חזר לפעול'`** בארבע האפליקציות, לצד
`MSG_OFFLINE`/`MSG_SAVED`/`MSG_BAD_LOGIN`. הוא החליף את «☁️ החיבור לנתונים
חזר» ב-schar-limud וב-gius, ונוסף ל-yoman שלא הייתה לה הודעת התאוששות
בכלל. ⛔ **אין להשתמש במחרוזת קשיחה** — הבדיקה נכשלת עליה.

### הבדיקה האוכפת
`tools/check-status-area.mjs` (ב-gius: `scripts/`) — **זהה בית-לבית
בארבעת הריפו פרט לבלוק `APP` שבראשו**, ורצה עם שערי התחביר לפני כל דחיפה.
היא נכשלת על שלושת סוגי הסטייה:
- **א. מיקום** — `sync-status-box` ו-`tech-info-box` אינם שני ה**ילדים
  הישירים** האחרונים של מיכל ההגדרות, בסדר הזה. ההשוואה היא על ילדים
  ישירים דווקא: עוגן שיושב בתוך כרטיס היה עובר בדיקה רופפת («שני המזהים
  מופיעים בסוף») גם כשמשהו אחר מוצג אחריו. ב-gius, שבונה את המסך כמחרוזת,
  הבדיקה היא על זנב `viewSettings()`.
- **ב. קוד** — אחד משלושת הבלוקים אינו תואם לחתימה שבטבלה למעלה.
- **ג. חיווט** — `statusAreaMount()` אינה נקראת בשום מקום מחוץ לבלוק
  המשותף, כלומר הרכיב קיים אך אינו מחובר. זו בדיוק התקלה של gius.
  בנוסף נכשלת על שריד `id="pend-status-box"` מחוץ לאזור המצב.

**שינוי מכוון בבלוק משותף** = עדכון הבלוק בארבע האפליקציות **ובארבעת
עותקי קובץ הבדיקה** (החתימות כתובות בתוכו), באותו סבב.

### תיאום גרסאות
`sw.js`: hanhala **v33** · yoman **v21** (`app-version` **15-2026-08-12-1**) ·
schar **v23** · gius **gius-v9**.
<!-- SHARED:end -->

### היישום כאן (yoman-avoda)
- אזור המצב יושב בסוף `panel-settings`; `renderSettings()` קורא
  `statusAreaMount()`.
- **`_tbLastSyncAt` + `_tbMarkSynced()`** — חותמת תצוגה על כל שיחה מוצלחת
  עם הענן (`autoSyncData` ו-`syncFromCloud`). ⛔ **אינה `_tbPushedAt`**
  ואינה נוגעת בפינוי (סבבים 11 ו-14).
- **טוסט חזרת הסנכרון** (`MSG_SYNC_BACK`, דגל `_tbNetWarned`) — הכישלון
  נשאר שקט בכוונה (פולינג כל 3 שניות; טוסט על כל מחזור היה רעש), אבל
  החזרה נאמרת: זה הרגע שבו מה שהמתין מתחיל לעלות.

---

<!-- SHARED:start id="iron-rule-8-docs" -->
## ⭐ כלל ברזל 8 — פרוטוקול תיעוד קבוע (סבב 18, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את שבעת כללי הברזל שלמעלה.

> **התיעוד עצמו הוא רכיב משותף, ולכן חלים עליו אותם תנאים שכלל ברזל 7
> קבע לקוד משותף: אותו נוסח, במקום קבוע, ומסומן במפורש. פרק שאינו מסומן
> «משותף» הוא פרטי לאפליקציה — ואין לגעת בו על סמך נוסח מריפו אחר.**

### הבעיה שנמדדה
ביקורת חיצונית סרקה את ארבעת קבצי ה-CLAUDE.md ומצאה עשרות פרקים שאמורים
להיות זהים. רובם אכן היו — אבל שום מנגנון לא אכף את זה, ולכן ארבעה נסחפו
בלי שאיש שם לב. באחד מהם הסחיפה הייתה **הפוכה**: פרק «בדיקה» של סבב 14
הועתק כלשונו לארבעת הריפו, כולל לשתי אפליקציות שבהן הבדיקות שהוא מתאר
מעולם לא רצו ולא יכלו לרוץ (אין בהן `verify` ואין `tier2`). תיעוד
שמועתק «כי כך כתוב אצל האחות» גרוע מתיעוד חסר: הוא נקרא כעדות.

### ההגדרה המחייבת — סימון משותף/פרטי
כל פרק שאמור להיות זהה בארבעת הריפו עטוף בסימוני HTML-comment:

```html
<!-- SHARED:start id="שם-מזהה-קצר" -->
…
<!-- SHARED:end -->
```

**הסימון הוא מקור האמת:**
- מה שבתוך הסימון — **חייב** להיות זהה בית-לבית בארבעת הריפו.
- מה שמחוצה לו — **פרטי לאפליקציה**, ואיש אינו מיישר אותו.

הסימון עוטף **רצף** של פרקים, לא בהכרח פרק בודד: פרק סבב שכולו משותף
פרט לזנב פר-אפליקציה (למשל שורת «**כאן:**» שבסוף «מזהים», או «מה נעשה
כאן») נחתך בדיוק שם — הרצף המשותף בפנים, הזנב בחוץ.

### שבעת הכללים
1. **כל סבב מוסיף פרק סבב במקום קבוע ובמבנה קבוע** — בסוף הקובץ, אחרי
   פרק הסבב הקודם, עם שלושת התת-פרקים ובסדר הזה: **מה נעשה** (משותף) ·
   **מה נעשה כאן** (פר-אפליקציה) · **אימותים** (פר-אפליקציה).
2. **כל סבב מקדם את שורת «עודכן לאחרונה»** שבראש הקובץ, בפורמט הקבוע
   `עודכן לאחרונה: סבב N · YYYY-MM-DD`. גם סבב שנגע רק בקובץ אחד מקדם
   אותה בקובץ שבו נגע.
3. **סבב שנוגע בקוד משותף חייב לעדכן את הפרק המשותף בארבעת הריפו באותו
   סבב** — לעולם לא באחד בלבד. פרק מסומן SHARED שמשתנה, משתנה בארבעתם,
   באותו שינוי בדיוק, ובאותו קומיט-סבב.
4. **⛔ אסור לסשן לשנות תוכן של פרק שאינו מסומן SHARED על סמך נוסח מריפו
   אחר.** זה בדיוק מה שייצר את באג «84 הטענות»: טקסט נכון באפליקציה אחת
   שהועתק לשלוש שבהן הוא שקרי. אם נראה שפרק פרטי «חסר» משהו שיש לאחות —
   זו אינדיקציה לבדוק את **המציאות באפליקציה הזו**, לא להעתיק.
5. **«בדיקה» ו«חתימת APK» הם פרקים פרטיים בהגדרה.** «בדיקה» מתאר מה
   באמת רץ באפליקציה הזו ולכן הוא נבדל בהכרח; «חתימת APK» מחזיק
   keystore, alias וטביעת SHA ייחודיים — **איחוד שלו יהיה שגוי ומסוכן**,
   ולכן הבדיקה נכשלת אם הוא סומן SHARED.
6. **סבב שכותב בדיקות שומר אותן כקובץ בריפו.** רתמה שרצה בסשן בלבד
   נעלמת ברגע שהסשן נסגר — הסשן הבא אינו יכול להריץ אותה, לא לפני שינוי
   ולא אחריו, ולכן היא אינה מגינה על דבר.
   **⛔ ובפרט: אין לתעד בדיקות כאלה כאילו הן קיימות.** «N טענות עברו»
   בפרק סבב, בלי קובץ שאפשר להריץ, נקרא כעדות שאינווריאנטה שמורה — בזמן
   שאין שום דבר ששומר עליה. זה בדיוק הכשל של סעיף 4, בכיוון אחר: תיעוד
   שמתאר מציאות שאיננה.
   ⚠️ הכלל חל על **בדיקות** — טענות שניתן להריץ מחדש. סריקה חד-פעמית או
   מדידה שנעשתה כדי להכריע החלטה אינן בדיקות; הן נרשמות כמדידה, ואין
   להציג אותן כשער.
7. **⛔ אין לזרוע פרטי כניסה — שם משתמש, סיסמה, מפתח, טוקן — בשום קובץ
   שנדחף לגיט.** לא בסכימה, לא במיגרציה, לא בקוד, לא ב-README ולא בקובץ
   בדיקה. ארבעת הריפו **ציבוריים**, ומפתחות ה-anon של Supabase יושבים
   ב-`index.html` — כלומר סיסמה שנדחפה היא סיסמה שכל אחד יכול להשתמש בה
   מול המסד החי.
   ⚠️ **ומחיקה מאוחרת אינה מסירה אותה.** הערך נשאר קריא בהיסטוריית ה-git
   לנצח, ומי שכבר מצא אותו מחזיק בו. הטיפול היחיד בסוד שנדחף הוא
   **להחליף אותו במסד**, ורק אחר כך לנקות את הקובץ.
   המשתמש הראשון נוצר **ידנית**, וקובץ ההתקנה מסביר איך — עם מצייני מקום
   ולא עם ערכים אמיתיים.

### הבדיקה האוכפת
`tools/check-docs.mjs` (ב-gius: `scripts/`, ו-`check-js.mjs` קורא לה) —
**זהה בית-לבית בארבעת הריפו פרט לבלוק `APP` שבראשה**, ורצה עם שערי
התחביר לפני כל דחיפה. היא נכשלת על חמישה סוגי סטייה:
- **א. סימון** — `SHARED:start`/`SHARED:end` אינם מאוזנים, מקוננים, בלי
  `id`, עם `id` כפול, או שרשימת המזהים בקובץ אינה תואמת לרשימה הקנונית.
- **ב. תוכן** — תוכנו של בלוק SHARED אינו תואם לחתימת ה-sha256 הקנונית
  הרשומה בקובץ הבדיקה.
- **ג. שורת העדכון** — «עודכן לאחרונה» חסרה, אינה בראש הקובץ, או בפורמט
  שגוי.
- **ד. פרק פרטי שסומן** — «חתימת APK» או «בדיקה» נמצאים בתוך בלוק
  SHARED, בניגוד לסעיף 5.
- **ה. שורת עדכון שלא קודמה** (סבב 20) — `CLAUDE.md` שונה מול
  `origin/main` אבל שורת «עודכן לאחרונה» זהה לזו שב-`origin/main`.
  סעיפים א–ד בודקים את **צורת** השורה; זה בודק שהיא באמת **קודמה**.
  ⚠️ **הסעיף הזה מדלג ואינו מפיל** כשאין git, כשאין `origin/main`, או
  כש-`CLAUDE.md` אינו קיים שם (סבב 20).

סימונים שבתוך גדר קוד (```) אינם נספרים — אחרת הדוגמה שבפרק הזה עצמו
הייתה נקראת כבלוק אמיתי.

**שינוי מכוון בפרק משותף** = עדכון הפרק בארבעת הריפו **ובארבעת עותקי
קובץ הבדיקה** (החתימות כתובות בתוכו), באותו סבב. אותו נוהל בדיוק שכלל
ברזל 7 קבע לבלוקי הקוד המשותפים.
<!-- SHARED:end -->

---

<!-- SHARED:start id="iron-rule-9-security-spread" -->
## ⭐ כלל ברזל 9 — שדרוג אבטחתי מתפשט (סבב 21, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את שמונת כללי הברזל שלמעלה.

> **כשסבב מחליף מנגנון אבטחה באפליקציה אחת — טיפול בסודות, אימות, הצפנה,
> הרשאות — הוא חייב לרשום במפורש, בפרק הסבב, האם המנגנון החדש רלוונטי גם
> לשאר האפליקציות. רלוונטי ולא יושם ⇐ נרשם פער מתועד עם טריגר.
> שקט אינו תשובה.**

### הבעיה שנמדדה
סבב 13 העביר את schar-limud מהשוואת סיסמה גלויה ל**טביעת PBKDF2-SHA256**.
ההחלטה שם נכונה ומנומקת: מיגרציה 009 הורידה את `sl_settings` למראה המקומי,
ו-`admin_pass` בטקסט גלוי **על הדיסק** לא היה מתקבל על הדעת.

מה שלא נעשה: **לא נרשם באותו סבב אם המנגנון רלוונטי גם לשלוש האחיות.**
לא «כן והוחל», לא «לא, וזו הסיבה» — פשוט כלום. התוצאה היא ש-hanhala
ו-gius המשיכו שמונה סבבים עם מודל סיסמאות אחר לגמרי, **בלי שאיש החליט
על כך** — ההבדל התגלה רק בביקורת של סבב 21. תיעוד שותק אינו מתעד החלטה;
הוא מסתיר את היעדרה.

### שלוש התשובות המותרות — ורק שלוש
סבב שנגע במנגנון אבטחה חייב לרשום **אחת** מהן, במפורש:
1. **הוחל בכולן** — עם ציון מה השתנה בכל אחת.
2. **אינו רלוונטי לאחרות** — עם **הנימוק**, לא כהצהרה. «אין שם סוד על
   הדיסק» הוא נימוק; «לא נדרש» אינו.
3. **רלוונטי, ולא יושם** — ואז **פער מתועד עם טריגר**: מה חסר, מה הסיכון,
   ו**מה האירוע שמחייב לפתוח את הסבב** (הדוגמה
   ההיסטורית: פער «הוספת משתמשים ל-gius» — נרשם בסבב 22 ונסגר בסבב 23).

⛔ **מה שאסור הוא הרביעית — לא לכתוב כלום.**

### ⚠️ הכלל דורש **רישום**, לא יישור — ואינו גובר על כלל ברזל 8 סעיף 4
הרישום נעשה **בפרק הפרטי של כל אפליקציה, לפי המציאות שלה**. כלל ברזל 9
**אינו** רישיון להעתיק נוסח מריפו אחר, ו**אינו** מחייב שהמנגנונים יהיו
זהים: אפליקציות שונות חיות במודלי איום שונים, ואחידות מנגנון אינה מטרה
בפני עצמה. שני הכללים אומרים יחד דבר אחד: **מה שנבדל — נבדל בכוונה
ובכתב.**

### ⛔ הלקח נרשם בארבעתן — גם כשהמנגנון רלוונטי רק לאחת (סבב 26)
**לקח אבטחתי שנלמד באפליקציה אחת נרשם כהערת ⛔ בארבע האפליקציות**, ליד
המקום בקוד שבו הוא עלול להישבר — ולא רק באפליקציה שבה הוא נלמד.

⚠️ **וזו אינה כפילות של שלוש התשובות שלמעלה.** הרישום שהן מחייבות הוא
**החלטה**: «הוחל» / «אינו רלוונטי, וזה הנימוק» / «פער עם טריגר», והוא
יושב בפרק הפרטי של כל אפליקציה. ההערה בקוד עונה על שאלה אחרת לגמרי —
**מה אסור לעשות כאן בעתיד.** אפליקציה שהמנגנון «אינו רלוונטי» לה עדיין
חשופה ל**כשל** שהוליד אותו: סשן עתידי יוסיף בתום לב בדיוק את הדבר
שהוסר במקום אחר, מפני שאצלו הוא מעולם לא היה — ולכן גם מעולם לא נכתב
אצלו שאסור.

> **הערה שיושבת במקום אחד אינה מגינה על הארגון.** היא מגינה על הקובץ
> שהיא יושבת בו, ועל שום קובץ אחר.

**הדוגמה החיה** היא הסבב שהוליד את הסעיף: שער הסיסמה הנפרד של מסך
ההגדרות הוסר מ-schar-limud (סבב 26), ובשלוש האחיות שער כזה לא היה
מעולם — כלומר לפי שלוש התשובות, «אינו רלוונטי». ובכל זאת ההערה נוספה
**בארבעתן**, בראש אזור ההגדרות: לכל אחת מהן יש מסך הגדרות, וכל אחת מהן
היא מקום שבו אפשר להמציא את השער הזה מחדש.

### ⛔ החלטת מודל אבטחה היא של המנהל, לא של הסשן
מנגנון שנבחר במפורש ע"י המנהל — כולל בחירה **לא** להקשיח — אינו «חוב
טכני» שסשן עתידי מוסמך לסגור מיוזמתו. סבב שמוצא פער מתועד **מדווח עליו
ומציע**; ההגירה עצמה מחייבת החלטה מפורשת. ר' הדוגמה החיה בפרקי הסיסמאות
של hanhala ושל gius: שם ההחלטה על טקסט גלוי היא **בחירה תפעולית מודעת**,
והמרתה ל-PBKDF2 בלי אישור הייתה **שוברת יכולת ניהול שהמנהל בחר בה בכוונה**.
<!-- SHARED:end -->

**הרישום הנדרש כאן (yoman-avoda):** ⛔ ליומן אין שער סיסמה, אין טבלת
משתמשים ואין תפקידים — הכניסה היא בחירת מוסד, ואין סוד להשוות מולו; מנגנוני
הסיסמאות של האחיות אינם רלוונטיים, ואין פער ואין טריגר. ⚠️ אבל «אין
משתמשים» ≠ «אין הרשאות עודפות»: צמצום הרשאות המפתח הציבורי (סבב 29,
`migrations/001`) חל גם כאן — `kv_rishon`/`kv_ramataviv` מחזיקות
`INSERT, SELECT, UPDATE` בלבד לשני התפקידים, ומוסד שלישי עתידי מחייב
`revoke` משלו.

---

<!-- SHARED:start id="iron-rule-10-users" -->
## ⭐ כלל ברזל 10 — מודל המשתמשים וההרשאות בארגון (סבב 26, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את תשעת כללי הברזל שלמעלה.

> **אפליקציה בארגון שיש בה משתמשים נבנית לפי שמונת הסעיפים שלמטה, מהיום
> הראשון.** הדפוס הזה לא תוכנן מראש — הוא התגבש בסבבים 13–26, סעיף אחרי
> סעיף, כל אחד מהם אחרי כשל שנמדד בפועל. הניסוח כאן נועד לדבר אחד:
> שהאפליקציה **הבאה** לא תעבור את אותה סדרה מחדש.

### ⚠️ למה זה כלל ולא פרק תיעוד
מי שבונה כאן אפליקציה חדשה **מעתיק ריפו קיים** — הוא לוקח את המודול
המשותף, את `LS_CFG`, את אזור המצב ואת מבנה `index.html`, ומתחיל לעבוד.
הוא **אינו** קורא שלושים פרקי סבבים היסטוריים כדי לגלות למה `role` הוא
בלי `DEFAULT` או למה הסיסמה אינה יורדת לדיסק. **דפוס שחי רק בפרקי
היסטוריה אינו עובר בהעתקה** — הוא נשאר מאחור, והאפליקציה החדשה נולדת עם
בדיוק הכשלים שכבר שילמנו עליהם. כלל ברזל כן עובר, מפני שהוא נקרא לפני
שכותבים ולא אחרי שנשבר.

### שמונת הסעיפים
לצד כל סעיף רשום **הסבב שבו נלמד** — הרקע המלא נמצא שם, ואין צורך לחפש.

1. **טבלת משתמשים: `role TEXT NOT NULL`, ⛔ בלי `DEFAULT`.**
   *(נלמד בסבב 26 והושלם בהשלמה שלו.)* יצירת משתמש בלי תפקיד **נכשלת
   במסד**, וזו ההתנהגות הרצויה: **תפקיד הוא החלטה, ולא ערך שנופל
   מאליו.** ברירת מחדל ש**מעניקה** הרשאה היא הכשל החמור מבין השניים,
   אבל גם ברירת מחדל שנכשלת סגור סוטה מהדפוס.
   ⚠️ **הערכים עצמם אינם משותפים ואין ליישר אותם** — כל אפליקציה ומודל
   ההרשאות שלה, בטבלה נפרדת ולעיתים בפרויקט Supabase נפרד.

2. **⛔ אין שער סיסמה נפרד מעל מסך ההגדרות.** *(נלמד בסבב 26.)* ההרשאה
   נגזרת **מהמשתמש המחובר בלבד**. סיסמה שנייה לאותו אדם אינה שכבת
   אבטחה נוספת — היא מנגנון שני עם מסך משלו, מסלול שינוי משלו ותחזוקה
   משלו, ובהתקנה טרייה היא נוטה ליפול חזרה לערך ברירת מחדל ולהיפתח לכל
   מי שמנחש אותו. ⚠️ הכלל חל גם על אפליקציה שאין בה משתמשים כלל: שם שער
   כזה אינו «יישור לאחיות» אלא **שכבת הרשאה שלמה יש מאין**.

3. **⛔ אין לזרוע משתמש, סיסמה או תפקיד בשום קובץ שנדחף לגיט.**
   *(נלמד בסבב 21 בהנהלה, ובסבב 24 ובהשלמתו ב-schar-limud וב-gius.)*
   לא בסכימה, לא במיגרציה, לא בקוד, לא ב-README ולא בקובץ בדיקה. הריפו
   **ציבורי**, ומפתחות ה-anon יושבים ב-`index.html` — כלומר סוד שנדחף
   הוא סוד שכל אחד יכול להשתמש בו מול המסד החי.
   ⚠️ **ומחיקה מאוחרת אינה מסירה אותו:** הערך נשאר קריא בהיסטוריית ה-git
   לנצח. הטיפול היחיד בסוד שנדחף הוא **להחליף אותו במסד**, ורק אחר כך
   לנקות את הקובץ. המשתמש הראשון נוצר **ידנית**, וקובץ ההתקנה מסביר
   איך — עם מצייני מקום ולא עם ערכים.

4. **הסיסמה גלויה בענן, ⛔ אך לעולם אינה יורדת למכשיר.**
   *(נלמד בסבב 13 ב-schar-limud, והורחב בסבבים 22–24.)* הטקסט הגלוי
   בענן הוא **החלטה תפעולית מתועדת** — המנהל צריך לראות ולנהל סיסמאות —
   ולא פער שממתין לסגירה; ⛔ שינוי שלה הוא החלטת המנהל, לא של הסשן
   (כלל ברזל 9). מה שיורד למכשיר הוא **`pass_salt` + `pass_fp` בלבד**:
   PBKDF2-SHA256, **100,000 סיבובים**, **מלח אקראי פר-משתמש**, ועם
   **קידומת הקשר פר-אפליקציה** בתוך המלח — ה-origin משותף לארבע
   האפליקציות, ובלי הפרדת הקשר טבלה מחושבת מראש עבור אחת מהן תקפה
   לכולן.
   ⛔ **כישלון גזירה מאפס את שני השדות ולא מדלג על הכתיבה** — טביעה
   ישנה ששורדת שינוי סיסמה ממשיכה לפתוח את הכניסה האופליין עם הסיסמה
   **הקודמת**, לנצח.

5. **כל נתיבי הכתיבה למראת המשתמשים עוברים דרך פונקציית סינון אחת.**
   *(נלמד בסבב 23 ב-gius, וחזר בסבב 26 שם.)* **רשימת היתר** ולא רשימת
   איסור — רשימת היתר נשארת נכונה גם כשתיווסף לטבלה עמודה רגישה חדשה.
   ⛔ **ואין נתיב שני שעוקף אותה.** זו הנקודה שנשברה בפועל יותר מפעם
   אחת: מסנן שיושב במשיכה בלבד, וכתיבה מקומית שכותבת את הסוד לדיסק
   מסביבו. כל נתיב חדש — משיכה, כתיבה מקומית, עדכון חלקי, מיגרציה,
   ייבוא — חייב לעבור דרך אותה פונקציה, ורצוי שגם שער הדיסק יסנן שוב
   כשכבה שנייה.

6. **כניסה אופליין מרובת-משתמשים היא דרישה, לא תוספת.**
   *(נלמד בסבב 22 בהנהלה, בסבב 23 ב-gius ובסבב 24 ב-schar-limud.)*
   **כל משתמש נכנס בכל מכשיר, גם בלי רשת** — מול הטביעה שבסעיף 4, ולא
   מול הענן. מטמון שמחזיק רק את המשתמש האחרון שנכנס **שובר את הדרישה**:
   מי שקיבל מכשיר שמישהו אחר עבד בו נעול בחוץ.
   ⚠️ **וההודעות נבדלות זו מזו.** «אין לך במכשיר במה לאמת» אינו «סיסמה
   שגויה»: ⛔ הודעה מאוחדת היא כשל שקט שמוביל את המשתמש להקליד שוב ושוב
   סיסמה נכונה לחלוטין, במקום לומר לו את הדבר היחיד שיפתור — חיבור אחד.

7. **קובץ התקנה שמוכרז «אידמפוטנטי» חייב שורת התכנסות לכל שינוי מבני —
   ולא רק `create table if not exists`.** *(נלמד בסבב 27.)*
   `create table if not exists` **מדלג על טבלה קיימת, ועל כל מה
   שבתוכה**: עמודה שנוספה אחרי ההתקנה הראשונה, אינדקס, טריגר, פוליסה,
   אילוץ, או `drop default`. כלומר התקנה קיימת שמריצה את הקובץ מחדש
   מקבלת «רץ בהצלחה» — **ונשארת בלי השינוי**, בשקט.
   לכן לכל שינוי מבני שנעשה מאז ההתקנה הראשונה יושבת בקובץ **גם** שורת
   התכנסות: `add column if not exists` · `create index if not exists` ·
   `create or replace` לטריגר · `drop policy if exists` + `create policy` ·
   `alter column … drop default` · `revoke`+`grant` מפורשים.
   ⛔ **ואין לגעת בנתונים — מבנה בלבד.** ה-`UPDATE` היחיד שמותר הוא
   מילוי עמודה שזה עתה נוספה, ורק ב-`WHERE <col> IS NULL`.
   ⚠️ **הסעיף יושב דווקא כאן** מפני שקובץ ההתקנה הוא המקום שבו נולדים
   טבלת המשתמשים, ה-`role` והטביעה — כלומר בדיוק שלושת הסעיפים
   הראשונים שלמעלה. התקנה שהריצה את הקובץ מחדש ולא קיבלה את
   `pass_salt`/`pass_fp` או את `role` היא התקנה **בלי כניסה אופליין
   ובלי הרשאות**, בלי שאיש יידע.

8. **⛔ האיסור על זריעת פרטי כניסה חל על כל סוגי הקבצים — לא רק על
   מיגרציות.** *(נלמד בסבב 27.)* SQL · HTML · Markdown · README ·
   קובץ בדיקה · פיקסטורה · כל קובץ שנדחף לגיט. ⛔ **וגם ערך «לדוגמה»
   אסור** — מציין מקום בלבד, לעולם לא סיסמה אמיתית ולא סיסמה גנרית
   שמישהו עלול להקליד.
   ⚠️ **הרקע, וזה מה שהופך את הסעיף לנחוץ:** סעיף 3 כבר אסר את זה
   מסבב 21 — ובכל זאת סוד שרד בהנהלה עד סבב 27, ב-`setup-db.html`,
   מפני שהחיפוש שאישר את «אין בריפו פרטי כניסה» נעשה **בקבצי SQL
   ובקוד בלבד**. כלי עזר ב-HTML לא נסרק, והצהרת סבב 24 נכתבה על סמך
   סריקה חלקית. ⛔ **סריקה שמוגבלת לסוג קובץ אחד אינה ראיה** —
   היא ראיה לסוג הקובץ שנסרק, ותו לא.

9. **`anon` ו-`authenticated` מקבלים `SELECT`, `INSERT`, `UPDATE` — ותו
   לא.** *(נלמד בסבב 29.)* ⛔ **אין להעניק להם `DELETE` או `TRUNCATE`.**
   מחיקה בארגון היא **תמיד סימון** — `deleted=true` + חותמת, או tombstone
   בתוך הערך — ו**לעולם לא הסרת שורה** (כלל ברזל 6 סעיף 1). כלומר ההרשאה
   מיותרת בהגדרה, ובתמורה היא פותחת למי שמחזיק את המפתח הגלוי
   שב-`index.html` דרך למחוק שורות ואף לרוקן טבלה שלמה.
   ⚠️ **והכלל חל על שני התפקידים גם כשאין משתמשי Auth:** `authenticated`
   קיים בכל פרויקט Supabase ומחזיק את אותן הרשאות בירושה, ולכן פתיחת
   signup או Auth בעתיד הייתה פוערת את ההגנה **בשקט**. ⛔ `service_role`
   אינו בכלל הזה — הוא תפקיד השרת ואינו נגיש מהדפדפן.
   ⚠️ **וזה אינו מתקיים מאליו:** פרויקט Supabase סטנדרטי מגיע עם
   `alter default privileges … grant all on tables to anon, authenticated`,
   ולכן **כל טבלה נולדת עם `DELETE` ו-`TRUNCATE`**. ⛔ **`GRANT` הוא
   אדיטיבי ואינו מסיר דבר** — נדרש `REVOKE` מפורש, וכל מיגרציה שמוסיפה
   טבלה חייבת אחד משלה.
   ⛔ **וטבלאות יומן הן החריג, בכיוון ההפוך:** `sync_log` ו-`kv_backup`
   מקבלות `INSERT` ו-`SELECT` בלבד — **בלי `UPDATE` ובלי `DELETE`, לשני
   התפקידים כאחד** — כדי שלא ניתן יהיה לזייף רישום קיים ביומן ראיות ולא
   להעלים אותו. ⚠️ אין «ליישר» אותן לשאר הטבלאות בשם האחידות: הסט הצר
   שם **הוא** ההגנה, ו-`UPDATE` נחוץ בשאר הטבלאות רק מפני שהכתיבה שם
   היא upsert.
   ⛔ **ובדיקת הרשאות סורקת את שני התפקידים, תמיד** *(נלמד בהשלמת סבב
   29)* — **הרשאה שהוחלה על תפקיד אחד בלבד היא הגנה חלקית.** זה נמדד
   כאן בפועל: שתי טבלאות היומן החזיקו `INSERT, SELECT` ל-`anon` ואת
   הסט **המלא** ל-`authenticated`, ולכן ההגנה שתועדה כקיימת התקיימה מול
   מפתח אחד בלבד. ⚠️ סריקה שכיסתה תפקיד אחד היא ראיה לתפקיד שנסרק,
   ותו לא — בדיוק כמו סעיף 8 לגבי סוגי קבצים.
<!-- SHARED:end -->

**כאן:** ליומן אין משתמשים, ולכן רק סעיף 2 חל בפועל — ⛔ אין להוסיף שער
סיסמה מעל ההגדרות (שער כזה כאן היה שכבת הרשאה שלמה יש מאין; הערת ההגנה
יושבת בקוד מעל `renderSettings()`). ⚠️ אם אי-פעם יתווספו משתמשים — הכלל
הזה הוא הדבר שיימצא בקובץ, במקום סדרת הסבבים שהאחיות עברו.

---

<!-- SHARED:start id="capability-matrix" -->
## ⭐ מטריצת היכולות של ארבע האפליקציות (סבב 27, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius).

> **מה שנבדל בין האפליקציות — נבדל בכתב, בטבלה אחת.** סבבים 17–26 חזרו
> ומצאו שוב ושוב הבדלי יכולת שאיש לא החליט עליהם ואיש לא רשם אותם: אחת
> עובדת אופליין והשנייה לא, לאחת כניסה מרובת-משתמשים ולשנייה מטמון של
> משתמש אחד, לאחת קובץ התקנה ולשנייה שדרוגים בלי מקור אמת. בכל אחד
> מהמקרים ה**גילוי** עלה סבב שלם. התשובה לשאלה «מה ההבדל בין
> האפליקציות» צריכה להיות **כתובה**, לא משהו שמחפשים בקוד.

⚠️ **הטבלה נמדדה מהקוד ולא הוצהרה** — כל תא אומת מול `index.html`,
`sw.js`, `migrations/`, `signing/` ו-`android/` של האפליקציה שלו.
⛔ **ומאותה סיבה אין לעדכן אותה «לפי ההיגיון»:** סבב שמשנה יכולת מודד
מחדש את התא שהוא שינה ומעדכן אותו **בארבעת הקבצים באותו סבב** (כלל
ברזל 8 סעיף 3).

### שלושת הערכים — ומה ההבדל ביניהם
- **✅** — היכולת קיימת ופעילה.
- **❌** — היכולת **חסרה** באפליקציה שהיא כן רלוונטית לה. כל ❌ נושא
  **נימוק**, ואם הוא פער — גם **טריגר**: האירוע שמחייב לפתוח סבב.
- **לא רלוונטי** — אין לאפליקציה הזו את המושג שהיכולת מדברת עליו, ולכן
  אין מה לממש. ⛔ זה **אינו** «❌ מנומס»: ❌ אומר «חסר»; «לא רלוונטי»
  אומר «אין כאן שאלה כזו».

| # | יכולת | yoman-avoda | hanhala-ruchanit | schar-limud | gius |
|---|---|---|---|---|---|
| 1 | כניסה אופליין | לא רלוונטי | ✅ | ✅ | ✅ |
| 2 | עריכת נתונים אופליין | ✅ | ✅ | ✅ | ✅ |
| 3 | מודל משתמשים | אין | רב-משתמשים | רב-משתמשים | רב-משתמשים |
| 4 | נתיב עדכון חלקי למראת המשתמשים | לא רלוונטי | ✅ | ❌ | ✅ |
| 5 | סוד במכשיר | אין | טביעה | טביעה | טביעה |
| 6 | פינוי אוטומטי | ✅ | ✅ | ❌ | ✅ |
| 7 | אימות פינוי מול הענן | ✅ | ✅ | לא רלוונטי | ❌ |
| 8 | שיתוף קבצים | ✅ | ❌ | ❌ | ❌ |
| 9 | מעטפת APK (WebView) | ✅ | ✅ | ✅ | ✅ |
| 10 | מפתח חתימה קבוע בריפו | ✅ | ✅ | ✅ | ✅ |
| 11 | מקור אמת יחיד לסכימה | ✅ | ✅ | ✅ | ✅ |
| 12 | קובץ התקנה מלא | ✅ | ✅ | ✅ | ✅ |
| 13 | גיבוי יומי אוטומטי | ✅ | ✅ | ✅ | ✅ |
| 14 | יומן פעולות | ✅ | ✅ | ✅ | ✅ |
| 15 | נתונים בטבלאות מובנות | ✅ | ❌ | ✅ | ✅ |
| 16 | שער תקינות JS | ✅ | ✅ | ✅ | ✅ |
| 17 | חלון חם במכשיר | ✅ | ❌ | ✅ | ✅ |
| 18 | שחזור מקומי מהענן | ✅ | ✅ | ✅ | ✅ |
| 19 | מסך שינוי סיסמה עצמי | לא רלוונטי | ✅ | ❌ | ✅ |
| 20 | מטמון-CDN מראש עם ריפוי עצמי | ✅ | ✅ | ✅ | ✅ |
| 21 | גיבוי יומי מטבלאות מובנות | ✅ | ❌ | ✅ | ✅ |
| 22 | פינוי גיבויים אוטומטי במסד | ✅ | ✅ | ✅ | ✅ |
| 23 | מנוע מיזוג עם הגנת ⏳ | ✅ | ✅ | ✅ | ✅ |
| 24 | חסימת משתמש מושבת בכניסה אופליין | לא רלוונטי | ✅ | ✅ | ✅ |
| 25 | מודול מזהי רשומות | ✅ | ✅ | ✅ | ✅ |
| 26 | מזהה מכשיר | ✅ | ✅ | ✅ | ✅ |
| 27 | ליבת מיזוג משותפת | ✅ | ✅ | ✅ | ✅ |
| 28 | מעטפת WebView חתומה | ✅ | ✅ | ✅ | ✅ |

### הנימוקים — שורה אחר שורה
**1 · כניסה אופליין.** ל-yoman **אין טבלת משתמשים, אין מסך כניסה ואין
תפקידים** — הכניסה היא בחירת מוסד, ולכן אין מה לאמת. בשלוש האחרות
האימות הוא מול `pass_salt`+`pass_fp` שבמכשיר (סעיף 4 של כלל ברזל 10):
hanhala בסבב 22, gius בסבב 23, schar-limud בסבב 24.

**2 · עריכת נתונים אופליין.** בארבעתן, לפי כלל ברזל 6: כתיבה
מקומית-תחילה, מיזוג ברמת רשומה, tombstones, וסימון ⏳ עד שיש ראיה
מהענן. schar-limud הייתה האחרונה שנסגרה (סבב 12 שלב 2), ו-gius לפניה
(סבב 11).

**3 · מודל משתמשים.** ל-yoman אין. בשלוש האחרות `role TEXT NOT NULL`
בלי `DEFAULT`, ו⛔ **הערכים אינם משותפים ואין ליישר אותם**:
`'admin'`/`'senior'`/`'junior'` בהנהלה · `'admin'`/`'user'` ב-schar ·
`'owner'`/`'manager'` ב-gius.

**4 · נתיב עדכון חלקי למראת המשתמשים.** כתיבת שורת משתמש **בודדת**
למראה, בלי משיכה מלאה — כך שהחלפת משתמש במכשיר אינה תלויה בסנכרון הבא.
hanhala (`ysUsersCacheSave`) ו-gius (`usersCacheSave`, סבב 26) — ✅.
❌ **ב-schar-limud, וזה אינו פער:** שם `doLogin` מריצה **משיכה מלאה
כפויה** (`slPullUsers(true)`) מיד אחרי כניסה מוצלחת, וזה ממלא בדיוק את
אותו תפקיד. ⛔ ואם ייבנה שם נתיב חלקי בעתיד — הוא חייב לעבור דרך
`slUserPub`, כמו שלוש נקודות האכיפה הקיימות (כלל ברזל 10 סעיף 5).
ל-yoman אין מראת משתמשים כלל.

**5 · סוד במכשיר.** בשלוש שיש בהן משתמשים יורדת **טביעה בלבד** —
PBKDF2-SHA256, 100,000 סיבובים, מלח אקראי פר-משתמש עם קידומת הקשר.
⛔ **הסיסמה עצמה אינה יורדת לדיסק באף אחת מהן.** ב-yoman אין סוד
כלשהו במכשיר, מפני שאין משתמשים.

**6 · פינוי אוטומטי.** yoman (`tb_archive`/`tb_entries`, רצפות 60/90
יום) · hanhala (ארבעת מפתחות ה-`kv`, 180 יום) · gius (`g_mirror_txns`/
`g_mirror_pledges` 400 יום, `g_mirror_tasks` 180).
❌ **ב-schar-limud, וזו החלטה מתועדת ולא פער:** `tier1` ו-`tier2` ריקים
בכוונה — `sl_mirror_transactions` הם **כספים**, ו-`sl_mirror_students`
הם אבות המפתח הזר שלהם. ⛔ אין להוסיף שם פינוי; בקשה «לחסוך מקום»
היא בקשה לפתוח את סבב «מראה-חלון» המתועד שם, לא לאלתר טלאי.

**7 · אימות פינוי מול הענן** (סבב 14 — `idOf`+`verify` על `tier2`).
yoman ו-hanhala — ✅. ל-schar אין מה לאמת, מפני שאין שם פינוי כלל
(שורה 6) — לכן «לא רלוונטי» ולא ❌.
❌ **ב-gius, וזה אינו פער:** העֵד שלה `g_synced_at` נכתב אחרי **משיכה
מלאה מוצלחת של כל שבע הטבלאות**, כלומר הוא כבר עדות מהענן ונכתב גם
בדפדפן שרק קורא — ולכן הבאג שסבב 14 בא לפתור מעולם לא היה קיים שם.

**8 · שיתוף קבצים.** ל-yoman יש גשר מקורי לשיתוף דוח כתמונה, **מוגבל
ל-origin שלנו** ב-`addWebMessageListener` עם נפילה-חזרה נעולה-כפליים
(סבב 13). בשלוש האחרות **אין `navigator.share` בקוד כלל** (אומת
ב-grep), ולכן אין גשר — וזו ההחלטה הנכונה: ⛔ לא נותנים גשר מקורי לדף
שנטען מהרשת בלי צורך. אם יידרש שם שיתוף — **מעתיקים את הדפוס של
yoman**, לעולם לא `addJavascriptInterface` חשוף.

**9 · מעטפת APK.** בארבעתן WebView מקורי שטוען מהרשת. ⛔ **לעולם לא
TWA ולא PWABuilder** — TWA מריץ את האתר בתוך כרום, וסינון התוכן
במכשירי המשתמשים חוסם את כרום. זה נמדד בפועל: ה-TWA של gius פשוט לא
נפתח.

**10 · מפתח חתימה קבוע בריפו.** לכל אחת keystore משלה, וטביעת ה-SHA
שלה כתובה בפרק «חתימת APK» ה**פרטי** שלה. ⛔ הפרק הזה פרטי בהגדרה
(כלל ברזל 8 סעיף 5) — איחוד שלו יהיה שגוי ומסוכן.

**11 · מקור אמת יחיד לסכימה** — קובץ אחד בריפו שהוא ההגדרה המלאה של
המסד. schar-limud: `migrations/000_initial_schema.sql`. gius:
`migrations/0001_init.sql` (+0002/0003 קדימה). yoman-avoda:
`migrations/000_initial_schema.sql`, שנוצר בסבב 28.
✅ **ב-hanhala — הפער נסגר בסבב 32.** קובץ ההתקנה נוצר שם בסבב 28, אבל
`index.html` המשיך להחזיק **עותק חלקי** של הסכימה בשני מקומות
(`showSetupScreen` להצגה, `setupWithServiceKey` להרצה) — `ys_users`
בלבד, בלי `kv`, `sync_log` ו-`kv_backup`. שניהם **מושכים** מעכשיו את
`migrations/000_initial_schema.sql`, בדיוק כפי ש-`setup-db.html` כבר
עשה. ⛔ **ואין עותק-גיבוי מוטבע:** הקובץ יושב על אותו origin שממנו נטען
הדף, ואם הוא אינו נגיש האפליקציה עצמה לא נטענה — כלומר העותק לא היה מגן
על תרחיש אמיתי, ומחירו (מקור אמת שני שמתיישן) ודאי. בכשל משיכה מוצגת
הודעה עם קישור ישיר לקובץ, ו⛔ **לא מורצת סכימה כלשהי**.
⚠️ **וזו הייתה בדיוק תבנית סבב 24 ב-schar-limud** (`SETUP_SQL_FALLBACK`),
ריפו אחר וכמעט שנתיים של סבבים אחרי — ר' הסעיף החדש בכלל ברזל 12.

**12 · קובץ התקנה מלא** — קובץ שהרצתו על מסד ריק נותנת התקנה עובדת.
✅ **בארבעתן, מסבב 28.** שתי האפליקציות שחסרו קיבלו שם קובץ שמופה
**מהמסד החי** ולא מקריאת הקוד:
- **yoman-avoda:** `migrations/000_initial_schema.sql` — `kv_rishon`
  ו-`kv_ramataviv` עם RLS, פוליסות והרשאות, ופרק שמסביר שההפרדה בין
  המוסדות היא **טבלה בענן וסיומת מפתח במכשיר**.
  ⚠️ `supabase-multitenant.sql` **אינו** קובץ התקנה ומעולם לא היה כזה:
  הוא הסתיים ב-`INSERT … SELECT … FROM public.kv`, כלומר הגירה חד-פעמית
  מטבלה שכבר אינה קיימת. ⛔ **הוא נמחק בסבב 33** — תוכנו מכוסה במלואו
  בקובץ ההתקנה, ולכן כל מה שנותר לו היה להיקרא בטעות כהוראת הפעלה.
- **hanhala-ruchanit:** `migrations/000_initial_schema.sql` — `kv`,
  `ys_users` (כולל `pass_salt`/`pass_fp` מסבב 22 וה-`CHECK` על `role`),
  `sync_log` ו-`kv_backup`. `setup-db.html` **מושך אותו** ואינו מחזיק
  עותק.
- **⚠️ ומה שעדיין לא נעשה, במפורש:** שני הקבצים **לא הורצו מול המסד**
  ולא נגעו בנתונים. הם נמסרים לאימות חיצוני **טבלה-טבלה** לפני המיזוג,
  וכל מקום שבו לא הושגה ודאות רשום בגוף הקובץ. ⛔ «נכתב» אינו «אומת».

**13 · גיבוי יומי אוטומטי** (סבב 30, שלב א) — ✅ **בארבעתן**, מאותו מודול
משותף. עד הסבב הזה גיבו רק hanhala ו-schar, כל אחת במימוש משלה; הליבה
חולצה, והכיסוי הורחב. ⚠️ **וזה נמדד ולא הונח:** הגיבוי של hanhala
**הפסיק לרוץ ב-2026-07-20** ואיש לא ידע — בדיוק הסיבה ש«גובה לאחרונה»
נוסף ל«מידע טכני» בארבעתן. ⛔ אין להשוות «יש גיבוי» ל«הגיבוי רץ».
מהשלמת סבב 35 המודול נושא גם **מדיניות שמירה** — עותק יומי נשמר 30 יום
ונגרע לפי רשימת-היתר של מפתחות (⛔ לעולם לא `PRE_*` — סבב 30) — וכן
**סינון סודות** דרך `BK_CFG.secrets`; ר' פרק המודול.

**14 · יומן פעולות** (סבב 30, שלב א; הורחב בסבב 35) — ✅ **בארבעתן**,
ל-`sync_log`. ⚠️ **מה שנרשם נבדל לפי האפליקציה** ואינו מיושר, והוא
נמדד מהקוד: הנהלה רושמת כניסות וסנכרון · schar ו-yoman — גיבוי, ומסבב
35 גם דחיפה (עם מוני שורות, רק כשנדחף משהו) ומשיכה ראשונה בסשן ·
gius — גיבוי בלבד. ⛔ **המשותף הוא המנגנון — הטבלה, התור האופליין
והתקרה — ולא רשימת האירועים**, שהיא פר-אפליקציה מעצם טבעה. ⛔ אין
לרשום את הפולינג התלת-שניתי ליומן (סבב 35) — טבלת ראיות ב-INSERT בלבד
הייתה מתמלאת באלפי שורות ביום.

**15 · נתונים בטבלאות מובנות** — שורה לכל רשומה, ולא ערך יחיד.
schar-limud (חמש טבלאות `sl_`) ו-gius (שבע טבלאות `g_`) — ✅ מאז ההקמה.
✅ **ב-yoman-avoda — הפער שנפתח בסבב 30 נסגר בסבב 32ב:** מיגרציות
`002`+`003` הורצו ב-2026-08-16 דרך `apply_migration` ונרשמו
ב-`schema_migrations` (`yoman_002_structured_tables` ·
`yoman_003_migrate_kv_to_rows`). ⚠️ שתיהן היו בפועל **no-op** — הכתיבה
הכפולה שרצה מאז סבב 30 כבר מילאה את הטבלאות — וזו בדיוק העדות שההרצה
נתנה. בדיקת השקילות רצה **מוסד-מול-מוסד**: ראשון לציון 455 רשומות
ו-156 סנאפשוטים, רמת אביב 265 ו-15, סה"כ **720/171 — התאמה מלאה
לערכי ה-`kv`**.
❌ **ב-hanhala-ruchanit — הפער עדיין פתוח, אבל שלב א נכתב בסבב 36.**
**כל** נתוני האפליקציה יושבים כמפתחות `kv`, והגדול בהם
(`ys_attend_sessions`) נמדד ב-2026-08-18 כ-**360KB · 273 סדרים ·
13,083 סימונים** — כלומר כל סימון נוכחות בודד דורס את מלוא הערך.
סבב 36 הוסיף את **שלב א בלבד**: מיגרציות אדיטיביות (`005` המבנה,
`006` ההעברה) וכתיבה כפולה לשתי טבלאות — `ys_sessions` (מטא הסדר)
ו-`ys_marks` (שורה לסימון) — לצד `ys_students_rows`.
⛔ **והתא נשאר ❌** (סבב 36): המיגרציות **נכתבו ולא הורצו**, וה-`kv`
הוא עדיין המאסטר — כל **קריאה** עוברת דרכו. «נכתב» אינו «רץ», ואין
לסמן ✅ לפני אימות שקילות מול המסד החי.
**⚠️ הטריגר לסגירה:** הרצת `005`+`006` ע"י המנהל, בדיקת השקילות
הדו-כיוונית שבגוף `006`, ואז שלב ב — מתג הקריאה וכיבוי
`YS_KV_LEGACY_WRITE`. ⛔ הכיבוי והמחיקה בסבבים נפרדים (סבב 30) —
אחרת אין חלון לגלות טעות.
**16 · שער תקינות JS** (סבב 33) — ✅ **בארבעתן**, `tools/check-js.mjs`:
חילוץ ה-JS המוטבע מ-`index.html`, `node --check` עליו ועל `sw.js`, כללי-אמת
שפרסר אינו תופס, והרצת **כל** שערי האחידות וחבילות בדיקות הסבבים.
⚠️ **וזה נמדד ולא הונח:** עד סבב 33 השער הזה חי **ב-gius לבדה, עשרה סבבים**,
ובשלוש האחיות אותה עבודה נעשתה בהעתקת שבע פקודות מ-CLAUDE.md ידנית — כלומר
דחיפה שדילגה על אחת מהן לא הותירה סימן. ⛔ **איש לא החליט על ההבדל הזה**,
והוא בדיוק מה שהוליד את כלל ברזל 14.
⚠️ **הכללים שבתוך השער נבדלים פר-אפליקציה ואינם מיושרים** — `APP.rules`
נמדד מ-`sw.js` ומ-`index.html` של כל אפליקציה: ל-schar אין דף אופליין,
ולכן אין לה את הכלל הזה (רשימת ה-CDN במטמון נוספה לה בסבב 35, ואיתה
כללי `mode:'cors'` ונעילת הגרסה). ⛔ המשותף הוא **המנגנון**, לא רשימת
הכללים.

**17 · חלון חם במכשיר** (סבב 35) — מודול משותף חדש («חלון חם ושחזור
מקומי»): המכשיר מחזיק את «החלון החם» בלבד, וההיסטוריה חיה בענן ונמשכת
לפי דרישה, לקריאה בלבד. ⛔ הפינוי דורש **שני** תנאים, בלי יוצאים מן
הכלל (סבב 35) — מחוץ לחלון **וגם** מסונכרן במלואו (אפס ⏳ לרשומה,
וראיה טרייה מהענן לפי מזהה + חותמת): רשומה שטרם עלתה אינה מפונה בשום
מצב, וספק ⇐ לא מפנים. החלון פר-אפליקציה: yoman — `archived=false`
(הארכוב הקיים מיושר כמקרה פרטי; סנאפשוט מדוגל ומסונכרן מפונה) ·
schar — תנועות שנת הלימודים הנוכחית (`acadYearOf` הקיימת; המצבה,
ההגדרות ושדה החוב תמיד חמים) · gius — אותו גבול-שנה שאומץ מ-schar
(`gAcadYearOf`; תורמים, התחייבויות ומשימות תמיד חמים).
❌ **ב-hanhala — פער מתועד, והמודול כבר יושב רדום בקוד:** הנתונים
יושבים כמפתחות `kv` שלמים (שורה 15), ופינוי פר-רשומה מעל ערך שלם היה
מוחק את הערך כולו. **הטריגר:** שלב ב של מעבר הטבלאות; החלון המתוכנן —
חודש עברי נוכחי + קודם.

**18 · שחזור מקומי מהענן** (סבב 35) — ✅ **בארבעתן**, מאותו מודול:
כפתור «☁️ שחזר הכול מהענן» באזור «מידע טכני ›», למנהל בלבד, עם אישור
דו-שלבי (לחיצה שנייה בתוך 8 שניות), שקורא ל-`lsRestoreAll` הקיימת
במודול האחסון הקפוא ⛔ בלי לגעת במודול עצמו (סבב 35) — עריכה שם הייתה
שוברת את חתימות כלל ברזל 7. הכפתור פעיל גם ברדימות (hanhala) — הוא
אינו תלוי בחלון.

**19 · מסך שינוי סיסמה עצמי** — שינוי סיסמה של המשתמש המחובר מתוך
האפליקציה, בלי גישת מנהל למסד. hanhala — ✅ (`changeMyPassword`, קיים
מסבבים קודמים) · gius — ✅ (סבב 35: כפתור 🔑 בכותרת, אימות הסיסמה
הנוכחית מול הענן באופן רגעי בלבד, גזירת טביעה מחדש דרך
`passFields`/`writeUser`; דורש רשת). **לא רלוונטי ב-yoman** — אין
משתמשים ואין סיסמאות (שורה 3). ❌ **ב-schar-limud, וזו החלטה מתועדת
ולא פער:** המנהל מנהל סיסמאות מלוח הבקרה של Supabase — «אין כאן מסך
ניהול משתמשים» (פרק «כניסה ומשתמשים» שם) — ומסך שינוי-עצמי היה נתיב
כתיבה שני ל-`sl_users` שאיש לא ביקש.

**20 · מטמון-CDN מראש עם ריפוי עצמי** — ✅ **בארבעתן, מסבב 35**: רשימת
CDN נעוצת-גרסה שנטענת מראש ב-`install` עם `mode:'cors'`, ו-`ensureCdnCached`
שמרפא חוסרים ב-`activate`. עד הסבב הזה schar נשענה על שמירת-בזמן-ריצה
בלבד — סקריפט CDN שלא ביקרו בו לפני הניתוק פשוט חסר אופליין. הדפוס
הועתק מ-hanhala (סבב 9 שם), לפי כלל ברזל 14.

**21 · גיבוי יומי מטבלאות מובנות** — מקור הגיבוי הוא הטבלאות עצמן ולא
מפתחות `kv`. schar ו-gius — ✅ מאז סבב 30 (`kind:'table'`). ✅
**ב-yoman מסבב 35:** `tb_entries` נגבית כטבלה — כל השורות, כולל
המדוגלות `archived` — עם `eq` פר-מוסד, `order` דטרמיניסטי לחתימה
הדיפרנציאלית, ומפתח גיבוי נפרד (`tb_entries_rows`) שאינו מתנגש בגיבוי
ה-`kv` הישן; הישן נשאר פעיל כל עוד הכתיבה הכפולה קיימת, ויוסר איתה.
❌ **ב-hanhala — אותו פער כמו שורה 15, באותו טריגר.** מסבב 36 שלושת
מקורות ה-`kind:'table'` **כבר רשומים** ב-`BK_CFG.sources()`
(`ys_sessions_rows` · `ys_marks_rows` · `ys_students_rows`) ונוספו
לרשימת-ההיתר של `migrations/004` באותו סבב — ⛔ אבל התא נשאר ❌ מפני
שהטבלאות עצמן טרם נוצרו, וגיבוי ממקור שאינו קיים מדלג בשקט. הטריגר:
הרצת `005`+`006`.

**22 · פינוי גיבויים אוטומטי במסד** (סבב 35ג) — משימת `pg_cron` יומית
שמוחקת מ-`kv_backup` עותקים יומיים בני 30+ יום, לפי **רשימת-היתר מפורשת
של מפתחות** ו⛔ **לא לפי קידומת** (קידומת ריקה הייתה תופסת גם גיבוי
חד-פעמי וגם מפתח של אפליקציה אחרת באותו פרויקט). היא רצה **בתוך המסד**
בהרשאות פנימיות, ולכן אינה מחייבת לפתוח `delete` ל-`anon` — ר' «שתי
השכבות» בפרק מודול הגיבוי.
✅ **בארבעתן מסבב 36 — הפער נסגר, והתא נמדד מול שני המסדים.** שתי
המיגרציות הורצו ע"י המנהל ב-2026-08-18:
`hanhala-ruchanit/migrations/004_backup_retention_cron.sql` לפרויקט
המשותף (חלה גם על yoman-avoda ועל schar-limud, שחולקות אותו)
ו-`gius/migrations/0005_backup_retention_cron.sql` לפרויקט הנפרד של gius.
**מה שנמדד** (`SELECT` בלבד, בשני הפרויקטים): המשימה `bk_retention_daily`
רשומה ב-`cron.job`, `active=true`, ומריצה `bk_retention_sweep(30)`;
הריצה הראשונה בפרויקט המשותף פינתה **37 שורות** על פני 32 מפתחות
ונרשמה ב-`sync_log` (`device_id='pg_cron'`, `action='retention'`);
ובדיקת השפיות של חלון קצר מ-7 ימים אומתה כמסרבת לרוץ.
⚠️ **והתזמון בפועל הוא `'0 3 * * *'`** — שני הקבצים תיארו 03:17 ויושרו
למציאות בסבב 36. ⛔ הקובץ הוא מקור האמת, ואי-התאמה בינו לבין המסד היא
בדיוק מה שמייצר גרסה שלישית שאיש אינו יודע עליה.

<!-- SHARED:end -->


**23 · מנוע מיזוג עם הגנת ⏳** (סבב 37) — רשומה מקומית שטרם עלתה לענן
מנצחת במיזוג **ללא תלות בחותמת**, כפי שכלל ברזל 6 דורש. עד סבב 37 הכלל
היה מיושם **בשתיים מארבע**: `slMerge` ב-schar (סבב 12 שלב 2) ו-`mergeRows`
ב-gius. ב-yoman וב-hanhala המיזוג הכריע לפי חותמת בלבד, ולכן עריכה
מקומית שטרם סונכרנה **נדרסה בשקט** ע"י גרסה שמכשיר אחר דחף אחריה —
בדיוק הכשל שכלל ברזל 6 מתאר, בשתי אפליקציות, שמונה-עשר סבבים.
⛔ **וזה לא היה פער מוכרז**: איש לא רשם «הוחל», «אינו רלוונטי» או «פער
עם טריגר» — הוא פשוט לא היה שם. הצד השני של אותו כלל — «רשומה מסומנת
נחשבת תמיד לדחיפה» — כן היה קיים בארבעתן.
⚠️ ההגנה **צרה בכוונה**: היא מכריעה רק בהתנגשות מפתח בין מקומי למרוחק,
ואינה נוגעת בשובר-השוויון (בשוויון הענן עדיין מנצח) ולא בכלל «רשומה
מקומית-בלבד שורדת».

**25 · מודול מזהי רשומות** (סבב 37ב) — **פונקציה אחת בשם אחד** שיוצרת
מזהה רשומה במכשיר, לפני שהרשומה ראתה שרת: `newClientId()`, בלוק משותף
זהה בית-לבית בארבעתן (35 שורות, חתימה `3edcfd6f5b4be129`).
⚠️ **הפער שנמדד לא היה היעדר — הוא היה ריבוי:** אותה יכולת בדיוק חיה
ב**חמישה שמות** — `newClientId` בשכר לימוד · `newId` ב-gius · `ysNewId`
בהנהלה · ודפוס בלי פונקציה ייעודית ביומן — כולן `crypto.randomUUID` עם
אותן שתי נפילות-חזרה, ⛔ **ואף שער לא אכף אף אחת מהן**. כלל ברזל 14 אוסר
«קיים רק באחת, בשקט»; ארבעה מימושים כמעט-זהים תחת ארבעה שמות הם אותו כשל
בדיוק, בכיוון ההפוך. השם `newClientId` נבחר מפני שהוא הוותיק והברור מכולם.
✅ **ב-yoman-avoda מסבב 38 — הפער נסגר, ובדיוק כפי שהטריגר דרש: שלושת
הממדים יחד.** עד אז המודול ישב בקובץ ואיש לא קרא לו, ושני מזהי היומן היו
`Date.now()` — כלומר שני מכשירים שהוסיפו רשומה באותה מילישנייה קיבלו
**אותו מזהה**, והמיזוג ראה בהם רשומה אחת.
⛔ **וההמרה אינה החלפת שורה** (סבב 37ב): המזהה שימש כ**מפתח מיון מספרי**
(`b.id - a.id`, `Number(x.id)`), נכנס **בלי מרכאות** למאפייני `onclick`
(`editEntry(123)`), ומושווה ב-`===` מול מספר. שלושתם הוחלפו באותו סבב —
`entryOrderTs` (סדר לפי `createdAt`, ועבור רשומות העבר לפי ה-`id`
שהוא עצמו חותמת היצירה) · `idArg` (ציטוט ורשימת-היתר של תווים) ·
`idEq` (השוואה כמחרוזת).
⛔ **ומזהים קיימים לא הומרו** (סבב 38) — הקוד סובל את שני הסוגים לנצח.
המרה הייתה מפצלת רשומה אחת לשתיים בכל מכשיר שטרם קיבל אותה.
⛔ **ומזהה מכשיר אינו מזהה רשומה** (סבב 37ב) — `getDeviceId`/`_randDeviceId`
הם מושג אחר (זהות של מכשיר, לא של נתון) ואינם עוברים דרך המודול. ⚠️ ונמדד
אגב כך: `_randDeviceId` **זהה בית-לבית בשלוש** (yoman · hanhala · schar),
ול-gius אין מזהה מכשיר כלל — פער נפרד שלא נגענו בו בסבב הזה.

**27 · ליבת מיזוג משותפת** (סבב 38) — כלל ההכרעה של כלל ברזל 6 יושב
ב**פונקציה אחת** (`_mergePick`) שארבעת מנועי המיזוג קוראים לה, בתוך בלוק
משותף זהה בית-לבית (91 שורות, חתימה `cc0329965238994c`). עד סבב 38 חיו כאן
ארבעה מנועים נפרדים שמימשו את אותם שלושה כללים — ⏳ גובר · שוויון→ענן ·
מקומית-בלבד שורדת — ⛔ **וזו בדיוק הסיבה שההגנה על ⏳ הייתה קיימת בשתיים
מארבע במשך שמונה-עשר סבבים**: ארבעה עותקים של כלל הכרעה הם ארבע הזדמנויות
שאחד מהם ייסחף.
⚠️ **ומה שנבדל נשאר נבדל — כפרמטר מדוד ולא כקוד כפול.** ארבע ידיות המדיניות
(`dedupe` · `remoteDupe` · `keyless` · `localPick`) מקודדות התנהגויות
**קיימות** במצבי קצה: איחוד כפילויות (ביומן — נדרש, שני סנאפשוטים לאותו
יום; בשכר ובגיוס — ⛔ אסור, כיווץ שקט של שתי שורות כספיות), הכרעת כפילות
מרוחקת (חותמת מול סדר), שורה מרוחקת בלי מפתח, ובחירת הרשומה המקומית
הראשונה מול האחרונה.
⛔ **אין לשנות אף ידית «לשם אחידות»** (סבב 38) — כל אחת מהן היא התנהגות
שנמדדה ונשמרה, ולא ברירת מחדל שנפלה מאליה.
⭐ **והשקילות נמדדה ולא הונחה:** דיפרנציאל של **125,000 מקרים** בין
המימוש החדש למימוש שהוחלף (נקרא מ-`git HEAD`), בארבע האפליקציות, כולל
`mergePair`, `onDrop`, ענף האובייקטים, מפתחות כפולים ורשומות בלי מפתח —
**אפס הפרשים**.

**26 · מזהה מכשיר** (סבב 38) — מזהה בן שמונה תווים שנוצר במכשיר ונשמר
ב-`lsSetRaw` (החריג המתועד של כלל ברזל 1), ומזהה את המכשיר ב-`sync_log`
וב-`kv_backup`. ✅ **בארבעתן מסבב 38.** עד אז `getDeviceId`/`_randDeviceId`
היו בשלוש בלבד — ⛔ **ול-gius לא הייתה שם אף הגדרה**, אף ש-`BK_CFG.device`
כבר קראה לפונקציה. ה-`try/catch` שסביב הקריאה החזיר `null` בשקט, וכל שורה
שנרשמה שם ירדה **בלי מזהה מכשיר**: מנגנון האבחון היה שם, והנתון שהוא נועד
לתת פשוט לא נאסף. הדפוס הועתק בית-לבית משכר לימוד — `_randDeviceId` זהה
בית-לבית בארבעתן, ומפתח האחסון נבדל בלבד (`sl_`/`ys_`/`tb_`/`g_device_id`).
⛔ **וזו שורה נפרדת משורה 25, לא הרחבה שלה** (סבב 38) — מזהה מכשיר הוא
זהות של **מכשיר** ולא של נתון: הוא אינו מפתח מיזוג, אינו חייב להיות
uuid, ו⛔ **אינו עובר דרך `newClientId()`**.
⛔ **ומזהה שכבר יושב במכשיר אינו משתנה לעולם** — הוא המפתח שלפיו קוראים
את היומן אחורה.

**24 · חסימת משתמש מושבת בכניסה אופליין** (סבב 37) — משתמש שהושבת אינו
נכנס, גם בלי רשת. hanhala (`ysVerifyOffline`, סבב 22) ו-gius
(`gVerifyOffline`, סבב 23) בודקות `active !== true` בשורה הראשונה.
✅ **ב-schar-limud מסבב 37, ושם זה היה חור אמיתי:** `slVerifyOffline`
בדקה `!u` בלבד, ול-`sl_users` **לא הייתה עמודת `active` כלל** — כלומר
לא הייתה שום דרך להשבית משתמש באפליקציית הכספים, ומי שעזב המשיך להיכנס
מכל מכשיר שכבר החזיק מראה מקומית. העמודה נוספה ב-`migrations/013`
(⛔ נכתבה ולא הורצה), `active` נכנסה ל-`SL_USER_COLS`, ו-`slUserPub`
משלימה `true` לשורה שאין בה את השדה — ⛔ **ברירת מחדל הפוכה הייתה נועלת
בחוץ את כל המשתמשים** בחלון שבין פריסת הקוד להרצת המיגרציה.
**לא רלוונטי ב-yoman** — אין משתמשים ואין כניסה (שורה 3).

**28 · מעטפת WebView חתומה** (סבב 40) — ✅ **בארבעתן.** עד הסבב הזה
`MainActivity.java` היה הקובץ היחיד בארגון ש**אף בדיקה לא נגעה בו**, אף
שהוא זה שקובע מה קורה כשאין רשת. השער (`tools/test_round40_shell.mjs`,
זהה בית-לבית ×4 פרט ל-`APP`) אוכף שתי שכבות: **חוזה התנהגות** — בורר
קבצים · דף אופליין ב-`text/html` · `onBackPressed` · השארת `http(s)`
במעטפת · JS+localStorage · וסגירת `file://`/`content://` — ו**חתימה על
הלוגיקה**, hash על המקור אחרי שהערות, מחרוזות ושם החבילה מנורמלים
החוצה.
⭐ **וזה מה שהמדידה נתנה:** אחרי נרמול הזהות, שלוש המעטפות חסרות-הגשר
(hanhala · schar · gius) נושאות **חתימה זהה בדיוק** —
`9d13db21feb1f616` — וכל אחת מהן אוכפת זאת אצלה, כך שסחיפה ביניהן
נתפסת בכל ריפו בנפרד. ⚠️ ל-yoman חתימה משלה
(`161b6a469d6b45eb`, כפולה באורכה) מפני שגשר השיתוף חי אצלה בלבד —
חריגה **מדודה**, לא סחיפה.
⛔ **והסבב הזה אינו מחלץ תבנית משותפת** (סבב 40) — חילוץ ארבע מעטפות
לקובץ אחד הוא סבב ייעודי, וריפקטור בלי שער קיים הוא בדיוק מה שכלל
ברזל 14 אינו מבקש. ⭐ החתימה היא **התנאי** לחילוץ כזה בעתיד: היא רשת
הביטחון שתמדוד שהוא לא שינה התנהגות.

---

<!-- SHARED:start id="iron-rule-11-comments" -->
## ⭐ כלל ברזל 11 — תקן ההערות בקוד (סבב 28, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את עשרת כללי הברזל שלמעלה.

> **הערה על אותו סוג דבר נראית אותו דבר בארבע האפליקציות.** כלל ברזל 7
> דרש עד היום זהות **תוכן** — הבלוקים המשותפים זהים בית-לבית. מעכשיו
> נדרשת גם זהות **צורה** באזורים הפרטיים: כותרת בלוק אחת, שלושה סימנים,
> תבנית איסור אחת ומילון מונחים אחד — כדי ששתי אפליקציות זו לצד זו
> ייקראו כשני עותקים של אותו ספר, ולא כשני פרויקטים של שני אנשים.

### הבעיה שנמדדה
סבב 27 הוסיף תיעוד פנימי ל-gius, ומדד אגב כך שהאחיות נבדלות זו מזו
**לא באחוזים אלא בכיסוי ובצורה**: אזורים לוגיים שלמים בלי שום כותרת,
הערות ⛔ שאומרות «אל תעשה» בלי לומר **מתי זה נלמד ולמה**, סימנים
שהומצאו לצורך שורה אחת, ואותו מושג בשלושה כתיבים.
⛔ **והנזק אינו אסתטי: הערת איסור בלי סבב ובלי סיבה היא הזמנה להסיר
אותה.** הסשן הבא קורא «אל תעשה X», אינו מוצא הצדקה, ומסיק שזו קפריזה —
ומחזיר בדיוק את הבאג שהאיסור בא למנוע.

### ששת הסעיפים
לצד כל סעיף רשום **הסבב שבו נלמד** — הרקע המלא נמצא שם.

1. **כותרת בלוק לכל אזור לוגי, בפורמט אחד ויחיד.**
   *(נלמד בסבב 27 ב-gius, והורחב לארבעתן בסבב 28.)* המסגרת היא **בדיוק
   74 תווי `═`**, שורת השם היא שלושה רווחים ואחריהם שם האזור (עד 78
   תווים), ואחריה מסגרת נוספת — גוף אופציונלי — ומסגרת סוגרת:

   ```
   /* ══…(74)══
      <שם האזור>
      ══…(74)══
      [גוף אופציונלי — נימוקים, ⛔, ⚠️]
      ══…(74)══ */
   ```

   ⚠️ **הרוחב הקבוע אינו קישוט:** הוא מה שהופך «אותו דבר» למדיד. מסגרת
   ברוחב אחר נראית לעין כמעט זהה, ולכן היא נסחפת בשקט — בדיוק הסחיפה
   שכלל ברזל 7 נבנה נגדה.

2. **שלושה סימנים בלבד — ⛔ איסור · ⚠️ אזהרה · ⭐ החלטה מרכזית.**
   *(נלמד בסבב 28.)* ⛔ **ואין להמציא סימנים נוספים.** סימן רביעי אינו
   מוסיף מידע — הוא מוריד את המשקל של השלושה: קורא שרואה חמישה סמלים
   שונים מפסיק להתייחס לאף אחד מהם כאל עצירה.
   ⚠️ הכלל חל על **פתיחת שורת הערה**. ✅/❌/⏳ בתוך משפט, או בתוך מחרוזת
   שמוצגת למשתמש, אינם סימון — הם טקסט.

3. **כל ⛔ נושא את הסבב שבו נלמד ואת הסיבה** — בתבנית
   **«⛔ אל תעשה X (סבב N) — הסיבה»**. *(נלמד בסבב 28.)* שני החלקים
   נדרשים **יחד**: הסבב הוא הדרך למצוא את הרקע המלא בלי לחפש, והסיבה
   היא מה שמונע מהסשן הבא לבטל את האיסור בתום לב.
   ⚠️ איסור שקדם למספור הסבבים נרשם כ«(נרשם בסבב N)» — האמת היא שהוא
   **תועד** אז, לא שנולד אז, ואין לכתוב מספר סבב שלא היה.

4. **טרמינולוגיה אחידה — אותו מונח עברי לאותו מושג בארבעתן.**
   *(נלמד בסבב 28.)* **מראה** (לא «מירור») · **טביעה** (לא «טביעת
   אצבע») · **אופק הפינוי** · **שער** · **פינוי** · **דחיפת-מצב**
   (עם מקף) · **מטמון** (לא «קאש») · **אופליין** · **tombstone**
   באנגלית. ⚠️ שני כתיבים לאותו מושג הופכים כל חיפוש טקסטואלי בין
   הריפו ל**חלקי** — וחיפוש חלקי הוא בדיוק איך שנשמט נתיב כתיבה שני
   (כלל ברזל 10 סעיף 5).

5. **כל החלטה ארכיטקטונית שנראית שרירותית מנומקת במקומה.**
   *(נלמד בסבב 27 ב-gius.)* קבוע שנראה שרירותי (רצפת גיל, תקרה,
   השהיה), טבלה שמוחרגת מרשימה, סדר שתי שורות שאסור להפוך — הנימוק
   יושב **ליד הקוד**, ולא רק ב-CLAUDE.md. ⚠️ מי שעורך את הקוד אינו
   קורא שלושים פרקי סבבים לפני שהוא נוגע בשורה.

6. **⛔ הבלוקים המשותפים והמודולים הקפואים אינם באזור התחולה.**
   *(נלמד בסבב 28.)* חמשת הבלוקים של סבבים 11–17 זהים בית-לבית
   ונשמרים ע"י `check-status-area.mjs`; עריכת הערה בתוכם מחייבת שינוי
   בארבעת הריפו **ובחתימות**. כלומר תקן שהיה חל עליהם היה **מייצר
   בעצמו** את הסחיפה שהוא בא למנוע. התקן חל על **הקוד הפרטי בלבד**.

### הבדיקה האוכפת
`tools/check-comments.mjs` (ב-gius: `scripts/`, ו-`check-js.mjs` קורא
לה) — **זהה בית-לבית בארבעת הריפו פרט לבלוק `APP` שבראשה**, ורצה עם
שערי התחביר לפני כל דחיפה. היא נכשלת על ארבעה סוגי סטייה:
- **א. כותרת בלוק** — כותרת שצורתה אינה תקנית (רוחב המסגרת, שורת השם),
  או **רצף של יותר מ-400 שורות קוד פרטי לא-ריקות בלי כותרת**.
  ⚠️ הכיסוי נמדד כמרווח בין כותרות ולא כ«כותרת לכל פונקציה»: אזור לוגי
  הוא רעיון, לא הצהרה. והמרווח נספר ב**שורות קוד פרטי לא-ריקות** ולא
  במספרי שורות — בין שני אזורים יושבים HTML ו-CSS, וספירה גולמית הייתה
  מדווחת «1700 שורות בלי כותרת» על אזור שאין בו קוד כלל.
- **ב. ⛔ בלי סבב או בלי סיבה** — בלוק הערה שיש בו ⛔ חייב לשאת
  `סבב <N>` **וגם** מפריד « — » שאחריו הנימוק.
- **ג. סימן שאינו מהרשימה** — שורת הערה שנפתחת בסמל שאינו ⛔/⚠️/⭐.
- **ד. טרמינולוגיה** — מונח מהרשימה בכתיב שאינו התקני.

⚠️ **הבדיקה קוראת הערות בלבד, דרך טוקניזציה של JS** (מחרוזות, תבניות
ו-regex). ⛔ **אין להחליף אותה בביטוי רגולרי על `//` ועל פתיחת בלוק:**
כל URL מכיל `//`, ולכן מחרוזת הייתה נקראת כהערה — `'✅ נשמר'` היה נספר
כסימן אסור, וביטוי רגולרי שמכיל כוכבית היה פותח בלוק שלא נסגר.

### ⛔ מה התקן אינו
**אינו יעד אחוזים.** ⛔ אין להשוות יחסי הערות בין האפליקציות, ואין
להמציא הערות כדי להגיע למספר. הדרישה היא **כיסוי**: כל אזור לוגי, כל
החלטה ארכיטקטונית וכל באג שתוקן — מכוסים, באותה צורה, בארבעתן.
⚠️ ואחוז גבוה אינו ראיה לכיסוי: קובץ יכול להיות מלא בהערות שחוזרות על
שם הפונקציה, ולא לענות על אף אחת מארבע השאלות שהתקן שואל.

<!-- SHARED:end -->

**כאן:** ⚠️ הערה נגררת נשארת נגררת — `if (YESHIVA !== 'rishon') return report;`
נושאת את «⛔ ראשון לציון בלבד (סבב 13) — דליפה בין מוסדות» באותה שורה;
ההצמדה היא מה שמונע «לפשט» את התנאי.

---

<!-- SHARED:start id="iron-rule-12-capabilities" -->
## ⭐ כלל ברזל 12 — יכולת משותפת נמדדת בשלושה ממדים (סבב 30 — השלמה, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את אחד-עשר כללי הברזל שלמעלה.

> **יכולת שמוכרזת «משותפת» מוגדרת בשלושה ממדים, לא באחד: ליבה זהה
> בית-לבית · **נקודת הפעלה זהה** · רישום תואם במטריצת היכולות.
> ⛔ שני מימושים שונים לאותה יכולת הם פער — גם כששניהם עובדים היום.
> הפער מתגלה רק כשאחד מהם נשבר, וזה בדיוק מאוחר מדי.**

### הבעיה שנמדדה
סבב 30 חילץ את הגיבוי היומי לבלוק משותף אחד, זהה בית-לבית בארבעתן —
כלומר קיים את כלל ברזל 7 במלואו. ובכל זאת התברר שהגיבוי של
hanhala-ruchanit **הפסיק לרוץ ב-2026-07-20** ואיש לא ידע:

| | היכן הופעל הגיבוי | התוצאה |
|---|---|---|
| schar-limud | בכניסה למערכת (`enterApp`) | ✅ רץ בכל יום שבו מישהו נכנס |
| hanhala-ruchanit | **מתוך מסלול הדחיפה** (`ysPushToCloud`) | 🔴 25 יום בלי גיבוי |

ההבדל אינו באג בקוד — **הקוד עצמו היה תקין בשתיהן.** ההבדל הוא היכן
הוא מופעל: בהנהלה הגיבוי רץ רק כשמישהו דחף אחד מארבעת המפתחות שהוא
גיבה. בתקופה סטטית — חופשה, שבוע בלי עריכות — אין דחיפה, אין גיבוי,
ואין שום סימן. ⛔ **מנגנון בטיחות שתלוי בפעילות רגילה אינו מנגנון
בטיחות:** הוא נעצר בדיוק בתקופה שבה תקלה עוברת בלי שאיש שם לב.

⚠️ **וזה נמדד ולא הונח:** `sync_log` מראה **444 פעולות ב-30 יום ו-47
בשבוע** באותה תקופה — האפליקציה הייתה בשימוש מלא. תשע מפתחות נוספים לא
גובו **38 יום**. כלומר לא «אף אחד לא נכנס», אלא «אף אחד לא דחף את
המפתחות הנכונים».

### שלושת הממדים
1. **ליבה זהה בית-לבית** — מה שכלל ברזל 7 כבר דורש, ומה
   ש-`check-status-area.mjs` כבר אוכף.
2. **⭐ נקודת הפעלה זהה.** לכל יכולת משותפת יש **תפקיד** קבוע שממנו היא
   מופעלת, זהה בארבעתן, גם כששם הפונקציה נבדל:
   - **יכולות עלייה** (`lsBoot` · `pendBoot` · `bkBoot`) — **פונקציית
     העלייה של האפליקציה**, זו שכבר קוראת לשתי האחרות.
   - **יכולות מסך** (`statusAreaMount` · `bkStatusMount`) — פונקציית
     מסך ההגדרות.
   ⛔ **ואין נקודת הפעלה שנייה.** קריאה נוספת לאותה יכולת ממקום אחר היא
   בדיוק הסטייה הזו בהתהוותה, גם אם היא «רק ליתר ביטחון».
3. **רישום תואם במטריצה** — יכולת שמסומנת ✅ קיימת בקוד, ומה שקיים בקוד
   מסומן ✅. ⛔ מטריצה שאינה תואמת לקוד גרועה מהיעדר מטריצה: היא נקראת
   כעדות.

### ⛔ מעטפת דקה אינה «נוחות» — היא נקודת הפעלה שנייה
כשיכולת נקראת דרך פונקציה מקומית שרק עוטפת אותה (`ysMaybeDailyBackup`,
`slMaybeDailyBackup`), **כל מקום בקוד יכול לקרוא לה** — והכלל שקובע
נקודת הפעלה אחת הופך להמלצה. לכן המעטפות הוסרו, והמודול חושף **קריאה
אחת** שקוד האפליקציה רשאי לעשות. הפונקציה הפנימית נשארת חשופה לבדיקות
ולמודול עצמו, ו-`check-capabilities.mjs` נכשל על כל קריאה אליה מקוד
האפליקציה.

### ⚠️ התרעה על מנגנון שקט שנעצר — `BK_STALE_DAYS`
המדד השני שהיה חסר: **גם אחרי שהגיבוי מופעל נכון, אין דרך לדעת שהוא
באמת רץ.** לכן «מידע טכני ›» מציג «גובה לאחרונה», ומעל
**`BK_STALE_DAYS` = 3 ימים** נוספת שורת התרעה.
⚠️ **הסף הוא שלושה ימים ולא יום אחד** — הדגל היומי אומר שכשל בודד הוא
רעש לגיטימי (מכשיר אופליין, סוף שבוע), ואילו שלושה ימים הם **דפוס**.
עם הסף הזה, ההשבתה בת 25 הימים הייתה מתגלה ביום הרביעי.
⛔ **ולא התראה קופצת** — סבב 15 קבע ששקט הוא ברירת המחדל באזור המצב,
וההתרעה יושבת במקום שבו מסתכלים כשבודקים.

### הבדיקה האוכפת
`tools/check-capabilities.mjs` (ב-gius: `scripts/`, ו-`check-js.mjs`
קורא לה) — **זהה בית-לבית בארבעת הריפו פרט לבלוק `APP` שבראשה**, ורצה
עם שערי התחביר לפני כל דחיפה. היא נכשלת על שלושה סוגי סטייה:
- **א. ליבה** — הבלוק המשותף של היכולת אינו תואם לחתימה הקנונית.
- **ב. נקודת הפעלה** — פונקציית החיווט אינה נקראת מתוך הפונקציה
  הקנונית, נקראת **גם** ממקום אחר, או שפונקציה אסורה (`bkMaybeDaily`)
  נקראת מקוד האפליקציה.
- **ג. מטריצה** — יכולת שמסומנת ✅ אינה קיימת בקוד, או להפך.

⚠️ **הסריקה נעשית על קוד בלבד**, אחרי טוקניזציה שמנקה הערות ומחרוזות.
⛔ אין להחליף אותה בחיפוש טקסטואלי: שם הפונקציה מופיע גם ברשימת ה-API
שבהערת המודול, וכל אזכור בתיעוד היה נספר כקריאה.

### ⛔ מנגנון פיצוי נבדק מחדש כשהמבנה שהוליד אותו תוקן (סבב 32)
**מנגנון שנולד כדי לפצות על מבנה או על מגבלה — נבדק מחדש כשהמבנה מתוקן,
ומוסר אם אינו נחוץ עוד.** ⛔ מנגנון פיצוי ששרד את הסיבה לו הוא **חוב טכני
שנראה כמו ארכיטקטורה**: הוא עבר ביקורת פעם אחת, הוא מתועד, והוא נראה
כהחלטה — ולכן איש אינו שואל עליו שוב.

⚠️ **וזה ממד רביעי ולא חזרה על השלושה שלמעלה.** הם שואלים «האם היכולת
מיושמת אותו דבר בארבעתן»; זה שואל **«האם היא עדיין נחוצה בכלל»**. יכולת
יכולה להיות זהה בית-לבית, מופעלת מאותה נקודה, ורשומה נכון במטריצה — ועדיין
לפצות על בעיה שנפתרה לפני שני סבבים.

**שלוש הדוגמאות שהולידו את הסעיף**, וכולן נמדדו:
- **סבב 24** — `SETUP_SQL_FALLBACK` ב-schar-limud: עותק מוטבע של הסכימה
  שנועד לכסות על כך שאין קובץ התקנה. משנוצר הקובץ, העותק נשאר — מקור אמת
  שני שמתיישן בשקט.
- **סבב 30** — הגיבוי היומי הופעל ממסלול הדחיפה, פיצוי על כך שלא הייתה
  נקודת עלייה משותפת. משנוצרה `bkBoot`, החיווט הישן היה נשאר ומשבית את
  הגיבוי 25 יום.
- **סבב 32** — שתי הדוגמאות החיות: **הארכוב ביומן** (פיצוי על כך שכל
  היומן נמשך כערך `kv` יחיד — בוטל כשהטבלאות המובנות אפשרו שליפה
  מסוננת), ו**הסכימה המוטבעת בהנהלה** (אותו דפוס של סבב 24, ריפו אחר).

⚠️ **הבדיקה היא שאלה, לא סריקה אוטומטית** — ולכן היא נשאלת בכל סבב שמשנה
מבנה: *מה נבנה כדי לפצות על המבנה הזה, והאם הוא עדיין נחוץ?* ⛔ והתשובה
נרשמת גם כשהיא «כן, ומהסיבה הזו» — שקט כאן נקרא כ«לא נשאל».
⚠️ **וההסרה אינה מיידית:** המנגנון הישן נשאר כנתיב חזרה, עם **טריגר כתוב**
לכיבויו ולמחיקתו — בדיוק כפי שסבב 30 קבע לכתיבה הכפולה.

**שינוי מכוון ביכולת משותפת** = עדכון בארבע האפליקציות **ובארבעת עותקי
קובץ הבדיקה** (החתימות ונקודות ההפעלה כתובות בתוכו), באותו סבב.
<!-- SHARED:end -->

---

<!-- SHARED:start id="backup-module" -->
## ⭐ מודול הגיבוי היומי ויומן הפעולות (סבב 30)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius).

הגיבוי היומי ויומן הפעולות הם בלוק קוד משותף, זהה בית-לבית בארבע
האפליקציות, בין `/* ═══ גיבוי יומי ויומן פעולות` לבין
`סוף מודול הגיבוי היומי`. מעליו יושבת `BK_CFG` — הדבר היחיד שנבדל.
**ה-API:** `bkBoot` · `bkMaybeDaily` · `bkLastAt`/`bkStale` ·
`bkStatusHTML`/`bkStatusMount` · `logAction`/`logFlush` · `bkToday`/`bkSig`.

- ⛔ **נקודת ההפעלה היחידה היא `bkBoot()` מפונקציית העלייה** (כלל ברזל 12) —
  קריאה ל-`bkMaybeDaily` מקוד האפליקציה אסורה, והבודק נכשל עליה.
  `logFlush` היא החריג המותר: ריקון תור חוזר, מותר על חזרת רשת או כניסה
  מוצלחת.
- **הליבה:** נעילת ריצה, דגל-יומי שנכתב **אחרי** ההצלחה, ובדיקת `error`
  מפורשת על ה-`select` ועל ה-`insert`. ⛔ אין להחזיר כתיבת-דגל-מראש
  (סבב 6) — גיבוי שנכשל היה מדלג בשקט על יממה שלמה.
- ⭐ **גיבוי דיפרנציאלי** — ערך שלא השתנה מאז הגיבוי האחרון אינו נכתב שוב,
  לפי חתימה מקומית (אורך + FNV-1a) שאינה נוגעת ברשת. ⚠️ זה תנאי קיום ולא
  קיצור דרך: כתיבה יומית עיוורת של הערך הגדול בארגון (~1.2MB) הייתה
  מוסיפה ~36MB בחודש. ⛔ ובספק — כותבים: חתימה חסרה או מכשיר חדש מביאים
  לכתיבה מלאה.
- ⭐ **מדיניות שמירה — עותק יומי נשמר 30 יום** (`BK_RETENTION_DAYS`,
  השלמת סבב 35): אחרי גיבוי מוצלח נגרעים מ-`kv_backup` עותקים ישנים
  מזה, לפי **רשימת-היתר של מפתחות** שנגזרת מ-`BK_CFG.sources()` —
  ⛔ לא לפי קידומת (השלמת סבב 35) — קידומת ריקה (הנהלה) הייתה תופסת גם
  מפתחות של אפליקציה אחרת. נכשלת סגור ואינה מפילה את הגיבוי; `retention`
  נרשם ליומן רק כשנמחק משהו בפועל.
- ⭐ **הפינוי עצמו רץ במסד, לא בלקוח** (סבב 35ג) — משימת `pg_cron` יומית
  שמוחקת עותקים בני 30+ יום לפי **רשימת-היתר מפורשת של מפתחות**, בדיוק
  אותה רשימה מבנית שב-`BK_CFG.sources()`. ⚠️ **שתי השכבות מכוונות ואינן
  כפילות:** ל-`kv_backup` יש `insert`+`select` בלבד לשני התפקידים (סבב 29,
  אומת מול שני הפרויקטים) ו⛔ אין לפתוח לה `delete` ל-`anon` — כלומר
  **המסד מפנה, והלקוח לא יכול ולא צריך**. `_bkRetention` שבמודול נשארת
  שכבה שנכשלת-סגור: מנסה, נדחית, ומדלגת בשקט בלי להפיל את הגיבוי.
  ⛔ אין להסיר אותה מהקוד (סבב 35ג) — היא מה שיפעל אם תוענק הרשאה
  סקופית, והיא מה שהבדיקות אוכפות. ⭐ **שתי המיגרציות הורצו ואומתו
  ב-2026-08-18** (סבב 36): `hanhala-ruchanit/migrations/004_backup_retention_cron.sql`
  (הפרויקט המשותף — חלה גם על yoman ועל schar) ו-`gius/migrations/0005_backup_retention_cron.sql`.
  המשימה `bk_retention_daily` פעילה בשני הפרויקטים בתזמון `'0 3 * * *'`,
  והריצה הראשונה במשותף פינתה 37 שורות ונרשמה ב-`sync_log`.
- ⛔ **סינון סודות** (השלמת סבב 35) — מפתח או שדה שברשימת
  `BK_CFG.secrets` אינו נכתב לגיבוי לעולם; הרשימה פרמטר פר-אפליקציה
  (בשכר: `SL_NEVER_MIRROR_SETTINGS`, שרוקנה — המנגנון דרוך גם כשהיא
  ריקה). הלקח: `admin_pass` שרד בגיבויים אחרי שנמחק מהמקור, ונוקה
  ידנית (17.8).
- ⛔ **טבלה שיש בה סוד מוחרגת מהגיבוי או נשלפת בעמודות מפורשות** (סבב 30) —
  `kv_backup` קריאה ל-`anon`, וגיבוי גורף של טבלת משתמשים היה מעתיק
  סיסמאות למקום שני.
- **היעד:** `kv_backup`, והיומן ל-`sync_log` — שתיהן `INSERT`+`SELECT`
  בלבד לשני התפקידים (כלל ברזל 10 סעיף 9). ב-gius הן חיות בפרויקט הנפרד
  שלה (`migrations/0004`), בסכימה זהה בית-לבית. ⚠️ רשימת האירועים שנרשמת
  ליומן היא פר-אפליקציה ואינה מיושרת — המשותף הוא המנגנון.
- **«גובה לאחרונה» והתרעת `BK_STALE_DAYS` = 3** מוצגים ב«מידע טכני ›»
  (כלל ברזל 12) — מנגנון שקט שנעצר נראה בדיוק כמו מנגנון שקט שעובד, ולכן
  חייבת להיות דרך לראות שהוא באמת רץ.
- ⛔ **גיבוי חד-פעמי לפני-פעולה** (קידומות `PRE_SYNC_UNIFY_`,
  `PRE_ROUND3B_`, `ORPHAN_`, `pre-delete-…`) **אינו נמחק אוטומטית
  לעולם** (סבב 30) — אינו מצטבר, והוא מחוץ לרשימת-ההיתר של מדיניות
  השמירה מעצם בנייתה, וכך גם מחוץ לרשימת-ההיתר של משימת ה-`pg_cron`.
  ⭐ הכלל (השלמת סבב 35): **באחריות יוצרו למחקו כשהפער שהוא מגן עליו
  נסגר** — לא באחריות המודול.
  ⚠️ **עובדה, ולא הנחה (סבב 35ג): 57 השורות שהיו בפרויקט המשותף תחת
  הקידומות האלה נמחקו ב-2026-08-18 ואינן ניתנות לשחזור** — הפרויקט על
  מסלול Free, שאינו כולל גיבויי פרויקט. מי שיחפש אותן לא ימצא אותן.
  ⛔ האיסור העקרוני נשאר בתוקף לכל גיבוי `PRE_*` **עתידי**.
<!-- SHARED:end -->

### היישום כאן (yoman-avoda)
`BK_CFG` פר-מוסד **בשלוש שכבות שאין לערבב** (סבב 30): הטבלה בענן היא
`KV_TABLE`, הדגל המקומי נושא סיומת `LS`, ומפתח הגיבוי נושא קידומת
`<מוסד>_`. שישה מפתחות מגובים (`tb_entries`, `tb_archive`, `tb_cats`,
`tb_subs`, `tb_subs_meta`, `tb_wa_phone`). ⛔ `bkBoot()` נקראת
מ-`selectYeshiva()` — `BK_CFG.sources()` נשענת על `KV_TABLE` שהוא `null`
עד בחירת מוסד, והפעלה מוקדמת יותר הייתה מגבה ריק בשקט. ⚠️ אין כאן סוד
להחריג — אין טבלת משתמשים.

---

<!-- SHARED:start id="iron-rule-13-shared-scope" -->
## ⭐ כלל ברזל 13 — סבב שנוגע ב-SHARED נפתח עם גישה לארבעת הריפו (סבב 32ב, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את שנים-עשר כללי הברזל שלמעלה.

> ⛔ **סבב שנוגע בבלוק SHARED נפתח מלכתחילה עם גישה לארבעת הריפו.**
> אי אפשר לצרף ריפו לסשן קיים, ולכן סשן שאין לו גישה לארבעתם **אינו
> נוגע בבלוקי SHARED** — הוא רושם את הצורך וסוגר.

### הבעיה שנמדדה
בסבב 32 השתנו שני בלוקי SHARED — `capability-matrix`
ו-`iron-rule-12-capabilities` — **בשניים מתוך ארבעת הריפו**: לסשן לא
הייתה גישה ל-schar-limud ול-gius, וניסיון לצרף אותם באמצע העבודה נחסם.
התוצאה היא בדיוק המצב שכלל ברזל 8 סעיף 3 אוסר — פרק «משותף» שאינו זהה
בארבעתם — ⚠️ ובצורתו המסוכנת ביותר: `check-docs.mjs` של שני הריפו שלא
עודכנו **המשיך לעבור** אצלם, מפני שהחתימות הישנות שלהם תאמו לתוכן הישן
שלהם. **החתימות הישנות הסתירו את הסטייה** — השער שנבנה לתפוס סחיפה בין
הריפו אישר אותה, כי הוא מודד כל ריפו מול העותק המקומי של הרשימה
הקנונית, ולא בין הריפו זה לזה.

### ⛔ ההשלכה המבצעית — לפני פתיחת הסבב, לא באמצעו
- סבב שמתכנן לגעת בבלוק SHARED — או שעלול לגעת בו: שינוי יכולת, כלל
  ברזל חדש, עדכון תא במטריצה — **נפתח מלכתחילה עם ארבעת הריפו בהיקף.**
- סשן שמגלה באמצע העבודה שנדרש שינוי SHARED ואין לו גישה לארבעתם —
  ⛔ **אינו משנה את הבלוק בחלק מהריפו.** הוא רושם את הצורך בפרק הסבב
  (מה צריך להשתנות, ולמה) וסוגר; השינוי עצמו נעשה בסבב שנפתח נכון.
  ⚠️ עדיף פער **גלוי** — בלוק שטרם עודכן באף ריפו — על פער **שקט** שהשער
  מאשר בשניים מארבעה.
- סבב 32 בחר בדיעבד בדרך השנייה — עדכן בשניים ורשם טריגר מפורט עם שתי
  החתימות — וזה מה שאיפשר לסבב 32ב לסגור את הפער בהעתקה בית-לבית.
  ⛔ אבל זו נסיבה מקילה, לא דפוס: הטריגר ישב בפרק פרטי של שני ריפו,
  ושום שער לא אכף אותו.
<!-- SHARED:end -->

---

<!-- SHARED:start id="iron-rule-14-org-wide" -->
## ⭐ כלל ברזל 14 — קיים באחת = קיים בארבעתן, או חריגה מנומקת (סבב 33, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את שלושה-עשר כללי הברזל שלמעלה.

> ⛔ **יכולת, שער או מודול שקיים באפליקציה אחת — או שהוא קיים בארבעתן, או
> שהוא רשום כחריגה מנומקת** (במטריצת היכולות, או ברשימת-ההיתר של
> `check-structure.mjs`). **«קיים רק באחת, בשקט» אינו מצב חוקי.**

### הבעיה שנמדדה
`check-js.mjs` — השער שמחלץ את ה-JS המוטבע ומריץ עליו `node --check` — נכתב
ב-gius בסבב 11, **וחי שם לבדו עשרה סבבים**. בשלוש האחיות אותה עבודה בדיוק
נעשתה בהעתקה ידנית של שבע פקודות מתוך פרק «לפני כל push»: אותן בדיקות, בלי
שום דבר שמוודא שכולן רצו. ⛔ **ואיש לא החליט על ההבדל הזה** — לא «הוחל
בכולן», לא «אינו רלוונטי, וזה הנימוק», ולא «פער עם טריגר». הוא פשוט היה שם.

⚠️ **וזה לא היה יחיד.** אותה סריקה מצאה שרידים שישבו בשורש של כל ריפו בלי
שאיש החליט עליהם — שלושה קובצי SQL שתוכנם מכוסה במלואו ב-`migrations/`,
סקריפט בנייה שאף workflow אינו קורא לו, ו-`.well-known/assetlinks.json`
משתי אפליקציות שכבר אינן TWA. כל אחד מהם היה «נכון בזמנו», וכל אחד מהם
שרד את הסיבה שלו.

### ⛔ למה זה כלל נפרד ולא כלל ברזל 12
כלל ברזל 12 שואל שלוש שאלות על יכולת שכבר **מוכרזת** משותפת: ליבה זהה,
נקודת הפעלה זהה, רישום תואם. ⚠️ **הוא אינו שואל דבר על יכולת שמעולם לא
הוכרזה** — ובדיוק שם `check-js` שרד: הוא לא היה «משותף שנסחף», הוא היה
**פרטי שאיש לא שאל עליו**. כלל 14 סוגר את החור הזה: קיום באחת הוא בעצמו
השאלה, גם לפני שמישהו קרא לזה יכולת משותפת.

### שלוש התשובות המותרות — ורק שלוש
לכל רכיב שקיים באפליקציה אחת:
1. **הוחל בארבעתן** — עם ציון מה נכנס לכל אחת.
2. **חריגה מנומקת** — נרשמת **במקום שהשער קורא**: תא «לא רלוונטי»/❌ עם
   נימוק במטריצת היכולות, או שורה ברשימת-ההיתר של `check-structure.mjs`
   שנושאת את הסיבה. ⛔ נימוק ב-CLAUDE.md לבדו אינו נחשב — הוא אינו נאכף.
3. **פער מתועד עם טריגר** — מה חסר, ומה האירוע שמחייב לפתוח את הסבב.

⛔ **מה שאסור הוא הרביעית — קיום שקט, בלי אף אחת מהשלוש.**

### ⛔ פער שנסגר בפעולת מנהל — מי מוחק את שורתו
הכלל הרגיל הוא שהסבב שסוגר פער מוחק את שורתו **באותו קומיט**. ⚠️ הוא
אינו מכסה את המקרה השכיח כאן: פער שנסגר בפעולת **מנהל** — הרצת מיגרציה,
מחיקת מפתח מהמסד, החלפת סוד. הסשן אינו יכול למחוק שורה שבזמנו עדיין
פתוחה, והמנהל אינו עורך קבצים — ולכן השורה שורדת את הסיבה שלה, ונקראת
כמציאות.

⛔ **שורת פער שנסגרה בפעולת מנהל נמחקת בסבב הראשון שנוגע בקובץ, כסעיף
אחרון, ⛔ ובלי לפתוח סבב ייעודי** (נלמד בסבב 39). ⚠️ המחיקה מותנית
במדידה — ר' כלל ברזל 15 — ⛔ ולא בזיכרון של מי שביקש אותה.

### ⚠️ «אחיד» אינו «זהה» — והשער אוכף את הראשון
`check-js.mjs` זהה בית-לבית בארבעתן פרט לבלוק `APP`, אבל **תוכן `APP.rules`
נבדל בכוונה**: הוא נמדד מ-`sw.js` ומ-`index.html` של כל אפליקציה, ול-schar
אין רשימת CDN במטמון ואין דף אופליין. ⛔ **כלל שנכתב על מה שאינו קיים הוא
הצהרה ולא מדידה** — בדיוק הכשל שכלל ברזל 8 סעיף 4 אוסר, בציר אחר.
כך גם המבנה: הסט הקנוני זהה, והחריגות (`setup-db.html` · `gen-icons.mjs` ·
`tools/fixtures`) יושבות ברשימת-היתר **עם נימוק לכל שורה**. ⚠️ `.nojekyll`
היה ברשימה הזו עד סבב 39 ועלה לסט הקנוני — חריגה שקיימת בשתיים מארבע היא
אסימטריה שלא הוכרעה, לא «חריגה מנומקת».

### הבדיקה האוכפת
`tools/check-structure.mjs` — **זהה בית-לבית בארבעת הריפו פרט לבלוק `APP`
שבראשה**, ורצה מתוך `check-js.mjs` לפני כל דחיפה. היא נכשלת על שלושה סוגי
סטייה:
- **א. תיקיות** — סט התיקיות בשורש אינו הסט הקנוני (`.github` · `android` ·
  `icons` · `migrations` · `signing` · `tools`). תיקייה חסרה **או עודפת**
  מפילה את השער.
- **ב. קובצי שורש** — קובץ שאינו ברשימה הסגורה המשותפת (`CLAUDE.md` ·
  `CONTEXT.md` · `README.md` · `index.html` · `manifest.json` · `sw.js`)
  ואינו ברשימת-ההיתר הפר-אפליקציתית.
- **ג. `tools/`** — שבעת הבודקים המשותפים חייבים להתקיים; כל קובץ אחר חייב
  להיות `test_round*` או חריגה מנומקת.

⚠️ **ורשימת-היתר שהתיישנה מפילה גם היא:** חריגה רשומה שאינה קיימת בפועל
נכשלת. ⛔ אחרת הרשימה הייתה הופכת בעצמה לשריד — בדיוק סוג הדבר שהסבב הזה
בא לסלק.
<!-- SHARED:end -->

---

<!-- SHARED:start id="iron-rule-15-gaps-verified" -->
## ⭐ כלל ברזל 15 — פרק «פערים פתוחים» נאכף בשער (סבב 39, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את ארבעה-עשר כללי הברזל שלמעלה.

> ⛔ **כל שורת פער נושאת תג שאומר איך יודעים שהיא עדיין פתוחה.** תנאי
> שניתן למדוד מהריפו — השער בודק אותו ו**נופל כשהוא כבר אינו מתקיים**.
> תנאי שאינו ניתן למדידה — נרשם ככזה, **בכתב ועם נימוק**. ⛔ שורה בלי תג
> אינה מצב חוקי.

### הבעיה שנמדדה
פרק «פערים פתוחים» היה הפרק היחיד בקובץ ש**שום שער לא קרא**. התוצאה
נמדדה בסבב 39 עצמו: שורות תיארו פערים שנסגרו — מפתח `kv` שנמחק
מהמסד, מיגרציה שהורצה מחדש, ומסלול `rpc` שמעולם לא היה קיים — והן
נקראו כמציאות עד שמישהו בדק ידנית. ⚠️ **וזה בדיוק ההפך מכשל התיעוד
הרגיל:** לא «חסר תיעוד» אלא **תיעוד שממשיך לתאר עולם שהשתנה**, בפרק
שכל תפקידו לומר מה עוד פתוח.

### תבנית התג — ⛔ אחת, ובסוף השורה
כל פריט ברמה העליונה בפרק «פערים פתוחים» מסתיים באחד משני אלה:

- **`[מדיד: <תנאי>]`** — התנאי שמתקיים **כל עוד הפער פתוח**.
- **`[לא-מדיד: <נימוק>]`** — ⛔ עם נימוק אמיתי, לא «לא ניתן».

⚠️ **התג נדרש מפני שפרוזה אינה ניתנת לגזירה אמינה.** ⛔ ואין לנחש מתוך
הטקסט (סבב 39) — ניחוש היה מייצר שער שנופל על ניסוח ועובר על מציאות,
כלומר גרוע משער שאינו קיים.

**ארבעת התנאים המדידים, וזה הסט כולו:**

| תנאי | הפער פתוח כל עוד |
|---|---|
| `code:<מחרוזת>` | המחרוזת **מופיעה** ב-`index.html` |
| `no-code:<מחרוזת>` | המחרוזת **אינה מופיעה** ב-`index.html` |
| `file:<נתיב>` | הקובץ **קיים** |
| `no-file:<נתיב>` | הקובץ **אינו קיים** |

### שלושת הכללים
1. **⛔ תנאי מדיד שחדל להתקיים מפיל את השער.** זו כל הנקודה: הפער נסגר,
   והשורה חייבת להימחק (כלל ברזל 14, «פער שנסגר בפעולת מנהל»).
2. **⛔ הבודק מדווח ואינו מוחק.** מחיקה אוטומטית של תיעוד היא בדיוק סוג
   הפעולה שאין ממנה חזרה בלי לקרוא git, והיא גם עוקפת את שיקול הדעת
   שקובע **מה** לכתוב במקום. השער אומר «השורה הזו נסגרה» ועוצר.
3. **⛔ `לא-מדיד` אינו פטור שקט.** הוא רישום מפורש שנקרא בעין, ומספרו
   נספר ומוצג. ⚠️ שורה שסומנה `לא-מדיד` רק כדי לעקוף את השער היא בדיוק
   הכשל שכלל ברזל 8 סעיף 4 אוסר, בציר אחר.

### ⭐ שני חישוקים על העוגן — ⛔ בלי שניהם השער נכשל בכיוון המסוכן
המנהל מדד: שינוי **רווחים בלבד** באותה שורה — `X = true` → `X  =  true` —
הפיל את השער עם «⭐ הפער נסגר», בזמן שהקוד **לא השתנה**. ⛔ זה כשל בכיוון
המסוכן: הודעה שמכריזה שפער נסגר היא הודעה שמזמינה מחיקת שורת תיעוד.

1. **⚠️ נרמול רווחים לפני ההשוואה.** רצף רווחים, טאבים ושורות חדשות מתקפל
   לרווח יחיד **בשני הצדדים**. ⛔ שמות, מרכאות ותוכן **אינם** מנורמלים —
   נרמול שלהם היה הופך את העוגן לשער שעובר על קוד אחר לגמרי.
2. **⭐ עוגן בסיס לכל תג `code:` — שם הסמל לבדו.** לא נמצא העוגן המלא,
   ⛔ **והסמל עצמו אינו קיים** ⇒ **«⚠️ העוגן נרקב — דורש בדיקה ידנית»**,
   ⛔ **ולעולם לא «הפער נסגר»** (סבב 39): סמל שנעלם — שינוי שם, מחיקה,
   ריפקטור — אינו ראיה שהפער נסגר אלא שהעוגן הפסיק להצביע על משהו,
   וההכרעה חוזרת לבן-אדם. נמצא הסמל ולא נמצא העוגן המלא ⇒ הפער אכן נסגר,
   וההודעה אומרת גם שעוגן הבסיס כן נמצא.
   ⚠️ מילות מפתח מדולגות בגזירת הסמל — הסמל ב-`function migrateOutbox`
   הוא `migrateOutbox`.

### הבדיקה האוכפת
`tools/check-gaps.mjs` — **זהה בית-לבית בארבעת הריפו** (אין בה בלוק `APP`;
היא קוראת את `CLAUDE.md` שלצידה), ורצה מתוך `check-js.mjs` לפני כל דחיפה.
היא נכשלת על ארבעה סוגי סטייה:
- **א. תג חסר** — פריט ברמה העליונה בלי `[מדיד:…]` ובלי `[לא-מדיד:…]`.
- **ב. תנאי שאינו מוכר** — `מדיד` עם תנאי שאינו מארבעת הסוגים שבטבלה.
- **ג. ⚠️ העוגן נרקב** — הסמל שבתג `code:` אינו קיים ב-`index.html`.
- **ד. ⭐ פער שנסגר** — התנאי המדיד כבר אינו מתקיים, והסמל כן קיים.

⚠️ **הפרק כולו אופציונלי** — ריפו בלי «פערים פתוחים» עובר. ⛔ אבל פרק
שקיים נבדק במלואו.
<!-- SHARED:end -->

**כאן:** `APP.rules` של `tools/check-js.mjs` נמדדו מהקבצים של האפליקציה
הזו: תבנית `CACHE_NAME` (`yoman-avoda-v<N>` — תבנית ולא מספר קבוע, כלל
שמקבע מספר חוסם את הקידום שהוא עצמו מחייב), דילוג על supabase, ניקוי מטמון
לפי קידומת, `mode:'cors'`, Content-Type בדף האופליין, ונעילת supabase-js
ל-2.111.0. חריגה מנומקת אחת ב-`check-structure.mjs`: `tools/fixtures/`
(הפיקסטורה של סבב 31). ⚠️ `.nojekyll` **אינו חריגה עוד** — הוא עלה לסט
הקנוני בסבב 39 והשער דורש אותו בארבעתן.

---

<!-- SHARED:start id="iron-rule-16-remnant" -->
## ⭐ כלל ברזל 16 — הנחת השריד (סבב 39, אוגוסט 2026)
**הפרק הזה זהה מילה במילה בארבעת קבצי ה-CLAUDE.md** (hanhala-ruchanit,
schar-limud, yoman-avoda, gius), והוא ממשיך את חמישה-עשר כללי הברזל שלמעלה.

> ⛔ **הוראת מחיקה שנימוקה «שריד», «מיותר» או «לא בשימוש» אינה מבוצעת לפני
> מדידה שמוכיחה שאין לו קורא.** נמצא קורא — הסשן **עוצר ומדווח**,
> ⛔ **ואינו מוחק, גם אם ההוראה הייתה מפורשת.**

### הבעיה שנמדדה
שלושה ממצאים בסבב 39 זוהו כ«שרידים» **לפי המראה** — קובץ שנראה יתום, מנגנון
שנראה מת, כלי שנראה כמו שארית של תקופה שהסתיימה. ⚠️ **ובשלושתם המדידה
הפריכה את ההנחה, ומחיקה הייתה שוברת אפליקציה:**
- **`android/…/res/xml/file_paths.xml` ב-gius** — ל-gius **אין** גשר שיתוף,
  ולכן הקובץ נראה כשארית של הדפוס שהועתק מיומן. ⚠️ המדידה: ה-`AndroidManifest`
  שלה **מכריז `FileProvider` שמצביע עליו**. מחיקה = קריסה בזמן ריצה.
- **`migrateOutbox()` ב-gius** — התור היוצא הוסר בסבב 12, והפונקציה שממירה
  אותו נראית כקוד שאיבד את מושאו. ⚠️ המדידה: היא ההגירה החד-פעמית שמצילה תור
  ישן במכשיר שטרם עבר. מחיקה = אובדן שקט של פעולות שממתינות במכשיר בשטח.
- **`_bkRetention` במודול הגיבוי** — הפינוי רץ מאז סבב 35ג **במסד** ב-`pg_cron`,
  ולכן השכבה שבלקוח נראית מיותרת. ⚠️ המדידה: היא שכבה **שנכשלת-סגור** שהבדיקות
  אוכפות, והיא מה שיפעל אם תוענק הרשאה סקופית.

⚠️ **המשותף לשלושתם: «נראה כמו שריד» הוא תיאור של המראה, לא ממצא.** מה
שהופך רכיב לשריד הוא **היעדר קורא** — וזה נתון שנמדד, לא נתון שמנחשים.

### ⛔ שלושת התנאים לפני כל מחיקה
1. **חיפוש קוראים בכל ארבעת הריפו** — לא בקובץ אחד ולא בסוג קובץ אחד
   (הלקח של כלל ברזל 10 סעיף 8: סריקה שמוגבלת לסוג קובץ אחד היא ראיה
   לסוג שנסרק, ותו לא). כולל: קוד · workflow · סקריפט · manifest אנדרואיד ·
   תיעוד · קובצי בדיקה.
2. **בדיקה שהרכיב אינו נתיב-חזרה או שכבת נפילה-חזרה** — «אין לו קוראים
   היום» אינו «אינו נחוץ»: מנגנון שנכשל-סגור, הגירה חד-פעמית ונתיב חזרה
   מכובה הם בדיוק רכיבים שאין להם קוראים **עד הרגע שבו הם נחוצים**.
3. **רישום המדידה בפרק הסבב** — מה נסרק, ומה נמצא. ⛔ «נמחק כשריד» בלי
   המדידה שלצידו הוא בדיוק הסוג של תיעוד שכלל ברזל 8 סעיף 6 אוסר.

### ⚠️ מה הכלל **אינו**
⛔ **אינו איסור על מחיקה.** באותו סבב עצמו נמחק `signing/sign-apk.bat`
**אחרי** שנמדד שאין לו אף קורא בארבעת הריפו — וזו בדיוק הדרך: המדידה היא
שהתירה את המחיקה, ולא המראה. הכלל אוסר דבר אחד — **מחיקה על סמך הנחה**.

⚠️ **והוא חל גם על הוראה מפורשת של המנהל.** מנהל שכותב «מחק את X, זה שריד»
מתאר את מה שהוא רואה; הסשן הוא זה שיכול למדוד. נמצא קורא ⇒ הסשן **מדווח
ומציע**, והמחיקה חוזרת להחלטה — בדיוק כמו כלל ברזל 9 («החלטת מודל אבטחה
היא של המנהל, לא של הסשן»), בכיוון ההפוך.
<!-- SHARED:end -->


---

## רתמות הבדיקה ההיסטוריות — מצב מקובל, ⛔ לא פער

סבבים מוקדמים הריצו את הרתמות שלהם **בסשן בלבד** ולא שמרו אותן כקובץ;
מסבב 30 כל סבב שומר קובץ בדיקה בריפו (כלל ברזל 8 סעיף 6). ⛔ **הפער הזה
אינו נסגר בדיעבד, וזו החלטה ולא הזנחה** (סבב 39):

- ⚠️ **רתמה שתיכתב היום מול הקוד הנוכחי מקבעת את ההתנהגות הנוכחית** — כולל
  באג שטרם התגלה. היא תיראה כמו רשת ביטחון, ותפעל בפועל כאישור: כל תיקון
  עתידי של אותו באג ייראה כרגרסיה ויפיל את השער.
- ⚠️ **הלקחים עצמם לא אבדו** — הם זוקקו בסבב 34 לכללי הברזל ולפרקי
  המצב-הנוכחי, ושם הם נאכפים על **כל** קוד חדש ולא רק על האזור שנבדק.
- ⛔ **ולכן אין לפתוח סבב «שחזור בדיקות» יזום** — החלטת המנהל. מה שכן חל:
  סבב שנוגע באזור כזה **משחזר את רתמת האזור באותו סבב**, מול השינוי שהוא
  עושה ולא מול הקוד ההיסטורי.

**כאן:** ⭐ מנוע המיזוג כוסה בסבבים 37–38 — `test_round37_merge_pending.mjs`
(הכרעת ההתנגשות והגנת ה-⏳) ו-`test_round38_merge_core.mjs` (הליבה המשותפת
וידיות המדיניות). ⚠️ **ועדיין לא מכוסים:** `mergeSubs`, גריעת ה-tombstones
ו-`migrateLegacyLsKeys()` — שלושתם ייכסו בסבב שייגע בהם.

## פערים פתוחים (yoman-avoda)
- **מחיקת `tb_archive` הישנה ומפתחות ה-`kv` של היומן** — הכתיבה הכפולה
  אליהם **כובתה בסבב 35** (הדגלים `TB_KV_LEGACY_WRITE`/
  `TB_ARC_LEGACY_WRITE`); הטריגר שנותר: מחיקה ב-**30.8**, בהחלטת
  המנהל, אחרי חלון של עבודה תקינה על הטבלאות בלבד. ⛔ הכיבוי והמחיקה
  בשני סבבים נפרדים (סבב 30) — אחרת אין חלון לגלות טעות.
  [לא-מדיד: קיום מפתחות ה-`kv` הוא מצב מסד, ואינו נראה מהריפו]

## סבב 34 (2026-08-17) — זיקוק וגיזום התיעוד
תיעוד בלבד, בארבעת הריפו: פרקי הכרוניקה נמחקו (git זוכר), הלקחים קודמו
לכללים ולפרקי מצב-נוכחי, נוספו «פערים פתוחים» ובלוק `backup-module`,
והרשימה הקנונית ב-`check-docs.mjs` עודכנה בארבעתם. ⛔ אפס שינוי בקוד הרץ.

## סבב 35 (2026-08-17) — יישור יכולות רוחבי + מודול החלון החם

### מה נעשה
- **מודול «חלון חם ושחזור מקומי»** — בלוק קוד משותף חדש, זהה בית-לבית
  בארבעתן (221 שורות, חתימה `0a8ea3499a688691`), נאכף
  ב-`check-capabilities.mjs`. ה-API: `hwBoot` · `hwSweep` · `hwNoteCloud` ·
  `hwDiskFilter` · `hwPastLoad` · `hwRestoreMount` · `hwEnabled`; מעליו
  `HW_CFG` — הדבר היחיד שנבדל. ⛔ הפינוי דורש שני תנאים יחד (סבב 35) —
  מחוץ לחלון **וגם** מסונכרן במלואו (אפס ⏳ + ראיה מהענן לפי מזהה
  וחותמת) — נכשל סגור, וספק ⇐ לא מפנים. כפתור «☁️ שחזר הכול מהענן»
  (מנהל בלבד, אישור דו-שלבי, קורא ל-`lsRestoreAll` בלי לגעת במודול
  הקפוא) — בארבעתן.
- **מודול הגיבוי הורחב** למקורות `kind:'table'` עם `eq`/`order`/`key`
  (חתימה חדשה `b34407a6fcc1af8b`, 258 שורות) — בארבעתן, זהה בית-לבית.
- **מטריצת היכולות** — נוספו שורות 17–21 ותוקנו שורות 14 ו-16 לפי
  המציאות הנמדדת; חתימת `capability-matrix` עודכנה בארבעת עותקי
  `check-docs.mjs` (`000b6d830afc37dd`), וכך גם חתימת
  `iron-rule-6-sync` (סגירת מקרה `admin_pass`).
- **השלמת הסבב (2026-08-18) — מדיניות שמירה למודול הגיבוי + יישור שם:**
  עותק יומי נשמר 30 יום ונגרע אחרי גיבוי מוצלח, לפי רשימת-היתר של
  מפתחות (לעולם לא `PRE_*` ולא מפתח של אפליקציה אחרת), וסינון סודות
  דרך `BK_CFG.secrets` — חתימת מודול הגיבוי `f17875916d7a4b3d` (315 שורות),
  בארבעתן. הגריעה נכשלת סגור: `DELETE` על `kv_backup` ממתין להרשאה
  סקופית בהחלטת המנהל, והמנגנון דרוך. שם משתנה ה-CDN ב-`sw.js` של
  schar יושר ל-`CDN_ASSETS`.

- **⭐ שער חתימה למעטפת — `tools/test_round40_shell.mjs`, זהה בית-לבית
  בארבעתן** פרט לבלוק `APP`. ⚠️ **הפער שנמדד בסבב 39 (סריקת ההמשך,
  סעיף 1):** ארבעה עותקים של אותה מעטפת, ⛔ **ואף בדיקה לא נגעה באף
  אחת מהן** — `MainActivity.java` היה הקובץ היחיד בארגון בלי שער, והוא
  זה שקובע מה המשתמש רואה כשאין רשת.
  השער אוכף **שתי שכבות**: **חוזה התנהגות** (בורר קבצים · דף אופליין
  ב-`text/html` · `onBackPressed` · השארת `http(s)` במעטפת · JS
  ו-localStorage · סגירת `file://`/`content://`), ו**חתימה על
  הלוגיקה** — hash על המקור אחרי שהערות, מחרוזות ושם החבילה מנורמלים
  החוצה. ⚠️ כלומר ניסוח javadoc, שם האפליקציה בעברית, צבע וכתובת
  **אינם** בחתימה, ואילו תנאי שהתהפך או קריאה שירדה — כן.
  ⭐ **ומה שהמדידה נתנה:** אחרי נרמול הזהות, שלוש המעטפות חסרות-הגשר
  נושאות **חתימה זהה בדיוק** — `9d13db21feb1f616` — וכל ריפו אוכף אצלו
  שהחתימה שלו היא הקנונית, כך שסחיפה בין השלוש נתפסת בכל אחד מהם בנפרד
  ולא בהשוואה ידנית. ל-yoman חתימה משלה (`161b6a469d6b45eb`), מפני
  שגשר השיתוף מכפיל אצלה את המעטפת — חריגה **מדודה**, לא סחיפה.
  ⛔ **הסבב הזה אינו מחלץ תבנית משותפת** (סבב 40) — זה סבב ייעודי,
  וריפקטור של ארבע מעטפות בלי שער קיים הוא ריפקטור בלי רשת ביטחון.
  ⭐ החתימה היא **התנאי** לחילוץ כזה בעתיד.
- **⭐ gius — שתי סטיות שנמדדו במעטפת ותוקנו.**
  **א · הקשחה שחסרה:** `setAllowFileAccess(false)` ו-`setAllowContentAccess(false)`
  ישבו בשלוש המעטפות ו⛔ **נעדרו מ-gius**. המעטפת שם טוענת
  `loadDataWithBaseURL(null, …)` ואינה קוראת דבר מהדיסק, ולכן זו הרשאה
  **מיותרת בהגדרה** — ובדיוק כזו שדף שנטען מהרשת יכול לנצל. ⚠️ ההשלמה
  אינה משנה התנהגות נצפית; היא סוגרת הרשאה שלא הייתה בשימוש.
  **ב · ניסוח ה-javadoc** יושר לשלוש (למה לא TWA · למה אין נכסים
  מוטבעים · למה אין גשר). ⛔ אחרי שני התיקונים **הלוגיקה של שלוש
  המעטפות זהה בית-לבית** — נמדד, לא הוצהר.
- **שורה 28 חדשה במטריצה** — «מעטפת WebView חתומה», ✅ בארבעתן, עם probe
  שדורש את **שני** התנאים: קובץ השער שקיים, ו-`shellSha` מוצהר בתוכו.
  ⛔ שער בלי חתימה מוצהרת הוא שער שאינו נועל דבר.

### מה נעשה כאן (yoman-avoda)
- חלון חם על `tb_archive`+LS — הארכוב הקיים מיושר כמקרה פרטי: מפונה רק
  סנאפשוט מסונכרן (אפס ⏳) ומאומת מול הענן. `hwNoteCloud` ניזון משתי
  משיכות `tb_archive`, ו-`hwDiskFilter` עוטף את כתיבות הארכיון לדיסק.
- הכתיבה הכפולה כובתה (`TB_KV_LEGACY_WRITE`/`TB_ARC_LEGACY_WRITE`);
  העֵד ואישור ה-⏳ על `tbRowsPush` בלבד; `scheduleSyncPush` דוחף שורות.
- הגיבוי היומי: מקור `kind:'table'` ל-`tb_entries` (`eq` פר-מוסד,
  `order('rec_key')`, מפתח `tb_entries_rows`).
- `tbSyncLog` — רישום `push` (עם מוני שורות, רק כשנדחף) ו-`pull` ראשון
  בסשן ל-`sync_log`; לא בפולינג.
- `_tbGdateTs` עברה ל-`parseGregLike` — פער סבב 31 נסגר.

- השלמה: `secrets: []` ב-`BK_CFG` (אין סוד — המנגנון דרוך); קידום
  `sw.js` ל-v28 ו-`app-version` ל-`22-2026-08-18-1`.
- **`mergeRecords` היא מעטפת** מעל `mergeCore` — ⛔ החתימה לא השתנתה,
  עשרות אתרי קריאה מסתמכים עליה. `dedupe: true` · `remoteDupe: 'ts'` ·
  `keepUnversionedLocal: true`.
  ⚠️ **`dedupe: true` נדרש כאן ואינו ברירת מחדל שקטה** — שתי קריאות
  `autoArchiveDay` על אותו יום מייצרות שני סנאפשוטים לאותו `gdate` באותו
  מערך, ובלי איחוד שניהם היו שורדים ומכפילים את הארכיון. זו האפליקציה
  היחידה שבה איחוד הכפילויות **נדרש**.
- `mergeArchive` ממשיכה להעביר `mergePair` — הליבה מקבלת אותו כפרמטר
  ומעבירה לו את הכרעת ה-⏳, בדיוק כפי שהיה.
- שלוש רתמות (`test_round30_stage_b` · `test_round31_archive` ·
  `test_round32_unify`) קיבלו את `_mergePick`/`mergeCore` לרשימת החילוץ.
- `sw.js` v31→v32 ו-`app-version` ל-`26-2026-08-18-1`.
### אימותים
- `node tools/check-js.mjs` עבר מלא, כולל `test_round35_hotwin.mjs`
  עם המוטציות (פינוי רשומה לא-מסונכרנת חייב להיכשל; כיבוי
  גיבוי-הטבלאות מפיל את הבדיקה).
- השלמת הסבב: `test_round30_stage_a.mjs` הורחב — עותק יומי בן 31 יום
  נגרע בריצה, עותק `PRE_*` בן 31 יום שורד, מפתח-סוד נחסם מכתיבה
  לגיבוי, ושלוש מוטציות (ביטול רשימת-ההיתר, ביטול דילוג-הסוד, ניפוח
  החלון) נתפסות.

## סבב 35ג (2026-08-18) — פינוי אוטומטי במסד, יישור CDN אחרון, ותיקון פער

### מה נעשה
- **פינוי גיבויים במסד — `pg_cron`** (⛔ נכתב ולא הורץ): משימה יומית
  ב-03:17 UTC שמוחקת מ-`kv_backup` עותקים בני 30+ יום לפי **רשימת-היתר
  מפורשת של מפתחות** — אותה רשימה מבנית שב-`BK_CFG.sources()`.
  ⛔ לא לפי קידומת, ו-`PRE_*`/`ORPHAN_*`/`pre-delete-*` מוגנים פעמיים:
  הם אינם ברשימה, **וגם** הפונקציה מסרבת לרוץ אם מפתח כזה נכנס אליה.
  בדיקות שפיות נוספות: רשימה ריקה ⇒ סירוב, חלון קצר מ-7 ימים ⇒ סירוב.
  ⛔ הרשאת ההרצה נשללת מ-`anon`/`authenticated` — פונקציית
  `security definer` נגישה ב-RPC הייתה נתיב המחיקה שהמיגרציה באה לעקוף.
  שתי מיגרציות, אחת לכל פרויקט: `hanhala-ruchanit/migrations/004_…`
  (המשותף) ו-`gius/migrations/0005_…`.
- **שתי שכבות מכוונות:** המסד מפנה, והלקוח **לא יכול ולא צריך** —
  `_bkRetention` נשארת שכבה שנכשלת-סגור, ⛔ ואין להסיר אותה.
- **hanhala — יישור ה-CDN האחרון:** `CDN` → `CDN_ASSETS` ב-`sw.js`
  (כבשלוש האחיות מסבב 35), אכיפת-שם נוספה לבדיקה, `CACHE_NAME` v42→v43
  ו-`YS_APP_VER` בהתאם.
- **תיקון פער שהתיישן:** 57 שורות ה-`PRE_*`/`ORPHAN_*` שבפרויקט המשותף
  **נמחקו ב-2026-08-18 ואינן ניתנות לשחזור** (מסלול Free — אין גיבויי
  פרויקט). הניסוח עודכן לעובדה בבלוק `backup-module` ובקובץ ההצעה;
  ⛔ האיסור העקרוני נשאר בתוקף לגיבויי `PRE_*` עתידיים.
- **מטריצת היכולות** — שורה 22 «פינוי גיבויים אוטומטי במסד», ❌ בארבעתן
  עם טריגר מפורש (הרצת המיגרציות), ⛔ לא ✅ לפני שרצו.
- **`tools/test_round35c_cron.mjs`** — בדיקה חדשה בארבעתן (זהה בית-לבית
  פרט לבלוק `APP`), רצה מתוך השער. היא מריצה את המיגרציה בסימולטור צר —
  רשימת-ההיתר, בדיקות השפיות ותנאי ה-DELETE — ולא מסתפקת ב-regex על
  הטקסט. **שתי המוטציות הנדרשות נתפסות:** הרחבת הרשימה למפתח `PRE_*`
  ⇒ הפונקציה מסרבת לרוץ, וריקון הרשימה ⇒ סירוב. ⚠️ ב-yoman וב-schar
  אין קובץ מיגרציה משלהן (הפרויקט משותף), ולכן שם נבדקים המודול
  והתיעוד בלבד — «נכתב» אינו «רץ», וגם לא «נבדק פעמיים».
- **`check-docs.mjs` — סיומת עברית במספר הסבב** (בארבעתן): שורת «עודכן
  לאחרונה» קיבלה `(\d+[א-ת]?)`. ⚠️ עד כה סבב-המשך שרץ **באותו יום** של
  הסבב שהוא ממשיך לא יכול היה לקדם את השורה כלל — התאריך זהה, והמספר
  «35ג» נדחה כפורמט שגוי.
- **השלמה (2026-08-18) — יישור רשימת-ההיתר למציאות המסד:** נמדד שב-`kv_backup`
  יושבות **23 שורות גיבוי-שגרה** שמפתחותיהן אינם ברשימה ולכן לא היו מתפנות
  לעולם — `rishon_tb_entries` · `rishon_tb_archive` · `ramataviv_tb_entries` ·
  `ramataviv_tb_archive`, שמות הגיבוי שרצו עד סבב 35 לפני המעבר
  לגיבוי-טבלאות. ⭐ ארבעתם נוספו למיגרציה **בשמם המפורש**, בהחלטת המנהל;
  ⛔ בדיקות השפיות לא נגעו ולא הורחבה שום קידומת. רשימת gius (`0005`)
  נמדדה מול `BK_CFG.sources()` שלה ונמצאה תואמת — אין שם מה לתקן.
  ⭐ **והבדיקה למדה לתפוס את זה:** `test_round35c_cron.mjs` **גוזר** את
  מפתחות הגיבוי מ-`BK_CFG.sources()` שבקוד (במקום להצהיר אותם) ודורש
  **שקילות דו-כיוונית** מול רשימת-ההיתר — כל מפתח שנכתב נמצא ברשימה,
  וכל מפתח ברשימה שייך לאפליקציה בפרויקט. ⚠️ היסט השמות הזה נולד בדיוק
  מרשימה מוצהרת שהתיישנה בשקט; מעכשיו הוא נתפס בשער ולא במסד.
  ⚠️ **ומה שנשאר בלתי-ניתן-לגזירה:** תרומת האחיות לרשימת הפרויקט המשותף
  (`APP.sisterKeys` ב-hanhala) — כל ריפו הוא שכפול נפרד, ולכן שינוי שם
  מפתח ב-yoman או ב-schar מחייב עדכון גם שם, באותו סבב (כלל ברזל 8 סעיף 3).

### מה נעשה כאן (yoman-avoda)
- אין קוד רץ שהשתנה — הפינוי חל על מפתחות הגיבוי של שני המוסדות
  (`rishon_*`/`ramataviv_*`) דרך המיגרציה של הפרויקט המשותף, שיושבת
  ב-hanhala.
- **השלמה (השלמת סבב 35ג) — ארבעת שמות גיבוי השגרה שיצאו משימוש נוספו
  לרשימת-ההיתר**, בהחלטת המנהל: `rishon_tb_entries` · `rishon_tb_archive` ·
  `ramataviv_tb_entries` · `ramataviv_tb_archive`. ⚠️ **נמדד מול המסד:
  23 שורות** שלהם יושבות ב-`kv_backup`, ומפני שהרשימה הראשונה כללה רק את
  שמות גיבוי-הטבלאות החדשים (`*_tb_entries_rows`) הן לא היו מתפנות לעולם.
  ⭐ זה **היסט שמות** ולא היסטוריה שיש להגן עליה — גיבוי שגרה יומי, בדיוק
  כמו מה שהחליף אותו. ⛔ ההוספה היא ארבעה שמות מפורשים ולא קידומת (השלמת
  סבב 35ג) — קידומת `rishon_` הייתה תופסת גם גיבויי `PRE_*` פר-מוסדיים.
  ⚠️ הניסוח הקודם כאן אמר שהם מוחרגים בכוונה; ההחלטה התהפכה במפורש, וזו
  הרשומה שלה.

### אימותים
- `node tools/check-js.mjs` עבר מלא, כולל `test_round35c_cron.mjs` —
  כאן הוא בודק את `_bkRetention` ואת התיעוד; ⚠️ מוטציות ה-SQL רצות
  בריפו שמחזיק את המיגרציה (hanhala), ולא כאן.

## סבב 36 (2026-08-18) — הנהלה עוברת לטבלאות מובנות, שלב א

### מה נעשה
- **hanhala-ruchanit — שלב א של מעבר הטבלאות, אדיטיבי בלבד** (⛔ נכתב
  ולא הורץ): שתי מיגרציות חדשות בפרויקט המשותף —
  `005_structured_tables.sql` (מבנה) ו-`006_migrate_kv_to_rows.sql`
  (העברה, אידמפוטנטית ב-`on conflict do nothing`, עם בדיקת שקילות
  דו-כיוונית בגוף הקובץ). ⭐ **ההכרעה: שורה לסימון, בשתי טבלאות — אב
  ובן.** `ys_sessions` נושאת את מטא הסדר ו-`ys_marks` שורה לכל
  תלמיד×סדר, לצד `ys_students_rows` למצבה. ⛔ ה-`kv` נשאר המאסטר וכל
  קריאה עוברת דרכו; המתג הוא שלב ב, בסבב נפרד.
- **תיקון עובדה — שורה 22 במטריצה הפכה ✅ בארבעתן.** שתי מיגרציות
  ה-`pg_cron` הורצו ב-2026-08-18, המשימות רשומות ופעילות בשני
  הפרויקטים, הריצה הראשונה במשותף פינתה **37 שורות** על 32 מפתחות
  ונרשמה ב-`sync_log`, ובדיקת השפיות של חלון קצר אומתה כמסרבת.
- **תיקון אי-התאמה בין הקובץ למסד:** שני קובצי המיגרציה תיארו תזמון
  `'17 3 * * *'` (03:17) בעוד שהמשימות שנרשמו בפועל הן `'0 3 * * *'`.
  הקבצים יושרו למציאות, ו-`test_round35c_cron.mjs` נועל את הערך שבמסד
  בארבעת הריפו. ⛔ הקובץ הוא מקור האמת, ואי-התאמה כזו היא בדיוק מה
  שמייצר גרסה שלישית שאיש אינו יודע עליה.

### מה נעשה כאן (yoman-avoda)
- אין קוד רץ שהשתנה, ואין קידום גרסה — הסבב נגע כאן בתיעוד ובשער בלבד.
- `tools/test_round35c_cron.mjs` — אכיפת התזמון עודכנה ל-`'0 3 * * *'`.
- שתי חתימות SHARED עודכנו ב-`check-docs.mjs` (`capability-matrix`,
  `backup-module`), ושורת הפער על מיגרציית ה-`pg_cron` **נמחקה** — היא
  רצה.

### אימותים
- `node tools/check-js.mjs` עבר מלא.
- מצב ה-`pg_cron` נמדד ב-`SELECT` בלבד מול הפרויקט המשותף: משימה אחת
  פעילה, `'0 3 * * *'`, ושורת `retention` אחת ב-`sync_log` עם 37 שורות.

## סבב 37 (2026-08-18) — תיקוני הביקורת

### מה נעשה
- **⭐ הגנת ⏳ במנוע המיזוג — הפער הגדול של הסבב.** כלל ברזל 6 דורש שרשומה
  מקומית שטרם אושרה בענן תנצח במיזוג **ללא תלות בחותמת**; הכלל היה מיושם
  **בשתיים מארבע** בלבד (`slMerge` ב-schar, `mergeRows` ב-gius). ב-yoman
  וב-hanhala המיזוג הכריע לפי חותמת בלבד, ולכן עריכה מקומית שטרם סונכרנה
  **נדרסה בשקט** ע"י גרסה שמכשיר אחר דחף אחריה. ⛔ ואיש לא החליט על ההבדל
  — לא «הוחל», לא «אינו רלוונטי», ולא «פער עם טריגר». ההגנה נוספה בשתיהן,
  **צרה בכוונה**: היא מכריעה רק בהתנגשות מפתח בין מקומי למרוחק, ואינה נוגעת
  בשובר-השוויון ולא בכלל «רשומה מקומית-בלבד שורדת».
  ⛔ **האיחוד למודול משותף (סעיף 4ב) לא בוצע, וזו החלטה מנומקת** — ר'
  «מה שלא נעשה» למטה.
- **⭐ `check-capabilities.mjs` הורחב משלוש שורות לכל המטריצה.** עד הסבב
  הזה נבדקו שורות 1, 13, 14 בלבד; תשע-עשרה השורות האחרות לא נבדקו כלל.
  האכיפה **דו-כיוונית**: ✅ שה-probe שלו אינו מוצא מפיל את השער, ו-❌
  שה-probe שלו כן מוצא מפיל אותו גם כן. ⛔ שתי שורות בלבד מוחרגות
  (21, 22) — שתיהן מצהירות על **עובדת מסד** שאין דרך לראות מהריפו, וזה
  כתוב במקום שהשער קורא.
- **⭐ מה שההרחבה מצאה מיד: שורה 20 שיקרה.** «מטמון-CDN מראש עם ריפוי
  עצמי ✅» הוכרז בארבעתן מסבב 35, בזמן ש-`ensureCdnCached` **לא היה קיים
  ב-gius כלל** — היא רק טענה את רשימת ה-CDN ב-`install`, ונכס שנכשל שם
  נשאר חסר לנצח. הדפוס הועתק בית-לבית מהאחיות.
- **⭐ schar-limud — פרצת כניסה אופליין.** `slVerifyOffline` בדקה `!u`
  בלבד, ול-`sl_users` **לא הייתה עמודת `active` כלל** — כלומר לא הייתה שום
  דרך להשבית משתמש באפליקציית הכספים. שתי האחיות בודקות `active !== true`
  מאז סבבים 22–23.
- **שתי שורות חדשות במטריצה** — 23 «מנוע מיזוג עם הגנת ⏳» ו-24 «חסימת
  משתמש מושבת בכניסה אופליין», שתיהן עם probe.
- **מפתח ניווט לכללי הברזל 6–14** בראש פרק «כללי ברזל של הארגון», מקובץ
  לפי נושא ועם מדרגת חומרה. ⛔ בלי מספור-מחדש — המספרים הם כתובות.
- **חמישה אינדקסים חלקיים** קיבלו מיגרציות שמחליפות אותם במלאים
  (`schar-limud/migrations/014`, `gius/migrations/0006`).
  ⚠️ **ותיקון עובדתי שנרשם בגוף הקבצים:** חמשתם **אינם ייחודיים ואינם יעד
  של `ON CONFLICT`** (נמדד ב-`pg_index.indisunique`), ולכן הם אינם גורמים
  ל-42P10 שתואר בסבב 12 — התקלה ההיא נגעה לאינדקסים על `client_id`, שכבר
  הוחלפו ב-007. מה שכן: מאז שהסינון עבר לצד הלקוח אין שאילתה שחוזרת על
  התנאי שלהם, ולכן הם אינם משרתים היום דבר.
- **⭐ טבלאות המשתמשים — הדפוס נקבע תוך כדי הסבב, ע"י המנהל, בשני צעדים.**
  ההצעה הראשונה כאן הייתה `active`+`updated_at` **בלי `deleted`**, בנימוק
  ש`active=false` הוא מנגנון המחיקה בארגון. המנהל הריץ ב-2026-08-18 את
  `schar_013_users_active_updated_deleted`, שהוסיפה ל-`sl_users` את שלוש
  העמודות — **ומיד אחריה את `users_drop_deleted_add_touch_trigger`, שהחזירה
  את ההכרעה לניסוח הראשון**: `deleted` **הוסרה** מ-`sl_users` ולא נוספה לאף
  טבלת משתמשים, ובמקומה נוצרה `public.users_touch_updated_at()` והטריגרים
  `sl_users_touch` ו-`ys_users_touch`. `g_users` ב-gius כבר נשאה
  `g_users_touch` — **היא הדפוס.**
  ⭐ **שלוש טבלאות המשתמשים זהות מעכשיו:** `active` · `updated_at` · טריגר
  חותמת · **בלי `deleted`**. ⛔ אין להחזיר את `deleted` לשום טבלת משתמשים
  (סבב 37) — ההשבתה היא `active=false` (כלל קריטי 4 ב-gius), ועמודה שנייה
  שמתארת «המשתמש הוסר» היא מקור אמת שני.
  ⚠️ **וזה מדגים את «הקובץ הוא מקור האמת» בשני הכיוונים:** המיגרציות
  והתיעוד שתיארו את `deleted` כעמודה קיימת נמחקו — `gius/0007` כולה,
  שנועדה רק לה — ולא נותר בארבעת הריפו אזכור לעמודה שאינה קיימת.
- **מצב ההרצה, ולא הצהרה:**
  `schar-limud/migrations/013` ו-`hanhala-ruchanit/migrations/007` — ✅
  **הורצו ואומתו** (2026-08-18; 007 רצה כחלק מ-`users_drop_deleted_add_touch_trigger`
  ולא בשם הזה). `schar-limud/014` ו-`gius/0006` — ✅ **הורצו ואומתו**
  (נמדד: אפס אינדקסים חלקיים בשני הפרויקטים). «נכתב» אינו «רץ», וההרצה
  היא החלטת המנהל.

### ⛔ מה שלא נעשה, ולמה (סעיף 4ב — איחוד מנועי המיזוג)
ההגנה (סעיף 4א) הוחלה בארבעתן; **האיחוד לליבה משותפת אחת נדחה**, לפי
הרשות המפורשת שבסעיף 4ג. הנימוק נמדד ולא הונח — ארבעת המנועים נבדלים
בארבעה ממדים שאינם פרמטר:
1. **hanhala ממזג גם אובייקטים ולא רק מערכים** — `ys_approvals` נשען על
   הענף הזה, ולשלוש האחרות אין ענף כזה. ⚠️ הצרכן שנרשם כאן במקור התחלף
   בסבב 38; הענף עצמו לא נגע, ולכן הממד עומד בעינו.
2. **מדיניות הרשומה המקומית-בלבד נבדלת:** ב-yoman/schar/gius היא שורדת
   תמיד; ב-hanhala היא מוכרעת ב-`keepUnversionedLocal` + `onDrop`, שמזין
   `dropped_legacy` ל-`sync_log`.
3. **ל-yoman יש `mergePair`** (מיזוג הרשומות שבתוך סנאפשוט הארכיון);
   לשלוש האחרות אין.
4. **גוזר החותמת נבדל** — `updatedAt` מספרי מול `updated_at` כמחרוזת ISO.
⚠️ **ומעל הכול:** ל-yoman ול-hanhala **לא הייתה עד הסבב הזה שום רתמת
בדיקה על מנוע המיזוג** (פער מתועד בשני הקבצים). ריפקטור של הפונקציה
הקריטית ביותר לאובדן נתונים, בארבע אפליקציות, בלי רשת ביטחון קיימת —
מסוכן יותר מהפער שהוא סוגר. ⭐ **הסבב הזה בנה את רשת הביטחון**
(`test_round37_merge_pending.mjs`, זהה בית-לבית בארבעתן, עם מוטציה), וזה
מה שהופך את האיחוד לאפשרי בסבב ייעודי.
**הטריגר:** סבב שנפתח עם ארבעת הריפו ועם האיחוד כמשימה יחידה.
✅ **הטריגר התקיים בסבב 38 — האיחוד בוצע.** שלושה מארבעת הממדים שנרשמו
כאן הפכו לפרמטר (`mergePair` · `onDrop`+`keepUnversionedLocal` · גוזר
החותמת), והרביעי — ענף האובייקטים — נשמר כמעטפת. ⭐ ומה שסבב 37 חיכה
לו נבנה בינתיים: רתמת המיזוג שאיפשרה למדוד את השקילות ב-125,000 מקרים.

### מה נעשה כאן (yoman-avoda)
- **`mergeRecords` מקבל `isPending`** — `pendEntry`/`pendArc` מזינים את
  `mergeEntries` ואת `mergeArchive`, ו-`mergePair` של הארכיון מקבל את
  ההכרעה כפרמטר (גם לבחירת בסיס הסנאפשוט וגם למיזוג הרשומות שבתוכו).
  ⚠️ בהתנגשות **בתוך** המערך המרוחק ההגנה אינה חלה — שני הצדדים שם
  מרוחקים.
- ⭐ **ותוך כדי כך נמצא מפתח סימון שגוי:** `arcSaveEntry` כתב
  `PK_ARC + gdateKey` — מפתח המפה של `getAllArchiveDays`, שהוא **gdate
  גולמי** (`"3/09/2025"`) — בעוד ששלוש נקודות הקריאה (`tbDirtyRows`,
  `HW_CFG.specs[].isPending` וההגנה החדשה) קוראות
  `PK_ARC + archiveKey(r)`, כלומר `arc:g:3/09/2025`. כלומר **אף אחת מהן
  לא ראתה את הסימון**: עריכת רשומה בארכיון לא נחשבה «לדחיפה», ושער הפינוי
  של החלון החם היה רשאי לפנות סנאפשוט שהעריכה שלו טרם עלתה. הסימון נגזר
  עכשיו מ-`archiveKey` של הסנאפשוטים שנגעו בפועל.
- `sw.js` v28→v29 ו-`app-version` ל-`23-2026-08-18-1`.

### אימותים
- `node tools/check-js.mjs` עבר מלא בעץ, כולל שתי החבילות החדשות.
- `test_round37_merge_pending.mjs` — 8 טענות, כולל המוטציה שמסירה את סעיף
  ה-⏳ ומפילה את טענת הבסיס.
- `test_round37_matrix.mjs` — 26 טענות: **היפוך כל אחת מ-22 השורות
  המכוסות מפיל את `check-capabilities`**, ושתי המוחרגות (21, 22) רשומות
  בשם ובנימוק.
- `test_round30_stage_b.mjs` ו-`test_round32_unify.mjs` הורחבו ב-`pendEntry`/
  `pendArc` — הם מריצים מעכשיו את המיזוג **עם** ההגנה.

### סריקת המשך (סעיף 6) — ⛔ דיווח בלבד, לא תוקן בסבב הזה
שני תחומים שהביקורת לא הספיקה נסרקו בארבעת הריפו. ⛔ **שום ממצא כאן לא
תוקן** — הם נרשמים כדי שייכנסו לסבב 38 בהחלטת המנהל, ולא ייעלמו עם הסשן
(כלל ברזל 8 סעיף 6).

**א · מסלולי ייבוא — קיימים באחת מארבע, בשקט.** ✅ **שלושת הממצאים נסגרו
בהשלמת הסבב** (ר' הפרק שמתחת); התיאור נשאר כרישום של מה שנמדד.
`importStudentsFromFile` ב-hanhala-ruchanit (ייבוא מצבת תלמידים מ-Excel/CSV)
הוא **מסלול הייבוא היחיד בארגון** — ל-yoman, ל-schar ול-gius אין ייבוא
כלל. ⛔ אין לו אף אחת משלוש התשובות של כלל ברזל 14: לא «הוחל בארבעתן»,
לא תא במטריצה, ולא פער עם טריגר. שלושה ממצאים בתוכו:
1. **⛔ המזהה נבחר במכשיר לפי `maxId++` ולא כ-uuid.** שני מכשירים
   שמייבאים במקביל מקצים **את אותם מזהים** לתלמידים שונים, ומנוע המיזוג
   יראה בהם רשומה אחת — כלומר תלמיד אחד נעלם בשקט. זו בדיוק התקלה שכלל
   ברזל 6 («מזהים») בא למנוע, והיא היחידה כאן שאין ממנה חזרה.
2. **הייבוא אינו מסמן ⏳.** הוא מוסיף N תלמידים וקורא `scheduleYsPush()`,
   אך אינו קורא `pendMark` — בניגוד לכל שאר מסלולי כתיבת התלמידים. ייבוא
   שנעשה אופליין אינו נספר במונה, אינו נחשב «לדחיפה», ומאז סבב 37 גם אינו
   מוגן בהגנת ה-⏳ שבמיזוג.
3. **תוצאת הכתיבה המקומית אינה נבדקת** — `saveStudents` נקראת ומיד אחריה
   מוצג סיכום «N תלמידים נוספו בהצלחה». באחסון מלא זו בדיוק ההודעה שכלל
   ברזל 2 אוסר.
⚠️ הספרייה עצמה תקינה: `xlsx@0.18.5` נעוץ ורשום ב-`CDN_ASSETS`.

**ב · בלוקי התיעוד הפרטיים — חמש אסימטריות שאיש לא החליט עליהן.**
⭐ **ומה שנמדד קודם:** **29 הפרקים המשותפים לארבעתם כבר יושבים בסדר זהה
בארבעת הקבצים**, וכך גם 17 בלוקי ה-SHARED. כלומר החלק השני של סעיף 9
(«קבע סדר קנוני והחל אותו») **כבר מקוים** — ככל הנראה מאז הגיזום של סבב
34 — ולא נדרש בו שינוי. מה שנותר הוא הפרקים הפרטיים:
1. **«מצב נוכחי»** — קיים ב-yoman וב-hanhala, חסר ב-schar וב-gius.
2. **«לקחים חיים בקוד»** — קיים ב-hanhala וב-schar, חסר ב-yoman וב-gius.
3. **«אייקונים»** — קיים ב-hanhala, schar ו-gius, חסר ב-yoman (שיש לה
   `icons/` ומעטפת APK).
4. **פרקי ייחוס שקיימים ב-gius לבדה** — «מבנה הריפו», «מבנה הנתונים»,
   «חישובים», «הרשאות», ובפרט **«Service worker — כללים שאסור לשבור»**,
   בזמן שלארבעתן יש `sw.js` עם אותו סוג כללים בדיוק.
5. **⛔ פרק מיושן ב-gius: «`.well-known/assetlinks.json` — שריד מתקופת
   ה-TWA».** הפרק אמר «הקובץ קיים בריפו; לא למחוק אותו» — **והקובץ אינו
   קיים**: התיקייה הוסרה בסבב 33, ורשימת-ההיתר של `check-structure.mjs`
   שם אף מציינת זאת במפורש. תיעוד שמתאר מציאות שאיננה, בדיוק מה שכלל
   ברזל 8 סעיף 6 אוסר. ✅ **נסגר בהשלמת הסבב** — הפרק נמחק, וארבעת
   הקבצים נסרקו לאזכורים דומים.


## סבב 37 — השלמה (2026-08-18): יישור המסד, מסלול הייבוא, ותיעוד מיושן

### מה נעשה
- **⭐ `deleted` הוסרה מכל טבלת משתמשים — והמיגרציות והתיעוד יושרו למסד.**
  המנהל הריץ ב-2026-08-18 את `users_drop_deleted_add_touch_trigger` בפרויקט
  המשותף: `deleted` **נמחקה** מ-`sl_users`, לא נוספה לאף טבלה, ובמקומה נוצרו
  `public.users_touch_updated_at()` והטריגרים `sl_users_touch` ו-`ys_users_touch`.
  ⭐ **שלוש טבלאות המשתמשים זהות מעכשיו** — `active` · `updated_at` · טריגר
  חותמת · **בלי `deleted`** — ו-`g_users` ב-gius, שנשאה `g_users_touch`
  מלכתחילה, היא הדפוס.
  **מה שהשתנה בקבצים:** `schar-limud/013` נכתב מחדש ושמו קוצר
  ל-`013_users_active_updated.sql`; `hanhala-ruchanit/007` נכתב מחדש ל-
  `007_users_updated_at_touch.sql` ו**סומן ✅ הורצה** — מה שרץ בפועל כיסה
  אותו במלואו; `gius/0007_users_deleted.sql` **נמחקה כולה**, היא נועדה
  לעמודה שאינה קיימת; שני קובצי `000_initial_schema.sql` יושרו — בלי
  `deleted`, **עם** הטריגר.
  ⛔ **ואין להחזיר את `deleted` לשום טבלת משתמשים (סבב 37)** — ההשבתה היא
  `active=false` (כלל קריטי 4 ב-gius), ועמודה שנייה לאותו מושג היא מקור
  אמת שני.
  ⚠️ **הטריגרים מחווטים ל-`users_touch_updated_at()` המשותפת** ולא לפונקציה
  פר-אפליקציה: שתי האפליקציות חולקות פרויקט אחד, ושתי הגדרות לאותה פונקציה
  הן גרסה שנייה שאיש אינו יודע עליה (הלקח של סבב 36).
- **⭐ מסלול הייבוא של hanhala — שלושת הממצאים של סעיף 6 נסגרו.**
  `importStudentsFromFile` הוא **מסלול הייבוא היחיד בארגון**, ולכן הוא
  הדפוס לכל ייבוא עתידי — ר' «דפוס הייבוא» למטה.
- **⭐ תיעוד שמתאר מציאות שאיננה — נמחק ותוקן.** פרק
  «`.well-known/assetlinks.json`» ב-gius נמחק (הקובץ הוסר בסבב 33), וארבעת
  קובצי ה-CLAUDE.md נסרקו אוטומטית לאזכורים של קבצים ותיקיות שאינם קיימים.
  ⚠️ **הסריקה הבחינה בין שני סוגים**, וזו ההבחנה שהכריעה מה לתקן:
  אזכור **תפעולי בהווה** («הקובץ קיים; לא למחוק») תוקן, ואזכור **היסטורי**
  («הקובץ נמחק בסבב 33») נשאר — היסטוריה נשארת היסטוריה (סבב 16).
  שני תיקונים נוספים יצאו מהסריקה: משפט בהנהלה שתיאר סריקה «גם
  ב-`supabase-sync-log.sql`» כאילו הקובץ קיים, ושורת `.nojekyll` בפרק
  «מבנה הריפו» של gius שנימוקה הצביע על תיקיית `.well-known/` שכבר אינה.

### ⭐ דפוס הייבוא בארגון (סבב 37) — ⛔ כל ייבוא עתידי נבנה כך
לייבוא יש שלוש חובות שאינן נגזרות מהפורמט אלא מכללי הברזל, וכולן נשברו כאן:
1. **מזהה שנוצר במכשיר, uuid v4** (`ysNewId()` — `crypto.randomUUID`, ואחריו
   `getRandomValues` ואז `Math.random`; אותו דפוס בדיוק כמו `newClientId()`
   ב-schar). ⛔ **לעולם לא `maxId++`** (סבב 37) — שני מכשירים שמייבאים
   במקביל מקצים את אותם מזהים לאנשים שונים, המיזוג רואה בהם רשומה אחת,
   ותלמיד נעלם בשקט. זה הממצא היחיד כאן שאין ממנו חזרה, והוא בדיוק מה
   שכלל ברזל 6 («מזהים») בא למנוע.
2. **סימון ⏳ לכל שורה מיובאת** — ייבוא הוא כתיבה מקומית לכל דבר. בלי
   הסימון הוא אינו נספר במונה, אינו נחשב «לדחיפה», שער הפינוי רשאי לפנות
   אותו לפני שעלה, ומאז סבב 37 גם הגנת ה-⏳ שבמיזוג אינה חלה עליו.
3. **בדיקת תוצאת הכתיבה המקומית לפני כל דיווח הצלחה** (כלל ברזל 2) —
   באחסון מלא הכתיבה מחזירה `false`, וסיכום «N נוספו בהצלחה» עליה הוא
   בדיוק הכשל השקט שהמודול המשותף בא למנוע.

### מה נעשה כאן (yoman-avoda)
⛔ אין קוד רץ שהשתנה, ואין קידום גרסה — ליומן אין טבלת משתמשים ואין מסלול
ייבוא, וההשלמה נגעה כאן בתיעוד בלבד: הפרק המשותף של הסבב, ורשימת מה שהשער
מריץ.

### אימותים
- `node tools/check-js.mjs` עבר מלא.


## סבב 37ב — השלמה שנייה (2026-08-18): `student_id` כטקסט + מודול מזהי הרשומות

### מה נעשה
- **⭐ הפער ש-37א פתח נסגר: `student_id` הוא `text`.** המנהל הריץ ב-2026-08-18
  את `hanhala_008_student_id_to_text` בפרויקט המשותף — `ys_marks.student_id`
  ו-`ys_students_rows.student_id` הומרו מ-`smallint` ל-`text`. **נמדד אחרי
  ההרצה** (`SELECT` בלבד): 13,144 סימונים · 71 תלמידים · 274 סדרים, וכל
  אחד-עשר האינדקסים שרדו.
  ⛔ **ההכרעה הייתה המרת העמודה הקיימת, ולא `student_key` לצידה** (סבב 37ב) —
  שתי עמודות למושג אחד הן מקור אמת כפול, בדיוק הלקח של `deleted` שנוספה
  והוסרה מטבלאות המשתמשים באותו יום; ו-`text` הוא ממילא הדפוס בשאר הארגון.
- **⭐ מודול מזהי הרשומות — בלוק SHARED חדש, זהה בית-לבית ×4.**
  ⚠️ **הפער שנמדד לא היה היעדר אלא ריבוי:** אותה יכולת בדיוק חיה ב**חמישה
  שמות** — `newClientId` בשכר לימוד · `newId` ב-gius · `ysNewId` בהנהלה ·
  ודפוס בלי פונקציה ייעודית ביומן — כולן `crypto.randomUUID` עם אותן שתי
  נפילות-חזרה, ⛔ **ואף שער לא אכף אף אחת מהן**. כלל ברזל 14 אוסר «קיים
  רק באחת, בשקט»; ארבעה מימושים כמעט-זהים תחת ארבעה שמות הם אותו כשל
  בדיוק, בכיוון ההפוך.
  **השם המשותף הוא `newClientId`** — הוותיק והברור מכולם, וזה שכבר היה
  בשימוש. ⛔ **אפס שינוי התנהגות (סבב 37ב)** — שלושת המסלולים הם אלה
  שהיו, והליבה שנבחרה היא זו של שכר לימוד (`typeof crypto` ולא
  `window.crypto`, כלומר עובדת גם מחוץ ל-`window`).
  ⛔ **ומזהים קיימים אינם משתנים לעולם** — האיחוד נוגע ל**יצירה** בלבד:
  מזהה הוא מפתח המיזוג, ושינוי שלו היה מפצל רשומה אחת לשתיים.
- **שורה 25 במטריצה — «מודול מזהי רשומות», עם probe דו-כיווני.** ה-probe
  בודק **קריאה מקוד האפליקציה** (`callSites` מדלגת על מה שבתוך הבלוקים
  המשותפים), ולכן מודול שיושב בקובץ ואיש אינו קורא לו נספר כ-❌.
- **⛔ מזהה מכשיר אינו מזהה רשומה, ולא אוחד** (סבב 37ב) —
  `getDeviceId`/`_randDeviceId` הם מושג אחר: זהות של **מכשיר**, לא של
  נתון. ⚠️ ונמדד אגב כך, כפי שנדרש: `_randDeviceId` **זהה בית-לבית
  בשלוש** (yoman · hanhala · schar) ול-gius אין מזהה מכשיר כלל. זהו פער
  נפרד — ⛔ לא נגענו בו כאן, וייפתח בסבב ייעודי אם יוחלט.

### מה נעשה כאן (yoman-avoda)
- **המודול המשותף נוסף** (זהה בית-לבית, חתימה `3edcfd6f5b4be129`),
  ❌ **ואף אתר יצירה אינו קורא לו — פער מתועד עם טריגר** (שורה 25).
  ⛔ ההמרה כאן אינה החלפת שורה, וזה נמדד ולא הונח: מזהה היומן
  (`Date.now()`) משמש **מפתח מיון מספרי** בשני מקומות (`b.id - a.id`
  ב-`renderLog`, ו-`Number(x.id)` ב-`tbSortRows`), נכנס **בלי מרכאות**
  לארבעה מאפייני `onclick` (`editEntry(1755...)`) ומושווה ב-`===` מול
  מספר. uuid שם היה שובר את סדר הרשימה ואת כפתורי העריכה והמחיקה של
  **כל** הרשומות הקיימות — כלומר שינוי התנהגות, בדיוק מה שהסבב הזה אסר
  על עצמו.
  **הטריגר:** סבב ייעודי שממיר את שלושת הממדים יחד — מפתח המיון, ציטוט
  המאפיינים, וההשוואה כמחרוזת.
  ✅ **הטריגר התקיים בסבב 38** — שלושת הממדים הומרו יחד.
- `sw.js` v29→v30 ו-`app-version` ל-`24-2026-08-18-1` — הקוד הרץ השתנה
  (הבלוק נוסף לקובץ).

### אימותים
- `node tools/check-js.mjs` עבר מלא.
- `test_round37_ids.mjs` — 14 טענות, כולל שתי המוטציות: שינוי בית בליבה
  מפיל את חתימת `check-capabilities`, ומונה רץ במקום uuid מייצר מזהים
  משותפים בין שני מכשירים.

## סבב 38 (2026-08-18) — סגירת הפערים האחרונים

### מה נעשה
- **schar-limud — סולם העמודות נמחק.** הפיגום שסבב 37 השאיר עם טריגר
  כתוב פג: `migrations/013` רצה, הקוד נפרס, ושלוש נקודות הקריאה חזרו
  ל-`select(SL_USER_COLS)` ישיר.
- **hanhala-ruchanit — `ys_attend` נמחק.** מנגנון נוכחות **שני** ונטוש,
  שנמדד במסד כרישום אחד בלבד בכל התקופה, ושהפונקציות שכתבו אליו כבר לא
  נקראו משום מקום. ⛔ `ys_attend_sessions` — הנוכחות האמיתית — לא נגע.
- **gius — מזהה המכשיר הושלם.** `BK_CFG.device` קראה ל-`getDeviceId()`
  שלא הייתה מוגדרת כלל, וה-`try/catch` שסביבה החזיר `null` בשקט: כל שורה
  ב-`sync_log` וב-`kv_backup` נרשמה בלי מזהה מכשיר. הדפוס הועתק
  בית-לבית משכר לימוד.
- **⭐ yoman-avoda — מזהי היומן עברו ל-`newClientId()`**, ואיתם שלושת
  הממדים שחסמו את ההמרה: מפתח המיון (`entryOrderTs`), ציטוט מאפייני
  ה-`onclick` (`idArg`) וההשוואה כמחרוזת (`idEq`). שורה 25 במטריצה →
  ✅ בארבעתן. ⛔ ומזהים קיימים לא הומרו — הקוד סובל את שני הסוגים לנצח.
- **⭐⭐ ליבת המיזוג אוחדה — הפער שסבב 37 דחה במפורש נסגר.** ארבעת
  מנועי המיזוג (`mergeRecords` · `ysMergeRecords` · `slMerge` · `mergeRows`)
  קוראים מעכשיו ל-`mergeCore` שבבלוק משותף זהה בית-לבית (91 שורות, חתימה
  `cc0329965238994c`), וכלל ההכרעה של כלל ברזל 6 יושב ב-**`_mergePick`
  בלבד** — ⏳ גובר · שוויון→ענן · מקומית-בלבד שורדת.
  ⛔ **אף כלל הכרעה לא השתנה**, ו⛔ **אף התנהגות פר-אפליקציה לא יושרה**:
  ארבע ידיות מדיניות (`dedupe` · `remoteDupe` · `keyless` · `localPick`)
  מקודדות את מצבי הקצה שנבדלו, כפרמטר מדוד ולא כקוד כפול.
  ⭐ **והשקילות נמדדה ולא הונחה: דיפרנציאל של 125,000 מקרים** מול המימוש
  שהוחלף (נקרא מ-`git HEAD`), בארבע האפליקציות — **אפס הפרשים**.
  שורה 27 חדשה במטריצה, ו-probe שורה 23 הפך **גנרי** בארבעתן — עדות בפני
  עצמה שהאיחוד אמיתי.
- **⭐ שורה 26 חדשה במטריצה — «מזהה מכשיר», ✅ בארבעתן, עם probe
  דו-כיווני** שדורש את **שתי** הפונקציות (`getDeviceId` **וגם**
  `_randDeviceId`): מעטפת בלי מחולל היא בדיוק המצב שהיה ב-gius.
  ⛔ שורה נפרדת ולא הרחבה של 25 (סבב 38) — מזהה מכשיר אינו מזהה רשומה.

### מה נעשה כאן (yoman-avoda)
- **⭐ מזהי היומן עברו ל-`newClientId()` — ושלושת הממדים יחד.** עד הסבב
  הזה המודול המשותף ישב בקובץ ואיש לא קרא לו (שורה 25 = ❌), מפני שהמזהה
  שימש **גם** כמפתח מיון, **גם** כארגומנט לא-מצוטט ב-`onclick`, ו**גם**
  בהשוואת `===` מול מספר. שלושתם הוחלפו באותו שינוי:
  - **מיון — `entryOrderTs(e)`** במקום `b.id - a.id` (שני מקומות
    ב-`renderLog`/`syncFromCloud`) ו-`Number(x.id)` ב-`tbSortRows`.
    הוא קורא `createdAt`, ⚠️ **ונופל חזרה ל-`id` ברשומות העבר** — אצלן
    ה-`id` **הוא** חותמת היצירה, והוא מקור הזמן היחיד שיש להן. ⛔ בלי
    הנפילה-חזרה כל ההיסטוריה הייתה מתמוטטת ל-0 ומאבדת את סדרה. **נמדד
    בבדיקה: סדר רשומות העבר זהה בדיוק לזה שהיה.**
  - **ציטוט — `idArg(v)`** בששת מאפייני ה-`onclick` (`editEntry` ·
    `delEntry` · `saveEntry` · `arcEditEntry` · `arcDeleteEntry` ·
    `arcSaveEntry`). ⛔ הסינון הוא **רשימת-היתר של תווים** ולא בריחה
    (סבב 38) — ערך מאפיין עובר פענוח-ישויות לפני שהוא נקרא כ-JS, ולכן
    `&#39;` שנוצר ב-`esc` היה חוזר להיות גרש וסוגר את המחרוזת.
  - **השוואה — `idEq(a, b)`** בכל עשרת אתרי ההשוואה. ⚠️ הוא נדרש גם
    לרשומות **הישנות**: מרגע שהמזהה מצוטט הוא חוזר מהמאפיין כ**מחרוזת**,
    ו-`===` מול מספר היה מפסיק למצוא אותן.
- **`createdAt` נוסף בשני אתרי היצירה** — ⛔ שדה נפרד ולא שימוש חוזר
  במזהה (סבב 38): מזהה אינו מפתח סדר.
- ⛔ **מזהים קיימים לא הומרו** (סבב 38) — הקוד סובל את שני הסוגים לנצח.
- ⛔ **מזהה הסנאפשוט לא נגע** (סבב 38) — הוא מושג אחר: הארכיון ממופתח
  לפי `gdate` (`archiveKey`), וה-`id` שם הוא נפילה-חזרה לסנאפשוטים ישנים
  בלבד. `tbSortRows` ממשיכה למיין אותו כמספר.
- שורה 25 במטריצה → **✅ בארבעתן**; שורה 26 («מזהה מכשיר») — ✅.
- `sw.js` v30→v31 ו-`app-version` ל-`25-2026-08-18-1`.

### אימותים
- `node tools/check-js.mjs` עבר מלא.
- **`tools/test_round38_ids_yoman.mjs`** — 30 טענות, פרטית לריפו הזה,
  מריצה את שלושת העוזרים האמיתיים ברתמת `vm`. הליבה היא **דו-קיום**:
  רשומה ישנה (מזהה מספרי) ורשומה חדשה (uuid) יחד — סדר נכון, ועריכה
  ומחיקה שעובדות על **שתיהן** אחרי שהמזהה עבר במאפיין וחזר כמחרוזת.
  **ארבע מוטציות:** מיון לפי המזהה · הסרת הציטוט · השוואה מספרית ·
  החזרת חותמת זמן כמזהה. כל אחת מפילה את טענת הבסיס שהיא מכוונת אליה.
- `test_round37_ids.mjs` — `wired` התהפך ל-`true`, ו-14 טענותיו עוברות.
- `test_round37_matrix.mjs` — היפוך תא 25 **ותא 26** מפיל את
  `check-capabilities`.
- שלוש רתמות קיימות (`test_round30_stage_b` · `test_round31_archive` ·
  `test_round32_unify`) קיבלו את `entryOrderTs` לרשימת החילוץ —
  ⚠️ `tbSortRows` קוראת לה, ובלעדיה היא זורקת ונתפסת ב-catch של
  `tbRowsGet`, כלומר הבדיקה הייתה מדווחת «אין רשת» במקום להיכשל.
- **`tools/test_round38_merge_core.mjs`** — בדיקה חדשה, **זהה בית-לבית
  בארבעת הריפו** פרט לבלוק `APP` (חתימה `c16cc45ef6eac219`), רצה מתוך
  השער. 19 טענות: קיום הבלוק, **שכלל ההכרעה מופיע בקוד פעם אחת בלבד**,
  שלושת כללי ברזל 6 דרך המעטפת האמיתית, וידיות המדיניות כמתועד.
  **חמש מוטציות:** הסרת סעיף ה-⏳ · היפוך שובר-השוויון · והיפוך ידית
  המדיניות של האפליקציה — כל אחת מפילה את טענת הבסיס שלה.
- ⭐ **המדידה שמאחורי האיחוד** (⚠️ מדידה ולא שער — המימוש הישן אינו קיים
  עוד בעץ, כלל ברזל 8 סעיף 6): דיפרנציאל של **125,000 מקרים** בין
  המימוש החדש למימוש שנקרא מ-`git HEAD`, בארבע האפליקציות, עם קלט שכלל
  מפתחות כפולים, רשומות בלי מפתח, חותמות חסרות, `mergePair`, `onDrop`
  (כולל רצף היומן) וענף האובייקטים — **אפס הפרשים**.
- ⚠️ **ומה שכן השתנה בפלט הרתמות, במפורש:** `test_round37_merge_pending.mjs`
  מפיק שורה אחת שונה מקו-הבסיס — טענה 1 אומרת מעכשיו «סעיף ה-⏳ קיים
  ב-`_mergePick()`» במקום בשם המנוע המקומי. זו **הצהרת מיקום**, והמיקום
  הוא בדיוק מה שהסבב הזה שינה; כל הטענות ההתנהגותיות (2א–7) זהות
  בית-לבית לקו-הבסיס, והמוטציה עדיין מפילה את טענת הבסיס.

## סבב 39 (2026-08-19) — ניקוי שורות פער שנסגרו

### מה נעשה
תיעוד בלבד, בארבעת הריפו. ⛔ אפס שינוי ב-`index.html`, ב-`sw.js` ובאף
`CACHE_NAME` — קידום גרסה בלי שינוי בקוד הרץ שולח באנר «גרסה חדשה זמינה»
לשום שינוי (סבב 26 השלמה).
- **שתי שורות פער בהנהלה נמחקו — שתיהן בוצעו ע"י המנהל ב-2026-08-18**,
  ונמדדו כאן ב-`SELECT` בלבד: מחיקת המפתח `ys_attend` (אפס ב-`kv`,
  ב-`kv_rishon`, ב-`kv_ramataviv` וב-`kv_backup`; גיבוי
  `pre-delete-2026-08-18:ys_attend` קיים וממתין למחיקת המנהל ב-~1.9),
  והרצה חוזרת של `migrations/004` — `bk_retention_keys()` מונה **34
  מפתחות**, `ys_attend` אינו בהם, והמשימה `bk_retention_daily` פעילה
  ב-`'0 3 * * *'`.
- **⭐ תיקון עובדה: `schar-limud/014` ו-`gius/0006` הורצו.** שתיהן היו
  רשומות «⛔ נכתבו ולא הורצו» בארבעת הקבצים — בסעיף ששמו דווקא «מצב
  ההרצה, ולא הצהרה». נמדד: **אפס אינדקסים חלקיים בשני הפרויקטים**.
  ⛔ אי-התאמה בין הקובץ למסד היא בדיוק מה שמייצר גרסה שלישית שאיש אינו
  יודע עליה (סבב 36), ולכן הן סומנו ✅ — אותו טיפול שסבב 37 נתן
  ל-`hanhala-ruchanit/007` באותה שורה עצמה.
- **⭐ סחיפה בין הריפו שנתפסה: `ysMergeAttend`.** סבב 38 מחק את הפונקציה
  יחד עם `ys_attend`, ועדכן את פרק סבב 37 **בהנהלה בלבד** ל-`ys_approvals`.
  שלושת הקבצים האחרים המשיכו לתאר **בהווה** פונקציה שאינה קיימת באף ריפו
  (אומת ב-grep על ארבעת קובצי `index.html`). ⛔ פרק «מה נעשה» של סבב חייב
  להיות זהה בארבעתם (כלל ברזל 8 סעיף 3) — שלושתם יושרו לנוסח המתוקן.
- ⛔ **אזכור היסטורי לא נגע** — «הקובץ נמחק בסבב 33» נשאר, ורק תיאור
  **תפעולי בהווה** של מנגנון שאינו קיים תוקן (ההבחנה של סבב 37 השלמה).

### מה נעשה כאן (yoman-avoda)
- ⛔ **אף שורת פער לא נמחקה כאן** — שלוש השורות נבדקו מול המציאות
  ונמצאו **פתוחות**, ולכן נשארו: מפתחות ה-`kv` הישנים עדיין קיימים
  (נמדד: `tb_entries`+`tb_archive` בשני המוסדות, מועד המחיקה 30.8 טרם
  הגיע) · `renderArcDetail` עדיין מדפיסה `d.hdate` בלי נפילה-חזרה
  (אומת בקוד; `d.gdate` שלצידה כן מוגנת) · `mergeSubs`, גריעת
  ה-tombstones ו-`migrateLegacyLsKeys()` עדיין ללא רתמה (אומת: אף קובץ
  ב-`tools/` אינו מזכיר אותם).
- שני התיקונים המשותפים הוחלו: «מצב ההרצה» ו-`ysMergeAttend`.

### אימותים
- `node tools/check-js.mjs` עבר מלא.
- מצב המסד נמדד ב-`SELECT` בלבד (2026-08-19), ⛔ בלי הרצת מיגרציות.

## סבב 39 — השלמה (2026-08-19): אכיפת הפערים, ושלושה פערים שנסגרו

### מה נעשה
- **⭐ כלל ברזל 15 — פרק «פערים פתוחים» נאכף בשער.** זה היה הפרק היחיד
  בקובץ ש**שום שער לא קרא**, וסבב 39 עצמו מדד את המחיר: שורות תיארו
  פערים שנסגרו, והן נקראו כמציאות עד שמישהו בדק ידנית. מעכשיו כל שורת
  פער נושאת תג — `[מדיד: <תנאי>]` או `[לא-מדיד: <נימוק>]` —
  ו-`tools/check-gaps.mjs` (זהה בית-לבית ×4, ⛔ בלי בלוק `APP`) נכשל
  כשתנאי מדיד **חדל להתקיים**.
  ⛔ **הבודק מדווח ואינו מוחק** — מחיקה אוטומטית של תיעוד עוקפת את שיקול
  הדעת שקובע מה לכתוב במקום.
  ⚠️ **התג נדרש מפני שפרוזה אינה ניתנת לגזירה אמינה** — ⛔ ולא ניחשנו
  מתוך הטקסט: ניחוש היה מייצר שער שנופל על ניסוח ועובר על מציאות.
- **⭐ תוספת לכלל ברזל 14 — מי מוחק שורת פער שנסגרה בפעולת מנהל.** הכלל
  הרגיל («הסבב שסוגר פער מוחק את שורתו באותו קומיט») אינו מכסה את המקרה
  השכיח כאן: הסשן אינו יכול למחוק שורה שבזמנו פתוחה, והמנהל אינו עורך
  קבצים. מעכשיו: **נמחקת בסבב הראשון שנוגע בקובץ, כסעיף אחרון, בלי סבב
  ייעודי** — ובכפוף למדידה.
- **⭐ שלושה פערים שאינם תלויי-זמן נסגרו:** שני ממצאי התצוגה ביומן ·
  מסלול ה-`exec_sql` בהנהלה · שכבת השורות של סדרי השינה.
- **טבלת «מצב המיגרציות במסד הייצור» הושלמה בארבעתם** — schar קיבלה את
  `013`+`014`, gius את `0005`+`0006`, ⭐ ו-yoman ו-hanhala קיבלו טבלה
  כזו **לראשונה** (עד כה מצב ההרצה שלהן היה מפוזר בין כותרות המיגרציות
  לפרקי הסבבים).
- **שתי שורות פער מטעות נוסחו מחדש:** ⚠️ נמדד ש-`HW_CFG.enabled = true`
  בשכר ובגיוס — החלון החם **כבר פעיל בשתיהן ורץ בכל עלייה**, ופשוט אין
  עדיין מה לפנות. הניסוח הקודם נקרא כאילו היכולת חסרה.
- **שורות «רתמות הבדיקה ההיסטוריות» יצאו מפרק הפערים** בשלושת הריפו שהיו
  בהן. ⛔ הן אינן חוב אלא **מגבלה מודעת**: רתמה שתיכתב היום מול הקוד
  הנוכחי מקבעת את ההתנהגות הנוכחית — כולל באג שטרם התגלה — והלקחים ממילא
  זוקקו לכללי ברזל בסבב 34.

### מה נעשה כאן (yoman-avoda)
- **⭐ שני ממצאי התצוגה של סבב 31 תוקנו, ושורת הפער נמחקה.**
  - `renderArcDetail` הדפיסה `d.hdate` **בלי נפילה-חזרה**, בזמן ש-`d.gdate`
    שלצידה כן הייתה מוגנת. הוחל הדפוס הקיים בשאר התצוגות —
    `d.hdate||d.gdate||arcSelDayKey`, בדיוק כמו ב-`renderArcBreadcrumb`.
  - `arcCleanupCorrupted` ריפאה `hdate` שבור **רק בתוך ענף «אין gdate»**,
    ולכן סנאפשוט מתוארך עם `hdate` שבור לא רופא לעולם. נוסף שלב «א2»
    שמרפא גם אותו. ⛔ הגזירה היא מ-`gdate` בלבד ולא מהשעון (סבב 39) —
    שעון היה נותן את יום הריצה במקום את יום הסנאפשוט, ⚠️ ועם עוגן
    בצהריים, כמו כל חישוב תאריך בפרויקטים האלה.
- **טבלת מצב המיגרציות נוצרה** — `000`–`005`; הפרוזה שקדמה לה כיסתה את
  `002`–`005` בלבד ו-`000`/`001` לא הופיעו בה כלל.
- **שורת «רתמות הבדיקה»** עברה לפרק «רתמות הבדיקה ההיסטוריות — מצב מקובל».
- ⛔ **שורת הפער היחידה שנשארה** — מחיקת מפתחות ה-`kv` — סומנה
  `[לא-מדיד]`: קיומם הוא מצב מסד ואינו נראה מהריפו.
- `sw.js` v32→v33 ו-`app-version` ל-`27-2026-08-19-1`.

### אימותים
- `node tools/check-js.mjs` עבר מלא.
- `test_round39_gaps.mjs` — 16 טענות, כולל שמונה מוטציות.

## סבב 39 — השלמה שנייה (2026-08-19): keystore, שלדי md, ואסימטריות אחרונות

### מה נעשה
- **⭐ gius — keystore חדש בשם אחיד, בהחלטת המנהל.** עד היום ישבו ב-`signing/`
  **שני** קבצים: `pwabuilder.keystore` (הפעיל — ה-APK המותקן בפועל חתום בו,
  נמדד ב-2026-08-19) ו-`gius.keystore` הישן, שמעולם לא חתם דבר. שניהם נמחקו,
  ובמקומם נוצר keystore חדש בשם `gius.keystore` — אותו דפוס שם בדיוק כמו בשלוש
  האחיות. ⛔ **APK חדש אינו מתקין על גבי התקנה קיימת** — נדרשת הסרה והתקנה
  מחדש; המנהל אישר במפורש (שני מכשירים, אפס נתונים שלא סונכרנו).

- **⭐ שלושת קובצי ה-md הנלווים קיבלו שלד קנוני, ושער שאוכף אותו.**
  `README.md`, `CONTEXT.md` ו-`android/README.md` היו «כמעט זהים» בארבעתן —
  ⛔ וזה בדיוק המצב שכלל ברזל 14 אוסר: לא «הוחל בארבעתן», לא חריגה מנומקת,
  ולא פער עם טריגר. מה שנמדד ותוקן:
  **א · `README.md`** — הכותרת ב-gius הייתה **באנגלית** בזמן ששלוש האחיות
  עברית; יושרה.
  **ב · `CONTEXT.md`** — ⛔ **שני שלדים**: schar ו-gius החזיקו «כללים
  קריטיים · טבלאות · מצב נוכחי», ול-yoman ול-hanhala לא היה מבנה כלל.
  ⚠️⚠️ **והחמור מכך:** אזהרת «Supabase — GRANT חובה לטבלאות חדשות» ישבה
  ב**שלוש** ו⛔ **נעדרה מ-yoman** — כלומר דווקא הכלל שאומר שכל טבלה נולדת
  עם `DELETE` ו-`TRUNCATE` לא היה בקובץ שסשן חדש קורא ראשון.
  ⚠️ **והנוסח הקנוני שנבחר הוא `revoke` ואז `grant`** ולא הנוסח שישב
  ב-schar ובהנהלה, שהעניק `delete` לשלושת התפקידים: הוא קדם למיגרציה 012
  ולכלל ברזל 10 סעיף 9, והעתקתו ל-yoman הייתה מפיצה הפרה במקום לסגור פער.
  **ג · `android/README.md`** — לשלוש שלד זהה; ל-gius חסרו שני פרקים
  והכותרות היו מתורגמות. יושר, ופרק «אייקונים» שקיים רק שם נושא שורת נימוק
  (מחולל האייקונים הוא פרטי ל-gius).
- **`check-docs.mjs` — סעיף ו חדש: שלד שלושת הקבצים.** לכל קובץ רשימת
  כותרות **נדרשות בסדר יחסי**; ⚠️ פרק **נוסף** הוא לגיטימי (העברת «אייקונים»
  ב-gius, «Notes» ביומן) — הבדיקה דורשת נוכחות וסדר, לא סגירות. ⛔ הבדיקה
  אינה משווה תוכן: התוכן פר-אפליקציה בהגדרה (כלל ברזל 8 סעיף 4), והשלד הוא
  מה שמשותף.
- **`tools/test_round39_md.mjs`** — בדיקה חדשה, **זהה בית-לבית בארבעתן**
  (⛔ בלי בלוק `APP`), רצה מתוך השער. 13 טענות, ובהן **שלוש מוטציות**:
  החלפת כותרת נדרשת ב-`## ---` בכל אחד משלושת הקבצים חייבת להפיל את
  `check-docs`.
- **⛔ סוד שנמצא ונמחק תוך כדי היישור:** ב-`CONTEXT.md` של הנהלה ישבה
  **סיסמת admin בטקסט גלוי**, בריפו ציבורי — הפרה ישירה של כלל ברזל 10
  סעיף 8 («האיסור חל על כל סוגי הקבצים»), שנעלמה מהסריקות מפני שהן כיסו
  SQL, קוד ו-HTML ולא `.md`. השורה הוסרה, ובמקומה נרשם ⛔ מפורש.
  ⚠️ **ומחיקה אינה תיקון:** הערך נשאר בהיסטוריית ה-git לנצח, והטיפול היחיד
  הוא **החלפתו במסד** — פעולת מנהל.

- **⭐ ארבע אסימטריות אחרונות בשכבת הבנייה — כל אחת הוכרעה לכיוון אחד, ×4.**
  **א · `signing/sign-apk.bat`** — היה ביומן בלבד. ⛔ **נמחק.** נמדד לפני
  המחיקה: **אפס קוראים בארבעת הריפו** — לא workflow, לא תיעוד ולא סקריפט —
  והוא מסלול חתימה **שני** שאין בו שערי הטביעה שב-`sign-apk.sh`. שני מסלולי
  חתימה למפתח שאסור לטעות בו הם שני מקורות אמת, וזה שאין לו קוראים הוא זה
  שייסחף.
  **ב · `.nojekyll`** — היה ביומן וב-gius בלבד, בשניהם כחריגה מנומקת.
  ⭐ **הוכרע ×4, והועלה לסט הקנוני** (`ROOT_FILES` ב-`check-structure.mjs`):
  מעכשיו **היעדרו מפיל את השער**, ושתי החריגות נמחקו מרשימות-ההיתר.
  ⚠️ הכיוון הוא הוספה ולא מחיקה מפני שהקובץ חסר-נזק, והוא מה שמונע
  מ-Jekyll לבלוע בשקט כל נכס עתידי שמתחיל בנקודה או בקו-תחתון.
  **ג · `manifest.json`** — hanhala קיבלה `orientation: "portrait"` (היה
  בשלוש), ו-gius **איבדה את `id`**. ⚠️ **הכיוון נמדד ולא הונח:** ה-`id`
  המפורש של gius (`/gius/`) **זהה בדיוק** ל-id המשתמע מ-`start_url: "./"`,
  ולכן הסרתו היא no-op; ואילו **הוספת** `id` לשלוש הייתה חייבת לשאת את ה-id
  המשתמע שלהן (`/<repo>/index.html`), וטעות של תו אחד שם הופכת אפליקציה
  **מותקנת** לאפליקציה זרה. ⛔ אין להוסיף `id` לאף אחת מהארבע.
  ⚠️ ומה שנמדד ו**לא** יושר: `orientation` ב-gius הוא `portrait-primary`
  ובשלוש `portrait` — ערך תצוגה פר-אפליקציה כמו `theme_color`, ושינויו הוא
  שינוי התנהגות שאיש לא ביקש.
  **ד · טריגר ה-`push` ב-`build-apk.yml` הועתק לשלוש.** הוא היה ב-gius
  לבדה, עם נימוק כתוב: בלי אירוע `push`, GitHub מציע «Run workflow» רק
  לקבצים שכבר יושבים על ענף ברירת המחדל — כלומר workflow חדש **אינו ניתן
  להרצה מענף עבודה**. ⛔ שיפור שקיבלה אחת ולא קיבלו השלוש הוא בדיוק מה שכלל
  ברזל 14 אוסר, ⛔ ואין למחוק אותו מ-gius. ה-`paths` נבדקו והם תקפים
  בארבעתן — `android/**` · `signing/**` · קובץ ה-workflow עצמו; ⚠️ קוד web
  אינו ברשימה **בכוונה**, מפני שהמעטפת טוענת מהרשת ואינה מצריכה APK חדש.
- **⭐ ושבעה פריטים נמדדו ונרשמו כתקינים — ⛔ בלי לגעת בהם.** ⚠️ הרישום כאן
  הוא כדי שלא ייבדקו שוב ולא ייקראו כשרידים:
  `android/app/src/main/res/xml/file_paths.xml` קיים ביומן וב-gius בלבד —
  **וזה תואם בדיוק לקוד**: `AndroidManifest.xml` של שתיהן מכריז `FileProvider`
  שמצביע עליו, ובשתי האחרות אין הכרזה ואין קובץ (נמדד ב-2026-08-19, התאמה
  מלאה). ⛔ מחיקתו ב-gius הייתה מפילה את ה-`FileProvider` בזמן ריצה.
  וכן: `cleanup-merged-branches.yml` **זהה בית-לבית בארבעתן** (אומת ב-md5) ·
  `AndroidManifest` עם אותן שתי הרשאות ו-minSdk 21 · `setup-db.html` ·
  `tools/fixtures/round31_archive.txt` · `android/copy-assets.sh` ·
  ומספור ה-sql/ה-mjs הנבדל בין הריפו. ⛔ אלה אינם ליקויים.

- **⭐ עוגני `check-gaps` הוקשחו — הכשל שנמדד היה בכיוון המסוכן.** המנהל
  מדד ששינוי **רווחים בלבד** באותה שורה (`X = true` → `X  =  true`) מפיל
  את השער עם «⭐ הפער נסגר», בזמן שהקוד **לא השתנה**. ⛔ הודעה שמכריזה
  שפער נסגר היא הודעה שמזמינה מחיקת שורת תיעוד — כלומר השער שנבנה לשמור
  על התיעוד היה זה שמזמין להרוס אותו. שני חישוקים נוספו, ושניהם ב-`code:`:
  **א · נרמול רווחים לפני ההשוואה** — רצף רווחים, טאבים ושורות חדשות
  מתקפל לרווח יחיד **בשני הצדדים**. ⛔ שמות, מרכאות ותוכן **אינם**
  מנורמלים: נרמול שלהם היה הופך את העוגן לשער שעובר על קוד אחר לגמרי.
  **ב · עוגן בסיס — שם הסמל לבדו.** לא נמצא העוגן המלא ו⛔ **גם הסמל
  אינו קיים** ⇒ **«⚠️ העוגן נרקב — דורש בדיקה ידנית»**, ⛔ ולעולם לא
  «הפער נסגר»: סמל שנעלם אינו ראיה שהפער נסגר אלא שהעוגן הפסיק להצביע על
  משהו. נמצא הסמל ולא נמצא העוגן המלא ⇒ הפער אכן נסגר, וההודעה אומרת גם
  שעוגן הבסיס כן נמצא. ⚠️ מילות מפתח מדולגות בגזירת הסמל — ב-`function
  migrateOutbox` הסמל הוא `migrateOutbox`.
  **חמש טענות חדשות ב-`test_round39_gaps.mjs`**, ובהן שלוש המוטציות
  שהסבב דרש: שינוי רווחים ⛔ **אינו** מפיל · שינוי **ערך** נופל כ«נסגר»
  ⛔ ולא כ«נרקב» · מחיקת **הסמל** נופלת כ«נרקב» ⛔ ולא כ«נסגר».
  בלוק ה-SHARED `iron-rule-15-gaps-verified` והחתימה הקנונית עודכנו
  בארבעתם.

- **⭐ כלל ברזל 16 — הנחת השריד.** בלוק SHARED חדש, זהה בית-לבית ×4:
  ⛔ **הוראת מחיקה שנימוקה «שריד», «מיותר» או «לא בשימוש» אינה מבוצעת לפני
  מדידה שמוכיחה שאין לו קורא**; נמצא קורא — הסשן עוצר ומדווח, ⛔ ואינו
  מוחק, גם אם ההוראה הייתה מפורשת.
  ⚠️ **הכלל נולד ממדידה ולא מעיקרון:** שלושה ממצאים בסבב הזה זוהו כשרידים
  **לפי המראה**, ובשלושתם המדידה הפריכה את ההנחה ומחיקה הייתה שוברת
  אפליקציה — `file_paths.xml` ב-gius (ה-`AndroidManifest` מכריז `FileProvider`
  שמצביע עליו) · `migrateOutbox()` (ההגירה החד-פעמית של התור שהוסר בסבב 12) ·
  `_bkRetention` (שכבה שנכשלת-סגור, שהפינוי במסד לא ייתר).
  ⛔ **והכלל אינו איסור על מחיקה:** באותו סבב `signing/sign-apk.bat` **כן**
  נמחק — אחרי שנמדד שאין לו אף קורא. המדידה היא שהתירה, לא המראה.
  מפתח הניווט עודכן ל-«כללי הברזל 6–16», והרשימה הקנונית וחתימות ה-SHARED
  עודכנו בארבעת עותקי `check-docs.mjs`.
  ⚠️ **ואגב הכלל תוקן תיאור שהתיישן באותו רגע:** `.nojekyll` תואר בכלל
  ברזל 14 כ«חריגה מנומקת» — והוא כבר אינו חריגה אלא חלק מהסט הקנוני
  (סעיף ב למעלה). ⛔ חריגה שקיימת בשתיים מארבע אינה «חריגה מנומקת» אלא
  אסימטריה שלא הוכרעה.

### מה נעשה כאן (yoman-avoda)
- **`CONTEXT.md` נכתב מחדש לשלד הקנוני** — הוא היה המיושן מבין הארבעה,
  ו⛔ **אזהרת ה-GRANT נעדרה ממנו לגמרי**. נוספו: «פרטי ריפו» · אזהרת
  ה-GRANT בנוסח `revoke`+`grant` (עם ההפניה ל-`migrations/001`) · חמישה
  כללים קריטיים · טבלת הטבלאות (`kv_rishon`/`kv_ramataviv` ו-`tb_entries`
  המאוחדת עם דגל `archived`) · «מצב נוכחי» עם `CACHE_NAME` ו-`app-version`
  הנוכחיים · «פרטי מערכת».
- ⚠️ **שני הפרקים הפרטיים כאן נשמרו כזנב מתחת לשלד** — תיקון URL ב-smali
  וכלל ה-Cache-APK. ⚠️ ונוספה להם הבהרה שנמדדה: הם רלוונטיים ל-APK שנבנה
  **לפני** `android/`, והמעטפת של היום טוענת מהרשת ואין בה URL לתקן.
- `README.md` ו-`android/README.md` **כבר תאמו את השלד** — לא נגעו.
- ⛔ **אין קוד רץ שהשתנה ואין קידום `CACHE_NAME`** — הסבב נגע כאן בתיעוד
  ובשער בלבד.

- **`signing/sign-apk.bat` נמחק** — הוא היה כאן בלבד, ולא היה לו אף קורא
  (נמדד: אפס אזכורים בארבעת הריפו). החתימה הידנית היא `signing/sign-apk.sh`,
  והבנייה היא דרך `build-apk.yml`.
- **`.nojekyll` עבר מרשימת-ההיתר לסט הקנוני** — הקובץ עצמו לא נגע;
  `rootExtra` כאן ריק מעכשיו.
- **טריגר ה-`push` נוסף ל-`build-apk.yml`** בנוסח שנמדד מ-gius, כולל ההערה
  שמסבירה למה הוא נחוץ.
- ⛔ **אין קוד רץ שהשתנה ואין קידום `CACHE_NAME`** — השינויים כאן הם בשכבת
  הבנייה ובשער.

### אימותים
- `node tools/check-js.mjs` עבר מלא.
- `test_round39_md.mjs` — 13 טענות, כולל שלוש המוטציות (כותרת נדרשת שהוחלפה
  ב-`## ---` בכל אחד משלושת הקבצים מפילה את `check-docs`).
- `test_round39_gaps.mjs` — 21 טענות (היו 16), ובהן שלוש המוטציות שהסבב
  דרש במפורש: שינוי **רווחים בלבד** ⛔ אינו מפיל · שינוי **ערך** נופל
  כ«⭐ הפער נסגר» · מחיקת **הסמל** נופלת כ«⚠️ העוגן נרקב», ⛔ ולא כ«נסגר».
- `check-structure.mjs` — `.nojekyll` בסט הקנוני, ולכן היעדרו מפיל את
  השער; אומת שהקובץ **זהה בית-לבית בארבעתן** מחוץ לבלוק `APP`.
- שלוש חתימות SHARED עודכנו בארבעת עותקי `check-docs.mjs`:
  `iron-rules-storage` (מפתח הניווט) · `iron-rule-14-org-wide`
  (`.nojekyll` אינו חריגה) · `iron-rule-15-gaps-verified` (הקשחת העוגן),
  ובלוק חדש — `iron-rule-16-remnant`.

### סריקת המשך (סעיף 12) — ⛔ דיווח בלבד, לא תוקן בסבב הזה
שני תחומים נסרקו בארבעת הריפו. ⛔ **שום ממצא כאן לא תוקן** — הם נרשמים
כדי שייכנסו לסבב הבא בהחלטת המנהל, ולא ייעלמו עם הסשן (כלל ברזל 8 סעיף 6).
⚠️ ההיקף המוערך שלצד כל ממצא הוא **הערכה** ולא מדידה.

**א · שכבת הבנייה**
1. **`MainActivity.java` — ארבעה עותקים של אותה מעטפת, בלי שום שער.** נמדד:
   hanhala (198 שורות) ו-schar (197) **כמעט זהות בית-לבית** — ההפרש כולו
   זהות, צבע אחד ומשפט עברי אחד; gius (185) נבדלת גם בניסוח ה-javadoc;
   yoman (391) כפולה מהן בגלל גשר השיתוף. ארבעתן מממשות את אותם
   ארבעה דברים (`onShowFileChooser` · דף אופליין ב-`text/html` ·
   `onBackPressed` · השארת `http(s)` בתוך המעטפת) — ⛔ ואף בדיקה אינה
   נוגעת בהן. **היקף:** שער חתימה על הקובץ עם מצייני-זהות ≈ חצי סבב;
   חילוץ לתבנית משותפת — סבב ייעודי.
2. **`build.gradle` — `versionCode`/`versionName` בלי שער.** הקבצים תואמים
   פרט ל-`applicationId`, לגרסה ולתלויות (רק ל-yoman יש `androidx.webkit`
   ו-`core`, בגלל הגשר). ⚠️ ומה שאין: שום דבר שמוודא ש-`versionCode` עלה
   כשהמעטפת השתנתה — בדיוק הכשל שקידום `CACHE_NAME` נבנה נגדו בשכבת ה-web.
   **היקף:** קטן — בדיקה שקוראת את `build.gradle` ומשווה מול git.
3. **אייקוני המעטפת — נמדדו ותקינים:** 10 קובצי `mipmap` ו-XML אדפטיבי אחד
   בכל אחת מהארבע. ⛔ אינו ליקוי; נרשם כדי שלא ייסרק שוב.
4. **⭐ `sw.js` — ארבעה מימושים שונים באמת, ואיש לא החליט על כך.** נמדד:
   92 שורות (schar) · 182 (yoman) · 210 (hanhala) · 242 (gius), עם קבוצות
   פונקציות **זרות זו לזו** — `cachePut` · `networkFirst`/`isVersionCheck` ·
   `cacheFirst`/`revalidate`/`handleNavigate`/`cachedShell`. מה שכן מיושר
   הוא רק מה שסבבים קודמים יישרו במפורש: תבנית `CACHE_NAME`, דילוג על
   supabase, רשימת ה-CDN ו-`ensureCdnCached` (קיים בארבעתן). ⚠️ **אסטרטגיית
   ה-fetch עצמה — הדבר שקובע מה המשתמש רואה אופליין — היא פר-אפליקציה
   ואינה רשומה בשום מקום כהחלטה.** **היקף:** מדידה + שורת מטריצה + הכרעה =
   סבב מלא; זה הקובץ המסוכן ביותר אחרי `index.html`.
5. **yoman — שני מרנדרים לאותו דוח.** `exportPDF()` (122 שורות) בונה HTML
   כמחרוזת וקורא `window.print()` בחלון קופץ, ואילו מסלול התמונה
   (`_buildReportDiv` + `_renderReport` מעל `html2canvas`) בונה את אותו דוח
   שוב, אחרת. ⚠️ שתי אמיתות לשאלה «איך נראה הדוח», ושינוי בעיצוב חייב
   להיעשות פעמיים או להתפצל בשקט. **היקף:** איחוד = סבב ייעודי (רינדור,
   RTL וגופנים עבריים).

**ב · מבנה פנימי**
6. **אורכי פונקציות — נמדדו, והתמונה בריאה.** חציון 8–11 שורות בארבעתן;
   פונקציות מעל 120 שורות: yoman 5 · hanhala 9 · schar 1 · gius 2.
   הארוכות באמת: `showPage` (251) ו-`closeReasons` (200) בהנהלה,
   `findTask` (268) ב-gius, `arcCleanupCorrupted` (138) ביומן.
   ⚠️ **המספר הוא מרחק עד הפונקציה הבאה ברמה העליונה**, ולכן הוא הערכה
   מלמעלה — הוא סופר גם קוד שאינו בתוך הפונקציה. **היקף:** פיצול פר-פונקציה,
   כמה שעות לכל אחת.
7. **מספור צעדים בהערות — קיים פעם אחת בכל הארגון.** ⚠️ ונמדד שהוא **אינו**
   ב-`renderArcDetail` אלא ב-`arcCleanupCorrupted` שביומן («א2.»), ונוסף
   בסבב 39 עצמו. ⛔ דפוס בן מופע אחד אינו דפוס: או שמרחיבים אותו לאזורים
   רב-שלביים, או שמסירים אותו. **היקף:** זעיר.
8. **שמות פונקציות פנימיות שחוזרים באותו קובץ.** בהנהלה `nameById` מוגדרת
   **פעמיים בגוף זהה** (שורות 7537 ו-9003), וכך גם `mkBtn`/`mkClearBtn`/
   `renderRow`; ב-gius `step`; ביומן `save`. ⚠️ כולן פונקציות פנימיות בתוך
   פונקציות שונות, ולכן אינן מתנגשות — אבל הן מקשות על חיפוש טקסטואלי,
   שהוא כלי העבודה העיקרי בקובץ בן אלפי שורות. **היקף:** קטן לכל אחת.
9. **⭐ קוד זהה בית-לבית **מחוץ** לכל בלוק משותף.** `_randDeviceId` זהה
   **בארבע** (חמש שורות, אומת ב-md5) ו-`getDeviceId` נבדלת רק במפתח
   האחסון — הן רשומות במטריצה (שורה 26) אבל ⛔ **אינן בתוך בלוק SHARED
   ואין להן חתימה**. בנוסף: `checkForUpdate` חולקת חלון של 10+ שורות זהות
   בין הנהלה ל-schar, ⚠️ **והשם עצמו נבדל בארבעתן** — `checkForUpdate` ·
   `checkForUpdates` · `checkForSwUpdate` · ואין כזו ב-gius. **היקף:**
   הפיכת מזהה המכשיר לבלוק משותף ≈ חצי סבב (הבלוק, החתימה, וה-probe כבר
   קיים); איחוד מנגנון העדכון — סבב ייעודי.
10. **⭐ ומה שהמדידה אומרת על הכפילות בכלל:** מחוץ לבלוקים המשותפים חוזרים
    בין הריפו **ארבעה בלבד** חלונות של 10 שורות זהות, ושלושה מהם בלוקי
    **הערות** (ניסוח ההודעות, תקן ההערות, והערת ההגנה על מסך ההגדרות).
    ⚠️ כלומר חילוץ המודולים המשותפים כמעט הושלם, והכפילות שנותרה יושבת
    **בשכבת הבנייה** (סעיפים 1 ו-4) ולא ב-JS.

## סבב 40 (2026-08-19) — שכבת ה-APK, המעטפת והסיסמאות

### מה נעשה
- **⭐ שער `versionCode` למעטפת — `tools/test_round40_gradle.mjs`, זהה
  בית-לבית בארבעתן** פרט לבלוק `APP`, ורץ מתוך `check-js.mjs`.
  ⚠️ **הפער שנמדד בסבב 39 (סריקת ההמשך, סעיף 2):** ארבעת קובצי
  `android/app/build.gradle` תואמים זה לזה פרט ל-`applicationId`, לגרסה
  ולתלויות — **ואף דבר לא ווידא ש-`versionCode` עלה כשהמעטפת השתנתה.**
  זה בדיוק הכשל שקידום `CACHE_NAME` נבנה נגדו בשכבת ה-web.
  ⛔ **וכאן הוא חמור יותר** (סבב 40): מטמון שלא קודם מתוקן בשחרור הבא,
  ואילו APK עם `versionCode` שאינו עולה **נדחה בזמן ההתקנה** או מותקן
  מעל עצמו ומשאיר את המשתמש עם המעטפת הישנה, בלי שום סימן.
  השער נכשל על ארבעה סוגי סטייה: **צורה** (`versionCode` שאינו מספר שלם
  חיובי, `versionName` חסר, `applicationId`≠`namespace`) · **זהות**
  (מזהה החבילה אינו זה שרשום ב-`APP`) · ⭐ **קידום** (קובץ תחת
  `android/` השתנה מול `origin/main` ו-`versionCode` לא עלה) ·
  **נסיגה** (`versionCode` ירד).
  ⚠️ **סעיף הקידום מדלג ואינו מפיל** כשאין git או אין `origin/main` —
  אותו דפוס בדיוק כמו סעיף ה של `check-docs.mjs` (סבב 20). ⛔ שער
  שנופל בסביבה בלי git היה חוסם דחיפה מסיבה שאינה קשורה לקוד.
  ⛔ **והשער אינו נוגע בתלויות** (סבב 40) — ליומן לבדה יש
  `androidx.webkit` ו-`androidx.core`, מפני שרק לה יש גשר שיתוף. זו
  חריגה מנומקת ומדודה, ואין ליישר אותה.

### מה נעשה כאן (yoman-avoda)
- `tools/test_round40_gradle.mjs` נוסף ונרשם ב-`check-js.mjs`.
  ⚠️ `versionCode 2` כאן הוא המעטפת שטוענת מהרשת (1 היה עותק ה-`file://`),
  ו-`applicationId` הנעול הוא `com.yoman.avoda`.
- ⛔ **התלויות לא נגעו** — `androidx.webkit` ו-`androidx.core` נחוצות כאן
  לגשר השיתוף (`WebViewCompat.addWebMessageListener`), והן החריגה המנומקת
  שהשער אינו בודק.
- `tools/test_round40_shell.mjs` נוסף ונרשם ב-`check-js.mjs`.
  ⛔ **המעטפת עצמה לא נגעה** — היא כבר נשאה את ששת סעיפי החוזה, כולל
  `setAllowFileAccess(false)`. החתימה שנרשמה היא `161b6a469d6b45eb`.
- ⚠️ **וזו החריגה היחידה מהחתימה הקנונית, והיא מדודה:** גשר השיתוף
  (`installShareBridge`/`syncLegacyBridge`/`isAppOrigin`) מכפיל כאן את
  המעטפת ל-8118 תווים מנורמלים מול 4014 בשלוש האחיות. השער מאמת בנוסף
  שהמסלול המאובטח (`ALLOWED_ORIGINS`/`addWebMessageListener`) קיים —
  ⛔ גשר בלי רשימת מקורות מפיל את השער.
- ⛔ אין קידום `versionCode` ואין קידום `CACHE_NAME` — לא המעטפת ולא
  הקוד הרץ השתנו כאן.
