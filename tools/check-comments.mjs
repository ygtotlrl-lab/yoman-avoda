#!/usr/bin/env node
/*  בדיקת תקן ההערות בקוד — סבב 28.
 *
 *  ⭐ משימת הבודק: סורק את ההערות שב-`index.html` וב-`tools/` — ⛔ ומפיל על
 *  סימן שאינו אחד משלושה, על ⛔ בלי סבב וסיבה, ועל מפריד ברוחב שגוי.
 *
 *  כלל ברזל 11 של הארגון: **הערה על אותו סוג דבר נראית אותו דבר בארבע
 *  האפליקציות.** סבב 27 הוסיף תיעוד פנימי ל-gius ומדד שהאחיות נבדלות ממנה
 *  לא באחוזים אלא ב**כיסוי ובצורה**: אזורים לוגיים שלמים בלי כותרת, הערות
 *  ⛔ בלי מספר הסבב שממנו נלמד האיסור ובלי סיבה, וסימנים שהומצאו לצורך
 *  שורה אחת. הבדיקה הזו רצה עם שערי התחביר לפני כל דחיפה, ונכשלת על ארבעת
 *  סוגי הסטייה:
 *
 *    א. **כותרת בלוק** — אזור לוגי בלי כותרת בפורמט התקני, או כותרת
 *       שצורתה אינה תקנית. הכיסוי נמדד מבנית: הקוד הפרטי חייב להיפתח
 *       בכותרת, ואסור שיהיה בין שתי כותרות רצף ארוך מ-MAX_AREA_LINES.
 *    ב. **⛔ בלי סבב או בלי סיבה** — בלוק הערה שיש בו ⛔ חייב לשאת
 *       `סבב <N>` **וגם** מפריד « — » שאחריו הנימוק.
 *    ג. **סימן שאינו מהרשימה** — שורת הערה שנפתחת בסמל חייבת להיפתח
 *       באחד משלושה: ⛔ איסור · ⚠️ אזהרה · ⭐ החלטה מרכזית.
 *    ד. **טרמינולוגיה** — מונח מהרשימה שנכתב בגרסה שאינה התקנית.
 *
 *  ⚠️ הבדיקה חלה על **האזורים הפרטיים בלבד**. חמשת הבלוקים המשותפים
 *  והמודולים הקפואים (סבבים 11–17) מוחרגים: הם זהים בית-לבית בארבע
 *  האפליקציות ונשמרים ע"י `check-status-area.mjs`, ועריכה שלהם כאן הייתה
 *  מחייבת שינוי בארבעתם ובחתימות — כלומר הבדיקה הזו הייתה מייצרת בעצמה
 *  את הסחיפה שהיא באה למנוע.
 *
 *  ⚠️ הבדיקה קוראת **הערות בלבד** ולא מחרוזות. הקובץ עובר טוקניזציה של JS
 *  (מחרוזות, תבניות ו-regex) — בלי זה `'✅ נשמר'` שבטוסט היה נספר כהערה
 *  שנפתחת בסמל אסור, ו-`/https:\/\//` היה נקרא כפתיחת הערה.
 *
 *  הקובץ זהה בית-לבית בארבעת הריפו פרט לבלוק `APP` שבראשו.
 */
import fs from 'node:fs';

/* ── APP — הדבר היחיד שנבדל בין הריפו ──────────────────────────────────── */
const APP = {
  app: 'yoman-avoda',
  file: 'index.html',
  /*  ⛔ שני המודולים האלה נעדרים כאן במכוון — אין מסך כניסה: `lock`
   *  (סבב 52) מפני שאין מה לנעול, ו-`sess` (סבב 53) מפני שאין משתמש
   *  מחובר להחזיק. הרישום התואם יושב במטריצה (שורות 37 ו-38)
   *  כ«לא רלוונטי». */
  skipBlocks: ['נעילת חוסר-פעילות — מודול משותף (סבב 52)',
               'מודל הסשן — מודול משותף (סבב 53)'],
};
/* ── סוף APP ───────────────────────────────────────────────────────────── */

/*  ⛔ השורות בטבלת התשתית שהקובץ הזה אוכף (סבב 72) — ⚠️ המיפוי היה
 *  חד-כיווני ב-`check-capabilities` בלבד, ⛔ ומי שערך שער כאן לא ראה
 *  אותו. ⭐ הבודק גוזר את המיפוי מכאן, ⛔ ואינו מחזיק רשימה משלו. */
export const ROWS = [17, 19, 21, 24, 27, 30, 35, 59, 60, 61, 111];

/* הבלוקים המשותפים והמודולים הקפואים — מוחרגים מכל ארבעת הסעיפים.
   ⚠️ הסימון הוא **טקסט הסמן בלבד**, בלי מסגרת ה-`═` שלפניו: במודול האחסון
   שם המודול יושב בשורה **השנייה** של הכותרת ולא בראשונה, וסמן שכלל את
   המסגרת היה מפספס אותו בשקט — כלומר מודול קפוא שהיה נבדק כאילו הוא קוד
   פרטי. ההחרגה מתחילה בפתיחת ההערה שלפני הסמן ונגמרת בסגירת ההערה
   שאחרי סמן הסיום. */
const SHARED_BLOCKS = [
  ['עמידות אחסון מקומי — מודול משותף (סבב 11)', 'סוף המודול המשותף'],
  ['ממתין לסנכרון — מודול משותף (סבב 12)',       'סוף מודול "ממתין לסנכרון"'],
  ['"מידע טכני" — קיפול מסכי האחסון',              'סוף רכיב "מידע טכני"'],
  ['אזור מצב — בלוק "☁️ סנכרון"',                  'סוף בלוק "☁️ סנכרון"'],
  ['גיבוי יומי ויומן פעולות — מודול משותף (סבב 30)', 'סוף מודול הגיבוי היומי'],
  ['חלון חם ושחזור מקומי — מודול משותף (סבב 35)',    'סוף מודול החלון החם'],
  ['מיזוג רשומות — מודול משותף (סבב 38)',            'סוף מודול המיזוג'],
  ['מזהי רשומות — מודול משותף (סבב 37א)',            'סוף מודול מזהי הרשומות'],
  ['מזהה מכשיר — מודול משותף (סבב 40)',              'סוף מודול מזהה המכשיר'],
  ['ניסיון חוזר בסנכרון — מודול משותף (סבב 44)',   'סוף מודול הניסיון החוזר'],
  ['מנגנון המשיכה — מודול משותף (סבב 51)',           'סוף מנגנון המשיכה'],
  ['נעילת חוסר-פעילות — מודול משותף (סבב 52)',  'סוף מודול נעילת חוסר-הפעילות'],
  ['מודל הסשן — מודול משותף (סבב 53)',              'סוף מודול הסשן'],
];
/* ⚠️ הבלוק המשותף החמישי — «CSS אזור המצב» — אינו ברשימה מפני שהוא יושב
   ב-`<style>` ולא ב-`<script>`, וכל מה שאינו JS מוטבע מולבן ממילא. הוספתו
   כאן הייתה מפילה את הבדיקה על סמן שלא ייתכן שיימצא.

   ⛔ הרשימה הזו חייבת להיות שקולה ל-`CAPS` שב-`check-capabilities.mjs`
   (סבב 45ב) — מודול משותף שנספר כאן כקוד פרטי נמדד בתקרת 400 השורות
   ומחייב כותרת שאין לו, ומודול שנעדר משם אינו נאכף בחתימה כלל. ⚠️ שתי
   הרשימות נבדלות בשני מקומות **מדודים בלבד**, ו-`test_lists.mjs`
   אוכף בדיוק אותם: `swcore` נעדר כאן (הוא ב-`sw.js`, וקובץ שאינו
   `index.html` אינו נסרק כאן כלל), ו-«מידע טכני» נעדר משם (הוא נאכף
   ב-`check-status-area.mjs`, ואין לו שורה במטריצת היכולות). */

/* פורמט הכותרת — רוחב קבוע, כך שארבע האפליקציות נראות אותו דבר לעין. */
const RULE_W   = 74;
const HDR_OPEN = new RegExp('^/\\* ═{' + RULE_W + '}$');
const HDR_RULE = new RegExp('^ {3}═{' + RULE_W + '}$');
const HDR_END  = new RegExp('^ {3}═{' + RULE_W + '} \\*/$');
/* כותרת «כמעט תקנית» — נתפסת ומדווחת במקום להיספר כטקסט רגיל. */
const HDR_NEAR = /^\/\* ═{20,}\s*$/;
const MAX_AREA_LINES = 400;
/* רוחב באנר ה-`───────────────────────────────` בקובצי השערים — ר' סעיף ה. */
const BANNER_W = 78;

const MARKERS = ['⛔', '⚠️', '⭐'];
const PICTO = /^\p{Extended_Pictographic}/u;

/* ⚠️ רק גרסאות שאינן יכולות להיות מזהה בקוד — כדי שלא ניפול על
   `` `lsSweep` `` או על שם משתנה בתוך גרשיים אחוריים. */
const TERMS = [
  { bad: /דחיפת מצב/g,   good: 'דחיפת-מצב' },
  { bad: /טביעת אצבע/g,  good: 'טביעה' },
  { bad: /אופק פינוי/g,  good: 'אופק הפינוי' },
  { bad: /קאש/g,         good: 'מטמון' },
  { bad: /מירור/g,       good: 'מראה' },
  { bad: /אוף-ליין/g,    good: 'אופליין' },
  { bad: /טומבסטון/g,    good: 'tombstone' },
];

let failures = 0;
const fail = (m) => { failures++; console.error('❌ ' + m); };
const pass = (m) => console.log('✅ ' + m);
/*  ⛔ אזהרה שאינה מפילה (סבב 72) — ⚠️ תקן שרוב הקבצים נכתבו לפניו
 *  חוסם כל דחיפה אם הוא מפיל, ⛔ והמספר הוא מה שיאמר מתי לסגור. */
const warn = (m) => console.warn('⚠️ ' + m);

const src = fs.readFileSync(APP.file, 'utf8');

/* ── בניית «הקוד הפרטי» — אותו קובץ, כשכל מה שאינו JS פרטי מולבן ───────── */
/* ⚠️ מלבינים ולא חותכים: מספרי השורות בהודעות השגיאה חייבים להתאים לקובץ
   האמיתי, אחרת ההודעה שולחת את הקורא לשורה הלא נכונה. */
function blank(text, from, to) {
  const seg = text.slice(from, to).replace(/[^\n]/g, ' ');
  return text.slice(0, from) + seg + text.slice(to);
}

let priv = src;
{
  /* א. הכל מחוץ ל-<script> מוטבע */
  const re = /<script(?![^>]*\bsrc=)[^>]*>([\s\S]*?)<\/script>/g;
  const keep = [];
  let m;
  while ((m = re.exec(src))) keep.push([m.index + m[0].indexOf('>') + 1, m.index + m[0].length - '</script>'.length]);
  let out = src;
  let cur = 0;
  for (const [a, b] of keep) { out = blank(out, cur, a); cur = b; }
  out = blank(out, cur, out.length);
  priv = out;

  /* ב. חמשת הבלוקים המשותפים */
  for (const [start, end] of SHARED_BLOCKS) {
    /*  ⛔ בלוק שהוכרז נעדר כאן (סבב 52) — הנימוק יושב ב-`APP`, ואין מה
     *  להחריג מהסריקה. */
    if ((APP.skipBlocks || []).indexOf(start) >= 0) continue;
    const i0 = priv.indexOf(start);
    if (i0 < 0) { fail(`סמן הבלוק המשותף «${start}» לא נמצא ב-${APP.file}`); continue; }
    const open = priv.lastIndexOf('/*', i0);
    const j = priv.indexOf(end, i0);
    const k = j < 0 ? -1 : priv.indexOf('*/', j);
    if (open < 0 || j < 0 || k < 0) { fail(`הבלוק המשותף «${start}» אינו סגור ב-${APP.file}`); continue; }
    priv = blank(priv, open, k + 2);
  }
}

/* ── טוקניזציה: איסוף ההערות בלבד ──────────────────────────────────────── */
/* ⛔ אין להחליף את זה בביטוי רגולרי על `//` ו-`/*` (סבב 28) — מחרוזת שמכילה `//`
   (כל URL) הייתה נקראת כהערה, ו-regex שמכיל `*` היה פותח בלוק שלא נסגר. */
function collectComments(text) {
  const out = [];
  const n = text.length;
  let i = 0, line = 1;
  let prevSignificant = '';
  const bump = (s) => { for (let k = 0; k < s.length; k++) if (s[k] === '\n') line++; };

  while (i < n) {
    const c = text[i];
    if (c === '\n') { line++; i++; continue; }

    if (c === '/' && text[i + 1] === '/') {
      let j = text.indexOf('\n', i);
      if (j < 0) j = n;
      out.push({ line, endLine: line, kind: 'line', text: text.slice(i, j),
                 own: /(^|\n)[ \t]*$/.test(text.slice(Math.max(0, i - 200), i)) });
      i = j;
      continue;
    }
    if (c === '/' && text[i + 1] === '*') {
      let j = text.indexOf('*/', i + 2);
      if (j < 0) j = n; else j += 2;
      const body = text.slice(i, j);
      const startLine = line;
      bump(body);
      out.push({ line: startLine, endLine: line, kind: 'block', text: body, own: true });
      i = j;
      continue;
    }
    if (c === '"' || c === "'" || c === '`') {
      const q = c; i++;
      while (i < n) {
        if (text[i] === '\\') { if (text[i] === '\n') line++; i += 2; continue; }
        if (text[i] === '\n') { line++; if (q !== '`') break; }
        if (text[i] === q) { i++; break; }
        i++;
      }
      prevSignificant = q;
      continue;
    }
    if (c === '/') {
      /* regex או חילוק — מוכרע לפי הטוקן המשמעותי הקודם */
      if (/[({[,;:=!&|?+\-*%~^<>]/.test(prevSignificant) || prevSignificant === '') {
        i++;
        let cls = false;
        while (i < n) {
          const d = text[i];
          if (d === '\\') { i += 2; continue; }
          if (d === '\n') { line++; break; }
          if (d === '[') cls = true;
          else if (d === ']') cls = false;
          else if (d === '/' && !cls) { i++; break; }
          i++;
        }
        prevSignificant = '/';
        continue;
      }
      i++; prevSignificant = '/'; continue;
    }
    if (!/\s/.test(c)) prevSignificant = c;
    i++;
  }
  return out;
}

/* איחוד רצף שורות `//` עצמאיות לבלוק אחד — הערה בת חמש שורות היא רעיון
   אחד, והדרישה למספר סבב חלה עליו ולא על כל שורה בנפרד. */
function blocksOf(text) {
  const out = [];
  for (const c of collectComments(text)) {
    const last = out[out.length - 1];
    if (c.kind === 'line' && c.own && last && last.kind === 'line' && last.own && last.endLine === c.line - 1) {
      last.endLine = c.endLine;
      last.text += '\n' + c.text;
      continue;
    }
    out.push({ ...c });
  }
  return out;
}

const blocks = blocksOf(priv);

/*  ⭐ סבב 65 — תחולת התקן הורחבה ל-`sw.js` ול-`tools/` (ממצא 13). ⚠️ עד
 *  כאן היא נעצרה ב-`index.html`, ולכן 185 בלוקי ההערה שבשערים עצמם לא
 *  נבדקו כלל — ⛔ כלומר התקן שנאכף על הקוד לא נאכף על מי שאוכף אותו.
 *  ⛔ **וסעיף א (כותרת בלוק וכיסוי) נשאר ב-`index.html` בלבד, וזו הגבלה
 *  מוצהרת ולא השמטה (סבב 65)** — מדד הכיסוי הוא «רצף קוד פרטי בלי כותרת»,
 *  והוא מדבר על אפליקציה בת אלפי שורות; קובץ שער בן 150 שורות עם כותרת
 *  אחת היה נכשל על כלום, וכפייה של מסגרת 74 על באנרי ה-`───────` הייתה מחליפה
 *  תקן קיים ומדוד בתקן שהומצא כאן. סעיפים ב–ד חלים במלואם, וסעיף ה נכתב
 *  במיוחד לצורת הבאנר שבשערים.                                            */
const EXTRA = [];
/*  ⭐ שורת הכותרת של כל קובץ (סבב 67) — ⛔ שתים-עשרה השורות הראשונות
 *  בלבד: זהו הבאנר, ומספר הסבב שיושב בו הוא מה שהחליף את מספר הסבב
 *  שהיה בשם הקובץ. ⚠️ סריקת הקובץ כולו הייתה מוצאת «סבב 12» בהערה
 *  כלשהי באמצעו והופכת את ההקלה לאוטומטית. */
const HEADERS = {};
{
  const add = (p) => { try { const t = fs.readFileSync(p, 'utf8');
    HEADERS[p] = t.split('\n').slice(0, 12).join('\n');
    EXTRA.push({ name: p, blocks: blocksOf(t), lines: t.split('\n') }); } catch (e) {} };
  add('sw.js');
  try {
    for (const f of fs.readdirSync('tools').sort()) if (f.endsWith('.mjs')) add('tools/' + f);
  } catch (e) {}
}
const SCOPE = [{ name: APP.file, blocks, lines: src.split('\n') }, ...EXTRA];

/* ── א. כותרות בלוק — צורה וכיסוי ──────────────────────────────────────── */
{
  const lines = priv.split('\n');
  const hdrs = [];
  let bad = 0;
  for (let i = 0; i < lines.length; i++) {
    const l = lines[i];
    if (HDR_OPEN.test(l)) {
      const title = lines[i + 1] || '';
      const rule  = lines[i + 2] || '';
      if (!/^ {3}\S/.test(title) || title.length > 78) {
        fail(`שורה ${i + 2}: כותרת בלוק — שורת השם אינה תקנית (שלושה רווחים + שם, עד 78 תווים)`); bad++;
      } else if (!HDR_RULE.test(rule) && !HDR_END.test(rule)) {
        fail(`שורה ${i + 3}: כותרת בלוק — חסרה שורת מסגרת תחתונה ברוחב ${RULE_W}`); bad++;
      } else {
        hdrs.push({ line: i + 1, title: title.trim() });
      }
      continue;
    }
    if (HDR_NEAR.test(l) && !HDR_OPEN.test(l)) {
      fail(`שורה ${i + 1}: מסגרת כותרת ברוחב לא תקני — נדרשים בדיוק ${RULE_W} תווי ═`); bad++;
    }
  }

  /* טווח הקוד הפרטי — השורה הראשונה והאחרונה שאינן ריקות אחרי ההלבנה */
  let first = -1, last = -1;
  for (let i = 0; i < lines.length; i++) if (lines[i].trim()) { if (first < 0) first = i; last = i; }

  if (hdrs.length === 0) {
    fail('אין באזור הפרטי אף כותרת בלוק תקנית');
  } else {
    /* ⚠️ הכיסוי נמדד כמרווח בין כותרות ולא כ«כותרת לכל פונקציה»: אזור לוגי
       הוא רעיון, לא הצהרה. רצף ארוך בלי כותרת הוא בדיוק מה שנמדד בסבב 28. */
    /* ⚠️ המרווח נספר ב**שורות לא-ריקות של קוד פרטי** ולא במספרי שורות:
       בין שני אזורים יושבים HTML ו-CSS שהולבנו, וספירה גולמית הייתה
       מדווחת «1700 שורות בלי כותרת» על אזור שאין בו קוד בכלל. */
    const nonBlank = new Array(lines.length + 1).fill(0);
    for (let i = 0; i < lines.length; i++) nonBlank[i + 1] = nonBlank[i] + (lines[i].trim() ? 1 : 0);
    const marks = [first + 1, ...hdrs.map(h => h.line), last + 1];
    for (let k = 1; k < marks.length; k++) {
      const gap = nonBlank[marks[k]] - nonBlank[marks[k - 1]];
      if (gap > MAX_AREA_LINES) {
        fail(`שורות ${marks[k - 1]}–${marks[k]}: ${gap} שורות קוד פרטי בלי כותרת בלוק (המותר ${MAX_AREA_LINES})`);
        bad++;
      }
    }
  }
  if (!bad && hdrs.length) pass(`כותרות בלוק: ${hdrs.length} אזורים, כולם בפורמט התקני והכיסוי מלא`);
}

/* ── ב. ⛔ עם סיבה (סבב 28) ─────────────────────────────────────────────── */
{
  let bad = 0, seen = 0;
  for (const { name, blocks: bs } of SCOPE) for (const b of bs) {
    if (b.text.indexOf('⛔') < 0) continue;
    seen++;
    /*  ⛔ מספר הסבב אינו נדרש עוד (סבב 72) — ⚠️ הערה שמסבירה **למה** אינה
     *  נעשית מובנת יותר בזכות מספר, ⛔ והדרישה הכפילה את «תוכן ההערות».
     *  ⭐ מה שנשאר הוא ה**סיבה**: היא מה שמונע מהסשן הבא לבטל את האיסור
     *  בתום לב, ⛔ ואותה שום מספר אינו נותן. */
    /* ⚠️ מפריד « — » בסוף שורה תקף בדיוק כמו באמצעה: הערה בת כמה שורות
       שמות הנימוק בשורה הבאה היא ניסוח לגיטימי, לא היעדר נימוק. */
    /*  ⭐ סבב 65 — ⛔ ששבוי בשורת **כותרת** (באנר `─`/`═`) אינו איסור אלא
     *  **שם** של הטענה שמתחתיה, והנימוק יושב בגוף. ⚠️ ההקלה מוגבלת לבלוק
     *  שכל ה-⛔ שבו יושבים בשורות כותרת: ⛔ אחד בגוף מחזיר את הדרישה. */
    const bodyStop = b.text.split('\n').filter(x => x.indexOf('⛔') >= 0 && !/[─═]{2,}/.test(x));
    const hasWhy   = / —\s/.test(b.text) || bodyStop.length === 0;
    if (!hasWhy) {
      fail(`${name}:${b.line}: הערת ⛔ בלי סיבה אחרי « — »`);
      bad++;
    }
  }
  if (!bad) pass(`הערות ⛔: ${seen} בלוקים, כולם עם סיבה`);
}

/* ── ג. סימנים — שלושה בלבד ────────────────────────────────────────────── */
{
  let bad = 0;
  for (const { name, blocks: bs } of SCOPE) for (const b of bs) {
    const ls = b.text.split('\n');
    for (let k = 0; k < ls.length; k++) {
      let t = ls[k];
      t = t.replace(/^\s*/, '').replace(/^\/\*+/, '').replace(/^\/\//, '').replace(/^\*+/, '').replace(/^\s*/, '');
      if (!t) continue;
      if (!PICTO.test(t)) continue;
      const ok = MARKERS.some(m => t.startsWith(m));
      if (!ok) {
        fail(`${name}:${b.line + k}: שורת הערה נפתחת בסמל שאינו ⛔/⚠️/⭐ — «${t.slice(0, 24)}»`);
        bad++;
      }
    }
  }
  if (!bad) pass('סימנים: כל שורת הערה שנפתחת בסמל משתמשת ב-⛔ / ⚠️ / ⭐ בלבד');
}

/* ── ד. טרמינולוגיה ────────────────────────────────────────────────────── */
{
  let bad = 0;
  for (const { name, blocks: bs } of SCOPE) for (const b of bs) {
    for (const t of TERMS) {
      t.bad.lastIndex = 0;
      if (t.bad.test(b.text)) {
        fail(`${name}:${b.line}: מונח לא תקני «${String(t.bad).replace(/[/g]/g, '')}» — הכתיב התקני הוא «${t.good}»`);
        bad++;
      }
    }
  }
  if (!bad) pass(`טרמינולוגיה: ${TERMS.length} מונחים נבדקו, כולם בכתיב התקני`);
}

/* ── ה. רוחב באנר ה-`──` בשערים ────────────────────────────────────────── */
/*  ⭐ סבב 65 — הבאנר היה תקן שני לא-מוצהר: 242 שורות ב-44 מתוך 45 קובצי
 *  `tools/`, ברוחב כולל 62–80. ⛔ תקן שלא נמדד נסחף, ולכן הרוחב נעול
 *  ל-`BANNER_W` — הערך שהיה כבר ברוב המכריע של השורות (165 מתוך 249).   */
{
  let bad = 0, seen = 0;
  for (const { name, blocks: bs, lines: srcLines } of SCOPE) {
    if (name === APP.file) continue;          /* ⚠️ שם התקן הוא מסגרת ה-`═` של סעיף א */
    for (const b of bs) {
      const ls = b.text.split('\n');
      for (let k = 0; k < ls.length; k++) {
        if (!/─{2,}/.test(ls[k])) continue;
        seen++;
        /*  ⚠️ נמדדת **שורת המקור** ולא פרוסת ההערה (סבב 65) — טקסט הבלוק
         *  מתחיל ב-`/*` ומשמיט את ההזחה שלפניו, ולכן מדידה עליו הייתה
         *  מדווחת 76 על שורה שרוחבה 78. */
        const full = srcLines[b.line - 1 + k];
        if (full === undefined || full.length === BANNER_W) continue;
        fail(`${name}:${b.line + k}: באנר «─» ברוחב ${full.length} במקום ${BANNER_W}`);
        bad++;
      }
    }
  }
  if (!bad) pass(`באנרי «─»: ${seen} שורות, כולן ברוחב ${BANNER_W}`);
}

if (failures) {
  console.error(`\n❌ ${APP.app}: ${failures} סטיות מתקן ההערות (כלל ברזל 11)`);
  process.exit(1);
}
/* ───────────────────────────────────────────────────────────────────────────
   ו · ⛔ כל מבחן נכנס עם מוטציה שרצה — או עם נימוק כתוב (סבב 72)
   ───────────────────────────────────────────────────────────────────────────
   ⛔ מה נאכף: ב**קוד** של `tools/test_*.mjs` — ולא בהערותיו — יש פעולת
   שינוי על מקור, ⛔ ותווית מוטציה שנכתבת לפלט בזמן ריצה. ⛔ או נימוק
   מפורש מדוע אינו ניתן למוטציה.
   ⛔ ולמה זה יכול להישבר: עד סבב 72 הבדיקה הייתה `indexOf('מוטציה')` על
   הקובץ **כולו**, ⚠️ כלומר אזכור בהערה נספר כמוטציה — בדיוק «השם קיים
   בקובץ» שכלל ה-probe אוסר. ⭐ נמדד: בלוק המוטציות של שער ה-`sw` ישב
   אחרי `process.exit` ולא רץ מעולם, והבדיקה דיווחה «יש מוטציה».
   ⚠️ מה שאינו נאכף כאן: **שהמוטציה אכן הפילה** — ⛔ זה נאכף במבחן עצמו,
   וכאן נמדד ומדווח כמה מהן נוקבות בשם הטענה שאמורה ליפול.
   ──────────────────────────────────────────────────────────────────────── */
{
  /*  ⛔ ההערות מולבנות לפני המדידה (סבב 72) — ⚠️ אזכור «מוטציה» בהערה
   *  אינו מוטציה, וזה בדיוק מה שהבדיקה הישנה ספרה. */
  const codeOf = (t) => t.replace(/\/\*[\s\S]*?\*\//g, ' ')
                         .replace(/(^|[^:])\/\/[^\n]*/g, '$1 ');
  /*  שינוי על מקור — ⛔ ולא כל כתיבה (סבב 72) — אלה ארבע הפעולות
   *  שמייצרות מוטנט. */
  const CHANGE = /\.replace\s*\(|appendFileSync\s*\(|writeFileSync\s*\(|splice\s*\(/;
  /*  תווית שנכתבת לפלט — ⭐ היא העדות שהמוטציה **רצה** ודיווחה על עצמה.
   *  ⛔ אין לקרוא אותה מההערות (סבב 72) — הערה אינה רצה. `מ1 ·`/`נ1 ·`
   *  הן צורת התווית הקצרה. */
  const LABEL = new RegExp("(['\"`])[^'\"`\\n]*?(מוטציה|מוטנט|מוטציית|מוטציות|[מנ]\\d+ ·)[^'\"`\\n]*?\\1");
  const ANTI  = new RegExp("(['\"`])[^'\"`\\n]*?(מוטציית-נגד|נ\\d+ ·)[^'\"`\\n]*?\\1");
  /*  שם הטענה שאמורה ליפול — ⛔ מוטציה בלי שם טענה אינה אכיפה (סבב 72) —
   *  היא עוברת גם כשהיא מפילה טענה **אחרת**. ⚠️ נמדד ומדווח,
   *  ⛔ ואינו מפיל עדיין. */
  const EXPECT = /expectFail|wantFail|shouldDiffer|\bhits\s*:/;

  let bad = 0, seen = 0, noAnti = 0, noExpect = 0, excused = 0;
  let names = [];
  try { names = fs.readdirSync('tools').filter((f) => /^test_.*\.mjs$/.test(f)); } catch (e) {}
  if (!names.length) fail('רשימת השערים ריקה — נמדדו 0 קבצים והצפוי לפחות אחד. '
     + '⛔ הטענה שמתחת רצה על רשימה ריקה ומדווחת «עבר»');
  for (const f of names) {
    let t = '';
    try { t = fs.readFileSync('tools/' + f, 'utf8'); } catch (e) { continue; }
    /*  ⚠️ נימוק מפורש הוא מסלול חוקי — ⛔ אך הוא נקרא בעין ואינו
     *  מדלג בשתיקה (סבב 67) — «אין כאן מה למוטט» חייב להיאמר. */
    if (/אינו ניתן למוטציה/.test(t)) { excused++; continue; }
    seen++;
    const code = codeOf(t);
    if (!CHANGE.test(code)) {
      bad++;
      fail(`tools/${f}: אין פעולת שינוי בקוד — נמדד 0 מתוך 1 ` +
           '(`.replace` · `writeFileSync` · `appendFileSync` · `splice`). ' +
           'מוסיפים מוטציה, או שורת נימוק «אינו ניתן למוטציה»');
    } else if (!LABEL.test(code)) {
      bad++;
      fail(`tools/${f}: יש שינוי ואין תווית מוטציה מודפסת — נמדד 0 תוויות ` +
           'מתוך 1 נדרשת. מוטציה שאינה מדווחת על עצמה אינה נראית כשהיא ' +
           'אינה רצה; מוסיפים תווית לקריאת הדיווח');
    }
    if (!ANTI.test(code)) noAnti++;
    if (!EXPECT.test(code)) noExpect++;
  }
  /*  ⛔ שתי המדידות מדווחות ⛔ ואינן מפילות עדיין (סבב 67, ומורחב 72) —
   *  ⚠️ רוב המבחנים נכתבו לפני שתי הדרישות, והפלה רטרואקטיבית הייתה
   *  חוסמת כל דחיפה עד שייכתבו מחדש. ⛔ מבחן **חדש** נכתב עם שתיהן,
   *  ⛔ והמספרים כאן הם מה שיאמר מתי הסגירה נדרשת. */
  /*  ⛔ probe שאינו יכול להיכשל נחשב כלא-קיים (סבב 72) — ⚠️ הנימוק נמדד:
   *  `\b` לפני אות עברית **לעולם** אינו תואם ב-JS, מפני ש-`\w` הוא
   *  `[A-Za-z0-9_]` בלבד ואין גבול-מילה בין שני תווים שאינם מילה.
   *  ⛔ ה-probe הראשון של «שורה N» «עבר» בלי לבדוק דבר, ⭐ והמוטציה היא
   *  שחשפה — ⛔ ולכן הצורה הזו מפילה כאן, ואינה מדווחת בלבד. */
  const DEAD_B = /\\b(?=[^\x00-\x7F])|(?<=[^\x00-\x7F])\\b/;
  let dead = 0;
  for (const f of fs.readdirSync('tools')) {
    if (!/\.mjs$/.test(f) || f === 'check-comments.mjs') continue;
    let t = '';
    try { t = fs.readFileSync('tools/' + f, 'utf8'); } catch (e) { continue; }
    for (const m of codeOf(t).split('\n')) {
      if (!DEAD_B.test(m)) continue;
      dead++;
      fail(`tools/${f}: \`\\b\` צמוד לתו לא-לטיני — נמדד גבול-מילה שלעולם ` +
           'אינו תואם והצפוי ביטוי שיכול להיכשל. מסירים את `\\b`');
    }
  }
  if (dead) bad += dead;

  /*  ⛔ ושדה שאינו מוגדר ב-`APP` הוא אותו כשל בדיוק (סבב 72) — ⚠️ הנימוק
   *  נמדד: טענה חדשה השוותה מול `APP.bg` שאינו קיים בשער הזה, ⭐ התנאי היה
   *  `undefined` והטענה «עברה» בלי לרוץ. ⛔ שדה שאינו רלוונטי לאפליקציה
   *  מוצהר ריק ⛔ ואינו נשמט — ⚠️ שדה חסר נקרא «לא נשאל», וריק נקרא
   *  «נמדד ואין». */
  const noStr = (t) => t.replace(/`(?:\\.|[^`\\])*`|'(?:\\.|[^'\\\n])*'|"(?:\\.|[^"\\\n])*"/g, "''");
  const appKeys = (code) => {
    const m = /const APP\s*=\s*\{/.exec(code);
    if (!m) return null;
    let i = m.index + m[0].length - 1, depth = 0, end = -1;
    for (let k = i; k < code.length; k++) {
      if (code[k] === '{') depth++;
      else if (code[k] === '}' && !--depth) { end = k; break; }
    }
    if (end < 0) return null;
    const keys = new Set();
    let d = 0;
    for (const mm of code.slice(i + 1, end).matchAll(/[{}]|([A-Za-z_$][\w$]*)\s*:/g)) {
      if (mm[0] === '{') d++;
      else if (mm[0] === '}') d--;
      else if (!d && mm[1]) keys.add(mm[1]);
    }
    return keys;
  };
  let ghost = 0, scanned = 0;
  for (const f of fs.readdirSync('tools')) {
    if (!/\.mjs$/.test(f)) continue;
    let t = '';
    try { t = fs.readFileSync('tools/' + f, 'utf8'); } catch (e) { continue; }
    const code = noStr(codeOf(t));
    const keys = appKeys(code);
    if (!keys) continue;
    scanned++;
    const used = new Set([...code.matchAll(/\bAPP\.([A-Za-z_$][\w$]*)/g)].map((m) => m[1]));
    for (const k of used) {
      if (keys.has(k)) continue;
      ghost++;
      fail(`tools/${f}: \`APP.${k}\` אינו מוגדר בבלוק APP — נמדד שדה אחד חסר ` +
           'והצפוי אפס. מצהירים אותו, ריק אם אינו רלוונטי לאפליקציה');
    }
  }
  if (ghost) bad += ghost;

  /*  ⛔ ושתי צורות נוספות שאינן יכולות להיכשל (סבב 72) — ⭐ אותה מחלה:
   *  ⚠️ `catch {}` שבולע את **האיסוף** משאיר רשימה ריקה, הלולאה שמתחתיו
   *  אינה רצה, ⛔ והשער מדפיס «עבר»; ⚠️ ו-`every` על מערך ריק מחזיר
   *  `true` בהגדרה — ⛔ טענה על «כל האיברים» של אוסף שלא נבנה. */
  const VAC = [
    { name: 'catch שבולע איסוף',
      re: /(?:const|let|var)?\s*(\w+)\s*=\s*(?:await\s*)?fs\.readdirSync[^\n]*?\}\s*catch\s*(?:\([^)]*\))?\s*\{\s*\}/g,
      ok: (code, m, v) => new RegExp(`\\b${v}\\.length`).test(code.slice(m.index, m.index + 700)),
      why: 'רשימה שנבלעה נשארת ריקה — ⛔ מוסיפים כשל על אורך אפס' },
    { name: 'every על מערך שעלול להיות ריק',
      re: /([A-Za-z_$][\w$.]*)\.every\s*\(/g,
      /*  ⚠️ החלון הוא ה**פסוקית** ולא השורה (סבב 72) — ⛔ שומר `.length`
       *  שנכתב בשורה שמעליה הוא אותו שומר. ⭐ ו-`fail` בפסוקית פוטר:
       *  שם מערך ריק **מפיל**, ⛔ וזו בדיוק ההתנהגות הנדרשת. */
      ok: (code, m, v) => {
        if (/^[A-Z][A-Z0-9_]*$/.test(v.split('.')[0])) return true;
        const a = Math.max(code.lastIndexOf(';', m.index), code.lastIndexOf('{', m.index)) + 1;
        const e = code.indexOf(';', m.index);
        const st = code.slice(a, e < 0 ? code.length : e);
        return /\.length/.test(st) || /\bfail\s*\(/.test(st);
      },
      why: '`[].every()` הוא `true` — ⛔ מוסיפים שומר `.length` באותה טענה' },
  ];
  let vac = 0;
  for (const f of fs.readdirSync('tools')) {
    if (!/\.mjs$/.test(f) || f === 'check-comments.mjs') continue;
    let t = '';
    try { t = fs.readFileSync('tools/' + f, 'utf8'); } catch (e) { continue; }
    const code = noStr(codeOf(t));
    for (const p of VAC)
      for (const m of code.matchAll(p.re)) {
        if (p.ok(code, m, m[1])) continue;
        vac++;
        fail(`tools/${f}: ${p.name} — נמדדה טענה שאינה יכולה להיכשל והצפוי אפס. ${p.why}`);
      }
  }
  if (vac) bad += vac;

  else pass(`שדות APP: ${scanned} שערים, ⛔ ואין טענה שמשווה מול שדה שאינו מוגדר`);

  if (!bad) pass(`מוטציות: ${seen} מבחנים (${excused} מנומקים), לכל אחד שינוי ותווית` +
                 ` (⚠️ ${noAnti} בלי מוטציית-נגד · ${noExpect} בלי שם הטענה שתיפול)`);
}

/* ───────────────────────────────────────────────────────────────────────────
   ז · ⛔ תקן הבאנר — ארבע פסקאות בסדר קבוע (סבב 72)
   ───────────────────────────────────────────────────────────────────────────
   ⛔ מה נאכף: הבאנר שבראש `tools/test_*.mjs` נושא ארבע פסקאות מסומנות,
   ⛔ בסדר הזה: **מה נאכף** · **הנימוק המדוד** · **מה יישבר בלעדיו** ·
   **מה אינו נאכף כאן**.
   ⛔ ולמה זה יכול להישבר: באנר שאומר «מה» ולא «למה» מלמד את הקורא לדלג,
   ⚠️ ואז הוא מדלג גם על ה«מה אינו נאכף» — ⛔ ומייחס לשער כיסוי שאין לו.
   ⭐ נמדד בסבב 72: 28 מ-172 נשאו «מה נאכף», ⛔ ו-12 בלבד «מה יישבר».
   ⚠️ המדידה **מדווחת ואינה מפילה** — ⛔ סגירת הקיימים היא סבב ייעודי,
   ⛔ ומבחן **חדש** נכתב עם ארבעתן.
   ──────────────────────────────────────────────────────────────────────── */
{
  const PARTS = ['מה נאכף', 'הנימוק המדוד', 'מה יישבר בלעדיו', 'מה אינו נאכף כאן'];
  let seen = 0, full = 0, partial = 0;
  let names = [];
  try { names = fs.readdirSync('tools').filter((f) => /^test_.*\.mjs$/.test(f)); } catch (e) {}
  if (!names.length) fail('רשימת השערים ריקה — נמדדו 0 קבצים והצפוי לפחות אחד. '
     + '⛔ הטענה שמתחת רצה על רשימה ריקה ומדווחת «עבר»');
  for (const f of names) {
    let t = '';
    try { t = fs.readFileSync('tools/' + f, 'utf8'); } catch (e) { continue; }
    seen++;
    /*  ⚠️ הבאנר הוא בלוק ההערה הראשון בלבד (סבב 72) — ⛔ סריקה על הקובץ
     *  כולו הייתה מוצאת את הפסקאות בכל הערה פנימית. */
    const end = t.indexOf('*/');
    const head = end < 0 ? '' : t.slice(0, end);
    let at = -1, got = 0;
    for (const p of PARTS) {
      const i = head.indexOf('**' + p + ':**');
      if (i > at) { at = i; got++; } else break;
    }
    if (got === PARTS.length) full++;
    else if (got) partial++;
  }
  if (full === seen) pass(`תקן הבאנר: ${seen} מבחנים, לכל אחד ארבע הפסקאות בסדר`);
  else warn(`תקן הבאנר: ${full}/${seen} מבחנים נושאים את ארבע הפסקאות בסדר ` +
            `(${partial} חלקיים) — הצפוי ${seen}/${seen}. מבחן חדש נכתב עם ארבעתן`);
}

/* ───────────────────────────────────────────────────────────────────────────
   ח · ⛔ דפוס ששת הבודקים והודעת הכשל (סבב 72)
   ───────────────────────────────────────────────────────────────────────────
   ⛔ מה נאכף: ששת הבודקים מחזיקים את **אותם עוזרים** — `failures` · `fail`
   · `pass` — אות באות. ⛔ ולצידם נמדדת הודעת הכשל: **מיקום · נמדד מול
   הצפוי · מה עושים**.
   ⛔ ולמה זה יכול להישבר: `check-js` החזיק דפוס שני (`ok`/`FAIL`, בלי
   מונה ובלי `pass`), ⚠️ ושני דפוסים לאותו דבר מלמדים לקרוא כל בודק
   מחדש. ⭐ ונמדד בסבב 72: 13 מ-117 קריאות `fail` נשאו ערך נמדד,
   ⛔ ו-10 בלבד אמרו מה לעשות.
   ⚠️ תוכן ההודעה **מדווח ואינו מפיל** — ⛔ סגירתו היא סבב ייעודי;
   ⛔ העוזרים עצמם כן מפילים.
   ──────────────────────────────────────────────────────────────────────── */
{
  const CHECKERS = ['check-js', 'check-structure', 'check-status-area',
                    'check-docs', 'check-comments', 'check-capabilities'];
  const HELPERS = ["let failures = 0;",
                   "const fail = (m) => { failures++; console.error('❌ ' + m); };",
                   "const pass = (m) => console.log('✅ ' + m);"];
  const VALUE  = /נמדד|במקום|הצפוי|מתוך/;
  const ACTION = /מוסיפים|גוזמים|יש ל|מעדכנים|עדכון|מסירים|מוחקים|לקדם|מתקנים|מיישרים|בודקים/;
  let bad = 0, sites = 0, withValue = 0, withAction = 0;
  for (const c of CHECKERS) {
    let t = '';
    try { t = fs.readFileSync(`tools/${c}.mjs`, 'utf8'); } catch (e) {
      fail(`tools/${c}.mjs: הבודק חסר — נמדדו ${CHECKERS.length - 1} מתוך ${CHECKERS.length}`);
      bad++; continue;
    }
    const miss = HELPERS.filter((h) => !t.includes(h));
    if (miss.length) {
      fail(`tools/${c}.mjs: חורג מדפוס העוזרים — נמדדו ${HELPERS.length - miss.length} ` +
           `מתוך ${HELPERS.length}. מיישרים ל-failures/fail/pass של חמשת האחרים`);
      bad++;
    }
    /*  ⚠️ נספרות קריאות `fail(` בגוף הבודק — ⛔ ולא הגדרתה (סבב 72) —
     *  ההגדרה היא שורה אחת בכל בודק, והיא הייתה מנפחת את המונה. */
    for (const m of t.matchAll(/\bfail\(([\s\S]*?)\);\n/g)) {
      sites++;
      if (VALUE.test(m[1])) withValue++;
      if (ACTION.test(m[1])) withAction++;
    }
  }
  if (!bad) pass(`דפוס הבודקים: ${CHECKERS.length} בודקים, עוזרים זהים ` +
                 `(⚠️ הודעת כשל: ${withValue}/${sites} עם ערך נמדד · ${withAction}/${sites} עם מה לעשות)`);
}

/* ───────────────────────────────────────────────────────────────────────────
   ט · ⛔ מספר בבאנר נגזר מהקבוע שבקוד (סבב 72)
   ───────────────────────────────────────────────────────────────────────────
   ⛔ מה נאכף: מספר שמוגדר כקבוע בראש קובץ `tools/` ⛔ אינו נכתב שוב
   בבאנר שלו — ⭐ הבאנר נוקב **בשם הקבוע**.
   ⛔ ולמה זה יכול להישבר: שני מקומות לאותו ערך נסחפים. ⭐ נמדד בסבב 72:
   `check-structure` הצהיר «שבעת הבודקים» בעוד ש-`CHECKERS` מנה שישה,
   ו-`check-docs` הצהיר «שישה פרקי סבבים / 3,000 שורות» בעוד שהקוד אוכף
   `DOC_MAX_ROUNDS` ו-`DOC_MAX_LINES`.
   ⚠️ מה שאינו נאכף כאן: מספר בבאנר שאין לו קבוע — ⛔ אין למה להשוות אותו.
   ⚠️ ומספר סבב ומספר כלל ברזל מוחרגים: ⛔ הם מזהים ולא ערכים.
   ──────────────────────────────────────────────────────────────────────── */
{
  let bad = 0, seen = 0;
  let names = [];
  try { names = fs.readdirSync('tools').filter((f) => f.endsWith('.mjs')); } catch (e) {}
  if (!names.length) fail('רשימת השערים ריקה — נמדדו 0 קבצים והצפוי לפחות אחד. '
     + '⛔ הטענה שמתחת רצה על רשימה ריקה ומדווחת «עבר»');
  for (const f of names) {
    let t = '';
    try { t = fs.readFileSync('tools/' + f, 'utf8'); } catch (e) { continue; }
    const end = t.indexOf('*/');
    if (end < 0) continue;
    const head = t.slice(0, end);
    for (const m of t.matchAll(/^const ([A-Z][A-Z0-9_]*)\s*=\s*(\d{2,})\s*;/gm)) {
      seen++;
      const re = new RegExp('(?<!סבב )(?<!כלל ברזל )(?<![\\d.,])' + m[2] + '(?![\\d.,])');
      if (!re.test(head)) continue;
      bad++;
      fail(`tools/${f}: הבאנר נוקב ב-${m[2]} בעוד שהקבוע ` + '`' + m[1] + '`' +
           ` מחזיק אותו — נמדדו 2 מקומות לערך אחד והצפוי 1. ` +
           'מחליפים את המספר בבאנר בשם הקבוע');
    }
  }
  if (!bad) pass(`מספרי הבאנר: ${seen} קבועים, אף אחד אינו כתוב פעמיים`);
}

/* ───────────────────────────────────────────────────────────────────────────
   י · ⛔ בלוק ה-APP — הבלוק היחיד שנבדל בין הריפו (סבב 72)
   ───────────────────────────────────────────────────────────────────────────
   ⛔ מה נאכף: כל קובץ `tools/` שמגדיר `const APP` עוטף אותו בשני הסמנים
   הקנוניים — פותח וסוגר, אות באות — ⛔ והפותח קודם לסוגר.
   ⛔ ולמה זה יכול להישבר: הסמן הוא מה שמאפשר להעתיק שער בין ריפו בלי
   לדרוס את החלק הפרטי. ⭐ נמדד בסבב 72: שישה קבצים נשאו סמן בנוסח אחר
   ואחד לא נסגר כלל — ⚠️ והשורה בטבלה הייתה ✅.
   ⚠️ מה שאינו נאכף כאן: **שזה ההבדל היחיד** — ⛔ ההשוואה הזו היא בין
   ריפו, והשער רץ בתוך אחד. ⭐ מה שכן נמדד ומדווח הוא כמה בלוקים נושאים
   שם אפליקציה.
   ──────────────────────────────────────────────────────────────────────── */
{
  const OPEN  = '/* ── APP — הדבר היחיד שנבדל בין הריפו ──────────────────────────────────── */';
  const CLOSE = '/* ── סוף APP ───────────────────────────────────────────────────────────── */';
  let bad = 0, withApp = 0, named = 0;
  let names = [];
  try { names = fs.readdirSync('tools').filter((f) => f.endsWith('.mjs')); } catch (e) {}
  if (!names.length) fail('רשימת השערים ריקה — נמדדו 0 קבצים והצפוי לפחות אחד. '
     + '⛔ הטענה שמתחת רצה על רשימה ריקה ומדווחת «עבר»');
  for (const f of names) {
    let t = '';
    try { t = fs.readFileSync('tools/' + f, 'utf8'); } catch (e) { continue; }
    if (!/^const APP = /m.test(t)) continue;
    withApp++;
    const a = t.indexOf(OPEN), b = t.indexOf(CLOSE);
    if (a < 0 || b < 0 || b < a) {
      bad++;
      fail(`tools/${f}: בלוק APP בלי שני הסמנים הקנוניים — נמדדו ` +
           `${(a >= 0 ? 1 : 0) + (b >= 0 ? 1 : 0)} מתוך 2 בסדר הנכון. ` +
           'מעתיקים את שורות הסמן מקובץ שער אחר, אות באות');
      continue;
    }
    if (/app: '/.test(t.slice(a, b))) named++;
  }
  if (!bad) pass(`בלוק ה-APP: ${withApp} קבצים, כולם עם שני הסמנים ` +
                 `(⚠️ ${named} מהם נושאים שם אפליקציה)`);
}

/* ───────────────────────────────────────────────────────────────────────────
   יא · ⛔ שער אינו מריץ את check-js במלואו, ⛔ ומבחן מנוטרל נושא נימוק (סבב 72)
   ───────────────────────────────────────────────────────────────────────────
   ⛔ מה נאכף: (1) `tools/test_*.mjs` שמריץ את `check-js` עושה זאת עם
   `CHECKJS_STAGES_ONLY`, ⛔ או שהוא השער שמצהיר על קו הבסיס; (2) מבחן
   שבלוק ה-`APP` שלו מכריז היעדר (`present: false` · `usersTable: null`)
   נושא נימוק בבאנר.
   ⛔ ולמה זה יכול להישבר: שני הכללים נכתבו ואיש לא מדד אותם. ⭐ נמדד:
   שתי ריצות פנימיות של הסט המלא עלו 42 מ-80 שניות; ⚠️ ומבחן שקודו
   מנוטרל נראה כרשת ביטחון של מאות שורות שאינה בודקת דבר.
   ⚠️ מה שאינו נאכף כאן: **יחס שורות-לטענה** — ⛔ מונה הטענות נבנה בזמן
   ריצה (לולאות), ⚠️ ונמדד שספירה סטטית שוגה בסדר גודל: 623 שורות מול
   טענה אחת. ⭐ מה שכן ניתן למדוד הוא ההכרזה שבבלוק `APP`.
   ──────────────────────────────────────────────────────────────────────── */
{
  const codeOf = (t) => t.replace(/\/\*[\s\S]*?\*\//g, ' ')
                         .replace(/(^|[^:])\/\/[^\n]*/g, '$1 ');
  /*  ⭐ קו הבסיס מוכרז (סבב 72) — ⛔ שער אחד רשאי להריץ את הסט המלא,
   *  ⚠️ והוא זה שמודד שאף שער אינו כותב לעץ. */
  const BASELINE = 'test_readonly.mjs';
  const ABSENT = /present:\s*false|usersTable:\s*null/;
  const REASON = /מנוטרל|טענות-\*\*חסר\*\*|טענות-חסר|אין כאן/;
  let bad = 0, seen = 0, muted = 0;
  let names = [];
  try { names = fs.readdirSync('tools').filter((f) => /^test_.*\.mjs$/.test(f)); } catch (e) {}
  if (!names.length) fail('רשימת השערים ריקה — נמדדו 0 קבצים והצפוי לפחות אחד. '
     + '⛔ הטענה שמתחת רצה על רשימה ריקה ומדווחת «עבר»');
  for (const f of names) {
    let t = '';
    try { t = fs.readFileSync('tools/' + f, 'utf8'); } catch (e) { continue; }
    seen++;
    const code = codeOf(t);
    if (f !== BASELINE && /['"`]check-js\.mjs['"`]/.test(code) &&
        !/CHECKJS_STAGES_ONLY/.test(code)) {
      bad++;
      fail(`tools/${f}: מריץ את check-js המלא — נמדדה ריצה אחת בלי ` +
           '`CHECKJS_STAGES_ONLY` והצפוי אפס. מוסיפים את דגל השלבים');
    }
    const app = /\/\* ── APP —[\s\S]*?\/\* ── סוף APP/.exec(t);
    if (app && ABSENT.test(app[0])) {
      muted++;
      const head = t.slice(0, t.indexOf('*/'));
      if (!REASON.test(head)) {
        bad++;
        fail(`tools/${f}: בלוק APP מכריז היעדר והבאנר בלי נימוק — ` +
             'נמדדו 0 אזכורים מתוך 1 נדרש. מוסיפים לבאנר שורה שאומרת ' +
             'מה מנוטרל כאן ואיפה הטענות כן רצות');
      }
    }
  }
  if (!bad) pass(`ריצת check-js: ${seen} מבחנים, אחד מריץ את הסט המלא ` +
                 `(⚠️ ${muted} מכריזים היעדר, וכולם עם נימוק בבאנר)`);
}

/* ───────────────────────────────────────────────────────────────────────────
   יב · ⛔ קובץ תיעוד אינו מסביר כלל שהטבלה אוכפת (סבב 72)
   ───────────────────────────────────────────────────────────────────────────
   ⛔ מה נאכף: כותרת פרק בשלושת הקבצים הנלווים אינה חופפת לשם של שורה
   בטבלת התשתית. ⛔ ולמה זה יכול להישבר: הטבלה אומרת «WebView ולא TWA»,
   ⚠️ ו-`android/README` חזר על כך בפרק בן 12 שורות — ⭐ נמדד: 56 שורות
   ×4 שכפלו ארבע שורות בטבלה. ⛔ עותק שני של כלל נסחף מהראשון בשקט.
   ⚠️ מה שאינו נאכף כאן: **גוף** הפרק — ⛔ ההשוואה היא על הכותרת בלבד,
   ⭐ והיא זו שאומרת מה הפרק בא להסביר.
   ──────────────────────────────────────────────────────────────────────── */
{
  /*  ⚠️ שמות שהם **מונח תפעולי** ולא כלל — ⛔ פרק שנושא אותם אינו חופף
   *  (סבב 72): הוא הוראה מעשית ולא הסבר, ⭐ ולכן הם מוצהרים כרשימה. */
  const OPERATIONAL = ['Build', 'מה בפנים', 'אייקונים', 'מסכים', 'פיתוח', 'APK'];
  /*  ⛔ `CLAUDE.md` במפורש (סבב 72) — ⚠️ `APP.file` כאן הוא `index.html`,
   *  והטבלה אינה שם: קריאה ממנו החזירה רשימת שמות **ריקה**, ⛔ והשער
   *  עבר על כל חפיפה. ⭐ שער שקורא קובץ רשאי לנקוב בשמו. */
  const docLines = fs.readFileSync('CLAUDE.md', 'utf8').split('\n');
  const a = docLines.findIndex((l) => /^<!--\s*SHARED:start\s+id="rules-table"/.test(l));
  const b = docLines.findIndex((l, i) => i > a && /^<!--\s*SHARED:end/.test(l));
  const norm = (t) => t.replace(/[`*⛔⚠️⭐#|]/g, ' ').replace(/\s+/g, ' ').trim();
  const rowNames = [];
  for (const l of docLines.slice(a, b)) {
    const m = /^\|\s*\d+\s*\|([^|]+)\|/.exec(l);
    if (m) rowNames.push(norm(m[1]));
  }
  let bad = 0, seen = 0;
  for (const f of ['README.md', 'CONTEXT.md', 'android/README.md']) {
    if (!fs.existsSync(f)) continue;
    const ls = fs.readFileSync(f, 'utf8').split('\n');
    /*  ⚠️ כותרת בתוך גדר קוד אינה כותרת (סבב 72) — ⛔ דוגמה ב-`bash`
     *  שמתחילה ב-`##` הייתה נספרת כפרק. */
    let fence = false;
    for (let i = 0; i < ls.length; i++) {
      if (ls[i].startsWith('```')) { fence = !fence; continue; }
      if (fence || !/^#{2,3}\s/.test(ls[i])) continue;
      seen++;
      const h = norm(ls[i]);
      if (!h || OPERATIONAL.some((o) => h === norm(o))) continue;
      const hit = rowNames.find((n) => n.length > 6 && (h === n || h.includes(n)));
      if (!hit) continue;
      bad++;
      fail(`${f}:${i + 1}: הכותרת «${h}» חופפת לשורה «${hit}» בטבלת התשתית — ` +
           'נמדדה חפיפה אחת מתוך אפס מותרות. הטבלה אומרת את הכלל, ' +
           'והקובץ מחזיק את ההוראה המעשית בלבד');
    }
  }
  if (!bad) pass(`תוכן הקבצים הנלווים: ${seen} כותרות, אף אחת אינה חוזרת על שורה בטבלה`);
}

/* ───────────────────────────────────────────────────────────────────────────
   יג · ⛔ שפת ההערה — עברית (סבב 67)
   ───────────────────────────────────────────────────────────────────────────
   ⛔ מה נאכף: בלוק הערה ב-`index.html` וב-`sw.js` שאין בו אף תו עברי
   ⛔ ויש בו פרוזה אנגלית. ⚠️ **שם מזהה אינו פרוזה** — באנר כמו
   שכל תוכנו «APP» או «check-js» הוא שם של דבר בקוד, ⛔ ורשימת-ההיתר מכסה
   בדיוק אותם. ⛔ ולמה זה יכול להישבר: הערה חדשה נכתבת בשפה שבה נכתב
   הקוד שהיא מסבירה, ובלי שער היא נשארת.
   ⛔ ה-`javadoc` שב-`android/` מוחרג במפורש — הוא תקן Java, ואינו בתחולה.
   ──────────────────────────────────────────────────────────────────────── */
{
  const HEB = /[֐-׿]/;
  /*  ⛔ רשימת-היתר ולא רשימת-איסור (סבב 67) — רשימת היתר נשארת נכונה
   *  כשייווסף מזהה חדש; רשימת איסור הייתה צריכה לגדול איתו. */
  const ALLOW = /^[\s*/─═|.\-]*(APP|check-[a-z-]+|sw\.js|index\.html|LOGIN|CORE|[A-Z_]{2,})[\s*/─═|.\-]*$/;
  let bad = 0, seen = 0;
  for (const { name, blocks: bs } of SCOPE) {
    if (name.startsWith('tools/')) continue;   /* ⚠️ שם מזהה בלבד — ר' הרשימה */
    for (const b of bs) {
      const t = b.text;
      if (HEB.test(t)) continue;
      const stripped = t.replace(/\/\*+|\*+\/|^\s*\/\/|^\s*\*/gm, ' ').trim();
      if (!/[A-Za-z]{3,}/.test(stripped)) continue;
      if (ALLOW.test(stripped)) continue;
      seen++; bad++;
      fail(`${name}:${b.line}: בלוק הערה באנגלית — הערות בעברית, ` +
           'מונחים טכניים ושמות מזהים באנגלית (כלל ברזל 11, סבב 67)');
    }
  }
  if (!bad) pass('שפת ההערה: אין בלוק הערה באנגלית ב-index.html וב-sw.js');
}

/*  ⛔ יציאה גם בסוף הקובץ (סבב 72) — ⚠️ עד כאן הייתה יציאה אחת בלבד,
 *  אחרי סעיף ה, ⛔ וכל הסעיפים שאחריה **דיווחו ולא הפילו**: השער הדפיס
 *  כשל והחזיר קוד יציאה אפס. ⭐ נמדד על סעיף החפיפה שנוסף בסבב הזה —
 *  הוא תפס את המוטציה, ⛔ והמוטציה בכל זאת «עברה». */
if (failures) {
  console.error(`\n❌ ${APP.app}: ${failures} סטיות מתקן ההערות (כלל ברזל 11)`);
  process.exit(1);
}
console.log(`\n✅ ${APP.app}: תקן ההערות תקין`);
